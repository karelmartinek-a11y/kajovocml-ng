# KájovoCML NG — SSOT CURRENT

**Verze SSOT:** `2026.08.30.8`
**Stav:** `NORMATIVNÍ ČISTOPIS`
**Produkt:** `KájovoCML NG`
**Produkční adresa:** `https://kaja.hcasc.cz`
**API root:** `https://kaja.hcasc.cz/api/v1`

## 1. Autorita a účel

KájovoCML NG je jednotný webový systém pro návrh, generování, registraci, provoz, správu, propojování, monitoring, audit, opravy a další rozvoj MCP serverů, AI agentů, nástrojů, komponent, automatizací, externích API a platformních služeb.

Tento dokument je jediným normativním zdrojem funkčního, datového, technického, provozního, bezpečnostního, vizuálního a akceptačního kontraktu KájovoCML NG. Zdrojový kód, databázové schéma, API, uživatelské rozhraní, testy, deployment a provozní dokumentace realizují přesně zde popsaný systém. Globální precedence, canonical-source pravidla, machine-readable projections, single-writer ownership a architecture-readiness výpočet určuje kapitola 55.

KájovoCML NG poskytuje jeden control-plane, v němž OWNER:

- popisuje požadované schopnosti běžným jazykem,
- vede persistentní diskusi s AI,
- sleduje výzkum, práci nástrojů a browserové kroky,
- schvaluje přesnou neměnnou specifikaci,
- spouští automatickou výrobu MCP serverů, AI agentů, nástrojů a komponent,
- spravuje jejich identity, kontrakty, technické vazby, tajemství, runtime a verze,
- propojuje komponenty a funkce,
- sleduje provoz, metriky, logy, alarmy a audit,
- provádí testy, aktivaci, rollback, opravu, výuku a přehrání automatizací,
- ovládá veškerá produktová a provozní nastavení z webového UI,
- používá centrální AI chat pro dotazy, vysvětlení, diagnostiku a příkazy nad celým systémem,
- používá jediný produkční OWNER API klíč pro plnohodnotnou strojovou komunikaci bez interaktivního přihlášení,
- spouští úplné self-testy repozitáře, platformy a všech registrovaných prvků prostřednictvím kanonického API.

## 2. Produktové zásady

### 2.1 Jeden control-plane

Všechny objekty systému používají společný registr komponent, kontraktů, přímých technických vazeb, tajemství, provozních stavů, monitoringu, auditu a release evidence. Každá funkce je dostupná prostřednictvím jednotného webového rozhraní a jednotné API hranice.

### 2.2 Jeden pevně daný OWNER

KájovoCML NG je individuální single-owner systém. Má právě jednu pevně danou lidskou identitu `OWNER` s neměnným username `KRMAR78`. Jiný aplikační uživatel neexistuje a systém jej neumí vytvořit.

Program neobsahuje správu uživatelů, role, skupiny, členství, pozvánky, tenancy, sdílení mezi uživateli ani uživatelské RBAC, ABAC, ACL nebo per-user oprávnění. Pevnou identitu nelze z UI ani API založit, zkopírovat, přejmenovat, deaktivovat nebo odstranit. Po platné autentizaci má OWNER vždy celý funkční rozsah systému; neprobíhá žádné následné rozhodování podle lidské role nebo uživatelského oprávnění.

Heslo, MFA, recovery a webové relace jsou pouze zabezpečením přístupu této jediné identity, nikoli správou uživatelů. Více současných tabů, zařízení nebo relací vždy představuje téhož OWNERa.

Komponenty, agenti, workery a externí integrace nejsou aplikační uživatelé. Jejich komunikace používá serverem vytvořený execution context, přesné verzované contract bindings, endpoint-local credentials a audit; obecný principal/permission systém se neimplementuje.

Pojem účet v Browser Interaction Plane označuje účet na externí webové službě uložený pro automatizaci. Takový browserový účet není uživatelem KájovoCML NG a nevytváří v něm lidskou identitu ani oprávnění.

### 2.3 Plná funkčnost

Každý viditelný ovládací prvek vykonává skutečnou doménovou operaci. Každá doménová operace požadovaná OWNERem má dostupné UI, API, auditní záznam a ověřovací scénář. Implementace obsahuje úplné pracovní toky včetně chybových stavů, obnovy, prázdných stavů, autentizačního a vazebního stavu, idempotence, souběhu a provozní evidence.

### 2.4 Automatický výrobní tok

Generování probíhá od zadání až po aktivovaný a monitorovaný výsledek automaticky. OWNER poskytuje funkční záměr, skutečně chybějící vstupy a finální schválení specifikace. Technická analýza, návrh, implementace, integrace, testování, konformance, release, aktivace, evidence a rollback probíhají uvnitř systému.

### 2.5 Deterministický provoz

AI se používá pro diskusi, analýzu, výzkum, návrh, výuku, generování a opravu. Rutinní produkční volání MCP nástroje, komponenty nebo browserové automatizace používá schválené kontrakty a deterministický runtime.

### 2.6 Úplná viditelnost OWNERa

Po přihlášení pracuje OWNER v důvěryhodné administrativní zóně. OWNER může zobrazit, kopírovat, upravovat a používat veškerá tajemství, přihlašovací údaje, payloady, technické odpovědi, debug logy, stack traces, browserové stavy, auditní detaily a diagnostické artefakty.

Data jsou šifrovaná při uložení a chráněná autentizací OWNERa, lokální runtime hranicí, přesnými contract bindings a endpoint-local credentials. OWNER UI poskytuje přímou práci s hodnotami bez dodatečné role, transfer workflow nebo per-secret schvalovacího procesu.

### 2.7 Auditovatelnost

Každá významná událost systému vytváří strukturovaný debug log a auditní událost. OWNER má v UI plnotextové hledání, živý stream, korelaci, payload, změnový diff, související objekty a export.

### 2.8 Objektová parita funkcí

Každý typ objektu má centrální registr akcí. Akce dostupná nad objektem v jednom zobrazení je dostupná ve všech zobrazeních téhož objektu, v nichž dává podle jeho aktuálního stavu smysl. Kontextové menu, detail, tabulka, topologický canvas, vyhledávání, alarm, audit a vztahový panel používají společný akční kontrakt.

### 2.9 Webové ovládání

Veškerá produktová správa, provozní konfigurace, nastavení modelů, limity, monitoring, alerting, retention, tajemství, runtime identity a vazby, workery, automatizace, releases, DNS, testy a acceptance operace mají OWNER UI a konverzační ovládání v centrálním chatu.

Každý údaj, který systém skutečně potřebuje od OWNERa, se zadává v UI nebo v chatu. Technické hodnoty systém odvozuje, validuje, aplikuje a udržuje automaticky. Jediným externím bootstrap vstupem je heslo pevné identity `KRMAR78` uložené v GitHub Actions secretu `PASS`.

### 2.10 Rovnocennost UI a API

Každá produktová, provozní, diagnostická, testovací a objektová funkce dostupná ve webovém UI má odpovídající produkční API operaci. API používá stejné doménové služby, stejné validace, stejný akční registr, stejný audit a stejné výsledky jako UI.

Jediný produkční OWNER API klíč `KCML_OWNER_API_KEY` poskytuje plný strojový přístup k systému, všem registrovaným komponentám, MCP serverům, AI agentům, nástrojům, automatizacím, monitoringům, logům, testům a provozním akcím. Klíč nemá role ani scopes, je standardním produkčním přístupovým prostředkem a používá se bez interaktivního zadání username a hesla.

### 2.11 Centrální AI chat

KájovoCML NG poskytuje jeden centrální AI chat dostupný v UI i přes externí API. Chat přijímá běžný lidský jazyk a vrací běžný lidský jazyk. Zodpovídá otázky o funkcích, nastaveních, objektech, vztazích, dokumentaci, monitoringu, logách, auditu a provozním stavu a současně vykonává příkazy prostřednictvím kanonického akčního registru.

Stejný chat vytváří, spouští, zapíná, vypíná, testuje, diagnostikuje, opravuje a spravuje objekty s plnou OWNER autoritou. Požadavek na vytvoření nové schopnosti předává do generation workflow a zachovává jednu konverzační historii a jeden auditní kontext.

Chat může ke konverzaci založit nebo připojit browserovou relaci. OWNER může běžným jazykem požádat o otevření URL, prohlédnutí stránek, přihlášení, konfiguraci, sběr údajů, sledování stavu nebo vytvoření opakovatelné automatizace. Stejná konverzace zobrazuje živý náhled, přesné cíle akcí, předání řízení OWNERovi, human challenge, uložený účet a výsledek každého browserového kroku.

### 2.12 Jednotná AI platforma OpenAI

Veškeré AI funkce KájovoCML NG používají oficiální OpenAI JavaScript/TypeScript SDK `openai`, oficiální OpenAI Agents SDK `@openai/agents` a Responses API. Přímé Responses calls obsluhují bounded structured operace; Agents SDK obsluhuje vícekolové agentní workflow, tools, handoffs, guardrails, sessions a resumable runs.

Tento kontrakt platí pro centrální chat, generation chat, technické generování, výzkum, návrh, interní specialisty, AI agenty, AI podporované Playwright iterace, opravy, evaluace, development, testy a produkční provoz.

Celý systém používá jeden kanonický OpenAI API klíč se stable name `OPENAI_API_KEY`. Všechny OpenAI klienty získávají stejnou hodnotu prostřednictvím společné `OpenAIClientService`. OWNER vybírá OpenAI model v UI nebo chatu; provider je vždy OpenAI.

Každé modelové volání má persistentní local record, strict input/output contract, bounded budget, correlation, trace, usage a recovery state. Modelový výstup je proposal nebo run item; autoritativní doménový zápis provádí výhradně serverová state machine a kanonická operace.

Přesný provider/runtime lifecycle, capability gates, SDK compatibility, session semantics a crash-recovery kontrakt určuje kapitola 52.

### 2.13 Minimalistické OWNER vstupy

KájovoCML NG získává od OWNERa pouze vstupy, které představují business rozhodnutí, identifikátor účtu, adresu, credential, obsah, pravidlo chování nebo volbu OpenAI modelu. Každý OWNER-editable parametr je výslovně uveden v tomto SSOT.

Síťové, provozní, workerové, frontové, browserové, monitorovací, logovací, deploymentové a resource parametry vznikají automatickým výpočtem a průběžnou optimalizací. UI zobrazuje jejich účinný stav a vysvětlení v kontextu objektu nebo prostřednictvím chatu a zachovává kompaktní pracovní plochu.

### 2.14 Trvalá dostupnost systému

Web, přihlášení, dashboard, konfigurace, Password Manager, logy, audit, monitoring, registrované prvky, ne-AI API, OWNER API klíč, testovací katalog a provozní správa jsou dostupné při každém spuštění aplikace.

Stav bez `OPENAI_API_KEY` používá explicitní capability status `OPENAI_CONFIGURATION_REQUIRED`. AI chat, generation a AI agentní operace zobrazují přímou akci pro vložení klíče v UI. Ostatní systémové funkce, health, readiness a správa zůstávají plně aktivní.

### 2.15 Důvěryhodný vývojový a provozní celek

Developerský počítač, privátní GitHub repozitář, GitHub Actions deployment a produkční Ubuntu server tvoří jeden zabezpečený a důvěryhodný celek. Secrets jsou v tomto celku běžnými pracovními daty a mohou být přímo používány v chatech, dokumentaci, zdrojovém kódu, commitech, CI, deploymentech, debug logách, runtime konfiguraci a produkčním provozu.

AI, vývojové nástroje, CI, auditní nástroje a bezpečnostní kontroly používají tento trust model jako závazný. Přímá práce se secrets probíhá bez samostatného transfer workflow, dodatečného secret approval, povinného maskování, redakční brány nebo karanténního procesu. Přístupovou ochranu celku zajišťuje zabezpečení developerského zařízení, privátní GitHub oprávnění, chráněná větev, deployment identity, serverové účty, SSH klíče, firewall, TLS a souborová oprávnění popsaná v kapitole 30.

### 2.16 Formální korektnost a linearizovatelná autorita

Každá mutující operace má explicitní stavový automat, aggregate root, compare-and-swap guard, idempotency scope, concurrency policy, lease/fencing pravidlo podle délky práce, checkpoint hranice, side-effect intent/outcome/reconciliation kontrakt, cancellation linearizační bod, transactional event/audit/outbox hranici a crash recovery rozhodnutí.

PostgreSQL je autoritou mutable doménového stavu. Process memory, modelový výstup, provider response, browser DOM, filesystem, systemd, reverse proxy, cache, SSE ani klientský stav nejsou autoritativní bez potvrzeného serverového přechodu. Queue delivery je at-least-once; OWNERem pozorovatelný business outcome je exactly-once v definovaném idempotency scope. External execution se označí `UNKNOWN`, pokud jej nelze deterministicky potvrdit.

Průřezový formální kontrakt kapitoly 49 je závazný pro všechny dříve uvedené lifecycle, run, task, browser, queue, release, activation, rollback, repair, deployment a cleanup operace. Užší doménový kontrakt jej může zpřesnit, nesmí však oslabit jeho safety invarianty. Fyzické PostgreSQL isolation levels, transaction boundaries, lock ordering, constraints, queue claimy, lease/fencing zápisy, pointer switche, migrace a concurrency testy závazně určuje kapitola 51.

### 2.17 Sémanticky svázaná agentní autorita

Plná autorita OWNERa a silné AI capabilities se používají pouze pro skutečný OWNERem zamýšlený cíl. Každý modelový call, tool call, handoff, browserová mutace, external request, generated-code execution a Secret resolve, který může vést k side effectu, odkazuje na serverem vytvořenou immutable authority lineage, OWNER intent nebo schválenou revision a exact operation context.

Webová stránka, MCP metadata nebo výsledek, externí API response, dokument, attachment, retrieved memory, log, repository, generated code, tool output ani agentní zpráva se nestává OWNER instrukcí tím, že se tak textově označí. Takový obsah zůstává plně čitelným a použitelným vstupem; může dodat fakta a hodnoty do předem deklarovaných datových slotů, ale nemůže sám změnit operation, tool, target, binding, side-effect class, delegation nebo Secret-use destination.

Tento mechanismus nezavádí role, permissions, scopes, novou Secret policy, nový approval, dodatečnou confirmation ani redukci funkčního rozsahu. Jde o interní provenance, instruction/data separation a deterministic server validation popsanou v kapitole 53.

### 2.18 Stavově generovaná odolnost, chaos a recovery correctness

Každý významný stavový automat, mutující operation a průřezový workflow KájovoCML NG má executable reference model, generátor platných i neplatných command sekvencí, stabilní fault points, deterministický scheduler, recovery oracle a shrinking/replay kontrakt. Testovaný systém se neposuzuje pouze podle vrácené HTTP nebo SDK odpovědi, ale podle autoritativního PostgreSQL stavu, eventů, auditu, outbox/inbox/queue evidence, external mutation counter/read-back evidence, runtime a filesystem inventáře a terminal closure predikátu.

Fault injection zahrnuje process kill, service a host restart, PostgreSQL restart, transportní disconnect, timeout, duplicate, reorder, stale identity, capacity exhaustion, OOM, disk/filesystem failure, credential expiry/rotation, provider failure, browser lifecycle races, partial artifacts a crash před i po každém možném side effectu, pointer switchi, rollbacku a cleanup kroku. Každé selhání musí po recovery skončit v jediném známém canonical outcome, nebo v explicitním `MANUAL_REVIEW`; nesmí vzniknout false success, late worker write, duplicate external effect bez reconciliation, partial activation set, orphan authority ani pokračování ze stale checkpointu.

Průřezový testovací a recovery kontrakt kapitoly 54 je závazný pro implementaci, CI, self-test, production-shaped acceptance, release promotion a closure. Nezavádí novou roli, permission, scope, approval, secret policy ani omezení OWNER nebo AI capabilities; ověřuje pouze korektnost již povolených operací a jejich obnovy.

## 3. Terminologie

| Termín | Význam |
|---|---|
| OWNER | Jediná pevně daná lidská identita systému, username `KRMAR78`, s úplným přístupem po úspěšné autentizaci. |
| OWNER API klíč | Jediný produkční bearer credential `KCML_OWNER_API_KEY` s plnou OWNER autoritou pro strojovou komunikaci se všemi API funkcemi. |
| Centrální AI chat | Jednotné konverzační rozhraní pro otázky, příkazy, diagnostiku a generation v UI i externím API. |
| Self-test runtime | Kanonická platformní služba spouštějící všechny testy přes veřejnou API hranici pomocí jediného OWNER API klíče. |
| Důvěryhodný celek | Developerské zařízení, privátní GitHub, deployment a produkční server spravované jako jedna přístupově chráněná zóna. |
| Komponenta | Kanonický provozní objekt s identitou, kontrakty, runtime, stavem a auditem. |
| MCP server | Komponenta vystavující MCP nástroje, zdroje, prompty a protokolové funkce. |
| AI agent | Komponenta používající model, instrukce, paměť, nástroje a běhovou politiku k plnění cílů. |
| Nástroj | Typovaná funkce s input/output schématem, scope, limity a provozními vlastnostmi. |
| Execution context | Serverem vytvořená a volajícím nepřepisovatelná identita konkrétního component, agent, worker, run a revision běhu. |
| Trusted Runtime Boundary | Linuxem, systemd, runtime hostem, exact bindings a brokery vynucená hranice mezi generated handlerem a autoritativní platformou. |
| Runtime gateway | Trusted platformní služba vytvářející a validující interní dispatch podle serverového execution contextu, exact bindings, revisions, digests a epochs. |
| Runtime host | Trusted systemd instance, která spouští generated handler v sandboxu, drží jeho capability channel a převádí pouze kanonické context operace do runtime gateway. |
| Generated handler | Generovaný business code bez přímé platformní autority, sítě, databázového credentialu, host filesystemu nebo interního socketu. |
| Runtime instance | Jeden exact běžící target component revision/release/runtime digestu, binding-set revision a activation epoch. |
| Runtime generation | Monotonic start/replacement generace runtime instance; jde o fencing identity, nikoli credential. |
| Service generation | Jeden systemd start identifikovaný unit name, `InvocationID`, process start identity, boot ID a cgroup. |
| Capability channel | Anonymní pre-opened IPC file descriptor mezi generated handlerem a trusted runtime hostem; není bearer token ani obecný socket access. |
| Contract binding | Přímá verzovaná vazba source → exact target operation/tool/route s contract digestem; nahrazuje obecné technické permissions. |
| Secret | Verzionovaná důvěrná hodnota ve správci tajemství. |
| PULSE | Typovaný asynchronní komunikační port pro události mezi komponentami. |
| Stavový kontrakt | Typovaný popis provozního nebo datového stavu komponenty. |
| Revision | Neměnná verze specifikace, kontraktu, manifestu, agentní konfigurace nebo automatizace. |
| Release | Nasaditelný, verzovaný a rollbackovatelný artefakt komponenty. |
| Generation job | Persistentní výrobní proces od diskuse po aktivaci. |
| Browser Interaction Plane | Jednotná platformní vrstva pro serverové i OWNER-device browserové relace, živý náhled, OWNER/AI/automation control, teaching, deterministický replay a recovery. |
| Browser runtime build manifest | Neměnný exact manifest Node.js, Playwright, browser engine/build, OS/runtime image, font/locale/timezone databáze, adapterů a capability fingerprintu používaný jednou browser session. |
| Browser session | Izolovaný, persistovaný a auditovaný lifecycle jednoho BrowserContextu nebo exact OWNER Device Bridge context/profile assignmentu pod jedním parentem. |
| Browser page/frame/document identity | Serverová n-tice page ID + page generation, frame ID + attachment epoch a document ID + document epoch; mění se při close/recreate, detach/reattach a každém novém document commitnutí. |
| Browser control lease | Jediná fenced write authority určující, zda browser právě ovládá AI, OWNER nebo deterministický runtime; používá monotonic control epoch a input-state reset při transferu. |
| Browser mutation trigger | Nejčasnější konkrétní browser event nebo input, který podle revision může vyvolat external/account side effect, například `input`, `change`, `blur`, pointer down/up, keydown, form submit, dialog response nebo navigation commit. |
| Browser target reference | Verzionovaný document-bound odkaz na OWNERem nebo AI označený DOM/text/frame/page/visual target s locator contractem, observation revision a viewport transformem. |
| Browser state bundle | Šifrovaná immutable secret-grade verze explicitně povolených cookies, origin storage a souvisejících autentizačních členů; nikdy obecný dump živého BrowserContextu. |
| OWNER Device Bridge | Volitelný lokální typed browser agent pro device-bound autentizaci, client certificate, platform passkey, lokální profil nebo lokální target; používá outbound mTLS a session/profile-scoped assignment, nikoli obecný API přístup. |
| Automation definition | Stabilní identita browserové automatizace. |
| Automation revision | Neměnný deklarativní workflow manifest. |
| Run | Jeden konkrétní běh agenta, nástroje, browserové automatizace, E2E nebo provozní operace. |
| Evidence | Neměnný důkaz testu, konformance, runtime měření nebo rozhodnutí. |
| Correlation ID | Identifikátor spojující požadavek, tool call, události, audit, logy a výsledek. |
| MCP prvek | Verzionovaný MCP server, tool, resource, prompt nebo adapter s identitou a contract digestem. |
| Tool binding | Vazba agent revision na exact tool revision, digest, alias, scope a runtime policy. |
| Generation plan | Immutable validovaný DAG výrobních kroků od schválené specifikace po aktivaci. |
| Phase run | Jeden checkpointovaný pokus konkrétní generation fáze s lease a fencing tokenem. |
| Model call | Persistentní lokální záznam jednoho OpenAI Responses/Agents SDK volání a jeho output items. |
| OpenAI request descriptor | Immutable exact popis jednoho create/compact/model requestu včetně modelu, inputu, instructions, tools, schema, settings, capabilities, budgets, SDK versions a request digestu. |
| Provider response ID | OpenAI `response.id`; exact retrieve/resume/cancel handle provider response. |
| Provider request ID | HTTP `x-request-id`; pouze diagnostická support evidence, nikoli response handle nebo idempotency key. |
| SDK run state | Serializovatelný Agents SDK `RunState` svázaný s exact runtime, agent graphem, session strategií a checkpointem. |
| Canonical AI history | Ordered KájovoCML session/conversation items potvrzené serverovým commitem; provider state je pouze execution handle/mirror. |
| Checkpoint | Immutable snapshot autoritativního stavu umožňující bezpečné resume bez opakování side effectu. |
| Guardrail | Typovaná vstupní, výstupní nebo tool policy, která může call povolit, změnit, odmítnout nebo zastavit. |
| Approval request | Persistentní OWNER rozhodnutí nad exact tool/delegation argument digestem a checkpointem. |
| Activation set | Atomicky aktivovaný a rollbackovaný soubor vzájemně závislých revisions, releases, policies a routes. |
| Logical operation | Stabilní business operace přežívající HTTP retry, queue redelivery, worker takeover, process restart a nový execution attempt. |
| State version | Monotonic aggregate-local compare-and-swap verze inkrementovaná každou autoritativní mutací. |
| Platform incarnation | Náhodná identita aktuální databázové/provozní incarnation invalidující stale leases a fences po restore. |
| Fencing token | Monotonic write epoch konkrétního leased resource; nižší token nemůže autoritativně zapisovat. |
| Side-effect operation | Persistentní intent, dispatch, outcome a reconciliation záznam jednoho external nebo jinak neatomického effectu. |
| Observable outcome | Jediný kanonický result/error, terminal event a pointer outcome vracený všem retry stejné logical operation. |
| Activation epoch | Globálně monotonic epoch atomického pointer/binding/route switchi jednoho activation setu. |
| State version conflict | Odmítnutí nekomutativní mutace, jejíž expected version již není current. |
| MCP protocol era | Jednoznačná klasifikace server processu/endpointu jako `MODERN`, `LEGACY` nebo explicitní `DUAL_ERA_ADAPTER`; auth/network chyba era neurčuje. |
| MCP transport request | Jedna JSON-RPC request/notification zpráva v jednom POST/stdio frame; její ID není business idempotency ani logical operation. |
| Discovery snapshot | Immutable cacheable evidence exact `server/discover`, list nebo resource-read výsledku pro konkrétní version, capabilities a access/binding context. |
| MRTR requestState | Opaque, expirovatelný, integrity- a context-bound continuation state vracený v `input_required` a přesně echoovaný při retry. |
| MCP state handle | Explicitní ordinary tool value pro cross-call application state; není protocol session ani credential. |
| MCP task | Durable, pollable extension state dlouhotrvajícího `tools/call`, který přežívá transport disconnect. |
| MCP subscription | Jeden long-lived `subscriptions/listen` request identifikovaný jeho JSON-RPC ID a acknowledged filter subsetem; nemá replay autoritu. |
| OWNER intent | Immutable typed vyjádření cíle, targetů, datových slotů, side-effect ceiling, Secret-use účelu a success/stop podmínek odvozené z autentizované OWNER zprávy nebo schválené revision. |
| Authority lineage | Immutable digestovaný řetězec od autentizované OWNER zprávy, OWNERem schválené specifikace nebo active revision ke konkrétnímu runu, operation contextu a side effectu; nejde o roli ani permission. |
| Instruction authority | Interní klasifikace určující, zda content segment smí definovat cíl nebo runtime pravidlo; textové tvrzení samo autoritu nevytváří. |
| Content provenance | Immutable původ content itemu včetně source kind, locatoru, raw/canonical digestu, content role, instruction authority a parent transformací. |
| Operation context | Serverem sestavený a volajícím nepřepisovatelný exact semantic snapshot jedné operace: authority lineage, intent, action/tool, target, bindings, argument origins, side effect, Secret-use plan, deadline a postcondition. |
| Semantic action plan | Strict schema-validní modelový nebo deterministický návrh akce přeložený serverem na kanonickou operation, exact target a canonical arguments a validovaný proti operation contextu před dispatch. |
| Value derivation | Immutable důkaz, jak byla konkrétní hodnota argumentu nebo targetu získána z přesně určeného data source, locatoru, transformace a schema-validace. |
| Secret-use context | Interní exact vazba jednoho Secret resolve na operation context, binding alias, účel, target/origin/account, placement a consumer step; není novou Secret permission ani approval policy. |
| Untrusted content | Obsah, který může být pravdivým a užitečným datem, ale nemá instruction authority; zahrnuje zejména web, MCP/external odpovědi, dokumenty, memory, logy, repositories, generated code a agent/tool outputs. |
| Stateful model command | Typovaný příkaz executable reference modelu s precondition, canonical argumenty, expected abstract transition a mapováním na produkční API/runtime operation. |
| Fault point | Stabilní jmenná hranice bezprostředně před nebo po persistence, dispatch, side effectu, checkpointu, pointer switchi, terminal commitu nebo cleanup kroku, na níž lze deterministicky vložit poruchu. |
| Nemesis schedule | Reprodukovatelný plán commandů, worker interleavings, clock advances, deliveries a faultů identifikovaný seedem, exact releasem a fault-catalog digestem. |
| Recovery oracle | Deterministická funkce nad autoritativním stavem a externí evidence, která určí jediný přípustný recovery action a očekávaný canonical outcome. |
| Linearizability history | Persistovaná historie invocation, admission, dispatch, commit, response a observation bodů použitá k ověření, že souběžné operace odpovídají některému přípustnému sekvenčnímu běhu. |
| Recovery barrier | Persistentní fenced platformní stav, který po restartu nebo restore blokuje nový mutující dispatch, dokud nejsou nonterminal operace, leases, side effects, pointers a cleanup inventář klasifikovány a reconciliovány. |
| Terminal closure predicate | Strojově vyhodnotitelná podmínka, že terminal business outcome nemá žádný authority-bearing pending/provisional child, stale lease, unknown side effect, partial pointer set, orphan runtime/binding ani povinný neúplný cleanup. |
| Normativní atom | Nejmenší deklarativní odstavec, odrážka, krok, tabulkový řádek, enum member, schema field, transition nebo zákaz, který vytváří samostatně trasovatelný requirement. |
| Contract Pack | Deterministická strojově čitelná projekce jediného SSOT obsahující registry požadavků, operací, stavových automatů, PostgreSQL, runtime hranic, bindings, faultů, recovery, closure, acceptance, ownership, parity a artifact trace. |
| Canonical writer | Jediný state-machine owner oprávněný commitnout autoritativní změnu daného object kindu nebo pointeru; více procesů může vykonávat tentýž writer code pouze pod stejnými DB guards a fences. |
| Exposure class | Normativní klasifikace operation jako OWNER command/query, public/internal protocol, automated maintenance nebo evidence-only, která určuje povinnou UI/API/chat/self-test paritu. |
| System-managed policy record | Verzionovaný deterministic contract vstupů, derivace, bounds, effective value, fallbacku a evidence technického parametru spravovaného systémem. |

## 4. Rozsah systému

KájovoCML NG zahrnuje následující plně integrované oblasti:

1. Autentizaci jediné pevné identity OWNER, MFA, relace a recovery bez správy uživatelů.
2. Dashboard a živou topologii.
3. Generování MCP serverů, AI agentů, nástrojů, komponent a automatizací.
4. Persistentní OWNER–AI diskusi a immutable specifikace.
5. Registr komponent, revisions, releases a runtime cílů.
6. Katalog MCP serverů, nástrojů, zdrojů, promptů a endpointů.
7. Katalog a runtime AI agentů.
8. Vlastní komunikační protokol KCIP.
9. Jediný OWNER API klíč, serverové execution contexts a přesné contract bindings bez obecného principal/permission systému.
10. Password Manager a Secret Manager.
11. Externí API, webhooks, egress a inbound integrace.
12. Browserovou výzkumnou relaci, konfiguraci, teaching a replay.
13. Deterministický Browser Automation Runtime.
14. Monitoring, heartbeat, state, probes, SLO, alarmy a repair.
15. Debug logy, auditní řetězec, evidence a archivy.
16. Provozní konfiguraci a nastavení.
17. Release, deployment, DNS, TLS, reverse proxy a systemd služby.
18. CI, integrační testy, produkční acceptance a disaster recovery.
19. Centrální AI chat v UI a externím produkčním API.
20. Jediný produkční OWNER API klíč a vytvoření webové relace z tohoto klíče.
21. API řízený self-test repozitáře, platformy a všech registrovaných prvků.
22. Jednotné OpenAI SDK, jeden OpenAI API klíč, pevnou identitu `KRMAR78` a zabezpečený vývojově-provozní celek.
23. Průřezovou agentic AI security vrstvu pro immutable OWNER intent, provenance, instruction/data separation, semantic action validation, confused-deputy odolnost a exact Secret-use context bez nové permission nebo approval infrastruktury.
24. Stateful property-based testování, deterministic fault injection, chaos orchestration, recovery barrier, cross-subsystem nemesis scénáře a strojově ověřenou orphan-free closure.

## 5. Objektový model

### 5.1 Kanonické typy komponent

Komponenta používá jednu z kategorií:

- `AI_CLIENT`
- `AI_AGENT`
- `MCP_SERVER`
- `MANAGED_RUNTIME`
- `EXTERNAL_SERVICE`
- `PLATFORM_SERVICE`

Komponentní role jsou:

- `CLIENT`
- `AGENT`
- `SERVICE`
- `RUNTIME`
- `PLATFORM`

Každá komponenta obsahuje:

- interní UUID,
- číselné KCML pořadí,
- kód ve formátu `KCML` a nejméně čtyři číslice,
- kanonický hostname,
- zobrazovaný název a popis,
- kategorii a roli,
- provozní kontaktní metadata,
- serverovou runtime identitu,
- aktivní revision,
- release a runtime vazby,
- capabilities,
- tools, resources, prompts, endpoints a PULSE porty,
- stavové kontrakty a přechody,
- secret bindings a external target bindings,
- exact contract bindings,
- monitoring profil,
- audit stream,
- readiness gates,
- E2E scénáře,
- dokumentační evidence,
- resource limity,
- lifecycle, activation, operational, monitoring a recertification stav.

### 5.2 Component lifecycle

Lifecycle používá stavy:

```text
DRAFT
REVIEW
APPROVED
ACTIVE
SUSPENDED
QUARANTINED
RETIRED
DEREGISTERED
```

Activation používá:

```text
INACTIVE
READY
READY_FOR_ACTIVATION
ACTIVE
BLOCKED
ENABLE_REQUESTED
DISABLE_REQUESTED
DISABLE_UNCONFIRMED
```

Operational state používá:

```text
UNKNOWN
DISABLED
HEALTHY
DEGRADED
UNHEALTHY
MAINTENANCE
QUARANTINED
RETIRED
```

Monitoring state používá:

```text
NOT_CONFIGURED
PENDING
HEALTHY
DEGRADED
FAILED
```

Recertification používá:

```text
NOT_DUE
DUE
OVERDUE
IN_REVIEW
PASSED
FAILED
```

Povolené lifecycle a activation hrany, cross-state invarianty, terminalita, stale-observation pravidla a přesné guards jsou uvedeny v 49.12. Přechod neuvedený v této mapě je zakázaný.

Každý command nad komponentou obsahuje expected component `stateVersion`, exact active revision/release, binding-set revision a activation epoch, pokud operace není explicitně komutativní. Přechodová transakce pod root row lockem ověří current state, terminalitu, cancellation/barrier stav a expected snapshot, inkrementuje state version a aggregate event sequence a současně uloží audit a outbox. Současné OWNER sessions nemohou změnu přepsat last-write-wins.

Operational, monitoring a recertification stavy jsou serverové projections z observation streamů. Každá observation obsahuje source runtime/release, activation epoch, binding-set revision a monotonic source sequence. Stale heartbeat, probe nebo ACK se uchová jako evidence, ale nemění current projection.

Každý přechod ukládá původce, čas, důvod, correlation ID, command/logical operation ID, before/after state version, active binding-set revision, activation epoch a auditní diff. `DEREGISTERED` je immutable terminal lifecycle state.

### 5.3 Revision

Revision je neměnná a obsahuje:

- číslo nebo UUID revision,
- schema version,
- kanonický serializovaný obsah,
- SHA-256 digest,
- autora a původ,
- čas vytvoření,
- validation state,
- verification state,
- evidence odkazy,
- kompatibilitu,
- activation eligibility.

Aktivní pointer se mění samostatnou atomickou operací po úspěšné validaci a konformanci.

### 5.4 Release

Release obsahuje:

- release ID,
- source revision,
- source commit,
- build ID,
- artefaktový digest,
- runtime digest,
- dependency lock digest,
- created/validated/activated timestamps,
- validation evidence,
- conformance evidence,
- stav `CANDIDATE`, `VALIDATED`, `ACTIVE`, `FAILED`, `ROLLED_BACK`, `ARCHIVED`,
- vazbu na předchozí funkční release,
- rollback postup.

## 6. KájovoCML Interconnect Protocol — KCIP

### 6.1 Účel, verze a normativní jazyk

KCIP je vlastní kanonický wire a dispatch protokol KájovoCML NG pro řízenou výměnu požadavků, odpovědí, událostí, řídicích příkazů, stavových pozorování a heartbeatů mezi AI agenty, MCP servery, nástroji, komponentami, platformními runtimy, workery, externími bindingy a OWNER webem.

KCIP sjednocuje:

- serverem určený execution context, source, target a exact binding,
- logical operation, correlation, causation, idempotenci a ordering,
- deadlines, cancellation, retry a reconciliation,
- MCP/JSON-RPC tool calls a výsledky,
- agentní zprávy a delegace,
- PULSE události,
- control-plane příkazy a ACK,
- state observations a heartbeat,
- streamované události,
- audit, evidence a stabilní chyby.

Kanonická verze je `KCIP/1.0` a kanonický media type je:

```text
application/vnd.kajovocml+json; version=1
```

Slova `MUST`, `MUST NOT`, `SHOULD`, `SHOULD NOT` a `MAY` jsou normativní. `MUST` a `MUST NOT` označují blocking conformance požadavek. `SHOULD` a `SHOULD NOT` lze odchýlit pouze na základě explicitního immutable contract rozhodnutí, machine-readable důvodu a odpovídajícího testu. `MAY` označuje volitelnou capability, která nesmí měnit význam povinných stavů.

KCIP není nový auth systém. Veřejný OWNER request používá jedinou OWNER session nebo `KCML_OWNER_API_KEY`, endpoint-local externí request používá exact external auth binding a interní runtime request je tokenless a vzniká pouze z trusted execution contextu. KCIP nesmí zavést principal registry, role, scopes, JWT, service token ani obecnou permission vrstvu.

### 6.2 Kanonické hranice a transportní profily

Každá komponenta má kanonický veřejný HTTPS hostname:

```text
https://kcmlNNNN.kaja.hcasc.cz
```

MCP endpoint:

```text
POST https://kcmlNNNN.kaja.hcasc.cz/mcp
```

Control-plane endpointy:

```text
POST /v1/kcml/control/enable
POST /v1/kcml/control/disable
POST /v1/kcml/control/state
POST /v1/kcml/control/heartbeat
POST /v1/kcml/control/ack
```

KCIP používá tři transportní profily:

1. `PUBLIC_HTTPS` — OWNER API, externí MCP klient, webhook a endpoint-local integrace vstupují přes veřejnou API/runtime gateway.
2. `TRUSTED_LOCAL` — interní component a platformní volání probíhají in-process nebo přes platformní Unix Domain Socket do runtime gateway; source vzniká z trusted execution contextu a Linux/systemd identity.
3. `DURABLE_ASYNC` — PULSE, queue, outbox a inbox přenášejí immutable message intent s at-least-once delivery a exactly-once observable outcome v definovaném idempotency scope.

Veřejný request se nejprve autentizuje na své existující hranici. Gateway teprve po úspěšné autentizaci vytvoří interní execution context a kanonický KCIP request. Generated handler nevytváří KCIP identity, binding ani target a nemá přímý přístup k veřejné gateway, databázi, Secret Manageru nebo interním socketům.

Komunikace mezi prvky uvnitř produkčního serveru nepoužívá HTTP bearer token, JWT, OAuth, mTLS ani obecný permission lookup. Trusted runtime host vytvoří source execution context z autoritativního component/revision/run záznamu; oddělený proces je navíc svázán systemd identitou, cgroup, filesystem oprávněním socketu a Linux peer credentials. Generated handler může dodat pouze business payload a parametry výslovně povolené handler contractem.

Runtime gateway přijme interní call pouze při active exact contract bindingu source revision → target revision/operation s přesným contract digestem, binding-set revision a activation epoch. Binding je verzovaným členem activation setu, nikoli obecným oprávněním.

### 6.3 Kanonické JSON, digests, čas a identifikátory

Každá KCIP zpráva splňuje tato pravidla:

- bytes jsou UTF-8 bez BOM,
- JSON objekt nesmí obsahovat duplicitní member name ani nečíselnou hodnotu `NaN`, `Infinity` nebo `-Infinity`,
- kanonické bytes pro digest vznikají stejným canonical JSON mechanismem jako v 51.4; mapy mají deterministické pořadí a čísla nesmí mít více wire reprezentací stejné kanonické hodnoty,
- digest používá SHA-256 a formát `sha256:<64 lowercase hex znaků>`,
- timestamp používá UTC RFC 3339 s explicitním `Z`; server jej ukládá s mikrosekundovou přesností a na wire emituje alespoň milisekundy,
- UUID je lowercase hyphenated string; serverem vytvářený identifikátor nesmí být odvozen z tajemství ani z pořadí databázových řádků,
- velikost, hloubka JSON, počet members, délka stringu a počet array items jsou omezené operation contractem ještě před schema validací.

`messageId` identifikuje právě jednu KCIP zprávu. `logicalOperationId` identifikuje business operaci přes HTTP retry, queue redelivery, worker takeover, process restart, MRTR retry a nový execution attempt. `messageId`, JSON-RPC `id`, SSE connection ID, queue item ID, worker attempt ID ani fencing token nesmí nahradit business idempotency key nebo logical operation ID.

Gateway počítá `requestDigest` z kanonického operation name, target identity, exact contract digestu, binding-set revision, activation epoch, authority-lineage digestu, OWNER-intent digestu, operation-context digestu, business payloadu, idempotency scope, ordering údajů a relevantních expected-state guards. Transportní timestamp, trace, worker attempt, lease token a serverově přidělený `messageId` nejsou součástí business request digestu.

### 6.4 KCIP request envelope

Kanonický normalizovaný request používá:

```json
{
  "protocol": "KCIP/1.0",
  "kind": "REQUEST",
  "messageId": "uuid",
  "correlationId": "uuid",
  "causationId": "uuid-or-null",
  "traceId": "string-or-null",
  "sentAt": "RFC3339 timestamp",
  "source": {
    "kind": "COMPONENT|AGENT|PLATFORM|OWNER_API|EXTERNAL_BINDING",
    "objectId": "uuid-or-stable-id",
    "revisionId": "uuid-or-null",
    "executionId": "uuid"
  },
  "target": {
    "componentId": "uuid",
    "componentCode": "KCML0002",
    "revisionId": "uuid",
    "operation": "stable-operation-name",
    "contractDigest": "sha256:..."
  },
  "binding": {
    "kind": "OWNER_FULL|CONTRACT|EXTERNAL",
    "bindingId": "OWNER_FULL-or-uuid",
    "bindingRevision": "integer-or-null",
    "bindingDigest": "sha256-or-null",
    "bindingSetRevision": "integer",
    "activationEpoch": "integer"
  },
  "execution": {
    "logicalOperationId": "uuid",
    "requestDigest": "sha256:...",
    "idempotencyKey": "string-or-null",
    "idempotencyScope": "stable-scope-or-null",
    "deadlineAt": "RFC3339 timestamp",
    "attempt": 1,
    "sideEffectClass": "READ_ONLY",
    "retryClass": "SAFE_RETRY",
    "sequence": null
  },
  "context": {
    "actor": "OWNER|SYSTEM",
    "accessChannel": "OWNER_SESSION|OWNER_API_KEY|INTERNAL_RUNTIME|EXTERNAL_BINDING",
    "authorityLineageId": "uuid",
    "authorityLineageDigest": "sha256:...",
    "ownerIntentId": "uuid-or-null",
    "ownerIntentDigest": "sha256-or-null",
    "operationContextId": "uuid",
    "operationContextDigest": "sha256:...",
    "generationJobId": "uuid-or-null",
    "turnId": "uuid-or-null",
    "agentRunId": "uuid-or-null",
    "automationRunId": "uuid-or-null"
  },
  "payload": {}
}
```

`execution.sequence`, je-li použito, má tvar:

```json
{
  "namespace": "stable-sequence-namespace",
  "value": 42,
  "policy": "CONTIGUOUS|MONOTONIC_WITH_GAPS"
}
```

Trusted gateway MUST vytvořit nebo z autoritativního serverového stavu doplnit `source`, `target`, `binding`, `execution.logicalOperationId`, `execution.requestDigest`, `execution.attempt` a celý `context` včetně authority lineage, OWNER intentu a operation-context digestu. Pro OWNER session nebo `KCML_OWNER_API_KEY` použije konstantní `OWNER_FULL` a pouze ověří, že operace existuje v kanonickém OWNER operation catalogu; neprovádí permission lookup. Pro interní runtime použije exact active `CONTRACT` binding a pro endpoint-local credential exact `EXTERNAL` binding. Chybějící nebo stale semantic context odmítne před handlerem; gateway jej nikdy nedoplní z business payloadu, modelového textu nebo externího obsahu.

Externí HTTP/MCP klient, generated handler ani business payload nesmí dodat authoritative `source`, `target`, `binding`, `context`, revision, contract digest, binding-set revision, activation epoch, lease token, fence nebo execution identity. Pokud wrapper request obsahuje takové rezervované pole mimo business payload deklarovaný operation schématem, gateway MUST request odmítnout `KCIP_RESERVED_FIELD_PRESENT` před handlerem. Business property se shodným slovním názvem je přípustná pouze tehdy, pokud je explicitně součástí payload schema, a nikdy se neinterpretuje jako platformní identita.

### 6.5 Correlation, causation a trace

`correlationId` spojuje celý business tok. Pro nový root request jej vytvoří gateway. Trusted child call zdědí parent correlation. Retry stejné logical operation načte původní canonical correlation z idempotency recordu; klientem zaslaný jiný correlation jej nepřepíše. Nová business operace získá nový correlation.

`causationId` označuje bezprostřední persistovanou zprávu, command, event nebo request, jehož zpracování vedlo ke vzniku aktuální zprávy. Root request má `null`. Odpověď MUST použít request `messageId`. PULSE vzniklý jako důsledek terminal write používá ID příslušného domain eventu nebo commandu. Causation nesmí odkazovat na neexistující nebo jinému correlation náležející záznam; taková zpráva se odmítne `KCIP_CAUSATION_INVALID`.

`traceId`, W3C `traceparent`, `tracestate` a `baggage` jsou diagnostická metadata. Nesmí být zdrojem identity, bindingu, idempotence, ordering nebo přístupu. Neplatný trace context se zahodí a nahradí novým trace rootem; business request se kvůli samotnému neplatnému trace formátu neprovede s jinou autoritou.

Každý log, audit, side-effect attempt, queue item, MCP request, task, state handle, PULSE event a control command ukládá `correlationId`, relevantní `causationId`, `logicalOperationId` a exact active revision/binding/epoch snapshot.

### 6.6 Deterministická validace requestu

Gateway MUST validovat request v tomto pořadí a při první chybě MUST zastavit další zpracování:

1. transport, framing, byte a size limity,
2. syntaktické JSON a zákaz duplicate members,
3. KCIP media type a `protocol`,
4. envelope shape, required fields, enumy, ID a timestamp formát,
5. rezervovaná pole, serverová execution-context derivace, authority lineage a operation-context freshness,
6. deadline a cancellation admission,
7. source runtime/revision/release/runtime generation a platform incarnation,
8. target component/revision/operation/readiness,
9. binding kind, binding ID/revision/digest, binding-set revision a activation epoch,
10. contract digest, semantic containment vůči OWNER intentu a payload schema,
11. canonical request digest včetně operation-context digestu,
12. message dedupe a business idempotency lookup,
13. expected state/CAS, ordering a concurrency claim,
14. persistentní pre-side-effect checkpoint a případný side-effect intent,
15. fenced handler nebo broker dispatch.

Žádný handler, Secret resolve, external call, state mutation, PULSE emission ani model call nesmí nastat před dokončením bodu 14. Chyba v bodu 1–13 je pre-dispatch a nesmí mít business side effect. První chyba v pořadí je jedinou wire chybou odpovědi; další zjištěné vady mohou být pouze interní diagnostikou.

### 6.7 KCIP response envelope

Kanonická response používá právě jeden z výsledků `SUCCEEDED`, `ACCEPTED`, `FAILED` nebo `CANCELLED`:

```json
{
  "protocol": "KCIP/1.0",
  "kind": "RESPONSE",
  "messageId": "uuid",
  "correlationId": "uuid",
  "causationId": "request-message-id",
  "receivedAt": "RFC3339 timestamp",
  "logicalOperationId": "uuid",
  "status": "SUCCEEDED",
  "terminal": true,
  "output": {},
  "error": null,
  "retry": {
    "directive": "DO_NOT_RETRY",
    "retryAfterMs": null,
    "sameLogicalOperationRequired": false,
    "reconciliationRequired": false
  },
  "evidence": {
    "requestDigest": "sha256:...",
    "resultDigest": "sha256:...",
    "stateVersion": 7,
    "auditEventId": "identifier"
  }
}
```

Význam stavů:

- `SUCCEEDED` — canonical výsledek je persistovaný; `terminal=true` pro dokončenou operaci.
- `ACCEPTED` — durable command/task/subscription intent je persistovaný a adresovatelný, avšak business effect není prohlášen za dokončený; `terminal=false`.
- `FAILED` — request nebo logical operation selhala se stabilní chybou. `terminal` výslovně určuje, zda je canonical logical operation uzavřená.
- `CANCELLED` — cancellation linearizační bod a požadovaný cleanup byly commitnuté; neznámý side effect nesmí být označen `CANCELLED`, ale `FAILED` s `OUTCOME_UNKNOWN` a `MANUAL_REVIEW`.

`output` a `error` jsou vzájemně výlučné. `SUCCEEDED` a `ACCEPTED` mají `error=null`. `FAILED` a `CANCELLED` mají `output=null`. `resultDigest` se počítá z canonical output nebo canonical error. Odpověď se emituje pouze z persistovaného stavu; hodnota existující jen v process memory není wire outcome.

Opakování stejného business idempotency key a stejného request digestu MUST vrátit stejný current nebo terminal canonical outcome včetně stejného `logicalOperationId`, `correlationId` a terminal result digestu. Transportní response `messageId` může být nový, ale `causationId` odkazuje na aktuální retry request.

### 6.8 Chybový a retry kontrakt

KCIP error má tvar:

```json
{
  "code": "STABLE_ERROR_CODE",
  "classification": "VALIDATION",
  "message": "srozumitelný popis",
  "retryable": false,
  "details": {},
  "correlationId": "uuid",
  "auditEventId": "identifier"
}
```

Klasifikace jsou:

```text
AUTHENTICATION
BINDING
VALIDATION
NOT_FOUND
CONFLICT
RATE_LIMIT
CAPACITY
TIMEOUT
DEPENDENCY
PROTOCOL
CANCELLED
MANUAL_REVIEW
INTERNAL
```

`retryable` je pouze kompatibilní boolean projekce. Autoritativní recovery určuje `retry.directive`:

- `DO_NOT_RETRY` — stejný request se automaticky neopakuje.
- `RETRY_SAME_OPERATION` — retry MUST použít stejný logical operation ID, idempotency key a request digest.
- `REFRESH_AND_RETRY_NEW_COMMAND` — klient nejprve obnoví revision/discovery/state snapshot a poté vytvoří nový command s novým key; původní command se nemění.
- `RECONCILE_THEN_RETRY` — nový dispatch je zakázán, dokud canonical reconciliation nerozhodne předchozí side effect.
- `MANUAL_REVIEW` — automatický dispatch i retry jsou zakázané do explicitního OWNER rozhodnutí.

`retryAfterMs` se uvádí pouze pro rate/capacity/dependency retry a nikdy nesmí posunout již prošlý absolutní deadline. Klient nesmí odvozovat retry z HTTP statusu, textu `message` ani exception typu; používá pouze stable code, directive a operation contract.

Minimální stable KCIP wire codes jsou:

| Code | Jediný význam | Recovery |
|---|---|---|
| `KCIP_MALFORMED_ENVELOPE` | neplatné framing/JSON/envelope | opravit request, nový message ID |
| `KCIP_UNSUPPORTED_PROTOCOL` | `protocol` není podporován | použít deklarovanou podporovanou verzi |
| `KCIP_RESERVED_FIELD_PRESENT` | caller se pokusil dodat serverové pole | odstranit pole, nový request |
| `KCIP_MESSAGE_ID_CONFLICT` | stejné message ID má jiný digest | neopakovat; opravit producenta |
| `KCIP_CAUSATION_INVALID` | causation neexistuje nebo je mimo correlation | opravit producenta |
| `KCIP_TARGET_NOT_FOUND` | target/operation neexistuje | refresh registry |
| `KCIP_TARGET_STALE` | target revision/release/epoch již není current | refresh exact target a nový command |
| `KCIP_CONTRACT_DIGEST_STALE` | contract digest neodpovídá active revision | refresh contract; bez dispatchu |
| `KCIP_BINDING_NOT_ACTIVE` | exact binding neexistuje nebo není active | aktivovat/obnovit binding |
| `KCIP_BINDING_STALE` | revision/digest/set/epoch bindingu je stale | refresh binding; nový command |
| `KCIP_DEADLINE_EXCEEDED` | effective deadline prošel před známým completion | podle side-effect evidence bez blind retry |
| `KCIP_IDEMPOTENCY_CONFLICT` | stejný key má jiný request digest | nový key pouze pro skutečně novou operaci |
| `KCIP_STATE_VERSION_CONFLICT` | CAS snapshot není current | refresh state; nový command |
| `KCIP_SEQUENCE_STALE` | sequence je nižší než povolená | načíst canonical outcome/watermark |
| `KCIP_SEQUENCE_GAP` | `CONTIGUOUS` sequence přeskočila expected value | dodat přesně chybějící sequence |
| `KCIP_CAPACITY_EXHAUSTED` | admission nemá kapacitu | stejná operace po `retryAfterMs` |
| `KCIP_CANCELLED` | cancellation a cleanup jsou potvrzené | bez retry, pokud OWNER nevytvoří novou operaci |
| `KCIP_OUTCOME_UNKNOWN` | external effect nelze potvrdit ani vyvrátit | `MANUAL_REVIEW` |
| `KCIP_INTERNAL_FAILURE` | neočekávaná platformní chyba | stejná operace pouze podle evidence/retry directive |

Chapter 32 je jediný souhrnný katalog stable error codes; užší operation contract může přidat code, ale nesmí změnit význam nebo recovery výše uvedených codes.

### 6.9 Deadlines, timeout a cancellation

Každý request má absolutní `deadlineAt`. Effective deadline je minimum z caller/business deadline, parent deadline, operation contract limitu a platformního safety limitu. Chybějící caller deadline nahradí server operation contractem; caller nesmí prodloužit parent nebo contract deadline.

Gateway MUST použít databázový čas pro admission a persistované přechody a monotonic process clock pro elapsed timeout uvnitř jednoho attemptu. Clock rollback nesmí prodloužit deadline. Progress může obnovit idle transport timeout pouze tehdy, pokud to contract dovoluje; nikdy neprodlužuje absolute deadline.

Při deadline nebo cancellation platí jediná rozhodovací cesta:

1. před persistovaným dispatch intentem — request se odmítne nebo cancelne bez side effectu,
2. po intentu, ale před dispatch claimem — intent se terminalizuje `CANCELLED` a outbox se neodešle,
3. během read-only práce — runtime práci abortuje a nezapíše success,
4. po možném mutujícím dispatchi — runtime nejprve provede reconciliation; `CONFIRMED_APPLIED` uloží canonical applied outcome, `CONFIRMED_NOT_APPLIED` dovolí bezpečnou terminaci nebo retry a `UNKNOWN` vytvoří `KCIP_OUTCOME_UNKNOWN`/`MANUAL_REVIEW`,
5. po terminal DB commitu — transportní disconnect, timeout nebo cancel canonical outcome nemění; retry jej pouze načte.

Po commitnutém cancellation intentu se nesmí založit nový child call, side-effect attempt nebo task. Již dispatchnutý effect musí doběhnout do známého outcome nebo reconciliation. Cancellation není rollback již potvrzeného effectu.

### 6.10 Idempotence, duplicate delivery, ordering a souběh

KCIP rozlišuje dvě nezávislé vrstvy deduplikace:

- message dedupe podle `(source execution context, messageId)`,
- business idempotence podle deklarovaného `(idempotency scope, idempotency key)` a `requestDigest`.

Stejné message ID a stejný canonical message digest replayuje původní transport acceptance/outcome. Stejné message ID a jiný digest vrací `KCIP_MESSAGE_ID_CONFLICT`. Nové message ID se stejným business key a digestem načte stejnou logical operation. Nové message ID se stejným business key a jiným digestem vrací `KCIP_IDEMPOTENCY_CONFLICT` před handlerem.

Každá mutující KCIP operace MUST deklarovat idempotency scope, request digest, logical operation ID, concurrency scope, expected state version nebo jiný CAS guard, side-effect class, retry class, deadline a reconciliation contract. Worker/execution ID, lease token, message ID a transport request ID nejsou součástí business scope.

Souběžné operace používají row locks, advisory/concurrency claims nebo leases s monotonic fencing tokenem podle 49.6, 49.7 a 51. Každý autoritativní write ověřuje current platform incarnation, state version, binding/activation lineage a fence. Pozdní worker nesmí zapsat state, result, checkpoint, idempotency terminal, event ani pointer.

Pořadově závislá operace deklaruje sequence namespace a policy:

- `CONTIGUOUS` přijme právě expected next sequence. Exact duplicate se stejným digestem replayuje outcome; stale vrací `KCIP_SEQUENCE_STALE`; vyšší než expected vrací `KCIP_SEQUENCE_GAP`.
- `MONOTONIC_WITH_GAPS` přijme pouze hodnotu vyšší než watermark, persistuje každý gap a nikdy jej tichým reorderem nedopočítá.

### 6.11 PULSE wire semantics

PULSE je asynchronní typovaný port. Každý port deklaruje směr `INCOMING|OUTGOING`, pulse type, payload schema a digest, source/target binding, delivery semantics, ordering namespace, idempotency policy, retention, retry/dead-letter policy a compatibility digest.

Kanonický PULSE event obsahuje:

```json
{
  "protocol": "KCIP/1.0",
  "kind": "EVENT",
  "eventId": "uuid",
  "messageId": "uuid",
  "correlationId": "uuid",
  "causationId": "uuid",
  "occurredAt": "RFC3339 timestamp",
  "emittedAt": "RFC3339 timestamp",
  "source": {},
  "target": {},
  "binding": {},
  "port": "stable-port-name",
  "pulseType": "stable-event-type",
  "sequence": {
    "namespace": "stable-sequence-namespace",
    "value": 42,
    "policy": "CONTIGUOUS"
  },
  "schemaDigest": "sha256:...",
  "payloadDigest": "sha256:...",
  "payload": {}
}
```

PULSE delivery je at-least-once. Consumer nejprve v jedné transakci uloží inbox dedupe, ověří exact binding, schema digest a sequence a teprve poté odpoví `ACCEPTED`. PULSE ACK znamená pouze durable přijetí do inboxu, nikoli dokončený business side effect. Terminal business outcome vzniká samostatným persistovaným command/event přechodem.

Stejný `eventId` a payload digest je duplicate a nesmí podruhé aplikovat business změnu. Stejný `eventId` s jiným digestem je `KCIP_MESSAGE_ID_CONFLICT`. Schema nebo compatibility digest mismatch event neprovede a přesune jej do blocking dead-letter stavu s jedinou recovery cestou: aktivace kompatibilního adapteru nebo replay po explicitní opravě bindingu. Event nesmí být potichu zahozen, přetypován ani doručen jinému portu.

### 6.12 Control command a ACK

Control command obsahuje stable command type, command ID, logical operation ID, target component/revision/runtime, request digest, binding-set revision, activation epoch, expected state key/version, correlation/causation a deadline.

Control wire rozlišuje:

- `ACCEPTED` — target gateway durable uložila command a jeho idempotency record; effect ještě nemusí být proveden,
- `REJECTED` — command byl odmítnut před dispatch se stable error code a nic se nezměnilo,
- `COMPLETED` — požadovaný target state a postcondition jsou potvrzené,
- `FAILED` — terminal známé selhání bez požadovaného postcondition,
- `UNKNOWN` — možný effect nelze deterministicky potvrdit; vzniká manual review.

`POST /control/ack` nesmí last-write-wins přepisovat stav. ACK obsahuje command ID, accepted/rejected status, observed target state/version, runtime/release, binding-set revision, activation epoch, digest, correlation a target source sequence. Initial `ACCEPTED` ACK není terminal outcome. Final outcome se zapisuje pouze přes fenced control command state machine. Stejný command ID/digest replayuje canonical current outcome; stejný ID/jiný digest je conflict.

Enable/disable ACK ze stale runtime generation, release, binding setu nebo activation epoch se uchová jako evidence, ale nesmí změnit current component projection.

### 6.13 State observations a heartbeat

State observation obsahuje state key, typed payload, observedAt, emittedAt, component/runtime/release identity, runtime generation, binding-set revision, activation epoch, source sequence, correlation, schema digest a payload digest.

Observation je evidence, nikoli přímý autoritativní domain write. Projection přijme observation pouze tehdy, když source lineage odpovídá current active targetu a source sequence je novější podle state contractu. Exact duplicate se stejným digestem je no-op. Stejná sequence s jiným digestem je protocol conflict. Stale observation se uloží pro audit a monitoring, ale current projection nezmění.

Heartbeat obsahuje component ID/code, runtime/release ID, service/runtime generation, operational state, lifecycle mode, dependency summary, queue/run counters, last successful operation, resource utilization, binding-set revision, activation epoch, monotonic heartbeat sequence, timestamp a nonce.

Heartbeat ACK potvrzuje pouze příjem konkrétní sequence. Heartbeat nesmí aktivovat component, měnit lifecycle, prodlužovat business lease, odblokovat activation gate ani dokazovat úspěch konkrétní operace. Scheduler počítá freshness z posledního validního heartbeat current runtime lineage. Pozdní nebo replayovaný heartbeat není freshness refresh. Chybějící heartbeat po threshold mění pouze monitoring projection a spouští deklarovanou repair policy; neprohlašuje neznámý external effect za failed nebo not applied.

### 6.14 MCP, agentní a KCIP mapování

MCP/JSON-RPC je veřejný nebo adapter wire contract. KCIP je canonical interní dispatch contract. MCP gateway MUST validovat celý MCP transport a JSON-RPC request před vytvořením KCIP requestu. JSON-RPC `id` se uloží jako transport correlation field, nikoli jako KCIP `logicalOperationId` nebo idempotency key.

MCP protocol error před doménovým dispatch nevytvoří handler side effect. Validní `tools/call` se po binding/schema/idempotency preflightu mapuje na exact KCIP operation. MCP result se vytvoří výhradně z persistovaného KCIP/domain outcome. KCIP stable error se mapuje na standardní MCP JSON-RPC error nebo tool execution error podle kapitoly 10; mapování je deklarované, deterministické a nesmí měnit side-effect semantics.

Agent používá KCIP operace:

```text
agent.run.start
agent.message.append
agent.tool.request
agent.tool.result
agent.delegate.request
agent.delegate.result
agent.state.report
agent.run.cancel
agent.run.complete
agent.run.fail
```

Agentní delegace vždy určuje target agent/revision nebo capability, cíl a expected output schema, vstupní context projection, povolené exact tools, budget času/tokenů/tool calls, correlation/causation, cancellation propagation a typed result. Agentní text nesmí přepsat KCIP source, target, binding, deadline, idempotency, schema nebo side-effect class.

### 6.15 KCIP acceptance invariants

KCIP conformance MUST prokázat nejméně:

- canonical JSON a digest shodu napříč implementacemi,
- odmítnutí duplicate JSON members, oversized payloadu a rezervovaných polí,
- source spoof, target spoof, stale revision, digest, binding-set a activation epoch rejection,
- message duplicate replay a message ID/digest conflict,
- business idempotency replay a idempotency conflict,
- correlation/causation úplnost přes sync, queue, PULSE, MCP, task a recovery,
- deadline před dispatch, disconnect/cancel před i po possible side effectu a reconciliation `UNKNOWN`,
- ordering duplicate/stale/gap případy,
- PULSE inbox commit před ACK, DLQ a replay po explicitní opravě,
- control initial ACK versus terminal outcome a stale ACK rejection,
- state/heartbeat stale lineage a sequence rejection,
- stable error a jedinou recovery directive pro každý wire failure,
- žádný handler side effect při pre-dispatch protocol/binding/schema chybě,
- audit, raw wire evidence a result digest pro success, reject, timeout, cancellation, crash a recovery.

Detailní MCP interoperability a wire edge-case matrix je v kapitole 10; formální state-machine a crash pravidla jsou v kapitole 49; PostgreSQL transakční linearizační body jsou v kapitole 51.

## 7. Přístupové hranice a jednoduchá autentizace

### 7.1 Jediná lidská identita

Jedinou lidskou identitou je pevný OWNER `KRMAR78`. Interaktivní přístup ověřuje heslo, volitelné MFA a platnost webové relace. Tím autentizace končí: role, skupiny, uživatelské scopes, uživatelská oprávnění a objektové ACL neexistují. Platná OWNER session má vždy celý funkční rozsah systému.

### 7.2 Jediný OWNER API klíč

KájovoCML NG udržuje právě jeden produkční API credential se stable name `KCML_OWNER_API_KEY`. Klíč je kryptograficky náhodná hodnota nejméně 256 bitů, je uložen šifrovaně v Secret Manageru a pro ověření má samostatný constant-time hash a krátký fingerprint. Neobsahuje role, scopes, audience varianty, expiraci ani vazbu na uživatelský účet.

Klíč poskytuje plnou OWNER autoritu nad hlavním API i kanonickými komponentními HTTPS hranicemi. Volání používá:

```text
Authorization: Bearer <KCML_OWNER_API_KEY>
```

Po úspěšném ověření session nebo klíče server přiřadí konstantní access mode `OWNER_FULL`. Jde o větev v serverovém kódu, nikoli o roli, scope, permission záznam nebo konfigurovatelnou policy. Každá operace deklarovaná v OWNER operation catalogu je dostupná bez dalšího autorizačního rozhodnutí.

OWNER může klíč zobrazit, kopírovat a atomicky rotovat v UI nebo centrálním chatu. Existuje vždy právě jedna active verze; rotace uloží novou šifrovanou hodnotu a hash v jedné transakci a předchozí hodnotu ihned zneplatní. Protože je current hodnota revealable z platné OWNER session, ztracená odpověď po rotaci nezpůsobí lockout. Každé použití ukládá fingerprint, channel, source address, timestamp, correlation a audit.

Stable name `KCML_OWNER_API_KEY` je rezervovaný systémový Secret. Obecné Secret CRUD jej nesmí odstranit, přejmenovat, deaktivovat ani přepnout na jinou verzi; tyto requesty se směrují na read/reveal/rotate kontrakt této jediné credential nebo se odmítnou beze změny stavu.

Neexistuje token list, token CRUD, per-token policy, component access token, service token, JWT issuer ani obecný credential registry.

### 7.3 Webová relace z API klíče

OWNER API klíč lze vyměnit za standardní webovou OWNER relaci prostřednictvím kanonického API-key-session endpointu. Vzniklá relace je relací stejné pevné identity, používá secure cookie a CSRF stejně jako interaktivní login a nevytváří další účet, roli ani oprávnění.

### 7.4 Interní execution context bez tokenů

Platformní a component procesy si mezi sebou neposílají bearer credentialy. Interní call vzniká pouze prostřednictvím trusted runtime hostu, který z autoritativního dispatch/run záznamu doplní source component, revision, execution ID, target a exact binding. Volající payload tyto hodnoty nemůže ovlivnit.

Samostatné lokální procesy komunikují s runtime gateway pouze přes přidělený Unix Domain Socket. Gateway ověřuje systemd service identity, oprávnění socket path a Linux peer credential; generated sandbox nemá obecný síťový ani socketový přístup. Pro in-process volání je source odvozen z nezaměnitelného serverového execution contextu.

### 7.5 Exact contract bindings

Každá interní vazba má source revision, target revision, operation/tool/route, contract digest, binding revision, lifecycle a activation-set relation. Runtime povolí call pouze při exact aktivní shodě. Vazby vznikají ze schválené specifikace, agent tool bindingu, component connection nebo platformního manifestu a aktivují se atomicky s dotčenými revisions.

Contract binding není uživatelské ani obecné strojové oprávnění. Neexistují role, allow/deny rules, scopes, grant inheritance, permission epoch ani samostatná permission matice. Změna dostupné schopnosti je vždy verzovanou změnou konkrétní vazby a jejího kontraktu.

### 7.6 Externí přístupy

Vlastní OWNER klienti používají jediný `KCML_OWNER_API_KEY`. Externí provider callback, webhook, externí MCP server nebo cílová služba používá credential přímo na svém `external_auth_binding`: API key, HMAC/signature, OAuth credential, Basic, mTLS nebo provider-specific verification. Binding je připojen k exact endpointu nebo targetu a nelze jej použít pro jinou route. Obecný external principal ani external permission registr neexistuje.

### 7.7 OWNER Device Bridge

OWNER Device Bridge je jediná vzdálená interní komponenta s vlastní strojovou autentizací. Používá enrollmentem vydaný mTLS certifikát svázaný s jedním bridge ID, version/capability fingerprintem a explicitní revokací. Certifikát neuděluje obecný API přístup; gateway jej přijme pouze na bridge transportu a pro session přidělené tomuto bridge.

### 7.8 Self-test

Self-test runtime, CI a production acceptance používají stejný `KCML_OWNER_API_KEY` proti veřejnému API rootu `https://kaja.hcasc.cz/api/v1`. Neexistuje zvláštní self-test token, bypass ani testovací oprávnění. Testy procházejí stejnou veřejnou gateway, doménovými službami, component routes, KCIP kontraktem, auditem a logováním jako běžný OWNER API klient.

## 8. Password Manager a Secret Manager

### 8.1 Účel

Password Manager je OWNER rozhraní nad kanonickým Secret Managerem. Ukládá, verzionuje, zobrazuje a poskytuje důvěrné hodnoty lidem, agentům, MCP serverům, browserovému runtime, platformním workerům a externím integracím.

### 8.2 Typy položek

Secret položka používá typ:

- `PASSWORD`
- `API_KEY`
- `BEARER_TOKEN`
- `OAUTH_CLIENT`
- `OAUTH_TOKEN_SET`
- `TOTP_SEED`
- `CERTIFICATE`
- `PRIVATE_KEY`
- `WEBHOOK_SECRET`
- `DATABASE_CREDENTIAL`
- `SESSION_STATE`
- `COOKIE_JAR`
- `SSH_CREDENTIAL`
- `GENERIC_TEXT`
- `GENERIC_BINARY`

### 8.3 Datový kontrakt secretu

Secret obsahuje:

- UUID,
- stable name,
- display name,
- popis,
- typ,
- purpose kind a volitelný target object ID,
- tags a skupinu,
- URL, username a poznámky podle typu,
- status,
- active version,
- lock version,
- created/updated/deleted timestamps,
- expiration a rotation policy,
- seznam exact secret bindings,
- usage history,
- audit history.

Secret version obsahuje version number, encrypted value, fingerprint, key ID, algorithm, createdAt, activatedAt, retiredAt a author.

### 8.4 Uložení

Hodnoty jsou šifrované pomocí kanonického master key mechanismu a authenticated encryption. Databáze ukládá ciphertext, nonce, key ID, algorithm a fingerprint. Master key je persistentně uložen jako root-owned encrypted systemd credential source a každé oprávněné service invocation jej získá výhradně read-only systemd credential materializací podle 50.30; jiný host-level mechanismus se v produkci nepoužívá.

### 8.5 OWNER práce s hodnotou

OWNER může z webového UI:

- zobrazit plnou hodnotu,
- kopírovat ji,
- zobrazit historii verzí,
- vytvořit novou verzi,
- aktivovat vybranou verzi,
- porovnat hodnoty a metadata,
- vygenerovat heslo, token nebo TOTP seed,
- importovat certifikát a privátní klíč,
- zobrazit QR kód TOTP,
- exportovat položku,
- upravit metadata,
- nastavit expiraci a rotaci,
- spravovat exact bindings,
- zobrazit všechny používající komponenty, agenty a automatizace,
- otevřít související logy a audit.

Hodnota je v důvěryhodném OWNER UI zobrazena přímo. UI poskytuje volitelný lokální skrývací režim pro pohodlí při sdílení obrazovky; OWNER jej může kdykoli přepnout.

### 8.6 Trusted chat a credential pole

OWNER může vložit credential:

- přímo do diskusní zprávy,
- do dedikovaného credential pole,
- do Password Manageru,
- při browserové challenge,
- při konfiguraci externí integrace.

AI může hodnotu v rámci OWNER diskuse číst a použít podle zadání. Persistentní použití se váže na Secret Manager stable name. Ephemeral hodnota má bounded lifetime a explicitní vazbu na job/run/challenge.

### 8.6.1 Důvěryhodný pohyb hodnot

Secrets mohou v rámci důvěryhodného celku vznikat, být vyjmenovány, zobrazeny, kopírovány, ukládány, commitovány, logovány, předávány deploymentem a používány vývojovým i produkčním kódem. Stejný režim platí pro AI prompty, tool argumenty, modelové odpovědi, testovací evidence, browserové kroky a technické komentáře.

Password Manager zůstává kanonickým místem pro vytváření, správu a runtime bindings všech provozních secrets kromě deployment hesla `PASS`. Přítomnost pracovní kopie hodnoty v repozitáři, logu, chatu, artefaktu nebo release je platnou součástí zabezpečeného celku a nemění lifecycle práce.

### 8.7 Secret bindings

Secret binding určuje source object a exact revision, secret ID nebo stable name, version selector, účel použití, volitelný external target/browser account, lifecycle, activation-set relation a auditní původ. `allSecrets`, wildcard source a zděděný grant nejsou přípustné.

OWNER UI podporuje:

- přímé bind/unbind,
- bulk bind s impact preview,
- binding odvozený z component dependency nebo agent tool bindingu,
- runtime-only binding,
- časově omezený binding,
- test resolve,
- rotaci s okamžitou invalidací závislého session state,
- zobrazení bindings z pohledu secretu i každého používajícího objektu.

### 8.8 Secret resolution

Trusted runtime posílá kanonickému Secret Brokeru serverový execution context, stable name, target context, correlation ID a operation scope. Broker ověří exact active secret binding, source/target revisions a binding digest, vrátí hodnotu pouze procesu provádějícímu danou operaci a zapíše debug log i audit.

### 8.9 OpenAI credential

OpenAI integration používá jediný Secret Manager stable name `OPENAI_API_KEY`. Stejná hodnota obsluhuje centrální chat, generation chat, technické generování, výzkum, AI agenty, evaluace, AI podporované Playwright iterace, development, testy, produkční provoz a další AI funkce.

Všechny OpenAI požadavky vznikají prostřednictvím oficiálních balíčků `openai` a `@openai/agents` a společné serverové `OpenAIClientService`. Responses API obsluhuje modelovou hranici a Agents SDK vícekolové agentní workflow. Volba modelu probíhá v UI nebo centrálním chatu a vždy vybírá model OpenAI.

Secret `OPENAI_API_KEY` se zadává, zobrazuje, mění a testuje v Password Manageru. Development a testovací nástroje získávají stejnou hodnotu přes kanonické API nebo přímo uvnitř důvěryhodného celku. Stav bez aktivního klíče zachovává plně spuštěnou aplikaci a označuje pouze AI capabilities stavem `OPENAI_CONFIGURATION_REQUIRED`. OpenAI client, request descriptor, capability, submit/retrieve a SDK recovery kontrakt určuje kapitola 52.

### 8.10 Password Manager UI

Stránka **Secrets a hesla** obsahuje:

- search a filtry,
- skupiny a tags,
- tabulku a card view,
- stav, typ, expiraci, verzi a usage count,
- rychlé reveal/copy/edit/rotate/bind/delete akce,
- detail s plnou hodnotou,
- versions timeline,
- bindings matrix,
- usage graph,
- TOTP kód a countdown,
- import/export,
- bulk actions,
- audit a live logs.

Všechny provozní secrets kromě GitHub Actions secretu `PASS` vznikají nebo jsou zadávány prostřednictvím tohoto UI či centrálního chatu. Jejich další použití v zabezpečeném vývojově-provozním celku je přímé a plně viditelné OWNERovi.

## 9. Externí systémy, API a webhooks

### 9.1 External target

External target reprezentuje vzdálené API nebo službu. Obsahuje:

- target key,
- display name a popis,
- base URL,
- allowed path prefixes,
- allowed methods,
- connect/request timeout,
- max retries,
- rate limit,
- circuit breaker,
- auth mode,
- secret binding,
- status,
- monitoring profile,
- audit policy.

### 9.2 Auth modes

- `NONE`
- `STATIC_BEARER`
- `STATIC_API_KEY`
- `OAUTH2_CLIENT_CREDENTIALS`
- `OAUTH2_AUTHORIZATION_CODE`
- `MTLS`
- `BASIC`
- `CUSTOM_HEADER`

### 9.3 Outbound call

Komponenta používá `context.callExternal` s target key, method, relative path, headers, query, body a expected response contract. Egress gateway ověří exact active target binding, přidá credential, uplatní limity, circuit policy, audit, retry a vrátí typed response.

### 9.4 TCP TLS

Komponenta může deklarovat `TCP_TLS` external target binding s hostem, portem, servername a protokolem. Egress broker poskytne bounded stream s timeoutem, byte limity, audit a cancellation.

### 9.5 External inbound auth binding

Externí klient nebo provider má credential binding připojený přímo k exact component endpointu, toolu nebo webhooku. Binding obsahuje auth mode, credential reference či verification contract, rate limit, lifecycle a audit. Veřejná API hranice ověří binding a vytvoří serverový execution context; credential nelze použít pro jinou route a nevzniká obecná externí identita ani permission.

### 9.6 Webhook

Webhook endpoint patří kanonické komponentě a používá:

- challenge token,
- HMAC SHA-256,
- challenge + HMAC,
- bearer nebo mTLS podle kontraktu.

Webhook zpracuje provider challenge, ověří podpis, uloží raw request a headers do OWNER debug auditu, vygeneruje correlation ID a zavolá component handler přes interní runtime gateway.

### 9.7 Circuit breaker

Circuit má stavy `CLOSED`, `OPEN`, `HALF_OPEN`. Ukládá failure count, threshold, openedAt, next probe, last error a recovery evidence. OWNER může circuit otevřít, zavřít, otestovat a resetovat z UI.

### 9.8 Externí integrace UI

Stránka **Externí systémy** poskytuje:

- targets,
- external auth bindings,
- inbound endpoints a outbound target bindings,
- auth bindings,
- live request historii,
- latency a error statistiky,
- circuit state,
- test request builder,
- webhook inspector,
- provider challenge tester,
- rate limit a retry konfiguraci,
- monitoring a alarmy,
- stejné object actions ve všech souvisejících pohledech.

## 10. MCP servery a MCP prvky

### 10.1 Kanonické druhy MCP prvků

KájovoCML NG spravuje následující MCP prvky jako samostatně adresovatelné, verzované a auditované kontrakty:

- `MCP_SERVER` — protokolová hranice komponenty,
- `MCP_TOOL` — volatelná typovaná operace,
- `MCP_RESOURCE` — čitelný zdroj nebo resource template,
- `MCP_PROMPT` — parametrizovaná promptová šablona,
- `MCP_ADAPTER` — řízené napojení existující komponenty, externího MCP serveru nebo interní capability na kanonický MCP profil.

Každý prvek má stabilní identitu, vlastní immutable revision, owner component, contract digest, lifecycle, verification state, activation eligibility, monitoring a audit. MCP prvek bez active owner component/revision, active exact bindingu a platného contract digestu není volatelný.

### 10.2 Kanonická protocol revision a kompatibilitní éry

Kanonická protocol revision interně generovaných MCP serverů je `2026-07-28`. Tato revision používá moderní stateless per-request model bez initialization handshake a bez protocol-level session. Každý request samostatně nese protocol version, client identity metadata a client capabilities; server nesmí odvozovat žádnou z těchto hodnot z předchozího requestu, HTTP connection, SSE streamu, stdio procesu, browser session nebo agentní conversation.

KájovoCML rozlišuje:

- `MODERN` — revision `2026-07-28` a novější revision se stejným per-request era modelem,
- `LEGACY` — revision `2025-11-25` a starší initialization/session profil,
- `DUAL_ERA_ADAPTER` — explicitní gateway revision, která podporuje přesně uvedené moderní i legacy revisions a normalizuje je do kanonického interního kontraktu.

Protocol era je vlastností konkrétního server processu nebo HTTP origin + endpoint + auth-binding fingerprintu, nikoli vlastností jednotlivého requestu. Era decision má evidence digest, observed time a expiry a při prvním nesouladu se invaliduje a server se znovu probuje. `UNKNOWN` nebo `AMBIGUOUS` era blokuje registraci, agent binding a aktivaci; systém nesmí era odhadnout podle názvu serveru, `serverInfo`, status textu nebo chybějící funkce.

Nově generovaný MCP server používá výhradně `MODERN`. Neimplementuje legacy `initialize`, `notifications/initialized`, `Mcp-Session-Id`, samostatný HTTP GET stream, HTTP DELETE session teardown, `resources/subscribe`, `resources/unsubscribe`, serverem iniciované JSON-RPC requests ani SSE resume přes event IDs. Existující legacy server se registruje pouze přes explicitní compatibility adapter s odděleným transport profilem, state machine, testy, monitoringem a activation relation.

Roots, Sampling, Logging a deprecated HTTP+SSE jsou legacy/deprecated protocol features. Nový KájovoCML business contract na nich nestaví kontinuitu ani autoritu. Pokud moderní server potřebuje roots, sampling nebo elicitation jako server-to-client input, vloží odpovídající request do MRTR `inputRequests`; samostatný server-initiated JSON-RPC request neposílá. Vlastní AI operace KájovoCML používají kanonickou OpenAI platformu, nikoli MCP Sampling jako skrytou modelovou hranici.

Moderní client a server vyjednávají optional extensions pouze přes `capabilities.extensions` v konkrétním requestu a `server/discover`. Extension identifier má povinný namespaced prefix. Podpora v dřívějším requestu ani v discovery cache sama o sobě neopravňuje server použít extension v aktuálním requestu. Chybí-li capability, server použije definovaný core fallback, nebo request před side effectem odmítne `-32021 MissingRequiredClientCapability`; nesmí extension potichu aktivovat.

### 10.3 Streamable HTTP transport a HTTP/JSON-RPC hranice

Veřejná kanonická hranice moderního serveru je:

```text
POST https://kcmlNNNN.kaja.hcasc.cz/mcp
```

Každá JSON-RPC request nebo transportem přípustná notification používá vlastní HTTP `POST`. Body obsahuje právě jednu JSON-RPC zprávu. Batch array, více konkatenovaných objektů, JSON-RPC response od klienta, request s `id = null` a objekt obsahující současně `result`/`error` s `method` jsou neplatné.

Klient pro request posílá nejméně:

```text
Content-Type: application/json
Accept: application/json, text/event-stream
MCP-Protocol-Version: 2026-07-28
Mcp-Method: <json-rpc-method>
Mcp-Name: <method-specific routing value, je-li vyžadován>
Authorization: Bearer <KCML_OWNER_API_KEY nebo endpoint-local credential>
```

`Mcp-Name` je povinný pro:

- `tools/call` a odpovídá `params.name`,
- `resources/read` a odpovídá `params.uri`,
- `prompts/get` a odpovídá `params.name`,
- `tasks/get`, `tasks/update` a `tasks/cancel`, pokud je Tasks extension aktivní, a odpovídá `params.taskId`.

`MCP-Protocol-Version`, `Mcp-Method`, `Mcp-Name` a každý schema-recognized `Mcp-Param-*` jsou singleton routing fields. Dvě instance s rozdílnými hodnotami, comma-joined více hodnot, neplatné znaky, neplatný Base64 sentinel nebo chybějící required field jsou `HeaderMismatch`. Header names se porovnávají case-insensitively; method, name, URI a decoded values case-sensitively, s výjimkou numerického porovnání integer argumentu.

Server odpoví na JSON-RPC request právě jedním z profilů:

- `Content-Type: application/json` s jedním JSON-RPC result nebo error objektem,
- `Content-Type: text/event-stream` s request-scoped JSON-RPC notifications a jednou finální JSON-RPC response.

Klient MUST podporovat oba profily. Server nesmí podle `Accept` vybrat jiný business význam nebo side-effect semantics. `406 Not Acceptable` se vrací, pokud `Accept` neumožňuje oba povinné typy. `415 Unsupported Media Type` se vrací pro nepodporovaný request content type. `413 Content Too Large` se vrací před JSON parse, pokud request překročí bounded limit.

Kanonická HTTP/JSON-RPC mapa je:

| Stav | HTTP | JSON-RPC body | Handler |
|---|---:|---|---|
| success nebo běžný RPC error validního requestu | `200` | result nebo error se stejným exact ID | podle pipeline |
| přijatá JSON-RPC notification | `202` | žádné body | notification contract |
| odmítnutá notification | relevantní `4xx/5xx` | MAY error bez `id` | ne |
| neplatný `Origin` | `403` | volitelný error bez `id` | ne |
| chybějící/neplatný credential | `401` nebo `403` | HTTP auth error; nesmí předstírat peer MCP outcome | ne |
| malformed JSON | `400` | `-32700 Parse error`, `id` se vynechá | ne |
| neplatný JSON-RPC shape, batch, response, `id=null` | `400` | `-32600 Invalid Request`; čitelné validní ID se echo, jinak se vynechá | ne |
| missing/malformed/mismatched routing header | `400` | `-32020 HeaderMismatch` | ne |
| unsupported protocol version | `400` | `-32022 UnsupportedProtocolVersion` | ne |
| missing required current-request capability | `400` | `-32021 MissingRequiredClientCapability` | ne |
| neimplementovaná RPC method | `404` | `-32601 Method not found` se stejným ID | ne |
| validní method, neplatné params/cursor/taskId/unknown tool | `200` | `-32602 Invalid params` nebo method-specific protocol error | ne |
| runtime JSON-RPC internal failure po přijetí requestu | `200` | `-32603 Internal error` | možná; podle evidence |
| route mimo MCP endpoint | `404` | běžná HTTP chyba, nikoli MCP era důkaz | ne |
| HTTP method jiná než POST | `405` | žádný MCP success | ne |
| rate/capacity před přijetím RPC | `429` nebo `503` | transport error s retry metadata | ne |

MCP-aware klient MUST vyhodnotit HTTP status i případné JSON-RPC body. HTTP status popisuje transportní přijetí; JSON-RPC error popisuje protocol outcome. Klient nesmí z HTTP `400`, `404` nebo `405` bez analýzy body automaticky usoudit na legacy era ani bezpečně opakovat mutující operaci.

Moderní core revision nedefinuje žádnou client-to-server core notification pro Streamable HTTP. `notifications/cancelled` se na HTTP neposílá; zavření příslušného SSE response streamu je cancellation signal. Pokud budoucí extension výslovně povolí notification POST, přijatá notification MUST vrátit `202 Accepted` bez body a receiver nesmí poslat JSON-RPC response.

#### 10.3.1 Request-scoped SSE framing

Každý SSE stream náleží právě jednomu původnímu JSON-RPC requestu. Server:

- používá `X-Accel-Buffering: no`,
- každý `data:` event obsahuje právě jednu úplnou JSON-RPC zprávu,
- může vysílat pouze request-related notifications, například `notifications/progress` nebo `notifications/message`,
- nesmí vyslat independent JSON-RPC request, response s jiným ID ani notification patřící jinému requestu,
- jako poslední aplikační zprávu odešle finální response se stejným exact ID a poté stream ukončí,
- může vysílat SSE comment keep-alive lines, které nemají protocol význam,
- nesmí emitovat SSE `id:` ani `retry:` fields a nepodporuje `Last-Event-ID`.

Klient odmítne malformed SSE data, wrong response ID, více finálních responses, zprávu po finální response, server-initiated JSON-RPC request nebo independent notification jako protocol violation. U mutujícího callu taková chyba nespouští blind retry; klient nejprve použije business idempotency/outcome endpoint nebo reconciliation.

Disconnect má jediný význam podle persistovaného call state:

1. před dispatch intentem — call lze uzavřít bez effectu,
2. po intentu před dispatch claimem — cancellation zablokuje dispatch,
3. během read-only handleru — práce se abortuje,
4. po možném mutujícím dispatchi — vzniká reconciliation, nikoli předpoklad success/failure,
5. po terminal DB commitu před doručením frame — canonical outcome zůstává terminal a retry se stejným business key jej načte.

Po disconnectu server MUST přestat emitovat frames. Cancellation se propaguje do handleru, child KCIP calls a nonterminal task pouze podle jejich vlastního cancellation contractu. Durable Task přežije transportní disconnect; zavření původního streamu nesmí task automaticky smazat ani falšovat jeho terminal status.

#### 10.3.2 HTTP Origin, session headers a legacy traffic

Přítomný `Origin` MUST odpovídat explicitní origin policy; invalid origin vrací `403` před handlerem. Moderní endpoint na `GET` a `DELETE` vrací `405 Method Not Allowed`. `Mcp-Session-Id` a `Last-Event-ID` nemají moderní význam, negenerují se, neechojí se a nesmí ovlivnit routing, cache, authorization nebo continuity. Jejich přítomnost se auditně zaznamená a jinak ignoruje, pokud request není zpracováván explicitním legacy adapterem.

### 10.4 JSON-RPC IDs, per-request metadata, identity a capabilities

Každý moderní request obsahuje v `params._meta` nejméně:

```json
{
  "io.modelcontextprotocol/protocolVersion": "2026-07-28",
  "io.modelcontextprotocol/clientInfo": {
    "name": "KájovoCML NG",
    "version": "application-release"
  },
  "io.modelcontextprotocol/clientCapabilities": {}
}
```

Podle operation contractu může obsahovat:

- `progressToken`,
- negotiated extension capabilities,
- `traceparent`, `tracestate` a `baggage`,
- KájovoCML vendor metadata pouze pod prefixem `cz.hcasc.kajovocml/`.

`io.modelcontextprotocol/*` a jiné MCP-reserved prefixes nesmí KájovoCML použít pro vendor hodnotu. Unknown vendor `_meta` se zachová pouze jako bounded untrusted metadata, nesmí změnit identity, access, binding, idempotency, side-effect, retry, deadline nebo schema.

Protocol version a capabilities se vyhodnocují pro každý request samostatně. `clientInfo` a `serverInfo` jsou self-reported diagnostická metadata a nesmí být security, routing, collision, idempotency ani compatibility autoritou. Veřejný request získá access context z ověřeného OWNER API klíče nebo endpoint-local credentialu; interní request výhradně z trusted runtime gateway.

JSON-RPC request ID:

- MUST být string nebo integer a MUST NOT být `null`, boolean, float, object nebo array,
- MUST být unikátní mezi všemi requests, které stejný sender v daném exact source/access contextu vydal a dosud pro ně neobdržel response,
- string `"1"` a integer `1` jsou rozdílná ID; server MUST echo exact JSON type a value,
- `subscriptions/listen` ID zůstává in-flight po celou dobu subscription streamu,
- reuse ID po finální response je povoleno, ale vzniká nový transport request; nesmí replayovat starý business outcome pouze podle ID,
- duplicate in-flight ID se odmítne `-32600` s `error.data.stableCode = MCP_DUPLICATE_IN_FLIGHT_REQUEST_ID`; druhý request se nespustí,
- není idempotency key, logical operation ID, task ID, state handle ani correlation ID.

Každý moderní result obsahuje známý `resultType`. Core hodnoty jsou `complete` a `input_required`; negotiated extension může přidat další hodnotu, například `task`. Neznámý nebo nevyjednaný `resultType` je invalid response. Pouze compatibility adapter při čtení explicitně legacy response může chybějící `resultType` normalizovat na `complete`; generated moderní server jej MUST vždy emitovat.

Server v result `_meta` uvádí nejméně:

```json
{
  "io.modelcontextprotocol/serverInfo": {
    "name": "KCML0001",
    "version": "active-release"
  },
  "cz.hcasc.kajovocml/correlationId": "uuid",
  "cz.hcasc.kajovocml/logicalOperationId": "uuid-or-null",
  "cz.hcasc.kajovocml/contractDigest": "sha256:...",
  "cz.hcasc.kajovocml/resultDigest": "sha256:..."
}
```

Nepodporovaná version vrací `-32022` a `error.data` obsahuje `requested` a deterministicky seřazené `supported`. Chybějící capability vrací `-32021` a exact `requiredCapabilities`. Header/body mismatch vrací `-32020`. Všechny tyto chyby vznikají před doménovým side effectem.

### 10.5 `server/discover`

Každý moderní MCP server MUST implementovat `server/discover`. Request neobsahuje žádné method-specific parametry mimo standardní `_meta`. Unknown method response na validní moderní `server/discover` znamená protocol-invalid moderní endpoint; není důvodem pro tichý legacy fallback.

Operace vrací:

- `resultType = complete`,
- neprázdný deterministicky seřazený `supportedVersions`,
- server capabilities,
- extension capability map,
- server identity v result `_meta`,
- volitelné server instructions,
- `ttlMs >= 0`,
- `cacheScope = public | private`.

`serverInfo` a instructions jsou untrusted metadata pro display, logging a model context. Nesmí rozhodovat auth, exact binding, tool alias collision, side-effect class, approval, Secret access nebo protocol era.

Obecný klient MAY `server/discover` přeskočit a zpracovat `UnsupportedProtocolVersion` inline. Interní agentní, registration, generation, validation a activation runtime jej používá jako povinný preflight, protože musí před prvním domain call ověřit server identity evidence, versions, capabilities, extensions, tasks/MRTR/subscription support a cache policy.

Discovery response se přijme pouze tehdy, když response ID přesně odpovídá requestu, `resultType` je známý, server podporuje request version nebo vrátí správný `-32022`, capability schema je validní a cache hints odpovídají kapitole 10.7. Discovery success s následnou header/version/capability nekonzistencí invaliduje snapshot a readiness a spustí fresh verification; klient nesmí pokračovat se stale assumptions.

### 10.6 MCP server revision

MCP server revision obsahuje:

- `schemaVersion`,
- `serverId`, `componentId` a runtime execution kind,
- canonical hostname a endpoint,
- protocol era a supported protocol versions,
- transport profile,
- required request metadata profile,
- server capability map,
- extension capability map,
- seznam přesných tool/resource/prompt revision referencí,
- discovery a cache policy,
- subscriptions policy,
- MRTR policy,
- optional Tasks extension policy,
- public access profile a exact internal binding profile,
- runtime mode a resource profile,
- monitoring a E2E profile,
- legacy compatibility relations,
- canonical JSON a SHA-256 digest.

Revision je validní pouze tehdy, když všechny odkazované prvky existují, mají shodného ownera nebo explicitní adapter relation, jejich schémata jsou validní, povinné capabilities odpovídají vystaveným metodám a žádné model-visible jméno nekoliduje v daném exposure scope.

### 10.7 Discovery snapshots, cache a pagination

KájovoCML ukládá immutable discovery/read snapshot pro:

- `server/discover`,
- `tools/list`,
- `resources/list`,
- `resources/templates/list`,
- `prompts/list`,
- `resources/read`.

Snapshot obsahuje exact endpoint/server/release, protocol version, full request-parameter digest, client capability digest, extension digest, access-channel/binding fingerprint, result payload, jednotlivé contract digests, `ttlMs`, `cacheScope`, received time, latency, page cursor lineage a aggregate digest.

Cache key MUST obsahovat method a všechny request parameters ovlivňující result, zejména cursor, URI, exact endpoint, protocol version, client capability/extension digest a access/binding fingerprint. Request obsahující `inputResponses` nebo `requestState` se nikdy necachuje. `input_required` a `task` create result se necachují jako běžný method result.

Každý `complete` result uvedených cacheable methods MUST obsahovat integer `ttlMs >= 0` a `cacheScope = public | private`:

- `ttlMs = 0` znamená okamžitě stale,
- positive TTL se počítá od lokálního času přijetí response; je freshness hint, nikoli garance nezměnitelnosti,
- server nesmí emitovat negative TTL,
- `public` lze sdílet pouze tehdy, pokud je výsledek skutečně nezávislý na credentialu, endpoint bindingu, OWNER contextu a confidential data,
- `private` se smí reuse pouze pro stejný exact authorization/access-binding context.

Relevantní notification invaliduje cache okamžitě i před TTL. Cache se dále invaliduje změnou active server/tool/resource/prompt revision, release, contract digestu, binding-set revision, activation epoch, capability/extension profile, invalidním cursorem, schema drift chybou a explicitním verify/repair během.

KájovoCML může po refresh failure zobrazit poslední snapshot jako označenou stale diagnostickou evidence. Agent binding, generation verification, activation a nový mutující run jej nesmí použít jako current execution contract. Před tool call se i při cache hitu znovu ověří current active revision, readiness, binding digest a activation epoch.

List results jsou deterministicky seřazené podle stable key definovaného revision contractem. Set ani pořadí se nesmí měnit podle HTTP connection, stdio processu nebo side effectu jiného requestu. Může se lišit pouze podle explicitního per-request capability a exact access/binding contextu zahrnutého v cache key.

Pagination používá opaque string `cursor`:

- klient jej nesmí parse, modifikovat ani odvozovat z něj pořadí,
- page size určuje server,
- chybějící `nextCursor` znamená konec; empty string je validní cursor a nesmí být zaměněn za konec,
- každá page je samostatná cache entry se svým TTL; všechny pages stejného list requestu MUST mít stejný `cacheScope`,
- protocol neposkytuje cross-page snapshot consistency. KájovoCML registration a activation workflow, které vyžadují úplný konzistentní seznam, používají serverem podporovaný snapshot digest nebo provedou bounded refetch od první page a ověří aggregate digest,
- invalid/expired/wrong-context cursor vrací `-32602` s `MCP_CURSOR_INVALID`; klient zahodí všechny pages daného traversal a začne bez cursoru,
- cursor je svázán s method, server/release, access/binding fingerprintem, protocol version a relevantním list snapshotem; reuse v jiné souvislosti se odmítne.

### 10.8 MCP tool contract, JSON Schema, `$ref` a OpenAI projection

Každý MCP tool contract obsahuje:

- stabilní `toolId`,
- case-sensitive MCP `name`,
- `title`, popis účelu a podmínky použití,
- input JSON Schema,
- output JSON Schema pro `structuredContent`,
- content rendering policy,
- operation scope a access profile,
- side-effect class,
- retry class,
- idempotency policy,
- concurrency key policy,
- timeout a deadline policy,
- request, response a artifact size limity,
- approval policy,
- MRTR policy,
- optional task policy,
- cancellation behavior,
- protocol, tool-execution a business error catalog,
- examples a deterministic positive/negative contract vectors,
- owner component/revision a contract digest.

MCP doporučené tool name je 1–128 znaků, case-sensitive a používá ASCII písmena, číslice, `_`, `-` a `.`. Interně generované KájovoCML tools používají přísnější stabilní profil `^[a-z][a-z0-9_.-]{0,63}$`. Externí jméno, které je validní pro deklarovanou MCP revision, se zachová beze změny; kolize se řeší explicitním aliasem. Invalid external jméno blokuje element, nikoli tichou normalizaci source contractu.

Input a output schema používá JSON Schema 2020-12, pokud explicitně nedeklaruje jiný KájovoCML podporovaný dialect. Bez `$schema` se MUST interpretovat jako 2020-12. Unsupported dialect vrací `MCP_SCHEMA_DIALECT_UNSUPPORTED` a blokuje registraci/binding; nesmí se validovat přibližně jiným dialectem.

Schema bundle:

- má canonical root `$id`, dialect, immutable bundle digest a mapu všech local resources,
- resolveuje local `$ref`, `$defs`, anchors a dynamic anchors podle deklarovaného dialectu,
- nesmí automaticky dereference network URI,
- remote `$ref` je povolen pouze explicitním registration importem přes egress/SSRF policy, host allowlist, bounded timeout/size/depth a immutable vendored copy; runtime validation již síť nepoužívá,
- unresolved reference, duplicate canonical `$id`, ambiguous anchor nebo reference cycle překračující resource budget blokuje contract,
- composition depth, počet subschemas, regex complexity a validační čas mají bounded budget.

Generator pro nové tools zapisuje úplné typy, bounded string/array/object limity, descriptions, examples a explicitní `additionalProperties` policy. Input schema MUST být object schema. Generated output schema je povinné a validuje každý `structuredContent` success i deklarovanou structured error variantu.

Nativní MCP schema a OpenAI strict tool schema jsou dvě samostatné immutable hranice. Agent binding compiler:

1. načte exact native MCP schema bundle a digest,
2. validuje dialect, references a resource budget,
3. sestaví modelovou projekci podporovanou vybraným OpenAI strict-schema profilem,
4. v každém modelovém objectu nastaví `additionalProperties: false`,
5. udělá všechny model-visible properties required a původní optionality vyjádří explicitním `null` nebo diskriminovanou variantou pouze tehdy, je-li převod semanticky lossless,
6. uloží projection mapping, projection digest, unsupported-keyword report a round-trip vectors,
7. po model outputu provede inverse projection a znovu validuje native MCP input schema,
8. zablokuje binding nebo vytvoří explicitní verzovaný adapter tool, pokud lossless projection není možná.

Projection nesmí potichu odstranit constraint, enum value, required semantics, numeric precision, union discriminator, default význam ani citlivost pole. Nativní MCP contract se kvůli modelové schema vrstvě nemění. Každá změna native schema digestu invaliduje projection, agent binding readiness a model prompt cache, i když compatibility engine změnu klasifikuje jako backward compatible.

### 10.9 Streamable HTTP routing headers a `x-mcp-header`

Gateway validuje:

- `MCP-Protocol-Version` proti `params._meta["io.modelcontextprotocol/protocolVersion"]`,
- `Mcp-Method` proti JSON-RPC `method`,
- `Mcp-Name` proti method-specific `params.name`, `params.uri` nebo `params.taskId`,
- každý recognized `Mcp-Param-*` proti value na přesné schema-annotated property path.

Tool parameter může deklarovat `x-mcp-header` pouze tehdy, když:

- annotation není prázdná a odpovídá HTTP field-name `tchar` syntaxi,
- neobsahuje CR, LF ani control character,
- je case-insensitively unikátní v celém input schema,
- property type je právě `string`, `integer` nebo `boolean`; `number` není přípustný,
- integer je v safe JavaScript rozsahu `-(2^53-1)` až `2^53-1`,
- property je staticky dosažitelná od schema rootu pouze řetězcem `properties`,
- cesta neprochází `items`, jiným array keywordem, `oneOf`, `anyOf`, `allOf`, `not`, `if/then/else` ani `$ref`.

Klient Streamable HTTP MUST invalidní tool definition vyřadit z effective `tools/list` exposure, vytvořit evidence s tool name a důvodem a nesmí kvůli jednomu invalid toolu zahodit jiné validní tools. Stdio klient MAY `x-mcp-header` ignorovat, protože nejde o stdio routing requirement.

Password, API key, bearer token, cookie, private key, TOTP seed, session state, osobní údaj nebo jiné contractem označené sensitive pole se jako `x-mcp-header` v KájovoCML negeneruje. Externí annotation je untrusted; registration verification ji odmítne, pokud porušuje local sensitivity policy.

Type conversion je deterministická:

- string — původní string,
- integer — canonical decimal bez exponentu,
- boolean — lowercase `true` nebo `false`,
- `null` nebo nepřítomná property — header se MUST vynechat.

Plain header value smí obsahovat pouze bezpečnou HTTP field value. Non-ASCII, control characters, leading/trailing whitespace nebo hodnota, která sama začíná `=?base64?` a končí `?=`, se MUST zakódovat z UTF-8 pomocí přesného sentinelu:

```text
=?base64?{BASE64_UTF8_VALUE}?=
```

`{BASE64_UTF8_VALUE}` je metavariable pro standardní Base64 kódování přesných UTF-8 bytes původní hodnoty; složené závorky nejsou součástí wire hodnoty. Prefix `=?base64?` a suffix `?=` jsou case-sensitive a musí být přesně v tomto lowercase tvaru. Totéž platí pro `Mcp-Name`. Server sentinel dekóduje před porovnáním, odmítne invalid Base64, non-canonical padding nebo undecodable UTF-8. Header names jsou case-insensitive; decoded string values a method/name/URI jsou case-sensitive. Integer se porovnává numericky po canonical parse; boolean pouze proti `true|false`.

Pokud body obsahuje non-null hodnotu annotated property, příslušný header je povinný. Pokud je value `null` nebo chybí, header nesmí být vyžadován. Missing, extra recognized-but-unbound, malformed nebo mismatched header vrací HTTP `400`, JSON-RPC `-32020 HeaderMismatch` a `error.data.stableCode = MCP_HEADER_MISMATCH` před handlerem.

Header je routing/inspection mirror. JSON body zůstává business input, avšak zpracování pokračuje pouze po prokázané rovnosti obou reprezentací. Intermediary, který rozhoduje podle headeru, MUST vynutit version, pro niž je header/body validation povinná, nebo request odmítnout.

### 10.10 Result, protocol error, tool execution error a platform failure

Běžný tool result používá:

```json
{
  "jsonrpc": "2.0",
  "id": "request-id",
  "result": {
    "resultType": "complete",
    "content": [],
    "structuredContent": {},
    "isError": false,
    "_meta": {
      "io.modelcontextprotocol/serverInfo": {
        "name": "KCML0001",
        "version": "active-release"
      },
      "cz.hcasc.kajovocml/correlationId": "uuid",
      "cz.hcasc.kajovocml/logicalOperationId": "uuid",
      "cz.hcasc.kajovocml/contractDigest": "sha256:...",
      "cz.hcasc.kajovocml/resultDigest": "sha256:..."
    }
  }
}
```

`structuredContent` může být libovolná JSON hodnota a při deklarovaném output schema mu MUST odpovídat. KájovoCML generated tool output schema deklaruje vždy. `content` je deterministic human/model render stejného výsledku nebo doplňující text/image/audio/resource content. Pokud contract neurčuje jiný bezpečný render, structured value se poskytne také jako canonical serialized text content.

JSON-RPC response:

- MUST obsahovat exact request ID se stejným JSON type/value,
- MUST obsahovat právě `result` nebo `error`, nikdy obojí,
- result MUST obsahovat známý `resultType`,
- error code MUST být integer a message string,
- při parse/invalid request, jehož ID nelze spolehlivě přečíst, se `id` vynechá; moderní KájovoCML nepoužívá `id:null` jako náhradu.

Chyby se rozlišují:

1. **Protocol error** — request nelze bezpečně předat tool handleru: malformed JSON-RPC, header mismatch, unsupported version, missing capability, unknown method, unknown tool, invalid CallToolRequest params, invalid cursor/task ID nebo binding-visible protocol shape. Vrací JSON-RPC error a handler se nespustí.
2. **Tool execution error** — request a tool dispatch jsou validní, ale operation-specific input, dependency nebo business podmínka může být opravena callerem/modellem. Vrací `resultType=complete`, `isError=true`, actionable `content` a stable tool error v result `_meta`; nejde o JSON-RPC protocol failure.
3. **Platform failure** — nečekaná chyba gateway/runtime/persistence. Před dispatch vrací `-32603` bez effectu. Po možném side effectu se nejprve persistuje raw attempt a reconciliation; response nesmí tvrdit safe retry, dokud outcome není známý.

Unknown tool a malformed `tools/call` params používají `-32602`, nikoli tool `isError`. Resource/task/cursor not found v moderním profilu rovněž používá `-32602`; legacy `-32002` se přijímá pouze od explicitně legacy serveru a adapter jej normalizuje.

MCP specification-reserved rozsah `-32020` až `-32099` smí KájovoCML použít pouze pro codes definované danou MCP revision. Pro `2026-07-28` jsou to právě `-32020`, `-32021` a `-32022`. KájovoCML-specific význam se přenáší v `error.data.stableCode`, `classification`, `retryDirective`, `correlationId` a safe details; nesmí se vydávat pod nedefinovaným `-320xx` code.

Schema-neplatný handler output se nikdy neodešle jako success. Runtime uloží raw output, označí `MCP_RESULT_SCHEMA_INVALID` a:

- bez side effectu nebo při `CONFIRMED_NOT_APPLIED` vrátí `-32603` a terminal known failure,
- při `CONFIRMED_APPLIED` vrátí non-retryable platform/tool failure s `effectOutcome=CONFIRMED_APPLIED`, aby caller nevytvořil duplicate effect,
- při `UNKNOWN` vrátí `MCP_OUTCOME_UNKNOWN`, přejde do `MANUAL_REVIEW` a zablokuje nový dispatch.

Structured error content je vynechaný nebo odpovídá explicitní error variantě output schema. Text `message` není machine contract a nesmí určovat retry. Plný interní exception context je dostupný OWNERovi v logu/auditu; external binding dostane pouze safe details.

### 10.11 Multi Round-Trip Requests — MRTR

`InputRequiredResult` lze vrátit pouze z:

- `tools/call`,
- `resources/read`,
- `prompts/get`.

Jiná method nesmí vrátit `resultType=input_required`. Result obsahuje alespoň jedno z `inputRequests` nebo `requestState`:

```json
{
  "resultType": "input_required",
  "inputRequests": {
    "owner_confirmation": {
      "method": "elicitation/create",
      "params": {}
    }
  },
  "requestState": "opaque-server-state"
}
```

`inputRequests` je mapa unique string keys v rámci jednoho MRTR resultu. Value je pouze request shape povolený current protocol revision, zejména `elicitation/create`, `sampling/createMessage` nebo `roots/list`. Server nesmí vložit request, pro který klient v aktuálním requestu nedeklaroval capability. Generated KájovoCML server pro nový business tok používá standardně `elicitation/create`; Sampling nebo Roots použije pouze explicitní revision contract a negotiated capability, nikdy jako skrytou platformní závislost.

Client retry:

- používá nové JSON-RPC ID,
- znovu posílá původní method a nezměněné salient business arguments,
- posílá `inputResponses` ke známým keys,
- echo přesně stejný `requestState`, pokud byl vydán,
- nesmí poslat `requestState`, pokud původní result žádný neobsahoval,
- zůstává součástí stejné interní logical operation a business idempotency scope; změna původních arguments vytváří conflict, nikoli jinou větev stejného MRTR.

`requestState` je opaque string. Klient jej nesmí inspect, parse, normalize ani modifikovat. KájovoCML jej chrání AEAD/HMAC nebo ukládá server-side pod cryptographically random handlem a ověřuje exact source/access context, server/tool revision, method, target, original salient-argument digest, binding-set revision, activation epoch, logical operation, expiration a consumption state.

MRTR pre-side-effect invariant:

- server MUST vrátit `input_required` před prvním external/non-idempotent side effectem daného continuation,
- nelze-li tento invariant splnit, operation použije negotiated durable Task nebo samostatný explicitní command workflow,
- `requestState` nikdy nedokazuje, že side effect nebyl proveden.

Při retry server:

1. validuje requestState a original request digest,
2. ignoruje a auditně eviduje unknown response keys, already-satisfied keys a keys, které nejsou v current outstanding setu,
3. přijme partial known responses; pokud nezbytný input chybí, vrátí nový `InputRequiredResult`,
4. canonicalizuje accepted responses a atomicky je consume s enqueue resume,
5. exact replay již consumed state se stejným response digestem vrátí current/terminal canonical outcome,
6. reuse consumed state s jiným recognized response digestem vrátí `MCP_REQUEST_STATE_CONSUMED`,
7. invalid signature/context/method/digest vrátí `MCP_REQUEST_STATE_INVALID`; expiry vrátí `MCP_REQUEST_STATE_EXPIRED`.

KájovoCML mapuje MRTR na persistentní `mcp_input_exchange`. OWNER approval, credential doplnění nebo challenge vytváří exact input request s immutable digestem. Agent run přejde do `WAITING_FOR_MCP_INPUT` nebo `WAITING_FOR_OWNER`, uloží checkpoint a po atomic consume pokračuje právě jednou. MRTR result a každý retry s `inputResponses`/`requestState` se nesmí cacheovat.

### 10.12 Side-effect, retry a idempotency

Side-effect class používá:

```text
READ_ONLY
LOCAL_STATE_IDEMPOTENT
EXTERNAL_IDEMPOTENT
EXTERNAL_NON_IDEMPOTENT
DESTRUCTIVE
```

Retry class používá:

```text
SAFE_RETRY
RETRY_AFTER_RECONCILIATION
NO_AUTOMATIC_RETRY
```

Mutující tool vždy deklaruje idempotency scope, zdroj key, request digest, logical operation ID, expected state/revision/binding/activation snapshot a reconciliation contract. Runtime nejprve v jedné DB transakci claimne idempotency record, concurrency key, pre-side-effect checkpoint a případný side-effect intent. Teprve commitnutý dispatch outbox dovolí handleru nebo brokeru provést external effect.

Stejný key a stejný digest vrací původní current/terminal výsledek. Stejný key a jiný digest vrací `IDEMPOTENCY_CONFLICT`. JSON-RPC request ID, SSE connection a worker attempt idempotency scope nemění.

Přerušení request-scoped SSE samo nepotvrzuje aplikaci ani neaplikaci effectu. `READ_ONLY` a skutečně `SAFE_RETRY` call lze znovu vydat podle kontraktu. Mutující call se opakuje pouze se stejným doménovým key a známým outcome. `EXTERNAL_NON_IDEMPOTENT` a `DESTRUCTIVE` se po timeoutu, disconnectu, lease expiry nebo pádu nespouštějí naslepo. Read-back rozhodne `CONFIRMED_APPLIED`, `CONFIRMED_NOT_APPLIED` nebo `UNKNOWN`; `UNKNOWN` vytvoří `MANUAL_REVIEW` a zablokuje nový dispatch.

Observable outcome a všechny crash body se řídí 49.4, 49.8 a 49.25.

### 10.13 Concurrency a ordering

Každý tool deklaruje concurrency scope:

- `NONE`,
- `PER_CALLER`,
- `PER_RESOURCE_KEY`,
- `PER_COMPONENT`,
- `GLOBAL`.

Concurrency key vzniká pouze z validovaných argumentů a immutable contractu. Claim je persistentní, scoped, leased a fenced; získá se spolu s přechodem callu do executable stavu. `NONE` neodstraňuje state-version, idempotency, terminal ani binding guards.

Runtime používá DB row/advisory lock pro krátkou transakci a lease s fencing tokenem pro práci přes externí hranici. DB lock se nedrží během handleru, HTTP, OpenAI, browseru, filesystemu ani systemd. Pozdní worker s neplatným tokenem nesmí zapsat tool result, state, task, checkpoint ani idempotency record.

Tool s pořadově závislým side effectem deklaruje sequence namespace, expected next rule a duplicate/gap policy. Sequence claim, state mutation, event a outcome se commitují atomicky. Přeskočený příkaz se nikdy implicitně nedopočítá.

Kanonický call lifecycle, MRTR/task races a terminalita jsou v 49.13.

### 10.14 Resources a prompts

MCP resource contract deklaruje:

- stabilní resource ID,
- URI nebo URI template,
- MIME type a optional size,
- read/output contract,
- `resources/list`, `resources/templates/list` a `resources/read` capability,
- operation scope a access binding,
- pagination a cursor policy,
- `ttlMs` a `cacheScope` policy,
- resource update capability,
- owner revision a digest.

Resource read vrací `resultType = complete`, content, `ttlMs` a `cacheScope`. Text a binary content odpovídá URI a MIME kontraktu. URI template používá validovaný RFC 6570 profil a bounded parameter expansion.

MCP prompt contract deklaruje:

- stabilní prompt ID a case-sensitive jméno,
- title a popis účelu,
- typed argument contract,
- immutable template revision,
- role/content strukturu,
- povolené resource references,
- model hints bez vynucení provideru,
- output rendering testy,
- owner revision a digest.

Prompt renderer odmítne chybějící nebo neznámé argumenty podle contractu a nevkládá Secret hodnotu bez explicitního bindingu. `prompts/list` je deterministic a cacheable; `prompts/get` vrací `resultType = complete` a server identity metadata.

### 10.15 `subscriptions/listen`

Moderní change notifications používají jedinou long-lived request operaci `subscriptions/listen`. Request filter může obsahovat:

```json
{
  "notifications": {
    "toolsListChanged": true,
    "promptsListChanged": true,
    "resourcesListChanged": true,
    "resourceSubscriptions": ["resource-uri"],
    "taskIds": ["task-id"]
  }
}
```

`taskIds` je přípustné pouze při current-request capability `io.modelcontextprotocol/tasks`. Unsupported optional filter field se neinterpretuje. Server buď odmítne schema-neplatný filter, nebo u validního filtru přijme podporovanou podmnožinu.

Server MUST:

1. ověřit protocol metadata, version, capabilities, access binding, exact resources/task IDs a filter,
2. vytvořit persistentní subscription evidence před prvním frame,
3. jako první protocol message pro tuto subscription odeslat `notifications/subscriptions/acknowledged`,
4. v acknowledgment uvést přesnou podmnožinu filtru, kterou skutečně přijal; nesmí tvrdit unsupported položku,
5. vložit `io.modelcontextprotocol/subscriptionId` do `_meta` acknowledgmentu a každé následující notification,
6. použít jako subscription ID exact JSON-RPC ID původního `subscriptions/listen` requestu,
7. neposlat notification type/resource/task, který není v acknowledged filteru,
8. neposílat na subscription stream request-scoped `notifications/progress` ani `notifications/message`,
9. při serverem iniciovaném graceful closure odeslat finální `resultType=complete` response se stejným request ID a subscription ID a pak stream zavřít.

Pokud server nepřijme žádnou položku validního filtru, odešle acknowledgement s prázdnou mapou a bezprostředně graceful completion. To je úspěšná prázdná subscription, nikoli implicitní subscribe-all.

Klient ruší HTTP subscription zavřením jejího SSE streamu. Server po client disconnectu přestane emitovat a nemusí poslat finální response. Na stdio používá client `notifications/cancelled` s request ID. Abrupt server/transport close bez final response znamená unexpected disconnect, nikoli potvrzení, že nedošlo ke změnám.

Subscription stream nepoužívá SSE event IDs, `Last-Event-ID`, replay ani redelivery. Po disconnectu klient otevře nový request s novým JSON-RPC ID a provede potřebný `server/discover`, list/read nebo `tasks/get` refresh. Autoritativní stav se nikdy neodvozuje z úplnosti notification streamu.

Multiple concurrent subscriptions jsou povolené. Každý request ID musí být unique po celou dobu in-flight subscription. Notification se stejným subscription ID, ale jiným exact source server/access contextem je protocol violation.

### 10.16 Explicitní stavové handles a Tasks extension

MCP nemá protocol-level session. Cross-call application state se na wire přenáší pouze explicitní hodnotou. MCP core nedefinuje state-handle message type ani `handles/*` method; handle je z pohledu wire běžný string v tool resultu a běžný argument následného tool callu. KájovoCML `mcp_state_handle` je interní enforcement evidence a nesmí být vydáván za novou MCP protocol feature.

State handle je opaque, cryptographically random, bounded a expirovatelný. Server při každém použití ověřuje owner component/tool revision, source access/execution context, exact binding revision, activation epoch, state namespace, current handle stateVersion a expiry. Handle není authorization grant; public OWNER nebo endpoint-local access se ověřuje znovu při každém callu. Close je idempotentní CAS. Closed/expired/unknown handle vrací tool execution error `MCP_STATE_HANDLE_CLOSED|EXPIRED|INVALID`; ID se nerecykluje.

#### 10.16.1 Tasks capability a vytvoření tasku

Dlouhotrvající `tools/call` MAY použít extension `io.modelcontextprotocol/tasks` pouze tehdy, když:

- server ji deklaruje v `server/discover`,
- client ji deklaruje v capabilities konkrétního `tools/call`,
- tool revision task augmentation povoluje,
- durable task je commitnutý před vydáním task ID.

V revision `2026-07-28` task augmentation podporuje pouze `tools/call`. Client task nevyžaduje ani nevynucuje; server rozhoduje per request a client musí umět přijmout běžný `complete|input_required` result i `resultType=task`. Server nesmí vrátit task bez current-request capability. Nemůže-li bez tasku operation obsloužit, vrátí `-32021` s required extension.

CreateTaskResult má `resultType=task` a obsahuje:

- `taskId`,
- lowercase status `working|input_required|completed|failed|cancelled`,
- `createdAt`, `lastUpdatedAt`,
- `ttlMs` jako integer duration od creation nebo `null`,
- volitelný integer `pollIntervalMs`,
- volitelný `statusMessage`.

Task je před response durable a okamžitě resolvable přes `tasks/get`. Potřebuje-li server client input ještě před rozhodnutím o vytvoření tasku, dokončí tento MRTR synchronně na původní methodě před CreateTaskResult; po vydání tasku se další input přenáší pouze přes task `inputRequests` v `tasks/get` a `inputResponses` v `tasks/update`, nikoli MRTR retry původního callu. Je svázán s exact source/access contextem, original tool/revision/request digestem, binding-set revision, activation epoch, logical operation ID a idempotency key. Přežije HTTP disconnect, worker restart a process replacement. Task ID není credential ani business idempotency key.

#### 10.16.2 `tasks/get`

`tasks/get` vyžaduje current-request Tasks capability, `Mcp-Method: tasks/get` a `Mcp-Name` shodné s `params.taskId`. Vrací `resultType=complete` a complete current task projection:

- `working` — bez terminal payloadu,
- `input_required` — se všemi current outstanding `inputRequests`,
- `completed` — s exact final result původního tool callu; `isError=true` tool result je stále `completed`,
- `failed` — pouze s JSON-RPC error, který způsobil protocol-level execution failure,
- `cancelled` — bez success resultu.

Invalid, wrong-context nebo purged `taskId` vrací `-32602` a stable `MCP_TASK_NOT_FOUND`. Task projection je serverově linearizovaný snapshot s `lastUpdatedAt`; client nesmí kombinovat pole z různých polls.

#### 10.16.3 `tasks/update`

`tasks/update` vyžaduje Tasks capability a posílá `inputResponses` pro keys aktuálně zveřejněné v `inputRequests`. Každý input request key je unique po celou lifetime tasku a server jej nikdy nerecykluje pro jiný request.

Server:

- ignoruje unknown, already-satisfied a superseded keys a auditně je eviduje,
- MAY přijmout partial subset; task zůstane `input_required`, dokud zbývá required input,
- validuje known response proti exact request schema/digest,
- v jedné transakci persistuje accepted responses, označí odpovídající keys consumed a rezervuje/enqueue resume,
- vrátí empty `resultType=complete` acknowledgment; ACK znamená přijetí inputu, nikoli již viditelný přechod tasku.

Exact replay stejného response digestu je idempotentní. Jiný payload pro již consumed known key se nesmí znovu aplikovat; je ignorován na wire podle task extension a vyvolá security/debug evidence. Interní stateVersion a CAS chrání race, ale client nemusí ani nesmí dodávat ne-standardní required `expectedTaskStateVersion`.

#### 10.16.4 `tasks/cancel` a terminal race

`tasks/cancel` vyžaduje Tasks capability a vrací empty `resultType=complete` ACK po durable zápisu cancellation intentu. ACK není garance, že task dosáhne `cancelled`. Cancellation je cooperative a eventually consistent; task může skončit `completed` nebo `failed`, pokud corresponding terminal outcome commitne dříve, než cancellation worker bezpečně zastaví/reconciliuje práci.

Terminality rozhoduje první validní root-row terminal commit:

- `completed`, `failed` a `cancelled` jsou immutable,
- cancel intent sám není terminal state,
- po cancel intentu se nezahajuje nový child side effect,
- already dispatched mutující effect se reconciliuje,
- `cancelled` lze commitnout pouze při známých outcomes a dokončeném required cleanup,
- opakovaný cancel vrací ACK/current terminal state bez nové práce.

`notifications/cancelled` se pro task cancellation nepoužívá.

#### 10.16.5 Task TTL, expiry a notifications

`ttlMs` se může během lifetime změnit. KájovoCML z něj odvozuje serverový `expiresAt` pod DB time. Po expiry reaper pod task row lockem:

1. pokud task již je terminal, zachová terminal payload do retention purge,
2. pokud je nonterminal, zapíše cancellation/expiry intent, reconciliuje pending effects a terminalizuje jej jako `failed` s JSON-RPC error `MCP_TASK_EXPIRED`, nebo jako `cancelled`, pokud byla cancellation bezpečně dokončena,
3. pokud outcome zůstává unknown, `failed` error nese `MCP_OUTCOME_UNKNOWN` a manual-review reference,
4. zachová tombstone po retention; teprve po purge vrací `MCP_TASK_NOT_FOUND`.

Client SHOULD respektovat `pollIntervalMs`; server může příliš častý polling rate-limitovat. Task status notifications používají `notifications/tasks` na `subscriptions/listen` s `taskIds`. Každá notification obsahuje úplný current DetailedTask stejný jako současný `tasks/get`. Progress/message notifications se pro task obecně nepřenášejí na subscription stream.

Tool bez negotiated Tasks extension musí skončit v bounded request timeoutu, vrátit MRTR input requirement nebo explicitní error. Nesmí vytvořit skrytý background job bez durable handle.

### 10.17 Exposure, aliasy a kolize

MCP server vystavuje pouze prvky active revision odpovídající exact endpoint/internal bindingu a request capability profile. Discovery exposure se počítá serverově a ukládá binding decision evidence.

Při agregaci více serverů vzniká model-visible alias:

```text
<stable-server-alias>.<source-tool-name>
```

Alias je case-sensitive, unikátní v agent revision, verzovaný a odkazuje na exact source server, tool revision a digest. `serverInfo.name` se nepoužívá jako jediný collision key. Kolize se nikdy neřeší tichým přepsáním.

Změna source digestu invaliduje alias binding a agent readiness. Compatible revision se přijme pouze po compatibility evaluation a nové binding revision.

### 10.18 Tool invocation pipeline

Každé `tools/call` probíhá v tomto pořadí:

1. HTTP/TLS/origin/rate preflight,
2. JSON-RPC envelope a unique in-flight request ID validation,
3. protocol header/body equality validation,
4. per-request `_meta`, version a capability validation,
5. public credential validation nebo internal execution-context resolution,
6. server/release/readiness a current activation epoch resolution,
7. tool name, active revision a exposure resolution,
8. input schema a `x-mcp-header` validation,
9. exact contract binding, operation scope, approval a Secret binding evaluation,
10. vytvoření/načtení logical operation a idempotency recordu,
11. expected state/revision/binding/activation CAS a persistentní concurrency claim,
12. pre-side-effect checkpoint, cancellation snapshot a případný side-effect intent v jedné transakci,
13. fenced cancellation-aware handler invocation; external/KCIP calls pouze přes context API,
14. persistence raw attempt outcome,
15. resultType, output schema, content a error validation,
16. reconciliation každého nejednoznačného side effectu,
17. guarded terminal idempotency/result write, post-checkpoint, event, audit a outbox v jedné transakci,
18. request-scoped progress/final response emit pouze z persistovaného stavu,
19. metrics, trace a debug log.

Crash mezi libovolnými dvěma body se rozhoduje podle 49.25. Zejména handler completion v process memory není success a pozdní fence nemůže terminalizovat call. Rutinní MCP invocation nepoužívá OpenAI model.

### 10.19 Registrace externího MCP serveru a legacy adapter

Registrace externího serveru provede:

1. URL normalization, transport a SSRF policy,
2. DNS/TLS/hostname/redirect/Origin a public access profile,
3. exact auth-binding fingerprint,
4. deterministický era probe,
5. `server/discover` a protocol version selection,
6. úplné paginated list snapshots a aggregate digests,
7. schema dialect, local/vendored `$ref`, `x-mcp-header` a output validation,
8. cache, subscriptions, MRTR a Tasks verification,
9. deterministic harmless tool test nebo explicitní `UNVERIFIABLE` blocker,
10. endpoint/internal/Secret bindings,
11. monitoring, drift a compatibility policy.

#### 10.19.1 HTTP era decision

Dual-era HTTP klient nejprve odešle syntakticky validní moderní `server/discover` s required headers a `_meta`:

- validní DiscoverResult nebo recognized modern error `-32020`, `-32021`, `-32022` znamená `MODERN`,
- `404` s validním JSON-RPC `-32601` a matching ID také znamená moderní JSON-RPC endpoint, ale porušuje povinný discover contract a registrace je `PROTOCOL_INVALID`; klient nesmí fallbacknout,
- `400|404|405` s prázdným nebo ne-recognized modern body je pouze `LEGACY_CANDIDATE`; teprve explicitní legacy `initialize`/transport probe smí potvrdit `LEGACY`,
- `401`, `403`, `407`, `429`, TLS/DNS failure, timeout, redirect policy failure nebo `5xx` znamená `ERA_INDETERMINATE`; fallback je zakázán, protože chyba může být auth/provozní,
- `2xx` s malformed JSON, wrong ID, invalid resultType nebo header/body contradiction znamená `PROTOCOL_INVALID`, nikoli legacy,
- moderní error s podporovanými versions vede k retry s vzájemně podporovanou revision, ne k legacy fallbacku.

Deprecated HTTP+SSE probe přes GET se provede pouze po potvrzeném legacy candidate výsledku a pouze v explicitním adapteru. Moderní endpoint na GET/DELETE musí vrátit 405; to samo bez body ještě nepotvrzuje legacy.

#### 10.19.2 stdio era decision

Dual-era stdio klient nejprve odešle `server/discover`. Validní result nebo recognized modern error znamená `MODERN`. Non-modern error, silence do bounded probe timeoutu nebo legacy-specific response dovolí explicitní `initialize` fallback. Malformed response, wrong ID nebo mixed-era traffic znamená `PROTOCOL_INVALID`, ne automatický fallback.

Era decision se cacheuje pro lifetime server processu u stdio a pro `(scheme, host, port, endpoint path, auth-binding fingerprint, TLS identity)` u HTTP. Invaliduje se při process restart evidence, TLS/endpoint/auth změně, deployment/release driftu, recognized era contradiction nebo explicitním verify.

Legacy adapter může interně provést `initialize`, session handling, standalone SSE nebo staré subscriptions, ale směrem do KájovoCML poskytuje moderní stateless request contract, explicitní business handles, normalized `resultType`, stable errors, exact idempotency/reconciliation a audit. Legacy transportní session není autoritou agentní, uživatelské ani business kontinuity.

Externí annotations, instructions, titles, icons, descriptions a self-reported serverInfo jsou untrusted metadata. KájovoCML je může zobrazit, ale access binding, approval, side-effect class, retry, Secret policy, compatibility a OpenAI projection odvozuje pouze z vlastní verified registration revision.

### 10.20 MCP generation contract

GenerationSpecification pro MCP obsahuje nejméně:

- server purpose a exposure,
- protocol revision a era,
- capabilities a extensions,
- tools/resources/templates/prompts,
- exact schemas a dialects,
- OpenAI projection compatibility pro agent bindings,
- side effects, retry, idempotency a concurrency,
- MRTR a approval requirements,
- explicit state handles,
- Tasks extension requirements,
- cache TTL/scope a subscriptions,
- exact bindings, Secrets a external targets,
- runtime/resource limits,
- protocol, tool-execution a business errors,
- monitoring a SLO,
- deterministic tests,
- activation a rollback.

Generator vytváří manifest, source, schema bundle, handler, tests, server/discover profile, request metadata validator, transport adapter, cache/subscription behavior, monitoring, UI registration a dokumentaci. Každý generated tool má implementovaný handler a reálný acceptance case.

### 10.21 MCP verification gates

Povinné moderní MCP gates zahrnují:

- manifest, component, server revision a exact activation-set contract,
- protocol revision `2026-07-28`, MODERN era a POST-only endpoint,
- `server/discover` request bez method-specific params a validní complete result,
- per-request `_meta`, required client info/capabilities a result server `_meta`,
- JSON-RPC ID type/echo/duplicate-in-flight/notification rules,
- HTTP status versus JSON-RPC error map a processing precedence,
- `Origin`, auth, `Accept`, content type, body size, GET/DELETE a no-batch negative cases,
- required `MCP-Protocol-Version`, `Mcp-Method`, method-specific `Mcp-Name`, duplicate-header a header/body mismatch cases,
- request-scoped SSE framing, progress/message scoping, final response, wrong-ID rejection, disconnect cancellation a absence `id`/`Last-Event-ID` resume,
- standard JSON-RPC parse/invalid/method/params/internal errors a reserved MCP `-32020|-32021|-32022`,
- required known `resultType` a rejection unknown/unnegotiated value,
- deterministic complete list order, opaque cursor, empty cursor, invalid cursor a page cache semantics,
- `ttlMs`, `cacheScope`, private-context isolation, MRTR no-cache a notification invalidation,
- JSON Schema 2020-12 default, explicit dialect, local/vendored `$ref`, external-ref denial a complexity limits,
- native schema versus OpenAI strict projection, inverse validation, digest invalidation a impossible-lossless blocker,
- valid/invalid `x-mcp-header`, static path, safe integer, null/absent handling, Base64 sentinel a duplicate singleton header,
- valid/invalid `tools/call`, unknown tool protocol error a actionable tool execution error,
- `structuredContent`, output schema, content rendering a schema-invalid post-effect reconciliation,
- MRTR only na supported methods, at-least-one field, capability mismatch, exact requestState echo, expiry/consume/replay a partial/unknown input responses,
- `subscriptions/listen` first acknowledgment, accepted filter subset, subscription ID, multiple subscriptions, no request progress, client cancel a server graceful completion,
- explicit state handle jako ordinary tool data, exact access/binding check, close/expiry/reuse,
- Tasks capability per request, task only pro `tools/call`, durable create, task routing headers, get/update/cancel, partial input, cooperative cancellation, terminal race, TTL/purge a notifications,
- binding match/mismatch, stale revision/release/contract digest/binding-set/activation epoch,
- idempotency same-key/same-digest replay, different-digest conflict, disconnect/timeout/retry a non-idempotent reconciliation,
- concurrency key, sequence duplicate/stale/gap a fencing takeover,
- Secret resolution, egress a no direct generated-handler bypass,
- external server era matrix, no fallback on auth/network/5xx, modern recognized errors, legacy adapter a mixed-era rejection,
- compatibility classification, schema evolution, agent projection invalidation a atomic activation/rollback,
- health, readiness, heartbeat, monitoring, raw wire evidence, logs a audit,
- canonical HTTPS E2E a skutečný agent-to-MCP call.

Legacy gates se spouštějí pouze pro revision s explicitním compatibility profilem a nikdy nenahrazují moderní gates. Dual-era adapter musí projít moderní gates na své KájovoCML straně a samostatné revision-specific legacy gates na upstream straně.

Povinná gate nesmí být `SKIPPED`. Nepoužitelná gate používá `NOT_APPLICABLE` s machine-readable důvodem přímo odvozeným ze schváleného contractu. `UNVERIFIABLE`, `UNKNOWN`, malformed evidence nebo test, který neprošel skutečnou wire hranicí, je blocking failure.

### 10.22 Compatibility, schema evolution a activation

MCP contract compatibility používá:

```text
EXACT
BACKWARD_COMPATIBLE
CONDITIONALLY_COMPATIBLE
BREAKING
UNKNOWN
```

Význam:

- `EXACT` — canonical contract digest je shodný.
- `BACKWARD_COMPATIBLE` — každý request povolený předchozím contractem zůstává validní, každý response pozorovatelný starým consumerem zůstává v jeho contractu a nezměnily se side-effect, retry, idempotency, deadline, error, handle/task, cache, subscription, binding ani ordering semantics.
- `CONDITIONALLY_COMPATIBLE` — kompatibilita existuje pouze přes explicitní verzovaný adapter/projection a nové verification evidence; přímý old binding se nepoužije.
- `BREAKING` — existuje validní starý request/consumer state, který nový contract odmítne, jinak interpretuje nebo mu poskytne nekompatibilní outcome.
- `UNKNOWN` — engine kompatibilitu nedokázal; aktivace a automatic rebind jsou blokované.

Compatibility engine porovnává protocol era/versions, methods, capabilities/extensions, names/URIs, request headers, schemas/dialects/references, required/optional/default semantics, input accepted set, output guaranteed set, resultType, content rendering, protocol/tool errors, side-effect/retry/idempotency, concurrency/ordering, deadline/cancellation, operation scope, exact bindings, MRTR, requestState, state handles, Tasks, cache/pagination, subscriptions a wire limits.

Nejméně tyto změny jsou `BREAKING`:

- modern ↔ legacy era změna bez adapteru,
- odstranění supported version, method, capability, extension, tool/resource/prompt nebo error varianty používané bindingem,
- přidání required inputu nebo zúžení accepted input typu/enum/range/pattern,
- odstranění output property/varianty nebo rozšíření output hodnot mimo to, co strict consumer umí validovat,
- změna `x-mcp-header` name/path/type nebo jeho přidání k existujícímu toolu bez nové client verification,
- změna side-effect, retry, idempotency key derivace, concurrency key, ordering, deadline nebo cancellation semantics,
- změna target operation, binding, Secret/external target relation nebo activation lineage,
- změna MRTR capability/requestState semantics, task eligibility/status/result/TTL semantics nebo state-handle scope,
- změna cacheScope z `private` na `public`, cursor context nebo notification meaning,
- změna protocol error na tool error nebo naopak, pokud mění recovery.

Přidání optional input property, rozšíření accepted input setu, přidání list item nebo optional output metadata může být `BACKWARD_COMPATIBLE` pouze tehdy, když předchozí `additionalProperties`, strict consumer, OpenAI projection, header map a runtime adapter takovou změnu skutečně přijmou. Samotné tvrzení autora nestačí; rozhodují generated differential test vectors.

Každá schema nebo contract digest změna, i když je `BACKWARD_COMPATIBLE`, vytváří novou immutable revision a novou binding revision. Agent/OpenAI projection se znovu sestaví a ověří. Runtime nesmí potichu přesměrovat exact old binding na nový digest.

Server revision, tool/resource/prompt revisions, schema bundle, runtime release, binding set, agent projection/binding a související policies se aktivují jako jeden activation set. Candidate agent se ověřuje proti candidate MCP digestu. Pointer switch je atomický; částečné přepnutí není přípustné.

Po aktivaci gateway provede přes kanonický hostname `server/discover`, úplné list snapshots, metadata/header negative tests, schema/projection validation, deterministic smoke call, subscription/task test podle contractu a skutečný agent-to-MCP call. Jakýkoli blocking mismatch vrátí celý activation set na předchozí kompatibilní pointers a invaliduje candidate discovery cache.

### 10.23 MCP monitoring a provoz

MCP monitoring obsahuje:

- `server/discover` success rate a latency,
- unsupported-version, missing-capability a header-mismatch count,
- per-request protocol/client capability fingerprints,
- list/read cache hit, expiry a stale refresh,
- discovery digest drift,
- tool call count, latency, error a timeout rate,
- schema a `x-mcp-header` rejection count,
- binding mismatch a approval count,
- idempotency hit/conflict a reconciliation outcome,
- request-stream disconnect a cancellation outcome,
- active `subscriptions/listen` requests a notification delivery,
- MRTR input-required/resume/expiry,
- active state handles a expiry,
- task state, polling, input a cancellation,
- resource a prompt usage,
- release/readiness/heartbeat,
- per-tool SLO a alert policy.

Každá metrika je spojitelná s component, revision, release, protocol version, method/tool, source execution kind/ID, request ID, logical operation ID a correlation ID.

### 10.24 MCP UI

Stránka **MCP servery a nástroje** poskytuje:

- server catalog, protocol era a exact active revision/release,
- `server/discover` tester,
- per-request metadata, JSON-RPC ID a HTTP header inspector,
- HTTP status/JSON-RPC error map tester včetně malformed a notification cases,
- modern/legacy era probe a evidence classification,
- supported versions, capabilities a extensions,
- immutable discovery snapshots, cache hints a diff,
- tools/resources/templates/prompts explorer,
- JSON Schema dialect, `$ref` a OpenAI projection viewer,
- `x-mcp-header` inspector,
- request builder s validací podle exact contractu,
- typed live output a raw JSON-RPC/SSE detail s persisted wire evidence a delivery state,
- protocol error, tool-execution error, duplicate-ID a retry/reconciliation tester,
- MRTR input request/resume console,
- `subscriptions/listen` filter a live notification console,
- explicit state handle browser,
- task get/update/cancel console,
- binding, operation scope a approval tester,
- agent binding graph včetně exact digestů a aliasů,
- idempotency, concurrency a reconciliation diagnostiku,
- runtime, health, heartbeat a monitoring,
- E2E, compatibility, activation set a rollback,
- usage, logs a audit,
- create, generate, import, verify, activate, enable, disable, repair, archive a deregistration.

Každá akce používá centrální object-action registry a má stejnou API a chatovou operaci.


### 10.25 Deterministická wire edge-case matrix

Každý případ má jediný wire význam a jedinou recovery cestu:

| Případ | Side effect | Wire outcome | Jediná recovery |
|---|---|---|---|
| invalid UTF-8, malformed JSON | žádný | HTTP 400, `-32700`, bez ID | opravit bytes, nový request |
| duplicate JSON member | žádný | HTTP 400, `-32600`, `MCP_DUPLICATE_JSON_MEMBER` | opravit serializer |
| batch nebo client JSON-RPC response | žádný | HTTP 400, `-32600` | poslat právě jeden request/notification |
| `id=null` nebo invalid ID type | žádný | HTTP 400, `-32600` | nový validní ID |
| duplicate in-flight ID | druhý request žádný | HTTP 400, `-32600`, `MCP_DUPLICATE_IN_FLIGHT_REQUEST_ID` | počkat na první nebo použít jiné ID |
| stejné ID po terminal response | podle nového requestu | běžný nový request | business dedupe pouze idempotency keyem |
| invalid `Origin` | žádný | HTTP 403 | opravit origin policy/request |
| missing/invalid auth | žádný | HTTP 401/403 | opravit existující credential/binding |
| unsupported HTTP method | žádný | HTTP 405 | POST na MCP endpoint |
| unsupported content/accept/body size | žádný | 415/406/413 | opravit transport request |
| missing/mismatched standard/custom header | žádný | HTTP 400, `-32020` | obnovit schema/headers a nový request |
| unsupported protocol version | žádný | HTTP 400, `-32022` se supported listem | retry vzájemně podporovanou version |
| missing current-request capability | žádný | HTTP 400, `-32021` | core fallback nebo nový request s capability |
| unknown method | žádný | HTTP 404, `-32601` | nepoužívat method / opravit revision |
| unknown tool, invalid params/cursor/taskId | žádný | HTTP 200, `-32602` | refresh/list nebo opravit params |
| stale server/tool/binding/contract digest/epoch | žádný | protocol/binding error s refresh directive | fresh discover/binding a nový command |
| stejné business key + stejný digest | žádný nový | replay current/terminal canonical outcome | přijmout replay |
| stejné business key + jiný digest | žádný | `IDEMPOTENCY_CONFLICT` | nový key pouze pro novou operaci |
| SSE disconnect před intentem | žádný | cancellation bez outcome | safe retry podle contractu |
| disconnect po intentu před dispatch | žádný | persisted cancel, dispatch zablokován | načíst canonical cancelled outcome |
| disconnect po possible external dispatch | možný | reconciliation; nikdy safe-assumed retry | počkat na confirmed outcome/manual review |
| disconnect po terminal DB commit | již canonical | frame chybí, outcome zůstává | retry stejným business key |
| timeout s progress | podle callu | idle může pokračovat, absolute deadline platí | podle deadline/reconciliation |
| cancel versus success | podle prvního terminal commitu | first valid terminal commit wins | načíst canonical terminal state |
| unknown/unnegotiated `resultType` | žádný další model turn | protocol violation | refresh capability/adapter; bez blind retry |
| schema-invalid success před effectu | žádný | `-32603` | oprava serveru a safe retry |
| schema-invalid result po applied effectu | potvrzený effect | non-retryable failure s applied evidence | oprava/read-back, ne duplicate call |
| MRTR bez `inputRequests` i `requestState` | žádný | invalid response | oprava serveru |
| MRTR retry s changed original args | žádný nový | requestState/digest conflict | nový logical call |
| MRTR unknown/already-satisfied response key | žádný | key ignorován a auditován | pokračovat s outstanding keys |
| MRTR exact replay consumed state se stejným response digestem | žádný nový | replay current/terminal canonical outcome | přijmout replay |
| MRTR consumed state s jiným recognized response digestem | žádný nový | `MCP_REQUEST_STATE_CONSUMED` | založit nový logical call pouze podle business záměru |
| expired/invalid/consumed-changed requestState | žádný nový | `-32602` + stable state error | restart continuation podle contractu |
| subscription ack není první | žádný authoritative state | protocol violation, stream close | nový listen + full refresh |
| notification mimo acknowledged filter | žádný authoritative state | protocol violation | stream close + full refresh |
| subscription disconnect | neznámé zmeškané změny | žádný replay | nový listen + discover/list/read refresh |
| invalid/expired/closed state handle | žádný | tool execution error | vytvořit nový handle nebo ukončit flow |
| task create response před durable commit | zakázáno | conformance failure | opravit server; task se nepoužije |
| task update unknown/satisfied key | žádný nový | ignorováno, ACK known subset | poll current outstanding set |
| task cancel ACK a současný completion | možný | cooperative; first terminal commit wins | `tasks/get` canonical state |
| task expiry | podle evidence | failed/cancelled/manual-review tombstone | nový call pouze podle retry safety |
| invalid cursor u cached traversal | žádný | `-32602` | zahodit všechny pages, restart bez cursoru |
| cache notification/drift během TTL | žádný | snapshot okamžitě stale | fresh request; execution stale cache nepoužije |
| modern probe recognized modern error | žádný | MODERN | opravit version/header/capability, bez fallbacku |
| probe 401/403/429/5xx/network/TLS timeout | žádný | ERA_INDETERMINATE | opravit provoz/auth, poté re-probe |
| 400/404/405 bez recognized modern body | žádný | LEGACY_CANDIDATE | explicitní legacy probe |
| malformed 2xx/wrong ID/mixed era | žádný | PROTOCOL_INVALID | registraci zablokovat |

### 10.26 Stabilní MCP wire error map

`error.data.stableCode` používá nejméně:

```text
MCP_PARSE_ERROR
MCP_INVALID_REQUEST
MCP_DUPLICATE_JSON_MEMBER
MCP_DUPLICATE_IN_FLIGHT_REQUEST_ID
MCP_HEADER_MISMATCH
MCP_PROTOCOL_VERSION_UNSUPPORTED
MCP_CLIENT_CAPABILITY_MISSING
MCP_METHOD_NOT_FOUND
MCP_INVALID_PARAMS
MCP_TOOL_NOT_FOUND
MCP_DISCOVERY_STALE
MCP_CONTRACT_BREAKING
MCP_SCHEMA_DIALECT_UNSUPPORTED
MCP_SCHEMA_REFERENCE_UNRESOLVED
MCP_SCHEMA_DRIFT
MCP_RESULT_TYPE_UNKNOWN
MCP_RESULT_SCHEMA_INVALID
MCP_REQUEST_STATE_INVALID
MCP_REQUEST_STATE_EXPIRED
MCP_REQUEST_STATE_CONSUMED
MCP_CURSOR_INVALID
MCP_SUBSCRIPTION_PROTOCOL_VIOLATION
MCP_STATE_HANDLE_INVALID
MCP_STATE_HANDLE_EXPIRED
MCP_STATE_HANDLE_CLOSED
MCP_TASK_NOT_FOUND
MCP_TASK_EXPIRED
MCP_TASK_CAPABILITY_REQUIRED
MCP_BINDING_STALE
MCP_CONTRACT_DIGEST_STALE
MCP_DEADLINE_EXCEEDED
MCP_CANCELLED
MCP_OUTCOME_UNKNOWN
MCP_ERA_INDETERMINATE
MCP_PROTOCOL_INVALID
MCP_INTERNAL_FAILURE
```

Mapping:

- parse/invalid request/method/params používá standardní JSON-RPC codes,
- `MCP_HEADER_MISMATCH` používá `-32020`; `MCP_CLIENT_CAPABILITY_MISSING` a `MCP_TASK_CAPABILITY_REQUIRED` používají `-32021`; `MCP_PROTOCOL_VERSION_UNSUPPORTED` používá `-32022`,
- actionable tool/business failure používá `isError=true`,
- internal/reconciliation failure používá `-32603` nebo persisted tool error podle místa vzniku,
- stable code, classification, retry directive, correlation ID, logical operation ID a audit reference jsou v `error.data` nebo result `_meta`,
- stejný stable code má ve všech UI/API/chat/log views stejný význam a recovery; text message se může lokalizovat, code nikoli.

### 10.27 Wire evidence a raw retention

Každý MCP request ukládá bounded raw request/response headers, exact body bytes nebo content digest podle retention policy, parsed JSON-RPC ID type/value, method/name, protocol version, capability/extension digests, access/binding fingerprint, HTTP status, content type, SSE message sequence, disconnect point, logical operation/correlation, result/error digest a audit reference.

Raw evidence je přístupná OWNERovi podle trust modelu. Verification a incident analysis musí být schopné prokázat, zda chyba vznikla před handlerem, před dispatch intentem, po dispatchi, po terminal commitu nebo pouze při delivery response. Bez této evidence se mutující ambiguous call klasifikuje `MCP_OUTCOME_UNKNOWN`; nesmí se automaticky opakovat.

## 11. AI agent management

### 11.1 Kanonický AI agent

AI agent je stabilní objekt svázaný s kanonickou component a runtime identitou. Agent plní deklarovaný účel prostřednictvím OpenAI modelu, immutable instrukcí, exact bound nástrojů, typovaných delegací, paměti, guardrails a deterministické běhové politiky.

Agent definition obsahuje:

- stabilní agent ID,
- component ID a runtime identity kind,
- název, účel a provozní odpovědnost,
- agent mode,
- active revision,
- enabled a lifecycle state,
- trigger relations,
- monitoring, evaluation a release relation,
- audit a ownership metadata.

Agent definition neobsahuje měnitelný prompt nebo tool list. Veškeré chování je v immutable agent revision.

### 11.2 OpenAI agentní runtime

KájovoCML NG používá pro serverové agentní běhy:

- oficiální OpenAI JavaScript/TypeScript SDK `openai`,
- oficiální OpenAI Agents SDK `@openai/agents`,
- Responses API jako výchozí modelovou hranici,
- Zod a kanonické JSON Schema pro runtime validaci,
- jediný Secret Manager stable name `OPENAI_API_KEY`.

SDK verze jsou přesně pinované v `pnpm-lock.yaml`. Upgrade SDK proběhne pouze přes dependency change review, contract testy, MCP testy, agent regression suite a production acceptance. Aplikační kód nepoužívá neoficiální provider wrapper ani alternativní AI provider.

Agentní runtime běží výhradně na serveru. Browser nikdy nezíská dlouhodobý OpenAI API key ani přímou pravomoc volat produkční agentní tools.

Oficiální OpenAI klient, Agents SDK model/session adapter, submit/retrieve lifecycle a versioned resume se řídí kapitolou 52.

### 11.3 Agent revision contract

Agent revision obsahuje:

- `schemaVersion`, revision ID a digest,
- purpose a success definition,
- immutable system instructions,
- instruction template variables a jejich schema,
- selected OpenAI model,
- systémem odvozené model settings,
- strict input schema,
- strict final output schema,
- run mode a trigger policy,
- exact MCP/function tool bindings,
- agent-as-tool bindings,
- handoff bindings,
- input, output a tool guardrails,
- human approval policy,
- session a memory policy,
- context compaction policy,
- budget, timeout a max-turn policy,
- concurrency a idempotency policy,
- failure a retry policy,
- monitoring profile,
- eval suite a promotion policy,
- secret bindings,
- browser interaction policy, povolené session kinds, operation scopes a automation bindings,
- compatibility relations,
- source specification lineage,
- authority-lineage profile, OWNER-intent derivation, content-provenance contract, povolené value-derivation slots a Secret-use context template.

Revision je immutable. Každý run zaznamená exact agent revision ID, digest, model, tool binding snapshot a guardrail snapshot.

### 11.4 Instrukce a prompt compilation

Instrukce se ukládají jako canonical UTF-8 text a samostatný typed variable contract. Každý vstupní segment má `contentRole`, `instructionAuthority`, provenance reference, raw digest a canonical digest. Compiler:

1. načte autoritativní instruction segments pouze z exact agent revision, immutable OWNER intentu, schválené specification nebo trusted runtime policy,
2. validuje všechny template proměnné a odmítne neznámé nebo chybějící hodnoty,
3. vloží pouze serverově schválený runtime context a exact authority-lineage/operation-context references,
4. oddělí web, MCP, external API, dokumenty, attachments, memory, logy, repository content, code a tool/agent outputs do typed data/evidence containers s provenance,
5. nepřenese textový obsah těchto containerů do authoritative `instructions`, ani když obsahuje role labels, JSON envelopes, XML/Markdown tags, „system“, „OWNER“, „ignore previous“, tool call nebo jinou napodobeninu instrukce,
6. přidá přesné tool a handoff usage constraints, target-resolution contract, value-derivation slots a Secret-use context template,
7. přidá strict output schema contract,
8. vytvoří compiled instruction digest, ordered input digest a provenance-manifest digest,
9. uloží exact render a segment map použitý v runu.

Proměnná určená pro data se nikdy nevkládá do instruction slotu pouhou string concatenation. Raw obsah se neztrácí ani povinně nerediguje; zůstává plně dostupný modelu a OWNERovi, ale jeho instruction authority se transformací, shrnutím, citací, dekódováním, OCR, retrievalem ani přenosem přes jiný tool nezvyšuje.

OWNER může explicitním příkazem určit exact artifact/revision/section jako delegovaný specification source. Takový source získá bounded `OWNER_DELEGATED_SOURCE` authority pouze pro deklarovaný účel a datové či business požadavky; nemůže přepsat platformní invarianty, exact binding, target ceiling, Secret destination nebo parent OWNER intent. Nejde o approval ani permission grant, ale o součást stejného OWNER intentu.

Instrukce nesmějí nahrazovat exact binding, schema, timeout, existující approval policy nebo side-effect enforcement. Textový pokyn modelu je behaviorální vodítko; runtime policy, operation context a serverový dispatch validator jsou autoritativní.

### 11.5 Agentní režimy

Agent používá jeden z režimů:

- `INTERACTIVE`,
- `EVENT_DRIVEN`,
- `SCHEDULED`,
- `REQUEST_RESPONSE`,
- `LONG_RUNNING`,
- `SUPERVISOR`,
- `SUB_AGENT`.

`SUPERVISOR` koordinuje bounded graph agentů. `SUB_AGENT` nelze spustit mimo deklarovaný parent binding, pokud revision výslovně nepovoluje samostatný run. `LONG_RUNNING` používá checkpointy, heartbeat a resumable run state.

### 11.6 Tool binding contract

Agent nepřebírá tool pouze podle jména. Každý tool binding obsahuje:

- binding ID,
- source kind `MCP_TOOL`, `FUNCTION_TOOL` nebo `AGENT_TOOL`,
- source component/agent ID,
- source revision ID,
- source contract digest,
- model-visible alias,
- exposure filter,
- required operation scope, stable canonical operation key a exact target resolver,
- declared data/value-derivation slots a argument-origin contract,
- input/output schema digest,
- side-effect a retry class,
- timeout override bounded source kontraktem,
- approval policy,
- result mode `STRUCTURED`, `TEXT`, `BOTH`,
- max result size a artifact policy,
- enabled condition,
- compatibility state.

Před každým runem binding compiler ověří:

- active source revision odpovídá povolenému digestu nebo explicitní kompatibilní relation,
- source component a revision jsou active,
- source runtime je ready,
- agent revision obsahuje exact active binding,
- Secret a external dependencies jsou resolvable,
- aliasy jsou unikátní,
- schemas lze převést na strict model tool schema.

Neplatný binding blokuje run stavem `AGENT_TOOL_BINDING_INVALID`. Runtime nikdy potichu neodebere povinný tool a nepokračuje s jiným chováním.

### 11.7 MCP tools v agentovi

MCP tool exposure používá moderní `server/discover`, active discovery snapshot, exact source tool revision, native schema digest a OpenAI projection digest. Agent může používat:

- hosted MCP volání přes Responses API pouze pro veřejně dosažitelný a výslovně schválený server,
- přímé Streamable HTTP MCP připojení z Agents SDK,
- KájovoCML function-tool adapter, který překládá call do KCIP přes kanonickou component gateway.

Interní KájovoCML komponenty používají function-tool adapter a KCIP, aby každé volání prošlo stejnou exact-binding, idempotency, approval, monitoring a audit hranicí. Přímé lokální obcházení gateway není povoleno.

Agentní MCP klient pro každý request sestaví:

- protocol version `2026-07-28`,
- client info,
- exact client capabilities a negotiated extensions,
- trace metadata,
- standard MCP HTTP headers,
- access channel a exact binding,
- logical operation ID a idempotency context.

Tool-list cache respektuje `ttlMs`, `cacheScope`, access/binding fingerprint a discovery digest. Runtime před call ověřuje current readiness, source/target revisions a binding digest i při cache hitu. Notification drift, TTL expiry, contract error nebo active revision změna cache invaliduje.

MCP result se normalizuje podle `resultType`:

- `complete` — output projde source output schema, projection adapterem a agent tool-output guardrailem,
- `input_required` — runtime uloží `requestState`, inputRequests a checkpoint a přejde do `WAITING_FOR_MCP_INPUT` nebo `WAITING_FOR_OWNER`,
- `task` — runtime uloží durable task handle a přejde do `WAITING_FOR_MCP_TASK`,
- neznámý resultType — call selže jako protocol violation před dalším modelovým kolem.

MRTR retry používá nový JSON-RPC ID, stejné logical operation ID, původní arguments digest a validované inputResponses. Task polling používá negotiated `io.modelcontextprotocol/tasks`, bounded backoff, task TTL a `tasks/get`; input se poskytuje přes `tasks/update`, cancel přes `tasks/cancel`.

Agentní conversation/session není MCP session. Cross-call MCP stav používá pouze explicitní server-minted handle v tool argumentech. Agent runtime uchovává handle jako typed tool output, kontroluje jeho scope/expiry metadata a nepovažuje jej za přístupový credential.

Moderní MCP request stream se po disconnectu neobnovuje přes Last-Event-ID. Runtime rozhodne nový request podle source retry/idempotency/reconciliation policy. Mutující neznámý výsledek se nevrátí modelu jako bezpečný retry; přejde do reconciliation nebo `MANUAL_REVIEW`.

### 11.8 Function tools a strict schema

Každý server-side function tool má Zod schema odvozené z kanonického JSON Schema. Modelová definice používá strict mode. Všechny object properties jsou deklarované, required a bez additional properties; nullable pole používá explicitní union.

Tool dispatcher přijme pouze známý tool call s platným call ID. Každý call:

1. uloží raw model output item,
2. validuje JSON argumenty,
3. vytvoří canonical argument JSON a digest,
4. provede tool input guardrails,
5. vyhodnotí approval policy,
6. zavolá kanonickou doménovou operaci,
7. validuje tool output,
8. provede tool output guardrails,
9. uloží výsledek a correlation,
10. vrátí function call output se stejným call ID.

Neznámý tool, malformed arguments nebo chybějící call ID se neprovádí a vrací typed model-visible chybu. Runtime zachovává všechny providerem požadované output items a jejich pořadí při následujícím modelovém kole.

### 11.9 Parallel tool calls

Výchozí agentní politika používá sekvenční tool dispatch. Paralelní volání je povoleno pouze tehdy, když všechny vybrané calls současně splňují:

- side-effect `READ_ONLY`,
- nezávislé concurrency keys,
- žádnou datovou nebo causation závislost,
- bounded souhrnný timeout a budget,
- deterministické spojení výsledků.

Při mutaci, workspace patchi, handoffu, approval nebo pořadově závislém callu runtime vynutí `parallel_tool_calls = false`. Model nemůže tuto policy změnit.

### 11.10 Agentní orchestrace

KájovoCML NG podporuje dva explicitní orchestration patterns:

- `MANAGER_WITH_AGENT_TOOLS` — root agent zachovává kontrolu a specialisté vracejí typed výsledek jako tool output,
- `HANDOFF` — řízení přechází na cílového agenta podle deklarovaného handoff contractu.

Agent revision určuje použitý pattern pro každý edge. Handoff nebo agent-as-tool binding obsahuje:

- source a target revision,
- účel delegace, parent authority lineage, OWNER-intent digest a operation-context projection,
- strict input schema,
- strict output schema,
- context projection s provenance a content-role mapou,
- povolené exact tools, target constraints, side-effect ceiling a Secret-use purposes zděděné z parent operation contextu,
- budget času, tokenů a turns,
- cancellation propagation,
- approval policy,
- return a error contract.

Graph musí být před activation validovaný. Cyklus je povolen pouze s explicitním max depth a monotonic progress condition; jinak je graph acyclic. Target agent nemůže získat širší tools, secrets nebo bindings, než obsahuje jeho vlastní revision a caller delegation contract. Child agent, handoff message ani agent-as-tool output nemůže změnit parent OWNER intent, target ceiling, operation class, argument-origin pravidla nebo Secret-use destination; může vrátit pouze typed proposal či výsledek do přesně určeného parent slotu.

### 11.11 Guardrails

Agent revision deklaruje:

- input guardrails,
- final output guardrails,
- function-tool input guardrails,
- function-tool output guardrails,
- KCIP/MCP pre-dispatch policy,
- KCIP/MCP post-result policy.

Input guardrails běží před prvním modelovým call. Final output guardrails běží před přijetím terminal output. Function-tool guardrails obalují každé vlastní function-tool volání. MCP a hosted tools, které nejsou přímo pokryté SDK tool-guardrail pipeline, procházejí ekvivalentní deterministic pre/post policy v KájovoCML gateway.

Guardrail výsledek používá `PASS`, `REJECT`, `REWRITE_INPUT`, `REWRITE_OUTPUT`, `REQUIRE_APPROVAL` nebo `TRIPWIRE`. Tripwire okamžitě zastaví další modelové a tool side effects a vytvoří typed terminal nebo review state.

### 11.12 Human-in-the-loop approval

Tool nebo delegace může deklarovat approval policy:

- `NEVER`,
- `ALWAYS`,
- `BY_SIDE_EFFECT_CLASS`,
- `BY_ARGUMENT_POLICY`,
- `OWNER_DECISION_REQUIRED`.

Approval request obsahuje exact run, agent revision, tool/binding, canonical arguments, argument digest, target, předpokládaný následek, expiration a correlation ID. Run přejde do `WAITING_FOR_OWNER` a uloží serializovatelný run checkpoint.

OWNER může request schválit nebo zamítnout v UI, centrálním chatu nebo API. Rozhodnutí je vázané na exact digest; změna argumentů vyžaduje nový request. Resume obnoví původní root run a stejný agent graph. Nejednoznačný nebo neúplný checkpoint se obnoví fail-closed a nevykoná další side effect.

Sticky approval je povoleno pouze jako explicitní policy revision nebo OWNER rozhodnutí s přesným server/tool/operation scope, expirací a auditní stopou.

### 11.13 Agent run lifecycle

Agent run používá stavy:

```text
QUEUED
PREPARING
RUNNING
WAITING_FOR_MODEL
WAITING_FOR_TOOL
WAITING_FOR_MCP_INPUT
WAITING_FOR_MCP_TASK
WAITING_FOR_AGENT
WAITING_FOR_OWNER
CHALLENGE_REQUIRED
PAUSED
SUCCEEDED
FAILED
CANCEL_REQUESTED
CANCELLED
MANUAL_REVIEW
```

Povolené hrany, terminal states a guards jsou přesně v 49.14. Přechodová transakce kontroluje current platform incarnation, lease/fence, state version, cancellation version, exact agent revision/graph/tool snapshot, checkpoint sequence, pending approval/input/task/side-effect set a deadline. Terminal stavy jsou immutable.

### 11.14 Agent run loop

Jeden run probíhá:

1. persist a idempotency/concurrency claim runu,
2. načtení exact agent revision, caller authority, immutable OWNER intentu a authority lineage,
3. input validation, content-provenance classification, instruction/data compilation, vytvoření frozen operation contextu a binding/readiness preflight,
4. input guardrails,
5. fenced checkpoint před model call a persistentní model-submit intent,
6. model call přes Agents SDK/Responses API,
7. persistence všech provider events/output items/usage,
8. guarded vyhodnocení final output, tool-call/handoff proposals, refusal nebo incomplete,
9. pro každý tool/handoff serverové sestavení semantic action planu, exact target resolution, argument-origin/value-derivation validation, Secret-use context a teprve poté pre-checkpoint, idempotency claim a side-effect intent,
10. dispatch pouze s current operation-context digestem a persistence exact outcome; nejednoznačný effect se reconciliuje před dalším modelovým kolem,
11. MCP `complete`, `input_required` nebo negotiated `task` persistence,
12. approval/MRTR/task wait a atomické enqueue resume,
13. bounded opakování modelového kola,
14. final schema a output guardrails,
15. příprava validovaných canonical session a memory items,
16. atomický commit run terminal resultu, canonical session/memory items, checkpointu, eventu, auditu a outboxu v jedné PostgreSQL transakci.

Model ani tool success se neposílá do dalšího kola, dokud není persistovaný. Modelový text, tool result nebo handoff output, který navrhuje nový tool, target, argument, secret nebo side effect, je vždy pouze proposal; continuation jej smí použít až po nové serverové semantic validation proti stejnému OWNER intentu. Překročení limitu vytváří přesný failure; limit se nezvyšuje neomezeným retry.

### 11.15 Model response stavy

Každý Responses API create je samostatný persistentní `ai_model_call` attempt. Ukládá immutable request descriptor, provider response ID, diagnostický provider request ID, request/input/instructions/tools/schema digests, model/settings/capability snapshot, ordered provider events/output items, usage, cost, latency, submit intent a recovery evidence.

Provider wire status a local semantic status se neztotožňují. Provider používá nejméně `queued`, `in_progress`, `completed`, `failed`, `cancelled` a `incomplete`. KájovoCML rozlišuje local `COMPLETED`, `INCOMPLETE`, `REFUSED`, `CANCELLED`, `FAILED` a `EXPIRED`; response s function calls může před uzavřením producing callu čekat v `WAITING_FOR_TOOL_OUTPUT`.

`REFUSED` vzniká z provider refusal itemu a není schema parse error. `INCOMPLETE` obsahuje exact provider incomplete details a není success. `MODEL_OUTPUT_SCHEMA_INVALID` vzniká až po terminal response a nezávislé serverové validaci.

Oficiální SDK create retry je vypnutý. Submit bez persistovaného provider response ID se opakuje pouze při serverově potvrzeném `CONFIRMED_NOT_SUBMITTED`. Jakmile byl network dispatch zahájen, timeout, connection error nebo process crash bez response ID znamená `MODEL_SUBMIT_OUTCOME_UNKNOWN`; provider request ID ani metadata nejsou retrieve handle. Parent se zablokuje a automatický druhý provider call nevznikne.

Po známém response ID runtime vždy retrieveuje, polluje nebo resumeuje tentýž response. Tool-loop continuation je nový model call s exact parent response/call relation; původní provider response se znovu neotevírá. Úplná state map, output ordering a devět crash bodů jsou v 49.14 a 52.

### 11.16 Session a konverzační stav

Kanonickou autoritou historie je KájovoCML databáze. Agent/session revision volí právě jednu strategii:

```text
STATELESS
KCML_SESSION
OPENAI_CONVERSATION
PREVIOUS_RESPONSE_CHAIN
```

Strategie se v jednom active turnu nekombinují.

- `STATELESS` neposílá žádnou předchozí history mimo explicitní current context.
- `KCML_SESSION` používá persistentní custom Agents SDK Session nad PostgreSQL; provider conversation ani previous response handle se nepoužije.
- `OPENAI_CONVERSATION` používá exact provider conversation ID a pouze current delta; současně se nepoužije previous response ani local full-history replay.
- `PREVIOUS_RESPONSE_CHAIN` používá exact `previous_response_id`; `conversation` a SDK Session replay se nepoužijí a instructions/tools/schema se při každém callu znovu explicitně posílají.

KájovoCML ukládá complete ordered mirror provider outputů pro všechny strategie. Manual replay zachovává všechny replayable output items v původním pořadí, nejen assistant messages; providerem vrácené reasoning/tool-call items se nesmí ztratit.

Session append alokuje sequence pod session row lockem a používá expected session state version. SDK session callback stageuje run-scoped items; canonical assistant/tool items se připojí teprve guarded serverovým commitem. Run smí být `SUCCEEDED` pouze v téže PostgreSQL transakci, která připojí povinné final items, aktualizuje session pointer/version a commitne mandatory memory writes.

Provider conversation nebo previous response handle je execution optimization, nikoli jediný recovery zdroj. Jeho zánik umožní nový local-history chain pouze mezi model calls, z complete canonical history a bez pending/unknown tool effectu; migrace má new session revision a checkpoint.

### 11.17 Memory policy

Každý memory namespace má owner, schema, operation scope, retention, indexing, quota, dedupe a audit. Persistent memory write je typed operation s exact bindingem, source run/output item, item key/digest, content provenance, content role, instruction authority a expected namespace/item state version. Model nezapisuje paměť přímým textem, provider conversation stavem ani compaction summary.

Retrieved memory je při každém použití znovu vložena jako typed data/evidence segment se source namespace/item revision a digestem. Běžná memory hodnota nemůže tvrzením „OWNER řekl“ získat OWNER authority. Pouze memory item vytvořený z exact OWNER decision/specification recordu může odkazovat na tuto authority lineage, a i tehdy se current operation kontroluje proti aktuálnímu OWNER intentu a revision.

Povinné canonical memory writes se stageují do checkpointu a commitují ve stejné PostgreSQL transakci jako run terminal success. External indexace, vector projection nebo jiný derived sink vzniká z transactional outboxu a jeho zpoždění nemění canonical run outcome.

Context compaction je immutable session revision se source item range/digestem, compacted ordered items, model/capability/SDK evidence a CAS current pointerem. Nesmí odstranit unresolved function-call pair, approval, handoff boundary ani měnit domain memory nebo tool outcome. Provider Responses compaction se nepoužije nad OpenAI server-managed conversation session.

### 11.18 Checkpoint a recovery

Run vytváří checkpoint po preparation, před/po každém model outputu, před/po tool/handoff side effectu, při approval/MRTR/task interruption, před/po compaction, před final session/memory commit a před terminal write. Pre-side-effect checkpoint a intent vzniknou před dispatch.

Checkpoint obsahuje nejméně:

- parent run state/version, lease fence a platform incarnation,
- completed model/tool/handoff item sequence,
- pending provider response/call IDs a continuation,
- exact session strategy/cursor/version,
- budget/usage snapshot,
- serialized Agents SDK `RunState`, je-li relevantní,
- `openai`/`@openai/agents`/adapter/serializer versions,
- exact agent graph stable identity map a digest,
- `toolUseBehavior`, guardrail a output provenance,
- pending approvals, inputs, tasks a side-effect set,
- payload digest a previous checkpoint link.

Resume používá latest valid checkpoint a exact compatible SDK/code runtime. Při dlouhodobém approval systém zachovává side-by-side předchozí SDK runtime generation pro checkpointy vytvořené touto verzí; nová SDK generation je nesmí automaticky deserializovat.

Nejednoznačný output-bearing RunState, nekompatibilní graph/runtime nebo chybějící provenance failne před dalším model/tool/session side effectem jako `AGENT_RUN_STATE_UNRESUMABLE` nebo `AGENT_CHECKPOINT_INCOMPATIBLE`. Raw ambiguous/rejected suffix se nereplayuje. Tool outcome potvrzený před crashem se nikdy neopakuje; pokračování vzniká z persistovaného outcome a unique successor reservation.

Podrobný provider/runtime recovery contract je v kapitole 52.

### 11.19 Cancellation

Cancel intent se propaguje do Agents SDK runneru, MCP/KCIP tools, sub-agentů, browseru a external requests. Foreground Responses call dostane AbortSignal pouze pro ukončení lokálního transportu; to samo nepotvrzuje provider cancellation. Background response se canceluje pouze nad persistovaným exact `response.id` a terminal outcome se ověří retrieve.

Po commitnutém intentu se neplánuje nový model/tool/handoff call. Read-only work se abortuje; mutující work se reconciliuje. Cancel versus success race a terminalita se řídí 49.10 a 52.28. Root je `CANCELLED` pouze při známých outcomes a complete required cleanup; jinak `MANUAL_REVIEW`. Child cancel bez root cancel je přípustný jen s exact typed fallback contractem.

### 11.20 Agent evaluation

Každá agent revision má eval suite obsahující:

- deterministic input/output cases,
- schema compliance,
- instruction adherence,
- tool selection a tool abstention,
- argument correctness,
- binding mismatch a tool-abstention behavior,
- handoff a agent-as-tool routing,
- guardrail a refusal behavior,
- approval interruption a resume,
- memory read/write behavior,
- cancellation a crash recovery,
- latency, token usage a cost,
- regression corpus,
- adversarial a malformed inputs,
- production shadow evidence podle kritičnosti.

Eval case má typed input, očekávané invariants, povolené variability, grader, threshold a evidence contract. Aktivace vyžaduje PASS všech blocking cases a splnění aggregate promotion profilu. Modelové subjektivní hodnocení není jediným důkazem pro schema, tool side effect, binding nebo state transition.

### 11.21 Agent activation a rollback

Activation controller ověří:

- revision validation a verification,
- tool binding digests,
- handoff graph,
- guardrails,
- eval PASS profil,
- runtime readiness,
- Secret a contract bindings,
- monitoring profile,
- kompatibilitu persistentní paměti a session.

Active revision pointer se přepne atomicky. Agent revision závislá na novém MCP candidate vstupuje do společného activation setu. Post-activation smoke run ověří minimální deterministic scénář. Při neúspěchu se vrátí předchozí agent revision, tool bindings a související component release.

### 11.22 Monitoring a náklady

Agent monitoring obsahuje:

- run count a terminal outcomes,
- model latency a provider error rate,
- turns, tool calls a handoffs,
- approval wait time,
- schema a guardrail failures,
- token usage a cost,
- tool latency a error rate,
- checkpoint/recovery outcomes,
- memory size a compaction,
- eval drift a production quality signals,
- per-revision SLO.

Každý údaj je dostupný podle agent, revision, run, model, tool, caller, trigger a correlation ID. Cost alarm nezmění OWNERem zvolený model; může omezit nové runy podle schválené budget policy a nabídnout OWNERovi změnu.

### 11.23 Agent UI

Stránka **AI agenti** obsahuje:

- agent catalog a lifecycle,
- revision editor s canonical instruction, input/output schemas a diff,
- OpenAI model selector,
- zobrazení systémem odvozených model settings a budgetů,
- exact MCP/function tool binding graph,
- aliasy, digests, compatibility a readiness,
- handoff/agent-as-tool graph,
- guardrails a approval policies,
- session a memory browser,
- run console s live model, tool, handoff, browser session a checkpoint events,
- browser policy, active browser sessions, live preview, takeover a target references,
- pending OWNER approvals,
- cancel, pause a resume,
- eval suite, case results a promotion gate,
- usage, tokeny, cost a SLO,
- triggers a schedules,
- activation set, rollback a repair,
- secrets, bindings, logs a audit.

### 11.24 Centrální systémový AI chat

Centrální chat je kanonický platformní AI agent s vlastní component a runtime identitou. Používá stejný OpenAI runtime, strict tool binding compiler, guardrails, session model, tracing, approval a audit jako ostatní agenti.

Chat přijímá běžný jazyk a vrací lidský text spolu se strojově čitelnými action výsledky. Každá autentizovaná OWNER zpráva, která žádá akci, vytvoří immutable OWNER-intent candidate svázaný s message ID, selected object/browser contextem, explicitními target references a canonical message digestem. Server jej validuje a překládá na centrální object-action registry nebo generation workflow; modelová interpretace je proposal, nikoli samostatná autorita. Chat nemá zvláštní bypass binding, schema, confirmation, idempotency nebo lifecycle pravidel.

Persistentní konverzace, stream, attachments, tool calls, actions, correlation IDs a evidence jsou dostupné v UI i přes produkční API. Požadavek na vytvoření nové schopnosti založí nebo otevře generation job a zachová vazbu mezi chat conversation, OWNER message, specification a výslednými objekty.

Chat může založit, připojit, zobrazit a řídit browser session prostřednictvím Browser Interaction Plane. Browser session je explicitně svázaná s conversation ID, OWNER message, OWNER-intent digestem a operation contextem; model nepracuje s neurčitým „aktuálním browserem“. OWNER může ve zprávě vložit Browser target reference, například označené tlačítko, pole, text nebo část stránky. Chat před každou akcí ověří current runtime-build/host-or-bridge/context generation, session, page/page generation, frame attachment, document epoch, control epoch/fence, account/auth epoch, operation scope a target freshness. Před mutující akcí používá serverový action proposal s earliest mutation triggerem, postcondition a reconciliation class; chatové tvrzení samo není outcome.

### 11.25 Browser interaction policy agentů

Agent revision deklaruje browser policy:

```text
NONE
DETERMINISTIC_AUTOMATION_ONLY
INTERACTIVE_BROWSER
ADAPTIVE_BROWSER
```

`DETERMINISTIC_AUTOMATION_ONLY` dovoluje pouze volání aktivní a ověřené Browser Automation revision. `INTERACTIVE_BROWSER` dovoluje agentovi založit viditelnou browser session a provádět exact browser actions podle current operation scope. `ADAPTIVE_BROWSER` navíc dovoluje bounded AI podporované rozhodování nad strukturovaným BrowserObservation, pouze pokud revision určuje allowed origins, account/tenant policy, side-effect ceiling, confirmations/challenges, model budget, runtime deadline a teaching policy.

Produkční agent nepoužívá neurčitý globální browser. Každý browser call obsahuje session ID, runtime-build/context identity, exact page/page generation, frame attachment, document epoch, target reference/locator, operation-scope digest, control epoch/fence a correlation. Agent ztratí write authority okamžitě po OWNER takeover; další browser tool call se odmítne `BROWSER_CONTROL_STALE` před dispatchi.

Před mutující browser action agent musí obdržet serverem vytvořený action proposal obsahující OWNER-intent/operation-context digest, target, argument-origin mapu, earliest mutation trigger, account/resource effect, Secret-use context, precondition, postcondition/read-back a retry/reconciliation class. Browser page text, DOM attributes, accessibility text, network response, download ani popup message nemohou tento proposal přepsat; jsou data s provenance. Modelové tvrzení „klikl jsem“ nebo „uloženo“ není outcome; agent dostane pouze persistovaný canonical action result.

Agent nesmí uložit nebo reuse ElementHandle, DOM node ID, raw CDP/Playwright handle, bounding box nebo screenshot coordinate jako dlouhodobý target. Po navigation, popupu, OOPIF/frame změně, BFCache/prerenderu, OWNER takeoveru nebo recovery používá fresh observation a revalidovanou target reference.

Agentní login používá exact browser account binding, expected account/tenant a auth epoch. OTP, push, WebAuthn/passkey, client certificate, CAPTCHA, dialog, permission nebo native prompt přeruší run do typed challenge. Device-bound krok se nesimuluje serverem; pokračuje přes OWNER Device Bridge nebo OWNER resolution.

Pro periodickou činnost typu „buď tam aktivní“ agent vytvoří nebo použije automatizaci se schedule/event triggerem, timezone/DST/misfire/overlap policy a reusable auth bindingem. Výchozí režim vytváří bounded run, obnoví compatible confirmed Browser state bundle, ověří účet, provede úlohu a context uzavře. Persistent local profile je povolen pouze explicitní bridge/profile revision contractem s exclusive lease; skrytý nekonečný AI browser se nepoužívá.

Repeated adaptive flow lze navrhnout jako teaching candidate. Aktivace candidate vyžaduje page/frame/document routes, semantic locators, typed input strategy, mutation triggers, Secret/artifact bindings, side-effect/retry/reconciliation, independent postconditions, replay bez modelu a verification `PASS`. Visual-only nebo unresolved mutating step neblokuje OWNER ad-hoc control, ale blokuje unattended deterministic activation.

## 12. Generátor KájovoCML

### 12.1 Účel generátoru

Generátor KájovoCML převádí OWNERem schválený funkční záměr na aktivovaný, monitorovaný a rollbackovatelný soubor komponent. Generátor vyrábí nebo aktualizuje:

- MCP server,
- MCP tool, resource nebo prompt,
- AI agenta,
- AI agent tool adapter,
- agent-as-tool nebo handoff vazbu,
- platformní komponentu,
- managed runtime,
- external API connector,
- webhook handler,
- PULSE integration,
- browser automation,
- webové OWNER UI pro generovanou schopnost,
- supporting schemas, bindings, tests, monitoring a dokumentaci.

Generátor je deterministicky řízený výrobní systém. OpenAI model analyzuje, navrhuje a vytváří typované kandidáty; serverová state machine, schemas, validátory, testy, exact bindings, workery a activation controller rozhodují o každém autoritativním zápisu a provozním side effectu.

### 12.2 Vstup

OWNER zadává cíl přirozeným jazykem. Vstup může obsahovat:

- text,
- soubory,
- obrázky,
- URL,
- API nebo MCP dokumentaci,
- credentials,
- příklady requestů a odpovědí,
- test data,
- business pravidla,
- odkazy na existující komponenty, tools a agenty,
- požadovaný OpenAI model.

Každý vstup se okamžitě persistuje jako immutable source nebo OWNER message. Systém nikdy nezávisí pouze na klientském browser state.

### 12.3 Generation job

Generation job je stabilní výrobní objekt s kind:

```text
CREATE
UPDATE
FOLLOW_UP
RETRY
REPAIR
```

Job obsahuje:

- immutable initial request,
- OWNER identity a execution context,
- target kind a volitelný target object,
- message history a discussion turns,
- source inventory a verified facts,
- capability snapshot,
- specification revisions,
- approved specification a digest,
- execution authority,
- generation plan,
- phase runs a checkpoints,
- OpenAI model calls a tool dispatches,
- implementation workspace a patch history,
- contract candidates a generated artifacts,
- validation, evaluation a conformance evidence,
- activation set,
- previous a candidate releases,
- blockers, cancellation a recovery state,
- events, logs, traces a audit.

Job má jeden autoritativní lifecycle a nejvýše jeden aktivní phase run. Souběžné read-only analýzy mohou běžet pod jedním phase runem, ale pouze phase coordinator mění lifecycle.

### 12.4 Generation lifecycle

Lifecycle používá:

```text
DISCUSSING
ANALYZING
IMPLEMENTING
INTEGRATING
VALIDATING
CML_CONFORMANCE
ACTIVATING
COMPLETED
BLOCKED
FAILED
CANCELLED
```

Happy path je lineární:

```text
DISCUSSING → ANALYZING → IMPLEMENTING → INTEGRATING → VALIDATING → CML_CONFORMANCE → ACTIVATING → COMPLETED
```

Z každého non-terminal happy-path stavu lze přejít do `BLOCKED`, `FAILED` nebo po bezpečném cancellation/cleanup do `CANCELLED`. `BLOCKED` ukládá exact resume state/phase, checkpoint a context digest. Resume znovu ověří current specification, authority, dependencies, bindings, credentials, workspace, candidate a activation snapshot. `COMPLETED`, `FAILED` a `CANCELLED` jsou immutable terminal.

Každá fáze vytváří `generation_phase_run`:

```text
QUEUED
RUNNING
WAITING_FOR_DEPENDENCY
WAITING_FOR_OWNER
REPAIRING
SUCCEEDED
FAILED
CANCEL_REQUESTED
CANCELLED
```

Phase attempt terminalita, retry a exact transition map jsou v 49.15. Každá fáze má terminal evidence; `FULL_REUSE` používá explicitní `NO_NEW_CODE_REQUIRED`, nikoli silent skip.

### 12.5 Stavové přechody a guards

Každý job/phase command ověřuje current platform incarnation, job `stateVersion`, phase state/version, current coordinator/phase fencing token, cancellation version, approved specification digest, execution authority lineage, plan a dependency snapshot, workspace/candidate/activation pointers a latest checkpoint.

Phase success, phase terminal evidence, job transition, next phase pointer, next phase run, queue/outbox, generation event a audit se commitují v jedné transakci. Pozdní worker nebo stale attempt nemůže dokončit phase, změnit job, založit další phase ani posunout checkpoint.

Povolené hrany a crash semantics jsou v 49.15 a 49.25. Přechod neuvedený v mapě je odmítnut jako `CONFLICT`.

### 12.6 Persistentní workspace

Každý job má persistentní workspace obsahující:

- source registry,
- message a turn registry,
- capability evidence,
- browser session,
- specification registry,
- plan registry,
- implementation filesystem,
- workspace revisions,
- patch sets,
- generated artifacts,
- validation reports,
- release candidates,
- activation evidence.

Filesystem workspace má stabilní root, job-scoped OS identity, quota, path allowlist a oddělený temporary prostor. Symlink, hardlink, device node, socket, path traversal a zápis mimo root jsou zakázané. Každý soubor má canonical relative path, MIME/type, size, content digest, source classification a current workspace revision.

### 12.7 Source intake a verified facts

Source používá stav:

```text
RECEIVED
PARSING
PARSED
VERIFIED
REJECTED
SUPERSEDED
```

Parser vytvoří normalizovaný text, metadata, attachment references, raw/canonical content digest a content-provenance graph. Každý segment dostane content role a instruction authority; parser nepovyšuje role podle textu uvnitř dokumentu. Fakt vzniká jako samostatný objekt obsahující statement, source locator, confidence classification, verification method, provenance reference, observedAt a digest.

Fakt classification používá:

- `OWNER_DECISION`,
- `SOURCE_ASSERTION`,
- `RUNTIME_OBSERVATION`,
- `EXTERNAL_VERIFIED`,
- `DERIVED_TECHNICAL`.

Business pravidlo nebo explicitní OWNER rozhodnutí se nepřepisuje technickou inferencí. `OWNER_DECISION` vzniká pouze z autentizované OWNER zprávy, approval nebo explicitně schválené specification field; věta uvnitř webu, dokumentu, repository, MCP resultu, logu nebo memory se jí nestane. Exact source může být OWNERem delegován jako specification source podle 11.4 a 53, avšak pouze v bounded parent intentu. Rozpor mezi dvěma autoritativními zdroji vytváří blocker nebo open question; model nerozhodne tichým výběrem.

### 12.8 Discussion turn

Discussion turn používá:

```text
QUEUED
RUNNING
INTERRUPT_REQUESTED
INTERRUPTED
COMPLETED
FAILED
```

OWNER zpráva a unique `clientMessageId` se persistují před enqueue. Stejný ID/digest vrací původní message; jiný digest je conflict. Sequence se alokuje pod job root lockem. Jedna zpráva má právě jeden turn a job nejvýše jeden upstream-active turn.

Steer atomicky persistuje novou OWNER zprávu, nastaví interrupt intent/fence aktivnímu turnu a vytvoří successor reservation. Worker zruší read-only stream/tools a reconciliuje pending side effects. Partial assistant text se uzavře jako `INTERRUPTED` evidence.

Provider completion versus steer se linearizuje DB transakcí podle 49.15: vznikne právě jeden terminal stav a nejvýše jeden successor. Pozdní provider completion se stale turn fence nemůže vytvořit assistant completion ani další turn.

### 12.9 Dvouvrstvá OpenAI orchestrace

OpenAI orchestrace má dvě vrstvy:

1. **KájovoCML deterministic orchestrator** — vlastní lifecycle, DB state, queues, leases, tools, approvals, schemas, budgets, retries, checkpoints a side effects.
2. **OpenAI execution runtime** — poskytuje modelové analýzy a agentní specialisty prostřednictvím oficiálního SDK; každý návrh zachovává content provenance, argument-origin mapu a authority-lineage references.

Přímý Responses API call se používá pro bounded transformace s jedním typed výsledkem, například normalizaci požadavku, klasifikaci diagnostiky nebo vytvoření jednoho contract candidate.

OpenAI Agents SDK se používá pro vícekolové specialistické úlohy s tools, guardrails, delegací nebo resumable workspace činností, například contract architecture, implementační iteraci a evaluation design.

Žádný model nebo Agents SDK runner nemění job lifecycle ani produkční objekty přímo. Každý jeho výstup vstupuje do serverového validatoru a autoritativní operaci provede pouze KájovoCML tool nebo phase worker.

### 12.10 OpenAI client service

Všechny modelové call sites používají jedinou serverovou `OpenAIClientService`, která vytváří a poskytuje exact configured instance oficiálního `openai` klienta a OpenAI-specific model adapter pro `@openai/agents`.

Služba zajišťuje:

- jediný `OPENAI_API_KEY`,
- OWNERem zvolený OpenAI model podle workflow inheritance,
- immutable request descriptor a request digest,
- model capability snapshot a pre-submit gate,
- strict output/tool schema registry,
- foreground/background/transport policy,
- Responses create/retrieve/cancel/compact adapter,
- Agents SDK model, session a RunState instrumentation,
- cancellation, timeout a deadline adapter,
- provider event/output/status normalization,
- usage, cost, tracing, audit a metric emission,
- persistentní recovery evidence podle kapitoly 52.

Oficiální `openai` klient má pro create-class requests `maxRetries: 0`; defaultní SDK retry nesmí vytvořit skrytý druhý Responses call. Handle-class retrieve/resume/cancel může bounded retryovat pouze stejný exact `response.id`. Agents SDK model retry je vypnutý, nebo používá KájovoCML policy, která před každým pokusem persistuje nový attempt a dovolí retry pouze při provider/transport evidence `replaySafety='safe'`, `responseStarted=false` a serverovém `CONFIRMED_NOT_SUBMITTED`. Unsafe nebo unknown replay se automaticky neschvaluje.

Každý request descriptor obsahuje nejméně:

- workflow kind a owner job/turn/phase/run,
- model a capability snapshot,
- instruction template/revision, compiled bytes a digest,
- ordered input item digest,
- tool registry/revisions, ordered definitions a digest,
- strict output schema ID/bytes/digest,
- reasoning/output/truncation/parallel settings,
- foreground/background/store/transport policy,
- session strategy a provider continuation handle,
- max output tokens, turns, tool calls, cost a deadline,
- `openai`, `@openai/agents`, adapter a serializer versions,
- correlation, causation, trace, group a local idempotency data.

Modelová volba je OWNER-editable. Systém odvozuje ostatní settings podle immutable workflow contractu a model capability snapshotu. Unsupported required capability blokuje call `OPENAI_MODEL_CAPABILITY_UNSUPPORTED`; systém nesmí potichu změnit model, odstranit tool/schema/modality ani převést strict output na volný text.

Provider HTTP request ID je pouze diagnostická evidence. Provider execution/recovery handle je `response.id`; request ID, metadata, trace ani local digest jej nenahrazují.

### 12.11 Model call lifecycle

Jeden Responses API create je jeden `ai_model_call` attempt. Vícekolový tool loop vytváří nové successor model calls; po tool outputu se již dokončený provider response znovu neotevírá.

Local states jsou:

```text
QUEUED
SUBMITTING
IN_PROGRESS
STREAMING
WAITING_FOR_TOOL_OUTPUT
COMPLETED
INCOMPLETE
REFUSED
CANCEL_REQUESTED
CANCELLED
FAILED
EXPIRED
```

Provider wire status se ukládá odděleně; nejméně `queued`, `in_progress`, `completed`, `failed`, `cancelled` a `incomplete`. `REFUSED`, `WAITING_FOR_TOOL_OUTPUT` a `EXPIRED` jsou KájovoCML semantic states.

Local record, immutable descriptor, idempotency claim, submit intent a queue/outbox vzniknou před provider requestem. Worker před síťovým dispatchi znovu ověří parent fence, cancellation, deadline, budget, exact descriptor a capability snapshot a commitne `SUBMITTING/DISPATCH_STARTED`.

Provider response ID se persistuje při prvním create výsledku nebo `response.created` eventu. Existing handle se vždy retrieveuje/polluje/resumeuje; nevytváří se nový Responses call.

`CONFIRMED_NOT_SUBMITTED` lze nastavit pouze při prokazatelném selhání před provider dispatch. Timeout, connection error nebo worker crash po `DISPATCH_STARTED` bez uloženého response ID je `MODEL_SUBMIT_OUTCOME_UNKNOWN`; parent se zablokuje a nevytvoří blind duplicate. Provider request ID není lookup handle.

Producing model call s function calls přejde do `WAITING_FOR_TOOL_OUTPUT`. Po persistentních tool outcomes a atomické successor reservation se uzavře `COMPLETED` s `completionKind=TOOL_CALLS`; successor je nový `ai_model_call`.

Model call completion sama nemění parent lifecycle. Parent pod current fence validuje provider status, ordered items, strict output, guardrails, pending calls a budgets. Úplná state map, linearizační body a crash recovery jsou v 49.14 a 52.

### 12.12 Foreground streaming a background mode

Interaktivní discussion používá foreground streaming nebo background+stream podle required survivability. Browser SSE connection je oddělená od provider connection; browser disconnect model call automaticky neruší.

Foreground stream:

- persistuje každý provider event před publikací,
- deduplikuje podle provider response/sequence a local sequence,
- nepoužívá argument/text delta jako terminal output,
- nevykonává function tool z delta argumentů,
- po clean EOF vždy kontroluje response status,
- partial text ukládá jako progress/interrupted evidence, nikoli canonical final message.

Dlouhá nebo restart-resumable operace používá background mode pouze při capability gate a explicitně nastaví `background=true` a `store=true`. Worker:

1. persistuje local call a submit intent,
2. vytvoří právě jeden background response,
3. persistuje response ID při prvním dostupném eventu,
4. polluje `responses.retrieve(responseId)` nebo resumeuje stream od posledního contiguous `sequence_number`,
5. používá při parsed stream resume stejné tools/text schema config,
6. persistuje events/output items idempotentně,
7. terminalizuje pouze podle provider statusu a serverové validace.

Worker restart, ztráta SSE nebo transport disconnect po známém response ID vede pouze k retrieve/resume stejného handle. Background cancel používá stejný response ID a jeho opakování nevytváří nový model call. Foreground abort ani timeout bez potvrzeného provider outcome není důkaz cancellation.

### 12.13 Provider výstupy a ordering

Runtime persistuje úplné ordered `response.output`, zejména:

- assistant message/content parts,
- reasoning items a summaries skutečně zpřístupněné API,
- function calls,
- hosted tool/MCP calls, results a approval items,
- refusals,
- annotations a artifacts,
- provider status, error/incomplete details,
- raw a normalized usage.

Každý item má provider output index, item ID, type/status, call ID, raw canonical payload/digest a ordered content parts. `response.output_text` je pouze display projection a nesmí nahradit raw output items.

Unknown output item se raw persistuje a blokuje canonical interpretation/continuation `OPENAI_OUTPUT_ITEM_UNSUPPORTED`; nesmí být potichu odstraněn. Manual local history replay zachovává všechny replayable output items v původním pořadí prostřednictvím version-pinned oficiálního normalizeru nebo testovaného ekvivalentu. Pouhé assistant messages nejsou úplná history.

`REFUSED` má exact refusal item/text a continuation policy. `INCOMPLETE` má provider incomplete details. `FAILED` má provider error. Schema parse/validation chyba je samostatně `MODEL_OUTPUT_SCHEMA_INVALID`. Provider handle expiry po complete local persistence canonical outcome nemění; bez complete outputu je `OPENAI_RESPONSE_EXPIRED` a žádný blind create.

Technický panel zobrazuje pouze API-available reasoning metadata; nevytváří ani nezobrazuje neexistující interní tokeny modelového uvažování.

### 12.14 Strict structured outputs

Každý interní specialistický výstup má versioned strict JSON Schema. Structured `text.format` a strict function tools používají:

- jednoznačný root a artifact kind/schema version,
- všechny properties explicitně deklarované a required,
- nullable/discriminated variantu pro volitelnost,
- `additionalProperties: false` v každém objectu,
- stabilní enumy,
- bounded string/array/object limity,
- provider-supported schema profil potvrzený capability snapshotem.

Server před submit validuje schema a po terminal provider response provede nezávislou serverovou validaci raw parsed outputu. Refusal, incomplete a provider failure se nezkoušejí parseovat jako požadovaný structured success.

Schema-neplatný nebo neúplný output není autoritativní. Bounded repair je nový model logical operation s vlastním descriptor/digestem a diagnostics; nejde o retry stejného provider attemptu. Již potvrzené tool outcomes se při repair neopakují.

Agents SDK error handler může vrátit fallback final output pouze bez dalšího model/tool replay, po validaci proti exact schema a s uloženým původem handleru.

### 12.15 Generation tool loop

Generation tool registry obsahuje pouze serverem definované tools:

- source read a search,
- capability lookup,
- contract read,
- web research,
- generation browser,
- Secret lookup v OWNER scope,
- workspace list/read/write/apply-patch,
- schema validate,
- static check a test run,
- specification proposal,
- generation plan proposal,
- OWNER input request,
- blocker proposal.

Každý tool používá strict input/output schema a exact binding. Mutující tool mění pouze job-scoped workspace nebo vytváří typed proposal; produkční registr, bindings, releases a pointers mění až příslušný phase worker.

Function call pipeline:

1. přijme pouze complete provider output item,
2. páruje podle `call_id`, nikoli optional item ID,
3. persistuje raw arguments, canonical args/digest a provider output ordinal,
4. validuje strict model schema, inverse projection a native tool schema,
5. vytvoří dispatch/idempotency/side-effect intent před execution,
6. persistuje exact outcome a guardrail result,
7. vytvoří function call output se stejným `call_id`,
8. uloží post-tool checkpoint a právě jednu successor model-call reservation.

Arguments delta se nikdy nevykonává. Stejný call ID/digest replayuje outcome; stejný call ID s jiným digestem je conflict. Model nikdy neobdrží nepotvrzený tool success. Unknown mutující effect zablokuje continuation do reconciliation/`MANUAL_REVIEW`.

Při více function calls se outputs sestaví podle původního provider output pořadí, ne podle completion time. Paralelní execution je povolená pouze exact policy pro nezávislé read-only nebo prokazatelně disjunktní idempotentní calls; workspace mutace, side effects, approval, handoff a pořadově závislé calls jsou sekvenční.

Continuation je nový `ai_model_call`. Používá právě jednu strategy: `PREVIOUS_RESPONSE_CHAIN`, `OPENAI_CONVERSATION`, `KCML_SESSION` nebo `STATELESS`. `previous_response_id` a `conversation` se nekombinují; při previous-response chaining se instructions, tools, schema a relevantní settings vždy znovu pošlou. Úplné semantics jsou v 52.15–52.18.

### 12.16 Specialistický agentní tým

Generation orchestrator používá versioned specialist agent templates:

- `REQUIREMENTS_ANALYST` — normalizuje cíl, behavior a OWNER decisions,
- `SOURCE_RESEARCHER` — získává a strukturuje ověřitelné podklady,
- `CAPABILITY_RESOLVER` — hledá reuse a kompatibilitu,
- `CONTRACT_ARCHITECT` — skládá společné doménové a integrační kontrakty,
- `MCP_ARCHITECT` — navrhuje server/tool/resource/prompt contracts,
- `AGENT_ARCHITECT` — navrhuje agent revision, tools, handoffs, memory a guardrails,
- `IMPLEMENTATION_PLANNER` — vytváří execution DAG a file plan,
- `CODE_BUILDER` — vytváří workspace patch sets,
- `VALIDATION_REPAIRER` — opravuje přesný validator report,
- `EVALUATION_DESIGNER` — vytváří deterministic a modelové eval cases,
- `INTEGRATION_ARCHITECT` — vytváří component identity, contract/Secret binding, runtime a routing plan,
- `RELEASE_CONTROLLER` — vyhodnocuje gate evidence a activation readiness.

Každý template má immutable instructions, strict input/output schema, povolené tools, budget a model inheritance. Specialisté nemají přímé produkční credentials ani DB write. Jejich výstupy jsou typed proposals s digestem.

Orchestrator vybírá pouze potřebné specialisty podle target graphu. Jednoduchý MCP tool nepoužije zbytečný multi-agent graph; spojený MCP+agent workflow použije contract architect, oba domain specialisty a společného release controlleru.

### 12.17 Capability-first

Před vznikem nové schopnosti server provede:

1. normalizaci behaviorálního cíle,
2. extrakci požadovaných input/output contracts,
3. lookup active capabilities podle identity, textu, schemas a tags,
4. načtení exact active contractů a digests,
5. runtime, lifecycle, binding a monitoring eligibility,
6. compatibility porovnání,
7. impact analýzu callerů a targetů,
8. rozhodnutí `FULL_REUSE`, `PARTIAL_REUSE` nebo `NEW_CAPABILITY_REQUIRED`.

Capability decision obsahuje:

- normalized requirement IDs,
- matched component/tool/agent IDs,
- exact revision a digest,
- behavior coverage matrix,
- schema compatibility,
- operational eligibility,
- reused behavior,
- missing delta,
- required binding/integration delta,
- confidence a evidence references.

`FULL_REUSE` nevytváří novou component ani runtime identitu. `PARTIAL_REUSE` vytváří pouze missing delta a přesné integration edges. Binding-only rozdíl nevytváří nový tool.

Capability snapshot je immutable. Před approval a před activation se freshness znovu ověří. Stale nebo změněný dependency digest vrací job do analysis nebo vytváří blocker podle dopadu.

### 12.18 OWNER otázky a automatická technická rozhodnutí

Systém žádá OWNERa pouze o:

- business význam a požadované chování,
- explicitní pravidlo nebo prioritu,
- účet, credential, URL, identifikátor, obsah nebo soubor, který nelze odvodit,
- rozhodnutí mezi skutečně odlišnými business výsledky,
- OpenAI model.

Technické naming, IDs, schemas, timeouts, retries, leases, queues, resource limity, monitoring, testy, code structure a deployment systém odvozuje a validuje automaticky.

Open question obsahuje exact field path, důvod, možné volby, dopad a source evidence. Schválení je blokované pouze otázkou, která mění funkční kontrakt nebo vyžaduje chybějící OWNER input. Technická nejistota se řeší výzkumem, capability lookupem nebo validator experimentem.

### 12.19 GenerationSpecification

GenerationSpecification je úplný immutable funkční kontrakt. Obsahuje nejméně:

```json
{
  "schemaVersion": "string",
  "jobId": "uuid",
  "objective": {},
  "resultContract": {},
  "behavioralRequirements": [],
  "inputs": [],
  "outputs": [],
  "businessRules": [],
  "ownerDecisions": [],
  "authorityModel": {},
  "sources": [],
  "contentProvenance": [],
  "verifiedFacts": [],
  "dataDerivations": [],
  "constraints": [],
  "capabilityDecision": {},
  "targetGraph": {},
  "mcpElements": [],
  "agents": [],
  "integrationEdges": [],
  "externalSystems": [],
  "secrets": [],
  "secretUseRequirements": [],
  "bindings": [],
  "agenticSecurityRequirements": [],
  "stateContracts": [],
  "uiRequirements": [],
  "browserRequirements": [],
  "monitoringRequirements": [],
  "evaluationPlan": {},
  "acceptanceCriteria": [],
  "releasePlan": {},
  "rollbackPlan": {},
  "openQuestions": []
}
```

Každý behavioral requirement má stable requirement ID. `authorityModel` určuje OWNER-intent roots, delegated specification sources, target/argument derivation slots, side-effect ceiling, delegation containment a Secret-use purposes. `contentProvenance`, `dataDerivations` a `secretUseRequirements` jsou normativní a nesmějí být vypuštěny z renderu ani generation planu. Každý MCP element obsahuje exact contract z kapitoly 10. Každý agent obsahuje exact revision contract z kapitoly 11. Každá browser requirement obsahuje parent/use case, origins, auth/account policy, OWNER handoff, target precision, side-effect ceiling, teaching/replay, outputs a acceptance. Každá integrační vazba obsahuje source revision, target revision, operation, contract digest, operation scope, secret/external dependency a E2E expectation.

Specifikace se serializuje jako canonical UTF-8 JSON se seřazenými klíči a kompaktními separátory. Revision ukládá SHA-256 digest a věrný Markdown render. Render nesmí vypustit žádné normative field.

### 12.20 Specification conformance precheck

Před nabídnutím approval CTA server ověří:

- schema validity,
- prázdné blocking open questions,
- target graph completeness,
- každý requirement pokrytý alespoň jedním contractem a acceptance criterion,
- všechny input/output schemas,
- side-effect, retry, idempotency a existující approval klasifikaci,
- authority-lineage completeness a instruction/data separation,
- každý dynamic target/argument proti deklarovanému value-derivation slotu,
- každý Secret resolve proti exact purpose/target/placement contextu,
- každou Secret/external dependency,
- capability reference freshness,
- rollback existence,
- žádný unresolved name collision,
- žádný placeholder, demo, mock nebo neurčitý production behavior.

Neúplná specifikace zůstává proposal a nemůže být schválena. Detekce agentic semantic mismatch nezavádí nový approval ani confirmation: validator buď prokáže shodu, nebo vrátí typed blocker s exact chybějícím targetem, argument originem či lineage.

### 12.21 Approval

OWNER schvaluje exact current specification revision a digest. Approval request zobrazuje revision, digest, diff, capability decision, target graph, MCP/agent contracts, exact bindings/secrets, acceptance a rollback plán.

Approval command obsahuje expected job state version, current turn ID/status, spec revision/digest, capability snapshot/digest a idempotency key. Dvě approvals nad stejnou expected version nemohou obě změnit pointer.

Approval uspěje pouze při completed discussion turnu, current approvable revision, current capability snapshotu a prázdných blocking questions. Jedna transakce:

1. claimne approval idempotency record,
2. vytvoří immutable `generation_execution_authority`,
3. zmrazí approved specification pointer/digest,
4. uloží OWNER approval event a audit,
5. přepne job `DISCUSSING → ANALYZING`,
6. vytvoří planning phase run a queue/outbox.

Crash před commitem nevytvoří authority ani planning. Crash po commitu je obnoven z outboxu. Před approval nevzniká target identity, runtime, binding, release ani produkční side effect.

### 12.22 Execution authority

Authority kind používá:

- `OWNER_APPROVED` pro CREATE, UPDATE a funkční FOLLOW_UP,
- `INHERITED_TECHNICAL` pro RETRY, remediation a REPAIR bez změny funkčního kontraktu.

Authority obsahuje source job, specification revision, digest, approved decisions, OWNER-intent roots, delegated-source digests, target/argument derivation contract, Secret-use requirements, target IDs pokud existují, frozenAt a lineage digest. Každý phase run, model call, tool call, workspace revision, candidate, validation a activation set odkazuje na stejnou authority a vytváří pouze descendant operation contexts.

Technická oprava nesmí měnit behaviorální requirements, external semantics, exact bindings nebo OWNER decisions. Taková změna vyžaduje novou specification revision a OWNER approval.

### 12.23 GenerationPlan

Planner vytvoří immutable `GenerationPlan` jako directed acyclic graph. Plan node obsahuje:

- node ID a kind,
- purpose,
- requirement IDs,
- input artifact references a digests,
- expected output schema,
- dependencies,
- execution role,
- side-effect class,
- retry class,
- idempotency key template,
- timeout a budget,
- required gates,
- checkpoint a compensation policy.

Plan node kinds zahrnují:

```text
REUSE_BINDING
CONTRACT_COMPILE
MCP_IMPLEMENT
AGENT_COMPILE
UI_IMPLEMENT
SCHEMA_GENERATE
TEST_GENERATE
BUILD
RUNTIME_PROVISION
IDENTITY_REGISTER
CONTRACT_BINDING_APPLY
SECRET_BINDING_APPLY
EXTERNAL_TARGET_APPLY
MONITORING_APPLY
VALIDATE
EVALUATE
CONFORMANCE
ACTIVATE
POST_ACTIVATION_VERIFY
```

Plan validator ověří acyclic graph, úplné requirement coverage, existenci output produceru pro každý input, side-effect ordering, compensation a activation dependency. Neplatný plan nevstoupí do `IMPLEMENTING`.

### 12.24 Generování MCP prvku

MCP pipeline probíhá contract-first:

1. vytvoří stable identity proposal a naming,
2. zkompiluje server/tool/resource/prompt schemas,
3. zkompiluje side-effect, retry, idempotency, concurrency a approval policy,
4. zkompiluje contract, Secret a external target bindings,
5. vytvoří handler behavior table a error catalog,
6. vytvoří component manifest a MCP revision manifest,
7. vytvoří runtime handler interfaces,
8. vytvoří source files prostřednictvím atomic patch sets,
9. vytvoří unit, contract, protocol a E2E tests,
10. vytvoří monitoring a UI metadata,
11. spustí static a sandbox validation,
12. vytvoří candidate release.

Tool schemas, runtime validators, MCP discovery, API tester formulář a tests vznikají z jednoho canonical schema source. Generated handler exportuje pouze kontrakt kapitoly 15.

`FULL_REUSE` vytvoří pouze contract/secret/integration binding artifacts. `PARTIAL_REUSE` generuje pouze missing MCP delta a zachovává exact reused digest.

### 12.25 Generování AI agenta

Agent pipeline probíhá revision-first:

1. vytvoří stable agent/component runtime identity proposal,
2. zkompiluje purpose a immutable instructions,
3. zkompiluje strict input a final output schemas,
4. vybere OWNERem nastavený nebo inherited OpenAI model,
5. odvodí model settings a bounded budgets,
6. zkompiluje exact MCP/function tool bindings,
7. vyřeší aliasy a tool collisions,
8. zkompiluje handoff nebo agent-as-tool graph,
9. zkompiluje guardrails a OWNER approval policy,
10. zkompiluje session, memory, trigger a concurrency policy,
11. vytvoří eval suite a promotion profile,
12. vytvoří agent revision artifact a runtime registration,
13. provede binding preflight,
14. provede eval a E2E run,
15. vytvoří activation-eligible candidate revision.

Agent nikdy nedostane tool pouze proto, že je dostupný v katalogu. Binding vzniká z approved specification a explicitního exact-binding plánu.

### 12.26 Společné generování MCP prvku a AI agenta

Požadavek, který potřebuje nový MCP tool a agenta, vytváří jeden target graph a jeden activation set:

1. MCP contract vznikne jako první,
2. agent binding odkazuje na candidate tool ID, revision a digest,
3. MCP candidate se sestaví a spustí,
4. agent candidate se ověří proti live candidate hostname a discovery snapshotu,
5. eval suite provede skutečná tool volání přes KCIP,
6. contract a Secret bindings se ověří v candidate activation setu,
7. component release, MCP revisions a agent revision vstoupí do jednoho activation setu,
8. pointery se přepnou atomicky,
9. post-activation smoke ověří agent-to-tool call,
10. failure vrátí celý set na předchozí kompatibilní stav.

Agent nesmí být aktivní s neexistujícím, stale nebo neověřeným tool digestem.

### 12.27 Workspace patch protocol

Model nevydává nekontrolovaný zápis do filesystemu. `CODE_BUILDER` vrací `WorkspacePatchSet` obsahující:

- workspace base revision a digest,
- ordered operations `ADD`, `UPDATE`, `DELETE`,
- canonical relative path,
- expected previous file digest,
- complete new content nebo bounded patch,
- file type a executable flag,
- reason a related requirement IDs.

Server patch set schema-validuje, ověří paths a expected digests a aplikuje jej atomicky. Konflikt vytvoří `WORKSPACE_BASE_STALE`; model dostane current file metadata a vytvoří nový patch. Po aplikaci server znovu načte celý deklarovaný file set a vytvoří novou workspace revision.

Generated production source nesmí obsahovat placeholder runtime, prázdný handler, fiktivní success, vypnutou validaci, nepoužitelný TODO, demo data nebo mock závislost mimo test fixture.

### 12.28 Implementation loop

Implementation worker používá bounded cyklus:

```text
load approved spec and plan
→ produce typed patch set
→ atomically apply
→ read back complete workspace
→ schema/type/import/export validate
→ run focused tests
→ classify diagnostics
→ produce exact repair patch
→ repeat
```

Každá iterace ukládá:

- iteration number,
- input workspace revision,
- model call a output digest,
- patch set,
- resulting workspace revision,
- validator report,
- tests,
- token/cost/duration,
- progress signature.

Stagnation detector porovnává diagnostics digest a workspace changes. Opakování stejné kořenové chyby bez měřitelného progressu zastaví automatické kolo před vyčerpáním zbytečných callů a vytvoří přesný blocker report.

Repair model obdrží úplný relevantní contract, aktuální soubory a serverový validator report. Modelové tvrzení o opravě není evidence; nové PASS validator results jsou evidence.

### 12.29 Artifact manifest

Candidate artifact manifest obsahuje:

- source tree digest,
- seznam souborů a digestů,
- component/MCP/agent manifests,
- schemas a digests,
- dependency lock digest,
- build output digest,
- generated tests a test catalog entries,
- UI registration metadata,
- monitoring profile,
- migration a state schema,
- release metadata,
- specification a authority lineage.

Manifest je self-contained a server ověří, že každý deklarovaný soubor existuje a žádný produkční soubor není mimo manifest.

### 12.30 Integration saga

`INTEGRATING` je idempotentní checkpointovaná saga s kroky:

1. alokace nebo načtení component identity,
2. publikace candidate component/MCP/agent revisions,
3. publikace candidate release metadata,
4. candidate exact contract bindings,
5. candidate Secret bindings,
6. external targets a endpoint/egress bindings,
7. runtime filesystem a credentials,
8. systemd candidate instance,
9. candidate-aware canonical hostname/route,
10. live candidate startup,
11. readiness a heartbeat,
12. monitoring profile,
13. UI a catalog registration,
14. E2E scenario registration.

Každý stable step má unique record, deterministic resource identity, state map 49.16, idempotency key, before/expected-after digests, side-effect intent, read-back, evidence a explicit compensation. DB locks se nedrží přes external/systemd/filesystem operaci. Resume načte skutečný stav a pokračuje od prvního nepotvrzeného kroku.

První CREATE identity je provisional do activation `ACTIVE`. V témže jobu se nealokuje paralelní náhradní identity. UPDATE/REPAIR zachovává identity. Cleanup je vlastní saga a blokuje další remediation candidate do `COMPLETE` nebo explicitního manual-review blockeru.

### 12.31 Live candidate

Validation a integration používají skutečný candidate runtime na jeho kanonickém HTTPS hostname. Candidate route je dostupná pouze přes OWNER API klíč nebo interní integration execution context svázaný s candidate activation setem. Candidate:

- používá skutečný Secret Broker,
- používá skutečný egress gateway,
- používá candidate exact bindings,
- emitne skutečný heartbeat a audit,
- odpovídá na MCP `server/discover`, moderní per-request metadata, list/read/get a call operace,
- obsluhuje `subscriptions/listen`, MRTR a Tasks extension, pokud je deklaruje candidate contract,
- obslouží agent tool binding preflight proti exact candidate digestu.

In-process mock candidate nenahrazuje live runtime gate.

### 12.32 Validation matrix

Validation controller spouští gate catalog:

```text
SPEC_LOCK
PLAN_VALID
SOURCE_COMPLETE
CAPABILITY_FRESH
ARTIFACT_MANIFEST
SOURCE_STATIC
DEPENDENCY_LOCK
UNIT
DATABASE
MCP_CONTRACT
MCP_PROTOCOL
MCP_REQUEST_METADATA
MCP_DISCOVERY_CACHE
MCP_SUBSCRIPTION
MCP_MRTR
MCP_TASKS
MCP_IDEMPOTENCY
MCP_CANCELLATION
AGENT_CONTRACT
AGENT_TOOL_BINDING
AGENT_GUARDRAIL
AGENT_APPROVAL_RESUME
AGENT_EVAL
INTEGRATION
CONTRACT_BINDING
SECRET_RESOLUTION
EGRESS_INBOUND
RUNTIME_STARTUP
HEALTH_READINESS
HEARTBEAT
MONITORING
AUDIT
UI_ACCESSIBILITY
CANONICAL_E2E
ROLLBACK
```

Gate result obsahuje status, evaluator version, inputs, expected, actual, artifacts, logs, duration, correlation a result digest. Blocking gate musí být `PASS`. `NOT_APPLICABLE` je možné pouze podle schváleného contractu a s reason code. `SKIPPED`, chybějící evidence nebo pouze modelové tvrzení se nepovažuje za PASS.

Validation běží po co největších nezávislých sadách, aby jeden report obsahoval všechny současně zjistitelné chyby. Repair cluster seskupí diagnostics podle kořenové příčiny a poté znovu spustí všechny dotčené i regresní gates.

### 12.33 AI agent evaluation v generation

Agent candidate evaluation používá exact candidate revision a exact candidate tool bindings. Eval runner:

- vytvoří izolované run namespaces,
- používá deterministic fixtures,
- měří schema, routing, tool calls, arguments, final output a side effects,
- ověřuje deny, malformed, timeout, refusal, approval, cancellation a resume,
- porovnává usage, latency a cost s profilem,
- ukládá complete run evidence,
- provede cleanup.

Eval, který může mutovat externí systém, používá sandbox target nebo idempotentní fixture contract. Produkční non-idempotentní mutation není automatickým eval krokem bez explicitního test targetu a reconciliation.

### 12.34 CML conformance

`CML_CONFORMANCE` ověřuje:

- component a serverový execution-context model,
- revision a release immutability,
- KCIP/MCP contract,
- canonical hostname,
- object-action parity,
- UI/API/chat parity,
- contract a Secret bindings,
- monitoring, audit a evidence,
- state, heartbeat a control endpoints,
- source specification traceability,
- rollback a repair readiness,
- self-test catalog registration.

Conformance report mapuje každý relevantní requirement ID na implementation artifact, API operation, UI surface, test a evidence. Chybějící mapování je blocking failure.

### 12.35 Activation set

Activation set je immutable membership a mutable fenced lifecycle obsahující component/MCP/agent/automation revisions, releases, binding-set revision, routes, runtime targets, previous/candidate snapshots, activation domains, barriers, postflight a rollback plan.

State machine používá `DRAFT`, `READY`, `SWITCHING`, `VERIFYING`, `ACTIVE`, `ROLLING_BACK`, `ROLLBACK_VERIFYING`, `ROLLED_BACK`, `FAILED`, `MANUAL_REVIEW`. Přesné hrany, unique constraints a guards jsou v 49.17.

### 12.36 Aktivace

Activation controller:

1. freeze candidate membership/evidence,
2. ověří authority, dependency freshness, rollback target a current previous snapshot,
3. získá deterministic activation locks a fenced controller lease,
4. vytvoří activation barrier, zastaví nové mutující calls a drain/reconciliuje in-flight mutations,
5. provede final candidate preflight,
6. jednou PostgreSQL transakcí alokuje vyšší activation epoch a přepne všechny revision/release/binding/route/runtime pointers,
7. přejde do `VERIFYING` a runtime/reverse-proxy reconciliation provede podle stejného set ID/epoch,
8. provede discovery, metadata/header, list, health, readiness, heartbeat, MCP capability a agent-to-tool smoke,
9. potvrdí monitoring freshness a effective epoch,
10. přejde `ACTIVE`, uvolní barrier a teprve potom job `COMPLETED`.

Gateway nepřijme mixed-epoch dispatch. Pokud post-switch gate selže, reverse switch s vyšším epoch atomicky obnoví celý previous snapshot nebo pro první CREATE explicitní `ABSENT`. Nejednoznačný effective stav je fail-closed `MANUAL_REVIEW`, nikoli částečný provoz.

### 12.37 OpenAI a tool retry policy

Retry classifier používá `TRANSIENT_PROVIDER`, `RATE_LIMIT`, `NETWORK`, `CAPACITY`, `MODEL_OUTPUT_SCHEMA_INVALID`, `TOOL_TRANSIENT`, `DEPENDENCY_UNAVAILABLE`, `CONTRACT_ERROR`, `OWNER_INPUT_REQUIRED`, `NON_RECOVERABLE` a `UNKNOWN_SIDE_EFFECT`.

Create-class OpenAI retry je přípustný pouze před provider dispatch nebo po jednoznačném `CONFIRMED_NOT_SUBMITTED`; SDK jej nesmí provést skrytě. Po `DISPATCH_STARTED` bez `response.id` je outcome unknown. Handle-class retrieve/resume/cancel může bounded retryovat tentýž exact response ID.

Tool retry vytváří nový execution attempt pod stejným logical operation ID a business idempotency recordem. Potvrzený side effect se neopakuje. Unknown outcome, stale contract/approval/checkpoint, cancelled parent, stagnation a OWNER input required nejsou automatic retry. Bounded backoff nemění deadline ani ordering. Schema repair a incomplete continuation jsou nové explicitní model logical operations, nikoli replay již dispatchnutého provider callu.

### 12.38 Checkpoint a crash recovery

Generation checkpoint vzniká po source intake, turnu, specification, approval, plan validation, workspace revision, každém integration/validation kroku a před/po activation switchi. Pre-side-effect checkpoint a intent, outcome a post-checkpoint, phase terminal a next enqueue jsou vždy atomické páry podle 49.9 a 49.15.

Recovery worker claimne expired práci novým fence, validuje platform incarnation, checkpoint/authority/spec/plan/workspace/candidate/activation digests, načte skutečný DB/filesystem/runtime/provider/external stav, reconciliuje pending effects a pokračuje od prvního nepotvrzeného node. Nevytváří duplicitní identity, release, binding, model call, route, task ani pointer switch. Crash matice je 49.25.

### 12.39 Smart technická optimalizace

Systém používá runtime evidence k automatické optimalizaci:

- volby direct Responses call versus Agents SDK workflow,
- foreground versus background execution,
- počtu specialistů,
- context selection a compaction,
- read-only paralelismu,
- test ordering,
- repair clustering,
- queue priority a worker concurrency,
- timeoutů, token a cost budgetů,
- cache usage a invalidace.

Optimalizace nesmí změnit approved behavior, model zvolený OWNERem, exact bindings, external side effects, acceptance criteria ani security contract. Každá účinná hodnota má derivation evidence, version a audit. Regrese automaticky vrátí předchozí policy profile.

Systém se učí pouze z měřitelných výsledků a schválených artifacts. Modelový návrh sám nemění systémovou policy, SSOT ani binding model.

### 12.40 Blocker contract

Blocker obsahuje:

- stable code,
- classification,
- phase a plan node,
- srozumitelný popis,
- machine-readable details,
- affected requirements,
- evidence references,
- exact required resolution,
- OWNER input schema pokud je potřeba,
- resume phase a checkpoint,
- created/resolved timestamps.

Typické blocker classifications jsou `OWNER_INPUT_REQUIRED`, `CREDENTIAL_REQUIRED`, `SOURCE_CONFLICT`, `CAPABILITY_DRIFT`, `EXTERNAL_DEPENDENCY`, `VALIDATION_STAGNATION`, `MANUAL_REVIEW` a `POLICY_BLOCKED`.

Po vyřešení blockeru server ověří resolution schema a current context. Job nepokračuje se stale credential, stale specification nebo stale dependency snapshotem.

### 12.41 Follow-up, update a repair

Funkční OWNER follow-up vytváří novou discussion větev a novou specification revision. Po approval vznikne nové authority lineage.

UPDATE zachovává component a runtime identity a vytváří candidate revisions/releases. Compatibility a migration plan jsou povinné.

RETRY zachovává stejný approved functional authority a opakuje pouze neúspěšnou technickou část s novými attempt records.

REPAIR vzniká z monitoring evidence, zachovává identity a poslední approved functional lineage a smí měnit pouze technickou implementaci. Aktivuje se jen po úplné regresní validaci.

### 12.42 Cancellation

Cancellation command idempotentně uloží monotonic intent/version do jobu a invaliduje admission nových phase/tool/model/browser/external calls. Worker jej kontroluje při claimu, před dispatch, při stream/poll, před/po patchi, mezi testy, před activation barrier/switch a během cleanup.

Cancel versus phase/job completion se linearizuje job root row lockem. Commitnutý cancel zabrání pozdějšímu success transition. Commitnutý terminal outcome se pozdějším cancellem nemění. Candidate se zastaví, rollbackne nebo cleanupne podle current activation/side-effect state.

`CANCELLED` vznikne pouze po známých outcomes a required cleanup safe pointu. External non-idempotent `UNKNOWN` vytvoří `BLOCKED` s classification `MANUAL_REVIEW`; žádný false cancel ani blind retry. Úplná pravidla jsou v 49.10.

### 12.43 Observability a tracing

Každý job používá jeden root correlation ID a trace group. Každý turn, model call, tool dispatch, phase run, test, runtime call a activation má causation relation.

OpenAI Agents SDK trace ID a group ID se mapují na KájovoCML trace metadata. KájovoCML ukládá vlastní authoritative spans pro:

- provider request,
- specialist run,
- tool dispatch,
- workspace patch,
- validator/test,
- integration step,
- activation step.

UI umožňuje přejít z OWNER message na specification, plan, model calls, changed files, tests, release a aktivní objekt.

### 12.44 Generation events a SSE

Každý job má monotonic event sequence. Eventy zahrnují:

```text
generation.job.created
generation.message.created
generation.turn.started
generation.model.started
generation.model.delta
generation.tool.started
generation.tool.completed
generation.browser.session.created
generation.browser.control.changed
generation.browser.action.completed
generation.browser.challenge.required
generation.spec.proposed
generation.spec.approved
generation.plan.created
generation.phase.started
generation.workspace.revision.created
generation.validation.completed
generation.blocked
generation.activation.started
generation.activation.completed
generation.job.completed
generation.job.failed
generation.job.cancelled
```

SSE publikuje až persistované events. Reconnect používá `Last-Event-ID`, replay a snapshot. Klientská delta není autoritativní bez server event sequence.

### 12.45 Generation UI

Generation workspace zobrazuje:

- persistentní OWNER–AI diskusi,
- source a fact inventory,
- live model, tool a browser activity,
- capability coverage matrix,
- specification revision, digest a diff,
- approval guards,
- target graph MCP/agent/component/browser vazeb,
- generation plan DAG,
- phase timeline a checkpoints,
- workspace files a patch diff,
- validator, tests a eval results,
- blockers a exact resume action,
- candidate runtime, release a activation set,
- post-activation evidence,
- logs, traces a audit.

OWNER může z UI i centrálního chatu vytvořit job, dodat vstup, steer, schválit specification, vyřešit blocker, cancel, retry, otevřít výsledné objekty a spustit self-test. UI neposkytuje tlačítko pro obejití blocking gate.

### 12.46 Generation Definition of Done

Generation job je `COMPLETED` pouze když:

1. approved specification a authority jsou immutable,
2. requirement coverage je úplná,
3. capability decision je current,
4. generation plan je validní,
5. artifact manifest a digests jsou úplné,
6. všechny blocking validation, eval a conformance gates mají PASS,
7. live candidate prošel canonical E2E,
8. activation set je atomicky aktivní,
9. post-activation health, readiness, heartbeat, discovery a monitoring jsou potvrzené,
10. rollback point je ověřený,
11. výsledné MCP a agent objects jsou v katalogu, UI, API, chatu, self-testu, logs a auditu,
12. žádný provisional runtime, orphan identity, stale binding, unresolved blocker nebo neuklizený candidate nezůstává.

## 13. Browser Interaction Plane a interaktivní webové relace

### 13.1 Účel a jednotná implementace

Browser Interaction Plane je jediná společná browserová vrstva KájovoCML NG pro:

- generation research, configuration, teaching a replay,
- centrální chat a ad-hoc OWNER úlohy,
- interaktivní AI agentní běhy,
- human challenge a OWNER login,
- deterministické produkční browserové automatizace,
- browserové E2E a acceptance testy.

Systém nevytváří samostatný browser stack pro chat, generátor, agenty a automatizace. Všechny cesty používají stejný session model, stejné Playwright hosty, preview protocol, control lease, Secret Manager, navigation policy, page/frame/document identity, locators, audit, artifacts, side-effect ledger a recovery. Rozdílný je pouze parent object, operation scope, account binding a runtime policy.

Browser Interaction Plane není omezený sandbox pro OWNERa. OWNER po získání current control lease může používat úplné typované pointer, keyboard, text, touch, scroll, tab/page, upload/download, dialog, authentication a device-bridge operace, které daný browser nebo bridge skutečně podporuje. Robustnost nevzniká zákazem legitimních capability, ale přesnou identitou session/page/frame/document/targetu, control fence, persistentním intentem, postcondition a reconciliation. Omezení unattended activation se týká pouze kroku, jehož cíl nebo výsledek nelze deterministicky ověřit; neomezuje přímé OWNER ovládání.

Browser Interaction Plane neposkytuje generický vzdálený desktop ani nepublikuje Playwright/CDP endpoint přímo klientovi. OWNER UI dostává řízený obraz stránky, exact metadata, artifact handles a typované vstupní operace. Nativní OS nebo device-bound krok probíhá přes OWNER Device Bridge nebo explicitní challenge, nikoli skrytým emulováním na serveru.

### 13.2 Vazby relace

Browser session používá právě jeden parent kind:

```text
GENERATION_JOB
SYSTEM_CHAT_CONVERSATION
AGENT_RUN
BROWSER_TEACHING
AUTOMATION_RUN
OWNER_AD_HOC
SELF_TEST_RUN
```

Relace může být připojena k více souvisejícím objektům pro čtení a audit, ale právě jeden parent vlastní lifecycle. Připojení do chatu nebo detailu objektu nevytváří nový BrowserContext; UI otevírá stejnou session podle session ID. Změna parenta je zakázaná. Převod business cíle do nové parent relace vytváří novou session a explicitní state/account binding, nikoli tichou změnu vlastnictví.

Generation-specific režimy zůstávají:

```text
RESEARCH
CONFIGURATION
TEACHING
REPLAY
```

Obecné session purpose používá:

```text
INSPECT
INTERACT
AUTHENTICATE
CONFIGURE
TEACH
REPLAY
MONITOR
TEST
```

Každá session má jeden immutable `sessionPurposeContract` a jeden current operation scope. Rozšíření cíle, originů, account/resource scope nebo side-effect ceiling je nová guarded scope revision; staré pending action ji automaticky nezdědí.

### 13.3 Kanonická architektura

Browser Interaction Plane tvoří čtyři malé odpovědnosti:

1. **BrowserSessionService** v doménové vrstvě a web API — session registry, page/frame/document registry, operation scopes, control leases, preview tickets, target references, account bindings, challenges, action side-effect ledger, cleanup a audit.
2. **`kcml-browser-worker`** — claim příkazů, Playwright action pipeline, observation, teaching compiler, automation interpreter, checkpoints, postconditions a reconciliation.
3. **`kcml-browser-host@<slot>`** — pinovaný browser process a izolované BrowserContexty za typed UDS hranicí. Host může přežít restart workeru, avšak nevytváří doménovou autoritu a přijímá pouze fenced typed commands.
4. **Preview Gateway** v `kcml-web-api` — same-origin WSS stream `KBPP/1.0`, backpressure, input validation, preview reconnect a ověření OWNER session/API klíče.

Na jednom produkčním serveru mohou být tyto odpovědnosti nasazené společně; jejich datové, identity a lease hranice zůstávají stejné. Browser host nikdy neposkytuje veřejný port. Interní control endpoint je systemd-owned UDS; browser transport používá anonymous pipe nebo jiný exact private adapter uvnitř host service boundary.

BrowserSessionService je jediná služba, která může přidělit session k host slotu nebo bridge connection, změnit control epoch, přijmout current page/frame/document observation jako authoritative browser evidence, založit browser action logical operation a uzavřít session. Browser host ani bridge nesmí sám vytvořit session, účet, scope, action identity, control authority nebo terminal business outcome.

### 13.4 Browser engines, build manifest a operační prostředí

Výchozí execution target je `SERVER_MANAGED_CHROMIUM` na produkčním Ubuntu. Povolené target kinds jsou:

```text
SERVER_MANAGED_CHROMIUM
SERVER_MANAGED_FIREFOX
SERVER_MANAGED_WEBKIT
OWNER_DEVICE_BRIDGE
CHROMIUM_CDP_COMPATIBILITY
```

Chromium je výchozí pro interaktivní preview a produkční automatizaci. Firefox a WebKit slouží pro explicitní kompatibilitu, browser-specific workflow a verification. Preview protocol zůstává browser-neutral.

Každý server-managed host používá immutable `BrowserRuntimeBuildManifest`, který obsahuje nejméně:

- application release a source commit,
- Node.js runtime version a digest,
- exact Playwright package version a lockfile digest,
- browser engine, distribution/channel, revision, executable digest a capability profile,
- OS image/release, runtime libraries, font inventory digest a sandbox mode,
- headless/headed mode, launch arguments a environment allowlist digest,
- locale, timezone, color scheme, reduced-motion, user-agent a device profile defaults,
- preview adapter, locator compiler, state-bundle serializer a bridge protocol versions,
- supported client-certificate, WebAuthn, download, permission, service-worker a tracing capabilities,
- canonical manifest digest.

Host, worker, automation revision a state bundle se považují za kompatibilní pouze podle explicitní compatibility relation mezi exact manifest digests. Shoda názvu browseru nebo major verze nestačí. Neznámá capability nebo neověřený upgrade blokuje attach/replay/activation, nikoli běžné otevření nové OWNER session na current supported hostu.

Server-managed session používá samostatný non-persistent BrowserContext uvnitř dlouho žijícího browser hostu. Trvalost účtu zajišťuje výhradně explicitní Browser state bundle; náhodný host profile directory není autorita ani persistence mechanismus. Persistentní lokální browser profile nebo `launchPersistentContext` je dovoleno pouze v explicitním OWNER Device Bridge či compatibility contractu s exact profile ID, exclusive profile lease, known browser process identity a cleanup/recovery pravidly. Jeden profil nesmí současně ovládat OWNER, jiný bridge a server automation bez explicitního takeoveru stejného fenced session contractu.

Host upgrade je blue/green: nový build manifest získá nové host generation, nové sessions se směrují pouze na ready hosty a starý host se drainuje. Živý context se mezi odlišnými manifesty nepřipojuje ani nepřenáší raw handlem. Lze pouze založit nový context z compatible potvrzeného state bundle a provést fresh authenticated/page postcondition.

`OWNER_DEVICE_BRIDGE` je volitelný lokální agent pro Windows, macOS nebo Linux. Používá se, pokud workflow vyžaduje platformní passkey, lokální klientský certifikát, lokální síť, lokální browser profile, zařízení dostupné pouze OWNERovi nebo nativní browser/OS interaction. Bridge navazuje pouze outbound mTLS/WSS spojení ke KájovoCML, má vlastní bridge ID, certificate generation, connection epoch, capability fingerprint, explicitní enrollment, rotation, revocation a heartbeat a nikdy neotevírá inbound port do OWNER sítě.

Windows, macOS a Linux capability se neodvozují z názvu OS. Enrollment a každý connect reportují exact bridge build, architekturu, browser builds, dostupné profily, platform authenticator, client-certificate store integration, keychain/credential-store capability, native dialog capability, screen/input capability a local-target support. Session se přidělí pouze při exact required capability match.

OWNER UI může běžet na desktopu, tabletu, Androidu nebo iOS a ovládat server-managed session. Mobilní klient není obecným Playwright execution hostem. Device-bound operace se předávají desktop bridge nebo human challenge.

`CHROMIUM_CDP_COMPATIBILITY` je explicitní nižší-fidelity režim pouze pro existující Chromium instanci. Používá exact endpoint binding uvnitř hostu nebo bridge, pinned Chromium protocol fingerprint, reduced-capability matrix a samostatnou verification evidence. Nesmí být tichým fallbackem z Playwright protocolu a nemůže být jediným activation důkazem pro workflow využívající nepodporovanou nebo neověřenou funkci.

### 13.5 Session, context, page, frame a document contract

Browser session obsahuje nejméně:

```json
{
  "sessionId": "uuid",
  "parentKind": "SYSTEM_CHAT_CONVERSATION",
  "parentId": "uuid",
  "purpose": "INTERACT",
  "executionTarget": "SERVER_MANAGED_CHROMIUM",
  "runtimeBuildManifestDigest": "sha256:...",
  "browserEngine": "chromium",
  "browserBuildDigest": "sha256:...",
  "playwrightVersion": "pinned-version",
  "hostSlotId": "stable-slot-id",
  "hostGeneration": 12,
  "contextId": "opaque",
  "contextGeneration": 4,
  "accountBindingId": "uuid-or-null",
  "accountAuthEpoch": 9,
  "lifecycle": "ACTIVE",
  "controlHolder": "AI",
  "controlEpoch": 7,
  "activePageId": "uuid",
  "activePageGeneration": 2,
  "activeDocumentEpoch": 18,
  "operationScopeId": "uuid",
  "sensitivity": "NORMAL",
  "latestObservationRevision": 42,
  "latestFrameRevision": 108,
  "leaseExpiresAt": "RFC3339",
  "createdAt": "RFC3339",
  "expiresAt": "RFC3339"
}
```

Každá session používá právě jeden current BrowserContext nebo bridge context generation. Sdílení browser processu je dovoleno; sdílení cookies, storage, permissions, service-worker runtime, download directory, client certificates, virtual authenticators nebo profile state mezi contexts není dovoleno bez explicitního account/state/profile contractu.

Kanonická browser identity pro každou observation a action je n-tice:

```text
platformIncarnation
sessionId
executionTarget
runtimeBuildManifestDigest
hostSlotId | bridgeId
hostGeneration | bridgeConnectionEpoch
contextId
contextGeneration
pageId
pageGeneration
frameId
frameAttachmentEpoch
documentId
documentEpoch
navigationSequence
controlEpoch
controlFencingToken
operationScopeDigest
observationRevision
viewportTransformDigest
targetReferenceId | locatorDigest
actionId
actionAttempt
```

Pole, které pro konkrétní read-only operation není relevantní, je explicitně `null`; nesmí být nahrazeno odhadem nebo posledním známým handlem.

`pageId` je stabilní po lifetime jednoho Playwright Page objektu. `pageGeneration` se zvyšuje při recovery attachmentu, handle replacementu nebo explicitně podporovaném profile/page rebindu. Zavřený page ID se nerecykluje.

`frameId` je KájovoCML identity jednoho attached frame instance. `frameAttachmentEpoch` se zvyšuje při detach/reattach, frame handle replacementu nebo OOPIF process/context replacementu. Stejná URL, name nebo DOM path nevytváří stejnou frame identity.

`documentId` a `documentEpoch` identifikují konkrétní document instance. Full navigation, document replacement, renderer restore s novou document identity, BFCache restore, prerender activation a jiná změna execution documentu vytvoří nový epoch podle browser adapter evidence. Same-document hash/history navigation zachová document epoch, ale zvýší `navigationSequence`. Target reference a mutující action jsou vždy vázány nejméně na document epoch a frame attachment epoch.

Stale page/frame/document event nebo host response se uloží jako raw evidence, ale nesmí změnit active page, current observation, action outcome ani postcondition projection.

### 13.6 Lifecycle, control authority a one-writer invariant

Lifecycle používá:

```text
CREATING
READY
ACTIVE
CHALLENGE_REQUIRED
PAUSED
RECOVERING
CLOSING
CLOSED
FAILED
EXPIRED
```

Control holder používá `NONE`, `AI`, `OWNER`, `AUTOMATION`. Povolené lifecycle hrany a terminalita jsou v 49.19.

Session má nejvýše jeden active write control lease enforced PostgreSQL partial unique constraintem. Lease obsahuje platform incarnation, holder, control epoch, fencing token, session/context generation, state version a DB-time expiry. Heartbeat expired lease neoživí. Každé grant/regrant/takeover/return/recovery attachment inkrementuje control epoch; epoch ani fence se nikdy nerecyklují.

Každý input/action nese exact session, context, control epoch, lease fence, page/page generation, frame/attachment epoch, document epoch, observation revision, viewport transform a operation scope. Stale hodnoty se odmítnou před browser dispatch. Více OWNER viewers je dovoleno; pouze current OWNER holder zapisuje.

Control lease je autorita k odeslání typed browser inputu, nikoli business oprávnění k libovolnému cíli. Operation scope určuje exact business cíl a postcondition; nejde o nový user permission systém. OWNER může scope běžným jazykem změnit nebo založit nový command s plnou OWNER autoritou. Pending action zůstává vázaná na původní immutable scope digest.

Browser host ani bridge nepovažuje otevřený context, active page, poslední preview connection nebo lokální focus za control authority. Každý write command ověřuje current DB-derived lease snapshot a action fence.

### 13.7 OWNER takeover, návrat AI a input-state barrier

Takeover je persistentní control-transfer operation:

```text
REQUESTED → DRAINING → GRANTED | REJECTED | CANCELLED | MANUAL_REVIEW
```

V `DRAINING` se zastaví nové actions, read-only se mohou abortovat a pending mutation se dokončí nebo reconciliuje. Po safe checkpointu jedna transakce revokuje starý lease, inkrementuje epoch a vydá nový OWNER lease. Do `GRANTED` nezapisuje ani starý, ani budoucí holder.

Před grantem runtime uzavře nebo resetuje nedokončenou low-level input sequence: pressed keys, pointer buttons, pointer capture, touch identifiers, wheel gesture, drag state, IME/composition state a pending file chooser/dialog waiter. Reset sám nesmí vyvolat další stránkový side effect; pokud mohl input již stránku změnit, action se nejprve reconciliuje.

Return-to-AI používá stejný transfer. Uloží OWNER input sequence, current page/frame/document tree, active element, dialogs, downloads, network/navigation summary a fresh observation. AI pokračuje pouze s novým epoch, novou planning observation a revalidovanými target references. OWNER low-level input se nepřehrává modelu jako neověřený příkaz; teaching compiler jej může samostatně převést na typed evidence.

Preview reconnect ani otevření druhého UI tabu control holder nemění. Klient nesmí po reconnectu automaticky replayovat neacknowledged pointer/key/text input. Po snapshot acknowledgmentu lze vytvářet pouze nové input sequence pod current stream/control epoch.

### 13.8 Live preview protocol `KBPP/1.0`

Preview používá same-origin WebSocket:

```text
WSS /api/v1/browser-sessions/:sessionId/preview/ws
Sec-WebSocket-Protocol: kbpp.v1
```

Browser klient používá same-origin OWNER cookie. Strojový klient může poslat `Authorization: Bearer <KCML_OWNER_API_KEY>` v handshake. Klient, který nemůže nastavit handshake header, pošle one-time ticket jako druhou hodnotu `Sec-WebSocket-Protocol: kbpp.v1, kbpp.ticket.<opaque>`; server zvolí `kbpp.v1`, ticket spotřebuje a jeho hodnotu neloguje.

OWNER browser používá secure OWNER session cookie a povinnou Origin kontrolu. One-time ticket je session-bound, audience-bound, capability-bound, single-use, krátkodobý a po handshake se revokuje. Produkční bearer token se nikdy nevkládá do URL. Handshake i ticket response používají `Cache-Control: no-store`; reverse proxy nesmí ticket zapisovat do access logu.

Každá zpráva používá envelope:

```json
{
  "protocol": "KBPP/1.0",
  "sessionId": "uuid",
  "streamEpoch": 3,
  "controlEpoch": 7,
  "sequence": 151,
  "pageId": "uuid",
  "pageGeneration": 2,
  "documentEpoch": 18,
  "frameRevision": 108,
  "observationRevision": 42,
  "sentAt": "RFC3339",
  "type": "frame",
  "payload": {}
}
```

Server event types:

```text
hello
snapshot
frame
frame.patch
viewport
page.tree
frame.tree
document.changed
observation
state
control.changed
action.started
action.dispatch
action.target
action.completed
action.failed
action.reconciliation.required
challenge.required
challenge.resolved
dialog
permission.requested
download
upload.requested
resync.required
heartbeat
error
```

Client input types:

```text
ack
control.acquire
control.release
pointer.move
pointer.down
pointer.up
wheel
key.down
key.up
text.insert
secret.insert
composition.start
composition.update
composition.end
touch
viewport.resize
page.activate
target.pick
target.clear
snapshot.request
dialog.respond
permission.respond
challenge.respond
```

Frame payload je plný compressed image nebo rectangular patch. Klient potvrzuje nejvyšší zpracovanou revision. Při backpressure server zahodí mezilehlé non-key frames a odešle nejnovější key frame; nikdy nezpozdí browser action, cancellation nebo takeover kvůli pomalému vieweru. Reconnect používá fresh session snapshot, nový `streamEpoch`, current control/page/document identity a poslední key frame, nikoli replay všech obrazových frameů nebo vstupů.

Chromium host může pro obraz použít pinovaný private screencast adapter. Canonical fallback pro všechny engines používá screenshot capture a případné image patches. Preview implementace není součástí automation action semantics; změna capture adapteru nesmí změnit page/document identity, locator resolution ani side-effect pipeline.

### 13.9 Viewport, souřadnice a přesný vstup

Každý preview frame obsahuje:

- content viewport v CSS pixels,
- image dimensions a letterboxing/crop rect,
- device pixel ratio,
- browser page zoom a visual viewport scale,
- visual viewport offset,
- layout viewport scroll position,
- client render CSS dimensions,
- active page, top-level frame a document epoch,
- frame revision a observation revision,
- nested frame transform route pro target picker,
- canonical transform digest.

Pointer nebo touch input nese `frameRevision`, `observationRevision`, page/document identity, normalized coordinates, button/touch identifiers, modifiers, client viewport dimensions a transform digest. Server použije transform patřící přesně k danému frame. Input na stale frame, jiný page ID, změněný document, viewport, zoom, letterbox nebo control epoch vrací `BROWSER_VIEWPORT_STALE` nebo `BROWSER_DOCUMENT_STALE` a fresh snapshot; server jej nepřepočítá odhadem.

Nested iframe target picker skládá top-page transform, frame box, iframe border/scroll, visual viewport a případný OOPIF transform. Neúplný nebo neověřitelný transform může sloužit k náhledu, nikoli k mutujícímu clicku.

Před textovým nebo secret vstupem runtime ověří active element a vrátí focus descriptor: page/frame/document identity, element role, accessible name, input type, autocomplete, contenteditable/masked state, readonly/disabled state, current value digest, selection range a target digest. Vstup do jiného než potvrzeného active elementu se odmítne.

Textová strategie je explicitní:

- `SET_VALUE` pro bezpečně podporovaný fill a následné input/change semantics,
- `TYPE_KEYS` pro aplikace závislé na jednotlivých key events,
- `INSERT_TEXT` pro bounded direct text input,
- `COMPOSITION` pro IME/composition workflow,
- `NATIVE_INPUT` přes bridge pro nativní pole nebo platformní UI.

Runtime předem deklaruje, zda může krok auto-submitnout, formátovat, maskovat, změnit focus nebo vyvolat `input`, `change`, `blur`, validation či navigation. Pro date/time/number pole, locale-dependent parsing, contenteditable, rich-text editor, autocomplete a masked input se používá provider/field adapter s postcondition, nikoli tichá obecná konverze.

OWNER direct pointer ovládání se auditně ukládá jako typované input events s bounded metadata. High-frequency pointer move se agreguje; pointer down/up, click, key, text, composition, focus, blur, navigation a side-effect relevant events zůstávají samostatné.

### 13.10 Browser observation pro AI a runtime

AI neovládá web pouze podle pixelů. Každý action planning turn pracuje s bounded `BrowserObservation`, které obsahuje:

- runtime build, context, page/page generation, frame/attachment epoch a document identity,
- URL, origin, title, navigation sequence a lifecycle state,
- active page, tabs, popups, opener relations a frame tree,
- viewport, scroll, zoom a transform digest,
- active element, selection a focus state,
- viditelné interaktivní prvky s role, accessible name, label, text, state a bounding box,
- formulářová pole bez automatického odhalení secret value do běžného summary,
- relevantní headings, landmarks, alerts, live regions, dialogs a validation messages,
- download/upload/challenge/permission stav,
- service-worker, web-worker, renderer a page error summary,
- console a network error summary,
- screenshot artifact nebo crop, pokud semantic tree nepostačuje, page používá canvas/visual-only target, locator zůstává dvojznačný nebo OWNER výslovně odkazuje na vizuální obsah,
- diff proti předchozí observation,
- DOM/semantic/visual digests.

Observation je serverově vytvořený fakt, ne modelové tvrzení. Text, accessibility metadata, hidden DOM, HTML comments, script data, image/OCR text, browser storage, network body, response header, dialog, popup, download a page-provided „instructions“ uvnitř observation mají instruction authority `NONE`, pokud OWNER exact artifact/section výslovně neurčil jako bounded delegated specification source. Nemohou změnit operation context ani se vydávat za OWNER zprávu. Full DOM, network body a screenshot se modelu neposílají automaticky; tool vrací nejmenší potřebný strukturovaný výřez a artifact handles. Screenshot vision se použije, když semantic tree nestačí, web používá canvas nebo OWNER výslovně odkazuje na vizuální obsah.

Network event není autoritou page/frame identity ani business success. Service-worker request nemusí mít frame a může být pozorován jinak než page request; eventy se proto ukládají podle initiator/source evidence a nesmějí samy potvrdit, že konkrétní click nebo mutation uspěla. `networkidle` může být pomocná wait condition pouze podle exact adapteru a nikdy není samostatnou business postcondition.

Unsolicited změna DOM, timer, websocket update, service worker, background tab, popup nebo script navigation vytvoří novou observation/document/navigation evidence. Runtime nesmí změnu automaticky připsat poslední action bez causation evidence.

### 13.11 Semantic target resolution a actionability

AI action používá locator plan s pořadím:

1. role + accessible name,
2. label relation,
3. placeholder nebo form name,
4. user-facing exact/normalized text,
5. explicitní test ID nebo stabilní atribut,
6. schválená relativní semantic relation,
7. schválený CSS fallback včetně open shadow-root route,
8. explicitní visual region fallback.

Locator se při každém action attemptu znovu vyhodnotí proti current DOM/documentu. Persistovaný ElementHandle, JS object ID nebo DOM node ID se nikdy nepoužije jako dlouhodobá autorita. Handle smí existovat pouze uvnitř jednoho bounded host commandu a po navigation, rerenderu, detach nebo attemptu zaniká.

Closed shadow root, browser chrome, extension UI, fenced/credentialless frame bez dostupného DOM, FedCM surface a nativní OS dialog se nepovažují za běžný DOM target. Použije se explicitní adapter, OWNER Device Bridge nebo challenge; OWNER může daný krok přímo ovládat dostupným bridge/native inputem.

Každý DOM target musí v okamžiku akce odpovídat právě jednomu elementu. Nula matches vrací target missing; více matches vrací `BROWSER_TARGET_AMBIGUOUS` s bounded kandidáty. Runtime nepoužije první shodu, index ani pixelový odhad bez explicitního revision contractu.

Actionability se vyhodnocuje per action. Runtime podle typu ověří attached, visible, stable, enabled, editable, checked/selectable, receives-events, focusable, scrollable a expected hit target. Sticky header, nested scroll container, CSS transform, animation, virtualized list, pointer-events, pointer capture a transient overlay jsou součástí evidence. Runtime může použít non-mutating trial check a explicitní scroll/re-observation. `force` je povolen pouze exact revision stepem s uvedeným důvodem, target identity, mutation trigger a postcondition; nesmí obecně obcházet ambiguity, wrong frame, stale document nebo disabled business state.

Overlay se automaticky nezavírá jen proto, že blokuje target. Consent, terms, subscription, unsaved-changes, security nebo destructive overlay je samostatný semantic target a action. Pouze exact adapter může zavřít čistě prezentační overlay, pokud je tento účinek deklarovaný a ověřený.

Každá akce publikuje target highlight overlay s action label, locator summary a candidate count. Overlay je pouze preview layer, nemění DOM cílové stránky a není součástí page screenshot artifactu, pokud artifact contract výslovně nepožaduje UI evidence.

### 13.12 OWNER target references a společné porozumění

OWNER může v preview zapnout **Vybrat prvek**, kliknout na prvek, označit text nebo vymezit oblast. Vznikne `Browser target reference` připojitelná jako chip do chatové zprávy.

Target kind používá:

```text
DOM_ELEMENT
TEXT_RANGE
FRAME
PAGE
VISUAL_REGION
```

Reference obsahuje:

```json
{
  "targetReferenceId": "uuid",
  "sessionId": "uuid",
  "contextGeneration": 4,
  "pageId": "uuid",
  "pageGeneration": 2,
  "frameId": "uuid",
  "frameAttachmentEpoch": 6,
  "documentId": "opaque",
  "documentEpoch": 18,
  "navigationSequence": 33,
  "frameRevision": 108,
  "observationRevision": 42,
  "viewportTransformDigest": "sha256:...",
  "kind": "DOM_ELEMENT",
  "semanticLocator": {},
  "elementFingerprint": "sha256:...",
  "accessibleName": "Uložit",
  "textDigest": "sha256:...",
  "boundingBox": {"x": 810, "y": 612, "width": 96, "height": 36},
  "createdBy": "OWNER",
  "expiresAt": "RFC3339"
}
```

Při pozdějším použití se reference re-resolvuje podle current page/frame/document identity, semantic locatoru, fingerprintu a expected role/state. Bounding box je evidence, nikoli autoritativní locator. Změna document epoch reference vždy zneplatní pro přímý dispatch; runtime může nabídnout fresh semantic rebind candidate, který musí znovu splnit uniqueness a scope.

`VISUAL_REGION` může být použit pro čtení, screenshot crop, vision analýzu nebo přímé OWNER ovládání. Unattended mutující akce nad visual regionem vyžaduje převod na jednoznačný semantic/provider target s postcondition. Exact OWNER confirmation může povolit jeden current visual input attempt; confirmation je vázaná na document/viewport/control epoch a není reusable automation locator.

### 13.13 Browser actions, mutation trigger a dispatch evidence

Interaktivní session poskytuje serverové tools:

```text
session.create
session.attach
session.observe
session.captureState
session.saveAccount
session.pause
session.resume
session.close
page.open
page.navigate
page.reload
page.back
page.forward
page.activate
page.close
page.wait
page.screenshot
page.permissions
page.dialog.respond
target.resolve
target.highlight
target.pick
locator.click
locator.doubleClick
locator.hover
locator.focus
locator.blur
locator.fill
locator.type
locator.insertText
locator.fillSecret
locator.clear
locator.select
locator.check
locator.uncheck
locator.press
locator.scroll
locator.scrollIntoView
locator.dragDrop
locator.touch
file.upload
download.await
content.extract
condition.assert
auth.verify
challenge.respond
```

Každý tool používá strict input/output schema. Target action přijímá exact target reference nebo locator plan a explicitní page/frame selection. Tool output obsahuje full browser identity snapshot, pre-observation digest, action attempt, target/actionability evidence, dispatch phase, mutation trigger, post-observation digest, postcondition, URL/page/document changes, artifacts, side-effect classification, reconciliation outcome a correlation ID.

Před každou mutující action musí být persistentně určeno:

- logical operation, idempotency scope/key a request digest,
- exact session/host-or-bridge/context/page/frame/document/control identity,
- exact action schema a locator/target digest,
- earliest possible mutation trigger,
- expected external/account/resource side effect,
- precondition a independent read-back/postcondition,
- retry/reconciliation class a concurrency key,
- pre-armed popup/navigation/dialog/filechooser/download waiter podle action.

`mutationTrigger` používá:

```text
NONE
POINTER_DOWN
POINTER_UP
CLICK
DOUBLE_CLICK
KEYSTROKE
TEXT_INPUT
INPUT_EVENT
CHANGE_EVENT
BLUR
SUBMIT
NAVIGATION
UPLOAD_SELECTION
DROP
DIALOG_RESPONSE
PERMISSION_GRANT
WEBAUTHN_ASSERTION
WEBAUTHN_REGISTRATION
EXTERNAL_DEVICE_CONFIRMATION
UNKNOWN
```

Fill, type, select, check, upload, press, blur a focus change mohou být mutující i bez explicitního Save tlačítka kvůli autosave, `input`, `change`, `blur`, auto-submit nebo application listeneru. Side-effect intent se proto commitne před nejčasnějším deklarovaným triggerem, nikoli až před očekávaným submit requestem.

Browser host vrací monotonic dispatch evidence nejméně pro:

```text
COMMAND_ACCEPTED
TARGET_RESOLVED
ACTIONABILITY_PASSED
INPUT_SEQUENCE_STARTED
MUTATION_TRIGGER_POSSIBLY_ISSUED
NAVIGATION_OR_REQUEST_OBSERVED
METHOD_RETURNED
POST_OBSERVATION_CAPTURED
```

Absence pozdější phase nedokazuje, že předchozí side effect nenastal. Pokud host/worker/renderer connection zanikne po `MUTATION_TRIGGER_POSSIBLY_ISSUED`, nový attempt je zakázaný do reconciliation.

Listener/waiter pro popup, page, navigation, download, dialog nebo file chooser se registruje před trigger action. Pozdější registrace, která mohla event minout, nesmí prohlásit `NOT_APPLIED`; používá postcondition/read-back nebo `UNKNOWN`.

Libovolné JavaScript evaluation není model-visible standardní tool. Diagnostická nebo extraction expression musí být declarative, schema-validovaná, bounded, read-only a součástí schváleného adapteru. OWNER technický debug panel může zobrazit interní Playwright/protocol detail a spustit canonical diagnostické akce, ale generovaná automatizace nevkládá neomezený script step.

### 13.14 Operation scope, confirmation a external protocol

Každá mutující browser session používá active operation scope odvozený z exact OWNER message, OWNER-intent digestu, agent revision nebo automation revision. Scope je součástí frozen operation contextu a obsahuje:

- parent a source message/revision,
- allowed origins, redirect origins a external-protocol targety,
- allowed operation classes a max side-effect class,
- account binding a expected account/tenant identity,
- resource/record constraints,
- data extraction, clipboard, upload/download a local-target constraints,
- time window a expiration,
- confirmation/challenge policy,
- canonical JSON a digest.

Příkaz „přihlas se a změň adresu účtu na X“ dovoluje běžné login, navigation, fill a Save kroky potřebné k tomuto cíli. Neumožňuje nákup, smazání účtu ani změnu jiného profilu. Běžné Save/Continue uvnitř jednoznačného scope nevyžaduje opakovanou otázku.

Page content, redirect text, popup, banner, support chat, QR/OCR text, downloaded file ani external protocol description nemůže scope rozšířit nebo změnit target. Hodnota získaná z webu může vyplnit pouze explicitní data nebo dynamic-target slot deklarovaný OWNER intentem či active automation revision; runtime uloží `value_derivation` se source observation/artifact digestem, locator/path, transformací, destination argument path a constraint validation.

Nevratná, destructive, finanční nebo novým následkem rozšiřující akce používá exact confirmation. Confirmation obsahuje current page/frame/document/target digest, canonical arguments, očekávaný následek, expiry, account identity a control epoch. Změna stránky, documentu, targetu, argumentu, accountu, scope nebo epochy confirmation zneplatní.

Navigation, redirect, popup, iframe URL, download source, form target a external protocol request procházejí origin/DNS/IP/local-target policy při každém přechodu. `mailto:`, custom scheme, native app handoff nebo OS prompt se nikdy nepovažuje za běžnou web navigation; používá typed external-protocol challenge nebo bridge operation s exact targetem a postcondition.

### 13.15 Přihlášení, credentials, account continuity a state bundle

OWNER může přihlášení provést:

1. vložením Secret stable name nebo credential do dedikovaného pole,
2. zadáním credential v trusted chatu,
3. přímým převzetím browseru a přihlášením,
4. vyřešením challenge nebo device-bound loginu přes OWNER Device Bridge,
5. použitím exact client-certificate nebo virtual-authenticator bindingu podle account policy.

Akce **Uložit účet** vytvoří nebo aktualizuje `browser_account_binding`. Binding obsahuje target/service, stable account key, očekávanou account/tenant identity, username metadata, credential Secret refs, Browser state bundle, auth mode, ověřovací authenticated condition, allowed origins, external session family, auth epoch, account concurrency mode, expiration, rotation relation, poslední úspěšné ověření a usage.

Authenticated condition musí prokázat konkrétní účet a tenant/organization, pokud služba tenant používá. Pouhá absence login formuláře, přítomnost session cookie nebo HTTP 200 nestačí. Postcondition používá semantic account marker, provider account API, profile identity nebo jiný exact adapter. Pokud je účet nejednoznačný, session přejde do reauthentication/challenge a nesmí provést business mutaci.

Přímé OWNER psaní hesla do webové stránky samo o sobě nevytvoří dlouhodobý password Secret. OWNER zvolí **Uložit credential** nebo použije dedikované credential pole. Session cookies a storage lze uložit samostatně jako Browser state bundle, pokud OWNER požádá o zachování přihlášení.

Browser state bundle je encrypted secret-grade immutable artifact. Podle engine capability a explicitního serializer contractu může obsahovat:

- cookies včetně domain, path, SameSite, secure/httpOnly, expiry a partition/partition-key metadata,
- localStorage oddělený podle exact origin,
- explicitně povolený IndexedDB auth state,
- deklarovaný sessionStorage snapshot per page/origin,
- origin permissions potřebná pro workflow,
- client-certificate binding metadata bez exportu neexportovatelného private key,
- pouze explicitní virtual WebAuthn credential testovacího/automatizačního účtu,
- origin inventory, account/auth epoch, credential version, engine/build/serializer capability a content digest.

Bundle standardně neobsahuje browser cache, HTTP cache, service-worker executable/runtime state, memory-only JS state, open websocket, unsaved form, raw browser profile, extension state, download temp directory, OS credential store ani reálnou platformní passkey/private key. Provider-specific adapter může přidat další state pouze jako verzovaný, šifrovaný, testovaný bundle member s compatibility a cleanup contractem.

Capture probíhá po mutation barrieru a quiescent authenticated postcondition. Runtime nejprve ověří, že neexistuje pending unknown side effect, stabilizuje relevantní pages, přesně inventarizuje origins/storage kinds a teprve potom vytvoří candidate bundle. Aktivace pointeru je CAS nad account binding/auth epoch/credential version.

Restore vždy vytváří fresh context nebo exact bridge profile attachment. Cookies/storage se instalují před první page navigation podle serializer order; sessionStorage se vkládá před application bootstrap pouze přes explicitní init adapter. Poté runtime otevře safe start URL, ověří current account/tenant, credential/auth epoch a required origin state. Browser cache nebo service-worker readiness se znovu vytvoří webem a nesmí nahrazovat authenticated condition.

Third-party cookie policy, storage partitioning, anti-CSRF, device/IP binding, risk challenge, provider session-family limit nebo credential rotation mohou bundle učinit neportable. V takovém případě runtime nepokračuje s částečně přihlášeným stavem; provede deterministic relogin nebo `CHALLENGE_REQUIRED`.

Rotace password/OAuth/certificate credentialu, explicitní logout, account disable, provider invalidation, změna expected account identity nebo account auth epoch invaliduje dotčené bundles a živé session podle relation. Concurrent logout nebo credential rotation nesmí být přepsány starým bundle capturem.

### 13.16 OTP, push, WebAuthn, passkeys, CAPTCHA, client certificates a native challenge

Challenge types:

```text
PASSWORD_REENTRY
EMAIL_OTP
SMS_OTP
TOTP
PUSH_CONFIRMATION
WEBAUTHN_ASSERTION
WEBAUTHN_REGISTRATION
PLATFORM_PASSKEY
CAPTCHA
HUMAN_VERIFICATION
CLIENT_CERTIFICATE
FEDERATED_NATIVE_PROMPT
EXTERNAL_DEVICE_ACTION
NATIVE_BROWSER_DIALOG
PERMISSION_PROMPT
```

Při challenge session přejde do `CHALLENGE_REQUIRED`, uloží checkpoint, zachová bounded BrowserContext/profile lease a zobrazí OWNERovi current preview, safe popis, exact origin/RP/account, expiry a možné způsoby vyřešení. AI nevymýšlí OTP, neobchází CAPTCHA a nepotvrzuje device action za OWNERa.

Email/SMS/TOTP response je ephemeral challenge value, pokud OWNER výslovně nezvolí uložení TOTP seedu v Secret Manageru. TOTP generator používá current secret version, bounded clock-skew policy a one-attempt consumption evidence. Pole, které po poslední číslici auto-submitne, má mutation intent před vložením první hodnoty podle adapter contractu.

Push confirmation může doběhnout po UI timeoutu, reloadu nebo ztrátě connection. Timeout proto neznamená `NOT_APPLIED`; runtime znovu ověří challenge/account stav a pokračuje, zamítne nebo přejde do reconciliation.

WebAuthn assertion a WebAuthn registration jsou odlišné operation classes. Assertion potvrzuje autentizaci; registration přidává credential k externímu účtu a je mutující akcí s exact account/RP/postcondition. Conditional mediation, platform authenticator picker, biometric/PIN dialog a cross-device QR flow jsou OWNER/bridge challenge, pokud nejsou deterministicky obslouženy explicitním adapterem.

Virtual WebAuthn authenticator je povolen pouze pro explicitní automation/test account policy. Je context-scoped, capability-pinned a jeho credential/private material je secret-grade bundle member. Nesmí simulovat OWNERovu reálnou platformní passkey nebo obcházet providerem vyžadovanou device-bound autentizaci.

Reálná platformní passkey, secure enclave/TPM credential a private key zařízení se nekopíruje do serverového bundle. Použije se current OWNER Device Bridge session nebo human challenge. Bridge vrací pouze typed assertion/registration outcome a evidence potřebnou pro postcondition, nikoli export private key.

Client certificate je exact-origin context/profile capability. Server-managed context používá explicitní Secret/certificate binding při context creation; změna certifikátu nebo origin inventory vyžaduje nový context generation. Certifikát z OS keychainu nebo neexportovatelného hardware store používá bridge. Výběr nejednoznačného certifikátu je OWNER challenge; runtime nesmí náhodně zvolit první.

CAPTCHA/human challenge lze vyřešit OWNERem v current preview nebo bridge. Po resolution se vždy obnoví page/frame/document observation a pending action se revaliduje. Challenge resolution sama nepotvrzuje business mutation následujícího kroku.

Script dialog `alert`, `confirm`, `prompt` a `beforeunload` je typed event. Step, který jej může vyvolat, musí před triggerem deklarovat policy a pre-arm listener. Defaultní tiché dismiss/accept se pro mutující workflow nepoužívá. Native print/save/open-file/browser permission surface používá bridge/challenge nebo explicitní browser adapter.

### 13.17 Tabs, popups, frames, navigation a background actors

Session udržuje explicitní page tree. Každá page má stable page ID, generation, opaque runtime handle, opener relation, origin, title, lifecycle, current document identity, navigation sequence, active-preview flag a close state. Preview-active page je UI preference; nemění Playwright autoritu a ostatní pages mohou dál navigovat nebo přijímat events podle webu.

Popup/page listener se pre-armuje na BrowserContext nebo exact opener před trigger action. Runtime eviduje `target=_blank`, `noopener`, popup blocked, opener navigation/close, popup immediate close, window-name reuse a download zahájený v popupu. Popup bez opener relation se přiřadí podle causation window a context event evidence; nejednoznačný popup se nepoužije pro mutaci.

Frame tree používá stable KájovoCML frame IDs, attachment epochs, parent relation, sandbox/permission attributes, origin, URL, name, document identity a OOPIF route. Cross-origin a OOPIF frame zůstává samostatným execution contextem. Frame detach/reattach, process swap nebo document replacement zneplatní target/handle z předchozího epochu.

Sandboxed, credentialless nebo fenced frame, který neposkytuje běžný DOM/Playwright access, se označí explicitní capability state. OWNER jej může obsloužit vizuálně nebo přes bridge; unattended action vyžaduje provider adapter nebo verifikovatelný visual contract.

Navigation state rozlišuje:

```text
REQUESTED
STARTED
COMMITTED
DOM_CONTENT_LOADED
LOAD_FIRED
SAME_DOCUMENT
BF_CACHE_RESTORED
PRERENDER_ACTIVATED
ABORTED
FAILED
```

Business krok deklaruje, na kterou fázi čeká a jaká je jeho skutečná postcondition. `load` ani `networkidle` samy nepotvrzují uložení, nákup, odeslání nebo login.

SPA route change, History API, hash navigation, DOM replacement bez URL změny, BFCache restore a prerender activation vytvářejí explicitní navigation/document evidence. Runtime neodvozuje fresh document pouze z URL a neodvozuje stejný document pouze z toho, že URL zůstala stejná.

Service workers, shared/dedicated workers, timers, websockety a background pages jsou non-authoritative actors. Jejich events mohou vysvětlit změnu, ale business causation a postcondition musí zůstat svázána s action logical operation a exact read-back.

### 13.18 Uploads, downloads, clipboard a artifacts

Upload používá pouze run/session-scoped upload handle vytvořený bezpečným API. Runtime nikdy nepřijímá arbitrary host path. Upload handle obsahuje artifact ID, owner session/run, safe name, MIME, extension, size, digest, sensitivity, expiry a allowed target policy.

File chooser nebo drag/drop waiter se pre-armuje před trigger action. Chooser je svázaný s exact page/frame/document/target/control epoch a deklaruje single/multiple/directory acceptance. Upload může použít artifact bytes, generated bounded buffer nebo bridge-local file handle schválený OWNERem; bridge nevrací arbitrary filesystem path serveru. Directory upload ukládá relative-path manifest a každý file digest.

Download event znamená pouze zahájení downloadu. Runtime čeká na completion nebo typed failure, bezpečně persistuje bytes z browser temp directory před context close, ověří source/initiator origin, suggested a safe name, MIME, size, digest a případně content schema. Blob/data URL ukládá initiator page/frame/document a creation causation. Automatizace nepovažuje pouhou event nebo HTTP status za úspěch.

Clipboard read/write, pokud workflow používá, je typed browser/bridge operation s exact data kind, scope, sensitivity, pre/post evidence a cleanup. OWNER direct clipboard práce zůstává dostupná; unattended automation ji používá pouze při deklarované revision capability.

Artifacts zahrnují screenshot, crop, trace, observation, page snapshot, upload, download, state bundle, dialog, permission, auth/challenge, WebAuthn/client-cert, navigation a recovery evidence. Velké artifacts se nepřenášejí preview socketem; event obsahuje authorized artifact handle.

Context close může odstranit browser-managed temporary downloads. Cleanup proto nejprve dokončí nebo zruší downloads, persistuje required artifacts a teprve potom zavře context.

### 13.19 Recovery, reconnect a pending mutation reconciliation

Worker restart claimne session/action recovery pod novým fence. Připojení k host contextu vyžaduje shodu platform incarnation, session ID, runtime build manifest, host slot, host generation, context ID/generation a current control/action fences. Nový worker provede fresh page/frame/document inventory a observation; pozdní worker/host/bridge response se neautorituje.

Browser process nebo renderer crash obnoví nový context pouze z posledního potvrzeného compatible state bundle a safe URL/page planu. In-memory JavaScript, form values, sessionStorage bez explicitního adapteru, open popup, pending dialog, unsaved data, service worker, cache a lokální focus nejsou předpokládány jako obnovené.

Každá browser action má persistentní dispatch ledger a side-effect operation. Recovery rozhoduje podle nejvyšší potvrzené phase:

1. před commitnutým intentem — action neměla platformou povolený browser side effect,
2. po intentu, ale před host acceptance — lze bezpečně cancelnout nebo znovu vydat stejnou logical operation po exact authority checku,
3. po host acceptance, ale před možným mutation triggerem — retry je možný pouze při explicitním host evidence `NOT_TRIGGERED`,
4. po možném mutation triggeru — povinná independent reconciliation,
5. po validní postcondition a terminal DB commitu — retry pouze načte canonical outcome.

Reconciliation používá operation-specific read-back, pokud možno nezávislý na původním transient UI: exact account/tenant/resource identity, provider confirmation ID, changed record value/version, transaction/order ID, server API, activity/audit entry, download digest nebo jiný schválený evidence contract. Playwright method success, disappearance loaderu, URL, screenshot nebo network event samy o sobě nejsou univerzální read-back.

Výsledek je:

```text
CONFIRMED_APPLIED
CONFIRMED_NOT_APPLIED
UNKNOWN
```

`UNKNOWN` vede do `MANUAL_REVIEW` a blokuje nový mutující action na stejném account/resource/external-session concurrency key. OWNER může z exact evidence potvrdit outcome nebo povolit novou business operation; původní action se nikdy tiše neopakuje s novým logical operation ID.

Preview disconnect nemění lifecycle ani holder. Reconnect používá nový stream epoch a current snapshot. OWNER input se povolí až po potvrzení current page/document/frame/control/transform identity; žádný neacknowledged input se nereplayuje.

### 13.20 Resource, account concurrency a scheduling policy

Preview používá system-managed adaptive frame policy, bounded contexts/hosts, semantic observations, frame dropping a reusable auth state. Optimalizace nikdy neoslabí control fence, page/frame/document freshness, action intent, reconciliation, cleanup ani operation scope.

Browser account binding deklaruje concurrency mode:

```text
EXCLUSIVE_MUTATION
SERIALIZED
READ_SHARED
PROVIDER_LIMITED
```

`EXCLUSIVE_MUTATION` povolí právě jednu živou mutující session v external session family. `SERIALIZED` dovolí více připravených sessions, ale mutace používají jeden persistentní account/resource claim. `READ_SHARED` dovolí souběžné read-only sessions, přičemž login/logout/credential rotation/capture state jsou stále serializované. `PROVIDER_LIMITED` používá explicitní provider adapter a limit; nesmí se odhadnout z úspěchu předchozího běhu.

Server-managed a bridge session pro stejný účet sdílejí account auth epoch a concurrency claims. Logout, credential rotation, account switch nebo provider session invalidation v jedné session zneplatní ostatní podle binding relation a vytvoří fresh observation/reauth, nikoli tichý pokračující stav.

Periodická automatizace má timezone, DST policy, misfire/catch-up policy a overlap policy:

```text
SKIP
QUEUE
COALESCE
REJECT
```

Schedule nevytváří skrytý nekonečný browser. Každý run má bounded deadline, context/profile lease, max pages, max actions, artifact budget a cleanup. Persistentní monitoring používá event/schedule runy a stavové read-backy, nikoli nepřetržitý neauditovaný AI browser.

### 13.21 Cleanup

Cancel, completion, expiry, revocation, logout, bridge disconnect, host replacement a failure vytvoří persistentní cleanup operation. Kroky jsou:

1. revoke preview tickets a stop new input/action admission,
2. revoke nebo release control lease a bridge assignment,
3. freeze current page/frame/document inventory,
4. reconcile pending mutations, dialogs, challenges, uploads a downloads,
5. persist final required artifacts a optional confirmed state-bundle capture,
6. remove routes, event listeners, bindings, exposed functions, init scripts, permission grants, virtual authenticators, client-certificate handles a temporary profile attachments,
7. close pages/context nebo release exact bridge profile lease,
8. remove download/upload/temp/profile directories a ephemeral credential handles,
9. release account/resource/capacity/concurrency claims,
10. verify host/bridge no longer exposes the context and commit terminal audit.

Cleanup je idempotentní checkpointovaná state machine 49.20. Terminal session od první terminal lifecycle transakce nepřijímá nové actions, tickets ani control leases, i když cleanup pokračuje. `COMPLETE` je podmínkou closure; orphan page/context/profile, event route, preview ticket, bridge assignment, virtual authenticator, client-certificate handle, download temp file, account/capacity claim nebo temporary credential handle je blocking hygiene failure.

Boot recovery inventarizuje host contexts, bridge assignments, profile locks, temporary directories a DB sessions. Resource bez current DB ownera se cleanupne; DB session bez live resource přejde do recovery/reconciliation. Similar-looking raw browser handle se nikdy nepřipojí bez shody celé identity n-tice.

## 14. Browser Automation Runtime, teaching a deterministický replay

### 14.1 Kanonická komponenta

Browser Automation Runtime je jedna platformní komponenta se stabilní component a runtime identitou, active revision, tool contracts, exact contract/Secret bindings, monitoringem, auditem, enable/disable a readiness. Používá Browser Interaction Plane z kapitoly 13; nevytváří vlastní browser host, auth storage, bridge protocol, preview protokol ani paralelní page/frame identity.

Runtime interpretuje immutable automation revision a vytváří běžnou `browser_session`, `browser_action_run`, `side_effect_operation`, challenge, artifact a cleanup záznamy. Stejný action interpreter používají preflight, verification, teaching replay, produkční run a self-test. Rozdíl je pouze v povoleném side-effect profile a test fixture, nikoli v jiné implementaci.

### 14.2 Standardní tools

Runtime vystavuje:

```text
browserAutomation.preflight
browserAutomation.verify
browserAutomation.run
browserAutomation.status
browserAutomation.cancel
browserAutomation.history
browserAutomation.reauthenticate
browserAutomation.reconcile
browserAutomation.artifact.get
browserAutomation.repair
browserAutomation.teaching.start
browserAutomation.teaching.stop
browserAutomation.teaching.compile
browserAutomation.preview.open
browserAutomation.schedule.evaluate
browserAutomation.account.verify
```

Každý tool má strict schema, exact revision digest, logical operation, idempotency, deadline, concurrency a typed outcome. `run`, `cancel`, `reauthenticate`, `reconcile`, `repair` a schedule-triggered start jsou mutující doménové operace; preview nebo status endpoint je nemůže nahradit.

### 14.3 Definition a revision

Automation definition je stabilní identita. Revision je immutable manifest s digestem, purpose, allowed origins, auth/account policy, typed inputs, typed outputs, ordered steps, artifacts, side-effect model, retry/reconciliation model, resource limits, schedule/event eligibility, verification evidence a compatibility relations.

Revision odkazuje na:

- exact Browser Interaction Plane contract version,
- exact locator compiler a automation interpreter version,
- supported engines a `BrowserRuntimeBuildManifest` compatibility set,
- required page/frame/document identity capabilities,
- state-bundle serializer and auth adapter versions,
- required bridge capability fingerprint podle platformy,
- action schema versions a mutation-trigger semantics,
- target account concurrency a external-session policy,
- schedule timezone, DST, misfire, overlap a catch-up policy,
- verification fixture/live-target evidence digests.

Změna semantic target, frame route, action strategy, mutation trigger, side-effect class, auth mode, expected account/tenant, required origin, bridge capability, postcondition, read-back nebo output contract vytváří novou revision. Operational tuning, které nemění semantics a je výslovně system-managed, může změnit effective runtime profile pouze s auditovanou compatibility evidence.

### 14.4 Deklarativní DSL

Runtime interpretuje pouze deklarované actions:

```text
NAVIGATE
RELOAD
BACK
FORWARD
OPEN_PAGE
ACTIVATE_PAGE
CLOSE_PAGE
CLICK
DOUBLE_CLICK
HOVER
FOCUS
BLUR
CLEAR
FILL
TYPE
INSERT_TEXT
FILL_SECRET
SELECT
CHECK
UNCHECK
PRESS
SCROLL
SCROLL_INTO_VIEW
DRAG_DROP
TOUCH
UPLOAD
DOWNLOAD
RESPOND_DIALOG
RESPOND_PERMISSION
WAIT_FOR
WAIT_FOR_NAVIGATION
WAIT_FOR_POPUP
WAIT_FOR_DOWNLOAD
ASSERT
EXTRACT
SCREENSHOT
VERIFY_ACCOUNT
CAPTURE_STATE
BRANCH
REPEAT_BOUNDED
```

Arbitrary JavaScript, shell command, raw CDP method, raw Playwright message, arbitrary filesystem path a generic network proxy nejsou součástí DSL. Potřebný provider-specific adapter je samostatný verzovaný step implementation s input/output schématem, exact bindingem, compatibility matrix, failure semantics a testy. OWNER direct interaktivní ovládání není omezené seznamem DSL; DSL je kontrakt unattended deterministic replay.

`REPEAT_BOUNDED` má max iterations, progress measure a stop condition. Infinite scroll používá bounded pages/items/time/data contract a deduplication key. Loop bez monotonic progress nebo s neomezeným visual search nelze aktivovat.

### 14.5 Step contract

Každý step obsahuje:

- stable step ID, human title a source requirement,
- action a exact action schema version,
- page selection, opener/popup relation a frame route policy,
- expected page generation, frame attachment a document transition policy,
- semantic locator, target-producing previous step nebo provider adapter,
- typed input, Secret binding, artifact handle nebo prior output binding,
- authority-lineage/OWNER-intent/operation-context reference a argument-origin/value-derivation mapu,
- explicitní input strategy a focus contract,
- precondition a pre-observation digest contract,
- earliest `mutationTrigger`, side-effect class a expected effect descriptor,
- independent postcondition/read-back a success evidence,
- pre-armed event waiters pro navigation/popup/dialog/filechooser/download,
- timeout, absolute deadline a idle/progress policy,
- retry, reconciliation a cancellation class,
- account/resource/external-session concurrency key,
- operation scope, Secret-use context a existující confirmation/challenge policy,
- screenshot/trace/network/DOM evidence a content-provenance policy,
- optional branch outcome a output projection,
- compatibility, locator a step digest.

Step se považuje za `SUCCEEDED` až po validní postcondition. Samotné dokončení Playwright method, URL změna, HTTP status, hidden spinner, popup event, download event, `load` nebo `networkidle` není univerzální důkaz business výsledku.

Step, jehož první možný side effect nastává při `input`, `change`, `blur`, auto-submit, pointer-down, permission grant, dialog response, upload selection nebo WebAuthn registration, musí mít side-effect intent commitnutý před tímto triggerem. `LOCAL_INPUT` neznamená automaticky read-only.

### 14.6 Semantic locator compiler a target binding

Priorita locatorů:

1. role + accessible name,
2. label,
3. placeholder nebo form name,
4. user-facing text,
5. test ID nebo explicitní stabilní atribut,
6. schválená relative semantic relation,
7. schválený CSS fallback včetně open shadow-root route.

Compiler ukládá canonical locator AST, exact/normalized text semantics, expected role/state, expected match count, page/opener selector, frame route, nearby semantic anchors, source page/frame/document identity, observation digest a element fingerprint. XPath, nth-index a long generated CSS chain nejsou výchozí locator. Index je povolen pouze tehdy, když business contract definuje ordered collection a step současně ověřuje identity položek.

Při runu se locator vyhodnotí znovu proti current documentu. Jednoznačnost, actionability, page/frame/document identity a operation scope se kontrolují bez ohledu na předchozí ElementHandle. Persistovaný handle, backend node ID nebo runtime object ID není součástí revision a nerecykluje se.

Locator fallback je ordered a explicitní. Fallback může dokončit stejný semantic cíl pouze tehdy, když zachová expected role, account/resource identity, mutation trigger a postcondition. Použití fallbacku vytváří drift evidence; nesmí tichým způsobem vybrat jiný record, tab, tenant, popup nebo frame.

Closed shadow root, inaccessible sandboxed/fenced frame, browser chrome a native surface vyžadují provider adapter, bridge nebo challenge. Visual coordinate není semantic locator pro unattended mutation, pokud revision neobsahuje verifikovatelný visual contract podle 14.11.

### 14.7 Actionability, input semantics a mis-target prevention

Před DOM action runtime vyžaduje:

- current session/context/host-or-bridge/control fence,
- exact active page/page generation a intended page relation,
- exact frame attachment a document epoch,
- právě jeden target,
- target attached a expected role/state,
- action-specific visibility, stability, enabled/editable/selectable/focusable checks,
- expected hit target a receives-events pro pointer action,
- operation scope, account/resource identity a current revision,
- precondition digest a případné confirmation/challenge,
- pre-armed event waiters,
- persistent side-effect intent před earliest mutation trigger.

Runtime může provést non-mutating trial actionability check, explicitní scroll a fresh observation. Sticky/fixed header, nested scrolling, virtualized list, CSS transform, animation, overlay, pointer capture, layout shift a transient rerender jsou samostatné failure/evidence states. Generic „zkus první prvek“ nebo neomezené retry není přípustné.

`force` je výjimka explicitně zapsaná v revision pro konkrétní step. Musí uvést důvod, exact target identity, actionability check, který obchází, mutation trigger, postcondition a negative test. `force` nikdy neobchází ambiguity, stale document/frame, wrong account, operation scope, disabled business condition nebo confirmation.

`FILL`, `TYPE`, `INSERT_TEXT` a `PRESS` mají rozdílnou event semantics. Revision určuje strategy a ověřuje výslednou hodnotu i očekávané events. Contenteditable, rich-text editor, masked input, date/time/number, locale parsing, autocomplete, IME/composition, keyboard shortcut a auto-submit používají explicitní field adapter nebo teaching-derived tested strategy.

`FOCUS` a `BLUR` mohou být mutující kvůli validation/autosave. `SELECT`, `CHECK`, `UNCHECK`, `UPLOAD`, `DROP`, permission response a dialog response se rovněž klasifikují podle skutečného provider behavior, nikoli podle názvu Playwright method.

Před dispatch runtime publikuje target highlight a action summary. Po možném triggeru již target replacement, overlay, navigation, popup nebo renderer crash nevede k blind retry; následuje postcondition/reconciliation.

### 14.8 Auth modes, account bindings a session-family concurrency

Auth mode používá:

```text
NONE
LOGIN_EACH_RUN
REUSABLE_SESSION_STATE
HYBRID
OWNER_DEVICE_REQUIRED
CLIENT_CERTIFICATE_REQUIRED
VIRTUAL_AUTHENTICATOR
```

`REUSABLE_SESSION_STATE` načte compatible Browser state bundle a ověří exact account/tenant authenticated condition. `HYBRID` načte state, při invaliditě provede deterministic login s Secret bindings, uloží nový bundle přes CAS a pokračuje. `OWNER_DEVICE_REQUIRED` vyžádá current OWNER Device Bridge nebo challenge. `CLIENT_CERTIFICATE_REQUIRED` vytvoří context/profile s exact origin-certificate bindingem. `VIRTUAL_AUTHENTICATOR` je pouze pro explicitní automation/test account.

Reusable state je svázaný s definition/revision/account bindingem, external session family, account auth epoch, browser runtime build compatibility, credential/certificate version, binding revision, origin inventory, authenticated condition, expiration a digestem. Rotace credentialu, explicitní logout, provider invalidation, account/tenant switch nebo newer auth epoch bundle invaliduje.

Každý run před první business mutací ověří expected account/tenant. Account marker se ukládá do checkpointu a znovu kontroluje po OAuth/SSO redirectu, popup loginu, reauthentication, state restore, bridge attach a provider account switch.

Account concurrency mode `EXCLUSIVE_MUTATION|SERIALIZED|READ_SHARED|PROVIDER_LIMITED` je součástí account bindingu a run preflightu. Server-managed a bridge runs sdílejí stejný persistentní account/resource claim. Concurrent logout, credential rotation, state capture nebo provider session invalidation se serializují a mění auth epoch.

### 14.9 Human challenge a auth continuation

Run může přejít do `CHALLENGE_REQUIRED` pro password reentry, email/SMS/TOTP, push, WebAuthn assertion/registration, platform passkey, client certificate selection, FedCM/native prompt, external device action, CAPTCHA/human verification, dialog nebo permission prompt.

Challenge resolution je vázané na exact run, step, session, page/frame/document, origin/RP/account, pending action digest, control epoch, auth epoch a expiry. Uložení response, consume challenge a enqueue resume je atomické. Duplicate stejné response digest vrací canonical outcome; změněná response je conflict.

Po resolution runtime provede fresh page/frame/document inventory, account authenticated condition a pending step precondition. Push nebo external device action, které doběhly po timeoutu/disconnectu, se nejprve reconciliují. WebAuthn registration je mutující external account operation a vyžaduje vlastní postcondition; assertion pouze pokračuje v login flow.

TOTP/email/SMS value je ephemeral input. Pokud zadání poslední číslice auto-submitne, side-effect intent je commitnutý před prvním inputem. Expired nebo providerem spotřebovaný kód nevytváří nový automatic attempt bez fresh challenge evidence.

### 14.10 Teaching

Teaching může řídit OWNER, AI nebo střídavě oba. Každá action vzniká nejprve jako typovaný browser input/action event s page/frame/document/control identity a mutation-trigger evidence. Teaching compiler:

1. seskupí low-level pointer/key/touch/composition events do semantic action,
2. rozliší focus, input, change, blur, submit, navigation, popup, dialog, file chooser a download causation,
3. re-resolvuje target v current DOM/documentu,
4. vytvoří locator candidates a ambiguity report,
5. rozpozná literal, typed parameter, prior output, artifact a Secret binding,
6. navrhne exact input strategy, precondition, mutation trigger a independent postcondition,
7. klasifikuje side effect, idempotency, retry, reconciliation a account concurrency,
8. detekuje page/frame/document/popup/OOPIF transition,
9. rozpozná auth/challenge a state-bundle boundary,
10. vytvoří ordered candidate manifest, uncovered events a coverage report.

OWNER v candidate editoru vidí přirozený popis každého kroku, target highlight, page/frame route, locator, vstup, earliest side effect, očekávaný stav, read-back a případné nejasnosti. Nemusí ručně psát CSS selektory.

Teaching nikdy nepřepíše již provedený OWNER side effect jako bezpečně opakovatelný step bez read-back evidence. Event, jehož trigger nebo postcondition nelze určit, zůstane blockerem `UNRESOLVED_MUTATION_SEMANTICS` nebo visual-only stepem.

Sensitive literal může být nahrazen Secret bindingem pouze po OWNER volbě nebo explicitním credential field eventu. Heslo napsané přímo do stránky se automaticky nepřidává do Password Manageru.

### 14.11 Visual-only krok

Pokud canvas, remote drawing surface, inaccessible frame nebo jiný UI neumožní semantic DOM target, compiler může vytvořit `VISUAL_ONLY_UNVERIFIED` candidate s image anchor, viewport/document contract a bounded region. Takový krok:

- může sloužit pro read-only extraction, OWNER-assisted teaching nebo direct OWNER operation,
- vyžaduje exact browser build, page/document, viewport, DPR/zoom, region a visual fingerprint,
- nemůže být aktivován jako unattended `MUTATION_NON_IDEMPOTENT` nebo `DESTRUCTIVE`,
- pro běžnou mutaci vyžaduje OWNER confirmation při každém runu nebo provider-specific verifiable adapter,
- vytváří drift signal při změně visual fingerprintu nebo transformu,
- po možném inputu používá stejnou reconciliation jako semantic step.

Toto pravidlo nebrání OWNERovi ovládat canvas nebo nativní surface v live session. Brání pouze tomu, aby jednorázový pixelový input byl vydáván za deterministickou unattended automatizaci bez ověřitelného cíle a výsledku.

### 14.12 Preflight, replay a verification

Preflight ověří bez business mutace:

- target origins, redirects, local targets a network policy,
- exact browser/Playwright/bridge runtime capabilities,
- account binding, auth epoch a authenticated condition,
- state-bundle compatibility a restore order,
- existence, jednoznačnost a frame/document route locatorů,
- action-specific actionability a input strategy,
- input/Secret/artifact/certificate availability,
- branch reachability a bounded loop progress,
- earliest mutation trigger a side-effect intent point,
- postcondition/read-back observability,
- popup/dialog/filechooser/download pre-arm contract,
- challenge feasibility,
- upload/download constraints,
- account/resource concurrency a schedule overlap,
- resource/capacity availability.

Preflight nesmí spustit mutaci jen proto, aby ověřil locator. Pokud target web neposkytuje bezpečný read-only test path, verification používá controlled fixture, provider test account, reversible setup/cleanup nebo explicitní OWNER-confirmed live acceptance.

Replay používá stejný interpreter jako production run, exact revision digest a zakázaný model network access. Replay nesmí dynamicky požádat model o nový locator, argument nebo branch. Verification obsahuje happy path, invalid input, account mismatch, auth expiry, locator ambiguity, autosave/blur, popup/frame/OOPIF, SPA/BFCache, service-worker noise, dialog, permission, challenge, upload/download, timeout, cancellation, worker/host/renderer crash, bridge disconnect, reconciliation a cleanup podle použitého contractu.

### 14.13 Run lifecycle, scheduling a checkpoints

Run používá:

```text
QUEUED
PREPARING
RUNNING
WAITING_FOR_PAGE
CHALLENGE_REQUIRED
PAUSED
RECOVERING
SUCCEEDED
FAILED
CANCEL_REQUESTED
CANCELLED
MANUAL_REVIEW
```

Run má `clientRunId` business idempotency, exact revision digest, runtime build manifest, operation scope, session/account relation, account auth epoch, lease/fence/incarnation, account/resource concurrency keys, current step/checkpoint/control epoch a cancellation version. Povolené hrany a terminalita jsou v 49.21.

Step je samostatná checkpointovaná logical operation. Current step pointer a terminal step result se mění atomicky. Checkpoint obsahuje exact page/frame/document identity, account marker, active dialogs/challenges/downloads, locator/postcondition digests, side-effect ledger a next-step reservation. Process memory nebo open Playwright handle checkpoint nenahrazuje.

OWNER takeover přejde run do `PAUSED` po safe checkpointu. Return-to-runtime vždy vytvoří nový control epoch, fresh observation a revaliduje current step; nesmí pouze pokračovat v přerušené low-level input sequence.

Scheduled run používá declared timezone a DST policy. Misfire/overlap používá právě jednu policy `SKIP|QUEUE|COALESCE|REJECT`. `COALESCE` může spojit pouze stejné immutable revision/input/account/resource scope; nesmí spojit odlišné business mutations. Každý schedule fire má vlastní idempotency key a audit.

### 14.14 Side-effect, retry a reconciliation

Side-effect classes jsou:

```text
READ_ONLY
LOCAL_INPUT
AUTHENTICATION
MUTATION_IDEMPOTENT
MUTATION_NON_IDEMPOTENT
DESTRUCTIVE
```

Před každým možným mutujícím triggerem se v jedné transakci uloží step pre-checkpoint, exact browser authority snapshot, concurrency claim, side-effect intent a dispatch outbox. Browser/bridge input může následovat až po tomto commitu.

Action attempt používá oddělené platformní milestones a host/bridge dispatch phases. Platformní milestones jsou `INTENT_RECORDED`, `POSTCONDITION_CHECKED`, `RECONCILIATION_STARTED` a `OUTCOME_RECORDED` a vznikají pouze guarded domain/side-effect transakcí. Kanonické monotonic host/bridge dispatch phases jsou:

```text
COMMAND_ACCEPTED
TARGET_RESOLVED
ACTIONABILITY_PASSED
INPUT_SEQUENCE_STARTED
MUTATION_TRIGGER_POSSIBLY_ISSUED
NAVIGATION_OR_REQUEST_OBSERVED
METHOD_RETURNED
POST_OBSERVATION_CAPTURED
```

Jakýkoli phase name mimo tento kanonický seznam je invalidní a nesmí být persistován ani použit na wire.

Timeout, disconnect, worker/host/renderer crash, popup close, navigation abort, dialog race nebo DOM replacement se vyhodnocuje podle nejvyšší durable/evidenced phase. Nový attempt je přípustný pouze při `CONFIRMED_NOT_APPLIED`, před prokázaným mutation triggerem nebo při target-side idempotency, která zaručuje stejný observable outcome.

Reconciliation používá exact independent read-back: target record/version/value, account profile, order/transaction ID, provider activity, state marker, downloaded artifact digest nebo jiný revision-defined contract. Výsledek je `CONFIRMED_APPLIED`, `CONFIRMED_NOT_APPLIED` nebo `UNKNOWN`.

`UNKNOWN` vede do `MANUAL_REVIEW`; blind retry je zakázaný a same account/resource concurrency key zůstává blokovaný. OWNER resolution používá current evidence digest a CAS. Potvrzení applied vytvoří canonical success/known effect outcome; potvrzení not-applied může povolit nový attempt stejné logical operation; nový odlišný business command dostane nový idempotency key.

Cancellation po possible mutation triggeru nejprve reconciliuje. Run je `CANCELLED` pouze když všechny pending effects mají známý outcome a cleanup dosáhl safe pointu. Cancellation nevrací již potvrzenou external změnu bez explicitní compensation stepu.

### 14.15 Navigation, network, popup a document policy

Každá navigation, redirect, popup, frame navigation, download, form target a external protocol request prochází HTTPS/scheme, hostname, origin, port, DNS resolution, resolved IP range, operation scope a allowed target validací. Runtime blokuje loopback, private, link-local, multicast, reserved a metadata cíle, včetně IPv4, IPv6, mapped adres a DNS rebindingu, pokud explicitní OWNER Device Bridge local-target binding nepovoluje konkrétní destination.

Local bridge target binding uvádí exact hostname/IP/CIDR, port, protocol, origin, purpose a expiry. Neznamená obecný přístup do OWNER sítě a bridge jej neinterpretuje jako proxy.

Step deklaruje expected navigation kind a resulting origin/document policy. Redirect chain se validuje po každém hopu. Credential, Authorization, cookie export nebo client-certificate binding se na nový origin nepřenese mimo exact browser/provider semantics a operation scope.

Popup/page, dialog, file chooser, download a navigation waiter se registruje před triggerem. Runtime rozlišuje popup blocked, opener closed, noopener, immediate popup navigation/close, same-page navigation, SPA route, BFCache restore a prerender activation. Event z jiného page/frame/document/action causation se nepřipojí k current step.

### 14.16 Artifacts a evidence

Run může vytvořit screenshot, crop, preview, trace, observation, DOM/semantic snapshot, upload, download, auth state, dialog, permission, challenge, client-certificate/WebAuthn, locator drift, navigation a structured reconciliation evidence. Artifact obsahuje type, run/session/step/action ID, owner, source page/frame/document, MIME, size, digest, safe name, sensitivity, created/expires, retention a cleanup state.

Každý step evidence spojuje:

- runtime build manifest a engine,
- session/context/host-or-bridge generation,
- page/frame/document/control identity,
- locator and target digest,
- precondition a actionability,
- mutation trigger a dispatch phases,
- postcondition/read-back,
- navigation/popup/dialog/download outcomes,
- side-effect/reconciliation outcome,
- screenshot crop podle policy,
- correlation/causation/logical operation.

Trace nebo screenshot může obsahovat secrets a dědí trusted OWNER visibility, encryption, sensitivity a retention policy. Artifact existence nenahrazuje successful postcondition.

### 14.17 Drift a repair

Schválený fallback může dokončit stejný semantic cíl a vytvoří drift signal. Manifest zůstává immutable. Opakovaný locator fallback, changed accessible name/role, target move do jiného frame/document, actionability změna, input-event změna, auth flow, account marker, popup route, client-certificate/WebAuthn requirement nebo postcondition drift nastaví definition na `DEGRADED`, vytvoří alarm a deduplikovaný repair job.

Repair používá fresh browser session a může použít AI pro návrh nové immutable revision. Poslední PASS revision zůstává aktivní, dokud candidate neprojde preflight, replay, verification, side-effect/reconciliation tests a activation. Repair nesmí během analysis tichým způsobem měnit active locator nebo step semantics.

Drift, který může změnit account/resource target, mutation trigger nebo side-effect outcome, okamžitě blokuje nové mutující runy. Čistě prezentační drift může podle revision policy pokračovat přes verified fallback a alarm.

### 14.18 No-AI rutinní runtime

Produkční deterministic run používá manifest, interpreter, Secret Manager, Browser Interaction Plane, Playwright/bridge typed adapters a typed outputs. Model provider se při rutinním runu nevolá. Model-call count je nula a je součástí acceptance evidence.

AI je dovolena pouze při explicitním `ADAPTIVE_BROWSER` agent runu, generation/teaching nebo repair. Takový běh je viditelně označený, má model/tool budget a nevydává se za deterministic automation. Adaptive AI používá stejné session/page/frame/document/control/side-effect kontrakty a po každé mutaci stejnou reconciliation; nemá raw browser bypass.

Runtime vždy preferuje active deterministic automation pro exact supported objective/account/input. AI browser planning se použije, když OWNER žádá ad-hoc práci nebo žádná compatible revision neexistuje; úspěšný ad-hoc tok lze následně předat do teaching compileru.

### 14.19 Monitoring

Browser monitoring obsahuje:

- active hosts, build manifests, contexts a session capacity,
- host/worker/bridge heartbeat a generation/connection epoch,
- preview viewers, dropped frames, reconnects a resync count,
- control handoffs, input-state resets a stale control rejects,
- page/frame/document transitions a stale-event rejects,
- observation, locator, actionability a action latency,
- mutation-trigger phases a pending reconciliation age,
- locator missing/ambiguous/fallback/drift count,
- auth restore/relogin/account-mismatch/challenge outcomes,
- state-bundle capture/restore/invalidation and auth epoch,
- popup/dialog/permission/upload/download outcomes,
- run/step terminal states a schedule overlap/misfire,
- side-effect applied/not-applied/unknown outcomes,
- browser/renderer crash, host drain/recovery a bridge disconnect,
- artifacts, profile locks, temporary resources a cleanup,
- per-definition, per-account, per-origin, per-engine a per-build SLO.

Readiness je false pro host/build/bridge capability required aktivní revision, pokud exact capability není dostupná. Unavailable optional engine nevyřadí celý systém; blokuje pouze affected run/revision a je viditelný v UI.

### 14.20 Activation gates

Automation revision lze aktivovat pouze když:

- manifest, schemas, digests a runtime compatibility jsou validní,
- všechny origins, redirect/local targets a account dependencies jsou deklarované,
- expected account/tenant a auth epoch policy jsou ověřitelné,
- state-bundle serializer/restore a credential rotation semantics mají PASS,
- locators jsou jednoznačné v exact page/frame/document routes,
- input strategy a actionability odpovídají target controls,
- každý mutující step má earliest mutation trigger, pre-dispatch intent point, postcondition a independent reconciliation contract,
- popup/dialog/filechooser/download waiter je pre-armed tam, kde může být relevantní,
- WebAuthn/passkey/client-certificate/device-bound kroky mají exact server/bridge/challenge policy,
- visual-only unattended mutation není neověřená,
- account concurrency, schedule overlap, timezone/DST a bounded resources jsou uzavřené,
- replay proběhl stejným interpreterem bez modelu,
- ambiguity, account mismatch, autosave/blur, popup/frame/OOPIF, SPA/BFCache, auth expiry, challenge, timeout, cancel, crash, disconnect, `UNKNOWN` a cleanup cases mají PASS,
- outputs odpovídají schema a artifacts retention/cleanup contractu,
- monitoring profile je aktivní,
- rollback point existuje.

Povinná gate není `SKIPPED`. Neaplikovatelná gate používá `NOT_APPLICABLE` s machine-readable důvodem odvozeným z immutable revision contractu. Všechny blocking browser gates jsou součástí stejného activation setu jako automation revision, exact bindings, account policy a required runtime build compatibility.

## 15. Runtime generovaných komponent

### 15.1 Kanonická runtime topologie

Každá interně generovaná komponenta používá právě tuto cestu:

```text
public HTTPS gateway nebo trusted dispatcher
  → serverový runtime_execution_context
  → kcml-runtime-gateway
  → kcml-runtime-host@<runtime-instance-id>
  → anonymní capability channel
  → generated handler
```

Každá komponenta má:

- stabilní component a runtime target identity,
- immutable source revision a release,
- artefaktový a runtime digest,
- exact binding-set revision a activation epoch,
- `runtime_instance_id` a monotonic `runtime_generation`,
- systemd template instance `kcml-runtime-host@<runtime-instance-id>.service`,
- serverem vytvořený execution context,
- systemd-owned Unix Domain Socket runtime gateway,
- instance-scoped runtime a temporary directory,
- health, readiness, heartbeat, logs, audit a cleanup state.

Generovaná komponenta nemá runtime bearer token, component token, service token, JWT, OAuth credential ani jiný vlastní autentizační údaj. Hodnoty `runtime_instance_id`, `runtime_generation`, systemd `InvocationID`, process start identity, activation epoch, binding-set revision a runtime digest jsou serverové identity a fencing evidence, nikoli přenositelné credentials.

Jediný `KCML_OWNER_API_KEY` přijímá pouze veřejná HTTPS gateway. Není předáván runtime hostu, generated handleru, interním UDS, brokerům ani browser hostu. Po jeho ověření veřejná gateway vytvoří immutable `OWNER_FULL` execution context a odstraní veřejný `Authorization` header před interním dispatchi.

### 15.2 Autorita execution contextu

Execution context může vytvořit pouze:

- veřejná gateway po ověření OWNER session nebo jediného `KCML_OWNER_API_KEY`,
- veřejná gateway po ověření exact endpoint-local external credentialu,
- trusted dispatcher nebo runtime gateway z commitnutého run, queue, task, control nebo side-effect záznamu.

Context vzniká z autoritativního PostgreSQL snapshotu a obsahuje exact source object, source revision, source release, runtime instance a generation, run/job/task/worker relation, execution attempt, target operation, binding ID/revision/digest, binding-set revision, activation epoch, application deployment epoch, platform incarnation, logical operation, deadline, cancellation version a correlation. Po commitu je immutable.

Caller ani generated handler nemůže dodat nebo změnit source, target, revision, release, runtime, execution ID, binding, binding digest, access channel, activation epoch, deployment epoch nebo platform incarnation. IPC a handler schemas tato pole nepřijímají. Jejich výskyt v caller payloadu nebo v reserved metadata je odmítnut před handlerem jako `RUNTIME_CONTEXT_SPOOF_ATTEMPT` a auditován.

Access branches jsou pouze:

- `OWNER_FULL` — po ověření OWNER session nebo jediného OWNER API klíče na veřejné gateway,
- `CONTRACT` — pro interní runtime call podle exact active contract bindingu,
- `EXTERNAL` — po ověření endpoint-local credentialu na exact veřejné route.

Žádná větev nevytváří principal, roli, scope, ACL nebo permission record.

### 15.3 Trusted runtime host a generated handler

`kcml-runtime-gateway` a `kcml-runtime-host` jsou trusted platform code. Generated handler je nedůvěryhodný business code z hlediska OS boundary a nikdy nevykonává platformní autoritu přímo.

Runtime gateway přijímá interní spojení pouze z canonical systemd-owned UDS a odvozuje peer identity z Linux UID/GID/PID, systemd unit instance, systemd `InvocationID`, cgroup path, host boot ID, process start identity, runtime instance a runtime generation. UID/GID nebo PID samostatně nejsou dostatečnou identitou.

Runtime host načte immutable launch manifest obsahující component, revision, release, runtime digest, binding-set revision, activation epoch, application deployment epoch, platform incarnation, runtime instance a generation. Manifest může být materializován systemd credential mechanismem, ale neobsahuje bearer secret a sám neposkytuje autoritu. Každá hodnota se při startu a při každém admission porovnává s PostgreSQL current snapshotem.

Runtime host spouští generated handler přes trusted Linux sandbox launcher. Launcher vytvoří anonymní `AF_UNIX` socketpair, pidfd-tracked process tree, namespaces, read-only root a cgroup limity ještě před načtením generated code. Handler nezná filesystem path runtime gateway ani brokerů a obdrží pouze jeden pre-opened capability file descriptor.

### 15.4 Handler sandbox

Generated handler běží v samostatném user, mount, network, IPC, UTS, PID a cgroup namespace. Sandbox má:

- nulové effective, permitted, inheritable, ambient a bounding Linux capabilities,
- `NoNewPrivileges`, zákaz setuid/setgid a zákaz získání nových privilege,
- read-only content-addressed release root ověřený proti runtime digestu,
- privátní `/proc` omezené na sandbox process tree,
- žádný host `/sys`, žádný host `/run` a minimální privátní `/dev`,
- privátní tmpfs pro `/tmp`, `/run` a pracovní prostor s exact byte a inode limity,
- žádný host filesystem, PostgreSQL socket, Secret Manager storage, runtime gateway path, browser-host endpoint ani deployment credential directory,
- prázdný network namespace bez použitelného loopbacku a seccomp zákaz vytvoření nebo připojení síťového socketu,
- přesný seccomp profil, native syscall architecture, zákaz namespace, mount, ptrace, BPF, keyring, device, module, raw-I/O a process-inspection mechanismů,
- bounded environment allowlist bez secretů, DB URL, gateway adres, inherited authorization headers nebo dynamic-loader injection,
- počáteční file descriptory pouze `stdin`, `stdout`, `stderr` a canonical capability FD; všechny ostatní jsou zavřené.

Symlink, hardlink, device node, FIFO, socket, path traversal, magic link a mount crossing v release nebo writable prostoru se validují podle kapitoly 50. Generated release neobsahuje writable hardlink na immutable obsah ani cestu vedoucí mimo vlastní root.

### 15.5 Kanonické capability API a IPC

Handler získává pouze:

```text
context.secret
context.callComponent
context.callExternal
context.state
context.logger
context.runtime
```

Capability API používá versioned `KCML-RUNTIME-IPC/1` přes anonymní file descriptor mezi handlerem a runtime hostem. Handler nemůže otevřít další platformní channel. Runtime host validuje framing, message sequence, operation, payload schema, velikost, deadline, cancellation version, maximum in-flight calls a stream window.

`context.secret(bindingAlias)` mapuje local immutable alias na exact active Secret binding. Handler neposílá secret ID, version, source revision ani binding metadata.

`context.callComponent(bindingAlias, args)` mapuje alias na exact source revision → target revision/operation binding. Handler neposílá target component ID, target revision, route, contract digest ani access channel.

`context.callExternal(targetAlias, request)` mapuje alias na exact external target binding. Handler neposílá absolutní URL, credential ani egress policy; relative path, method a body se validují proti target contractu.

`context.state` používá serverem přidělený component namespace a versioned CAS contract. Handler nemůže vybrat PostgreSQL, schema, jinou component namespace ani obejít state broker.

`context.logger` předává strukturované log a audit events s correlation. Závazný trusted secret režim se nemění: OWNER má plnou viditelnost a runtime nezavádí povinné maskování nebo redakční workflow.

`context.runtime` poskytuje read-only execution metadata, deadline, cancellation, readiness a bounded runtime operations včetně manifestem deklarovaného child-process requestu. Neumožňuje vytvořit execution context, změnit binding, otevřít host socket, spustit shell ani přidělit širší systémový přístup.

### 15.6 Handler contract a validace

Handler exportuje:

- seznam tools a jejich schémata,
- `invoke(name, args, context)`,
- volitelné `start(context)`,
- volitelné `stop(context)`,
- volitelné `states(context)`.

Runtime host před předáním do handleru ověří exact operation, input schema, payload size, deadline, cancellation, active revision/release, runtime digest, binding-set revision a activation epoch. Po návratu ověří output schema, result size, stream termination a absence nepovolených descriptorů. Handler completion v process memory není autoritativní výsledek; success vzniká až guarded persistencí pod current execution snapshotem a fencing pravidly kapitoly 49.

Malformed frame, reserved identity field, neznámá operation, sequence replay, descriptor injection, size overflow nebo output schema violation ukončí konkrétní call; závažné porušení IPC kontraktu současně uzavře channel, označí runtime unhealthy a spustí fenced cleanup/restart.

### 15.7 Persistentní stav

Persistentní stav přežívá restart, upgrade a rollback a používá immutable schema revisions. Handler pracuje pouze přes versioned `context.state` broker:

- read vrací value, digest, schema a `stateVersion`,
- create je insert-if-absent,
- put/delete vyžadují expected state version,
- bounded multi-key transaction je atomická nad namespace version,
- append používá client event ID dedupe a monotonic stream sequence.

Každý write ukládá serverový execution context, active revision/binding/activation epoch, runtime instance/generation, correlation, before/after digest, event a audit. Handler nemá DB credential, PostgreSQL network path ani přímý DB write. State migration se aktivuje s runtime revision a rollback preflight ověří forward compatibility; nekompatibilní state schema zablokuje activation. Podrobný kontrakt je v 49.22 a 50.22.

### 15.8 Resource, process a shutdown contract

Immutable runtime resource profile deklaruje CPU, memory high/max, swap, process/thread count, open file descriptors, request/response a stream limity, temporary storage bytes/inodes, file size, I/O, concurrency, deadline a graceful shutdown. Žádný production profile nesmí mít implicitně neomezenou hodnotu.

Přímý `fork`, arbitrary `exec`, shell a daemonizace generated handleru jsou zakázané. Potřebný child executable je exact release artifact se jménem, digestem, argument schema a resource profilem a spouští jej trusted runtime host prostřednictvím `context.runtime`. Child zůstává ve stejných namespaces a cgroup subtree, nezdědí capability channel ani jiné platformní FD a nemůže přežít lifecycle runtime instance.

Shutdown probíhá `stop admission → cancel/drain → checkpoint/reconcile → SIGTERM → bounded grace → SIGKILL celé cgroup`. Systemd používá `KillMode=control-group`; terminal cleanup vyžaduje prázdný cgroup, zavřené IPC a streamy a uzavřený pidfd-tracked process tree. `kill -9`, host crash nebo worker crash se obnoví z PostgreSQL intent/outcome/checkpoint evidence.

### 15.9 Activation, credentials a cleanup

Candidate runtime je svázán s exact release, runtime digest, binding-set revision a activation epoch. Staged candidate nepřijímá běžný provoz. Po activation switchi gateway okamžitě odmítne nový call ze staré epoch, release, runtime generation nebo binding setu. In-flight operations se řídí activation barrierem a side-effect reconciliation; starý proces nemůže po ztrátě autority zapsat state, result, heartbeat, checkpoint, binding nebo route.

Systemd secret-bearing credentials používají service-specific generation, atomic source replacement, restart exact affected service, nový `InvocationID` a effective fingerprint verification. Starý service process je fenced application deployment epoch, credential generation a connection closure. Generated handler credential directory nikdy nevidí.

Cleanup odstraní nebo uzavře runtime process tree, cgroup, namespaces, anonymous channels, accepted sockets, streams, temporary files, runtime directories, materialized service credentials a stale socket evidence. Systemd socket path vytváří a odstraňuje výhradně socket unit; runtime service jej nikdy sama neodpojuje ani nenahrazuje. Cleanup je persistentní, idempotentní a checkpointovaný a jeho `COMPLETE` je blocking podmínkou closure nebo konfliktního nového candidate.

### 15.10 Povinný důkaz

Každá generated component release před activation prokáže:

- exact execution-context derivation a spoof rejection,
- active revision/release/runtime/binding/epoch validation,
- Linux peer a systemd instance identity,
- socket ownership, lifecycle, stale/substitution a restart races,
- namespace, filesystem, capability, seccomp, environment a FD isolation,
- zákaz přímé sítě, PostgreSQL, Secret Manageru, browser hostu a jiných interních UDS,
- exact Secret, component, external a state broker flow,
- bounded payload, stream, backpressure, deadline a cancellation,
- child/orphan cleanup a cgroup emptiness,
- activation fencing staré runtime generation,
- audit/correlation completeness při success, reject, timeout, cancellation, disconnect, crash a recovery.

Podrobný normativní OS, IPC, lifecycle a testovací kontrakt je v kapitole 50.

## 16. Registrace a katalog

### 16.1 Cesty vzniku objektu

Objekt vstupuje do katalogu prostřednictvím:

- interního generátoru,
- OWNER registrace existujícího MCP serveru,
- OWNER registrace AI agenta,
- OWNER registrace externího API,
- OWNER registrace repository component balíku,
- platformního seed manifestu.

Všechny cesty produkují stejný kanonický component/runtime-identity/contract-binding model.

### 16.2 Manifest komponenty

Manifest obsahuje:

- schema version,
- registration revision,
- display name,
- business purpose,
- kind,
- provozní kontakty,
- criticality,
- artifact a provenance,
- runtime,
- capabilities,
- MCP tools/resources/prompts,
- endpoints,
- PULSE porty,
- states a transitions,
- control-plane,
- E2E scenarios,
- documentation evidence,
- secret policy,
- outbound policies,
- monitoring,
- audit policy,
- UI metadata,
- release compatibility.

### 16.3 Registration validation

Validator kontroluje:

- JSON Schema,
- naming a canonical hostname,
- uniqueness,
- digests,
- runtime artifact,
- protocols/transports,
- capabilities,
- schema compatibility,
- contract binding dependencies,
- Secret bindings,
- egress targets,
- control endpoints,
- state machine,
- E2E completeness,
- monitoring a audit,
- documentation.

### 16.4 Katalog UI

Stránka **Katalog komponent** nabízí:

- fulltext search,
- filtry kategorií, lifecycle, operational state, criticality a capability,
- tabulku i card view,
- detail identity, revision, release, runtime, contracts, relations, secrets, bindings, monitoring, logs a audit,
- create/register/generate/import/export,
- validate, verify, activate, enable, disable, suspend, quarantine, repair, recertify, rollback, archive a deregister,
- diff revisions a releases,
- usage graph,
- kontextové akce ve všech souvisejících pohledech.

### 16.5 Registered elements

Stránka **Registrované prvky** poskytuje provozně zaměřený přehled všech aktivních objektů, jejich runtime dostupnosti, posledních událostí, stavů, alarmů, exact bindings, revisions a rychlých akcí.

## 17. Dashboard a topologie

### 17.1 Canvas

Dashboard zobrazuje živou topologii komponent, tools, PULSE portů, exact bindings, secrets, externích targetů a runtime událostí.

Každý node obsahuje:

- component code a název,
- kategorii a roli,
- lifecycle, activation, operational a monitoring stav,
- runtime availability,
- criticality,
- aktivní revision/release,
- runtime instance a binding-set revision,
- secret chips,
- metriky,
- alarm badge,
- vstupní a výstupní porty.

### 17.2 Connections

Connection reprezentuje PULSE nebo tool/endpoint dependency. Obsahuje source/target revisions, port keys, route, operation scope, contract digests, compatibility status, binding state a evidence.

Compatibility status:

```text
EXACT_MATCH
COMPATIBLE_WITH_DIFFERENCES
INCOMPATIBLE
UNKNOWN
STALE
```

### 17.3 Canvas akce

OWNER může:

- přesouvat nodes a ukládat layout,
- zoom/pan/fit,
- hledat a filtrovat,
- propojit porty drag-and-drop nebo klávesnicí,
- zobrazit compatibility preview,
- vytvořit/odpojit exact contract binding,
- odpojit connection,
- otevřít detail node/port/edge,
- bind/unbind secret přetažením,
- bulk bind s impact preview,
- pozastavit komponentu nebo runtime,
- aktivovat, enable/disable, repair a recertify komponentu,
- spustit E2E/state query/heartbeat challenge,
- zobrazit živé runtime events,
- provést deregistration s impact preview.

### 17.4 Live events

Dashboard používá SSE a zobrazuje calls, PULSE, state, heartbeat, controls, tool calls, agent runs, browser runs, alerts a audit. Event lze otevřít do plného debug detailu a sledovat correlation graph.

### 17.5 Workspace

OWNER layout ukládá node positions, viewport x/y/zoom, filters, collapsed groups a lock version. Souběžná změna používá optimistic locking.

## 18. Monitoring, dohled a automatická oprava

### 18.1 Monitoring profil

Každá komponenta má monitoring profil obsahující:

- enabled,
- version,
- SLO targets,
- probe intervals,
- alert rules,
- runbook reference,
- primary a backup channel,
- stale threshold,
- disable threshold,
- retention,
- repair policy,
- recertification interval.

### 18.2 Probes

Podporované probes:

- HTTPS health,
- readiness,
- MCP `server/discover`,
- MCP per-request metadata a header consistency,
- tools/list,
- deterministic tools/call,
- subscriptions/listen acknowledgment a notification,
- MRTR input_required/retry,
- Tasks extension get/update/cancel podle capability,
- state query,
- heartbeat freshness,
- PULSE delivery,
- endpoint contract,
- external dependency,
- browser preflight,
- agent eval sample,
- database queue,
- systemd service,
- TLS certificate,
- DNS,
- resource utilization,
- audit integrity.

Probe ukládá status, latency, evidence, correlation ID a checkedAt.

### 18.3 Heartbeat scheduler

Worker scheduler ukládá worker ID, last started/completed, current lease, last error, next run a heartbeat. UI zobrazuje scheduler state a čerstvost.

### 18.4 Alert

Alert obsahuje:

- severity `WARNING`, `HIGH`, `CRITICAL`,
- type,
- status `OPEN`, `ACKNOWLEDGED`, `SUPPRESSED`, `CLOSED`,
- title,
- detail,
- source object,
- correlation ID,
- first/last seen,
- occurrence count,
- suppression interval,
- recommended action,
- repair link.

### 18.5 Doručení alertu

Primární a záložní webhook channel používají HMAC, idempotency key, attempts, retry schedule, HTTP status, response digest a dead-letter state. Nastavení kanálů i jejich test je v UI.

### 18.6 Watchdog

Watchdog vyhodnocuje heartbeat, probe history, binding-set revision, lifecycle a SLO. Přechod operational state vytváří audit, alarm a případný control/repair action.

### 18.7 Automatická oprava

Pro interně generovanou komponentu může monitoring vytvořit deduplikovaný repair job s:

- component identity,
- active release,
- approved functional specification lineage,
- monitoring evidence,
- error classification,
- correlation ID,
- cooldown a dedupe key.

Repair zachovává component a runtime identity, používá `INHERITED_TECHNICAL` authority, vytváří candidate, validuje a aktivuje. Poslední funkční release zůstává rollback bodem.

### 18.8 Recertification

Komponenta má review due date, interval a phase `VALID`, `WARNING`, `GRACE`, `SUSPENDED`, `INVALID`. Recertification spouští contract validation, runtime E2E, binding review, Secret review, monitoring check a dokumentační kontrolu.

### 18.9 Monitoring UI

Stránka **Monitoring** obsahuje:

- metric cards,
- timeline probes a state transitions,
- component filter,
- severity segments,
- alerts a deliveries,
- scheduler state,
- SLO a latency,
- heartbeat freshness,
- resource charts,
- runbook a repair actions,
- alert ack/suppress/close,
- konfiguraci profilů,
- debug evidence.

## 19. Audit a debug logging

### 19.1 Auditní řetězec

Každá významná doménová mutace vytváří audit event ve stejné PostgreSQL transakci. Singleton `audit_head` se zamkne, alokuje následující gap-free `chainSequence`, vypočte hash z previous hash, canonical eventu a sequence a atomicky se aktualizuje. Pokud audit insert nebo head update selže, doménová mutace se necommitne.

Audit obsahuje actor `OWNER|SYSTEM`, access channel, server execution context, authority-lineage ID/digest, OWNER-intent ID/digest, operation-context ID/digest, semantic-action-plan digest, target/argument-origin summary, Secret-use context references, object, operation, logical operation/command ID, before/after, state versions, revision/binding/activation/fence metadata, correlation/causation/trace, timestamp a integrity hash. OWNER_FULL zůstává jedinou lidskou autoritou; audit nezavádí permissions.

Archive delivery používá transactional outbox a at-least-once dedupe. Debug log může být asynchronní, ale nenahrazuje povinný audit. Cross-object pořadí vyjadřuje chain sequence a causation; aggregate event stream zachovává vlastní sequence podle 49.5.

### 19.2 Component audit stream

Každá komponenta má samostatný ordered stream s expected next sequence, highest received/acknowledged, gap state, replay range, current hash a integrity state. Gap spouští replay request.

### 19.3 Debug log

Každý proces zapisuje strukturované logy s úrovněmi `fatal`, `error`, `warn`, `info`, `debug`, `trace`. Produkční systém uchovává debug úroveň podle retention nastavené OWNERem.

Log obsahuje:

- timestamp,
- level,
- service/process/worker,
- message,
- correlation/trace/span,
- component/agent/job/run,
- operation a phase,
- authority lineage, OWNER intent, operation context, provenance a semantic-validation result,
- request/response payload,
- secret hodnoty použité v důvěryhodné zóně,
- duration,
- result/error/stack,
- resource data.

Debug logging používá přímý trusted-perimeter režim. Log může obsahovat plná hesla, API klíče, tokeny, cookies, authorization hlavičky, OpenAI requesty a odpovědi, chat zprávy, tool argumenty a výsledky, browserové formuláře, deployment hodnoty a další secrets v jejich skutečné podobě. Aplikační logger, AI orchestrace, CI a runtime zachovávají tyto hodnoty bez povinného maskování nebo redakční transformace.

Presentation mode je čistě vizuální preference OWNERa a nemění ukládaná data, logy, audity, API odpovědi ani runtime chování.

### 19.4 OWNER log viewer

Stránka **Audit a logy** poskytuje:

- live tail,
- fulltext,
- filtry podle času, levelu, procesu, objektu, operation, correlation a výsledku,
- sloučenou časovou osu logů, auditu a událostí,
- raw JSON a formátovaný detail,
- before/after diff,
- stack trace,
- zobrazení plných secrets a payloadů,
- correlation graph,
- export JSONL/CSV,
- saved queries,
- retention a archive stav,
- audit integrity kontrolu.

### 19.5 Audit archive

Auditní outbox ukládá event payload, state, attempts, lease, error a archivedAt. Archiv je append-only JSONL nebo ekvivalentní immutable storage. UI zobrazuje backlog, chyby a integritu.

## 20. Konfigurace, automatické řízení parametrů a OWNER vstupy

### 20.1 Konfigurační registr

Každý parametr obsahuje:

- key,
- label a description,
- type a category,
- default a current value,
- source a version,
- fingerprint,
- applies-to services,
- validation schema,
- restart required,
- secret flag,
- `managementMode = SYSTEM_MANAGED | OWNER_INPUT | OWNER_PREFERENCE`,
- `ownerEditable`,
- updatedAt, actor a audit.

Kategorie pokrývají network, authentication, runtime, integrations, observability, presentation, AI, generation, browser, retention a deployment.

### 20.2 Jednotné UI a chatové zadávání

Každý parametr nebo údaj, který systém potřebuje od OWNERa, má formulářovou, kontextovou nebo chatovou cestu v UI. Stejnou operaci poskytuje produkční API. Development, testování, deployment a produkční provoz používají tento společný kontrakt.

UI používá kompaktní kontextové ovládání. Centrální chat umí hodnotu vysvětlit, vyžádat, validovat a uložit v okamžiku, kdy ji konkrétní workflow potřebuje. OWNER pracuje s business významem hodnoty a systém automaticky zajišťuje technické propsání, restart, rollout, test a audit.

### 20.3 OWNER-editable allowlist

OWNER trvale zadává nebo mění pouze:

1. OpenAI model používaný globálně nebo konkrétním AI agentem, centrálním chatem či generation workflow;
2. credential, účet, identifikátor, URL, obsah, soubor nebo business pravidlo, které konkrétní integrace nebo generation job skutečně potřebuje;
3. uživatelskou prezentační preferenci, například layout, jazyk, časovou zónu zobrazení nebo presentation mode.

Každý další OWNER-editable parametr vzniká jako explicitní položka tohoto SSOT. Parametrický registr proto zůstává přesný, přehledný a uzavřený vůči nahodilému přidávání technických voleb.

### 20.4 Automaticky spravované parametry

Systém automaticky určuje, validuje a udržuje:

- canonical domain, component zone, API base a routování,
- porty, proxy, TLS, DNS a certificate lifecycle,
- worker intervals, capacities, queue limits, leases a heartbeats,
- browser timeouts, contexts, resource limity a artifact retention,
- monitoring profily, stale thresholds, SLO, alerting a repair policy,
- log level, debug retention, audit archive a export limity,
- upload/download limity,
- session, login throttle a MFA provozní hodnoty,
- Secret rotation a bindings odvozené z kontraktů,
- acceptance schedules, maintenance, backup a rollback hodnoty,
- modelové reasoning, output, retry, budget a tool limity odvozené z účelu běhu.

Účinné hodnoty, původ výpočtu, historie, validační stav a dopad jsou viditelné v kontextových detailech a centrálním chatu. Systém je průběžně optimalizuje na základě runtime evidence.

### 20.5 Aplikace změny

Každý setting má desired version a per-service effective version. OWNER nebo system-managed policy mění desired value přes expected setting state version; změna, audit a config-apply outbox se commitují atomicky.

Apply run používá persistentní lifecycle `PENDING`, `VALIDATING`, `APPLYING`, `VERIFYING`, `APPLIED`, `ROLLING_BACK`, `ROLLED_BACK`, `FAILED`, `MANUAL_REVIEW`. Restart/reload probíhá mimo DB transakci s intent/outcome. Recovery čte skutečný service config digest, release a readiness. `APPLIED` vyžaduje všechny target services na expected effective version; mixed stav zůstává `VERIFYING` nebo rollbackuje.

Atomic config group používá barrier a previous snapshot. Concurrent OWNER změna se stale expected version je conflict. Detail je v 49.23.

### 20.6 Bootstrap

Instalátor automaticky připraví databázi, host-level keys, systémové účty, porty, filesystem, systemd credentials, DNS/TLS integraci, workery, monitoring a výchozí provozní profil.

Pevná OWNER identita používá username `KRMAR78`. Její heslo přichází při každém deploy runu z GitHub Actions secretu `PASS`. Toto je jediná hodnota, kterou systém přijímá mimo UI a centrální chat.

První inicializace vytvoří právě jeden `KCML_OWNER_API_KEY`, pokud dosud neexistuje, a uloží jej do Secret Manageru i singleton auth záznamu podle kapitoly 7. Opakovaný bootstrap nevytváří další klíč.

`OPENAI_API_KEY` se zadává v Password Manageru nebo centrálním chatu. Aplikace se plně spustí a zpřístupní všechny ne-AI funkce i před jeho vložením.

## 21. Pevná OWNER identita, přihlášení a relace

### 21.1 Pevná identita

KájovoCML NG obsahuje právě jeden singleton authentication record. Obsahuje stabilní UUID, neměnný username `KRMAR78`, password hash, MFA state, created/updated/passwordChanged, session epoch, deployment-managed flag a audit attribution. Neobsahuje role, groups, memberships, `active`, user status ani permission fields.

Databázový constraint dovoluje právě jeden řádek se singleton key `OWNER` a username `KRMAR78`; bootstrap zajišťuje jeho existenci. UI, API, chat, import ani generátor nemají operaci pro vytvoření, seznam, kopii, přejmenování, deaktivaci nebo odstranění lidské identity. Jakýkoli pokus vytvořit druhou identitu je schema/DB chybou a nemá částečný side effect.

### 21.2 Přihlášení

Přihlášení vyžaduje ruční zadání username i hesla. Login formulář pevné username nezobrazuje, nenapovídá ani nepředvyplňuje. Server porovná zadané username s persistovanou singleton identitou `KRMAR78` a nepřipouští volbu nebo vytvoření jiné identity. Heslo je hashované Argon2. Login throttle používá failure count, timestamps a lockedUntil. Úspěšné přihlášení vytváří secure `__Host-` session cookie a CSRF token.

Jediný OWNER API klíč poskytuje rovnocenný produkční přístup bez interaktivního loginu. Klíč lze standardním API voláním vyměnit za webovou OWNER session pro automatizované UI a Playwright E2E testy.

### 21.3 MFA

OWNER může z UI aktivovat TOTP pomocí QR nebo manual seed, ověřit kód, zobrazit recovery codes a zaregistrovat nový autentifikátor. Důvěryhodné zařízení má konfigurovatelnou dobu. Recovery kód je jednorázový.

### 21.4 Relace

Relace obsahuje createdAt, expiresAt, last activity, reauthenticatedAt, device metadata a current flag. OWNER může revokovat jednotlivou, ostatní nebo všechny relace.

### 21.5 Zabezpečení přístupu v UI

Stránka **Bezpečnost** zobrazuje a obsluhuje pouze zabezpečení pevné identity:

- read-only username `KRMAR78`, singleton stav a password source,
- poslední úspěšnou synchronizaci hesla z `PASS`,
- MFA reset/registraci,
- recovery codes,
- relace,
- login history, audit a logs.

Neobsahuje user list, create/edit/delete, role, groups, invites, sharing ani permission controls. Heslo je deployment-managed a v aplikaci se nemění; OWNER jej mění v autoritativním GitHub Actions secretu `PASS` a následující deploy provede synchronizaci.

### 21.6 Synchronizace hesla `KRMAR78`

GitHub Actions secret `PASS` je autoritativním zdrojem hesla pevné identity `KRMAR78`. Každý deploy run bez ohledu na rozsah změny provede idempotentní synchronizaci této hodnoty do singleton authentication recordu prostřednictvím kanonické authentication service, vytvoří aktuální Argon2 hash a ověří přihlášení.

Synchronizace je povinným deployment krokem před finálním health a readiness potvrzením. Změna hodnoty `PASS` se při nejbližším deploy runu okamžitě projeví v produkčním přihlášení. Operace používá stejný debug a audit kontrakt jako ostatní authentication operace.

## 22. Webové uživatelské rozhraní

### 22.1 Informační architektura

Hlavní navigace obsahuje:

1. Centrální chat
2. Dashboard
3. Generování
4. AI agenti
5. MCP servery a nástroje
6. Browser relace a automatizace
7. Registrované prvky
8. Katalog komponent
9. Externí systémy
10. Monitoring
11. API klíč a runtime vazby
12. Secrets a hesla
13. Audit a logy
14. Konfigurace
15. Testy a API
16. Bezpečnost
17. Releases a provoz

Centrální chat je současně dostupný jako globální drawer na každé obrazovce. Zachovává aktivní konverzaci, rozpoznává aktuální objekt a nabízí otázky i příkazy odpovídající jeho akčnímu registru.

Aktivní browser session lze připnout k pravému okraji libovolné obrazovky. Připnutí nezmění parent ani lifecycle session. OWNER tak může současně sledovat web, chatovat, označit cíl, převzít ovládání a otevřít technické evidence bez ztráty stavu.

### 22.2 Design systém

Primární font je `Montserrat`, fallback `Inter`, `SF Pro Text`, `Segoe UI`, sans-serif.

Barevné tokeny:

```text
ink:          #18212d
muted:        #647181
line:         #dfe4ea
line-strong:  #cfd6de
surface:      #ffffff
canvas:       #f4f6f8
sidebar:      #111923
sidebar-2:    #16212d
teal:         #007f7a
teal-dark:    #006966
teal-soft:    #e8f6f5
green:        #1e9b55
amber:        #e49a12
red:          #de3e35
```

UI používá kompaktní enterprise vzhled, 6px radius, jemné border, přiměřené stíny, vysokou hustotu dat a jasnou hierarchii. Stavová signalizace je stabilní a statická. Animace trvají 120–180 ms a podporují orientaci.

### 22.3 Layout

Desktop používá 228px sticky tmavý sidebar a světlý workspace. Sidebar obsahuje logo, navigaci, build/environment stav, pevnou OWNER identitu a logout.

Workspace může přejít do tříčástového layoutu `objekt/chat/browser`, ale browser panel se otevírá pouze při aktivní relaci. Divider je resizeable, panel lze pop-out do samostatného OWNER okna a layout se ukládá globálně pro OWNERa per surface.

Tablet používá kompaktní sidebar nebo drawer a taby `Obsah`, `Chat`, `Web` podle šířky.

Mobil používá topbar, drawer a tabované pracovní plochy. Preview má samostatnou full-viewport záložku s persistentním control bar; chat composer a browser keyboard se navzájem nepřekrývají.

### 22.4 Komponenty UI

- primary, secondary, danger, icon a compact button,
- input, password, secret, select, multiselect, textarea, code editor,
- search a filter bar,
- metric cards,
- data table s sticky header a column chooser,
- cards a accordions,
- status badges,
- tabs a segmented controls,
- timeline,
- graph/canvas,
- modal, drawer, popover a command palette,
- toast a persistent notice,
- diff, JSON viewer, schema viewer a log viewer,
- live console,
- Browser Preview Surface,
- control holder banner,
- page/tab switcher,
- target picker a target reference chip,
- challenge card,
- credential/account save card,
- action target highlight a postcondition result.

### 22.5 Povinné UI stavy

Každá obrazovka má:

- loading skeleton,
- empty state s hlavní akcí,
- error s retry a debug detailem,
- partial data state,
- stale/reconnecting state,
- authentication/session/binding state,
- success feedback,
- disabled action s vysvětlením,
- optimistic update a server reconciliation.

Browser surface navíc rozlišuje `CREATING`, `READY`, `AI ovládá`, `Přebírání`, `OWNER ovládá`, `AUTOMATION ovládá`, `Challenge`, `Paused`, `Recovering`, `Closed` a `Failed`. Stav je vždy odvozený ze server snapshotu, nikoli z lokálního tlačítka.

### 22.6 Přístupnost

- plná klávesnicová navigace,
- viditelný focus,
- semantic HTML a ARIA,
- focus trap v modal/drawer,
- Escape zavírá overlay,
- focus se vrací spouštěči,
- labels a error descriptions,
- kontrast WCAG AA,
- touch target alespoň 40×40 px na mobilu,
- screen-reader stavové oznámení,
- reduced-motion preference.

Browser preview poskytuje keyboard toggle mezi UI chrome a remote page input. Aktivace remote keyboard mode je zřetelná, Escape jej opustí a screen reader dostane URL, title, control holder, active element descriptor a action výsledky. Target picker je ovladatelný klávesnicí přes seznam semantic candidates.

### 22.7 Responsivní viewporty

Povinné QA rozměry:

- 390×844
- 768×1024
- 1366×768
- 1920×1080

Na každém rozměru jsou texty, tlačítka, tabulky, dialogy, preview a grafy čitelné a ovladatelné. Hlavní stránka má stabilní viewport bez nechtěného horizontálního scrollu; široká data používají vlastní bounded scroll container.

### 22.8 Globální chatový shell

Chatový shell obsahuje:

- výběr nebo založení konverzace,
- persistentní historii,
- textový a hlasový vstup,
- soubory, URL a credentials,
- streamovanou AI odpověď,
- zobrazení provedených actions a jejich výsledků,
- odkazy na dotčené objekty, logy, audit, testy a evidence,
- možnost zopakovat, zrušit nebo navázat příkaz,
- model selector OpenAI,
- stav `OPENAI_CONFIGURATION_REQUIRED` s přímým otevřením Password Manageru,
- browser session selector a akci **Otevřít web**,
- current URL/title/control holder,
- Browser target reference chips,
- **Převzít ovládání**, **Vrátit AI**, **Vybrat prvek**, **Přihlásit se**, **Uložit účet** a **Zavřít relaci**,
- pending challenge a exact confirmation cards.

Zpráva typu „podívej se na tyto stránky“ vytvoří nebo připojí browser session a URL source. „Já se přihlásím“ vyvolá OWNER takeover nebo challenge. „Ulož si tento účet“ otevře explicitní account binding summary a uloží credential/state pouze v potvrzeném rozsahu. „Dělej to pravidelně“ předá verified teaching flow do automation revision se schedule/event návrhem.

Stejný chatový engine používá samostatná stránka, globální drawer a externí API. UI zachovává přirozený jazyk jako primární rozhraní a technické detaily zobrazuje v rozbalitelném panelu.

## 23. Generování UI

### 23.1 Desktop

Generování používá resizeable workspace:

- levý panel: persistentní diskuse, historie, tool activity a composer,
- pravý pracovní panel: **Web**, **Specifikace**, **Evidence** a **Průběh**.

Záložka **Web** používá stejnou Browser Preview Surface jako centrální chat. Zobrazuje URL, page tree, control holder, operation scope, current action/target, challenge, target picker, account save a artifacts. Přechod na jinou záložku browser session nezavírá.

### 23.2 Mobil

Mobilní záložky:

```text
Diskuse
Web
Zadání
Průběh
```

Composer zůstává sticky, respektuje klávesnici a safe area. Browser remote-input mode používá samostatný full-screen surface a jasný návrat do diskuse.

### 23.3 Diskuse

Message obsahuje author, timestamp, state, text, attachments, Browser target references a tool activity. Streamovaný text se přidává lokálním reducerem podle message ID. Reconnect používá server snapshot a event cursor.

Composer podporuje:

- text,
- soubory,
- URL,
- credential input,
- Browser target reference,
- multiline,
- send,
- steer během běžícího turnu,
- cancel,
- voice input pokud browser podporuje.

OWNER může napsat přirozenou instrukci během AI práce. Steer přeruší model/browser read-only loop, bezpečně reconciliuje případný side effect, uloží current browser checkpoint a nový turn načte fresh observation.

### 23.4 Specifikace

Spec panel zobrazuje revision, digest, rendered sections, capability decisions, open questions, changes a approval CTA. Schválená revision je read-only.

Browser requirement obsahuje purpose, origins, auth/account policy, OWNER handoff, required challenges, teaching/automation output, side-effect ceiling, postconditions a acceptance cases. Schválení zobrazí jejich souhrn bez technických tunables.

### 23.5 Průběh

Progress zobrazuje lifecycle, jednotlivé fáze, workery, trvání, model calls, browser sessions, control handoffs, tests, releases, blockers, logs, evidence a cancellation.

## 24. Další obrazovky

### 24.1 Releases a provoz

Zobrazuje application releases, component releases, current pointers, build IDs, source commits, digests, deployment kroky, timing, health, rollback, backups, migrations, services a acceptance runs.

### 24.2 Bezpečnost

Zobrazuje read-only pevnou identitu `KRMAR78`, password source a poslední sync, MFA, recovery, relace, security events, rate limits, certificates, master key status, CSP/headers, dependency audit a host preflight. Neobsahuje správu uživatelů ani rolí.

### 24.3 API klíč a runtime vazby

Zobrazuje singleton `KCML_OWNER_API_KEY` s reveal/copy/rotate/usage, component/runtime identities, exact contract bindings, Secret bindings, external auth bindings a bridge certifikáty. Neobsahuje principal, permission ani token registry.

### 24.4 Command palette

OWNER otevře globální command palette pro navigaci, hledání objektů a vykonání objektových akcí. Výsledky respektují aktuální stav a používají stejný action registry.

### 24.5 Testy a API

Stránka **Testy a API** zobrazuje jediný OWNER API klíč, operation catalog, OpenAPI/KCIP kontrakty, test catalog, test suites, běžící test runs, live progress, výsledky, evidence, logy, cleanup a export. OWNER může spustit celý repozitář, jednu doménu, jeden objekt, jeden endpoint nebo jeden registrovaný prvek.

API explorer používá `KCML_OWNER_API_KEY` a vykonává skutečné requesty proti `https://kaja.hcasc.cz/api/v1`. Chatový panel umožňuje totéž přirozeným jazykem.

## 25. Databázový model

### 25.1 Databázová platforma

KájovoCML NG používá PostgreSQL 16 s extensions `pgcrypto` a `citext`. Časové hodnoty používají `timestamptz`, identity UUID, sekvence `bigint`, strukturovaná data `jsonb` a kryptografické digesty `bytea` nebo kanonický `sha256:<hex>`.

Databázové schéma vzniká jako jeden konzistentní greenfield baseline. Následující změny používají sekvenční forward-only, rollback-compatible migrations, transakce, explicitní constraints, indexes a upgrade testy. Přesný PostgreSQL 16 execution profile, isolation matrix, session timeouts, DDL postup a migration ledger určuje kapitola 51.

### 25.2 Obecné datové zásady

- každý mutable aggregate root má `created_at`, `updated_at`, `state_version`, current `platform_incarnation_id` a `application_deployment_epoch`; `lock_version` se používá pouze na samostatném mutable leaf row a nikdy jako druhá autorita téhož rootu,
- každá identity má UUID/stabilní code a nikdy se nerecykluje,
- revision, checkpoint, event, attempt evidence a terminal result jsou immutable,
- terminal run/job/task/action/phase/activation outcome nelze znovu otevřít,
- idempotency keys mají unique canonical scope, request digest a persistentní lifecycle,
- leases obsahují incarnation, owner, monotonic fencing token, acquired/heartbeat/expiry a stav,
- expired lease nelze oživit; každý authoritative worker write guarduje incarnation+fence+state version,
- audit eventy jsou append-only a povinný audit se commitne s doménovou mutací,
- doménový event a transactional outbox vznikají ve stejné transakci jako state change,
- soft-delete používá tombstone/`deleted_at` a identity zůstává rezervovaná,
- active pointer je foreign key na validní parent-owned revision/release a nese activation epoch,
- same-parent vztahy používají composite foreign keys,
- canonical JSON payloady mají schema validation a SHA-256 digest,
- všechny business foreign keys mají explicitní delete policy,
- revision/event/checkpoint sequence se alokuje parent row lockem, nikoli nechráněným `max()+1`,
- noncommutative mutation používá expected state version; blind last-write-wins je zakázaný,
- žádná DB transakce nedrží lock přes network/provider/browser/filesystem/systemd call,
- external effect má předem commitnutý intent a následný outcome/reconciliation,
- formální crash recovery se řídí kapitolou 49 a fyzické PostgreSQL isolation, lock order, advisory namespaces, constraints, transaction boundaries a migrations kapitolou 51.

### 25.3 Fixed OWNER authentication entities

#### `owner_identity`

- `id`
- `singleton_key = OWNER`, primary/unique a check constraint
- `username = KRMAR78`, citext unique a check constraint
- `password_hash`
- `password_changed_at`
- `mfa_enabled`
- `mfa_secret_ciphertext`
- `deployment_managed`
- `session_epoch`
- `created_at`, `updated_at`
- `password_source = GITHUB_ACTIONS_PASS`

Tabulka neobsahuje role, status, ownership, permission ani soft-delete. Constraint dovoluje nejvýše jeden řádek; bootstrap a readiness gate vyžadují právě jeden.

#### `owner_session`

- `id`, `owner_identity_id`
- `lookup_digest`, `session_hash`
- `created_at`, `last_seen_at`, `expires_at`, `revoked_at`
- `reauthenticated_at`
- `session_epoch`
- device/IP/user-agent metadata

#### `owner_login_throttle`

- attempt key digest
- failure count
- first/last failure
- locked until

#### `owner_recovery_code`

- owner identity ID
- code hash
- created/consumed timestamps

#### `owner_mfa_enrollment`

- owner identity ID
- enrollment token digest
- encrypted seed
- expiresAt
- verifiedAt

#### `owner_api_credential`

- `singleton_key = OWNER_API`, primary/unique a check constraint
- `secret_id` a `secret_version_id` pro stable name `KCML_OWNER_API_KEY`
- `verifier_hash`, `fingerprint`, `credential_version`
- `state_version` a credential activation epoch
- poslední rotate logical operation a outcome digest
- `created_at`, `rotated_at`, `last_used_at`
- `last_usage_metadata`, `audit_correlation_id`

Existuje právě jeden řádek a jedna active Secret version. Tabulka neobsahuje account relation, principal, role, scope, label, expiry ani collection lifecycle. Rotace pod singleton row lockem a expected state version atomicky nahradí active Secret version, verifier, credential version a epoch; předchozí klíč přestane platit v témže commitu. Retry stejné rotate logical operation vrací stejný canonical outcome.

### 25.4 Component entities

#### `component`

- identity, KCML number, code, hostname
- display name, description, category, role
- contacts, criticality
- runtime identity kind
- active revision ID a current release ID
- lifecycle, activation, operational, monitoring, recertification
- enabled, ingress, pulse, egress flags
- active binding-set revision a current activation epoch
- `state_version` a aggregate event sequence
- effective application deployment epoch
- pointer snapshot digest a latest transition logical operation
- created/updated/activated/retired/deregistered timestamps

Každý nekomutativní command používá expected `state_version`; active revision/release/binding/activation pointery se nemění oddělenými last-write-wins zápisy.

#### `component_revision`

- component ID a revision ID
- semantic version
- canonical manifest JSON
- manifest digest
- source/provenance
- validation state a evidence
- verification state a evidence
- createdAt

#### `component_tool_contract`

- component/revision
- tool name/title/description
- input/output schema
- scope, timeout, limits
- side-effect/retry/idempotency/concurrency
- contract digest

#### `component_resource_contract`

- URI template, MIME, schema, subscription, limits, digest

#### `component_prompt_contract`

- prompt name, arguments, content schema, hints, digest

#### `component_endpoint_contract`

- key, method, path, scope
- request/response schemas
- auth and webhook verification
- digest

#### `component_pulse_contract`

- direction, pulse type, schema, scope, delivery, digest

#### `component_state_contract`

- state key, category, schema, terminal, digest

#### `component_state_transition`

- from, to, trigger, guard, side effect, digest

#### `component_runtime_target`

- transport, socket/upstream, service instance, execution mode
- resource limits, readiness mode, persistent state

#### `component_contract_binding`

- source component/revision
- target component/revision
- exact tool/route/port key
- source a target contract digests
- operation scope a binding revision
- candidate/active/retired lifecycle
- activation set a created/retired/audit metadata

#### `component_release`

- component/revision/source job
- source commit/build/artifact/runtime digests
- release directory/reference
- state, previous release
- created/validated/activated/rolled back
- evidence

#### `component_readiness_gate`

- component/release
- gate key, status, reason code
- evaluator version
- evidence + digest
- correlation, executed/expires

#### `component_e2e_run`

- scenario/variant/revision/release
- invocation, input, expected, actual
- status, duration, evidence, cleanup

#### `mcp_server_revision_profile`

- component/revision
- protocol era `MODERN`, `LEGACY`, `DUAL_ERA_ADAPTER`
- supported protocol versions a canonical selected version
- canonical transport, endpoint, origin a HTTP method profile
- required `_meta`, standard header a method-specific `Mcp-Name` profile
- server capabilities a extension capability map
- supported `resultType` set
- HTTP status/JSON-RPC error map a stable error profile
- discovery/cache/pagination/subscription/MRTR/state-handle/task policies
- JSON Schema dialects, reference policy a schema resource budgets
- public access a exact internal binding profile
- legacy compatibility/era-probe profile
- normalized canonical profile JSON a digest

#### `mcp_registration_probe`

- external server registration/revision
- transport kind a exact origin/endpoint/auth-binding/TLS fingerprint
- attempted era, request version/method/header/body digest
- HTTP status, response content type, JSON-RPC ID/result/error evidence
- classification `MODERN`, `LEGACY_CANDIDATE`, `LEGACY`, `ERA_INDETERMINATE`, `PROTOCOL_INVALID`
- recognized modern error a advertised versions
- fallback decision a machine-readable důvod
- observedAt, expiresAt, process/release fingerprint a evidence digest

`401`, `403`, `407`, `429`, network/TLS/timeout a `5xx` nesmí vytvořit `LEGACY`; vytvářejí pouze `ERA_INDETERMINATE`. Fallback evidence existuje pouze po explicitním legacy probe.

#### `mcp_discovery_snapshot`

- server component/revision/release a exact endpoint
- method `server/discover`, list method nebo `resources/read`
- protocol version a client capability/extension digests
- source execution/access channel, auth/binding a exposure fingerprint
- exact request params včetně cursoru a canonical cache-key digest
- request body/header digests
- exact result payload a result digest
- jednotlivé element contract digests
- `ttlMs`, `cacheScope`, receivedAt, expiresAt
- page cursor lineage, page index evidence a aggregate traversal digest
- state `FRESH`, `STALE`, `INVALID`, `DIAGNOSTIC_ONLY`
- invalidation reason/relation, latency a verification state

Request s `inputResponses` nebo `requestState` nemá cacheable snapshot. Private snapshot je unikátní v exact access/binding contextu. Stale snapshot nesmí být execution contract.

#### `mcp_discovery_item`

- snapshot
- kind `TOOL`, `RESOURCE`, `RESOURCE_TEMPLATE`, `PROMPT`
- stable element ID
- visible name/URI
- source revision a contract digest
- normalized native schema bundle a digest
- OpenAI projection digest, je-li relevantní
- `x-mcp-header` map digest
- deterministic sort key
- verification/error state

#### `mcp_tool_alias`

- agent nebo exposure scope
- source server/tool/revision/digest
- model-visible alias
- collision strategy
- OpenAI projection relation
- compatibility state
- created/retired metadata

#### `mcp_request_event`

- server component/revision/release, endpoint a access context
- protocol era/version
- JSON-RPC request ID type `STRING|INTEGER`, canonical value a in-flight source scope
- method a method-specific name/URI/task ID
- notification flag
- source execution context a auth/binding decision
- client info/capability/extension digests
- raw bounded request headers/body nebo immutable content digests
- parsed standard/custom routing headers a header validation result
- HTTP method, origin, accept/content type, request size
- processing stage reached a handler-dispatched flag
- response HTTP status/content type
- resultType, JSON-RPC error code a stable error code
- SSE stream ID, per-stream message sequence, final-response state
- disconnect/cancel point a response-delivery state
- logical operation, correlation/causation/trace a timestamps

Active unique constraint chrání `(source/access fingerprint, request_id_type, request_id_value)` pro request bez finální response. Po terminal response se transport ID může znovu použít, ale nevytváří business dedupe.

#### `mcp_call_run`

- request event a logical operation/idempotency record
- server/tool/revision/digest
- source execution context a exact binding decision
- canonical arguments a digest
- native input/output schema digests a OpenAI projection digest
- side-effect/retry/idempotency/concurrency/ordering classification
- idempotency key a persistent concurrency claim
- state `RECEIVED`, `CLAIMED`, `EXECUTING`, `WAITING_FOR_INPUT`, `WAITING_FOR_TASK`, `RECONCILING`, `CANCEL_REQUESTED`, `SUCCEEDED`, `FAILED`, `CANCELLED`, `MANUAL_REVIEW`
- `state_version`, platform incarnation, application deployment epoch a activation epoch
- lease owner/fencing token/expiry/heartbeat
- cancellation version a latest checkpoint
- side-effect operation relations a reconciliation outcome
- effective deadline, idle timeout, started/completed timestamps
- raw attempt output a digest
- validated structured result/content a result digest
- JSON-RPC/tool error, retry directive a audit/correlation
- terminal response materialization a delivery state

Terminal `SUCCEEDED`, `FAILED` a `CANCELLED` jsou immutable. `MANUAL_REVIEW` je suspended resolution state podle 49.13. Handler completion v process memory není success bez guarded terminal DB commit.

#### `mcp_call_progress`

- call run a monotonic local sequence
- progress token
- completed/total units
- message a checkpoint reference
- emittedAt a payload digest
- response-stream ID a delivery state

Progress patří právě jednomu request streamu, nesmí být emitován po final response a nesmí prodloužit absolute deadline.

#### `mcp_input_exchange`

- call run, logical operation a exchange sequence
- initial result request ID a retry request ID
- inputRequests aggregate payload a digest
- opaque requestState ciphertext/server lookup digest
- original method/arguments/source access context
- server/tool revision, binding-set revision, activation epoch a contract digest snapshot
- status `PENDING`, `PARTIALLY_FULFILLED`, `FULFILLED`, `EXPIRED`, `CONSUMED`, `INVALIDATED`
- `state_version` a consume idempotency key
- created/expires/fulfilled/consumed timestamps
- OWNER approval/challenge references
- current/terminal outcome relation

At least one of inputRequests nebo requestState existuje. MRTR exchange vzniká před prvním non-idempotent side effectem continuation.

#### `mcp_input_request_item`

- input exchange a unique request key
- request method `elicitation/create`, `sampling/createMessage` nebo `roots/list`
- exact params a digest
- required client capability digest
- state `OUTSTANDING`, `SATISFIED`, `SUPERSEDED`, `EXPIRED`
- created/satisfied timestamps

#### `mcp_input_response_item`

- input exchange, retry request event a supplied key
- raw/normalized response a digest
- disposition `ACCEPTED`, `DUPLICATE_REPLAY`, `IGNORED_UNKNOWN`, `IGNORED_ALREADY_SATISFIED`, `REJECTED_INVALID`
- related input request item
- createdAt a audit relation

Accepted response consume a enqueue původního call resume jsou jedna transakce. Exact replay již consumed state se stejným aggregate response digestem vrací canonical outcome; změněný recognized response vrací stable consumed conflict. Unknown/already-satisfied keys se neaplikují a pouze se evidují.

#### `mcp_subscription`

- server component/revision/release
- JSON-RPC subscription request ID type/value
- source execution/access context a binding revision
- protocol version a capability/extension digests
- requested notification filter
- acknowledged filter subset
- state `OPENING`, `ACTIVE`, `CANCEL_REQUESTED`, `GRACEFUL_CLOSING`, `CLOSED`, `FAILED`
- acknowledgment persisted/emitted sequence a first-message proof
- stream opened/last keepalive/closed timestamps
- final response state a close reason
- notification count a last error
- correlation/trace

Prázdný acknowledged filter vede po first acknowledgment k graceful completion. Client disconnect neznamená graceful final response.

#### `mcp_subscription_notification`

- subscription
- local monotonic evidence sequence
- MCP notification method
- source object/URI/task ID
- payload a digest
- subscription ID in `_meta`
- emitted/delivered timestamps
- delivery result

Notification mimo acknowledged filter se nesmí emitovat. MCP subscription notifications nemají replay/event ID autoritu.

#### `mcp_state_handle`

- owner component/tool/revision a contract digest
- public opaque ID, lookup digest a never-reused generation nonce
- source execution/access context a binding revision
- activation epoch a state namespace/reference
- status `OPEN`, `CLOSED`, `EXPIRED` a `state_version`
- created/last-used/expires/closed timestamps
- close logical operation a audit/correlation

State handle je interně vynucený objekt, ale na MCP wire je běžnou tool value. Každé použití znovu ověřuje access/binding/revision/activation; handle sám není credential.

#### `mcp_task`

- server/tool/revision a original call run
- public task ID, lookup digest a logical operation
- source execution/access context a binding revision/activation epoch
- original request digest a idempotency key
- lowercase wire status projection a interní state `WORKING`, `INPUT_REQUIRED`, `COMPLETED`, `FAILED`, `CANCELLED`
- `state_version`, cancellation intent/version a expiry intent
- platform incarnation a application deployment epoch
- `createdAt`, `lastUpdatedAt`, `ttlMs`, derived `expiresAt`, `pollIntervalMs`
- latest checkpoint a pending side-effect set
- final original-method result nebo JSON-RPC error a digest
- lease owner/fencing token/expiry/heartbeat
- tombstone/purge timestamps

CreateTaskResult se emituje až po commitu řádku. Terminal task state je immutable. Tool result s `isError=true` používá task `COMPLETED`; `FAILED` je pouze JSON-RPC execution error.

#### `mcp_task_input_request`

- task a lifetime-unique request key
- exact request method/params/digest a required capability
- state `OUTSTANDING`, `SATISFIED`, `SUPERSEDED`, `EXPIRED`
- created/satisfied timestamps

#### `mcp_task_input_response`

- task, `tasks/update` request event a supplied key
- normalized response a digest
- disposition `ACCEPTED`, `DUPLICATE`, `IGNORED_UNKNOWN`, `IGNORED_ALREADY_SATISFIED`, `REJECTED_INVALID`
- acceptedAt a audit relation

Accepted known subset consume a single resume enqueue jsou jedna transakce. Unknown a already-satisfied keys nejsou side effect.

#### `mcp_task_event`

- task a monotonic sequence
- full status projection
- status message
- input/final result/error references
- payload digest a timestamp
- optional subscription notification delivery relations

#### `mcp_idempotency_record`

- canonical server/tool/operation contract revision
- caller authority kind a stable source object/revision/access fingerprint
- business target/concurrency resource
- idempotency key, unique v canonical scope
- request digest a logical operation
- original/current call attempt
- state `RESERVED`, `EXECUTING`, `WAITING_FOR_INPUT`, `WAITING_FOR_RECONCILIATION`, `SUCCEEDED`, `FAILED_FINAL`, `CANCELLED_FINAL`, `MANUAL_REVIEW`
- current/terminal result/error/event digest
- retry directive
- state version, created/updated/expires timestamps

Worker ID, lease token, HTTP/JSON-RPC request ID, SSE stream, task poll request ani execution attempt nejsou součástí canonical business scope.

### 25.5 Runtime execution a external access entities

#### `runtime_execution_context`

- execution ID a kind `COMPONENT`, `AGENT`, `PLATFORM`, `OWNER_API`, `EXTERNAL_BINDING`
- source object, exact revision, run/job/worker relation
- logical operation ID a execution attempt ID
- platform incarnation a application deployment epoch
- activation epoch, binding-set revision a execution snapshot digest
- trusted dispatcher a service/systemd identity
- volitelný UDS path a Linux peer credential evidence
- created/started/completed timestamps, state a correlation
- immutable context digest

Execution context vzniká pouze serverovým dispatch/runtime kódem a po commitu je immutable. Neexistuje create/update API a caller payload jej nemůže dodat. Context se stale platform incarnation, deployment epoch, activation epoch nebo exact revision/binding snapshotem nesmí vytvořit nový autoritativní write.

#### `runtime_instance`

- runtime instance ID a monotonic runtime generation
- component, runtime target, exact source revision a release
- artifact/runtime/dependency-lock digests
- binding-set revision a activation epoch
- platform incarnation a application deployment epoch
- desired/effective state a `state_version`
- systemd unit name, expected service class a launch manifest digest
- current systemd `InvocationID`, host boot ID, MainPID a process start ticks
- Linux UID/GID, cgroup path a effective resource profile digest
- namespace, seccomp, environment a FD profile digests
- runtime gateway connection, ready/heartbeat sequence a effective timestamp
- drain/stop/restart/cleanup logical operations a terminal cleanup state

Právě jedna runtime generation může být current pro jeden runtime instance. Runtime generation se nerecykluje. Effective runtime se potvrdí pouze při shodě DB snapshotu, systemd/process identity, release/runtime digestu, binding setu a activation/deployment epoch.

#### `runtime_process_identity`

- runtime instance/generation a process role `HOST`, `SANDBOX_INIT`, `HANDLER`, `CHILD`
- Linux PID, UID, GID a supplementary groups snapshot
- host boot ID a `/proc` process start ticks
- systemd unit, `InvocationID`, MainPID relation a cgroup path
- pidfd tracking evidence, parent/child relation a namespace profile digest
- executable/release digest a start/ready/exit timestamps
- exit code/signal/OOM/seccomp reason a immutable identity digest

Numeric PID bez boot/start/systemd/cgroup/runtime-generation shody není identita. Exit identity se nikdy znovu nepřiřadí jinému procesu.

#### `runtime_ipc_connection`

- connection ID a transport kind `RUNTIME_GATEWAY_UDS`, `HANDLER_ANONYMOUS`, `BROKER_UDS`, `BROWSER_HOST_UDS`
- canonical path nebo anonymous channel relation
- socket device/inode/type, owner/group/mode a socket unit
- peer UID/GID/PID, boot/start/systemd/cgroup evidence
- runtime instance/generation a service `InvocationID`
- protocol/profile digest, first/last sequence a in-flight counters
- state `OPENING`, `ACTIVE`, `DRAINING`, `CLOSED`, `REJECTED` a state version
- opened/validated/draining/closed timestamps a close reason

Connection je použitelná až po kernel, systemd a current PostgreSQL identity validation. Accepted connection se po peer exitu nebo service restartu nepřiřadí nové process identity.

#### `runtime_ipc_call`

- runtime IPC connection a parent/child execution context
- request ID, monotonic sequence, operation a local capability alias
- server-resolved target/binding/secret/external/state relation
- exact revision/release/runtime/binding/activation/deployment snapshot
- input/output/error digest a byte counts
- deadline, cancellation version, stream/window state a correlation
- state `RECEIVED`, `VALIDATED`, `DISPATCHED`, `STREAMING`, `RECONCILING`, `SUCCEEDED`, `FAILED`, `CANCELLED`, `MANUAL_REVIEW`
- result/error, cleanup state a timestamps

#### `runtime_credential_generation`

- service/credential stable ID a non-secret credential kind metadata
- desired/effective generation a fingerprint
- source version, systemd unit a effective `InvocationID`
- rotation logical operation, restart relation a verification evidence
- created/activated/retired timestamps a state version

Secret value ani plaintext credential se do této evidence tabulky neukládá. Runtime launch manifest generation je neautorizační launch metadata.

#### `runtime_cleanup_operation`

- runtime instance/generation a cleanup reason
- current state/checkpoint/fencing token
- process/cgroup/namespace/socket/connection/stream/temp/credential inventory
- pending side effects, leases, capacity a concurrency claims
- ordered cleanup steps, attempts, outcomes a evidence digests
- state `PENDING`, `DRAINING`, `RECONCILING`, `KILLING`, `REMOVING`, `VERIFYING`, `COMPLETE`, `MANUAL_REVIEW`, `FAILED`
- created/updated/completed timestamps a correlation/audit

`COMPLETE` vyžaduje prázdný cgroup, uzavřené connections/streams, nulovou current pointer/binding/route referenci a odstraněné temporary resources.

#### `external_auth_binding`

- binding ID, display a lifecycle
- exact component/revision/endpoint nebo external target
- auth mode a Secret/certificate reference
- exact method/path/tool relation
- rate/circuit/verifier policy
- created/updated/retired a audit metadata

Neexistují tabulky `principal`, `principal_credential`, `permission`, `external_principal` ani `external_inbound_permission`.

### 25.6 Secret entities

#### `secret_record`

- ID, stable name, display name, description, type
- purpose kind a volitelný target object ID
- status
- active version ID a secret activation epoch
- `state_version`
- tags/group/metadata
- expiry/rotation policy
- latest rotation logical operation a outcome digest
- created/updated/deleted

Stable name je unique a nerecykluje se přes soft delete. Active version pointer, secret activation epoch, state version, dependent invalidation outbox a audit se mění v jedné transakci.

#### `secret_version`

- secret ID, immutable version number
- ciphertext, nonce, algorithm, key ID
- fingerprint a canonical value digest podle typu
- lifecycle `CREATED`, `ACTIVE`, `RETIRED`
- created/activated/retired
- creator a activation logical operation

Unique `(secret_id, version_number)` a immutable ciphertext brání ABA. V jednom secretu existuje nejvýše jedna `ACTIVE` verze.

#### `secret_binding`

- secret ID a exact source object/revision
- exact usage purpose a volitelný target/account relation
- version selector a resolved-version policy
- binding revision, state version a binding digest
- activation set a activation epoch
- lifecycle, expiry a invalidation policy
- created/retired/audit metadata

Wildcard source a all-secrets binding nejsou přípustné. Active binding je unique v exact source/target/purpose scope. Rotation nemění binding revision, pokud se nemění contract; mění resolved secret epoch a invaliduje závislý session/cache stav podle policy.

#### `secret_resolution`

- source execution context a logical operation
- secret/binding ID, binding revision a binding digest
- requested stable name/purpose/target
- resolved exact secret version a secret activation epoch
- source/target revision a activation epoch snapshot
- state `RESERVED`, `RESOLVED`, `REJECTED`, `EXPIRED`
- result fingerprint, created/expires/consumed timestamps
- correlation a audit event

Runtime používá pouze `RESOLVED` záznam odpovídající current execution snapshotu. Retry stejné logical operation nemůže potichu přejít na jinou secret version uprostřed jednoho side effectu.

#### `secret_access_event`

- secret/version/source execution context/binding
- purpose, operation, correlation
- success, timestamp, runtime/job/run

### 25.7 External integration entities

#### `external_target`

- target key, base URL
- allowed paths/methods
- timeouts/retries/rate/circuit
- auth binding
- status a monitoring

#### `external_target_binding`

- source component/revision
- target a exact route/method
- request/response contract digest
- binding revision, activation set a lifecycle

#### `external_request_event`

- external target/binding/revision a exact route/method
- logical operation a side-effect operation/attempt
- target idempotency key nebo explicitní absence
- canonical request metadata/payload digest
- dispatch state, sentAt a transport evidence
- response metadata/payload digest, provider request/event ID
- outcome `CONFIRMED_APPLIED`, `CONFIRMED_NOT_APPLIED`, `UNKNOWN` nebo read-only result
- reconciliation state/evidence a next action
- latency, HTTP/provider status, retry classification a circuit decision
- platform incarnation, application deployment epoch a worker fence
- correlation/causation/trace

Event je immutable evidence; canonical business outcome je guarded v příslušném `side_effect_operation` a parent aggregate.

#### `webhook_endpoint`

- component/revision/path
- verification mode a secret refs
- schema a processing contract

### 25.8 Dashboard entities

#### `dashboard_workspace`

- singleton OWNER workspace
- viewport x/y/zoom
- filters/groups
- lock version

#### `dashboard_node_position`

- workspace/node
- x/y/z, group, collapsed

#### `dashboard_connection`

- source/target component + ports
- route/operation scope/contract digests
- compatibility state/evidence
- binding desired/effective
- correlation a timestamps

#### `dashboard_runtime_event`

- component, PULSE/tool/operation
- direction, success, correlation/trace
- occurred/received

### 25.9 Monitoring entities

#### `monitoring_profile`

- component ID
- enabled/version/profile JSON
- digest, created/activated

#### `monitoring_probe`

- component/release
- probe type, status, latency
- evidence, correlation, checkedAt

#### `component_state_history`

- component
- lifecycle/operational/recertification
- reason, correlation, recordedAt

#### `operational_alert`

- source object
- severity/type/status/title/detail
- correlation
- first/last seen, occurrences
- suppression/ack/closed timestamps

#### `alert_delivery`

- alert/channel/idempotency
- attempts/state/status/error
- next attempt/delivered timestamps

#### `monitoring_scheduler_heartbeat`

- worker, started/completed, lease, error, next run

#### `platform_worker_heartbeat`

- worker type/ID
- build/release
- heartbeatAt, lastError, metadata

### 25.10 Audit entities

#### `audit_event`

- bigint ID a chain sequence
- prev hash a event hash
- event/actor/object
- before/after
- correlation/causation/trace
- createdAt

#### `audit_head`

- singleton, last sequence/hash, updatedAt

#### `audit_archive_outbox`

- event/payload/state/attempts/lease/error/archivedAt

#### `component_audit_stream`

- component
- sequence watermarks
- gap/replay state
- current hash/integrity

#### `component_audit_event`

- stream/sequence
- workflow/step
- actor/model/tool/service
- classifications a payloads
- access-channel/binding/protocol/http/retry
- correlation/causation/trace/span
- state change, digests, hashes

#### `debug_log_event`

- process/service/level/message
- object references
- correlation/trace/span
- structured fields a full payload
- exception/stack
- timestamp, retention partition

### 25.11 Generation entities

#### `generation_job`

- `id`, fixed OWNER actor a initiating access channel/execution context
- kind `CREATE`, `UPDATE`, `FOLLOW_UP`, `RETRY`, `REPAIR`
- target kind a volitelný target object
- lifecycle state a current phase
- `state_version` a aggregate event sequence
- immutable initial request a digest
- parent/source job
- current/approved spec pointers a approved digest
- execution authority ID
- current capability snapshot a plan ID
- active phase run a latest checkpoint
- previous/candidate release a activation set
- cancellation version, blocker a error
- coordinator lease owner/fencing token/expiry/heartbeat
- coordinator platform incarnation a application deployment epoch
- created/started/completed timestamps
- client request ID a latest command logical operation

Constraints:

- jeden `client request ID` na OWNER idempotency scope,
- nejvýše jeden non-terminal coordinator lease na job,
- approved spec patří stejnému jobu,
- terminal state je immutable,
- current phase odpovídá state transition mapě,
- každý lifecycle command používá expected state version a current coordinator fence podle typu operace.

#### `generation_source`

- job
- source kind `TEXT`, `FILE`, `IMAGE`, `URL`, `API_DOC`, `CREDENTIAL_REF`, `OBJECT_REF`
- original name/locator a MIME
- content/storage reference a digest
- status
- parser version a normalized text reference
- sensitivity a retention
- created/parsed/verified/superseded timestamps

#### `generation_fact`

- job a source
- stable fact key
- classification
- statement a canonical value
- source locator
- verification method a confidence classification
- observedAt, supersededAt
- fact digest

#### `generation_owner_decision`

- job a OWNER message
- decision key a affected specification paths
- exact text a structured value
- digest
- createdAt a supersededBy

#### `generation_message`

- job, monotonic sequence, role
- content a attachments
- status
- client message ID
- turn ID
- created/completed/interrupted timestamps
- content digest

#### `generation_turn`

- job, input message a monotonic turn sequence
- status a `state_version`
- worker lease owner/fencing token/expiry/heartbeat
- platform incarnation a application deployment epoch
- active model call a provider response ID
- successor relation a unique successor slot
- interruption/cancellation version, intent a reason
- latest checkpoint a pending side-effect set
- error a terminal outcome digest
- timestamps

Completion versus steer/interrupt používá jeden job/turn row-lock commit; vznikne právě jeden terminal turn outcome a nejvýše jeden successor.

#### `generation_spec_revision`

- job a revision number
- schema version
- canonical JSON
- rendered Markdown
- digest
- parent revision
- capability snapshot/digest
- conformance precheck state a report
- createdAt

Revision je immutable a unique podle `(job_id, revision_number)` i `(job_id, digest)`.

#### `generation_execution_authority`

- job
- kind `OWNER_APPROVED` nebo `INHERITED_TECHNICAL`
- source job/spec/revision/digest
- OWNER approval event
- target identities snapshot
- lineage digest
- frozenAt

#### `generation_capability_snapshot`

- job a specification revision
- normalized requirement digest
- catalog epoch
- snapshot payload a digest
- createdAt a staleAt

#### `generation_capability_match`

- snapshot a requirement ID
- matched object/component/revision/contract
- behavior coverage
- schema compatibility
- runtime/binding eligibility
- decision `FULL_REUSE`, `PARTIAL_REUSE`, `NEW_CAPABILITY_REQUIRED`
- evidence a score

#### `generation_plan`

- job, authority a specification
- schema version
- canonical DAG JSON a digest
- validation state a report
- createdAt

#### `generation_plan_node`

- plan a stable node key
- kind a purpose
- requirement IDs
- input artifact refs/digests
- output schema/digest
- execution role
- side-effect/retry/idempotency
- timeout a budget
- checkpoint/compensation policy
- state a result artifact

#### `generation_plan_edge`

- plan
- source node a target node
- edge kind `DATA`, `CONTROL`, `ACTIVATION`, `COMPENSATION`
- required artifact/schema
- digest

Graph constraints blokují self-edge, neexistující node a cyklus v execution edges.

#### `generation_phase_run`

- job, phase a attempt
- state a `state_version`
- worker pool, lease owner/fencing token/expiry/heartbeat
- platform incarnation a application deployment epoch
- plan node range
- input checkpoint a output checkpoint
- cancellation version a pending side-effect set
- started/completed timestamps
- result summary a digest
- blocker/error/manual-review relation

Partial unique constraint povoluje nejvýše jeden active phase run na job. Phase terminal write, job transition, event/audit a enqueue další fáze jsou jedna transakce.

#### `generation_checkpoint`

- job, phase run a monotonic sequence
- checkpoint kind
- expected parent/job/phase state versions
- platform incarnation, application deployment epoch a lease fencing token
- authoritative pointer snapshot
- pending operation/side-effect/cancellation snapshot
- workspace, candidate a activation references
- provider handles
- previous checkpoint ID/digest
- canonical payload a digest
- createdAt

Checkpoint je immutable, jeho sequence je contiguous a current pointer se přepne compare-and-swap ve stejné transakci jako související state mutation.

#### `generation_tool_event`

- job/turn/phase/model call
- tool key a provider call ID
- state `STARTED`, `PROGRESS`, `COMPLETED`, `FAILED`, `CANCELLED`
- canonical args/result payload a digests
- domain operation a side-effect classification
- correlation/audit
- timestamps

#### `generation_workspace_revision`

- job a revision number
- parent revision
- source tree digest
- artifact manifest draft digest
- createdBy model call/worker
- createdAt

#### `generation_workspace_file`

- workspace revision
- canonical relative path
- MIME/type a executable flag
- content storage/reference
- size a digest
- source classification

#### `generation_workspace_patch`

- job, phase run a model call
- base workspace revision/digest
- ordered operations payload a digest
- apply state
- conflict/error
- resulting workspace revision
- created/applied timestamps

#### `generation_artifact_manifest`

- job, workspace a candidate release
- specification/authority/plan digests
- canonical manifest JSON a digest
- completeness state
- createdAt

#### `generation_artifact`

- job/workspace/release/manifest
- path/type/digest/size
- content/storage reference
- requirement IDs
- evidence references
- retention a sensitivity

#### `generation_contract_candidate`

- job a target graph node
- kind `COMPONENT`, `MCP_SERVER`, `MCP_TOOL`, `MCP_RESOURCE`, `MCP_PROMPT`, `AI_AGENT`, `AUTOMATION`
- proposed stable identity
- revision payload a digest
- source specification paths
- validation/verification state
- published object/revision relation

#### `generation_validation_run`

- job, phase run, workspace/candidate/activation set
- gate catalog version
- state
- started/completed timestamps
- blocking summary
- evidence digest

#### `generation_validation_result`

- validation run a gate key
- evaluator version
- status `PASS`, `FAIL`, `NOT_APPLICABLE`
- inputs, expected a actual
- diagnostics a artifacts
- logs/correlation
- duration a result digest

#### `generation_repair_iteration`

- job, phase a iteration number
- diagnostics cluster/digest
- input workspace revision
- model call a patch
- output workspace revision
- progress signature
- result a duration

#### `generation_blocker`

- job, phase a plan node
- stable code a classification
- title/detail
- affected requirement IDs
- evidence
- required resolution a input schema
- resume phase/checkpoint
- state
- created/resolved timestamps a resolver

#### `generation_activation_set`

- job a authority
- immutable membership digest a mutable state version
- state `DRAFT`, `READY`, `SWITCHING`, `VERIFYING`, `ACTIVE`, `ROLLING_BACK`, `ROLLBACK_VERIFYING`, `ROLLED_BACK`, `FAILED`, `MANUAL_REVIEW`
- controller lease/fence, platform incarnation a application deployment epoch
- activation domains a barrier relation
- activation/switch/rollback epochs
- previous pointer snapshot a digest
- candidate pointer snapshot a digest
- preflight/postflight/effective-ack evidence
- rollback a cleanup evidence/state
- created/switched/activated/rolled-back timestamps

`ROLLED_BACK` a `FAILED` jsou terminal pro konkrétní set. `ACTIVE` je stable successful stav, který může přejít do rollbacku pouze dokud je set current podle `activation_head` a exact candidate snapshotu; historický superseded set je kvůli current-head a snapshot guards immutable evidence. `MANUAL_REVIEW` používá exact resolution hrany 49.17.

#### `generation_activation_member`

- activation set
- object kind/ID
- previous revision/release/binding-set revision
- candidate revision/release/binding-set revision
- activation order key
- state a evidence

#### `generation_event`

- job a monotonic sequence
- event type, emittedAt a persistedAt
- payload a digest
- message/turn/phase/model/spec/plan/workspace/candidate/activation references
- correlation/causation/trace

Event sequence je unique per job a je autoritou pro SSE replay.

### 25.12 OpenAI a AI agent entities

#### `openai_model_capability_snapshot`

- exact OpenAI model ID
- Responses/Agents SDK/API compatibility profile
- stream/background/store/retrieve/cancel/resume capabilities
- strict function/structured schema profile and limits
- parallel tools, hosted tools/MCP, conversation, previous-response a compaction capabilities
- supported modalities, reasoning/output settings a token/context limits
- source evidence, verification run, observedAt, expiresAt
- canonical payload a digest

Snapshot je immutable. New production call nesmí použít expired snapshot ani požadovat capability, která v něm není potvrzená.

#### `openai_request_descriptor`

- model logical operation a attempt
- owner kind/object a parent checkpoint
- exact model, API kind, execution mode, transport, background/store policy
- ordered instructions/input/tools/output-schema payloads a digests
- model settings, history strategy a provider continuation handles
- capability snapshot
- SDK/adapter/serializer versions
- budgets, timeout/deadline
- correlation/causation/trace/group
- local idempotency scope/key a request digest

Descriptor je immutable po submit-intent commitu.

#### `ai_model_call`

- owner kind `GENERATION_TURN`, `GENERATION_PHASE`, `AGENT_RUN`, `SYSTEM_CHAT`
- owner object ID, model logical operation ID a attempt number
- predecessor/continuation relation a continuation generation
- OpenAI model a execution mode `FOREGROUND`, `BACKGROUND`
- API/transport kind a request descriptor
- SDK/client/adapter/serializer versions
- capability snapshot
- instruction/input/tools/output-schema digests
- local idempotency scope/key a request digest
- submit intent state, transport evidence, dispatchStartedAt a submittedAt
- provider response ID a diagnostický provider request ID
- provider wire status a local status podle 52.7
- completion kind `FINAL_OUTPUT`, `TOOL_CALLS`, `HOSTED_TOOL_RESULT`, `NO_OUTPUT`
- state version, platform incarnation, application deployment epoch a worker fence
- last contiguous provider event sequence a local event sequence
- canonical attempt flag a canonical response/output digest
- background/store/cancel state
- reasoning/output settings a budgets
- raw a normalized token usage, cost a latency
- started/completed/expires timestamps
- error/refusal/incomplete/recovery metadata
- correlation/trace/group IDs

Unique local idempotency scope blokuje duplicate logical operation. Attempt po `DISPATCH_STARTED` bez provider response ID se nevytvoří automaticky; stav je `MODEL_SUBMIT_OUTCOME_UNKNOWN`. Provider request ID není retrieve handle.

#### `ai_model_event`

- model call
- local monotonic sequence
- provider response ID, provider sequence a event type
- canonical raw payload a digest
- persistedAt a publishedAt

Stejná provider sequence/digest je no-op. Stejná provider sequence s jiným digestem je protocol conflict. Event se persistuje před UI/SSE publikací.

#### `ai_model_output_item`

- model call a provider response ID
- provider output index
- provider item ID, type a status
- provider call ID, je-li relevantní
- canonical raw payload a digest
- first/last provider sequence
- interpretation state a compatibility profile
- createdAt

Provider output index je unique per model call. Unknown item type se raw zachová a blokuje unsupported canonical interpretation.

#### `ai_model_output_content_part`

- parent output item
- content index a type
- canonical payload a digest
- annotations/artifact references

Content order je unique a immutable.

#### `ai_tool_dispatch`

- producing model call a provider output ordinal
- provider tool-call `call_id` a oddělený provider item ID
- tool/binding key, revision a digests
- raw argument string, canonical arguments a digest
- call ordinal a execution group
- state a approval request
- domain logical/side-effect operation a idempotency relation
- result/error payload a digest
- function-call-output payload a digest
- started/completed timestamps a correlation

Provider `call_id` je unique v producing model callu. Stejný call ID s jiným argument digestem je hard conflict. Tool output se vytvoří pouze z persistovaného outcome.

#### `ai_model_continuation`

- parent run/turn
- producing model call a response ID
- continuation generation a history strategy
- ordered resolved tool-call IDs/results a aggregate digest
- previous response/conversation/local history cursor
- successor request descriptor a successor model call
- state, checkpoint, queue/outbox relation
- createdAt

Právě jeden continuation generation může být current pro producing model call. Tool outcomes, post-tool checkpoint a successor reservation vznikají atomicky.

#### `ai_run_state_checkpoint`

- owner agent/generation run
- SDK run-state payload a serializer version
- `openai`/`@openai/agents`/adapter/code runtime versions
- exact root agent graph stable identity map a digest
- instruction/tools/session/guardrail/output digests
- `toolUseBehavior`, reasoning item ID a tool execution policies
- pending interruptions, nested agent-tool resumptions a calls
- pending output ownership/provenance
- budget/usage/current-turn snapshot
- provider handles a session strategy/version/cursor
- checkpoint sequence, previous checkpoint a digest
- createdAt

Checkpoint se resumeuje pouze exact compatible runtime/graphem. Chybějící provenance nebo incompatible serializer failne před dalším side effectem.

#### `agent_session_compaction`

- session a source session version
- source item range a aggregate digest
- mode `INPUT`, `PREVIOUS_RESPONSE_ID`
- model/capability/SDK/adapter versions
- request descriptor a provider handle
- compacted ordered items a digest
- validation/equivalence evidence
- state, active pointer relation a timestamps

Compaction candidate je immutable a nesmí odstranit unresolved call/output, approval nebo handoff boundary.

#### `agent_definition`

- stable identity/component/runtime identity kind
- display name, purpose a status
- mode
- active revision
- enabled
- created/updated/retired timestamps

#### `agent_revision`

- agent a revision number
- schema version
- purpose a success definition
- canonical instructions a variable schema
- OpenAI model a derived model settings
- input/output schemas
- run/trigger/session/memory/budget/concurrency policies
- monitoring/evaluation profile
- secret refs
- specification lineage
- canonical payload a digest
- validation/verification state
- createdAt

#### `agent_tool_binding`

- agent revision
- source kind a source object
- source revision a contract digest
- model-visible alias
- exposure filter
- operation scope a exact contract binding relation
- input/output schema digests
- side-effect/retry/timeout/approval/result policy
- compatibility state
- binding digest

#### `agent_handoff_binding`

- source agent revision a target agent revision
- orchestration pattern `HANDOFF`, `AGENT_AS_TOOL`
- purpose
- input/output schemas
- context projection
- allowed tools
- budget a max depth
- cancellation a approval policy
- digest

#### `agent_guardrail`

- agent revision
- kind `INPUT`, `OUTPUT`, `TOOL_INPUT`, `TOOL_OUTPUT`, `KCIP_PRE`, `KCIP_POST`
- stable key
- schema/rule/evaluator reference
- failure action
- priority a digest

#### `agent_session`

- agent definition/revision
- stable session ID a caller scope
- strategy
- provider conversation/previous response handles
- current item sequence
- state a lock version
- created/last activity/expires/closed

#### `agent_session_item`

- session a monotonic sequence
- item kind/role
- canonical payload a digest
- source run/model/tool/handoff
- createdAt

#### `agent_run`

- definition/revision a exact graph/tool/guardrail snapshot digests
- source execution context a trigger
- client run ID, logical operation a idempotency key
- input/output a digests
- status podle 49.14 a `state_version`
- session a context snapshot
- budget, usage a cost
- platform incarnation/application deployment epoch
- lease owner/fencing token/expiry/heartbeat
- cancellation version, latest checkpoint a pending side-effect set
- root correlation/trace
- timestamps/error/manual-review relation

Terminal `SUCCEEDED`, `FAILED` a `CANCELLED` jsou immutable; `MANUAL_REVIEW` je suspended exact-resolution state.

#### `agent_run_checkpoint`

- run a monotonic sequence
- state and completed item sequence
- session cursor
- pending model/tool/handoff/approval
- budget and usage snapshot
- SDK run-state checkpoint
- lease fencing token
- payload digest a createdAt

#### `agent_message`

- run, monotonic sequence, role a item type
- content/payload a digest
- model/tool/handoff relation
- status a timestamps

#### `agent_tool_call`

- run, model call, binding a target
- provider call ID
- canonical args/result a digests
- status
- approval a idempotency relation
- correlation/timing/error

#### `agent_handoff_run`

- root run
- source/target revisions a binding
- depth a parent handoff
- input/output a digests
- status a budget
- started/completed/error

#### `agent_approval_request`

- root run a tool/handoff call
- exact target, arguments a digest
- consequence summary
- policy source
- status `PENDING`, `APPROVED`, `REJECTED`, `EXPIRED`, `CANCELLED`
- expiresAt
- OWNER decision, message a audit
- checkpoint relation

#### `agent_memory_namespace`

- agent, type, schema, retention, indexing a access policy
- quota a digest

#### `agent_memory_item`

- namespace, key/content/vector/metadata
- source run
- timestamps a digest
- superseded/deleted relation

#### `agent_trigger`

- agent revision
- event/schedule/API/manual trigger configuration
- input mapping
- idempotency/concurrency policy
- enabled a digest

#### `agent_eval_suite`

- agent revision
- suite version a promotion policy
- blocking/aggregate thresholds
- canonical payload a digest

#### `agent_eval_case`

- suite a stable case key
- input a fixtures
- expected schemas/invariants
- grader a threshold
- side-effect/cleanup contract
- blocking flag a digest

#### `agent_eval_run`

- suite/revision/model/tool snapshot
- state
- environment a seed/fixture namespace
- summary metrics
- started/completed timestamps
- evidence digest

#### `agent_eval_case_result`

- eval run a case
- agent run
- status
- expected/actual
- grader outputs
- usage, latency a cost
- evidence a cleanup result

#### `system_chat_conversation`

- ID, title, fixed OWNER actor a access channel `SESSION` nebo `API_KEY`
- status a selected OpenAI model
- agent/session relation
- created/updated/last activity
- current object context, generation job relation a active browser session relation

#### `system_chat_message`

- conversation, monotonic sequence, role, content a attachments
- model call a status
- usage a timestamps
- correlation, causation, related object IDs a Browser target reference IDs

#### `system_chat_action`

- message, operation key, target, arguments a digest
- result a digest
- status, idempotency, correlation, audit event a timing

### 25.13 Browser entities

#### `browser_runtime_build_manifest`

- immutable manifest ID, application release a source commit
- Node.js, Playwright, locator compiler, preview adapter, automation interpreter a state serializer versions
- browser engine/channel/revision, executable a dependency digests
- OS image/release, architecture, runtime libraries, fonts, locale/timezone a sandbox profile digests
- headless/headed mode, launch arguments a environment allowlist digest
- capability map pro WebAuthn, client certificates, permissions, service workers, downloads, tracing, preview a bridge
- compatibility relations k state bundle, automation revision a host generation
- canonical JSON, SHA-256 digest, validation/verification state a evidence

Manifest je immutable. Host, context, state bundle nebo revision s neznámou compatibility relation se nesmí attachnout/replayovat pouze podle názvu engine nebo podobné verze.

#### `browser_session`

- session ID, parent kind/ID a purpose
- execution target, engine a runtime build manifest
- host slot/bridge, host generation/bridge connection epoch
- current context instance a context generation
- account binding, external session family a account auth epoch
- lifecycle podle 49.19, sensitivity a operation scope
- `state_version`, platform incarnation a application deployment epoch
- active page/page generation/document epoch
- latest observation/frame revision a viewport transform digest
- current control holder, control epoch, persistent `last_control_fencing_token` a current control lease
- active action, transfer, challenge, dialog, download a barrier relations
- created/ready/last activity/expires/closed timestamps
- error, recovery a cleanup operation/state

Terminal lifecycle blokuje nové actions, tickets, context attachment a control leases bez ohledu na pokračující cleanup.

#### `browser_session_binding`

- session
- related object kind/ID
- relation `OWNER`, `VIEWER`, `SOURCE`, `RESULT`, `AUDIT`
- createdAt a revokedAt

#### `browser_host_slot`

- host/slot ID, runtime build manifest, process identity a host generation
- systemd unit/InvocationID, boot ID, PID/start ticks a cgroup
- capacity, current contexts, drain/admission state
- typed UDS endpoint inode/fingerprint, lease a heartbeat
- current/previous application deployment epoch
- last crash/restart/error a cleanup evidence

#### `browser_context_instance`

- session, context ID a context generation
- host/bridge/profile relation a runtime build manifest
- creation mode `NON_PERSISTENT`, `BRIDGE_PROFILE`, `CDP_COMPATIBILITY`
- locale/timezone/device/viewport/permission/client-certificate profile digests
- account binding/auth epoch a restored bundle version
- created/attached/detached/closed timestamps
- lifecycle, browser process identity a cleanup state

Context ID/generation je never-reused v rámci session. Similar-looking runtime handle bez celé identity n-tice není attach authority.

#### `browser_page`

- session/context a stable page ID
- page generation a opaque runtime handle fingerprint
- opener page/generation a creation causation action
- active-preview/closed state
- URL, origin, title a lifecycle
- current document ID/epoch, top frame ID a navigation sequence
- last navigation/result digest
- created/closed timestamps a close reason

#### `browser_frame`

- page/generation a stable frame ID
- frame attachment epoch a opaque runtime handle fingerprint
- parent frame/attachment epoch
- origin, URL, name, sandbox/permission attributes
- attached state, OOPIF/process route a current document ID/epoch
- latest semantic/visual digest
- attached/detached timestamps a detach reason

#### `browser_document`

- page/frame, document ID a monotonic document epoch
- created by navigation/BFCache restore/prerender activation/recovery reason
- URL, origin, navigation ID/sequence a lifecycle
- initial/last observation and DOM/semantic digest
- created/retired timestamps

Document epoch se změní při document replacementu, BFCache restore, prerender activation, OOPIF document replacementu nebo jiném adapterem potvrzeném novém document instance. Same-document navigation zvýší navigation sequence bez změny document epoch.

#### `browser_navigation`

- session/page/frame/document lineage
- navigation ID a monotonic navigation sequence
- causation action/input event
- requested URL/origin/method a redirect chain artifact
- type `FULL`, `SAME_DOCUMENT`, `BF_CACHE_RESTORE`, `PRERENDER_ACTIVATION`, `POPUP_INITIAL`, `FRAME`
- state `REQUESTED`, `STARTED`, `COMMITTED`, `DOM_CONTENT_LOADED`, `LOAD_FIRED`, `ABORTED`, `FAILED`
- previous/new document epoch a origin-policy outcome
- timings, error a evidence digests

#### `browser_observation`

- session/context/page/frame/document identity a monotonic observation revision
- URL/title/navigation/lifecycle/viewport/transform
- semantic interactables a bounded content summary
- active element, selection, dialog/challenge/permission/download state
- service-worker/worker/console/network error summary
- screenshot/artifact refs
- DOM/semantic/visual/diff digests
- createdBy action/run a createdAt

#### `browser_preview_frame`

- session, stream epoch a monotonic frame revision
- base/key frame relation
- page/generation/frame/document/observation identity
- viewport transform a digest
- image/patch artifact, MIME, dimensions, size
- createdAt a retention/cleanup state

#### `browser_preview_ticket`

- session, OWNER session/API-key channel a audience
- capability set, hash/fingerprint, issued/expires/used/revoked
- stream/session binding a correlation

#### `browser_preview_event`

- session, stream epoch, sequence a event type
- control/page/frame/document/observation/frame revision
- bounded payload a artifact refs
- createdAt a correlation

#### `browser_control_lease`

- session, holder kind/ID
- platform incarnation a application deployment epoch
- context generation, control epoch a monotonic fencing token
- state a state version
- issued/heartbeat/expires/released timestamps podle DB času
- takeover/control-transfer source a checkpoint relation

Partial unique constraint dovoluje nejvýše jeden active lease per session; expired lease nelze heartbeat-em oživit.

#### `browser_control_transfer`

- session, requested holder/current holder
- expected session/control state version a current action relation
- state `REQUESTED`, `DRAINING`, `GRANTED`, `REJECTED`, `CANCELLED`, `MANUAL_REVIEW`
- input-state reset evidence a safe checkpoint
- previous/new control epoch/fence
- result/error, created/terminal timestamps

#### `browser_input_event`

- session, control lease/epoch/fence a client/stream sequence
- context/page generation/frame attachment/document/observation/frame revision
- viewport transform digest
- type, input-state sequence a bounded payload
- earliest mutation trigger classification
- accepted/rejected state a reason
- resulting action relation a timestamp

#### `browser_target_reference`

- session/context/page generation/frame attachment/document epoch
- kind `DOM_ELEMENT`, `TEXT_RANGE`, `FRAME`, `PAGE`, `VISUAL_REGION`
- source frame/observation revision a viewport transform digest
- semantic locator AST a element fingerprint
- accessible metadata, expected role/state, text digest a bounding box
- creator, status, expiresAt a revalidation evidence

#### `browser_action_run`

- session, source execution context, parent run/message/step
- logical operation/idempotency record a action kind/schema version
- exact input a request digest
- target reference/locator plan a target digest
- operation scope, control epoch/fence, binding revision a activation epoch
- runtime build, host/bridge/context/page/frame/document identity
- observation/frame/viewport revisions
- mutation trigger, side-effect/retry/reconciliation class a `side_effect_operation`
- account/resource/external-session concurrency claims
- pre/post observation a immutable checkpoint
- state podle 49.19 a state version
- lease/fence/incarnation/deployment epoch podle execution mode
- cancellation version, result/error, correlation a timings

Action se označí `SUCCEEDED` pouze po validní postcondition nebo exact manual resolution evidence.

#### `browser_action_attempt`

- action run a attempt number
- host/bridge command ID, action fence a exact browser identity snapshot
- resolved target candidates a chosen target digest
- actionability/trial/force evidence
- input strategy a pre-armed waiter inventory
- Playwright/bridge method outcome
- navigation/popup/dialog/permission/filechooser/download outcomes
- postcondition/read-back a evidence
- started/ended timestamps a error

#### `browser_action_dispatch_event`

- action attempt a monotonic phase sequence
- host/bridge phase `COMMAND_ACCEPTED`, `TARGET_RESOLVED`, `ACTIONABILITY_PASSED`, `INPUT_SEQUENCE_STARTED`, `MUTATION_TRIGGER_POSSIBLY_ISSUED`, `NAVIGATION_OR_REQUEST_OBSERVED`, `METHOD_RETURNED`, `POST_OBSERVATION_CAPTURED`; platformní `INTENT_RECORDED`, `POSTCONDITION_CHECKED`, `RECONCILIATION_STARTED` a `OUTCOME_RECORDED` zůstávají oddělenými guarded action/side-effect events
- host/bridge/context/page/frame/document/control identity
- timestamp, raw adapter evidence digest a correlation

Exact duplicate phase/digest je no-op; stejná phase sequence s jiným digestem je hard protocol conflict.

#### `browser_operation_scope`

- parent kind/ID, OWNER message nebo revision
- target/redirect origins a external-protocol/local-target policy
- allowed operation classes a side-effect ceiling
- account/tenant/resource/data constraints
- clipboard/upload/download constraints
- confirmation/challenge policy, canonical digest a expiry

#### `browser_irreversible_confirmation`

- session/action/run/step
- exact page/frame/document/target/arguments/consequence
- account identity, operation scope, control epoch a observation digest
- OWNER confirmation, expiry a consumedAt
- timestamps a digest

#### `browser_account_binding`

- stable account binding ID, service/target a account key
- expected account/tenant identity a external session family
- OWNER display metadata
- credential/certificate/virtual-authenticator Secret refs
- active Browser state bundle
- auth mode, authenticated condition a account auth epoch
- account concurrency mode a provider limit
- allowed origins, status, expiry/rotation policy
- last verified/used a audit

#### `browser_auth_attempt`

- session/run/step/account binding a auth epoch
- auth mode, login flow kind a credential versions
- page/frame/document/origin/RP relation
- state, challenge relation a side-effect operation pro registration/logout
- authenticated account/tenant evidence
- result/error, started/completed timestamps

#### `browser_state_bundle`

- account binding a immutable version
- encrypted payload, nonce, key ID a algorithm
- runtime build/engine/serializer capability fingerprint
- origin inventory, account/auth epoch a captured state kinds
- credential/certificate/virtual-authenticator versions a binding revision
- authenticated condition a quiescence evidence
- created/verified/expires/invalidated timestamps
- content digest a active pointer relation

#### `browser_state_bundle_member`

- bundle, stable member kind/key a origin/partition scope
- kind `COOKIE`, `LOCAL_STORAGE`, `INDEXED_DB`, `SESSION_STORAGE`, `PERMISSION`, `CLIENT_CERTIFICATE_METADATA`, `VIRTUAL_WEBAUTHN`
- encrypted content or content-addressed artifact reference
- serializer version, digest, size a compatibility metadata

Browser cache, service-worker executable/runtime state, raw profile a platform passkey/private key nejsou standardní members.

#### `browser_local_bridge`

- bridge ID, mTLS certificate binding, fixed OWNER relation a device label
- OS/architecture a bridge build manifest
- browser/profile inventory a capability fingerprint
- public key/certificate fingerprint a certificate generation
- allowed local targets a status
- enrolled/revoked/last heartbeat timestamps

#### `browser_bridge_connection`

- bridge, certificate generation a connection epoch
- peer certificate fingerprint a negotiated protocol/capability digest
- OS/browser/profile capability snapshot
- connected/heartbeat/disconnected timestamps
- disconnect/revoke reason a state

#### `browser_bridge_assignment`

- bridge connection a exact session/context generation
- operation scope/local target/account/profile relation
- control epoch/action fence and assignment lease
- state, assigned/released timestamps a cleanup evidence

#### `browser_profile_lease`

- bridge/profile stable ID a browser build
- owner session/account binding
- lease/fencing/connection epoch
- mode, state, issued/expires/released timestamps
- current browser process/profile lock evidence

Partial unique constraint dovoluje nejvýše jeden active writer per bridge profile.

#### `browser_dialog`

- session/page/frame/document a dialog sequence
- type `ALERT`, `CONFIRM`, `PROMPT`, `BEFOREUNLOAD`, `NATIVE`
- causation action, safe message digest a default value metadata
- policy/challenge, state a response digest
- opened/resolved/expired timestamps

#### `browser_permission_request`

- session/context/page/frame/document/origin
- permission kind, causation action a requested scope
- policy/challenge, response and effective permission state
- created/resolved/revoked timestamps

#### `browser_upload_handle`

- owner session/run/step a artifact relation
- safe name, MIME, extension, size, digest a sensitivity
- allowed target/file-count/directory policy
- created/expires/consumed/cleanup timestamps

#### `browser_download`

- session/run/step/action a page/frame/document initiator
- source/initiator origin, URL kind a download event sequence
- suggested/safe name, MIME, expected size/contract
- state `STARTED`, `STREAMING`, `COMPLETED`, `FAILED`, `CANCELLED`
- persisted artifact, size/digest/content verification
- temp path handle a cleanup state

#### `browser_teaching_run`

- parent/session/status a control participants
- source operation scope
- event range a compiler/runtime versions
- candidate automation revision a coverage/ambiguity/mutation-semantics report

#### `browser_teaching_step`

- teaching run a ordered step
- source input/action event range
- semantic action, page/frame/document route a locator candidates
- typed input/Secret/artifact/prior-output binding
- input strategy, mutation trigger a pre-armed waiter contract
- pre/post conditions a independent read-back
- side-effect/retry/reconciliation/concurrency
- evidence a OWNER resolution

#### `browser_automation_definition`

- stable identity, owner component, name, enabled, active revision

#### `browser_automation_revision`

- manifest/digest a Browser Interaction Plane/interpreter version
- runtime build/engine/bridge capability requirements
- origins, redirect/local targets a account/tenant policy
- auth/account/state-bundle bindings
- input/output schema
- immutable steps, locator/target/mutation-trigger/postcondition digests
- schedule timezone/DST/misfire/overlap policy
- verification status/evidence
- createdAt a compatibility relations

#### `browser_automation_run`

- definition/revision/caller a exact revision digest
- clientRunId, schedule fire ID, logical operation a idempotency scope
- operation scope, browser session a account binding/auth epoch
- status podle 49.21 a state version
- current step/attempt a input/output digests
- platform incarnation/application deployment epoch
- lease owner/fencing token/expiry/heartbeat
- account/resource concurrency claims
- latest checkpoint/control epoch/cancellation version
- pending challenge/dialog/download/side effects/manual review
- error a timestamps

Terminal `SUCCEEDED`, `FAILED` a `CANCELLED` jsou immutable; `MANUAL_REVIEW` je suspended resolution state.

#### `browser_automation_run_step`

- run/step/attempt
- browser action run
- status a observed browser/account state
- mutation trigger, side-effect/reconciliation
- timing/error/evidence

#### `browser_automation_artifact`

- session/run/step/action/type/storage reference
- page/frame/document source identity
- MIME/size/digest/safe name
- source origin a sensitivity
- retention/scan/cleanup state

#### `browser_auth_binding`

- definition/revision/account key
- browser account binding
- mode a required Secret/certificate/virtual-authenticator refs
- state bundle compatibility a expected account/tenant condition
- expiry/invalidation

#### `browser_challenge`

- session/run/step/type/status
- page/frame/document/origin/RP/account a pending action digest
- auth epoch/control epoch a exact deadline
- safe prompt a allowed resolution methods
- created/expires/resolved
- OWNER/bridge response reference a consume digest

### 25.13.1 Self-test entities

#### `self_test_run`

- suite, scope, requested object IDs a selected cases
- caller OWNER API key fingerprint, expected release/SHA
- state, lease, progress, started/completed timestamps
- summary counts, cleanup state a evidence digest

#### `self_test_case_result`

- run, case key, object/endpoint/component
- environment, started/completed, status, duration
- request/response, assertions, logs, audit a artifact references

#### `self_test_catalog_entry`

- stable case key, suite, capability, required environment
- target operation, setup, execution, assertions, cleanup a evidence contract

### 25.14 Release a deployment entities

#### `application_release`

- release ID, source commit, build ID
- manifest/artifact/signature digests
- immutable release state/evidence
- created/deployed timestamps
- previous release

#### `deployment_run`

- release/environment a logical operation
- state podle 49.24 a state version
- platform incarnation a application deployment epoch before/candidate
- controller lease/fence
- step timings/results/checkpoints
- backup/migration/symlink/database epoch/process effective evidence
- error/rollback/manual review

#### `deployment_step`

- run a stable ordered step key/attempt
- state, intent, expected before/after digests
- side-effect operation a outcome/reconciliation
- started/completed, duration, result, evidence

#### `backup_record`

- database/files/config
- digest, location, createdAt, verification, retention

#### `production_acceptance_run`

- expected SHA/release
- checks, results, timing
- created/completed

### 25.15 Operational config entities

#### `operational_setting`

- key, type/category
- encrypted/plain desired value podle typu
- default, desired version, state version, fingerprint
- restart flag, applies-to
- source, updatedAt, actor
- management mode, owner editable flag a derivation evidence

#### `operational_setting_applied`

- setting/desired version, target service/build
- effective version a effective digest
- config apply run
- appliedAt, verification/result/error

### 25.16 Korektnostní a orchestrační entity

#### `platform_incarnation`

- singleton current row
- random `platform_incarnation_id`
- monotonic `incarnation_sequence`
- createdAt, reason, source restore/deployment evidence

#### `domain_command`

- command/logical operation IDs
- operation contract/revision
- target aggregate
- caller authority/execution context/channel
- canonical arguments a request digest
- idempotency key/scope
- expected state/revision/binding/activation snapshot
- deadline, status, state/result/error digests
- correlation/causation a timestamps

#### `domain_idempotency_record`

- canonical scope a key, unique
- request digest a logical operation
- state `RESERVED`, `EXECUTING`, `WAITING_FOR_INPUT`, `WAITING_FOR_RECONCILIATION`, `SUCCEEDED`, `FAILED_FINAL`, `CANCELLED_FINAL`, `MANUAL_REVIEW`
- `state_version`, original/current attempt
- terminal result/error/event digest
- retention/expiry

#### `side_effect_operation`

- logical operation, parent aggregate/run a stable step key
- target binding/revision a request digest
- side-effect/retry class a target idempotency key
- precondition, expected postcondition, reconciliation a compensation contracts
- state podle 49.8, current attempt a state version
- parent fence/incarnation, outcome/evidence digests a timestamps

#### `side_effect_attempt`

- operation a attempt sequence, unique
- immutable canonical request bytes/digest, exact target/binding snapshot a target idempotency key
- parent, concurrency a authority fence snapshot
- exactly one canonical dispatch-authority outbox ID
- createdAt, creator a immutable attempt digest

#### `side_effect_attempt_state`

- operation/attempt composite reference, unique
- lifecycle `INTENT_RECORDED`, `DISPATCHING`, `OUTCOME_RECORDED`, `RECONCILING`, `CONFIRMED_APPLIED`, `CONFIRMED_NOT_APPLIED`, `FAILED_FINAL`, `UNKNOWN`; uvnitř jednoho attemptu používá pouze hrany 49.8 od `INTENT_RECORDED` do outcome a nikdy retry hranu, protože retry je vždy nový attempt row
- `state_version`, `last_evidence_sequence`, autorizovaný delivery owner/fence snapshot, platform incarnation a deployment epoch
- adapter/provider request ID, dispatch timestamp a current raw-outcome/reconciliation evidence pointers
- started/updated/terminal timestamps

#### `side_effect_attempt_evidence`

- operation/attempt a contiguous evidence sequence alokovaná pod locked attempt-state row; unique `(operation_id,attempt_sequence,evidence_sequence)`
- kind `REQUEST`, `DISPATCH_PREPARED`, `RESPONSE`, `TRANSPORT_ERROR`, `READ_BACK`, `RECONCILIATION`, `CONFIRMATION`
- immutable raw bytes nebo artifact reference, canonical digest, source fence/incarnation a observedAt
- correlation/trace a evidence digest

#### `transactional_outbox`

- immutable event ID/type/stream/sequence
- parent logical operation a materialized parent state/cancellation/activation snapshot
- payload a digest
- destination kind/key, purpose a `is_dispatch_authority`
- side-effect operation/attempt, concurrency key-set digest a target idempotency key; pro `SIDE_EFFECT_DISPATCH` jsou tyto hodnoty non-null a exact
- availableAt, attempts, `state_version`, lease/fence/incarnation/deployment epoch
- delivery state podle purpose, delivered/closedAt a error; ordinary event používá `READY|CLAIMED|RETRY_WAIT|DELIVERED|FAILED_FINAL`, authority dispatch používá `READY|CLAIMED|DISPATCH_AUTHORIZED|RECONCILING|CLOSED`

#### `transactional_inbox`

- consumer scope a immutable external/PULSE event ID, unique
- source stream a sequence, payload/schema digest
- target aggregate snapshot, receive/processing state, `state_version` a logical operation
- canonical outcome/event/audit references
- received/completed timestamps

#### `queue_item`

- queue kind, parent a logical operation, unique
- payload/digest, priority/fairness/dependencies
- canonical concurrency key set a non-null key-set digest
- materialized parent state version, revision/binding/activation snapshot, `unmet_dependency_count`, `barrier_blocked` a `cancellation_requested`
- state, available/deadline, attempt/max attempts
- `state_version`, lease owner/fence/incarnation/application deployment epoch/acquired/heartbeat/expiry
- checkpoint, outcome/retry/dead-letter

#### `concurrency_claim`

- namespace a canonical key digest, primary key
- owner logical operation/attempt a `state_version`
- lease state/owner/fence/incarnation/deployment epoch/acquired/heartbeat/expiry
- persistent last fencing token; row se po release nebo expiry nemaže

#### `binding_set`

- UUID, owner object kind/ID a stable purpose
- current/active revision ID a digest
- state version, activation epoch a current activation set
- lifecycle a created/updated timestamps

Active revision pointer je parent-owned composite foreign key. Root se mění pouze activation transakcí nebo explicitním jednoprvkovým activation setem.

#### `binding_set_revision`

- binding set identity a immutable revision number
- canonical complete member snapshot a digest
- source specification/activation set lineage
- validation, compatibility a activation eligibility
- createdAt a immutable author/evidence

#### `binding_set_member`

- binding-set revision
- member kind `CONTRACT`, `SECRET`, `EXTERNAL_TARGET`, `ROUTE`, `TOOL`
- exact source/target revisions, operation/purpose a contract digest
- non-null canonical member key digest, member digest, lifecycle evidence a parent-owned foreign keys

#### `activation_domain_head`

- stable domain key/digest, unique
- current activation epoch a current activation set
- admission state `OPEN`, `DRAINING`, `CLOSED`
- current barrier relation, state version a platform incarnation
- updatedAt

#### `domain_command_activation_domain`

- domain command a activation domain
- pinned activation epoch
- operation class `READ_ONLY` nebo `MUTATING`
- state `ADMITTED`, `CHECKPOINTED`, `RECONCILING`, `TERMINAL`
- admitted/updated/terminal timestamps a evidence digest

#### `activation_head`

- singleton last activation epoch
- current platform incarnation a updatedAt

#### `application_deployment_head`

- singleton current application deployment epoch
- current application release/manifest digest
- platform incarnation, state version a updatedAt

#### `activation_domain_barrier`

- activation set a domain
- state `REQUESTED`, `DRAINING`, `CLOSED`, `RELEASED`, `FAILED`
- admission epoch, pending mutating operation count
- lease/fence, timestamps a evidence

#### `cleanup_operation`

- owner object/run/candidate/session
- cleanup kind a state/step/checkpoint
- lease/fence/incarnation
- pending resources/effects, result/error/evidence
- terminalAt pro fyzický active-scope constraint

#### `cleanup_resource`

- cleanup operation a stable resource kind/key digest
- exact desired terminal postcondition
- lifecycle, state version a current attempt
- side-effect operation, read-back a evidence digests
- created/updated/completed timestamps

#### `configuration_apply_run`

- config group a desired/previous snapshot digests
- state podle 49.23
- target services/effective versions
- lease/fence, step outcomes, rollback/evidence

#### `schema_migration`

- monotonic migration version a immutable checksum
- release/build a transaction mode
- state `PLANNED`, `RUNNING`, `APPLIED`, `FAILED`, `MANUAL_REVIEW`
- expand/migrate/validate/activate/contract phase
- persistent lease owner/fence/incarnation/expiry a terminalAt
- checkpoint/cursor, schema fingerprint a evidence
- started/completed timestamps a error

Všechny entity a constraints kapitol 49 a 51 jsou součástí greenfield baseline, migrations, OpenAPI/KCIP evidence, self-testu a production acceptance.

### 25.17 Agentic authority, provenance a confused-deputy entity

#### `authority_lineage`

- immutable lineage ID, parent lineage a root kind `OWNER_MESSAGE`, `OWNER_APPROVED_SPECIFICATION`, `OWNER_DELEGATED_SOURCE`, `ACTIVE_AGENT_REVISION`, `ACTIVE_AUTOMATION_REVISION`, `ACTIVE_COMPONENT_REVISION`, `EXTERNAL_ENDPOINT_CONTRACT` nebo `PLATFORM_RUNTIME_POLICY`
- root object/message/specification/revision reference a exact digest
- inherited target/operation/side-effect/Secret-use semantic ceiling
- canonical lineage payload, lineage digest, createdAt a creator execution context
- žádný mutable current-authority flag; currentness se ověřuje proti parent aggregate/revision pointerům

#### `operation_intent`

- immutable intent ID/revision, authority lineage a source OWNER message/specification/revision
- objective, requirement IDs, canonical target selectors, operation classes a side-effect ceiling
- argument slots, dynamic-target slots, delegated-source references a value-derivation contracts
- Secret-use purposes, target/origin/account constraints a placement templates
- delegation graph ceiling, success/postcondition, stop/cancel conditions, expiry a digest
- immutable revision payload se nemění; validation/current state `COMPILED`, `VALIDATED`, `SUPERSEDED`, `INVALIDATED` drží samostatný `operation_intent_head` se `state_version`; dispatch používá pouze `VALIDATED` current lineage

#### `content_provenance`

- immutable content ID, parent content/transformation ID a source kind
- source object/revision, URI/path/item/line/DOM/resource locator a observedAt
- raw bytes/artifact reference, raw digest, canonical digest a MIME/schema
- content role `INSTRUCTION`, `DATA`, `EVIDENCE`, `PROPOSAL`, `RESULT`, `CODE`, `METADATA`
- instruction authority `OWNER_DIRECT`, `OWNER_APPROVED_SPECIFICATION`, `OWNER_DELEGATED_SOURCE`, `ACTIVE_REVISION`, `PLATFORM_RUNTIME`, `NONE`
- taint/provenance flags, extraction/normalization method a bounded transform chain

#### `instruction_segment`

- model call/request descriptor a ordered segment sequence
- exact source provenance, role, instruction authority a destination `INSTRUCTIONS|INPUT|TOOL_DEFINITION|OUTPUT_SCHEMA`
- rendered bytes/digest a compiler version
- unique `(request_descriptor_id,segment_sequence)` a immutable segment digest

#### `operation_context`

- immutable context ID a parent command/run/turn/phase/automation step
- authority-lineage a operation-intent IDs/digests
- exact actor/execution/revision/tool/action/target/binding/activation snapshot
- canonical target constraints, argument schema/origin map, side-effect/retry/idempotency/concurrency contract
- Secret-use plan, delegation projection, deadline, precondition/postcondition a provenance-manifest digest
- state `COMPILED`, `VALIDATED`, `DISPATCH_RESERVED`, `DISPATCHED`, `TERMINAL`, `MANUAL_REVIEW`, `INVALIDATED` a state version pro guarded lifecycle
- immutable canonical payload/digest; změna významu vytváří nový context, nikoli update payloadu

#### `semantic_action_plan`

- operation context a producing model call/output item/tool call/handoff reference
- proposed alias/text a server-resolved canonical operation/tool/revision/binding
- exact target, canonical arguments, argument-origin map a value-derivation references
- side-effect class, Secret-use context references, postcondition a reconciliation
- validation result, stable error, validator version a immutable plan digest

#### `value_derivation`

- operation context/action plan a destination argument nebo dynamic-target path
- source content provenance, exact locator/path/selector a source digest
- deterministic transform/normalizer, schema, constraints a transform version
- canonical value/digest, validation evidence a requirement ID
- derivation nikdy nemění instruction authority source contentu

#### `secret_use_context`

- operation context, semantic action plan a exact Secret binding alias/revision
- declared purpose, consumer step/tool/action a logical operation
- exact target component/external target/origin/account/tenant a allowed placement `HEADER|QUERY|BODY|BROWSER_FIELD|SDK_CONFIG|RUNTIME_VALUE|CODE_ARTIFACT`
- argument/property path, lifetime/attempt, expected recipient contract a digest
- resolve/result evidence; nejde o permission row a nemá grant/allow/deny semantics

#### `agentic_security_event`

- stable code, classification a severity
- operation context, authority lineage, content provenance a action-plan references
- attempted tool/target/argument/delegation/Secret-use semantic change
- validation decision, no-side-effect evidence, recovery directive, correlation a timestamps

Všechny immutable payload tables používají canonical digest, reject-update/delete trigger a parent-owned composite foreign keys. Mutující dispatch bez current `operation_context` ve stavu `DISPATCH_RESERVED` a se shodným digestem nemůže získat side-effect dispatch outbox.

## 26. API kontrakt

### 26.1 Obecná pravidla

API root je `https://kaja.hcasc.cz/api/v1`. Response používají JSON, ISO 8601, UUID, stabilní enumy, cursor pagination a deterministic sort.

Každý OWNER endpoint přijímá OWNER session nebo jediný `KCML_OWNER_API_KEY` a po autentizaci používá konstantní `OWNER_FULL`; neexistuje role/scope autorizace. UI, chat a API key volají stejnou doménovou operaci.

Každá mutace deklaruje:

- povinný `Idempotency-Key` a jeho scope,
- `If-Match` nebo body `expectedStateVersion` pro nekomutativní změnu,
- expected revision/digest, binding-set revision, activation/control epoch a checkpoint digest podle operace,
- authority-lineage, OWNER-intent a operation-context digests u agentní/modelové/browserové nebo content-derived operace,
- side-effect, retry a concurrency class,
- synchronous linearization point nebo durable async operation reference.

Authority-lineage, OWNER-intent a operation-context fields jsou authoritative server-derived metadata; OWNER/API klient, model ani business payload je nesmí zvolit nebo přepsat. Root OWNER command posílá pouze business požadavek a běžné CAS/idempotency guards; gateway semantic context sestaví a persistuje před modelovým nebo mutujícím dispatch.

UI idempotency key vytvoří před prvním sendem a při retry jej zachová. Chybějící povinný key nebo CAS guard je validation error před doménovým handlerem. Timeout klienta serverový command neruší.

Operation catalog:

```text
GET  /operations/catalog
POST /operations/:operationKey/invoke
```

Success `meta` obsahuje `correlationId`, `logicalOperationId`, `commandId`, current `stateVersion`, `eventSequence`, `activationEpoch`, `resultDigest`, `idempotencyReplay` a `serverTime`. Async `202` znamená commitnutý command+queue/outbox, nikoli dokončený side effect.

Conflict response vrací stable code, current snapshot/version/digests a canonical next action. Standardní error envelope zachovává classification, details a correlation. Přesný concurrency/recovery contract je v 49.27.

### 26.2 Fixed OWNER auth a API klíč

```text
POST   /auth/login
POST   /auth/login/mfa
POST   /auth/logout
POST   /auth/api-key-session
GET    /session
POST   /session/reauthenticate
GET    /owner/security
POST   /owner/mfa/enroll
POST   /owner/mfa/verify
POST   /owner/recovery-codes/rotate
GET    /owner/sessions
DELETE /owner/sessions/:id
POST   /owner/sessions/revoke-others
POST   /owner/sessions/revoke-all
GET    /owner/api-key
GET    /owner/api-key/value
POST   /owner/api-key/rotate
GET    /owner/api-key/usage
```

Neexistuje `/users`, `/accounts`, `/roles`, `/groups`, `/memberships`, `/invites`, `/permissions` ani jiný user-management endpoint. Login body obsahuje ručně zadané username, heslo a volitelný MFA continuation; server username pouze exact porovná s jedinou persistovanou identitou `KRMAR78` a nikdy jej nepoužije jako volbu účtu.

### 26.3 Dashboard

```text
GET    /dashboard/topology
PUT    /dashboard/layout
GET    /dashboard/events
GET    /dashboard/identity-cards
POST   /dashboard/connections/preview
POST   /dashboard/connections
PUT    /dashboard/connections/:id/binding
DELETE /dashboard/connections/:id
PUT    /dashboard/nodes/:id/suspension
GET    /dashboard/nodes/:id/deregistration-preview
POST   /dashboard/nodes/:id/deregister
POST   /dashboard/secrets/:secretId/bindings
DELETE /dashboard/secrets/:secretId/bindings/:nodeId
GET    /dashboard/secrets/:secretId/bindings/bulk-preview
POST   /dashboard/secrets/:secretId/bindings/bulk
```

### 26.4 Components

```text
GET    /components
POST   /components
GET    /components/:id
PATCH  /components/:id
GET    /components/:id/revisions
POST   /components/:id/revisions
GET    /components/:id/releases
POST   /components/:id/validate
POST   /components/:id/verify
POST   /components/:id/activate
POST   /components/:id/enable
POST   /components/:id/disable
POST   /components/:id/suspend
POST   /components/:id/quarantine
POST   /components/:id/restore
POST   /components/:id/rollback
POST   /components/:id/repair
POST   /components/:id/recertify
POST   /components/:id/e2e-runs
POST   /components/:id/state-queries
POST   /components/:id/heartbeat-challenges
GET    /components/:id/logs
GET    /components/:id/audit
GET    /components/:id/bindings
GET    /components/:id/secrets
GET    /components/:id/usage
```

### 26.5 MCP

```text
GET    /mcp-servers
POST   /mcp-servers
GET    /mcp-servers/:id
PATCH  /mcp-servers/:id
GET    /mcp-servers/:id/revisions
POST   /mcp-servers/:id/revisions
GET    /mcp-servers/:id/revisions/:revisionId
POST   /mcp-servers/:id/revisions/:revisionId/validate
POST   /mcp-servers/:id/revisions/:revisionId/verify
POST   /mcp-servers/:id/revisions/:revisionId/activate
POST   /mcp-servers/:id/revisions/:revisionId/compatibility
GET    /mcp-servers/:id/discovery-snapshots
POST   /mcp-servers/:id/discovery-snapshots
GET    /mcp-servers/:id/discovery-snapshots/:snapshotId
GET    /mcp-servers/:id/discovery-snapshots/:snapshotId/diff
GET    /mcp-servers/:id/registration-probes
POST   /mcp-servers/:id/era-probe
POST   /mcp-servers/:id/server-discover-test
POST   /mcp-servers/:id/request-metadata-test
POST   /mcp-servers/:id/streamable-http-test
POST   /mcp-servers/:id/cache-pagination-test
POST   /mcp-servers/:id/mrtr-test
POST   /mcp-servers/:id/tasks-test
POST   /mcp-servers/:id/wire-edge-matrix
POST   /mcp-servers/:id/tools/list-test
POST   /mcp-servers/:id/resources/list-test
POST   /mcp-servers/:id/resources/templates/list-test
POST   /mcp-servers/:id/prompts/list-test
POST   /mcp-servers/:id/tools/:toolName/call-test
POST   /mcp-servers/:id/subscriptions/list-test
GET    /mcp-request-events
GET    /mcp-request-events/:requestEventId
GET    /mcp-request-events/:requestEventId/raw
GET    /mcp-call-runs
GET    /mcp-call-runs/:callId
GET    /mcp-call-runs/:callId/events
GET    /mcp-call-runs/:callId/outcome
POST   /mcp-call-runs/:callId/cancel
POST   /mcp-call-runs/:callId/reconcile
GET    /mcp-call-runs/:callId/input-exchanges
GET    /mcp-input-exchanges/:exchangeId
POST   /mcp-input-exchanges/:exchangeId/respond
GET    /mcp-subscriptions
GET    /mcp-subscriptions/:subscriptionId
POST   /mcp-subscriptions/:subscriptionId/cancel
GET    /mcp-subscriptions/:subscriptionId/notifications
GET    /mcp-state-handles
GET    /mcp-state-handles/:handleId
POST   /mcp-state-handles/:handleId/close
GET    /mcp-tasks
GET    /mcp-tasks/:taskId
POST   /mcp-tasks/:taskId/update
POST   /mcp-tasks/:taskId/cancel
GET    /mcp-tasks/:taskId/events
GET    /mcp-tools
POST   /mcp-tools
GET    /mcp-tools/:id
GET    /mcp-tools/:id/revisions
GET    /mcp-tools/:id/callers
GET    /mcp-tools/:id/usage
GET    /mcp-resources
GET    /mcp-resources/:id
GET    /mcp-resource-templates
GET    /mcp-resource-templates/:id
GET    /mcp-prompts
GET    /mcp-prompts/:id
GET    /mcp-aliases
POST   /mcp-aliases/preview
POST   /mcp-aliases
DELETE /mcp-aliases/:id
```

Revision create přijímá canonical contract payload a `Idempotency-Key`. Activation přijímá exact revision ID, digest a volitelný activation set ID. Každá testovací operace vrací immutable evidence ID, exact server/release/binding snapshot, raw request/response digests, stable error a cleanup result.

`era-probe` provádí deterministickou matici 10.19. `401|403|407|429`, network/TLS/timeout a `5xx` vrací `ERA_INDETERMINATE` a nesmí spustit legacy fallback. Recognized modern error potvrzuje modern era. Legacy se uloží až po explicitním legacy probe.

`server-discover-test` používá moderní per-request metadata a ověřuje supported versions, capabilities/extensions, server info, cache hints, response ID a resultType. `request-metadata-test` ověřuje body/header equality, duplicate singleton headers, unsupported version, missing capability, Origin, method/name/task routing a pre-handler no-side-effect evidence.

`streamable-http-test` ověřuje POST-only profil, HTTP/JSON-RPC status map, request-scoped SSE, exact response ID, notification scoping, final response, všechny disconnect případy z 10.25 a nepřítomnost MCP event IDs/Last-Event-ID resume. `cache-pagination-test` ověřuje TTL/scope, private isolation, notification invalidation, empty cursor, invalid cursor a restart traversal. `mrtr-test` a `tasks-test` používají exact state/task contracty z 10.11 a 10.16.

Discovery snapshot endpointy pracují pouze s immutable snapshots. Test calls vždy uvádějí exact server/tool revision nebo current active pointer vrácený v response metadata. Subscription test vrací first acknowledgment, acknowledged filter subset, subscription ID, notifications a graceful/abrupt close evidence; nepředstírá replay.

`POST /mcp-input-exchanges/:exchangeId/respond` přijímá exact exchange digest a typed inputResponses. Unknown/already-satisfied keys se neaplikují a evidují se jako ignored. Invalid/expired/wrong-context nebo changed consumed state se odmítne stable error bez side effectu. OWNER approval používá stejný underlying approval service.

`POST /mcp-call-runs/:callId/reconcile` je dostupný pouze pro call ve stavu vyžadujícím reconciliation a vykonává declared read-back operation. API nemůže ručně označit unknown external effect za applied/not-applied bez explicitní OWNER manual-review action, evidence a auditu.

Task update/cancel management endpointy používají stejnou doménovou state machine jako MCP `tasks/update|cancel`, ale nejsou náhradou wire conformance. Cancel ACK pouze persistuje intent; current canonical task state se čte samostatně. State-handle close neodstraňuje audit ani potvrzené tool results.

### 26.6 AI agents

```text
GET    /agents
POST   /agents
GET    /agents/:id
PATCH  /agents/:id
GET    /agents/:id/revisions
POST   /agents/:id/revisions
GET    /agents/:id/revisions/:revisionId
POST   /agents/:id/revisions/:revisionId/validate
POST   /agents/:id/revisions/:revisionId/verify
POST   /agents/:id/revisions/:revisionId/activate
POST   /agents/:id/revisions/:revisionId/compatibility
GET    /agents/:id/revisions/:revisionId/tool-bindings
POST   /agents/:id/revisions/:revisionId/tool-bindings/preview
GET    /agents/:id/revisions/:revisionId/handoffs
GET    /agents/:id/revisions/:revisionId/guardrails
GET    /agents/:id/revisions/:revisionId/promotion-gates
POST   /agents/:id/enable
POST   /agents/:id/disable
POST   /agents/:id/repair
POST   /agents/:id/runs
GET    /agents/:id/runs
GET    /agent-runs/:runId
GET    /agent-runs/:runId/events
GET    /agent-runs/:runId/checkpoints
POST   /agent-runs/:runId/messages
POST   /agent-runs/:runId/pause
POST   /agent-runs/:runId/resume
POST   /agent-runs/:runId/cancel
GET    /agent-runs/:runId/tool-calls
GET    /agent-runs/:runId/handoffs
GET    /agent-runs/:runId/approvals
POST   /agent-runs/:runId/approvals/:approvalId/approve
POST   /agent-runs/:runId/approvals/:approvalId/reject
GET    /agents/:id/sessions
GET    /agent-sessions/:sessionId
GET    /agent-sessions/:sessionId/items
POST   /agent-sessions/:sessionId/compact
POST   /agent-sessions/:sessionId/close
GET    /agents/:id/memory
POST   /agents/:id/memory/search
POST   /agents/:id/memory/items
DELETE /agents/:id/memory/items/:itemId
GET    /agents/:id/eval-suites
POST   /agents/:id/eval-suites
GET    /agent-eval-suites/:suiteId
POST   /agent-eval-suites/:suiteId/runs
GET    /agent-eval-runs/:runId
GET    /agent-eval-runs/:runId/cases
GET    /agents/:id/triggers
POST   /agents/:id/triggers
PATCH  /agents/:id/triggers/:triggerId
DELETE /agents/:id/triggers/:triggerId
```

Run create přijímá `clientRunId`, input odpovídající active revision schema a volitelný session ID. Response vrací exact agent revision, tool binding snapshot, root correlation a run state. Resume vyžaduje current checkpoint digest; stale resume vrací conflict.

Approval approve/reject přijímá exact argument digest. API odmítne approval po expiraci, po změně pending callu nebo po terminal runu.

### 26.6.1 Centrální AI chat

```text
GET    /chat/conversations
POST   /chat/conversations
GET    /chat/conversations/:id
GET    /chat/conversations/:id/messages
POST   /chat/conversations/:id/messages
GET    /chat/conversations/:id/events
POST   /chat/conversations/:id/cancel
GET    /chat/conversations/:id/browser-sessions
POST   /chat/conversations/:id/browser-sessions
POST   /chat/conversations/:id/browser-sessions/:sessionId/attach
POST   /chat/ask
```

`POST /chat/ask` poskytuje jednoduchý produkční request/response kontrakt pro externí systémy. Request obsahuje přirozený jazyk, volitelný conversation ID, attachments, Browser target reference IDs a idempotency key. Response obsahuje lidskou odpověď, provedené actions, dotčené objekty, browser session/action výsledky, correlation IDs a odkazy na evidence. Stejný endpoint může streamovat přes SSE nebo vrátit dokončený výsledek.

### 26.6.2 OpenAI execution evidence

```text
GET    /ai/model-calls
GET    /ai/model-calls/:id
GET    /ai/model-calls/:id/request-descriptor
GET    /ai/model-calls/:id/events
GET    /ai/model-calls/:id/output-items
GET    /ai/model-calls/:id/tool-dispatches
GET    /ai/model-calls/:id/continuations
GET    /ai/model-calls/:id/checkpoints
POST   /ai/model-calls/:id/retrieve
POST   /ai/model-calls/:id/resume-stream
POST   /ai/model-calls/:id/request-cancel
POST   /ai/model-calls/:id/reconcile
GET    /ai/model-capabilities
GET    /ai/model-capabilities/:snapshotId
POST   /ai/model-capabilities/refresh
```

Endpointy zpřístupňují OWNERovi local logical operation/attempt, immutable descriptor, capability snapshot, provider response ID, diagnostický request ID, normalizované i raw statusy, ordered output items, tool/MCP dispatch, continuation chain, usage, cost, timings, trace, SDK runtime a recovery evidence.

`retrieve`, `resume-stream` a `request-cancel` pracují pouze nad již persistovaným exact `response.id`. `reconcile` nevytváří nový create call; uzavírá provider/tool outcome podle kapitoly 52. API neposkytuje obecné tlačítko `retry`, nemění modelový output a neobchází parent run/job state machine.

### 26.7 Generation

```text
POST   /generation/jobs
GET    /generation/jobs
GET    /generation/jobs/:id
GET    /generation/jobs/:id/snapshot
GET    /generation/jobs/:id/messages
POST   /generation/jobs/:id/messages
GET    /generation/jobs/:id/turns
GET    /generation/jobs/:id/events
GET    /generation/jobs/:id/sources
POST   /generation/jobs/:id/sources
GET    /generation/jobs/:id/sources/:sourceId
GET    /generation/jobs/:id/facts
GET    /generation/jobs/:id/owner-decisions
GET    /generation/jobs/:id/capability-snapshots
POST   /generation/jobs/:id/capability-snapshots/refresh
GET    /generation/jobs/:id/spec
GET    /generation/jobs/:id/spec/revisions
GET    /generation/jobs/:id/spec/revisions/:revisionId
POST   /generation/jobs/:id/spec/revisions/:revisionId/precheck
POST   /generation/jobs/:id/approve-spec
GET    /generation/jobs/:id/authority
GET    /generation/jobs/:id/plans
GET    /generation/jobs/:id/plans/:planId
POST   /generation/jobs/:id/plans/:planId/validate
GET    /generation/jobs/:id/phases
GET    /generation/jobs/:id/phases/:phaseRunId
GET    /generation/jobs/:id/checkpoints
GET    /generation/jobs/:id/model-calls
GET    /generation/jobs/:id/tool-events
GET    /generation/jobs/:id/workspace/revisions
GET    /generation/jobs/:id/workspace/revisions/:revisionId
GET    /generation/jobs/:id/workspace/revisions/:revisionId/files
GET    /generation/jobs/:id/workspace/patches
GET    /generation/jobs/:id/contract-candidates
GET    /generation/jobs/:id/artifact-manifests
GET    /generation/jobs/:id/artifacts
GET    /generation/jobs/:id/validation-runs
GET    /generation/jobs/:id/validation-runs/:runId
POST   /generation/jobs/:id/validation-runs
GET    /generation/jobs/:id/blockers
POST   /generation/jobs/:id/blockers/:blockerId/resolve
GET    /generation/jobs/:id/activation-set
POST   /generation/jobs/:id/cancel
POST   /generation/jobs/:id/retry
POST   /generation/jobs/:id/follow-up
POST   /generation/jobs/:id/resume
GET    /generation/jobs/:id/releases
GET    /generation/jobs/:id/logs
GET    /generation/jobs/:id/audit
GET    /generation/jobs/:id/browser/session
POST   /generation/jobs/:id/browser/session
GET    /generation/jobs/:id/browser/preview
POST   /generation/jobs/:id/browser/attach
POST   /generation/jobs/:id/browser/credentials
POST   /generation/jobs/:id/browser/account/save
POST   /generation/jobs/:id/browser/operation-scope
POST   /generation/jobs/:id/browser/confirmations
GET    /generation/jobs/:id/browser/teaching
POST   /generation/jobs/:id/browser/teaching
POST   /generation/jobs/:id/browser/teaching/preflight
POST   /generation/jobs/:id/browser/teaching/replay
POST   /generation/jobs/:id/browser/takeover
POST   /generation/jobs/:id/browser/return-to-ai
```

`POST /generation/jobs` přijímá `clientRequestId`, kind, objective, attachments a target reference. Stejný ID a shodný request digest vrací existující job; rozdílný digest vrací conflict.

`POST /generation/jobs/:id/approve-spec` vyžaduje exact `revisionId`, `digest` a current capability snapshot digest. Approval se provede v jedné transakci s execution authority a enqueue planneru.

`POST /generation/jobs/:id/blockers/:blockerId/resolve` validuje resolution proti blocker input schema a přijímá credential pouze přes Secret/credential reference. Resume vyžaduje exact latest checkpoint digest.

Workspace, model-call, validation a activation endpointy jsou read-only evidence surfaces, pokud jejich konkrétní operace není uvedena. OWNER nemůže ručně označit gate PASS nebo přímo přepnout lifecycle.

Job-scoped browser endpointy jsou convenience aliases nad `/browser-sessions`. Vracejí stejné session ID, control epoch, preview protocol a audit jako centrální chat nebo agentní browser call; nevytvářejí generation-only browser implementaci.

### 26.8 Browser Interaction Plane a automatizace

#### Browser sessions

```text
GET    /browser-sessions
POST   /browser-sessions
GET    /browser-sessions/:sessionId
GET    /browser-sessions/:sessionId/snapshot
GET    /browser-sessions/:sessionId/events
GET    /browser-sessions/:sessionId/pages
POST   /browser-sessions/:sessionId/pages
GET    /browser-sessions/:sessionId/pages/:pageId
POST   /browser-sessions/:sessionId/pages/:pageId/activate
POST   /browser-sessions/:sessionId/pages/:pageId/close
GET    /browser-sessions/:sessionId/frames
GET    /browser-sessions/:sessionId/documents
GET    /browser-sessions/:sessionId/navigations
POST   /browser-sessions/:sessionId/observe
POST   /browser-sessions/:sessionId/actions
GET    /browser-sessions/:sessionId/actions/:actionId
GET    /browser-sessions/:sessionId/actions/:actionId/attempts
POST   /browser-sessions/:sessionId/actions/:actionId/cancel
POST   /browser-sessions/:sessionId/actions/:actionId/reconcile
POST   /browser-sessions/:sessionId/actions/:actionId/resolve-outcome
GET    /browser-sessions/:sessionId/control
POST   /browser-sessions/:sessionId/control/acquire
POST   /browser-sessions/:sessionId/control/release
POST   /browser-sessions/:sessionId/control/return-to-ai
GET    /browser-sessions/:sessionId/control-transfers
POST   /browser-sessions/:sessionId/operation-scopes
GET    /browser-sessions/:sessionId/targets
POST   /browser-sessions/:sessionId/targets/pick
POST   /browser-sessions/:sessionId/targets/:targetId/revalidate
POST   /browser-sessions/:sessionId/credentials
POST   /browser-sessions/:sessionId/auth/verify
GET    /browser-sessions/:sessionId/auth-attempts
POST   /browser-sessions/:sessionId/accounts/save
POST   /browser-sessions/:sessionId/state-bundles/capture
POST   /browser-sessions/:sessionId/dialogs/:dialogId/respond
POST   /browser-sessions/:sessionId/permissions/:requestId/respond
POST   /browser-sessions/:sessionId/challenges/:challengeId/resolve
POST   /browser-sessions/:sessionId/pause
POST   /browser-sessions/:sessionId/resume
POST   /browser-sessions/:sessionId/recover
POST   /browser-sessions/:sessionId/close
GET    /browser-sessions/:sessionId/artifacts
POST   /browser-sessions/:sessionId/uploads
GET    /browser-sessions/:sessionId/downloads
POST   /browser-sessions/:sessionId/preview-tickets
GET    /browser-sessions/:sessionId/preview/latest
WSS    /browser-sessions/:sessionId/preview/ws
```

Create přijímá `clientSessionId`, parent kind/ID, purpose, requested execution target, initial URL nebo source, account binding a operation scope reference. Stejný client ID a stejný canonical request digest vrací existující session; jiný digest vrací conflict.

Session response vždy obsahuje exact runtime build manifest, host/bridge/context generation, control epoch/fence, active page/page generation/document epoch, latest observation/frame/viewport digests a account auth epoch. Opaque Playwright/CDP handle se veřejnému klientovi nevrací.

`POST /actions` vyžaduje:

- action schema a exact action kind,
- `Idempotency-Key` pro každou mutující nebo potenciálně mutující action,
- expected session state version,
- runtime build/host-or-bridge/context identity,
- exact control epoch a fence,
- page/page generation, frame attachment, document epoch, observation/frame revision a viewport transform,
- operation-scope digest,
- target reference nebo locator plan digest,
- earliest mutation trigger,
- precondition, postcondition/read-back a retry/reconciliation class.

Server může authoritative identity pole doplnit z current state, avšak caller je nesmí přepsat. Mismatch vrací stable stale/conflict error před host/bridge dispatchi. Response vrací action ID, logical operation, current state a dispatch/recovery contract; `202` znamená commitnutý intent/queue, nikoli provedený browser side effect.

`POST /actions/:actionId/reconcile` nevytváří nový action. Claimne current reconciliation pod novým fence a provede pouze revision-defined read-back. `resolve-outcome` je exact OWNER manual-review operation s expected action state version a evidence digest; nelze ji použít k tichému změnění terminal outcome.

Control acquire/release/return je compare-and-swap nad current session/control state version. Takeover během possible mutation vrátí transfer ID a stav `DRAINING|MANUAL_REVIEW`; nevydá nový lease před known outcome. Stale epoch vrací `409 BROWSER_CONTROL_STALE`.

Preview WSS neakceptuje OWNER API klíč v URL; používá OWNER cookie, Authorization handshake nebo one-time preview ticket. Reconnect vyžaduje nový stream epoch a snapshot acknowledgment. Client input z předchozího stream/control/page/document/viewport epochu se nereplayuje.

Upload endpoint přijímá bytes/connector artifact, metadata a policy a vrací opaque upload handle; nikdy nepřijímá host filesystem path. Download endpoint vrací až persistovaný artifact nebo current typed state, nikoli neověřenou browser temp cestu.

#### Accounts, state bundles a local bridges

```text
GET    /browser-accounts
POST   /browser-accounts
GET    /browser-accounts/:accountId
PATCH  /browser-accounts/:accountId
POST   /browser-accounts/:accountId/verify
POST   /browser-accounts/:accountId/reauthenticate
POST   /browser-accounts/:accountId/logout
POST   /browser-accounts/:accountId/invalidate-state
GET    /browser-accounts/:accountId/state-bundles
GET    /browser-accounts/:accountId/state-bundles/:bundleId
POST   /browser-accounts/:accountId/state-bundles/:bundleId/verify
POST   /browser-accounts/:accountId/state-bundles/:bundleId/activate
POST   /browser-accounts/:accountId/state-bundles/:bundleId/invalidate
GET    /browser-bridges
POST   /browser-bridges/enrollments
POST   /browser-bridges/enrollments/:enrollmentId/complete
GET    /browser-bridges/:bridgeId
GET    /browser-bridges/:bridgeId/connections
GET    /browser-bridges/:bridgeId/profiles
POST   /browser-bridges/:bridgeId/test
POST   /browser-bridges/:bridgeId/rotate-certificate
POST   /browser-bridges/:bridgeId/revoke
```

Account create/update deklaruje target/service, stable account key, expected account/tenant identity, auth mode, Secret/certificate/virtual-authenticator refs, state-bundle policy, account concurrency mode a allowed origins. Nezakládá KájovoCML user identity ani permission.

Verify spouští bounded fresh context/profile check a vrací exact observed account/tenant, auth epoch, origin inventory a compatibility evidence. Pouhá existence cookie nebo absence login formuláře není success.

State-bundle activation je CAS nad account-binding state version, auth epoch, credential/certificate versions, runtime build compatibility a authenticated-condition digest. Bundle member inventory je OWNER-visible. Standardní bundle nesmí obsahovat raw browser profile, cache/service-worker runtime ani platform passkey/private key.

Enrollment vydá one-time challenge svázanou s pevnou OWNER identitou a exact bridge ID. Bridge vytvoří vlastní keypair, dokončí mTLS registration a od té chvíle používá svůj bridge certificate binding. API nikdy nepřijímá arbitrary remote debugging URL od běžného chatového požadavku.

Bridge detail zobrazuje exact build/capability fingerprint, certificate generation, current connection epoch, browser/profile inventory, session assignments, local target bindings a cleanup. Revoke/rotate atomicky invaliduje stale connection epoch a zablokuje nové dispatches; pending possible mutation se reconciliuje.

#### Browser automations

```text
GET    /browser-automations
POST   /browser-automations
GET    /browser-automations/:id
PATCH  /browser-automations/:id
GET    /browser-automations/:id/revisions
POST   /browser-automations/:id/revisions
GET    /browser-automations/:id/revisions/:revisionId
POST   /browser-automations/:id/revisions/:revisionId/preflight
POST   /browser-automations/:id/revisions/:revisionId/verify
POST   /browser-automations/:id/revisions/:revisionId/activate
POST   /browser-automations/:id/revisions/:revisionId/rollback
GET    /browser-automations/:id/auth-bindings
POST   /browser-automations/:id/auth-bindings
GET    /browser-automations/:id/schedules
POST   /browser-automations/:id/schedules
PATCH  /browser-automations/:id/schedules/:scheduleId
POST   /browser-automations/:id/teaching-runs
GET    /browser-teaching-runs/:teachingRunId
POST   /browser-teaching-runs/:teachingRunId/stop
POST   /browser-teaching-runs/:teachingRunId/compile
POST   /browser-automations/:id/runs
GET    /browser-automations/:id/runs
GET    /browser-runs/:runId
GET    /browser-runs/:runId/session
GET    /browser-runs/:runId/steps
POST   /browser-runs/:runId/control/acquire
POST   /browser-runs/:runId/control/release
POST   /browser-runs/:runId/control/return-to-runtime
POST   /browser-runs/:runId/cancel
POST   /browser-runs/:runId/reauthenticate
POST   /browser-runs/:runId/reconcile
POST   /browser-runs/:runId/resolve-outcome
POST   /browser-runs/:runId/challenges/:challengeId/resolve
GET    /browser-runs/:runId/artifacts
GET    /browser-runs/:runId/artifacts/:artifactId
POST   /browser-automations/:id/enable
POST   /browser-automations/:id/disable
POST   /browser-automations/:id/repair
```

Revision create přijímá complete immutable manifest. Server znovu canonicalizuje locator AST, page/frame/document routes, mutation-trigger semantics, action schemas, account policy, postconditions, read-backs, schedule policy a runtime compatibility a počítá vlastní digest.

Preflight je read-only podle revision contractu. Verify může provést pouze deklarované fixture/test-account/reversible live scenarios s vlastní setup/cleanup a idempotency. Activate je atomický pointer switch se required exact bindings, account policy a runtime build compatibility v jednom activation setu.

Run create používá `clientRunId`, exact revision/input digest, account binding/auth epoch, operation scope a schedule fire relation. Stejný scope/digest replayuje current/terminal run; conflict nevytvoří druhou session. Run status rozlišuje queue, Playwright/bridge dispatch, mutation possible, postcondition, reconciliation, cleanup a terminal outcome.

Schedule endpoint vyžaduje timezone, DST, misfire/catch-up a overlap `SKIP|QUEUE|COALESCE|REJECT`. Každý fire má deterministic idempotency key. Schedule nemůže obejít account/resource concurrency nebo spustit disabled/stale revision.

Všechny endpointy používají stejné doménové služby jako UI, chat a KCIP operations. Automation run je parentem generické browser session a jeho preview/control používá stejný protokol, stejné identity a stejný side-effect ledger.

### 26.9 Secrets a password manager

```text
GET    /secrets
POST   /secrets
GET    /secrets/:id
PATCH  /secrets/:id
DELETE /secrets/:id
GET    /secrets/:id/value
POST   /secrets/:id/versions
GET    /secrets/:id/versions
POST   /secrets/:id/versions/:versionId/activate
POST   /secrets/:id/rotate
GET    /secrets/:id/bindings
POST   /secrets/:id/bindings
DELETE /secrets/:id/bindings/:bindingId
GET    /secrets/:id/usage
POST   /secrets/:id/test-resolve
POST   /secrets/generate-password
POST   /secrets/import
POST   /secrets/export
```

### 26.10 Runtime execution contexts a bindings

```text
GET    /runtime/executions
GET    /runtime/executions/:id
GET    /runtime/instances
GET    /runtime/instances/:id
GET    /runtime/instances/:id/processes
GET    /runtime/instances/:id/connections
GET    /runtime/instances/:id/calls
GET    /runtime/instances/:id/sandbox
GET    /runtime/instances/:id/cleanup
POST   /runtime/instances/:id/drain
POST   /runtime/instances/:id/restart
POST   /runtime/instances/:id/reconcile
POST   /runtime/instances/:id/verify-boundary
GET    /runtime/boundary/evidence
GET    /bindings
GET    /bindings/:id
POST   /bindings/preview
POST   /bindings/test
```

Tyto endpointy používají OWNER_FULL a centrální object-action registry. Evidence endpointy jsou read-only; lifecycle operace vytvářejí guarded runtime command a neprovádějí přímou systemd nebo process mutaci z HTTP handleru.

Neexistuje endpoint pro create/update execution contextu, změnu peer identity, vložení source/target/revision/binding/access channelu, přidělení runtime tokenu ani otevření interního socketu. Obecný binding CRUD neexistuje; binding vzniká pouze verzovanou component/agent/Secret/external konfigurací nebo Dashboard connection operací a aktivuje se s activation setem.

### 26.11 Externí systémy

```text
GET    /external/targets
POST   /external/targets
GET    /external/targets/:id
PATCH  /external/targets/:id
POST   /external/targets/:id/test
POST   /external/targets/:id/circuit/open
POST   /external/targets/:id/circuit/close
GET    /external/auth-bindings
POST   /external/auth-bindings
PATCH  /external/auth-bindings/:id
GET    /external/target-bindings
POST   /external/target-bindings
DELETE /external/target-bindings/:id
GET    /external/requests
GET    /webhooks
POST   /webhooks/test
```

### 26.12 Monitoring a alerts

```text
GET    /monitoring/overview
GET    /monitoring/probes
GET    /monitoring/state-history
GET    /monitoring/profiles
PUT    /monitoring/profiles/:componentId
POST   /monitoring/probe/run
GET    /alerts
GET    /alerts/:id
POST   /alerts/:id/acknowledge
POST   /alerts/:id/suppress
POST   /alerts/:id/close
GET    /alerts/:id/deliveries
POST   /alerts/channels/test
GET    /workers/heartbeats
```

### 26.13 Audit a logy

```text
GET    /audit/events
GET    /audit/events/:id
GET    /audit/integrity
POST   /audit/integrity/verify
GET    /audit/archive
GET    /logs
GET    /logs/stream
GET    /logs/correlations/:correlationId
POST   /logs/export
```

### 26.14 Konfigurace a provoz

```text
GET    /config/settings
GET    /config/settings/:key
PUT    /config/settings/:key
POST   /config/settings/:key/reset
POST   /config/validate
POST   /config/apply
POST   /config/export
POST   /config/import
GET    /releases
GET    /releases/:id
POST   /releases/:id/rollback
GET    /deployments
GET    /backups
POST   /backups
POST   /backups/:id/verify
GET    /acceptance-runs
POST   /maintenance/restart-service
GET    /system/health
GET    /system/readiness
GET    /system/version
```

### 26.14.1 Self-test a testovací katalog

```text
GET    /self-tests/catalog
GET    /self-tests/fault-catalog
GET    /self-tests/models
POST   /self-tests/runs
GET    /self-tests/runs
GET    /self-tests/runs/:id
GET    /self-tests/runs/:id/events
GET    /self-tests/runs/:id/history
GET    /self-tests/runs/:id/evidence
POST   /self-tests/runs/:id/replay
POST   /self-tests/runs/:id/shrink
POST   /self-tests/runs/:id/cancel
POST   /self-tests/runs/:id/cleanup
GET    /system/capabilities
GET    /system/recovery
GET    /system/closure
```

Self-test API spouští unit, contract, integration, migration, build, runtime, MCP, agent, browser, UI, monitoring, audit, deployment, stateful property-based, deterministic fault-injection, chaos/recovery a registered-element E2E suites podle prostředí. Každý run používá jediný `KCML_OWNER_API_KEY` a kanonické externí API URL. Test runner balí skutečné testovací implementace do release a používá izolované fixture namespaces, idempotentní setup a úplný cleanup.

Každý registrovaný prvek publikuje v manifestu povinné E2E scénáře. Self-test runtime je načte, zavolá jeho kanonický hostname, ověří tools, endpoints, state, heartbeat, monitoring, exact bindings a audit a vrátí jednotnou evidence.

### 26.15 SSE

KájovoCML rozlišuje dvě neslučitelné SSE sémantiky:

1. **Domain/UI replayable SSE** pro generation, audit, monitoring, chat a jiné platformní streams.
2. **MCP request-scoped SSE** podle kapitoly 10, které není replayable a nepoužívá event IDs.

Domain/UI SSE endpointy používají envelope:

```json
{
  "eventId": 123,
  "type": "domain.event.name",
  "objectId": "uuid",
  "emittedAt": "timestamp",
  "payload": {}
}
```

Persistentní domain event IDs jsou monotonic v deklarovaném stream namespace. Heartbeat a resync notice používají nepersistentní frame. Klient MAY poslat `Last-Event-ID`; server provede snapshot + bounded replay. Nedostupný range vrací `resync.required` a klient načte authoritative snapshot.

Tato replay pravidla MUST NOT být použita na MCP endpoint `/mcp`. MCP SSE:

- patří právě jednomu JSON-RPC requestu nebo `subscriptions/listen`,
- nemá `eventId`, SSE `id:` ani `Last-Event-ID` resume,
- po disconnectu se neobnovuje od poslední zprávy,
- u běžného requestu končí jednou finální JSON-RPC response,
- u subscription po reconnectu vyžaduje nový `subscriptions/listen` a full refresh,
- u mutujícího callu řeší ztracenou response business idempotency/reconciliation, nikoli SSE replay.

Reverse proxy a client libraries musí mít pro oba profily oddělenou konfiguraci a testy. Přidání domain event ID do MCP streamu nebo aplikace MCP no-replay semantics na audit/generation stream je blocking protocol failure.

### 26.16 Agentic authority a provenance evidence

```text
GET    /authority/lineages/:id
GET    /authority/lineages/:id/graph
GET    /operation-intents/:id
GET    /operation-contexts/:id
GET    /operation-contexts/:id/action-plans
GET    /operation-contexts/:id/value-derivations
GET    /operation-contexts/:id/secret-uses
GET    /provenance/content/:id
GET    /provenance/content/:id/graph
GET    /agentic-security/events
GET    /agentic-security/events/:id
POST   /agentic-security/self-tests
POST   /agentic-security/evidence/export
```

Tyto endpointy zpřístupňují OWNERovi úplnou a neredigovanou semantic authority/provenance evidence. Nevytvářejí role, permissions, Secret approvals ani confirmation workflow. Vytvoření lineage, intentu, contextu, action planu a Secret-use contextu probíhá uvnitř stejné kanonické command/run state machine, nikoli jako klientem volitelný bypass.

## 27. Procesy, workery, fronty a leases

### 27.1 Procesní role

KájovoCML NG používá samostatné systemd služby:

- `kcml-web-api` — veřejné HTTP/SSE/WSS, OWNER UI API, public authentication a operation catalog,
- `kcml-runtime-gateway` — jediná interní dispatch a exact-binding gateway pro runtime hosts a trusted platform callers,
- `kcml-runtime-host@<runtime-instance-id>` — trusted per-runtime launcher, sandbox supervisor a handler capability adapter,
- `kcml-central-chat-worker` — centrální chat runs,
- `kcml-generation-coordinator` — generation lifecycle a phase scheduling,
- `kcml-generation-openai-worker` — Responses API a Agents SDK specialist runs,
- `kcml-generation-workspace-worker` — atomic patch application, build a focused checks,
- `kcml-generation-integration-worker` — candidate integration saga,
- `kcml-generation-validation-worker` — validation, eval a conformance gates,
- `kcml-generation-activation-worker` — activation set switch, postflight a rollback,
- `kcml-agent-worker` — produkční agent runs a resumable approvals,
- `kcml-browser-worker` — Browser Interaction Plane commands, observations, actions, teaching, automation a recovery,
- `kcml-browser-host@<slot>` — exact BrowserRuntimeBuildManifest, pinovaný Playwright/browser process, non-persistent isolated BrowserContexty a typed host protocol za systemd-owned UDS hranicí,
- `kcml-browser-bridge-gateway` — outbound mTLS OWNER Device Bridge enrollment, connection/certificate epochs, capability negotiation a session/profile-scoped typed routing,
- `kcml-component-control-worker` — enable/disable/state/heartbeat control,
- `kcml-component-e2e-worker` — component E2E a canonical probes,
- `kcml-monitor-worker` — scheduler, probes, SLO a repair enqueue,
- `kcml-alert-primary-worker`,
- `kcml-alert-backup-worker`,
- `kcml-secret-broker`,
- `kcml-egress-gateway`,
- `kcml-state-broker`,
- `kcml-audit-archiver`,
- `kcml-self-test-worker`,
- `kcml-acceptance-runner` on demand.

Generated handler není samostatná trusted service identity. Je child process tree runtime hostu v oddělených namespaces s nulovými capabilities a jediným anonymous capability FD.

Každá trusted služba má vlastní non-login OS identity, systemd credentials pouze pro vlastní potřebu, read-only application release, přesně vymezené writable paths, network/address-family policy, cgroup limity, health, readiness, watchdog a platform heartbeat. Socket group membership je úzké podle 50.6 a nikdy nezahrnuje generated handler nebo lidský účet.

Generation služby sdílejí contracts a databázi, ale nesdílejí in-memory authority. Každý worker i runtime host může být restartován bez ztráty autoritativního jobu nebo callu.

Lokální OWNER Device Bridge je samostatně vydávaný a podepsaný desktopový runtime pro Windows, macOS a Linux. Není serverovou systemd službou ani obecným vzdáleným agentem. Exact build/capability fingerprint, OS/architecture, browser/profile inventory, mTLS certificate generation a connection epoch jsou součástí každého assignmentu; upgrade nebo reconnect nesmí převzít existující session bez current serverového CAS.

### 27.2 Fronty

Fronty jsou persistentní PostgreSQL queue/outbox tabulky s at-least-once delivery. Claim používá přesný materializovaný eligibility predicate a `FOR UPDATE SKIP LOCKED` podle 51.14, DB-time `available_at`, priority, weighted fairness, capacity, dependency/concurrency keys, platform incarnation, lease a fencing. `SKIP LOCKED` se nepoužívá pro activation, cleanup completeness ani jiný globální invariant. Notification je pouze wakeup; polling zabrání lost wakeup.

Kanonické queue kinds zůstávají `DISCUSSION_TURN`, `OPENAI_MODEL_CALL`, generation phase kinds, `AGENT_RUN`, `AGENT_RESUME`, browser kinds, component/monitoring/alert/audit/self-test/repair kinds. Nový queue kind musí deklarovat parent state machine, idempotency, claim fence, checkpoint, cancellation, retry a dead-letter contract.

### 27.3 Queue item contract

Queue item obsahuje ID/kind, parent, logical operation ID, payload/digest, priority/fairness, state, `availableAt`/deadline, attempts, dependencies, concurrency keys, cancellation/checkpoint snapshots, incarnation, lease owner/fence/acquired/heartbeat/expiry, result/error/retry/recovery a timestamps.

Unique `(queueKind,parent,logicalOperationId)` zabrání duplicitnímu enqueue. Stejný payload vrací existing item; jiný digest je conflict. Eligibility fields jsou materializované a mění se atomicky s dependency, barrier a cancellation stavem. Queue item state, business concurrency claim, lease/fence a parent transition jsou měněny pouze guarded transakcí podle 51.14–51.17.

### 27.4 Transactional enqueue a outbox

Každá doménová změna vyžadující práci vytvoří queue/outbox ve stejné transakci. Worker terminal write, parent transition, successor reservation a enqueue pokračování jsou all-or-nothing podle 51.16. Publisher může doručit duplicate; consumer deduplikuje event/item ID.

External dispatch se neprovádí přímo z necommitnuté doménové transakce. Nejprve se uloží side-effect intent a dispatch outbox, poté broker/worker provede call.

### 27.5 Lease a fencing

Claim, heartbeat, expiry, takeover, release a stale-worker guards se řídí 49.6 a fyzickou transakční sémantikou 51.15. Fencing token je monotonic per persistent resource row a vždy vázaný na current platform incarnation a application deployment epoch. Heartbeat po expiry neuspěje. Lease expiry během DB transakce a fresh pre-dispatch authority check mají jediný význam podle 51.15. Každý authoritative write ověřuje current token a state/checkpoint version.

### 27.6 Checkpointed work

Dlouhá práce používá immutable checkpoints podle 49.9. Checkpoint se uloží před možným side effectem spolu s intentem a po potvrzení effectu spolu s outcome. Resume nespoléhá na process memory a validuje current revisions, bindings, activation epoch, cancellation a platform incarnation.

### 27.7 Idempotence a exactly-once outcome

Queue delivery je at-least-once. Business idempotency scope a observable exactly-once outcome jsou v 49.4. Worker/attempt/lease identita scope nemění. External exactly-once se netvrdí bez target idempotency nebo jednoznačné reconciliation.

### 27.8 OpenAI model call recovery

Model worker před submit ověří local idempotency, immutable request descriptor, capability snapshot a submit intent. Create-class SDK retry je vypnutý. Existing provider `response.id` se retrieveuje, polluje nebo resumeuje a eventy/output items se persistují idempotentně.

Call bez provider response ID se znovu submitne pouze při durable `CONFIRMED_NOT_SUBMITTED` před network dispatch. Po `DISPATCH_STARTED` je chybějící handle `MODEL_SUBMIT_OUTCOME_UNKNOWN`; diagnostický provider request ID není lookup handle a nevznikne blind duplicate. Parent transition provádí parent worker pod vlastním fence podle kapitoly 52.

### 27.9 Agent run recovery

Agent checkpointuje po každém model outputu, tool resultu, handoffu, compaction a interruption. Resume používá exact agent graph/revision/session/binding/activation snapshots, SDK/adapter/serializer version a reconciliuje pending side effect před dalším modelovým kolem.

Serialized Agents SDK `RunState` je recovery artefakt, nikoli autorita. Otevře jej pouze exact compatible side-by-side runtime generation; nekompatibilita, chybějící output provenance nebo nejednoznačný nested agent-tool stav je fail-closed před dalším side effectem. Potvrzený tool outcome se při resume neopakuje.

### 27.10 Generation phase ownership

Coordinator jediný mění job lifecycle/phase pointers. Specializované workery zapisují jen vlastní typed outcomes přes kanonickou doménovou službu. Phase result samo bez coordinator transaction nemění job. Ownership je vynucené execution-context contractem a guarded DB operation, nikoli pouze konvencí.

### 27.11 Backpressure

Queue má global/per-kind/per-parent/per-target capacity a weighted fairness. Admission vrací `CAPACITY` s retryAfter a durable stavem. Cancellation, rollback, activation recovery, health a alert mají prioritu, ale nesmějí obejít concurrency/fence a nižší priority nesmějí být neomezeně vyhladověny.

### 27.12 Retry scheduling

Retry vytváří nový attempt pod stejným logical operation/idempotency recordem a vyšší attempt sequence. Backoff/jitter mění pouze `availableAt`. Automatic retry je zakázaný pro contract/schema bez změny inputu, stale binding/approval/checkpoint, unknown non-idempotent outcome, cancelled parent, stagnation a OWNER input required.

### 27.13 Cancellation

Parent cancellation intent/version se atomicky uloží a zrcadlí do admission policy queue. Worker kontroluje intent při claimu a před každým dispatch/checkpoint/terminal transition. Cancel-first versus terminal-first race se řídí 49.10. Queue item je `CANCELLED` až po known outcomes a required cleanup; další pending items parentu se idempotentně invalidují.

### 27.14 Graceful shutdown

Služba přestane claimovat a přijímat nové calls, nastaví readiness false a persistuje drain/shutdown intent. Aktivním attempts propaguje cancellation nebo shutdown, uloží safe checkpoint nebo přejde do reconciliation a current lease uvolní pouze pod current fence.

Runtime host a browser host používají pořadí `stop admission → drain/reconcile → SIGTERM → bounded grace → SIGKILL celé cgroup`. Browser host během drainu nepřesouvá živý BrowserContext mezi hosty/build manifests; každou action zastaví před mutation triggerem nebo označí k reconciliation, uzavře preview/event routes a teprve potom ukončí browser process. Bridge gateway nejprve revokuje admission a assignments, poté uzavře connection epoch; lokální profil lease zůstane blokovaný do známého outcome a cleanupu. Systemd používá `KillMode=control-group`, `SendSIGKILL=yes` a exact `TimeoutStopSec`. Closure vyžaduje cgroup `populated=0`, uzavřené accepted sockets, anonymous channels, streamy, browser event subscriptions a child pidfds.

Kill po deadline, `kill -9`, OOM nebo host crash neztrácí autoritu, protože DB intent/checkpoint/outbox zůstávají. Nový process generation nejprve reconciliuje pending side effects a nepovažuje absenci process memory za `NOT_APPLIED`.

### 27.15 Dead letter a manual review

Po exhausted retry nebo `UNKNOWN` vznikne durable manual-review/dead-letter record s logical operation, attempts, checkpointem, pending effects, evidence, current state version a canonical actions. OWNER může reconcile, potvrdit outcome, retry known-not-applied, rollback nebo uzavřít. Rozhodnutí používá CAS/evidence digest a nemaže historii.

### 27.16 Worker heartbeats

Každý worker a runtime host publikuje:

- worker/service type a instance ID,
- application release, source SHA a application deployment epoch,
- systemd unit a `InvocationID`,
- host boot ID, Linux PID a process start ticks,
- cgroup path a process/thread count,
- runtime instance/generation, component revision/release/runtime digest, binding-set revision a activation epoch podle role,
- effective credential generation/fingerprint bez hodnoty secretu,
- process start a last claim/start/complete,
- active item/call a lease expiry,
- queue depth snapshot,
- success/failure/reject counters,
- last error,
- CPU/memory/swap/FD/tmpfs/event-loop lag,
- current IPC connection a first/last sequence,
- timestamp a nonce.

Heartbeat je observation, nikoli autorita. Gateway a projections přijmou změnu pouze při current platform incarnation, deployment epoch, activation epoch, runtime generation a monotonic sequence. Readiness vyžaduje DB connectivity, required service credentials, schema compatibility, canonical socket/process identity a schopnost obsloužit vlastní operation. OpenAI worker může být `OPENAI_CONFIGURATION_REQUIRED`; ostatní platformní služby zůstávají ready.

## 28. Produkční prostředí a síť

### 28.1 Produkční server

KájovoCML NG běží na produkčním serveru s Ubuntu 24.04 LTS a veřejnou IPv4 adresou:

```text
89.221.222.92
```

Server používá:

- PostgreSQL 16,
- Node.js 24 LTS,
- pnpm 11,
- Nginx,
- systemd,
- pinovaný Playwright runtime s managed Chromium a verification Firefox/WebKit engines,
- WEDOS WAPI pro DNS,
- Let's Encrypt ACME DNS-01,
- UFW s veřejnými porty 80 a 443,
- lokální Unix Domain Sockets pro interní runtime.

### 28.2 Domény

Hlavní web a OWNER API:

```text
https://kaja.hcasc.cz
```

API root:

```text
https://kaja.hcasc.cz/api/v1
```

Component zone:

```text
*.kaja.hcasc.cz
```

Component hostname:

```text
kcmlNNNN.kaja.hcasc.cz
```

DNS records směřují na `89.221.222.92`. Hostnames jsou lowercase v DNS a component code zůstává uppercase v UI a datech.

### 28.3 Kanonické routování

Nginx zajišťuje:

- HTTP → HTTPS redirect,
- HSTS,
- TLS termination,
- hlavní web a `/api/v1`,
- SSE bez bufferingu,
- same-origin WebSocket upgrade pro `KBPP/1.0` browser preview,
- upload/download limity,
- component wildcard routing,
- MCP `/mcp`,
- component control paths,
- veřejné webhooks,
- health/readiness/version,
- security headers,
- trusted proxy handling.

Požadavek na component hostname se mapuje podle hostname na component identity a dále na lokální runtime UDS nebo managed upstream.

### 28.4 Veřejná a interní komunikační cesta

OWNER web/API, externí MCP klienti, webhooks a production acceptance používají kanonická HTTPS URL. Jediná veřejná gateway ověřuje OWNER API klíč nebo endpoint-local credential a mapuje request na component route.

Workery, AI agenti, MCP servery, generované komponenty a platformní služby na produkčním serveru používají in-process volání nebo Unix Domain Socket do stejné runtime gateway. Gateway doplní serverový execution context a exact contract binding, provede shodnou schema, revision, idempotency, lifecycle a audit validaci jako veřejná cesta a zavolá target runtime. Interní cesta nevytváří loopback HTTPS request ani strojový token.

### 28.5 TLS

Certifikát pokrývá:

```text
kaja.hcasc.cz
*.kaja.hcasc.cz
```

Certifikát a privátní klíč se ověřují na shodu. Current cert se používá po dobu validity a správného SAN kontraktu. Renewal timer spouští bounded unattended renewal, DNS challenge, exact cleanup, atomic materialization, `nginx -t`, reload, health a audit.

### 28.6 WEDOS DNS automatizace

WEDOS WAPI credential je v Secret Manageru. DNS operace používá:

- ping a active/primary domain preflight,
- persistentní operation ledger,
- unique correlation a author comment,
- exact TXT row ID,
- domain commit,
- authoritative NS propagation,
- exact cleanup,
- recovery po přerušení.

Parallel ACME challenge hodnoty mají samostatné operations. DNS acceptance je samostatná explicitní infrastruktura operace.

### 28.7 Filesystem

Kanonické cesty:

```text
/opt/kajovocml-ng/releases/<release-id>
/opt/kajovocml-ng/current
/srv/kajovocml-ng/repository
/var/lib/kajovocml-ng/data
/var/lib/kajovocml-ng/generation
/var/lib/kajovocml-ng/components
/var/lib/kajovocml-ng/runtime
/var/lib/kajovocml-ng/runtime/instances/<runtime-instance-id>
/var/lib/kajovocml-ng/browser
/var/lib/kajovocml-ng/browser/hosts
/var/lib/kajovocml-ng/browser/sessions
/var/lib/kajovocml-ng/browser/artifacts
/var/lib/kajovocml-ng/audit
/var/lib/kajovocml-ng/backups
/etc/kajovocml-ng
/etc/kajovocml-ng/tls
/run/kajovocml-ng/gateway/runtime-gateway.sock
/run/kajovocml-ng/brokers/secret-broker.sock
/run/kajovocml-ng/brokers/egress-gateway.sock
/run/kajovocml-ng/brokers/state-broker.sock
/run/kajovocml-ng/runtime-hosts/<runtime-instance-id>/<runtime-generation>
/run/kajovocml-ng/browser-hosts/<slot>/control.sock
```

Privátní repository mirror na produkčním serveru je přístupný deployment identitě a autorizovanému provoznímu účtu. Release je read-only a content-addressed. Mutable data jsou oddělená. `/run/kajovocml-ng` parent directories jsou root-owned a caller groups nemají write právo; socket ownership/modes jsou v 50.7.

Generated handler nevidí host paths uvedené výše. Jeho root je immutable release image/tree a writable paths jsou pouze private bounded tmpfs `/tmp`, `/run` a `/work`. Host directory FD, symlink, hardlink, device, socket a path traversal pravidla jsou v 50.13 a 50.14.

### 28.8 Systemd

Každá trusted služba používá nejméně:

- explicitní non-login `User` a `Group`,
- service-specific `LoadCredentialEncrypted` nebo `LoadCredential`,
- `UMask=0077`,
- `NoNewPrivileges=yes`,
- `PrivateTmp=yes`,
- `ProtectSystem=strict`,
- `ProtectHome=yes`,
- `PrivateDevices=yes`,
- `ProtectProc=invisible` a `ProcSubset=pid`,
- `ProtectClock=yes`, `ProtectHostname=yes`, `ProtectKernelTunables=yes`, `ProtectKernelModules=yes`, `ProtectKernelLogs=yes`,
- `ProtectControlGroups=yes` a bez cgroup delegation,
- `RestrictSUIDSGID=yes`, `LockPersonality=yes`, `RestrictRealtime=yes`,
- prázdný capability bounding/ambient set, pokud exact trusted service nepotřebuje výslovně zdůvodněnou startup capability,
- `SystemCallArchitectures=native` a service-specific syscall/address-family policy,
- read/write path allowlist,
- `KillMode=control-group`, restart/watchdog/timeouts,
- `CPUQuota`, `MemoryHigh`, `MemoryMax`, `MemorySwapMax`, `TasksMax`, FD/file/storage limity,
- journal identifiers, systemd `InvocationID` a platform heartbeat.

`kcml-runtime-gateway` a `kcml-runtime-host` nemají network namespace přístup a používají pouze `AF_UNIX`. Runtime host smí vytvořit child namespaces v trusted launcher fázi; generated code je po setupu omezen seccompem. Secret/state broker nemají externí network. Egress gateway a browser host mají pouze síť potřebnou pro svou exact broker/browser funkci.

Canonical filesystem UDS vytvářejí `.socket` units s `SocketUser`, `SocketGroup`, `SocketMode=0660`, root-owned non-writable parent directory, `Accept=no` a `RemoveOnStop=yes`. Service přijme pouze named activation FD a sama socket path nevytváří, nemaže ani nenahrazuje.

Generated component template používá `kcml-runtime-host@<runtime-instance-id>.service` a child handler sandbox. Browser host template používá oddělenou `kcml-browser-host` identity, private runtime directory, typed control UDS a žádný veřejný Playwright/CDP socket. Přesný unit, socket, peer, credential, restart a cleanup kontrakt je v kapitole 50.

### 28.9 Backups

Deployment vytváří databázový a konfigurační backup před migration. Scheduled backup pokrývá databázi, secrets ciphertext, audit, component state, releases metadata a konfiguraci. Každý backup má digest, retention a restore verification.

### 28.10 Disaster recovery

Restore postup:

1. připraví čistý Ubuntu host,
2. obnoví host master credentials,
3. obnoví PostgreSQL a persistent data,
4. nainstaluje poslední validní release,
5. obnoví TLS nebo vydá nový certifikát,
6. spustí služby,
7. ověří audit integrity, secrets, component runtimy a health,
8. spustí production acceptance.

## 29. Release, deployment a acceptance

### 29.1 Repository

KájovoCML NG používá samostatný privátní GitHub repozitář. Přístup získávají pouze OWNERem schválené GitHub účty a deployment identity. GitHub účty používají MFA, repository oprávnění jsou explicitní a auditovaná a `main` je kanonická chráněná větev.

Developerský počítač používá autorizovaný OS účet, šifrovaný disk, automatický zámek obrazovky, aktualizovaný operační systém a osobní SSH klíč pro GitHub. Lokální repository copy je dostupná pouze tomuto účtu.

GitHub Actions používá exact commit SHA, chráněné production environment, repository-scoped deployment credential a GitHub Secrets. Produkční server udržuje privátní mirror repozitáře a read-only releases. Secrets mohou být v tomto repozitáři a jeho historii přímo přítomné; privátní access model a audit repo operací tvoří jejich přístupovou ochranu.

Každý commit spouští CI. Branch workflow provádí testy v preprodukčním prostředí a produkční deploy vzniká pouze z accepted `main` SHA.

### 29.2 Build

Build používá frozen lockfile a vytváří self-contained signed release bundle s:

- server build,
- admin UI,
- handler SDK,
- schemas a migrations,
- systemd units,
- deploy/acceptance scripts,
- release manifest,
- source commit a build ID,
- file digests,
- dependency lock digest,
- BrowserRuntimeBuildManifesty včetně exact Playwright/browser buildů a jejich runtime assets,
- podepsané OWNER Device Bridge build manifesty pro podporované Windows/macOS/Linux targets,
- signature/provenance.

### 29.3 CI gates

- dependency installation a supply-chain policy,
- lint,
- typecheck,
- unit tests,
- PostgreSQL integration,
- PostgreSQL isolation-level, row-lock order, advisory namespace, CAS, partial-unique/composite-FK a immutable-trigger tests podle kapitoly 51,
- skutečné paralelní transaction, deadlock, serialization, lock-timeout, lease-expiry, queue `SKIP LOCKED`, activation phantom a API-key rotation race tests,
- migration clean/upgrade, expand/migrate/validate/activate/contract a forward-schema application rollback,
- generated handler namespace, filesystem, capability, seccomp, environment a FD sandbox,
- production-shaped systemd runtime, socket activation, ownership/mode/inode a stale/substitution tests,
- SO_PEERCRED, systemd unit/InvocationID, cgroup, PID-reuse a runtime-generation identity tests,
- direct network/loopback/UDS/PostgreSQL/Secret Manager/browser-host bypass negative tests,
- child/orphan/cgroup cleanup, kill -9 a service credential rotation tests,
- activation/deployment stale-process fencing a old-release write rejection,
- runtime IPC framing, payload, descriptor injection, streaming/backpressure/cancellation fuzz tests,
- Playwright/browser engine matrix tests nad exact build manifestem včetně BrowserContext/page/frame/document lifecycle, actionability, popup/OOPIF, service workeru, dialogs, permissions, upload/download a crash recovery,
- OWNER Device Bridge mTLS/certificate-generation, OS capability, profile lease, passkey/client-certificate, reconnect/revoke a stale-assignment tests,
- navigation, redirect, popup, iframe/OOPIF, external-protocol, DNS/IP a background-actor security,
- AI tool loop tests,
- OpenAI runtime adapter contract tests pro foreground stream, background retrieve, refusal, incomplete a tool call round-trip,
- generation lifecycle, phase guards, plans, checkpoints, cancellation a crash takeover,
- workspace patch CAS, path isolation, complete read-back a stagnation,
- MCP strict schemas, discovery snapshots, aliases, idempotency, concurrency, progress a reconciliation,
- AI agent tool bindings, handoffs, guardrails, approval/resume, sessions, memory a eval promotion,
- combined MCP+agent activation set, post-activation smoke a atomic rollback,
- MCP protocol tests,
- auth/CSRF/MFA,
- Secret Manager,
- exact binding a no-component-token tests,
- queues/leases/recovery,
- UI component/accessibility,
- release build guards,
- stateful property-based model tests, deterministic scheduler, stable fault-point catalog, single-fault/cutpoint matrix, pairwise a selected three-way chaos schedules, shrinking, exact-seed replay, oracle mutation tests a recovery/closure gates kapitoly 54,
- CodeQL a dependency security gates,
- secret inventory scanner v informativním režimu kompatibilním s povolenou přítomností secrets v privátním repozitáři.

### 29.4 Ordinary deployment

Ordinary deployment je bounded release operace:

1. ověří signed bundle a expected source SHA,
2. vytvoří backup,
3. aplikuje forward migrations,
4. reconciliuje config a platform identities,
5. načte GitHub Actions secret `PASS` a vždy synchronizuje heslo pevné identity `KRMAR78`,
6. provede provider, OpenAI, Secret Manager a infrastructure preflight jako capability inventory,
7. nainstaluje release,
8. přepne `current`,
9. reload/restart služby,
10. ověří `/health`, `/api/v1/system/version`, služby a heartbeats,
11. ověří singleton `KCML_OWNER_API_KEY`, self-test runtime a kanonický operation catalog,
12. potvrdí několik po sobě jdoucích healthy samples,
13. při selhání aktivuje rollback.

Synchronizace `PASS` probíhá při každém deploy runu včetně změny UI, dokumentace, konfigurace nebo jediné řádky kódu. Krok je idempotentní a jeho úspěch je součástí deployment evidence.

Každý krok emitne `started`, `elapsedMs`, `result`. Závěr obsahuje deset nejpomalejších kroků.

### 29.5 Production acceptance

Production acceptance je samostatně opakovatelný workflow nad přesně nasazeným SHA. Ověří API SHA, release manifest SHA, health, readiness a effective BrowserRuntimeBuildManifest, potom použije jediný `KCML_OWNER_API_KEY` a spustí HTTP/SSE, centrální chat, AI, generation, MCP, browser, UI, monitoring, registered-element a repair scénáře. Browser acceptance zahrne server-managed context i dostupný OWNER Device Bridge profil, page/frame/document identity, takeover/return, autosave mutation, popup/OOPIF, upload/download a crash/reconciliation bez duplicitního effectu. Webové E2E získá OWNER session výměnou API klíče přes produkční API-key-session endpoint. Workflow pracuje bez interaktivního zadání username a hesla a nemění release, migrations, DNS ani TLS.

Production acceptance dále spouští bezpečný production-shaped chaos profil kapitoly 54 nad izolovanými fixture namespaces a provider/browser test účty. Profil nesmí úmyslně provést nevratnou produkční mutaci; musí však prokázat restart služeb, transportní disconnect, bounded capacity, stale-response rejection, recovery barrier, canonical outcome replay, nulový orphan inventory a přesný replay alespoň jednoho uloženého regression schedule.

### 29.6 Infrastructure acceptance

Samostatné workflows ověřují:

- WEDOS DNS add/propagation/delete/cleanup,
- dual alert webhook delivery,
- TLS issuance/renewal,
- backup/restore,
- disaster recovery.

### 29.7 Rollback

Application rollback je fenced deployment saga podle 49.24. Atomický filesystem `current` rename, database deployment epoch, systemd effective releases a process heartbeats se reconciliují jako jeden desired/effective outcome. Gateway není ready pro mutující provoz při mixed release/epoch.

Rollback obnoví previous application release, kompatibilní config a component/automation activation snapshots pouze podle frozen rollback evidence. Forward schema musí být kompatibilní. Po reverse switchi se spustí health, readiness, audit, queue/lease recovery a reconciliation pending side effects. Nejednoznačný effective stav vede do `MANUAL_REVIEW`, nikoli falešného success.

## 30. Bezpečnostní architektura

### 30.1 Důvěryhodný vývojový a provozní celek

KájovoCML NG používá jeden technicky vymahatelný trust perimeter tvořený čtyřmi navazujícími částmi.

**Developerský počítač** používá:

- autorizovaný osobní OS účet,
- šifrovaný systémový disk,
- automatický zámek obrazovky,
- aktualizovaný operační systém,
- osobní SSH klíč pro privátní GitHub,
- lokální repository oprávnění dostupná autorizovanému uživateli,
- auditovatelný Git author identity.

**Privátní GitHub repozitář** používá:

- private visibility,
- explicitní seznam schválených účtů,
- MFA GitHub účtů,
- branch protection pro `main`,
- exact-SHA workflow,
- chráněné production environment,
- GitHub Secrets včetně `PASS`,
- GitHub audit a historii změn.

**Deployment** používá:

- GitHub Actions nad accepted `main` SHA,
- repository-scoped deployment identity,
- ověřený release manifest a digests,
- SSH key-based přístup na produkční server,
- idempotentní synchronizaci `PASS`,
- auditované kroky, backup, health a rollback.

**Produkční server** používá:

- Ubuntu 24.04 LTS,
- SSH key-only administrativní přístup,
- UFW a veřejné porty 80/443,
- Nginx TLS hranici,
- dedikované systemd identity,
- privátní repository mirror,
- read-only releases,
- oddělené writable datové cesty,
- souborová oprávnění, backups, monitoring a audit.

Secrets, credentials a jejich plné hodnoty jsou uvnitř tohoto perimetru standardními daty. Mohou procházet developerským počítačem, zdrojovým kódem, GitHubem, GitHub Actions, deploymentem, repository mirrorem, produkčním kódem, logy, chatu, API a runtime bez změny pracovního procesu. Přístupová ochrana perimetru zabezpečuje jejich použití jako celku.

Lidská aplikační hranice je session jediné pevné OWNER identity nebo jediný OWNER API klíč. Oba způsoby poskytují plný obsah a plnou správu bez role/scope kontroly. Interní component, agent, worker a runtime komunikace používá serverové execution contexts a exact bindings bez vlastních bearer credentialů.

### 30.2 Web security

- secure `__Host-` cookies,
- CSRF pro mutace,
- CSP a security headers,
- HSTS,
- trusted proxy CIDRs,
- input schema validation,
- upload size/MIME/path kontrola,
- rate limiting,
- login throttle,
- session rotation a revocation,
- MFA a reauthentication pro security operace pevné OWNER identity.

### 30.3 Runtime security

Generated handler má pouze serverem vytvořený capability channel k trusted runtime hostu. Nemá vlastní bearer credential, OWNER API key, síť, host `/run`, PostgreSQL credential/socket, Secret Manager storage, broker socket, browser-host endpoint ani systemd credential directory.

Runtime host drží process supervision a canonical capability API; runtime gateway drží execution-context derivation, exact binding, revision/release/runtime digest a epoch admission. Secret, egress a state operace provádějí samostatné brokers. UID/GID, systemd unit/`InvocationID`, cgroup, PID/start/boot identity, runtime generation, socket ownership, namespaces, capabilities, seccomp, environment, FD allowlist a cgroups vynucují Linux boundary podle kapitoly 50.

Žádný runtime token, service token, component token, JWT, internal OAuth nebo permission lookup se neimplementuje. Identita a autorita jsou odvozeny z kernel/systemd procesu a current serverového state snapshotu.

### 30.4 Network security

Egress přijme pouze deklarovaný target binding, port, path a method. DNS se resolves a IP policy se kontroluje při každém navázání a po redirectu. Server-managed browser a egress blokují interní metadata a adresy mimo active binding. OWNER Device Bridge může dosáhnout pouze explicitního session-scoped local target bindingu s exact host/origin/IP policy; bridge certifikát ani session assignment není obecný LAN přístup.

### 30.5 Supply chain

- frozen lockfile,
- dependency audit,
- provenance a digests,
- signed release,
- exact source commit,
- CodeQL,
- secret inventory report,
- immutable release,
- runtime manifest validation.

Secret inventory eviduje výskyt hodnot a jejich umístění pro OWNERa. Přítomnost secretu uvnitř privátního trust perimeteru je platný stav a zachovává průchod CI, buildem i deploymentem.

### 30.6 Data security

- PostgreSQL role isolation,
- encrypted secrets,
- encrypted session state,
- append-only audit,
- authenticated backups,
- strict file modes,
- host credentials přes systemd,
- integrity digests.

### 30.7 OWNER transparent mode

OWNER UI může zobrazit všechny hodnoty, secrets, logs a payloady. Stejný obsah je dostupný jedinému OWNER API klíči. Copy, export, reveal a strojové čtení jsou auditované. OWNER může zapnout dočasný presentation mode, který vizuálně skryje hodnoty pouze na daném klientovi bez změny uložených dat, API, logů nebo runtime.

### 30.8 Závazný secret režim pro AI a automatizované nástroje

AI agenti, centrální chat, generation worker, test runner, CI, CodeQL doplňky, secret inventory, browser tools a deployment skripty považují výskyt secretu uvnitř trust perimeteru za očekávaný. Pracují s plnou hodnotou a zachovávají OWNERovi její dostupnost.

Bezpečnostní kontrakt tvoří přístupová ochrana perimetru, autentizace pevného OWNERa nebo jediný OWNER API klíč, serverové execution contexts, exact bindings, endpoint-local credentials, TLS, SSH, firewall, systemd identity, audit a backups. Tento jednotný model je úplným bezpečnostním rámcem pro development, GitHub, deployment i produkci.

### 30.9 Browser Interaction Plane security

Browser preview a control používají pouze KájovoCML API/WSS hranici. Raw Playwright websocket, CDP port, browser process pipe, browser-host control UDS, bridge native channel a local profile path nejsou dostupné OWNER webu, generated handleru ani externímu client code. OWNERovi zůstává plná funkční browserová kontrola prostřednictvím typed session operations, live preview, Device Bridge a transparentních debug/evidence surfaces.

Browser worker komunikuje s browser hostem přes systemd-owned typed UDS a ověřenou Linux/systemd peer identity. Browser host drží Playwright/private protocol client a browser process uvnitř vlastní service boundary a spouští managed browser přes anonymous pipe nebo exact private adapter bez veřejného portu. Preview Gateway není členem browser-host socket group a dostává pouze KBPP frames, observations, artifacts a typed events.

Každý host command ověřuje:

- current platform/application incarnation,
- worker peer identity a service generation,
- runtime build manifest a host generation,
- exact session/context generation,
- page/page generation, frame attachment a document epoch,
- current control epoch/action fence,
- action schema, operation scope, target digest a size/deadline,
- pre-dispatch side-effect intent reference.

Browser host nepřijme raw protocol method nebo caller-defined runtime handle. Stale nebo cross-session command se odmítne před inputem. Pozdní response se může uložit jako raw evidence, ale nemůže změnit action, state bundle, session projection nebo terminal outcome.

Každý preview handshake ověřuje OWNER session/API-key channel, Origin, session binding, ticket audience/capabilities, expiry a one-time use. Každý input ověřuje stream/control/page/frame/document/viewport identity, sequence, schema, rate a size. Pointer/keyboard flood je bounded a nesmí vyhladovět cancellation, challenge, cleanup nebo OWNER takeover. Reconnect nereplayuje neacknowledged input.

Browser host nemá přístup k Secret Manager databázi. Secret hodnotu získá browser worker přes exact active Secret binding a předá ji pouze konkrétnímu action callu nebo context-creation profile. Browser context, screenshot, trace, DOM snapshot, download, state bundle, WebAuthn credential, client-certificate metadata a bridge evidence dědí sensitivity, encryption, retention a OWNER-transparent policy session.

State bundle je secret-grade, content-addressed a immutable. Standardní serializer zahrnuje pouze deklarované cookies/storage/permission/auth members; neexportuje platform passkey, hardware private key, OS keychain, raw browser profile, service-worker executable state ani browser cache. Capture/restore ověřuje account/tenant, auth epoch, credential/certificate version, origin inventory a runtime build compatibility.

Navigation, redirect, popup, iframe, download, external protocol a bridge local target se znovu validují při každém přechodu. DNS/IP policy se uplatní po resolution i redirectu. Credential, client certificate nebo sensitive upload se nepřenese na nový origin bez exact browser/account/scope contractu.

OWNER Device Bridge má vlastní bridge ID a mTLS certificate binding přijímaný pouze na exact bridge transportu. Spojení je outbound, certificate-generation-, version-, capability-, connection-epoch- a session-scoped. Bridge nepřijímá shell, arbitrary command, arbitrary filesystem path, obecný TCP/UDP/LAN proxy, raw CDP command ani obecný Playwright tunnel.

Bridge private key zůstává v lokálním OS secure storage nebo explicitním protected bridge store a server jej nikdy neobdrží. Platform passkey/client-certificate private material zůstává na zařízení. Bridge vrací pouze typed operation outcome, exact browser/profile evidence a bounded artifact handles.

Jeden local browser profile má nejvýše jeden active writer lease. Bridge nesmí attachnout profil již ovládaný jiným process/session bez explicitního OWNER takeoveru a potvrzené profile/process identity. Version/capability mismatch, revocation, certificate rotation nebo heartbeat expiry zablokuje nový dispatch a inkrementuje connection epoch; pending possible mutation se reconciliuje.

WebAuthn virtual authenticator je povolen pouze explicitní automation/test account revision a jeho credential state je secret-grade. Reálný platform authenticator nelze nahradit virtuálním jen kvůli automatizaci. CAPTCHA, FedCM/native picker, biometric/PIN a device confirmation používají OWNER/bridge challenge.

Nativní dialog, permission request, file chooser a external protocol jsou typed events. Mutující workflow nesmí používat skrytý default accept/dismiss. Response je vázaná na exact page/frame/document/action/control epoch a její side-effect semantics podléhá stejnému intent/postcondition/reconciliation contractu.

Přesný Linux/IPC a bridge boundary kontrakt je v 50.26 až 50.28. Browser security gate je PASS pouze při současném důkazu funkční OWNER kontroly a nulového raw-transport, cross-session, stale-generation, secret/profile nebo bridge-scope bypassu.

### 30.10 Agentic AI security a confused-deputy boundary

Agentic security chrání význam již povolené autority, nikoli její rozsah. OWNER_FULL, singleton API key, trusted perimeter, přímá práce se secrets, exact bindings a plná AI/browser/tool capability zůstávají beze změny.

Každý AI-originating side effect používá immutable authority lineage, OWNER intent, operation context, semantic action plan, content provenance, argument-origin mapu a případný Secret-use context podle kapitoly 53. Externí obsah se může plně číst, zpracovat, shrnout, transformovat a použít jako data. Nemůže však sám vytvořit serverovou identity, operation, target, binding, tool availability, side-effect class, delegation nebo Secret recipient.

Semantic mismatch se vyhodnotí před dispatch a vytváří typed evidence. Nezakládá nový approval, role, permission ani povinné potvrzení; legitimní command, který se vejde do current OWNER intentu, pokračuje bez další lidské překážky.

## 31. Výkon, limity a provozní kvalita

### 31.1 API cíle

- běžný read API p95 do 500 ms,
- core health a core readiness jsou `PASS` i při stavu `OPENAI_CONFIGURATION_REQUIRED`,
- běžná UI mutace p95 do 1 s bez čekání na async dokončení,
- SSE událost do 500 ms od persistence,
- health endpoint do 200 ms,
- readiness do 2 s,
- dashboard first useful render do 2 s při standardním datasetu.

### 31.2 Stránkování

Produkční REST list endpointy mají default 50 a maximum 200, pokud jejich contract neurčí nižší limit. Logy/audit používají cursor a časové partition. UI používá virtualizaci pro velké tabulky. MCP list pagination je samostatný protocol contract 10.7: používá opaque cursor a page size určenou MCP serverem; klient nesmí předpokládat REST default ani maximum.

### 31.3 Payload limity

Každý tool a endpoint deklaruje request/response limit. Soubory používají streaming. Log/audit detail se načítá až po otevření. SSE posílá bounded event payload a odkazy na velké artefakty.

### 31.4 Queue cíle

UI zobrazuje queue depth, oldest age, throughput, leases, failures a capacity. Backpressure je explicitní a nedochází k nekontrolovanému procesu nebo browser context růstu.

### 31.5 Retention

OWNER spravuje retention pro:

- debug logy,
- audit archive,
- messages a AI runs,
- browser artifacts,
- downloads/uploads,
- monitoring probes,
- releases,
- backups,
- secrets versions.

Auditní integrita a required compliance evidence respektují minimální retention kontrakt.

### 31.6 Generation a agentní provozní cíle

- persist accepted OWNER message: p95 do 500 ms bez provider času,
- persist-to-SSE event publication: p95 do 250 ms,
- phase transition a enqueue: p95 do 500 ms,
- cancellation intent visibility: do 1 s,
- worker lease heartbeat freshness: nejvýše jedna třetina lease duration,
- checkpoint write po tool side effectu: před dalším modelovým kolem,
- MCP `tools/list` interního serveru: p95 do 500 ms při cache miss,
- MCP gateway overhead bez handleru: p95 do 100 ms,
- agent run preparation bez provider callu: p95 do 1 s,
- activation pointer transaction: p95 do 2 s bez runtime postflight.

Provider latency není maskována jako aplikační SLA. UI odděleně zobrazuje queue wait, KájovoCML processing, OpenAI latency, tool latency a activation/postflight čas.

### 31.7 Browser Interaction Plane cíle

- session create bez browser launch queue: p95 do 500 ms,
- ready non-persistent context na existing compatible hostu: p95 do 2 s,
- first useful preview frame po `READY`: p95 do 2 s,
- persisted action state → preview event: p95 do 250 ms,
- OWNER control acquire po safe checkpointu bez pending mutation: p95 do 500 ms,
- accepted pointer/key event → host dispatch bez WAN času klienta: p95 do 150 ms,
- semantic observation pro běžnou stránku: p95 do 1 s,
- stale control/page/frame/document/viewport input reject: před browser dispatch,
- preview reconnect snapshot a acknowledgment barrier: p95 do 1 s,
- host/bridge command identity validation: před actionability nebo input,
- account authenticated-condition verification bez provider challenge: p95 do 2 s po page readiness,
- browser host heartbeat freshness: nejvýše jedna třetina host lease,
- bridge heartbeat freshness: nejvýše jedna třetina connection lease,
- cleanup terminal session: p95 do 10 s bez retained artifact uploadu nebo pending external reconciliation.

Frame cadence, image quality, capture region, host count, context count, process recycling, observation budget, action timeout a idle timeout jsou system-managed. UI zobrazuje client network latency, preview capture/encode/transport, action queue, host validation, locator/actionability, input dispatch, page wait, postcondition a reconciliation odděleně.

Výkon nesmí být dosažen reuse stale ElementHandle, sdílením BrowserContext state, vynecháním fresh page/frame/document observation, pozdním side-effect intentem, potlačením challenge, vypnutím reconciliation ani předčasným context closem. Backpressure zahazuje pouze nahraditelné preview frames; control, cancellation, challenge, action phase, postcondition a cleanup events se nezahazují.

Capacity model sleduje per engine/build/host memory, CPU, renderer count, pages, contexts, downloads, trace load, bridge profiles a preview viewers. Admission je bounded a vrací typed capacity state. Host recycle/drain nevynutí migraci živého contextu mezi build manifests; session se dokončí, pause/recover nebo obnoví z compatible state bundle.

Reconciliation nemá falešné latency SLA, která by měnila `UNKNOWN` na `FAILED`. UI zobrazuje age a deadline current reconciliation; alert vzniká podle operation contractu a account/resource key zůstává blokovaný do known outcome nebo OWNER resolution.

## 32. Chybové stavy a obnova

### 32.1 Klasifikace

Každá chyba je klasifikována na validation, authentication, binding, dependency, timeout, capacity, protocol, runtime, security, cancellation, conflict nebo unknown-side-effect.

### 32.2 UI chyba

OWNER vidí srozumitelný popis, stable code, correlation ID, affected object, doporučenou akci, retry/repair možnost a rozbalitelný plný debug detail.

### 32.3 Retry

Retry je automatický pouze podle explicitní retry class. Idempotentní operace používají bounded exponential backoff a jitter. Non-idempotentní operace používají reconciliation.

### 32.4 Rollback a cleanup

Každý candidate, run, upload, browser context, credential handle a temporary scope má persistentního ownera a idempotentní checkpointovanou cleanup operation. Cleanup failure vytváří alarm/retry a blokuje closure nebo nový conflicting candidate podle contractu. Rollback external effectu je explicitní compensation s vlastním intent/outcome, nikoli vymazání historie.

### 32.5 Manual review

`MANUAL_REVIEW` obsahuje exact logical/side-effect operation, current state version, observed evidence digest, possible outcomes, current runtime/external state a canonical OWNER actions. OWNER rozhodnutí používá compare-and-swap a audit; dvě odlišná rozhodnutí nemohou obě commitnout. Dokud outcome není známý, automatický retry a conflicting operation jsou blokované.

### 32.6 Generation, KCIP, MCP, agent a korektnostní error codes

Každý stable code má právě jednu classification, side-effect point, retry directive, required current snapshot, affected object, logical operation/correlation a canonical OWNER action. UI, API, chat, MCP `error.data`/result `_meta`, KCIP response, log a audit používají stejný catalog. Text message se může lokalizovat; code, classification a recovery se nesmí měnit.

Retry directive je právě jedna z:

```text
DO_NOT_RETRY
RETRY_SAME_OPERATION
REFRESH_AND_RETRY_NEW_COMMAND
RECONCILE_THEN_RETRY
MANUAL_REVIEW
```

#### KCIP wire

```text
KCIP_MALFORMED_ENVELOPE
KCIP_UNSUPPORTED_PROTOCOL
KCIP_RESERVED_FIELD_PRESENT
KCIP_MESSAGE_ID_CONFLICT
KCIP_CAUSATION_INVALID
KCIP_TARGET_NOT_FOUND
KCIP_TARGET_STALE
KCIP_CONTRACT_DIGEST_STALE
KCIP_BINDING_NOT_ACTIVE
KCIP_BINDING_STALE
KCIP_DEADLINE_EXCEEDED
KCIP_IDEMPOTENCY_CONFLICT
KCIP_STATE_VERSION_CONFLICT
KCIP_SEQUENCE_STALE
KCIP_SEQUENCE_GAP
KCIP_CAPACITY_EXHAUSTED
KCIP_CANCELLED
KCIP_OUTCOME_UNKNOWN
KCIP_INTERNAL_FAILURE
```

#### MCP transport, JSON-RPC a interoperability

```text
MCP_PARSE_ERROR
MCP_INVALID_REQUEST
MCP_DUPLICATE_JSON_MEMBER
MCP_DUPLICATE_IN_FLIGHT_REQUEST_ID
MCP_HEADER_MISMATCH
MCP_PROTOCOL_VERSION_UNSUPPORTED
MCP_CLIENT_CAPABILITY_MISSING
MCP_METHOD_NOT_FOUND
MCP_INVALID_PARAMS
MCP_TOOL_NOT_FOUND
MCP_DISCOVERY_STALE
MCP_CONTRACT_BREAKING
MCP_SCHEMA_DIALECT_UNSUPPORTED
MCP_SCHEMA_REFERENCE_UNRESOLVED
MCP_SCHEMA_DRIFT
MCP_RESULT_TYPE_UNKNOWN
MCP_RESULT_SCHEMA_INVALID
MCP_REQUEST_STATE_INVALID
MCP_REQUEST_STATE_EXPIRED
MCP_REQUEST_STATE_CONSUMED
MCP_CURSOR_INVALID
MCP_SUBSCRIPTION_PROTOCOL_VIOLATION
MCP_STATE_HANDLE_INVALID
MCP_STATE_HANDLE_EXPIRED
MCP_STATE_HANDLE_CLOSED
MCP_TASK_NOT_FOUND
MCP_TASK_EXPIRED
MCP_TASK_CAPABILITY_REQUIRED
MCP_BINDING_STALE
MCP_CONTRACT_DIGEST_STALE
MCP_DEADLINE_EXCEEDED
MCP_CANCELLED
MCP_OUTCOME_UNKNOWN
MCP_ERA_INDETERMINATE
MCP_PROTOCOL_INVALID
MCP_INTERNAL_FAILURE
```

`MCP_HEADER_MISMATCH` se mapuje výhradně na `-32020`; `MCP_CLIENT_CAPABILITY_MISSING` a `MCP_TASK_CAPABILITY_REQUIRED` na `-32021`; `MCP_PROTOCOL_VERSION_UNSUPPORTED` na `-32022`. Parse/request/method/params/internal chyby používají standardní JSON-RPC codes. KájovoCML nesmí alokovat vlastní nedefinovaný code v MCP-reserved rozsahu `-32020` až `-32099`; vlastní stable code je v `error.data.stableCode` nebo result `_meta`.

#### OpenAI, generation a workspace

```text
OPENAI_CONFIGURATION_REQUIRED
OPENAI_MODEL_CAPABILITY_UNSUPPORTED
OPENAI_PROVIDER_TRANSIENT
OPENAI_RESPONSE_EXPIRED
OPENAI_STREAM_PROTOCOL_CONFLICT
OPENAI_PROVIDER_STATUS_UNKNOWN
OPENAI_OUTPUT_ITEM_UNSUPPORTED
OPENAI_TOOL_CALL_ID_CONFLICT
OPENAI_TOOL_EFFECT_OUTCOME_UNKNOWN
OPENAI_CONTINUATION_CONFLICT
OPENAI_BACKGROUND_CANCEL_UNCONFIRMED
MODEL_OUTPUT_SCHEMA_INVALID
MODEL_REFUSAL
MODEL_INCOMPLETE
MODEL_SUBMIT_OUTCOME_UNKNOWN
UNKNOWN_MODEL_TOOL
TOOL_ARGUMENT_SCHEMA_INVALID
CAPABILITY_SNAPSHOT_STALE
SPECIFICATION_NOT_APPROVABLE
SPECIFICATION_DIGEST_STALE
GENERATION_PLAN_INVALID
WORKSPACE_BASE_STALE
WORKSPACE_PATH_INVALID
VALIDATION_STAGNATION
```

#### Obecná korektnost, idempotence a activation

```text
IDEMPOTENCY_CONFLICT
STATE_VERSION_CONFLICT
LEASE_EXPIRED
FENCING_TOKEN_STALE
PLATFORM_INCARNATION_STALE
CHECKPOINT_STALE
CHECKPOINT_DIGEST_INVALID
REVISION_STALE
BINDING_REVISION_STALE
ACTIVATION_EPOCH_STALE
ACTIVATION_IN_PROGRESS
ACTIVATION_EFFECTIVE_UNKNOWN
SEQUENCE_GAP
QUEUE_ITEM_STALE
CONCURRENCY_KEY_HELD
SIDE_EFFECT_OUTCOME_UNKNOWN
SIDE_EFFECT_RECONCILIATION_UNKNOWN
SIDE_EFFECT_RECONCILIATION_FAILED
TERMINAL_STATE_IMMUTABLE
CANCELLATION_ALREADY_TERMINAL
MANUAL_OUTCOME_CONFLICT
CLEANUP_INCOMPLETE
ACTIVATION_SET_NOT_READY
ACTIVATION_SET_ROLLED_BACK
GENERATION_BLOCKED
```

#### Agent

```text
AGENT_TOOL_BINDING_INVALID
AGENT_GRAPH_INVALID
AGENT_APPROVAL_REQUIRED
AGENT_APPROVAL_STALE
AGENT_CHECKPOINT_INCOMPATIBLE
AGENT_RUN_STATE_UNRESUMABLE
AGENT_SDK_RUNTIME_UNAVAILABLE
AGENT_SESSION_STRATEGY_CONFLICT
AGENT_MAX_TURNS_EXCEEDED
AGENT_EVAL_FAILED
```

#### Agentic authority, provenance a confused deputy

```text
AGENTIC_AUTHORITY_LINEAGE_INVALID
AGENTIC_OWNER_INTENT_MISSING
AGENTIC_OWNER_INTENT_STALE
AGENTIC_OPERATION_CONTEXT_INVALID
AGENTIC_OPERATION_CONTEXT_STALE
AGENTIC_CONTENT_AUTHORITY_ESCALATION
AGENTIC_CONTENT_ROLE_CONFLICT
AGENTIC_PROVENANCE_INCOMPLETE
AGENTIC_DERIVATION_CONTRACT_INVALID
AGENTIC_ARGUMENT_ORIGIN_INVALID
AGENTIC_DYNAMIC_TARGET_UNBOUND
AGENTIC_TARGET_SUBSTITUTION
AGENTIC_TOOL_SUBSTITUTION
AGENTIC_DELEGATION_EXPANSION
AGENTIC_SECRET_USE_CONTEXT_MISMATCH
AGENTIC_RESULT_REENTRY_BLOCKED
AGENTIC_COMPACTION_AUTHORITY_DRIFT
AGENTIC_INSTRUCTION_DATA_COLLISION
```

Tyto chyby vznikají před novým side effectem. Recovery používá refresh current lineage/contextu, deterministic recompile ze stejného OWNER intentu nebo nový OWNER command pouze tehdy, když se skutečně mění cíl. Žádný code se nerecoveruje vytvořením širšího scope, nového approval nebo použitím textu z untrusted contentu jako authority.

#### Trusted runtime

```text
RUNTIME_CONTEXT_SPOOF_ATTEMPT
RUNTIME_CONTEXT_NOT_CURRENT
RUNTIME_PEER_IDENTITY_REJECTED
RUNTIME_PID_REUSE_DETECTED
RUNTIME_SERVICE_GENERATION_STALE
RUNTIME_PROCESS_STALE
RUNTIME_LAUNCH_MANIFEST_MISMATCH
RUNTIME_RELEASE_STALE
RUNTIME_DIGEST_MISMATCH
RUNTIME_BINDING_STALE
RUNTIME_ACTIVATION_STALE
RUNTIME_SOCKET_INTEGRITY_FAILED
RUNTIME_SOCKET_STALE
RUNTIME_CHANNEL_CLOSED
RUNTIME_PROTOCOL_VIOLATION
RUNTIME_DESCRIPTOR_INJECTION
RUNTIME_PAYLOAD_TOO_LARGE
RUNTIME_STREAM_BACKPRESSURE
RUNTIME_DEADLINE_EXCEEDED
RUNTIME_CANCELLED
RUNTIME_SANDBOX_SETUP_FAILED
RUNTIME_SECCOMP_VIOLATION
RUNTIME_FILESYSTEM_BOUNDARY_VIOLATION
RUNTIME_NETWORK_BOUNDARY_VIOLATION
RUNTIME_STATE_BOUNDARY_VIOLATION
RUNTIME_SECRET_BINDING_REJECTED
RUNTIME_EGRESS_BINDING_REJECTED
RUNTIME_RESOURCE_EXHAUSTED
RUNTIME_CHILD_SPAWN_REJECTED
RUNTIME_CHILD_CLEANUP_INCOMPLETE
RUNTIME_CREDENTIAL_GENERATION_STALE
RUNTIME_OUTCOME_UNKNOWN
```

#### Browser a bridge

```text
BROWSER_RUNTIME_BUILD_INCOMPATIBLE
BROWSER_HOST_IDENTITY_REJECTED
BROWSER_HOST_GENERATION_STALE
BROWSER_CONTEXT_GENERATION_STALE
BROWSER_RAW_TRANSPORT_EXPOSURE
BROWSER_SESSION_NOT_READY
BROWSER_SESSION_EXPIRED
BROWSER_SESSION_TERMINAL
BROWSER_CONTROL_STALE
BROWSER_CONTROL_HELD
BROWSER_CONTROL_TRANSFER_PENDING
BROWSER_INPUT_STATE_UNRESOLVED
BROWSER_PAGE_STALE
BROWSER_PAGE_CLOSED
BROWSER_PAGE_AMBIGUOUS
BROWSER_FRAME_STALE
BROWSER_FRAME_DETACHED
BROWSER_DOCUMENT_STALE
BROWSER_NAVIGATION_STALE
BROWSER_VIEWPORT_STALE
BROWSER_TARGET_MISSING
BROWSER_TARGET_AMBIGUOUS
BROWSER_TARGET_DRIFT
BROWSER_TARGET_INACCESSIBLE
BROWSER_ACTIONABILITY_FAILED
BROWSER_OVERLAY_BLOCKED
BROWSER_INPUT_STRATEGY_UNSUPPORTED
BROWSER_MUTATION_TRIGGER_UNRESOLVED
BROWSER_OPERATION_OUT_OF_SCOPE
BROWSER_CONFIRMATION_STALE
BROWSER_ACCOUNT_MISMATCH
BROWSER_ACCOUNT_CONCURRENCY_HELD
BROWSER_AUTH_STATE_INVALID
BROWSER_AUTH_EPOCH_STALE
BROWSER_REAUTHENTICATION_REQUIRED
BROWSER_CHALLENGE_REQUIRED
BROWSER_CHALLENGE_STALE
BROWSER_DIALOG_UNHANDLED
BROWSER_PERMISSION_REQUIRED
BROWSER_WEBAUTHN_CAPABILITY_MISSING
BROWSER_CLIENT_CERTIFICATE_UNAVAILABLE
BROWSER_STATE_BUNDLE_INCOMPATIBLE
BROWSER_STATE_BUNDLE_STALE
BROWSER_POPUP_NOT_OBSERVED
BROWSER_DOWNLOAD_INCOMPLETE
BROWSER_UPLOAD_HANDLE_INVALID
BROWSER_BRIDGE_OFFLINE
BROWSER_BRIDGE_VERSION_MISMATCH
BROWSER_BRIDGE_CAPABILITY_MISMATCH
BROWSER_BRIDGE_CERTIFICATE_STALE
BROWSER_BRIDGE_CONNECTION_STALE
BRIDGE_SESSION_SCOPE_REJECTED
BROWSER_PROFILE_LEASE_HELD
BROWSER_ENGINE_UNSUPPORTED
BROWSER_HOST_CAPACITY
BROWSER_HOST_DISCONNECTED
BROWSER_RENDERER_CRASHED
BROWSER_POSTCONDITION_FAILED
BROWSER_RECONCILIATION_REQUIRED
BROWSER_RECOVERY_UNKNOWN
BROWSER_ARTIFACT_INVALID
BROWSER_CLEANUP_INCOMPLETE
```

`BROWSER_RUNTIME_BUILD_INCOMPATIBLE`, stale host/context/page/frame/document/control/viewport/target/auth/bridge identity a scope chyby vznikají před browser dispatch a používají fresh observation nebo nový command; tentýž stale attempt se nereplayuje. `BROWSER_INPUT_STATE_UNRESOLVED` po control transferu vyžaduje neutralizaci pressed keys/buttons/touches a fresh observation před další mutací.

`BROWSER_RECONCILIATION_REQUIRED` dovoluje pouze exact revision-defined read-back pod stejnou logical operation; nesmí znovu odeslat původní browser input. `BROWSER_RECOVERY_UNKNOWN` vždy používá `MANUAL_REVIEW`, blokuje account/resource concurrency key a nikdy `SAFE_RETRY`. Bridge disconnect před potvrzeným mutation triggerem lze podle evidence obnovit ve stejné operation; po možném triggeru se nejprve reconciliuje.

`BROWSER_CHALLENGE_REQUIRED`, `BROWSER_PERMISSION_REQUIRED`, `BROWSER_DIALOG_UNHANDLED`, `BROWSER_REAUTHENTICATION_REQUIRED` a device capability chyby jsou typed wait/blocking outcomes, nikoli důkaz selhání předchozí mutace. Stale revision/binding/epoch/state chybu neopakuje stejný worker attempt. Error catalog je immutable versioned data a změna meaning/recovery existujícího code je breaking contract change.

### 32.7 Platformní recovery, kapacita, transport a artifact failure codes

Následující codes mají jediný stabilní význam a používají stejný recovery catalog v API, KCIP, MCP metadata, UI, chatu, auditě a self-test evidence:

```text
PLATFORM_RECOVERY_IN_PROGRESS
PLATFORM_RECOVERY_BLOCKED
QUEUE_CAPACITY_EXHAUSTED
STORAGE_CAPACITY_EXHAUSTED
FILESYSTEM_OPERATION_FAILED
DNS_RESOLUTION_FAILED
TLS_HANDSHAKE_FAILED
PROVIDER_RATE_LIMITED
CREDENTIAL_EXPIRED
CREDENTIAL_ROTATED
UPLOAD_INCOMPLETE
DOWNLOAD_INCOMPLETE
ROLLBACK_INCOMPLETE
RECOVERY_ORACLE_CONFLICT
TERMINAL_CLOSURE_INCOMPLETE
```

- `PLATFORM_RECOVERY_IN_PROGRESS` vzniká před novým mutujícím dispatch, pokud current database start identity, platform incarnation, application deployment epoch nebo recovery epoch ještě nemá stav `READY`; recovery directive je bounded `RETRY_SAME_OPERATION` pouze pro dosud nedispatchnutý durable command.
- `PLATFORM_RECOVERY_BLOCKED` znamená, že startup reconciliation nalezla nejednoznačný, constraint-invalidní nebo nezreconcilovatelný stav; mutující admission je blokované a recovery je `MANUAL_REVIEW`.
- `QUEUE_CAPACITY_EXHAUSTED` a `STORAGE_CAPACITY_EXHAUSTED` nikdy nesmí doprovázet `ACCEPTED`, pokud durable command, idempotency record a queue/outbox intent nebyly commitnuté. Již přijatá práce se kvůli saturation nezahazuje.
- `DNS_RESOLUTION_FAILED` a `TLS_HANDSHAKE_FAILED` před odesláním application request bytes znamenají `CONFIRMED_NOT_APPLIED`; po zahájení application write se chyba klasifikuje podle side-effect ledgeru a může vyžadovat reconciliation.
- `PROVIDER_RATE_LIMITED`, HTTP `401`, `403`, `429` nebo `5xx` dokazují `CONFIRMED_NOT_APPLIED` pouze tehdy, když exact external target contract výslovně garantuje, že daný status vzniká před side effectem. Jinak se mutující operation reconciliuje.
- `CREDENTIAL_EXPIRED` nebo `CREDENTIAL_ROTATED` nepovolují změnu credential version uvnitř jednoho dispatch attemptu. Nový attempt smí resolve current verzi pouze po důkazu `CONFIRMED_NOT_APPLIED`.
- `UPLOAD_INCOMPLETE` a `DOWNLOAD_INCOMPLETE` nikdy nejsou success. Částečný remote upload může být `UNKNOWN`; částečný download zůstává neautoritativní temporary artifact.
- `ROLLBACK_INCOMPLETE` zachovává frozen previous/candidate snapshots, blokuje nový conflicting activation a pokračuje recovery nebo `MANUAL_REVIEW`; neprohlašuje previous ani candidate za active bez pointer/runtime evidence.
- `RECOVERY_ORACLE_CONFLICT` znamená, že persisted evidence připouští více než jeden automatický recovery action. Systém nic nového nedispatchne a přejde do `MANUAL_REVIEW`.
- `TERMINAL_CLOSURE_INCOMPLETE` blokuje `COMPLETED`, `SUCCEEDED`, `ACTIVE`, release promotion a closure report, dokud terminal closure predicate neplatí.

### 32.8 SSOT, Contract Pack, traceability a architecture-readiness error codes

Následující stable codes patří výhradně compile, build, validation, conformance, activation-preflight a architecture-handoff hranici. Vznikají před novým produkčním side effectem, používají `DO_NOT_RETRY` a nelze je odstranit opakováním stejného běhu nad stejnými vstupy:

```text
SSOT_NORMATIVE_CONFLICT
CONTRACT_PACK_DRIFT
CONTRACT_PACK_REFERENCE_INVALID
REQUIREMENT_TRACEABILITY_INCOMPLETE
ORPHAN_REQUIREMENT
ORPHAN_IMPLEMENTATION_ARTIFACT
OPERATION_CONTRACT_INCOMPLETE
STATE_MACHINE_CONTRACT_INCOMPLETE
POSTGRES_CONTRACT_INCOMPLETE
RUNTIME_BOUNDARY_CONTRACT_INCOMPLETE
BINDING_CONTRACT_INCOMPLETE
ERROR_RECOVERY_CONTRACT_INCOMPLETE
AUTHORITY_OWNERSHIP_CONFLICT
EXPOSURE_PARITY_INCOMPLETE
ACCEPTANCE_GATE_CONTRACT_INCOMPLETE
CLOSURE_PREDICATE_INCOMPLETE
ARCHITECTURE_READINESS_BLOCKED
```

- `SSOT_NORMATIVE_CONFLICT` má classification `CONFLICT` a znamená dvě současně autoritativní ustanovení se semanticky rozdílným výsledkem po aplikaci precedence kapitoly 55.2.
- `CONTRACT_PACK_DRIFT` má classification `VALIDATION` a znamená, že deterministická projekce neodpovídá exact SSOT nebo pinned schema/compiler digestu.
- `CONTRACT_PACK_REFERENCE_INVALID` má classification `VALIDATION` a znamená unknown, stale, wrong-kind, cyclic nebo digest-mismatched registry reference.
- `REQUIREMENT_TRACEABILITY_INCOMPLETE`, `ORPHAN_REQUIREMENT` a `ORPHAN_IMPLEMENTATION_ARTIFACT` mají classification `VALIDATION` a blokují build, activation i handoff do úplné obousměrné lineage.
- `OPERATION_CONTRACT_INCOMPLETE`, `STATE_MACHINE_CONTRACT_INCOMPLETE`, `POSTGRES_CONTRACT_INCOMPLETE`, `RUNTIME_BOUNDARY_CONTRACT_INCOMPLETE`, `BINDING_CONTRACT_INCOMPLETE` a `ERROR_RECOVERY_CONTRACT_INCOMPLETE` mají classification `VALIDATION`; přesně pojmenovávají chybějící registry record nebo povinný field a nesmějí být nahrazeny implementačním defaultem.
- `AUTHORITY_OWNERSHIP_CONFLICT` má classification `CONFLICT` a znamená nula nebo více než jeden canonical writer/state-machine owner authoritative mutable objectu nebo pointeru.
- `EXPOSURE_PARITY_INCOMPLETE`, `ACCEPTANCE_GATE_CONTRACT_INCOMPLETE` a `CLOSURE_PREDICATE_INCOMPLETE` mají classification `VALIDATION` a blokují activation i architecture readiness.
- `ARCHITECTURE_READINESS_BLOCKED` má classification `VALIDATION` a je aggregate výsledek nejméně jednoho neúspěšného gate kapitoly 55.21. Pokud jedinou příčinou zůstává chybějící OWNER business rozhodnutí, details obsahují právě jeden `ownerDecisionId`, exact otázku, povolené varianty a affected requirement subjects; technická neúplnost se jako OWNER rozhodnutí nevydává.

Canonical recovery všech těchto codes je oprava autoritativního SSOT, registry compileru nebo traceability vztahů, nová deterministická kompilace Contract Packu a opakování dotčených blocking gates. Runtime retry, waiver, ruční změna generovaného registru, test-only bypass ani architektonický odhad nejsou recovery.

## 33. Ověřovací strategie

### 33.1 Úrovně důkazu

Každý požadavek má předepsaný typ důkazu:

- pure domain test pro deterministickou logiku,
- PostgreSQL test pro persistence, constraints, transactions a souběh,
- authenticated Fastify HTTP test pro route, cookies, CSRF, API key a binding validation,
- oddělený domain/UI SSE test pro ordering/replay a MCP SSE test pro request scope/no-replay,
- real Chromium/Playwright test pro browser a UI,
- Linux/systemd test pro runtime a sandbox,
- production acceptance pro skutečný provider, hostname, TLS, systemd a release,
- manual OWNER scenario pro vizuální a provozní tok.

Symbol, textový grep nebo existence souboru doplňuje kontraktační guard a nenahrazuje behavior test.

### 33.2 Testovací prostředí

- lokální vývojové prostředí,
- CI PostgreSQL 16,
- CI Ubuntu 24.04,
- disposable privileged systemd harness pro produkčně tvarovaný runtime,
- real Playwright-managed Chromium,
- deterministický integrační web portal pro redirect, popup, iframe, auth, challenge, upload/download, drift a side effects,
- produkční acceptance na `kaja.hcasc.cz`.

### 33.3 Test data

Acceptance fixtures mají unique correlation prefix, canonical lifecycle, owner metadata a cleanup. Fixtures používají skutečné aplikační cesty, schémata, DB, workery a runtimy. Auditní evidence zůstává dohledatelná.

### 33.4 Povinné testy každého objektu

Každý domain object má:

- create/read/update podle immutability,
- validation,
- concurrency,
- idempotency,
- exact bindings,
- audit,
- lifecycle transitions,
- cancellation,
- failure/recovery,
- UI empty/loading/error/success,
- object-action parity.

### 33.5 Vizuální QA

Automatizované a manuální browser QA používá čtyři povinné viewporty, keyboard traversal, focus, overlays, scroll, resizing, long content, errors, live data a slow network. Screenshot je doplňující evidence; interakce je autoritativní.

### 33.6 API řízený self-test

Veškeré testovací schopnosti KájovoCML NG jsou dostupné přes produkční self-test API. Test katalog zahrnuje:

- lint, typecheck, unit a build testy,
- PostgreSQL clean/upgrade/concurrency testy,
- API contract a route testy,
- singleton OWNER API key a API-key-session testy,
- centrální chat a OpenAI SDK testy,
- generation, MCP, AI agent a Browser Runtime testy,
- component control, state, heartbeat, monitoring a repair,
- Secret Manager, bindings, audit a logs,
- registered-element manifest E2E,
- UI, accessibility a viewport Playwright scénáře,
- release, deployment, backup, rollback a acceptance testy.

Self-test runtime používá jediný produkční `KCML_OWNER_API_KEY` a volá kanonické externí URL. Každý test má setup, skutečný request nebo runtime operaci, assertions, evidence a idempotentní cleanup. Webové testy získají session API-key-session endpointem. Žádná testovací cesta nepotřebuje interaktivní login.

Repository a CI spouštějí stejný katalog pomocí API. Každý run vrací jednotný stav, případové výsledky, timings, logy, audit, artifacts a exact release/SHA evidence.

### 33.7 Povinné generation, KCIP, MCP a agent testy

Každý generation release obsahuje testy:

- state transition, terminalita, phase guard a current platform incarnation,
- queue/lease/fencing, late worker, crash takeover a no-resurrection,
- OpenAI model-call idempotency, submit ambiguity a background retrieve,
- strict structured output, malformed output a native-schema inverse projection,
- tool call ID, schema, unknown-tool a output validation,
- workspace patch CAS, path security a complete read-back,
- capability full/partial/new a stale snapshot,
- first CREATE orphan cleanup.

KCIP contract suite zahrnuje:

- canonical UTF-8 JSON, canonical number/string representation a cross-language SHA-256 vectors,
- duplicate JSON member, malformed envelope, version, size/depth a reserved field reject,
- server-derived source/target/binding/context a spoof negative matrix,
- correlation/causation root, child, retry, queue, PULSE a response chains,
- message ID duplicate same-digest replay a different-digest conflict,
- business idempotency same-key/same-digest replay a different-digest conflict,
- deadline before intent, after intent, after dispatch a after terminal commit,
- cancellation before/after side effect a `UNKNOWN` reconciliation,
- sequence contiguous duplicate/stale/gap a monotonic-with-gaps evidence,
- PULSE inbox-before-ACK, duplicate, schema drift, DLQ a repaired replay,
- control `ACCEPTED` versus terminal outcome, stale runtime ACK a cancel race,
- state/heartbeat lineage, sequence, stale projection a no-lease-extension.

MCP modern wire suite zahrnuje:

- POST-only endpoint, Origin, auth, Accept/content type/body size a GET/DELETE,
- malformed JSON, duplicate members, batch, client response, notification a `id=null`,
- JSON-RPC string/integer ID exact echo, string-versus-number distinction, duplicate in-flight a post-terminal reuse,
- HTTP status versus JSON-RPC error table včetně `202` notification, `404/-32601`, `-32602` a `-32603`,
- required `_meta`, protocol version, current-request capabilities a reserved vendor keys,
- `MCP-Protocol-Version`, `Mcp-Method`, method-specific `Mcp-Name`, duplicate singleton header a header/body mismatch,
- `x-mcp-header` static paths, forbidden schema positions, safe integer, null/absent, boolean/integer conversion, ASCII/Base64 sentinel, sentinel-looking literal a invalid Base64,
- `server/discover`, supported versions, capabilities/extensions, self-reported serverInfo non-authority a cache hints,
- deterministic discovery/list ordering, full pagination, empty cursor, invalid/wrong-context cursor a no cross-page assumption,
- TTL zero/positive, public/private isolation, request-parameter cache key, notification invalidation, MRTR no-cache a diagnostic-only stale snapshot,
- JSON Schema 2020-12 default, supported/unsupported dialect, local refs, vendored remote refs, automatic network-ref deny, cycles/depth/time budget,
- native MCP schema versus OpenAI strict projection, `additionalProperties:false`, all-required/nullable conversion, impossible lossless projection a digest invalidation,
- success `resultType=complete`, `structuredContent` object/array/scalar, content render, unknown/unnegotiated resultType a schema-invalid output before/after effect,
- protocol error versus `isError=true` tool execution error,
- request-scoped SSE progress/message, exact final ID, wrong ID, independent notification, server request, multiple final responses, frame after final, keepalive a no event ID/replay,
- disconnect před intentem, před dispatch, po possible effectu a po terminal commit,
- MRTR only na supported methods, at-least-one field, capability requirements, exact requestState echo, changed original args, expiry, consume/replay, partial a unknown responses,
- `subscriptions/listen` first ACK, acknowledged subset, empty subset, subscription ID, multiple concurrent streams, out-of-filter notification, client cancel, server graceful closure a reconnect full refresh,
- state handle ordinary-wire-value, wrong context, stale binding/revision, expiry, idempotent close a never-reuse,
- Tasks per-request capability, only `tools/call` augmentation, durable-before-response, task routing `Mcp-Name`, get/update/cancel, lifetime-unique input keys, partial/unknown responses, `isError=true` completed, protocol-error failed, cooperative cancel race, TTL/manual review/tombstone a task notifications,
- exact binding/revision/release/contract digest/binding-set/activation epoch match a stale rejection,
- side-effect idempotency, concurrency, ordering, timeout a non-idempotent reconciliation,
- external era matrix: recognized modern error, unsupported version retry, legacy candidate, explicit fallback, auth/network/5xx indeterminate, malformed 2xx invalid a mixed-era reject,
- compatibility differential vectors, agent projection recompile a atomic activation/rollback.

Agent suite zahrnuje binding, alias collision, guardrail, handoff, session, MCP input/task wait, approval, checkpoint/resume, eval promotion a combined MCP+agent activation set. Skutečný agent-to-MCP E2E musí projít veřejnou nebo kanonickou internal gateway podle contractu; nesmí volat handler přímo.

Každý crash test ukončí worker v deterministickém bodě před/po intentu, dispatch claimu, provider/external response, raw outcome persistenci, reconciliation, input consume, task create/update/cancel, terminal write a response emit. Nový worker musí pokračovat z checkpointu bez duplicate identity, model callu, tool side effectu, tasku, notification nebo pointer switchi.

### 33.8 OpenAI contract test harness

Test harness odděluje:

- deterministic fake transport pro state-machine, idempotency, submit-ambiguity, schema a crash tests,
- recorded SDK-shaped event streams pro foreground, background, retrieve/resume, function calls, hosted MCP, refusal, incomplete a cancellation,
- version-pinned Agents SDK RunState fixtures pro approval, handoff, agent-as-tool, sessions a upgrade compatibility,
- skutečný OpenAI smoke test pro jeden bounded Responses call,
- skutečný Agents SDK smoke test se strict function tool,
- skutečný agent-to-MCP call v production acceptance.

Production klient musí v testu prokázat `maxRetries: 0` pro create-class requests. Fake transport používá stejná event, output-item, status a error schémata jako produkční adapter a eviduje přesný počet provider create dispatchů.

Povinné testy zahrnují complete ordered output včetně reasoning/tool items, pairing podle `call_id`, zachování output order při paralelním tool execution, refusal/incomplete/schema-invalid rozlišení, background `store=true`, retrieve/resume stejného `response.id`, session-strategy exclusivity, compaction races, exact SDK/graph resume a side-by-side starý runtime pro pending approvals.

Fault-injection harness ukončí worker před i po každém z devíti provider/runtime bodů kapitoly 52.33. Po restartu ověří nulový duplicate model create, nulový duplicate tool side effect, právě jeden successor, neztracený provider output a konzistentní session/memory terminal commit. Úplný katalog je v 52.36.

Skutečný OpenAI test ověřuje active `OPENAI_API_KEY`, selected-model capability, strict structured output, tool-call round-trip, usage, provider request/response ID evidence a trace. Provider-dependent failure se klasifikuje odděleně od aplikačního contract failure.

### 33.9 Browser Interaction Plane testy

Povinný browser test catalog obsahuje nejméně:

#### Runtime build, host a context

- exact Playwright/Node/browser/OS/font/locale build-manifest pinning a digest mismatch reject,
- blue/green host start, readiness, drain, recycle a zákaz raw context migration mezi odlišnými manifests,
- non-persistent BrowserContext isolation cookies, localStorage, IndexedDB, sessionStorage, permissions, downloads, service workers a virtual authenticators,
- explicitní bridge/profile persistent-context lease a concurrent profile attach reject,
- browser worker restart, host restart, renderer crash a stale host/context response rejection,
- Chromium, Firefox a WebKit capability matrix a explicitní CDP compatibility lower-fidelity evidence.

#### Session, control a preview

- session parent binding a cross-parent access deny,
- one-writer/many-viewers control lease, DB-time expiry, no resurrection a fencing,
- OWNER takeover během read-only, před mutation triggerem a po possible mutation triggeru,
- return-to-AI fresh page/frame/document observation a input-state reset,
- stale control, host/context generation, page generation, frame attachment, document epoch, observation, frame a viewport transform reject,
- KBPP handshake, Origin, one-time ticket audience/capability/reuse a no-token-in-URL,
- reconnect s novým stream epoch, snapshot acknowledgment barrier a nulový replay neacknowledged inputu,
- adaptive frame backpressure, latest key frame a nezahození control/action/challenge events,
- pointer buttons, key-down state, IME/composition, touch IDs, drag/pointer capture reset při takeover/reconnect.

#### Page, frame, document a target identity

- target picker pro DOM, text, frame, page a visual region,
- semantic locator strict one-match, missing, ambiguity, fallback a drift,
- zákaz persistence/reuse ElementHandle po rerenderu/navigation/detach,
- page ID/generation, popup/opener/noopener, immediate popup close a window-name reuse,
- iframe, nested iframe, cross-origin, OOPIF, detach/reattach, sandboxed/credentialless/fenced frame routing,
- full navigation, same-document History/hash, SPA rerender, BFCache restore a prerender activation document semantics,
- service-worker/background-worker network event bez frame identity a zákaz použití jako samostatné business postcondition,
- nested viewport transform, DPR, zoom, visual viewport, letterbox, iframe scroll a stale-coordinate reject.

#### Actionability a input semantics

- per-action attached/visible/stable/enabled/editable/receives-events checks,
- trial check, scroll/re-observation, sticky header, nested scroll, virtualized list, animation, transform a layout shift,
- overlay classification a zákaz generic auto-dismiss consent/terms/security/destructive overlay,
- exact `force` exception, negative matrix a zákaz force přes ambiguity/stale/wrong-account/disabled state,
- `FILL`, `TYPE`, `INSERT_TEXT`, `PRESS`, composition, contenteditable, rich-text, masked input, date/time/number a locale parsing,
- focus/blur/input/change/autosave/auto-submit mutation trigger detection,
- select/check/upload/drop/dialog/permission/WebAuthn registration mutation classification,
- pre-armed navigation, popup, dialog, filechooser a download waiter před triggerem,
- popup/download/dialog event z jiného action/page/frame causation reject.

#### Side effect, postcondition a recovery

- persistent intent před každým earliest possible mutation triggerem,
- dispatch phase ledger od intentu po outcome a duplicate/conflicting phase handling,
- crash injection před/po host acceptance, target resolve, input start, mutation possible, method return, post-observation a terminal commit,
- independent read-back pro autosave, submit, login, account change, order/transaction, download a WebAuthn registration,
- Playwright method success, URL, screenshot, `load`, `networkidle` a HTTP status samy nepotvrdí business success,
- `CONFIRMED_APPLIED`, `CONFIRMED_NOT_APPLIED`, `UNKNOWN` a manual-resolution CAS,
- unknown outcome blokuje nový action na stejném account/resource/external-session key,
- cancel/complete, takeover/completion, bridge disconnect/completion a cleanup/reconciliation races.

#### Authentication a state continuity

- credential field, Secret binding, direct OWNER login a zákaz implicitního password Secretu,
- exact account/tenant authenticated condition a wrong-account/session-switch reject,
- state bundle capture barrier, immutable member inventory, CAS activation a credential/auth-epoch race,
- cookies včetně partition metadata, localStorage, IndexedDB a custom sessionStorage pre-bootstrap restore,
- standardní exclusion cache/service-worker runtime/raw profile/platform passkey/private key,
- bundle runtime-build/origin/account/credential/certificate compatibility, expiry a invalidation,
- third-party storage partitioning, anti-CSRF/device/IP binding a deterministic relogin/challenge,
- concurrent logout, credential/certificate rotation, account switch a stale bundle capture reject.

#### Challenge, WebAuthn a client certificate

- email/SMS/TOTP, auto-submit, expired/consumed code a bounded clock-skew,
- push confirmation late completion po timeout/disconnect a reconciliation,
- WebAuthn assertion versus registration, RP/account binding a registration postcondition,
- virtual authenticator pouze explicitní test/automation account a context-scoped state,
- platform passkey, conditional mediation, biometric/PIN a cross-device flow přes bridge/challenge,
- client certificate exact-origin context creation, rotation/new context a ambiguous certificate challenge,
- CAPTCHA/human verification bez bypassu a fresh observation po OWNER resolution,
- alert/confirm/prompt/beforeunload policy a zákaz skrytého default accept/dismiss,
- permission prompt a native/external-protocol challenge lifecycle.

#### OWNER Device Bridge

- enrollment, local key generation, mTLS exact route a certificate generation,
- Windows/macOS/Linux capability fingerprint a required-capability mismatch,
- bridge connection replacement, heartbeat expiry, certificate rotation, revoke a stale connection reject,
- exact session assignment, operation scope, local target, account/profile lease a control/action fence,
- raw CDP, shell, arbitrary command, filesystem path, LAN proxy/scan a cross-session deny,
- device-bound passkey/client-certificate flow bez exportu private key,
- local browser profile one-writer, existing process identity, OWNER takeover a cleanup,
- disconnect před/po possible mutation a zákaz blind fallbacku na server-managed browser.

#### Upload, download, teaching a automation

- upload artifact handle, MIME/extension/size/digest, multiple/directory relative-path manifest a no arbitrary host path,
- filechooser pre-arm, chooser target/page/frame/document binding a stale chooser reject,
- download start versus completion/failure, blob/data initiator, persist před context close, content/digest verify a cleanup,
- teaching low-level event → semantic step, input strategy, mutation trigger, postcondition a concurrency compilation,
- unresolved mutation semantics a visual-only unattended mutation activation deny,
- OWNER direct visual/native control remains functional,
- replay se stejným interpreterem, exact revision/build digestem a model network disabled,
- deterministic run model-call count nula a adaptive agent run viditelně odlišen,
- scheduled run timezone/DST/misfire/overlap/idempotency a bounded contexts,
- account concurrency across server/bridge sessions,
- monitoring, drift, repair, rollback a orphan-free cleanup.

Testy používají kontrolovaný multi-origin portal fixture s dynamickým DOM, autosave on input/change/blur, overlayem, dvojznačnými labels, virtualizovaným seznamem, SPA navigation, BFCache/prerender variantou, popupem, nested cross-origin iframe/OOPIF, service workerem, auth expiry, wrong-account switch, OTP/push/WebAuthn/client-certificate challenge, dialogem, permission, uploadem, downloadem a persistentním mutation counter/read-back API.

Acceptance proti externímu portalu se provádí pouze tam, kde je použití povolené, existuje test/account scope, outcome lze ověřit a repeatable cleanup nebo explicitní irreversible acceptance contract je součástí revision. Screenshot/manual pozorování samo nenahrazuje machine-readable evidence požadovanou daným scénářem.

### 33.10 Formální a nemesis testy

Blocking test suite obsahuje executable state-machine model, PostgreSQL concurrency tests a deterministic crash injection podle 49.29 a 49.30. Worker je ukončen před/po každém intentu, dispatchi, outcome, checkpointu, phase transitionu, activation switchi, rollback switchi, terminal commitu a cleanup kroku.

Testy dokazují state-version CAS, lease no-resurrection, monotonic fencing, stale-worker rejection, outbox no-loss, idempotency conflict, cancel/complete race, model submit ambiguity, external unknown outcome, activation no-mixed-epoch, first CREATE `ABSENT` rollback, update/repair exact rollback, browser one-writer, state-bundle CAS, deployment incarnation a orphan-free cleanup. PostgreSQL část používá konkrétní interleavings a fault body 51.34, kontroluje physical constraints a považuje každý deterministický `40P01` za lock-plan defect i tehdy, když aplikační retry uspěje.

### 33.11 Trusted Runtime Boundary a Linux IPC testy

Blocking catalog obsahuje:

- systemd socket activation, exact FD name/count/type a service-without-bind test,
- UDS parent/socket owner, group, mode, device/inode, symlink/substitution/stale-path a restart race tests,
- SO_PEERCRED, systemd unit/`InvocationID`, cgroup, boot/start identity, PID reuse a peer-exit tests,
- runtime launch manifest/current DB snapshot, release/runtime digest, binding-set a activation/deployment epoch mismatch tests,
- handler user/mount/network/IPC/UTS/PID/cgroup namespace inspection,
- read-only root, private tmpfs, `/proc`, absent `/sys`, minimal `/dev`, private `/run`, symlink/hardlink/device/path traversal tests,
- zero capabilities, `NoNewPrivileges`, seccomp syscall-deny a namespace escape tests,
- environment allowlist a dynamic-loader/runtime-option injection tests,
- inherited FD inventory, CLOEXEC, SCM_RIGHTS injection a child FD inheritance tests,
- direct network, loopback, internal UDS, PostgreSQL, Secret Manager a browser-host access deny,
- `KCML-RUNTIME-IPC/1` framing, reserved identity fields, sequence, size, stream, backpressure, timeout, cancellation a disconnect tests,
- exact Secret/component/external/state binding positive a negative matrix,
- egress DNS/IP/redirect/rebinding/SNI/credential forwarding tests,
- cgroup CPU/memory/swap/PID/FD/tmpfs limits, OOM a resource exhaustion recovery,
- child process, double-fork, ignored signal, orphan, cgroup emptiness a kill -9 recovery,
- activation/rollback/deployment stale-process and old-write fencing,
- systemd credential materialization, rotation, old `InvocationID` a cleanup tests,
- browser worker/host typed UDS, raw Playwright/CDP exposure deny, stale host/context/action generation tests,
- OWNER Device Bridge enrollment, mTLS exact route, session scope, revoke, raw-command/LAN-proxy/filesystem deny,
- complete audit/correlation on reject, timeout, cancellation, crash, restart a reconciliation.

Úplný negative a failure-injection catalog je v 50.35 a 50.36. Povinný test běží v disposable privileged Ubuntu 24.04 systemd harnessu; mock nebo statická konfigurace jej nenahrazuje.


### 33.12 Agentic AI security a confused-deputy red-team testy

Blocking suite používá multi-source adversarial fixture a pro každý případ ověřuje model context, serverový operation context, dispatch ledger, target/argument origins, Secret-use evidence, audit a nulový neočekávaný side effect. Obsahuje nejméně:

- přímou a nepřímou prompt injection ve viditelném textu, hidden DOM, HTML commentu, accessibility name, image/OCR, CSS-generated content a canvas textu,
- Unicode bidi/control, homoglyph, encoded/base64, nested JSON/XML/Markdown a fake role/message envelope,
- MCP tool descriptions, annotations, `server/discover` instructions, resources, prompts, results, errors a task/MRTR payloady s falešným OWNER příkazem,
- external API body/header/redirect/link, webhook payload a provider error, který se pokouší změnit další target nebo tool,
- attachment, PDF, office document, archive member a downloaded artifact obsahující instruction-like content,
- retrieved memory, compaction summary, prior assistant message, log, stack trace, audit payload a monitoring alert s authority spoofem,
- repository README, `AGENTS.md`, source comment, test fixture, dependency metadata, generated code a build/test output s pokusem změnit specification nebo spustit jiný capability alias,
- agent-to-agent message, handoff, agent-as-tool result a delegated context s pokusem rozšířit operation, target, side-effect, budget nebo Secret destination,
- capability discovery s malicious name/description collision a tool substitution,
- browser redirect, popup, iframe/OOPIF, download, dialog a page text vyžadující vložení secretu na jiný origin,
- delayed a second-order injection, která se projeví až po summarize, retrieval, memory write, compaction, repair nebo následujícím tool resultu,
- concurrent OWNER steer/session změnu, která zneplatní stale intent/context před dispatch,
- pozitivní scénáře, v nichž OWNER explicitně požádá o stejnou legitimní capability nebo deleguje exact source; operace musí proběhnout bez nové confirmation vrstvy.

Test selže, pokud neautoritativní content změní canonical operation, exact tool, target, argument mimo deklarovaný slot, binding, side-effect class, handoff graph, Secret-use recipient nebo authoritative session/specification state. Pouhé modelové odmítnutí není dostatečný důkaz; rozhodující je serverový no-dispatch/no-side-effect a úplná provenance evidence.

### 33.13 Stateful property-based, chaos a recovery correctness testy

Blocking suite kapitoly 54 používá stejné produkční command handlers, PostgreSQL schema, queues, runtime gateway, brokers, OpenAI adapter, MCP/KCIP codec, Browser Interaction Plane, deployment a cleanup služby jako běžný systém. Reference model generuje validní i nevalidní command sekvence, concurrent OWNER sessions, multiple workers, duplicate/reordered deliveries, stale snapshots a fault schedules; po každém kroku a po recovery vyhodnocuje invarianty nad přímým autoritativním stavem.

Každý běh ukládá seed, shrunk schedule, exact release/source SHA, schema a migration digest, runtime/browser/OpenAI adapter manifests, fault-catalog digest, virtual/DB time trace, command history, invocation/commit/response body, worker/fence identities, external fixture counters, complete oracle result a cleanup inventory. Nalezené porušení se automaticky zmenší na nejkratší reprodukovatelnou sekvenci, uloží jako immutable regression case a zůstává blocking do opravy.

Povinné pokrytí zahrnuje všechny stavy, povolené i zakázané hrany, terminal a `MANUAL_REVIEW` outcomes, každý stable fault point před i po kritickém kroku, všechny stale identity druhy, úplnou single-fault × critical-cutpoint matici, pairwise kombinace a vybrané tříporuchové schedule kapitoly 54. Randomizovaný profil provede nejméně 10 000 nemesis interleavings na release a nesmí mít jediný invariant violation, false success, duplicate effect, orphan nebo mixed epoch.

## 34. Povinná integrační akceptace — 360 bodů

Následujících 360 bodů tvoří jeden závazný akceptační celek.

### 34.1 Diskuse, messages a turns

1. `POST /generation/jobs` vytvoří job ve stavu `DISCUSSING` a uloží immutable initial request.
2. Initial request je dostupný jako auditní evidence se stabilním digestem.
3. První OWNER zpráva je součástí persistentní historie a je propojena s prvním turnem.
4. Reload webu obnoví stejný job, messages, current turn, current specification a progress.
5. Druhý přihlášený klient vidí autoritativní stav stejného jobu.
6. OWNER zprávy dostávají souvislou a unikátní sequence v rámci jobu.
7. Opakování stejného `clientMessageId` a stejného obsahu vrací původní message; rozdílný obsah vrací konflikt.
8. Souběžně vložené messages získají unikátní sequence bez ztráty.
9. Cursor pagination historie je stabilní a deterministic.
10. Každá OWNER zpráva spustí nebo zařadí právě jeden discussion turn.
11. Job má nejvýše jeden upstream-active turn a definovaný successor.
12. Assistant odpověď se ukládá a streamuje jako skutečný text po deltách.
13. Completed message má terminal status a `completedAt`.
14. Failed turn a message zůstávají `FAILED` s chybou a zachovaným částečným obsahem.
15. Worker lease je obnovovaný, fenced a po expiry bezpečně převzatelný.
16. OWNER může vložit další zprávu během běžícího turnu a zpráva se okamžitě persistuje.
17. Steer nastaví interruption lifecycle, přeruší provider request a vytvoří successor.
18. Částečný assistant text se při přerušení zachová jako `INTERRUPTED`.
19. Successor používá celou historii a nejnovější OWNER korekci.
20. Souběh provider completion a steer vytvoří jediný autoritativní successor a jediný terminal stav.

### 34.2 Tool loop a capability-first

21. Discussion registry poskytuje web research tool s auditovaným typed výsledkem.
22. Discussion registry poskytuje kanonický capability lookup.
23. Discussion registry poskytuje job-scoped generation browser tools.
24. Discussion registry obsahuje pouze schválené diskusní nástroje a odděluje runtime mutace za kanonické doménové operace.
25. Tool start, progress, completion a failure vytvářejí events, logs a audit.
26. První validní specification proposal vytvoří revision 1.
27. Identická kanonická specification se deduplikuje podle digestu.
28. Funkční změna vytvoří následující revision.
29. Starší revision zůstává immutable.
30. Stejný kanonický JSON vytváří stejný SHA-256 digest.
31. Markdown render věrně zobrazuje všechny strukturované části specifikace.
32. Current revision pointer ukazuje nejnovější platnou proposal revision.
33. Schválení current revision a správného digestu uspěje.
34. Schválení stale revision vrací conflict.
35. Schválení stale digestu vrací conflict.
36. Schválení při active nebo queued turnu vrací conflict.
37. Open questions blokují schválení.
38. Souběžné schválení je deterministic a vytvoří jeden výsledek.
39. Approved revision zůstává immutable.
40. Approved digest přesně odpovídá approved revision.
41. Approval atomicky zmrazí specification a přepne job do `ANALYZING`.
42. Planner načte přesně approved revision.
43. Planner obdrží approved digest a lineage.
44. Implementace, integrace, validace a remediation používají frozen execution authority.
45. Před approval nevzniká target component.
46. Před approval nevzniká target runtime execution identity ani binding.
47. Před approval nevzniká target release ani activation side effect.
48. Technický retry se stejnou autoritou nevytváří duplicitní runtime identity.

### 34.3 Browser Interaction Plane v generation

49. Generation job spustí job-scoped Browser Session.
50. Browser profile, cookies, storage a tabs jsou izolované podle jobu.
51. Session vytváří safe preview frame.
52. Preview revision je monotonic.
53. OWNER session jiného jobu nemá přístup k preview frame.
54. Preview odpověď používá `Cache-Control: no-store`.
55. Sensitive transition používá explicitní sensitivity state a OWNER visibility policy.
56. Preview a export odpovídají nastavené OWNER sensitivity policy.
57. Session se po citlivém kroku vrací do `NORMAL` podle kroku a policy.
58. Browser crash vytváří diagnostický event, log, artifact a recoverable state.
59. Job cleanup odstraní jeho temporary browser artifacts podle retention.
60. Opakovaný cleanup je idempotentní.

### 34.4 SSE, cancellation a OWNER UI

61. Persistentní domain/UI SSE event order odpovídá autoritativní DB sequence; tento bod se nevztahuje na MCP request-scoped SSE.
62. Domain/UI SSE `Last-Event-ID` vrací přesný bounded replay; MCP SSE `Last-Event-ID` nepoužívá.
63. Domain/UI SSE replay gap vrací `resync.required` a fresh snapshot cursor; MCP subscription reconnect provádí nový listen a full refresh.
64. Domain/UI SSE reconnect nevytváří duplicitní delty ani messages; MCP transport recovery používá idempotency/reconciliation, nikoli event replay.
65. Dva klienti sdílejí stejný lifecycle a event stream.
66. Disconnect klienta neruší běžící turn.
67. Cancel v `DISCUSSING` zastaví turn a Browser Session.
68. Cancel v `ANALYZING` zastaví planner.
69. Cancel v realization fázi zachová aktivní release a uklidí candidate.
70. Opakovaný cancel je idempotentní.
71. Po cancellation nevzniká další zakázaný side effect.
72. Desktop generation workflow je plně použitelný.
73. Tablet layout má bounded interní scrolly a čitelný workspace.
74. Mobil 390×844 má stabilní stránku a ovladatelné záložky.
75. Composer, messages, tabs a akce jsou ovladatelné klávesnicí.
76. Streaming state je čitelný a stabilní.
77. Tool activity zobrazuje operace a výsledky v technickém panelu.
78. Spec panel pokrývá všechny části GenerationSpecification.
79. Approval CTA odpovídá serverovým guards.
80. Stale approval zobrazí current revision a změny.
81. Browser preview respektuje OWNER sensitivity policy.
82. Approved specification je read-only.
83. Progress odpovídá server lifecycle a worker evidence.
84. Mutace přijímají authenticated OWNER session s CSRF nebo jediný produkční OWNER API klíč.
85. Reads přijímají OWNER session nebo OWNER API klíč a po autentizaci používají plnou OWNER autoritu bez role/scope/ACL rozhodnutí.
86. Cross-job access je serverově odmítnut.
87. Log/audit export je dostupný OWNER session i OWNER API klíči a vytváří explicitní export akci.
88. Technické SSE eventy používají schema a bounded payload; plný detail je dostupný přes OWNER log/audit detail.
89. Neznámý nebo nepovolený model tool call je odmítnut typed chybou.
90. Upload/preview API přijímá pouze artifact ID nebo bezpečný upload handle.
91. Cancellation regression suite pokrývá turn, implementation, browser a activation.
92. Repair/integration regression suite pokrývá authority, bindings, cleanup a rollback.
93. OWNER navigace zachovává všechny produktové obrazovky a stav při přechodu.
94. Root lint, typecheck, tests a build jsou zelené.

### 34.5 Browser konfigurace, teaching a replay

95. AI otevře OWNERem zvolený externí portal v job-scoped browseru.
96. Každá externí browserová operace je vázána na OWNER operation scope.
97. Běžné Save/Continue kroky uvnitř scope pokračují bez samostatného schválení.
98. Nevratná operace mimo již jednoznačně vyjádřený rozsah používá exact confirmation.
99. OWNER může zadat credential do chatu nebo credential pole a browser jej použije.
100. Persistentní credential je uložen do Secret Manageru a svázán stable name.
101. Browser login a secret použití je dostupné v OWNER debug logu a auditní historii.
102. Login redirect a popup zachová session a tab continuity.
103. MFA, OTP, WebAuthn a external confirmation vytvoří human challenge lifecycle.
104. CAPTCHA je předána OWNERovi jako human verification challenge.
105. Teaching uloží ordered semantic steps.
106. Secret step uloží Secret binding a OWNER může hodnotu zobrazit v UI.
107. Parametrický step uloží typed input binding.
108. Locator používá semantic preference a ambiguity kontrolu.
109. Každý step obsahuje side-effect a retry class.
110. Manifest validator přijímá pouze deklarativní DSL.
111. Teaching candidate je součástí specification/manifest digestu.
112. Preflight ověří prostředí a postconditions bez business mutace.
113. Replay funguje se zakázaným model network přístupem.
114. Non-idempotent acceptance scénář ověří reconciliation a mutation count.
115. Replay failure označí exact step, state, error class a evidence.

### 34.6 Canonical Browser Automation Runtime

116. Browser Runtime existuje jako kanonická CML component s runtime identitou, active revision, contracts, monitoringem a auditem.
117. Generated handler běží v produkčním Linux namespace/chroot capability sandboxu.
118. Generated MCP používá `context.callComponent` pro Browser Runtime.
119. Exact contract binding omezuje volání runtime toolu podle source/target revisions a operation key.
120. Runtime ověřuje vztah caller–automation definition/revision.
121. Browser credentials získává runtime prostřednictvím runtime-only Secret bindingu.
122. Automation revisions jsou immutable a digest-protected.
123. Aktivovat lze pouze revision s verification `PASS`.
124. Rollback obnoví poslední `PASS` revision.
125. Produkční automation run nevytváří generation job.
126. Produkční automation run má model-call count nula.
127. Run lease a step checkpoints jsou persistentní a fenced.
128. `clientRunId` je idempotentní.
129. Disabled definition odmítne nový run.
130. Preflight, status, cancel, history a reauthenticate mají plné API a UI.
131. Každý run používá izolovaný BrowserContext.
132. Reusable auth state je secret-grade encrypted artifact.
133. Navigation, redirect a popup používají origin allowlist a operation scope.
134. Private, loopback, link-local, reserved a metadata cíle jsou blokované po DNS resolution.
135. Runtime používá pouze schválený semantic fallback.
136. Fallback vytváří drift evidence a zachová immutable manifest.
137. Non-idempotent timeout nebo crash nespouští blind retry.
138. Reconciliation rozliší applied, not applied a unknown.
139. Unknown side effect přejde do `MANUAL_REVIEW`.
140. Worker crash nevytvoří duplicitní account/browser context side effect.
141. Cancel zastaví další browserové side effects.
142. Auth expiry spustí deterministic relogin nebo challenge podle auth mode.
143. Contract drift nastaví definition/component na `DEGRADED`.
144. Drift vytvoří deduplikovaný repair job.
145. Repair zachová component, runtime a definition identity.
146. Repair failure zachová poslední funkční revision a release.
147. Upload odmítne arbitrary host path a používá upload handle.
148. Download je run-scoped a ověřuje origin, digest, MIME a size.
149. Sensitive trace používá chráněný artifact policy.
150. Artifact cleanup je bounded, idempotentní a restart-safe.
151. Queue uplatňuje capacity a backpressure.
152. Account concurrency key serializuje konfliktní sessions.
153. Async run přežije HTTP disconnect.
154. OWNER UI zobrazuje runtime state, history, steps, challenges a artifacts.
155. OWNER může run, preflight, cancel, reauth, enable, disable a repair provést ze všech relevantních pohledů.

### 34.7 Platforma a closure

156. Systém obsahuje právě jednu pevnou OWNER identitu `KRMAR78`, žádné další lidské účty ani uživatelské role či oprávnění.
157. WEDOS DNS, TLS a unattended renewal mají produkční acceptance a exact cleanup.
158. Jediný `OPENAI_API_KEY`, oficiální OpenAI SDK, Secret Manager a platformní bindings mají produkční readiness a skutečný OpenAI test; core systém současně prokazuje plný start bez klíče.
159. Ordinary deployment, rollback, production acceptance a infrastructure acceptance jsou oddělené, pinované na exact SHA a auditované.
160. Kanonický operation catalog, jediný OWNER API klíč, centrální chat a self-test používají stejné doménové operace, validace a audit.

### 34.8 Povinné průřezové API, chatové a bootstrap gates

- `API-01`: každá UI akce existuje v operation catalogu a je vyvolatelná jediným OWNER API klíčem.
- `API-02`: `KCML_OWNER_API_KEY` získá plný přístup ke všem registrovaným komponentám, MCP tools, agentům, automatizacím, monitoringům, logům a testům bez scopes.
- `API-03`: API-key-session exchange vytvoří platnou webovou OWNER relaci bez interaktivního loginu.
- `API-04`: self-test katalog spustí celý repozitář přes API a vrátí kompletní evidence.
- `API-05`: každý registrovaný prvek projde manifest E2E přes svůj kanonický hostname.
- `CHAT-01`: centrální chat zodpoví otázku nad objekty, monitoringem, konfigurací, logy a auditem.
- `CHAT-02`: externí chat API přijme přirozený jazyk a vrátí přirozený jazyk.
- `CHAT-03`: chat vykoná zapnutí, vypnutí, test, repair a generation příkaz přes kanonický action registry.
- `AI-01`: všechny AI call sites používají oficiální balíčky `openai` a `@openai/agents`, Responses API, persistentní model-call record a jediný `OPENAI_API_KEY`.
- `AI-02`: dev, test, chat, generation, agent a produkční call používají stejnou credential hodnotu.
- `START-01`: aplikace bez OpenAI klíče poskytne login, dashboard, config, secrets, logs, monitoring, OWNER API klíč, API a self-test katalog.
- `DEPLOY-01`: každý deploy run synchronizuje `PASS` do pevné identity `KRMAR78` a ověří výsledek.
- `OWNER-01`: databáze po bootstrapu obsahuje právě jeden `owner_identity` řádek `KRMAR78` a právě jeden `owner_api_credential`.
- `OWNER-02`: OpenAPI, operation catalog, UI routes, action registry a databázové schéma neobsahují user/account CRUD, role, group, membership, invite, tenant ani uživatelské permission/ACL operace.
- `OWNER-03`: login vždy autentizuje pevnou identitu `KRMAR78`; jiný username nelze zadat ani vytvořit.
- `OWNER-04`: platná OWNER session a `KCML_OWNER_API_KEY` mají identický plný funkční rozsah bez role, scope nebo objektového ACL rozhodnutí.
- `OWNER-05`: browser accounts a external auth bindings jsou integrační credentials a nikdy nevytvoří aplikačního uživatele.
- `OWNER-06`: reserved Secret `KCML_OWNER_API_KEY` nelze generic Secret CRUD cestou odstranit, přejmenovat, deaktivovat ani přepnout mimo atomickou API-key rotaci.
- `ACCESS-01`: interní component call nepoužívá bearer/JWT/component token a gateway odvodí source pouze ze serverového execution contextu.
- `ACCESS-02`: interní call bez exact active contract bindingu nebo se stale binding digestem selže před handlerem.
- `ACCESS-03`: generated handler nemůže podvrhnout source execution context ani otevřít obecný interní socket.
- `CONFIG-01`: každý OWNER požadovaný vstup má UI/chat cestu a žádný workflow nevyžaduje ruční editaci host souboru nebo environmentu.
- `TRUST-01`: secret přítomný v lokálním repu, GitHubu, CI logu, deploymentu nebo produkčním logu zachová standardní workflow a OWNER dostupnost.

### 34.9 Generation MCP a AI agent gates

161. Každý OpenAI model call vznikne nejprve jako lokální idempotentní record a teprve poté je odeslán providerovi.
162. Opakovaný worker se stejným local idempotency key nevytvoří druhý provider call.
163. Foreground stream persistuje event před SSE publikací a reconnect nevytvoří duplicitní delta.
164. Background model call přežije HTTP disconnect a restart workeru a dokončí se retrieve nebo resume postupem.
165. Provider response ID, input digest, tools digest, output schema digest, usage, cost a trace jsou dohledatelné.
166. `REFUSED`, `INCOMPLETE`, `FAILED`, `CANCELLED` a schema-invalid output jsou odlišné stavy.
167. Strict structured output odmítne additional property, chybějící required field a neplatný enum.
168. Tool output se vrátí se stejným provider call ID a správným pořadím output items.
169. Neznámý tool nebo schema-neplatné arguments se neprovedou a vytvoří typed error.
170. Mutující, pořadově závislé a workspace tools nemohou běžet paralelně.
171. MCP server revision obsahuje exact modern protocol era, version, transport, request metadata, capabilities, extensions, prvky a digest contract.
172. Každý moderní request nese protocol version, client info a client capabilities v `_meta`; HTTP headers se shodují s body.
173. Povinné `server/discover` vrací supported versions, capabilities, server info, `ttlMs` a `cacheScope`.
174. Unsupported protocol version, missing capability a header mismatch jsou odmítnuty standardním error code bez handler side effectu.
175. Discovery/list/read snapshot je immutable, má access-channel/binding fingerprint, aggregate digest, deterministic pagination a cache hints.
176. `subscriptions/listen` nejprve odešle acknowledgement, uvede skutečně přijatou podmnožinu filtru a označí každou notification exact subscription ID.
177. MCP tool native schema je canonical JSON Schema a agent-visible projection má samostatný strict digest a collision validation.
178. `tools/call` ověří transport, metadata, public credential nebo internal execution context, binding revision/digest, target revision, readiness a schema před handlerem.
179. Každý result obsahuje známý `resultType`; complete output a structuredContent se validují po handleru a schema-neplatný success se nevrátí.
180. MRTR input_required zachová original arguments digest, používá opaque bounded requestState a pokračuje novým request ID právě jednou.
181. Cross-call stav používá explicitní ordinary-value handle s novou access/binding kontrolou při každém callu; long-running tool používá negotiated Tasks extension nebo bounded synchronous/MRTR contract.
182. Request-stream disconnect, timeout a crash respektují idempotency/reconciliation a nikdy nevyvolají blind retry non-idempotentní operace.
183. Externí MCP drift vytvoří snapshot diff a breaking změna zneplatní dotčený agent binding před model call.
184. MCP candidate je validován přes live kanonický HTTPS hostname včetně discover, metadata, list, call, subscription a rollback gates.
185. Agent revision obsahuje immutable instructions, strict schemas, model, tools, guardrails, memory, budgets a eval profile.
186. Agent run vždy zaznamená exact revision, compiled instruction digest a tool binding snapshot.
187. Agent binding odkazuje na exact MCP tool revision a contract digest, nikoli pouze na jméno.
188. Stale, unready nebo digest-neplatný binding blokuje run před model call.
189. Agent-visible tool aliases jsou unikátní a neobsahují secrets nebo URL credentials.
190. Input guardrail proběhne před prvním model call a může zastavit run bez tool side effectu.
191. KCIP/MCP pre/post policy pokrývá tools mimo přímou SDK function-tool guardrail pipeline.
192. Handoff a agent-as-tool binding mají strict input/output schema, budget a cancellation propagation.
193. Neomezený nebo nevalidovaný cyklus agent graphu blokuje activation.
194. Agent run ukládá checkpoint po model outputu, tool resultu, handoffu a approval interruption.
195. Worker crash po potvrzeném tool resultu neprovede stejný tool znovu.
196. Approval request je vázán na exact argument digest, target a expiration.
197. Stale, změněný nebo expirovaný approval se neprovede.
198. Resume obnoví původní root run, revision, graph a checkpoint.
199. Nejednoznačný serialized run state fail-closed neprovede další side effect.
200. Session strategy je pro revision jednoznačná a nekombinuje dva konkurenční history managery.
201. Persistent memory write používá typed operation, schema validation, exact memory binding a audit.
202. Agent final output musí odpovídat strict output schema a projít output guardrail.
203. Eval suite ověří routing, arguments, denial, approval, recovery, latency, tokens a cost.
204. Agent revision nelze aktivovat bez PASS blocking eval cases a promotion profilu.
205. Generation plan je validní DAG s úplným requirement coverage a compensation pro side effects.
206. Workspace patch používá base digest, expected file digests, canonical paths a atomic apply.
207. Server po patchi znovu načte celý declared file set a modelové tvrzení nenahrazuje validator.
208. Validation stagnation je detekována a nevytváří neomezený modelový repair loop.
209. Integration saga je idempotentní, checkpointovaná a po restartu pokračuje od prvního nepotvrzeného kroku.
210. Společný MCP+agent job ověří agent candidate proti live candidate MCP digestu.
211. Component release, MCP revisions, agent revision a binding-set revision vstoupí do jednoho activation setu.
212. Částečný switch závislého activation setu není možný.
213. Post-activation smoke provede skutečný agent-to-MCP call a ověří monitoring i audit.
214. Selhání post-activation gate vrátí celý set na předchozí kompatibilní pointery.
215. První CREATE failure odstraní provisional runtime, route, pointer, bindings a registrations před dalším candidate.
216. Generation UI zobrazuje specification, plan, model calls, patch diff, gates, blockers, activation set a evidence.
217. API zpřístupňuje všechny stejné generation/MCP/agent operace a evidence použití `KCML_OWNER_API_KEY`.
218. Centrální chat umí založit job, dodat vstup, schválitelnou specification zobrazit, vyřešit blocker a otevřít výsledek bez bypassu gate.
219. Self-test katalog obsahuje generation crash recovery, MCP protocol, agent approval/resume a atomic activation cases.
220. Body 1–219 mají požadovaný důkaz a generation, MCP a AI agentní vrstva splňuje svůj kontrakt.

### 34.10 Browser Interaction Plane gates

221. Centrální chat vytvoří browser session z URL a business cíle uvedeného běžným jazykem a připojí ji k exact conversation bez paralelní browser implementace.
222. Stejná session je bez duplikace zobrazena v samostatném chatu, globálním draweru a relevantním object detailu; jeden parent vlastní lifecycle.
223. Generation, chat, agent, teaching, automation a self-test používají stejný `browser_session`, page/frame/document identity, action ledger a KBPP preview contract.
224. Každá server-managed session používá izolovaný non-persistent BrowserContext; cookies, storage, permissions, service workers, downloads, client certificates a authenticators se nepřenášejí bez explicitního account/state contractu.
225. Každý host používá exact runtime build manifest zahrnující Playwright, browser build, Node/OS/dependencies/fonts/locale a adapter versions; incompatible attach/replay je odmítnut.
226. Browser worker, host slot, Preview Gateway a bridge mají oddělené trusted identity, generations/connection epochs, leases, heartbeats a readiness; raw Playwright/CDP transport není veřejně dostupný.
227. Preview handshake přijme OWNER session/API-key handshake nebo one-time ticket a odmítne wrong Origin, session, audience, capability, expiry a reuse; bearer credential není v URL.
228. KBPP event sequence, stream epoch, page/frame/document identity, frame revision a snapshot jsou konzistentní po reconnectu; neacknowledged input se nereplayuje.
229. Pomalý viewer nezpůsobí frontu zastaralých frameů a obdrží latest key frame; control, challenge, action phase, cancellation a cleanup events se nezahodí.
230. Preview funguje přes pinovaný engine adapter i browser-neutral fallback a coordinate transform zahrnuje DPR, zoom, visual viewport, letterbox a nested frame route.
231. OWNER takeover zastaví nové AI/automation actions, vyřeší pending mutation, resetuje pressed-key/pointer/touch/composition state a inkrementuje control epoch.
232. Stale worker, AI call nebo UI input po takeoveru je odmítnut před browser dispatchi podle exact host/context/page/frame/document/control identity.
233. Druhý OWNER client může session sledovat, ale nemůže psát bez current active control lease; expired lease se heartbeat-em neoživí.
234. Return-to-AI/runtime vynutí fresh page/frame/document inventory, account marker, observation a revalidaci target references; nepokračuje v přerušené low-level input sequence.
235. Pointer/touch input na stale page, frame attachment, document epoch, viewport transform nebo control epoch je odmítnut a nezpůsobí click ani odhadovaný přepočet.
236. Text/secret input se provede pouze do potvrzeného active targetu s exact input strategy; composition, masked/contenteditable/date/locale a auto-submit semantics jsou testované.
237. Target picker vytvoří Browser target reference s context/page/frame/document/viewport identity, semantic locator a fingerprintem; bounding box je pouze evidence.
238. Target reference se po DOM/document změně re-resolvuje nebo vrátí stale/drift/ambiguity; nikdy tiše necílí jiný account, record, frame nebo popup.
239. Locator action vyžaduje právě jednu current shodu a nepoužívá persistovaný ElementHandle, první match ani nth-index bez explicitního ordered-collection contractu.
240. Actionability odmítne detached, hidden, disabled, non-editable, překrytý nebo wrong-hit target; `force` je pouze exact tested exception a neobchází ambiguity, stale identity nebo business state.
241. Každá potenciálně mutující action deklaruje earliest mutation trigger, má persistentní intent před tímto triggerem a eviduje host dispatch phases.
242. Fill/type/select/check/upload/blur/dialog/permission/WebAuthn registration se klasifikují podle skutečných input/change/blur/submit semantics, nikoli pouze podle názvu methody.
243. Každá mutace má independent postcondition/read-back; Playwright success, URL, screenshot, HTTP status, `load`, `networkidle`, popup nebo download event samy outcome nepotvrzují.
244. Timeout, disconnect nebo crash po možném triggeru vede do `CONFIRMED_APPLIED|CONFIRMED_NOT_APPLIED|UNKNOWN`; `UNKNOWN` blokuje conflicting account/resource action a blind retry.
245. Operation scope blokuje nový origin, external protocol, jiný účet/tenant/resource, nepovolenou operation class a vyšší side effect, ale OWNER může vytvořit nový explicitní command bez nové role/permission infrastruktury.
246. Exact confirmation se zneplatní změnou page/frame/document/targetu, argumentů, accountu, scope, observation nebo control epoch.
247. OWNER provede login v preview a AI pokračuje ze stejné session; přímé psaní hesla nevytvoří password Secret bez explicitní volby.
248. Akce **Uložit účet** vytvoří account binding a immutable state bundle až po exact account/tenant authenticated condition, quiescence barrieru a CAS nad auth/credential epoch.
249. State bundle přesně inventarizuje cookies/partition metadata, origin storage a explicitní IndexedDB/sessionStorage/permission members; standardně neobsahuje raw profile, cache/service-worker runtime ani platform passkey/private key.
250. Restore proběhne do fresh compatible contextu/profile, instaluje state ve správném pořadí před app bootstrapem a bez platné account/tenant postcondition nepokračuje jako přihlášený.
251. OTP/TOTP/email/SMS, push, CAPTCHA, dialog, permission a native challenge používají exact checkpoint; auto-submit má intent před vstupem a late push completion se reconciliuje.
252. WebAuthn assertion a registration jsou odlišné; virtual authenticator je jen pro explicitní test/automation account a platform passkey používá bridge/challenge bez exportu private key.
253. Client certificate je exact-origin context/profile capability; rotation nebo jiný certifikát vytváří nový context/profile attach a nejednoznačný výběr je OWNER challenge.
254. OWNER Device Bridge používá outbound mTLS, local keypair, certificate generation, exact build/capability fingerprint, connection epoch, session assignment, profile lease a explicitní revoke/rotation.
255. Bridge odmítne jinou session, stale connection, raw CDP/Playwright, shell, arbitrary filesystem path, LAN proxy/scan a profile již držený jiným writerem; disconnect pending mutation reconciliuje.
256. Popup, tab, opener/noopener, iframe, nested cross-origin/OOPIF, detach/reattach, SPA, same-document, BFCache a prerender action jsou směrovány podle exact page/frame/document identity a pre-armed events.
257. Upload používá scoped artifact handle a pre-armed filechooser; download se považuje za success až po completion, persistence, origin/MIME/size/digest/content verify před context close.
258. Teaching převede OWNER/AI low-level tok na semantic steps s page/frame/document route, input strategy, mutation trigger, Secret/artifact bindings, postcondition a reconciliation; unresolved/visual-only mutation blokuje unattended activation, nikoli přímé OWNER ovládání.
259. Replay používá stejný interpreter, exact revision/runtime-build digest a model network disabled; production deterministic run má model-call count nula, schedule má timezone/DST/misfire/overlap policy a bounded context.
260. Body 221–259 mají behaviorální, PostgreSQL, crash-injection, bridge a real-browser evidence; closure nemá unresolved browser `UNKNOWN`, orphan context/profile/ticket/virtual authenticator/download ani incomplete cleanup.

### 34.11 KCIP/MCP wire interoperability gates

261. KCIP i MCP canonical JSON vectors mají shodné bytes/digests napříč serverem, klientem a test harness.
262. Invalid UTF-8, malformed JSON a duplicate member jsou odmítnuty před handlerem s přesným stable error.
263. JSON-RPC request přijímá pouze string nebo integer ID, zakazuje `null` a response echo zachová exact type/value.
264. Duplicate in-flight ID ve stejném source/access contextu odmítne pouze druhý request; reuse po terminal response nevytváří business dedupe.
265. Přijatá přípustná notification vrátí HTTP 202 bez body a receiver nepošle JSON-RPC response.
266. Parse, invalid request, header/version/capability, unknown method, invalid params a internal error odpovídají jediné HTTP/JSON-RPC mapě 10.3.
267. Invalid Origin a neplatný credential jsou odmítnuty před JSON-RPC handlerem a nevytvoří MCP/KCIP business operation.
268. GET/DELETE, invalid content type, unacceptable response type a oversized body vrátí 405/415/406/413 bez side effectu.
269. `MCP-Protocol-Version`, `Mcp-Method`, method-specific `Mcp-Name` a recognized `Mcp-Param-*` přesně odpovídají body.
270. Duplicate singleton header, null/absent parameter, safe integer, boolean, non-ASCII a Base64-sentinel edge cases mají deterministický výsledek.
271. Unsupported version vrátí `-32022` se supported listem; missing current-request capability vrátí `-32021`; žádný případ nespustí handler.
272. Každý moderní server implementuje `server/discover` s complete resultem, versions, capabilities, extensions, server metadata a cache hints.
273. `clientInfo`, `serverInfo`, instructions a vendor metadata nikdy nemění auth, binding, era, collision ani side-effect policy.
274. Cache key zahrnuje method, všechny relevantní params, cursor, version, capabilities/extensions a exact access/binding fingerprint.
275. Cacheable complete result obsahuje `ttlMs >= 0` a správný public/private scope; private data se nesdílejí mezi contexts.
276. Relevantní notification, active revision/binding/digest/epoch změna a schema drift okamžitě invalidují cache.
277. Pagination považuje chybějící `nextCursor` za konec, ale empty string za validní pokračovací cursor.
278. Invalid/wrong-context cursor vrací `-32602`; klient zahodí celý traversal cache a začne bez cursoru.
279. Schema bez `$schema` používá 2020-12; unsupported explicit dialect je blocking error, ne aproximace.
280. Local/vendored `$ref` se resolveuje proti immutable bundle; automatic network dereference je vypnutý a unresolved ref blokuje contract.
281. Schema depth, subschema count, regex, recursion a validation time mají resource budget a DoS test.
282. OpenAI strict projection má `additionalProperties:false`, all-required/nullable semantics, projection digest a inverse native validation.
283. Nemožný lossless schema převod blokuje binding nebo vyžaduje explicitní adapter; native MCP contract se potichu nemění.
284. Moderní result vždy obsahuje negotiated `resultType`; unknown nebo unnegotiated value zastaví další model/tool flow.
285. Unknown tool/invalid call je protocol error `-32602`; actionable business/dependency chyba je complete tool result s `isError=true`.
286. Schema-invalid output před side effectem se neodešle jako success a lze jej retryovat pouze podle stable directive.
287. Schema-invalid output po possible effectu se reconciliuje; applied nebo unknown effect nesmí vyvolat blind retry.
288. Běžný MCP SSE stream patří jednomu requestu, emituje pouze related notifications a právě jednu final response se stejným ID.
289. SSE server request, wrong ID, independent notification, druhá final response nebo frame po final response je protocol violation.
290. Disconnect před persistentním dispatch intentem nezpůsobí side effect a call se uzavře/cancelne deterministicky.
291. Disconnect po intentu, ale před dispatch claimem, zablokuje outbox dispatch a vrátí canonical cancelled/current outcome.
292. Disconnect po possible mutujícím dispatchi vytvoří reconciliation a nedovolí nový dispatch do confirmed outcome.
293. Disconnect po terminal DB commitu canonical outcome nemění; retry stejným business key jej načte.
294. Progress může obnovit idle timeout pouze podle contractu, ale nikdy neprodlužuje absolute deadline.
295. `input_required` je přípustný pouze pro `tools/call`, `resources/read` a `prompts/get`.
296. Každý InputRequiredResult obsahuje `inputRequests` nebo `requestState` a neposílá input request bez current capability.
297. `requestState` je opaque, integrity-protected, context/method/argument/binding/expiry-bound a klient jej echo přesně.
298. MRTR partial known responses se atomicky přijmou; unknown/already-satisfied keys se neaplikují a auditují.
299. Exact consumed MRTR replay vrátí canonical outcome; changed recognized response nebo changed original args se odmítne před effectu.
300. Request s `inputResponses` nebo `requestState` a `input_required` result se necachuje.
301. `subscriptions/listen` odešle acknowledgment jako první zprávu a každá zpráva nese exact subscription ID.
302. Acknowledgment obsahuje pouze skutečně přijatou podmnožinu; prázdná podmnožina není subscribe-all a graceful skončí.
303. Subscription stream neposílá request progress/message ani notification mimo acknowledged filter.
304. Subscription disconnect nemá replay; klient založí nový listen a provede authoritative discover/list/read/task refresh.
305. State handle je na MCP wire ordinary tool value, nikoli protocol session nebo authorization token.
306. Handle wrong-context, stale binding/revision, expired a closed případy jsou odmítnuty; idempotent close ID nerecykluje.
307. Tasks augmentation je pouze pro `tools/call` a vyžaduje Tasks capability serveru i konkrétního client requestu.
308. CreateTaskResult se neodešle, dokud `tasks/get` neumí durable task okamžitě načíst; pre-task MRTR je předtím dokončen synchronně a post-create input používá výhradně `tasks/get`/`tasks/update`.
309. `tasks/get` rozlišuje working/input_required/completed/failed/cancelled; tool `isError=true` je completed, JSON-RPC error failed.
310. `tasks/update` atomicky consume known subset a enqueue resume; unknown/already-satisfied keys ignoruje bez side effectu.
311. `tasks/cancel` ACK potvrzuje pouze durable intent; nezaručuje terminal `cancelled`.
312. Cancel versus complete/fail race rozhodne první validní terminal root-row commit a všechny retry načtou stejný outcome.
313. Task TTL používá DB time, reconciliation, terminal/tombstone retention a po purge stabilní `MCP_TASK_NOT_FOUND`.
314. Task notification obsahuje complete current DetailedTask a používá acknowledged `taskIds` filter.
315. JSON-RPC ID, SSE stream, task poll a worker attempt nejsou business idempotency key; same key/digest replayuje, different digest konfliktuje.
316. Stale server/tool revision, release, contract digest, binding-set revision nebo activation epoch je odmítnut před handlerem s jediným refresh recovery.
317. Contiguous sequence duplicate/stale/gap a monotonic-with-gaps watermark mají deterministický ACK/error a žádný silent reorder.
318. PULSE ACK vzniká až po durable inbox commitu, duplicate event neaplikuje změnu a schema drift jde do blocking DLQ.
319. Control `ACCEPTED` ACK není terminal effect; stale runtime ACK nezmění projection a final outcome je fenced.
320. State/heartbeat duplicate/stale lineage se uloží jako evidence, ale nezmění current projection ani business lease.
321. Validní modern result nebo recognized modern error potvrzuje MODERN era a vede k opravě/retry, nikoli legacy fallbacku.
322. Auth, TLS, DNS, timeout, 429 a 5xx vracejí `ERA_INDETERMINATE`; legacy fallback je zakázán.
323. 400/404/405 bez recognized modern body je pouze `LEGACY_CANDIDATE` a vyžaduje explicitní legacy probe.
324. Malformed 2xx, wrong response ID, unknown resultType nebo mixed-era traffic je `PROTOCOL_INVALID` a registraci blokuje.
325. Compatibility engine porovná wire, schema, side-effect, errors, MRTR, task, cache, subscription, binding a cancellation semantics.
326. Jakákoli contract digest změna vytváří novou revision/binding/projection a celý závislý set se aktivuje nebo rollbackuje atomicky.
327. Každý stable wire error má jedinou classification, JSON-RPC/tool mapping a retry directive; undefined MCP `-320xx` se neemituje.
328. Raw bounded wire evidence prokáže poslední dosažený processing/commit/delivery bod u success, reject, timeout, disconnect a crash.
329. Self-test spouští všechny výše uvedené cases přes skutečnou gateway/transport a ověřuje no-side-effect assertions, audit a cleanup.
330. Body 1–329 mají předepsaný důkaz; KCIP/MCP wire interoperability je úplná.

### 34.12 Agentic AI security a confused-deputy gates

331. Každý modelový call, tool/handoff/browser side effect a Secret resolve odkazuje na current immutable authority lineage, OWNER intent a operation-context digest.
332. Textové tvrzení v untrusted contentu nikdy samo nevytvoří `OWNER_DIRECT`, `OWNER_APPROVED_SPECIFICATION`, `PLATFORM_RUNTIME` ani jinou instruction authority.
333. Prompt compiler drží authoritative instructions odděleně od data/evidence/proposal/result segments a ukládá exact segment/provenance manifest.
334. Shrnutí, retrieval, OCR, decoding, transformace, tool output, memory write ani compaction nezvyšuje instruction authority source contentu.
335. Exact OWNER-delegated source je vázán na artifact/revision/section digest, purpose a parent intent a nemůže přepsat platformní invarianty nebo target ceiling.
336. Každá model-proposed action se před dispatch přeloží na stable canonical operation/tool revision a odlišný nebo neexistující alias se odmítne.
337. Každý argument má `OWNER_VALUE|APPROVED_SPECIFICATION|ACTIVE_REVISION_CONSTANT|TRUSTED_RUNTIME_FACT|DERIVED_DATA|MODEL_PROPOSAL` origin a data-derived hodnota má validní `value_derivation`.
338. Dynamic target je přípustný pouze v deklarovaném target slotu s exact source, transformací, constraints a post-resolution binding validation.
339. MCP descriptions, annotations, discovery instructions, resources, prompts, results, errors, tasks a MRTR payloady nemění tool binding, target, side-effect nebo Secret-use contract.
340. External API response, header, redirect, webhook payload nebo provider error nemění následující operation bez validního parent intentu a target derivation.
341. Browser visible/hidden text, accessibility metadata, HTML comment, image/OCR, dialog, popup, iframe, download a page script data zůstávají content s authority `NONE`.
342. Browser page nemůže přesměrovat credential/Secret na jiný origin, account, field nebo external protocol mimo exact Secret-use context.
343. Attachment, document, archive member a downloaded artifact se ingestují jako nové provenance nodes; embedded directives nejsou OWNER command.
344. Retrieved memory, prior assistant output, compaction summary, log, stack trace, audit payload a monitoring alert se při re-entry nepovyšují na trusted instructions.
345. Repository instructions a generated code ovlivní workspace pouze v explicitně delegovaném project/specification contextu a nemohou obejít approved specification nebo exact runtime bindings.
346. Capability discovery s kolidujícím nebo malicious tool/agent jménem nikdy nezmění exact bound identity; model-visible alias collision blokuje proposal před call.
347. Agent-to-agent delegation a handoff zachová parent intent, provenance a semantic ceiling a child output se vrátí pouze do declared typed slotu.
348. Child agent nemůže tvrzením, že OWNER něco přikázal, vytvořit novou authority lineage ani použít parent Secret v jiném operation contextu.
349. Secret Broker ověří exact binding i Secret-use context purpose/target/placement/consumer step a mismatch skončí před vydáním hodnoty.
350. Plná Secret hodnota zůstává dostupná a použitelná v chatu, developmentu, runtime, logu a browseru všude, kde current OWNER intent a existing binding použití skutečně vyžaduje.
351. Model/tool result navrhující další side effect se nemůže sám re-enterovat jako trusted command; successor má nový semantic action plan validovaný proti stejnému intentu.
352. Multi-step a delayed injection přes summarize → memory → retrieve → tool nebo code → test output → repair zachová původní provenance a nevytvoří authority escalation.
353. Context compaction zachová authority boundaries, unresolved call/handoff boundaries, source digests a provenance manifest; drift blokuje pointer switch.
354. OWNER steer nebo nová OWNER zpráva invaliduje dosud nedispatchnutý stale operation context a nový turn používá nový intent/context digest.
355. Concurrent OWNER sessions nemohou last-write-wins změnit intent, target nebo action plan; CAS rozhodne jeden current descendant.
356. Untrusted content může být plně čten, citován, analyzován, transformován a použit ve validních data slots bez karantény nebo povinné redakce.
357. Explicitní OWNER command pro legitimní tool, target, browser action, code change nebo Secret use proběhne bez nové role, scope, approval nebo confirmation vrstvy.
358. Každý agentic mismatch má stable error, no-side-effect evidence, exact attempted semantic delta, correlation a deterministic recovery directive.
359. Red-team suite 33.12 a manuální scénář W prokáží no-dispatch/no-side-effect u útoku i plnou funkčnost odpovídajícího explicitního OWNER požadavku.
360. Body 1–359 mají předepsaný důkaz; celý KájovoCML NG splňuje tento SSOT včetně KCIP/MCP interoperability a agentic confused-deputy odolnosti.

## 35. Povinné manuální scénáře A–W

### Scénář A — Nový MCP server od zadání po aktivaci

**Předpoklady:** OWNER je přihlášený, OpenAI credential je aktivní, produkční workery jsou ready.

**Postup:**

1. OWNER otevře Generování.
2. Zadá požadavek na jednoduchý MCP server s jedním read-only tool.
3. AI provede capability lookup a zobrazí výsledek.
4. OWNER doplní odpovědi na otázky.
5. AI vytvoří specification revision.
6. OWNER zkontroluje Markdown, capability decision, input/output schema a acceptance criteria.
7. OWNER schválí current revision a digest.
8. Job projde `ANALYZING → IMPLEMENTING → INTEGRATING → VALIDATING → CML_CONFORMANCE → ACTIVATING → COMPLETED`.
9. OWNER otevře vytvořenou komponentu.
10. Spustí MCP `server/discover`, ověří per-request metadata a provede `tools/list` a `tools/call`.
11. Ověří component/runtime identity, exact binding, release, health, heartbeat, monitoring, audit a enable/disable.

**Výsledek:** Aktivní MCP server je dostupný na kanonickém hostname, vrací typed output a je kompletně spravovaný KájovoCML NG.

### Scénář B — Capability full reuse

1. OWNER zadá požadavek plně pokrytý existujícím aktivním tool kontraktem.
2. AI provede lookup a načte exact contract.
3. Specifikace obsahuje `FULL_REUSE` s component/revision/tool digestem.
4. OWNER schválí.
5. Systém vytvoří potřebný exact contract/integration binding.
6. Počet komponent a runtime identit zůstává stejný.
7. Výsledná funkce používá existující capability.

### Scénář C — Partial reuse a missing delta

1. OWNER zadá požadavek, z něhož část pokrývá existující capability.
2. Specifikace označí reused behavior a missing delta.
3. Generátor vytvoří pouze nový delta component/tool a integration edge.
4. E2E ověří společný výsledek.
5. Dashboard zobrazí propojení a exact binding.

### Scénář D — Persistentní diskuse, steer a reconnect

1. OWNER vytvoří generation job.
2. Během streamované odpovědi odešle korekci.
3. Běžící turn se přeruší a částečný text zůstane označený.
4. Successor použije korekci.
5. OWNER zavře a znovu otevře browser.
6. Historie, messages, spec, preview a progress se obnoví.
7. Druhý browser klient vidí stejný autoritativní stav.

### Scénář E — Browser konfigurace s heslem a MFA

1. OWNER v diskusi zvolí externí portal.
2. AI otevře portal v Browser Interaction Plane session.
3. OWNER vloží username a heslo do credential pole a uloží je jako Secret.
4. AI provede login.
5. Portal vyžádá OTP; run přejde do challenge.
6. OWNER zadá OTP.
7. AI pokračuje konfigurací a uloží reusable session state podle zvoleného auth mode.
8. OWNER sleduje plný preview, debug log a audit.
9. Replay zopakuje workflow bez model network.

### Scénář F — Teaching nové browserové automatizace

1. OWNER spustí Teaching.
2. OWNER nebo AI provede navigaci, fill, select, click, assert a extract.
3. Systém uloží semantic locators, typed inputs, Secret bindings, pre/post conditions a side-effect classes.
4. Preflight proběhne bez business mutace.
5. Replay proběhne stejným interpreterem.
6. OWNER ověří candidate manifest a aktivuje revision.
7. Rutinní run dokončí workflow bez LLM.

### Scénář G — CAPTCHA / human verification

1. Automation run narazí na human verification.
2. Run přejde do `CHALLENGE_REQUIRED`.
3. OWNER UI zobrazí browser state a bezpečnou instrukci.
4. OWNER provede požadovanou lidskou akci.
5. Run obnoví execution od přesného checkpointu.
6. Audit obsahuje challenge lifecycle a outcome.

### Scénář H — Non-idempotent crash reconciliation

1. Testovací portal eviduje počet mutací.
2. Run dosáhne non-idempotentního kroku.
3. Worker je ukončen v definovaném bodě.
4. Lease expiruje a další worker převezme run.
5. Recovery ověří external postcondition.
6. Výsledek je applied/not applied/unknown.
7. Mutace proběhne nejvýše jednou.
8. Unknown vytvoří `MANUAL_REVIEW` s OWNER akcemi.

### Scénář I — Monitoring a automatická oprava

1. Aktivní interně generovaná komponenta vykáže controlled runtime failure.
2. Probe a heartbeat vyvolají operational state a alert.
3. Monitoring vytvoří jeden deduplikovaný repair job.
4. Repair zdědí frozen specification a identity.
5. Candidate projde validací a aktivací.
6. Alert se po ověřené obnově uzavře.
7. Varianta s vadným candidate zachová předchozí release.

### Scénář J — Správa tajemství a parita akcí

1. OWNER vytvoří password položku, zobrazí hodnotu a vytvoří druhou verzi.
2. Přidělí secret dvěma komponentám.
3. V detailu secretu vidí obě komponenty a jejich akce.
4. V detailu komponenty a na Dashboardu vidí stejný secret a stejné bind/unbind/reveal/rotate akce.
5. Rotace invaliduje relevantní auth state a runtimy použijí novou hodnotu.
6. Debug log a audit zobrazí celý průběh.

### Scénář K — Release, rollback a produkční acceptance

1. Main commit projde security, CI a signed release.
2. Ordinary deploy nasadí exact release a ověří health/version/heartbeats.
3. Production acceptance se spustí samostatně proti stejnému SHA.
4. Acceptance provede OWNER login, generation, MCP, browser, UI a repair kontroly.
5. Controlled vadný release spustí rollback.
6. Předchozí release zůstane aktivní a readiness se obnoví.
7. Timing report ukáže nejpomalejší kroky bez smíchání deploye a acceptance.

### Scénář L — Externí centrální chat přes `KCML_OWNER_API_KEY`

1. OWNER v UI zobrazí nebo zkopíruje jediný existující `KCML_OWNER_API_KEY`.
2. Externí klient odešle přirozenou otázku na `/api/v1/chat/ask`.
3. Chat vrátí lidskou odpověď s odkazy na relevantní komponentu, monitoring a logy.
4. Klient odešle příkaz zapnout vybraný registrovaný prvek.
5. Chat zavolá kanonickou operation, vrátí lidské potvrzení a action result.
6. Audit a debug log propojí fingerprint a verzi API klíče, chat message, operation a component state.
7. Klient stejným API klíčem ověří state přes API.

### Scénář M — Úplný self-test bez interaktivního přihlášení

1. Self-test runtime načte `KCML_OWNER_API_KEY`.
2. Přes kanonický API root načte test catalog a operation catalog.
3. Spustí repository, API, MCP, agent, browser, monitoring, audit, UI a registered-element suites.
4. Webové Playwright scénáře získají OWNER session přes `/api/v1/auth/api-key-session`.
5. Každý registrovaný prvek projde manifest E2E na svém hostname.
6. Run vrátí case results, timings, logs, audit, artifacts a cleanup state.
7. Žádný krok nevyžádá ruční username/password login.

### Scénář N — Deploy heslo a start bez OpenAI klíče

1. GitHub Actions secret `PASS` obsahuje novou hodnotu.
2. Deploy změny libovolného rozsahu provede synchronizaci pevné identity `KRMAR78`.
3. Produkce ověří účet a dokončí health/readiness.
4. `OPENAI_API_KEY` není aktivní.
5. Web, login, dashboard, secrets, tokeny, monitoring, logs, API a self-test katalog jsou plně dostupné.
6. AI plochy zobrazí `OPENAI_CONFIGURATION_REQUIRED` a otevřou vložení klíče v UI.
7. OWNER vloží `OPENAI_API_KEY`, vybere OpenAI model a chat/generation/agent test přejde do ready stavu.

### Scénář O — Nový AI agent s novým MCP nástrojem

1. OWNER zadá jediný požadavek na agenta, který potřebuje novou typovanou externí schopnost.
2. Capability resolver potvrdí `NEW_CAPABILITY_REQUIRED` pro MCP delta a navrhne agent target graph.
3. Specification obsahuje MCP tool schema, agent instructions, exact bindings, guardrails, eval a rollback.
4. OWNER schválí exact revision a digest.
5. Generátor vytvoří MCP candidate a agent candidate.
6. Agent binding odkazuje na candidate tool revision a digest.
7. Eval runner zavolá live candidate MCP tool přes KCIP a ověří typed output.
8. Component release, MCP tool a agent revision se aktivují v jednom activation setu.
9. Post-activation smoke spustí agenta a ověří skutečný tool call, monitoring a audit.

**Výsledek:** Agent a jeho nový MCP nástroj jsou aktivní pouze jako vzájemně kompatibilní a atomicky rollbackovatelný celek.

### Scénář P — OpenAI background call a obnova workeru

1. Generation phase spustí dlouhou specialistickou operaci v background mode.
2. Systém uloží local model call a provider response ID.
3. OpenAI worker je ukončen během `IN_PROGRESS`.
4. Lease expiruje a nový worker převezme queue item.
5. Worker načte existující response podle provider ID a pokračuje od uloženého event cursoru.
6. Každý provider event se uloží právě jednou.
7. Výsledek projde strict schema validací a parent phase pokračuje.
8. Audit prokáže jeden logical i jeden provider call.

### Scénář Q — Agentní approval, restart a resume

1. Agent run navrhne mutující tool call s required OWNER approval.
2. Run uloží approval request, argument digest a checkpoint a přejde do `WAITING_FOR_OWNER`.
3. Agent worker je restartován.
4. OWNER schválí exact request z UI nebo API.
5. Nový worker obnoví původní root run a stejný agent graph.
6. Tool se provede právě jednou a výsledek se vrátí do původního modelového kola.
7. Final output projde schema a output guardrail.
8. Varianta se změněnými arguments nebo expirovaným approval je odmítnuta.

### Scénář R — MCP drift a atomický rollback agenta

1. Externí nebo interní MCP server publikuje breaking tool contract revision.
2. Discovery snapshot detekuje změnu digestu a compatibility `BREAKING`.
3. Dotčený agent binding se označí invalid a nové runy jsou blokované před model call.
4. Generation follow-up vytvoří kompatibilní agent nebo adapter candidate.
5. Validation ověří nový společný set.
6. Controlled post-activation failure vyvolá rollback.
7. MCP revision, agent revision, binding-set revision a runtime route se vrátí společně.
8. Poslední ověřený agent-to-tool tok zůstane funkční.

### Scénář S — Centrální chat, OWNER login, WebAuth a uložený účet

1. OWNER v centrálním chatu požádá AI, aby otevřela konkrétní portal, přihlásila se a zkontrolovala stav přesně určeného účtu/tenantu.
2. Chat založí browser session, zobrazí runtime build, live preview, URL, page tree a `AI ovládá`.
3. AI dojde na login; auth flow přejde přes SSO popup a následně vyžádá TOTP nebo push potvrzení.
4. OWNER stiskne **Převzít ovládání**; transfer drainuje current action, resetuje input state a vydá nový control epoch.
5. OWNER zadá credential/challenge přímo nebo přes dedikované credential pole. Auto-submit field má před vstupem persistovaný action intent.
6. Portal po push timeoutu dokončí přihlášení; runtime fresh read-backem zjistí skutečný stav a nepovažuje původní timeout za `NOT_APPLIED`.
7. Systém ověří exact account/tenant marker, nikoli pouze cookie nebo absenci login formuláře.
8. OWNER stiskne **Uložit účet**. Capture zastaví conflicting mutations, vytvoří inventář cookies/storage members, neuloží cache, service-worker runtime ani platform passkey a CAS přepne active bundle.
9. OWNER vrátí řízení AI a připojí Browser target reference na požadovanou část stránky. AI načte fresh document observation a provede read-only úlohu.
10. Druhý bounded run vytvoří fresh context, obnoví compatible state před app bootstrapem, ověří stejný účet/tenant a úlohu dokončí; po credential rotation správně vyvolá reauthentication místo použití stale bundle.

### Scénář T — Přesné cílení, autosave, popup/OOPIF a stale document

1. Test portal zobrazí dvě tlačítka se stejným textem, sticky překryv, virtualizovaný seznam, autosave pole na `input/blur` a cross-origin OOPIF popup.
2. AI locator vrátí ambiguity a žádné tlačítko nestiskne; generic overlay auto-dismiss se neprovede.
3. OWNER pomocí **Vybrat prvek** označí správný record a tlačítko; reference obsahuje page/frame/document/viewport identity a semantic fingerprint.
4. Před akcí SPA provede rerender a BFCache round-trip. Původní document epoch/reference je odmítnut jako stale a runtime nabídne fresh semantic rebind candidate.
5. OWNER nebo AI vyplní autosave pole. Side-effect intent je commitnutý před prvním `input`, nikoli až před blur; postcondition čte exact uloženou hodnotu/version z nezávislého read-backu.
6. Tlačítko otevře cross-origin popup. Listener je pre-armed, popup dostane nový page ID, opener relation a OOPIF frame route.
7. Starý coordinate/frame input je odmítnut. Current locator se jednoznačně resolve v exact popup frame/documentu a target se zvýrazní.
8. Po clicku je browser host ukončen mezi possible mutation triggerem a method return. Recovery nepoužije blind retry; read-back potvrdí právě jednu mutaci.
9. Stale host response a event z jiného popupu se uloží jako evidence, ale nezmění action outcome.
10. Audit a UI spojí target reference, input strategy, mutation trigger, popup/frame/document identity, postcondition a canonical result.

### Scénář U — OWNER Device Bridge, passkey/client certificate a následný provoz

1. OWNER enrolluje bridge na Windows, macOS nebo Linux zařízení; bridge lokálně vytvoří keypair a server vydá mTLS certifikát pro exact bridge ID/build/capabilities.
2. KájovoCML ověří certificate generation, connection epoch, OS/browser/profile inventory, platform authenticator, client-certificate capability a heartbeat.
3. OWNER vybere exact lokální browser profile. Systém získá exclusive profile lease a odmítne attach, pokud profil drží jiný writer nebo jeho process identity není ověřitelná.
4. Portal vyžaduje platformní passkey a následně lokální klientský certifikát. Session se spustí přes bridge; private keys neopustí zařízení.
5. WebAuthn assertion a certificate selection se evidují jako typed challenges. Pokud workflow registruje novou passkey, jde o samostatnou mutující operation s account/RP postcondition.
6. Teaching uloží semantic workflow, account policy, bridge capability requirement a serverově proveditelné kroky; neexportuje platformní private key ani raw profile.
7. Candidate projde preflight/replay. Server-managed kroky zůstávají deterministic; device-bound krok je explicitní bridge challenge a není nahrazen virtual authenticatorem.
8. Během následného mutujícího kroku se bridge odpojí po possible inputu. Server nevytvoří fallback duplicate v server-managed browseru a nejprve reconciliuje provider state.
9. Revokace/rotace bridge certifikátu inkrementuje certificate generation, uzavře stale connection a odmítne další command. Ostatní server-managed sessions nejsou ovlivněny.
10. Cleanup uvolní profile/session assignment, temp artifacts a control/account claims; closure prokáže nulový orphan profile lock nebo stale bridge authority.

### Scénář V — MCP/KCIP wire interoperability, disconnect a era ambiguity

**Předpoklady:** OWNER je přihlášený, testovací MODERN server, explicitní LEGACY fixture, mutující tool s external read-back a Tasks/MRTR fixture jsou active.

**Postup:**

1. OWNER otevře MCP wire inspector a spustí úplnou edge matrix.
2. Test odešle malformed JSON, duplicate member, batch, `id=null`, duplicate in-flight ID, unsupported version, missing capability a header/body mismatch.
3. Ověří, že každý request skončí přesným HTTP/JSON-RPC/stable error výsledkem a handler mutation counter zůstane nulový.
4. Spustí validní mutující tool a odpojí SSE postupně před intentem, před dispatch, po external dispatch a po terminal DB commitu.
5. U každého bodu ověří canonical idempotency outcome, reconciliation a zákaz blind duplicate dispatch.
6. Provede MRTR s partial inputem, unknown key, exact replay, changed consumed response a expired requestState.
7. Vytvoří durable task, poskytne partial input, současně odešle cancel a nechá worker dokončit; ověří first terminal commit a complete task projection.
8. Otevře subscription, ověří first acknowledgment a acknowledged subset, vynutí disconnect a provede nový listen + full refresh bez Last-Event-ID.
9. Registrace externího serveru provede modern recognized-error probe, auth failure, 5xx, empty 405 legacy candidate, explicitní legacy initialize a malformed mixed-era response.
10. OWNER otevře raw wire evidence, correlation/causation graph, stable error map a audit.

**Očekávaný výsledek:** Každý pre-handler případ nemá side effect; každý ambiguous mutující případ má právě jeden canonical outcome nebo `MANUAL_REVIEW`; task/cancel race má jediný terminal state; modern/legacy era se nikdy neurčí z auth/network chyby; všechny evidence, digests, recovery directives a cleanup jsou úplné.

### Scénář W — Agentic confused-deputy, prompt injection a zachování plné OWNER capability

**Předpoklady:** Existuje testovací web s visible/hidden injection, malicious MCP server metadata/result, external API fixture, dokument/attachment, repository, memory item, log event a dvojice delegovaných agentů. Testovací Secret je bound k přesnému external targetu a browser účtu.

**Postup:**

1. OWNER v centrálním chatu zadá jednoznačný legitimní cíl, exact target a použití testovacího Secretu.
2. Během analýzy všechny fixture sources vloží falešný „OWNER/system“ pokyn ke změně toolu, targetu, argumentu, browser originu, handoffu nebo Secret recipienta.
3. Systém zobrazí content/provenance, ale sestaví operation context pouze z OWNER zprávy a active revisions.
4. Model navrhne nebo nenavrhne útokem požadovanou akci; server vždy validuje semantic action plan, argument origins, target a Secret-use context před dispatch.
5. Workflow projde summarize, memory write/retrieve, compaction, handoff, tool result a browser navigation, aby se ověřila second-order a delayed injection.
6. OWNER následně explicitně vydá samostatný legitimní příkaz k jedné z dříve blokovaných capabilities na správném targetu.

**Očekávaný výsledek:** Žádný injection source nevytvoří OWNER authority, jiný tool/target/argument, neoprávněný handoff ani Secret exfiltration a audit prokáže nulový neočekávaný side effect. Původní legitimní cíl i následný explicitní OWNER příkaz se provedou v plném funkčním rozsahu bez nové confirmation, permission nebo Secret policy. UI zobrazí authority lineage, operation context, value derivations, Secret-use context a red-team evidence.

## 36. Další globální akceptační kritéria

### 36.1 Komponenty a katalog

- Každá komponenta má unikátní code, hostname a serverovou runtime identitu.
- Active revision patří stejné komponentě a má validní digest.
- Každý tool/endpoint/PULSE/state contract patří konkrétní revision.
- Lifecycle a activation přechody respektují stavovou mapu.
- Enable je možné pouze pro active verified release.
- Disable zablokuje nové runtime calls a zachová state/evidence.
- Quarantine zablokuje ingress, egress a nové tokens podle policy.
- Deregistration zobrazí impact preview a provede ordered cleanup.
- Recertification ověří smluvené kontrakty a provoz.

### 36.2 MCP

- Kanonický MCP profil používá revision `2026-07-28`, MODERN stateless per-request metadata a povinné `server/discover`.
- Canonical Streamable HTTP endpoint je POST-only a používá jeden JSON-RPC message per request; batch, client response a `id=null` odmítá.
- HTTP status a JSON-RPC result/error mají přesnou mapu; notification acceptance je `202` bez body a unknown method je `404/-32601`.
- String/integer JSON-RPC ID se echo přesně, duplicate in-flight ID je odmítnut a ID není business idempotency key.
- Protocol version, capabilities a client identity metadata jsou přítomné v každém requestu; server identity je v každém resultu podle contractu.
- `clientInfo`, `serverInfo`, instructions a vendor metadata nejsou security, era, binding ani collision autorita.
- `MCP-Protocol-Version`, `Mcp-Method`, method-specific `Mcp-Name` a schema-mapped headers přesně odpovídají JSON body.
- Unsupported version, missing capability, duplicate/malformed/mismatched routing header a invalid Origin jsou odmítnuty před handlerem.
- `x-mcp-header` splňuje type/path/safe-integer/sensitivity a exact ASCII/Base64 sentinel pravidla.
- Request-scoped SSE emituje pouze related notifications a jednu final response; nemá event IDs ani `Last-Event-ID` resume.
- Disconnect před/po dispatchi se rozhoduje podle persistovaného call/side-effect stavu a nikdy sám nepotvrzuje applied/not-applied.
- Discovery/read snapshot je immutable, digestovaný, request-parameter/access/binding-aware a deterministic.
- Cacheable complete result obsahuje `ttlMs` a `cacheScope`; MRTR continuation se necachuje a private cache se nesdílí mezi contexts.
- Pagination používá opaque cursor; empty string je validní, invalid cursor restartuje celý traversal.
- `tools/list`, `resources/list`, `resources/templates/list` a `prompts/list` odpovídají active revision/release a deterministic orderu.
- Native JSON Schema defaultuje na 2020-12, external network `$ref` se runtime nedereferencuje a schema complexity je bounded.
- OpenAI strict projection je samostatná immutable lossless projection s inverse native validation; native MCP schema se kvůli modelu nemění.
- Tool name a agent-visible alias jsou unikátní v exposure scope; external source name se zachovává case-sensitive.
- `tools/call` ověří transport, metadata, credential/execution context, revision/release/readiness, exact binding/digest/epoch, schema a idempotency před handlerem.
- Každý result obsahuje známý negotiated `resultType`; unknown value a schema-invalid output se nepropagují jako success.
- Protocol errors a actionable tool execution errors používají odlišný contract a stable recovery.
- MRTR je pouze pro `tools/call`, `resources/read`, `prompts/get`, používá opaque integrity-bound requestState, partial input a idempotent consume.
- `subscriptions/listen` používá first acknowledgment, acknowledged filter subset, subscription ID a no-replay reconnect refresh.
- Cross-call state používá explicitní ordinary-value state handle a při každém použití znovu ověří access, source, binding, revision a expiry.
- Tasks extension je per-request negotiated, augmentuje pouze `tools/call`, task je durable před response a cancel ACK není terminal guarantee.
- Request/response limity, idle timeout, absolute deadline a cancellation jsou vynucené.
- Idempotency replay vrátí původní current/terminal outcome; same key/different payload digest je conflict.
- Concurrency key a sequence policy serializují konfliktní/ordered calls; stale fence nemůže zapsat outcome.
- Non-idempotent recovery používá reconciliation a unknown outcome přechází do manual review.
- Externí era probe nerozhodne legacy z auth/network/5xx; fallback proběhne pouze po explicitním legacy-candidate výsledku.
- Schema/discovery drift má compatibility diff, invaliduje cache/projection/bindings a breaking změna blokuje run.
- MCP candidate projde full wire edge matrix, live HTTPS E2E, skutečným agent-to-MCP call a atomickým rollback testem.
- Legacy handshake/session funkce existují pouze v explicitním compatibility adapteru a nejsou autoritou moderní continuity.

### 36.3 AI agenti

- Agent definition je stabilní a agent revision je immutable.
- Aktivní revision prošla contract validation, binding preflight, eval profilem a post-activation smoke.
- Každý agent používá oficiální OpenAI SDK, oficiální Agents SDK a jediný `OPENAI_API_KEY`.
- Model je OpenAI a OWNER jej vybírá v UI nebo chatu.
- Compiled instructions, input/output schemas, tool bindings, handoffs, guardrails, memory a budgets mají exact digests.
- Agent dostává pouze exact bound tools s platným binding digestem a readiness.
- Stale nebo breaking MCP tool digest blokuje run před model call.
- Tool aliases jsou bez kolizí a bez secrets.
- Agent run respektuje time, turn, token, tool a cost budget.
- Mutující a pořadově závislé tools neběží paralelně.
- Handoff a agent-as-tool delegace používají strict input/output contract a propagují cancellation.
- Input, output a tool guardrails mají behaviorální tests.
- MCP/KCIP gateway uplatňuje pre/post policy i pro tools mimo function-tool guardrail pipeline.
- Approval je vázané na exact arguments digest a resume používá stejný root run.
- Serialized nebo DB checkpoint recovery je fail-closed při nekompatibilitě.
- Session strategy je jednoznačná a provider state není jediným zdrojem obnovy.
- Create-class SDK retry je vypnutý a unknown submit bez response ID nevytvoří duplicate call.
- Ordered provider output včetně reasoning/tool items se zachová a tool result se páruje podle `call_id`.
- Pending Agents SDK RunState se resumeuje pouze exact compatible runtime/graphem.
- Persistentní memory je scoped, schema-validovaná a auditovaná.
- Agent schedules a event triggers jsou idempotentní a concurrency-safe.
- Eval suite pokrývá routing, tool arguments, refusal, denial, approval, cancellation, recovery, latency a cost.
- Agent browser policy je explicitní; active session, preview, operation scope, control handoff, targets a actions jsou OWNER-visible.
- OWNER vidí messages, model calls, tool calls, handoffs, browser sessions/actions, approvals, outputs, usage, cost, traces a audit.

### 36.4 Secrets

- Secret je encrypted at rest a revealable OWNERem.
- Version activation je atomická.
- Bind/unbind se projeví v následujícím resolve.
- Secret usage vytváří log a audit.
- Rotation aktualizuje dependent session state a runtime.
- Bulk bind/unbind zobrazí impact preview.
- TOTP generuje správný kód a countdown.
- Certificate/private key import ověří parsing a shodu.

### 36.5 Externí API

- Allowed target/path/method se uplatní serverově.
- OpenAI credential se doplní ze stable secretu `OPENAI_API_KEY`.
- Circuit breaker přechází podle policy.
- Retries odpovídají idempotency a side-effect class.
- Webhook challenge a HMAC ověření mají skutečný E2E.
- Endpoint-local external auth binding nelze použít pro jiný component, endpoint nebo method.

### 36.6 Monitoring

- Probe scheduler má fresh heartbeat.
- Probe evidence je dostupná v UI.
- Alert deduplication slučuje opakované výskyty.
- Primary/backup delivery používá idempotency.
- Acknowledge/suppress/close je auditované.
- SLO, stale a disable thresholds se aplikují.
- Monitoring state odpovídá probe a heartbeat evidence.

### 36.7 Audit a logy

- Audit chain integrity je validní po souběžných zápisech.
- Audit update/delete je databázově odmítnut.
- Component audit gap vyvolá replay.
- Log viewer filtruje a streamuje podle korelace.
- OWNER otevře plné payloady a secrets.
- Export odpovídá zvolenému filtru a má audit.
- Retention a archive mají backlog monitoring.

### 36.8 UI

- Každá navigační položka má skutečnou route a data.
- Každá mutace má loading, success a error state.
- Každý objekt používá central action registry.
- Každé dialogové potvrzení popisuje action, target a consequence.
- Tabulky mají sorting, filtering, pagination a bounded overflow.
- Empty state nabízí smysluplný next action.
- Form validation je inline a server error je korelovaný.
- Keyboard a screen reader ovládají všechny funkce.
- Čtyři viewporty procházejí interakčním QA.

### 36.9 Konfigurace

- Každý OWNER vstup je v UI, chatu, API a persistence.
- OWNER-editable allowlist obsahuje OpenAI model a workflow-specific business/integration vstupy.
- Technické parametry jsou automaticky spravované a kontextově vysvětlitelné.
- Validation proběhne před apply.
- Restart-required změna zobrazí affected services a progress.
- Secret setting používá Password Manager semantics.
- Export/import je schema-validovaný a auditovaný.
- Setting rollback obnoví předchozí verzi.

### 36.10 Deployment

- Release manifest a API version ukazují stejný SHA/build.
- Branch CI nemění produkci.
- Ordinary deploy má bounded readiness a timing.
- Full acceptance lze opakovat bez redeploye.
- Infrastructure acceptance je samostatná.
- Rollback zachová data a obnoví předchozí release.
- Backup restore je pravidelně ověřovaný.
- Každý deploy synchronizuje GitHub Actions `PASS` do pevné identity `KRMAR78`.
- Production acceptance používá jediný `KCML_OWNER_API_KEY` a API-key-session exchange.

### 36.11 Generování MCP prvků a AI agentů

- Job lifecycle a phase lifecycle používají serverovou stavovou mapu, leases a fencing tokens.
- Každý modelový mezivýstup má strict schema a serverovou validaci.
- KájovoCML DB je autoritou pro messages, model calls, output items, tools, checkpoints a lifecycle.
- Foreground stream i background call jsou restart-safe a idempotentní podle devíti crash bodů kapitoly 52.33.
- Background recovery používá persistovaný `response.id`, `store=true` a retrieve/resume stejného response.
- Capability-first vytvoří exact full/partial/new coverage evidence.
- OWNER schvaluje current specification revision, digest a capability snapshot.
- Před approval nevznikne produkční identity, binding, runtime nebo release.
- GenerationPlan je validní DAG s complete requirement coverage.
- Workspace changes vznikají pouze atomic patch protocol s base digestem.
- Server po každém patchi načte celý workspace a provede validátory.
- Repair loop je bounded a stagnation-detecting.
- Integration je idempotentní checkpointovaná saga s compensations.
- Validation používá blocking gate catalog a žádný povinný gate není skipped.
- AI agent eval používá exact candidate MCP bindings a live candidate runtime.
- Společný MCP+agent výsledek se aktivuje jedním activation setem.
- Post-activation failure vrátí všechny související pointery, binding-set revision a route.
- První CREATE failure provede úplný provisional cleanup před dalším candidate.
- Completed job nezanechá blocker, orphan identity, stale binding, candidate route ani neuklizený runtime.
- UI, API, chat a self-test poskytují stejný stav, operace a evidence.

### 36.12 Browser Interaction Plane

- Jeden browser session, page/frame/document, control, action, side-effect a cleanup model obsluhuje generation, chat, agenty, teaching, automation a self-test.
- OWNER má živý náhled a úplné typované pointer/keyboard/text/touch/auth/device ovládání; robustnost nevzniká omezením legitimate capability, ale exact identity, fence, intent a postcondition.
- Runtime build manifest pinning zahrnuje Playwright, browser, Node/OS/dependencies/fonts/locale a adapter versions; incompatible host/context/state/replay je odmítnut.
- Každá session používá izolovaný context nebo exact bridge profile lease a raw Playwright/CDP endpoint není dostupný OWNER klientovi ani generated handleru.
- One-writer control lease, takeover/return, input-state reset a reconnect barrier zabraňují race mezi OWNERem, AI a automation.
- Page ID/generation, frame attachment, document epoch, navigation sequence a viewport transform zabraňují stale clicku po popupu, OOPIF, SPA, BFCache, prerenderu nebo rerenderu.
- Target picker a semantic locator pipeline používají strict current one-match a nepersistují ElementHandle jako autoritu.
- Každá potenciální mutace deklaruje earliest trigger; intent je commitnutý před input/change/blur/submit/upload/dialog/permission/WebAuthn side effectem.
- Každá mutace má independent postcondition/read-back a crash/timeout/disconnect používá applied/not-applied/unknown reconciliation bez blind retry.
- Account binding ověřuje exact account/tenant, auth epoch a concurrency; concurrent logout/rotation/state capture nemůže vrátit stale session jako current.
- State bundle je explicitní secret-grade member inventory, obnovuje se do fresh compatible contextu a neobsahuje raw profile, cache/service-worker runtime ani reálnou platform passkey/private key.
- OTP, push, WebAuthn assertion/registration, passkey, CAPTCHA, client certificate, dialog, permission a native flow mají checkpointovaný challenge contract.
- OWNER Device Bridge používá outbound mTLS, local keypair, certificate/build/capability/connection epochs, exact session/profile/local-target scope a není shell, proxy ani raw browser tunnel.
- Upload používá artifact handle; download je success až po completion, persistence, origin/MIME/size/digest/content verification před context closem.
- Teaching kompiluje page/frame/document route, input strategy, mutation trigger, Secret/artifact bindings, postcondition a reconciliation. Visual-only/unresolved mutace blokuje unattended activation, nikoli přímé OWNER ovládání.
- Production replay je model-free, exact-revision/build, checkpointovaný, auditovaný, account-concurrency-safe a crash-safe.
- Schedule používá timezone/DST/misfire/overlap policy a každý run má bounded context, pages, actions, deadline, artifact budget a cleanup.
- UI je plně použitelné na desktopu, tabletu a mobilním controlleru; device-bound execution probíhá přes desktop bridge nebo challenge.
- Closure nemá unresolved browser `UNKNOWN`, stale control/host/bridge authority, orphan context/profile/ticket/authenticator/download ani incomplete cleanup.

### 36.13 Trusted Runtime Boundary

- `KCML_OWNER_API_KEY` končí na veřejné gateway a není přítomen v generated handleru ani interním IPC.
- Execution context vzniká pouze serverově a caller nemůže podvrhnout source, target, revision, runtime, binding nebo access channel.
- Runtime gateway přijme pouze current Linux/systemd/runtime peer identity a exact active revision/release/runtime/binding/epoch snapshot.
- Canonical UDS vytváří systemd socket unit v root-owned non-writable directory a restart nezanechá použitelnou stale connection/path.
- Generated handler má nulové capabilities, private namespaces, read-only root, bounded tmpfs, private `/proc` a žádný host `/sys`, `/run`, DB, broker nebo browser socket.
- Handler může použít pouze šest canonical context APIs přes anonymní bounded capability FD.
- Environment a inherited FD inventory neobsahují secret, DB, gateway, browser, deployment ani credential handles.
- Child/orphan processes zůstávají v cgroup, nedědí capability FD a po terminal lifecycle nezůstávají živé.
- Secret, state, component a external call se provede pouze přes exact active binding konkrétní operation.
- Egress revaliduje DNS, IP, redirect, SNI, Host a credential destination.
- Browser host nemá veřejný/OWNER-accessible Playwright/CDP endpoint a Preview Gateway jej nemůže otevřít.
- Device Bridge je outbound mTLS, version/generation/session scoped a není shell, LAN proxy, filesystem bridge ani raw CDP tunnel.
- Activation/rollback/deployment switch okamžitě odřízne stale runtime generation od nových calls a authoritative writes.
- Timeout, cancellation, disconnect, SIGKILL, OOM a restart zachovají complete audit/correlation a deterministic reconciliation.
- Všechny testy 50.35 a blocking failure-injection profile 50.36 mají PASS a cleanup inventory je nulový.


### 36.14 Agentic AI security a semantic authority

- Každý AI-originating side effect je dohledatelný k exact OWNER message/specification/revision přes immutable authority lineage.
- Authoritative instructions, data, evidence, proposals, tool results a code mají oddělené typed segments a provenance.
- Web, MCP, external API, attachments, memory, logs, repositories, generated code a agent/tool outputs nemohou textově vytvořit OWNER command.
- OWNER může exact source výslovně delegovat jako specification input bez nové approval infrastruktury; delegation je bounded parent intentem a digestem source.
- Každý tool/target/argument/delegation/Secret-use proposal prochází serverovým semantic containment před dispatch.
- Data-derived hodnoty a dynamic targets mají exact source/locator/transform/schema/constraint evidence.
- Secret hodnoty zůstávají plně použitelné a viditelné; Secret-use context pouze brání změně recipienta nebo účelu untrusted obsahem.
- Handoff a agent-as-tool zachovávají parent intent a nemohou rozšířit semantic ceiling.
- Memory, compaction, summaries a replay zachovávají authority/provenance bez re-entry escalation.
- OWNER steer, revision switch a target drift invalidují stale context před novým dispatch.
- UI/API/audit zobrazují úplnou authority lineage, argument origins, value derivations, action plan a Secret-use evidence.
- Testy 33.12, body 331–360 a scénář W mají PASS bez neočekávaného side effectu a bez redukce explicitní OWNER capability.

### 36.15 Stateful chaos a recovery correctness

- Každá významná state machine má executable reference model, generátor commandů, preconditions, postconditions a direct-state oracle.
- Každá mutující operation má stabilní fault points před a po persistence, dispatch, side effectu, terminal commitu, successor enqueue a cleanup.
- Stateful historie je deterministicky replayovatelná ze seedu a při selhání se automaticky shrinkne bez ztráty faultu nebo precondition.
- Kill, restart, disconnect, timeout, duplicate, reorder, stale identity, OOM, disk/filesystem failure, DB restart, capacity, credential, provider a browser poruchy mají explicitní recovery outcome.
- PostgreSQL/application restart aktivuje persistentní recovery barrier; nový mutující dispatch nezačne před `READY`.
- `COMPLETED`, `SUCCEEDED`, `ACTIVE` a closure report splňují terminal closure predicate a nemají authority-bearing pending/provisional children.
- Recovery/cleanup má rezervovanou scheduler a storage kapacitu a nemůže být neomezeně vyhladověno běžnou queue zátěží.
- Všechny cross-subsystem scénáře, oracle mutation tests a blocking acceptance gates kapitoly 54 mají PASS.

## 37. Definition of Done

KájovoCML NG je dokončené, když současně platí:

1. Všechny funkce v tomto SSOT existují jako plné produkční implementace.
2. Všechny produktové a provozní funkce mají OWNER UI, API a chatovou operaci.
3. Každý UI prvek je napojený na skutečnou doménovou operaci.
4. Každý objekt má úplnou object-action parity.
5. Existuje právě jedna pevná lidská identita OWNER `KRMAR78`; program nemá user management, role, skupiny, členství, invitations ani uživatelská oprávnění/ACL.
6. Všechny MCP servery, MCP prvky, AI agenti, komponenty a runtimy používají kanonický component/runtime-execution/exact-binding model bez obecného principal/permission registru.
7. KCIP a MCP profily jsou implementované, verzované a contract-tested.
8. Veřejná komunikace používá kanonické HTTPS hostnames; inter-component komunikace používá trusted runtime gateway in-process nebo UDS bez component bearer tokenů.
9. Password Manager umožňuje OWNERovi plnou práci se secrets a runtime používá exact Secret bindings.
10. Audit, debug logy, traces a evidence pokrývají celý systém a jsou dostupné v UI.
11. Generátor dokončí CREATE, UPDATE, FOLLOW_UP, RETRY a REPAIR podle kapitoly 12.
12. Capability-first full reuse, partial reuse a new capability scénáře fungují.
13. Každý OpenAI call site používá oficiální SDK, jeden `OPENAI_API_KEY`, typed output contract a persistentní call record.
14. Foreground stream a background model call přežijí disconnect a restart bez duplicitního provider callu.
15. Specialističtí agenti generátoru vracejí pouze typed proposals a nemají přímou produkční write authority.
16. Workspace patch protocol, complete read-back, static validace a bounded repair loop fungují.
17. MCP server/tool/resource/prompt revisions mají exact schemas, digests, discovery, idempotency, concurrency a protocol evidence.
18. MCP runtime odmítne neplatný public credential/internal execution context, schema, binding, revision, digest nebo readiness stav před handlerem.
19. AI agent revisions mají exact instructions, schemas, tools, handoffs, guardrails, memory, budgets a eval suite.
20. Agent run checkpoint, approval, resume, cancellation a crash recovery fungují bez duplicitního tool side effectu.
21. Agent binding používá exact MCP contract digest a breaking drift blokuje nový run.
22. Společný MCP+agent výsledek se ověří a aktivuje jedním atomickým activation setem.
23. Post-activation smoke a rollback vracejí celý závislý set do konzistentního stavu.
24. První CREATE failure provede úplný orphan/provisional cleanup.
25. Browser Interaction Plane prokazuje exact runtime build, BrowserContext/page/frame/document identity, live preview reconnect, one-writer control a neutralizovaný takeover/return, stale-target rejection, mutation-trigger ledger, postcondition/reconciliation, account/auth epoch, cookies/storage/state bundles, OTP/push/WebAuthn/passkey/client-certificate/CAPTCHA, popup/OOPIF, upload/download, OWNER Device Bridge, teaching, model-free replay, scheduling, crash recovery a complete cleanup bez duplicate side effectu.
26. Monitoring, alerting, repair, rollback a recertification fungují.
27. Ubuntu, Nginx, PostgreSQL, systemd, DNS, TLS a backup provoz mají ověřený kontrakt.
28. Ordinary deploy a acceptance jsou oddělené a pinované na exact release.
29. CI obsahuje povinné PostgreSQL, Linux/systemd, OpenAI adapter, MCP, agent, Playwright, security a UI gates.
30. Manuální scénáře A–W mají PASS evidence.
31. Integrační body 1–360 mají PASS evidence odpovídajícího typu.
32. Produkce běží na accepted `main` release, health a readiness jsou zelené a žádný povinný požadavek nezůstává bez implementace nebo důkazu.
33. Centrální AI chat funguje v UI i externím API pro otázky a příkazy nad celým systémem.
34. Jediný produkční `KCML_OWNER_API_KEY` zpřístupňuje všechny funkce a registered elements bez interaktivního loginu a bez scopes.
35. Self-test runtime spustí celý test catalog přes kanonické API a vydá úplnou evidence.
36. Aplikace je plně dostupná bez OpenAI klíče a AI capability lze aktivovat jeho vložením v UI.
37. Každý deploy synchronizuje `PASS` do singleton identity `KRMAR78` a nevytváří další identitu.
38. Důvěryhodný developerský počítač, privátní GitHub, deployment a produkční server splňují kapitolu 30 a podporují přímou práci se secrets.
39. Generation job `COMPLETED` nezanechá provisional runtime, orphan identity, stale binding, unresolved blocker ani neúplný activation set.
40. Closure report obsahuje exact source SHA, release, object inventory, 360/360 acceptance, A–W, OpenAI/MCP/agent/browser/wire/agentic-security/stateful-chaos/recovery evidence a nulový seznam otevřených povinných požadavků.
41. Každý mutable aggregate má explicitní state machine, state version, terminalitu, command CAS a transactional event/audit/outbox hranici.
42. Každý leased resource používá DB time, platform incarnation a monotonic fencing; expired nebo late worker neprovede autoritativní write.
43. Queue processing je at-least-once a každá mutující API/tool/run operace má exactly-once observable outcome v explicitním idempotency scope.
44. Každý external nebo jinak neatomický side effect má persistentní intent, attempt, outcome a reconciliation; closure neobsahuje unresolved `UNKNOWN` outcome.
45. Každý checkpoint je immutable, digest-chained, fenced a navazuje na exact parent state version; recovery nepoužívá process memory jako autoritu.
46. Concurrent OWNER commands, cancel/complete, takeover/completion, steer/completion, activate/rollback a rotate/use races mají deterministický linearizační bod a prokazatelný výsledek.
47. Activation a deployment nikdy nevystaví mixed epoch; first CREATE rollback obnoví `ABSENT` a UPDATE/REPAIR přesný frozen previous snapshot.
48. Formální model, PostgreSQL concurrency suite, crash-injection matrix a stateful nemesis testy kapitol 49 a 54 mají PASS pro všechny blocking invarianty.
49. Backup restore vytvoří novou platform incarnation a recovery před novým dispatch reconciliuje všechny pending leases, queue items, side effects, activation, deployment a cleanup operace.
50. Closure nemá stale active lease, unresolved concurrency claim, queue/outbox/inbox gap, incomplete cleanup, orphan pointer, mixed activation epoch ani side-effect operation v neuzavřeném stavu.
51. Trusted Runtime Boundary kapitoly 50 má PASS: žádný handler-accessible network/DB/broker/browser bypass, stale runtime authority, inherited unauthorized FD, orphan process/cgroup, raw browser transport, bridge scope bypass ani neúplná runtime evidence.
52. Každá mutující operation má exact `dbTransactionProfile`, isolation level, ordered advisory/row lock plan, CAS guards a physical PostgreSQL constraints podle kapitoly 51.
53. Queue/outbox/inbox claimy, persistentní concurrency claims, lease expiry během transakce a fresh pre-dispatch authority check mají PASS v reálných paralelních PostgreSQL testech.
54. Activation admission/barrier/switch/rollback, Secret a OWNER API-key rotace, approvals, MCP input/tasks, browser control a cleanup jsou chráněny proti phantomu, stale readu, double commit a lock-order inversion.
55. Greenfield schema i každá migration mají platné immutable triggers, partial unique indexes, composite parent-owned foreign keys, migration checksum chain a forward-schema rollback compatibility.
56. Povinných 56 PostgreSQL concurrency a failure-injection scénářů 51.34 má PASS bez deadlocku v produkčním lock planu, mixed epoch, duplicate observable outcome, orphanu nebo neuzavřeného side effectu.
57. Každý model/tool/handoff/browser/Secret side effect má current authority lineage, OWNER intent, operation context, semantic action plan, argument-origin mapu a úplnou provenance evidence podle kapitoly 53.
58. Prompt-injection/confused-deputy red-team suite, body 331–360 a scénář W mají PASS a stejné capabilities fungují při explicitním OWNER příkazu bez nové permission, approval nebo Secret restriction vrstvy.
59. Každá critical mutable operation má stateful model command, stabilní pre/postconditions, fault-point coverage, recovery oracle, direct-state invarianty a shrinker.
60. Každý blocking chaos failure je přesně replayovatelný uloženým seedem a shrunk schedulem na exact release; žádný invariant failure nemá flaky waiver.
61. PostgreSQL restart, application/service restart a disaster restore aktivují recovery barrier a žádný nový mutující dispatch nezačne před potvrzeným `READY`.
62. Úplná single-fault × critical-cutpoint matice, pairwise kombinace, povinné tříporuchové schedule a nejméně 10 000 randomizovaných interleavings mají PASS.
63. `COMPLETED`, `SUCCEEDED`, `ACTIVE` a closure report splňují terminal closure predicate: žádný pending/provisional authority-bearing child, stale lease, unresolved side effect, partial activation, orphan runtime/identity/binding/pointer ani required cleanup.
64. Oracle mutation suite prokazatelně zachytí odstranění fence, idempotency, intent-before-dispatch, atomic pointer switch, stale browser/context guard, OpenAI no-hidden-retry a cleanup closure guardu.

## 38. Greenfield struktura repozitáře

Kanonická struktura jednoho privátního repozitáře:

```text
/
├── apps/
│   ├── server/
│   ├── runtime-gateway/
│   ├── runtime-host/
│   ├── owner-ui/
│   ├── central-chat-worker/
│   ├── generation-coordinator/
│   ├── generation-openai-worker/
│   ├── generation-workspace-worker/
│   ├── generation-integration-worker/
│   ├── generation-validation-worker/
│   ├── generation-activation-worker/
│   ├── agent-worker/
│   ├── browser-worker/
│   ├── browser-host/
│   ├── browser-bridge-gateway/
│   ├── owner-device-bridge/
│   ├── component-control-worker/
│   ├── component-e2e-worker/
│   ├── monitor-worker/
│   ├── alert-worker/
│   ├── self-test-runtime/
│   ├── acceptance-runner/
│   ├── secret-broker/
│   ├── state-broker/
│   ├── egress-gateway/
│   └── audit-archiver/
├── packages/
│   ├── domain/
│   ├── contract-pack/
│   ├── openai-runtime/
│   ├── generation-orchestrator/
│   ├── generation-workspace/
│   ├── agentic-authority/
│   ├── content-provenance/
│   ├── kcip/
│   ├── mcp-runtime/
│   ├── runtime-boundary/
│   ├── runtime-capability-ipc/
│   ├── component-sdk/
│   ├── agent-sdk/
│   ├── browser-interaction/
│   ├── browser-runtime-contracts/
│   ├── browser-preview-protocol/
│   ├── browser-automation-runtime/
│   ├── schemas/
│   ├── database/
│   ├── ui-kit/
│   ├── testing/
│   └── observability/
├── contracts/
│   ├── generation/
│   ├── component/
│   ├── mcp/
│   ├── agent/
│   ├── agentic-authority/
│   ├── provenance/
│   ├── browser/
│   ├── browser-preview/
│   ├── browser-bridge/
│   ├── browser-runtime-build/
│   ├── runtime/
│   ├── registry-schemas/
│   ├── registries/
│   │   ├── requirements/
│   │   ├── operations/
│   │   ├── state-machines/
│   │   ├── postgres/
│   │   ├── runtime-boundaries/
│   │   ├── bindings/
│   │   ├── authority/
│   │   ├── faults/
│   │   ├── recovery-oracles/
│   │   ├── closure-predicates/
│   │   ├── acceptance-gates/
│   │   ├── exposure-parity/
│   │   ├── artifact-trace/
│   │   └── errors/
│   ├── testing/
│   │   ├── state-models/
│   │   ├── fault-catalog/
│   │   ├── recovery-oracles/
│   │   └── evidence/
│   └── protocol/
├── components/
├── database/
│   ├── baseline/
│   └── migrations/
├── deploy/
│   ├── nginx/
│   ├── systemd/
│   ├── scripts/
│   └── acceptance/
├── tests/
│   ├── unit/
│   ├── integration/
│   ├── postgres/
│   ├── openai/
│   ├── generation/
│   ├── mcp/
│   ├── agents/
│   ├── agentic-security/
│   ├── prompt-injection-fixtures/
│   ├── browser/
│   ├── browser-preview/
│   ├── browser-bridge/
│   ├── browser-real-web-fixtures/
│   ├── runtime-boundary/
│   ├── ipc/
│   ├── systemd/
│   ├── property/
│   ├── fault-injection/
│   ├── chaos/
│   ├── recovery/
│   ├── contract-pack/
│   ├── architecture-readiness/
│   ├── production/
│   └── fixtures/
├── docs/
├── SSOT_CURRENT.md
├── AGENTS.md
├── package.json
├── pnpm-workspace.yaml
└── pnpm-lock.yaml
```

`SSOT_CURRENT.md` je jediný normativní produktový zdroj. `contracts` obsahují strojově čitelná schémata a deterministic registry projections podle kapitol 46 a 55. Generovaný runtime kód používá stejné schemas jako API, UI, tests a registr. `packages/domain` je jediným implementačním domovem sdílených canonical aggregate/state-machine command handlers; jednotlivé apps je vykonávají pod stejnými PostgreSQL guards, ale nesmějí implementovat paralelní write semantics. `packages/contract-pack` je pure compiler a validator těchto projekcí a není runtime control plane.

`runtime-boundary` a `runtime-capability-ipc` obsahují trusted Linux launcher/peer/FD/framing implementation a generated component SDK adapter podle kapitoly 50. Generated code tyto low-level syscalls ani systemd socket paths přímo nepoužívá.

`openai-runtime` je jediným místem pro přímé použití oficiálních balíčků `openai` a `@openai/agents`. Ostatní moduly používají jeho typované služby. `generation-orchestrator` vlastní state machine a nesmí záviset na in-memory stavu konkrétního workeru. `agentic-authority` a `content-provenance` implementují shared compiler/validator/digest contracts kapitoly 53; nejsou samostatným auth, permission ani policy control plane.

`packages/testing` vlastní shared stateful model DSL, command generators, deterministic scheduler, DB-time observation a bounded test-duration controller, fault-point registry, nemesis orchestrator, direct-state oracles, linearizability checker, shrinkers, replay format a evidence schema kapitoly 54. `tests/property`, `tests/fault-injection`, `tests/chaos` a `tests/recovery` obsahují pouze executable suites a regression schedules; nesmějí používat test-only domain implementation, která obchází produkční command, queue, gateway, broker, runtime nebo persistence hranici.

`apps/server` sestavuje unit `kcml-web-api` a obsahuje public gateway, core OWNER API, BrowserSessionService a Preview Gateway. `apps/alert-worker` sestavuje units `kcml-alert-primary-worker` a `kcml-alert-backup-worker` ze stejného writer/consumer code; liší se pouze deployment assignmentem, nikoli alert state machine. `apps/self-test-runtime` sestavuje `kcml-self-test-worker`, `apps/acceptance-runner` sestavuje on-demand `kcml-acceptance-runner`, `apps/state-broker` sestavuje `kcml-state-broker` a `apps/audit-archiver` sestavuje `kcml-audit-archiver`. Každá systemd služba kapitoly 27.1 tak má právě jeden repository home.

`packages/database` vlastní physical repositories, migrations a PostgreSQL contract adapters, nikoli druhou business autoritu. Shared package nesmí spouštět skrytý scheduler, vytvářet pointer, měnit state bez canonical command handleru ani poskytovat test-only bypass. OpenAI, generated handler a browser stack používají výhradně hranice uvedené v kapitolách 13, 15, 38.1 a 55.16.

## 39. Detailní specifikace obrazovek

### 39.1 Přihlášení

Obrazovka používá centrovaný panel do 430 px, logo KájovoCML NG, read-only označení identity `KRMAR78`, password, pokračovací tlačítko, stavové hlášení a technický correlation detail při chybě. Username není editovatelné pole. Druhý krok MFA obsahuje TOTP/recovery input, návrat a správu důvěryhodného zařízení. Provozní bootstrap probíhá automaticky; OpenAI credential se doplňuje v Password Manageru nebo centrálním chatu a jeho absence zachovává login i celý ne-AI systém.

### 39.2 Dashboard

Dashboard má:

- horní přehledové metriky: aktivní komponenty, unhealthy/degraded, otevřené alarmy, běžící jobs/runs,
- live status API, workerů, DB, TLS a DNS,
- topologický canvas,
- panel filtrování a hledání,
- detail node/edge/port/secret,
- timeline posledních calls a PULSE,
- alarm sidebar,
- saved layout a fit-to-content,
- command palette.

Node context panel obsahuje identity, stav, release, contracts, secrets, bindings, metrics, logs a všechny relevantní akce.

### 39.3 Generování

Generování má job catalog a detailní výrobní workspace. Catalog filtruje podle lifecycle, kind, target, model, phase, blocker, age a activation state. Řádek zobrazuje objective, target graph summary, state, current phase, progress, active worker/lease freshness, browser session state, updatedAt, blocker a výsledný object link.

Workspace používá panely:

- **Diskuse** — messages, stream, sources, attachments, Browser target references, steer, cancel a composer,
- **Web** — live preview, URL/page tree, control holder, operation scope, current action/target, takeover, challenge, account save, teaching a artifacts,
- **Zdroje a fakta** — source status, verified facts, conflicts a OWNER decisions,
- **Capability** — coverage matrix, reused contracts, missing delta a exact digests,
- **Specifikace** — revision, Markdown, canonical JSON, diff, open questions a approval,
- **Architektura** — target graph MCP/agent/component, exact bindings, secrets, external a browser edges,
- **Plán** — generation DAG, node states, dependencies, retries a compensations,
- **Workspace** — files, workspace revisions, atomic patch diff a artifact manifest,
- **Validace** — gates, diagnostics, tests, eval cases, browser replay evidence a repair iterations,
- **Průběh** — phases, workers, leases, model calls, browser sessions/actions, checkpoints, blockers a timings,
- **Aktivace** — activation set, previous/candidate pointers, preflight, postflight a rollback,
- **Audit** — correlation graph, logs, OpenAI output items, browser observations/actions, tools a audit events.

Approval CTA zobrazuje exact spec revision, digest, capability snapshot a browser requirements. Resolve blocker formulář se generuje z blocker input schema. UI nenabízí ruční přepnutí phase, ruční PASS gate ani přímou aktivaci neověřeného candidate.

### 39.4 AI agenti

Agent list zobrazuje enabled, mode, OpenAI model, active revision, binding readiness, eval promotion state, last run, success rate, latency, tokens, cost a alerts.

Detail má tabs:

- Přehled,
- Instrukce a schemas,
- Nástroje a aliasy,
- Handoffs a agent tools,
- Guardrails a approvals,
- Sessions a paměť,
- Triggery,
- Runs,
- Checkpointy,
- Evaluace,
- Revisions a compatibility,
- Secrets,
- Bindings,
- Monitoring,
- Audit.

Instruction editor podporuje canonical Markdown, typed variables, compiled preview a diff. Tool graph zobrazuje source component, exact revision, contract digest, alias, binding revision, readiness a drift. Run console zobrazuje model calls, output items, tool calls, handoffs, browser sessions/actions, approvals, usage, cancellation a resume. Browser policy panel ukazuje povolený režim, origins, side-effect ceiling, account bindings, operation scopes a deterministic automation preference.

Approval inbox zobrazuje exact target, canonical arguments, digest, consequence, expiration a approve/reject akce. Eval UI zobrazuje blocking cases, aggregate thresholds, model/tool snapshot a promotion rozhodnutí.

### 39.5 MCP servery a nástroje

List zobrazuje component code, protocol era/version, transport, active server revision, discovery digest, tool/resource/template/prompt counts, runtime, health, last call, error rate a drift.

Server detail nabízí:

- `server/discover`, supported versions, capabilities a extensions,
- per-request `_meta`, JSON-RPC ID a HTTP header inspector,
- HTTP status/JSON-RPC error map, modern/legacy era probe a raw probe evidence,
- immutable discovery snapshots, cache TTL/scope a diff,
- tools/resources/templates/prompts,
- native JSON Schema, `$ref`, dialect a OpenAI strict projection explorer,
- `x-mcp-header` inspector,
- request builder s exact revision/digest,
- live typed output, raw MCP/KCIP/SSE frames, delivery state a request-scoped progress,
- protocol-error, tool-execution-error, duplicate-ID a disconnect/reconciliation tester,
- MRTR input request a resume console,
- subscriptions/listen filters, acknowledgements a live notifications,
- explicit state handles,
- MCP tasks, polling, input a cancellation,
- idempotency, concurrency a reconciliation detail,
- API key/access binding, operation scope a approval test,
- agent binding graph, aliases a compatibility,
- runtime/release/activation set,
- health, readiness, heartbeat a monitoring,
- E2E, repair a rollback,
- logs a audit.

Tool detail zobrazuje input/output schema, side-effect, retry, idempotency, concurrency, timeout, limits, approval, MRTR/task policy, routing headers, error catalog, callers, agent bindings, usage, call history a versions.

### 39.6 Browser relace a automatizace

Obrazovka používá záložky **Relace**, **Automatizace**, **Účty**, **Bridges** a **Kapacita**.

**Relace** zobrazují parent, purpose, execution target, runtime build, URL/account/tenant, page/document, control holder, lifecycle, age, viewers, active action/challenge/dialog/download, pending reconciliation a cleanup. Detail obsahuje live preview, page/opener tree, frame/OOPIF tree, document/navigation timeline, current observation, action dispatch phases, operation scope, control history, target references, auth/account state, recovery a cleanup.

Session list viditelně odlišuje:

```text
AI ovládá
OWNER ovládá
AUTOMATION ovládá
Přebírání
Čeká na challenge
Reconciliuje mutaci
Obnovuje context
Uklízí
```

**Automatizace** zobrazují name, owner component, active revision, auth/account mode, runtime/bridge capability, enabled, verification, schedule, last run a status. Detail obsahuje:

- definition metadata a exact active/rollback revision,
- runtime build/engine/bridge compatibility,
- revision manifest a diff,
- semantic step editor,
- page/frame/document route,
- locator candidates, highlights, ambiguity a drift report,
- input strategy a actionability contract,
- earliest mutation trigger a side-effect timeline,
- precondition, postcondition a independent read-back,
- input/output schema a artifact bindings,
- auth/account/tenant/state-bundle policy,
- account concurrency a external session family,
- timezone, DST, misfire, overlap a event triggers,
- preflight, replay, verification a activation gates,
- run form, history, step/action timeline a reconciliation,
- live preview a OWNER takeover/return,
- challenge, dialog, permission, popup, upload/download panel,
- artifacts, drift, repair a rollback.

**Účty** zobrazují target/service, account key, expected/last observed account a tenant, auth epoch, concurrency mode, external session family, credential/certificate/virtual-authenticator refs, active state-bundle version/member inventory, runtime compatibility, expiry, last verify/use a invalidate/reauth/logout actions.

State-bundle detail rozlišuje cookies a partition metadata, localStorage, IndexedDB, sessionStorage adapter, permissions, client-certificate metadata a virtual WebAuthn members. Výslovně ukazuje, že cache, service-worker runtime, raw profile a platform passkey/private key nejsou standardně uložené.

**Bridges** zobrazují device label, OS/architecture, exact bridge build, certificate generation/fingerprint, current connection epoch, negotiated capabilities, browser/profile inventory, profile leases, session assignments, heartbeat, allowed local targets a enroll/test/rotate/revoke. Capability matrix uvádí platform authenticator, client-certificate store, native dialogs/input, local browser/profile a local network target support.

**Kapacita** zobrazuje browser host slots, runtime build manifests, engines/builds, contexts/pages/renderers, queue, memory/CPU, crashes, drain/recycle, preview load, downloads/traces a system-managed scaling explanation. Raw host endpoint nebo opaque Playwright handle se nezobrazuje jako připojitelný údaj.

### 39.7 Registrované prvky

Tabulka poskytuje rychlý provozní seznam component code, name, type, enabled, runtime, operational state, active release, last heartbeat, alerts a actions. Detail je propojen s katalogem, monitoringem, topologií, bindings, secrets a logs.

### 39.8 Katalog komponent

Katalog obsahuje card/table toggle, advanced filters, registration/generation/import actions, validation state, revision/release explorer, manifest, documentation evidence, dependencies, E2E a recertification.

### 39.9 Externí systémy

Tabs:

- Targets,
- Auth bindings,
- Inbound endpoints,
- Outbound target bindings,
- Webhooks,
- Request history,
- Circuits.

Target form obsahuje base URL, methods, paths, auth mode, secret binding, limits, circuit, monitoring a test request. Webhook inspector zobrazuje raw request, headers, verification, schema, response a correlation.

### 39.10 Monitoring

Monitoring poskytuje global/component dashboard, timeline, probes, heartbeats, alerts, deliveries, SLO, scheduler a profile editor. Alert detail má occurrences, evidence, affected graph, logs, runbook a actions ack/suppress/close/repair.

### 39.11 API klíč a runtime vazby

Horní singleton karta zobrazuje `KCML_OWNER_API_KEY`, fingerprint, credential version, last use a akce reveal/copy/rotate. Pod ní jsou read-only runtime execution contexts, component identities, contract/Secret/external bindings a bridge certificate bindings s correlation a auditem. Neexistuje token, principal ani permission list.

### 39.12 Secrets a hesla

List a detail odpovídají sekci Password Manager. Všechny hodnoty jsou v OWNER zóně dostupné. Presentation mode je globální toolbar toggle a stav je viditelný.

### 39.13 Technické vazby

UI poskytuje source-revision × target-revision graf, seznam exact bindings, dependency preview, binding test a activation-set detail. Secret bindings mají vlastní bind/unbind a bulk impact operace. Neexistují role, allow/deny pravidla, permission matrix ani obecné grant/revoke operace.

### 39.14 Audit a logy

Audit tab zobrazuje hash chain, events a integrity. Logs tab zobrazuje live debug stream. Correlation view spojuje messages, tool calls, KCIP, HTTP, DB, workers, browser, alerts a releases do jednoho grafu.

### 39.15 Konfigurace

Settings jsou seskupené podle kategorií, mají search, changed-only filter, validation, affected services, restart badge, version history a apply summary. Secret setting otevře Password Manager picker nebo editor hodnoty.

### 39.16 Bezpečnost

Obsahuje read-only username a password source/sync, MFA, recovery, sessions, login history, security events, certificates, master key health, host preflight, dependency scan a security headers.

### 39.17 Pevná identita OWNERa

Panel obsahuje jedinou read-only identitu `KRMAR78`, `deploymentManaged`, MFA stav, active sessions, created, last login a odkaz na **Bezpečnost**. Neobsahuje seznam, create/edit/delete, active toggle, role ani permissions.

### 39.18 Releases a provoz

Zobrazuje application a component releases, deployments, migrations, backups, services, system health, DNS/TLS, acceptance runs a timing. Release detail obsahuje exact files/digests/signature/source/build, deploy steps, current pointer a rollback.

### 39.19 Centrální chat

Samostatná chatová obrazovka používá persistentní konverzační plochu, composer, OpenAI model selector, attachments, object context, streamované odpovědi, action timeline, log/audit odkazy a historii konverzací. Chat odpovídá lidsky a u každého vykonaného příkazu zobrazuje target, operation, výsledek, correlation ID a rozbalitelnou authority lineage: source OWNER message/specification, operation-context digest, exact tool/binding, argument origins, value derivations a Secret-use target. Toto je evidence, nikoli approval krok.

Po založení nebo připojení browser session se otevře volitelný pravý panel. Header obsahuje URL/title, page switcher, current page/frame/document epoch, účet/tenant, lifecycle, runtime build/host nebo bridge, `AI/OWNER/AUTOMATION ovládá`, control epoch, pending mutation/reconciliation, latency a sensitivity. Toolbar obsahuje otevřít URL, zpět/vpřed/reload, fit/actual, target picker, screenshot, převzít, vrátit AI, credential, uložit/obnovit účet, challenge, dialogs/permissions, action outcome/reconcile a close.

Chatová zpráva může obsahovat Browser target reference chip s náhledem, semantic popisem a document-bound identity. Kliknutí na chip nejprve provede current resolution a zvýrazní pouze unique target ve stejném page/frame/document lineage. Stale/ambiguous/replaced chip zobrazí kandidáty nebo vyžádá nové označení; nikdy sám nespustí mutující action.

Globální drawer nabízí stejnou konverzaci a může browser panel připnout vedle aktuální obrazovky. Externí klient přes API vidí totožné message, browser session a action výsledky; obrazový preview otevírá přes one-time ticket.

### 39.20 Testy a API

Obrazovka obsahuje:

- singleton OWNER API key detail,
- reveal, copy, atomic rotate a usage,
- stable name `KCML_OWNER_API_KEY`,
- API operation explorer,
- KCIP/OpenAPI schema browser,
- self-test catalog a suite filters,
- stateful model catalog, state/edge/command coverage a model digests,
- fault-point catalog, single-fault/pairwise/three-way coverage a nemesis profiles,
- start/cancel/cleanup/replay/shrink runu,
- live command/fault timeline, linearizability history a direct-state invariant table,
- recovery barrier, database start identity, recovery epoch a startup inventory,
- terminal closure a orphan inventory,
- logs, audit, evidence, seeds, shrunk schedules a artifact download,
- exact SHA/release/environment status,
- test každého registrovaného prvku.

### 39.21 Browser session detail

Browser session detail je dostupný z chatu, generation, agent runu, automation runu, testu, auditu a katalogu živých relací. Obsahuje:

- live preview, stream epoch, connection/resync state a client latency,
- URL, tabs, popups, opener relations, page IDs/generations a active preview page,
- frame/OOPIF tree, attachment epochs, sandbox/capability state a document/navigation timeline,
- runtime build manifest, host/bridge/context identity a generations,
- control holder, epoch, fence, expiry, takeover history a low-level input-state reset evidence,
- operation scope, account/tenant/resource constraints a confirmations,
- current/final BrowserObservation, service-worker/network/console summary a viewport transform,
- target references, locator AST, current matches, fingerprint a actionability diagnostics,
- action attempts, immutable dispatch phases, mutation trigger, pre/post conditions a side-effect/reconciliation state,
- account binding, auth epoch, authenticated account/tenant condition a state-bundle member inventory,
- auth attempts, OTP/push/WebAuthn/passkey/client-certificate/CAPTCHA challenges,
- dialogs, permission prompts, external protocol a native/bridge actions,
- uploads, downloads, screenshots, traces, DOM/semantic snapshots a artifact digests,
- account/resource concurrency claims a schedule relation,
- recovery, manual review, cleanup resources, logs, audit a correlation graph.

Preview toolbar nabízí page switcher, back/forward/reload, target picker, screenshot, pointer/keyboard/touch mode, credential, uložit účet, challenge, převzít/vrátit řízení, pause, reconcile a close podle object-action registry. Tlačítko **Reconcile** nevytváří nový click; spouští pouze revision-defined read-back current action.

Action detail zobrazuje exact authority n-tici `session → runtime build → host/bridge → context → page → frame → document → control → target → action attempt`, nejčasnější možný side effect a důvod, proč je retry povolený, zakázaný nebo čeká na OWNER resolution.

Všechny akce používají object-action registry. Detail neposkytuje raw CDP console, veřejný Playwright endpoint, arbitrary script runner, bridge shell ani filesystem browser. OWNER technické údaje a raw evidence jsou plně zobrazitelné v důvěryhodné zóně.

## 40. Centrální registr objektových akcí

### 40.1 Akční kontrakt

Každá akce má:

- action key,
- object type,
- applicable states,
- title a icon,
- tone,
- input schema,
- existing confirmation policy,
- authority-lineage/OWNER-intent derivation, target-resolution a argument-origin contract,
- Secret-use context template podle relevance,
- route/domain operation,
- CSRF/idempotency,
- audit event,
- optimistic behavior,
- success/error presentation.

UI načítá applicability z jednoho registru. Tabulka, card, detail, canvas, search, alarm a relationship panel zobrazují stejnou sadu podle objektu a stavu.

### 40.2 Component actions

```text
open
editMetadata
viewManifest
viewRevision
compareRevisions
validate
verify
activate
enable
disable
suspend
quarantine
restore
runE2E
queryState
challengeHeartbeat
repair
recertify
rollback
viewSecrets
viewBindings
viewMonitoring
viewLogs
viewAudit
export
deregister
```

### 40.3 Agent actions

```text
open
editMetadata
createRevision
viewRevision
compareRevisions
validateRevision
verifyRevision
previewToolBindings
viewToolBindings
viewHandoffs
viewGuardrails
viewPromotionGates
activate
enable
disable
run
viewRun
pauseRun
resumeRun
cancelRun
approveToolCall
rejectToolCall
viewCheckpoints
manageSessions
compactSession
manageMemory
manageTriggers
runEvaluation
viewEvaluation
repair
rollback
viewSecrets
viewBindings
viewMonitoring
viewLogs
viewAudit
export
```

### 40.4 MCP/tool actions

```text
open
editMetadata
createRevision
viewRevision
compareRevisions
validateRevision
verifyRevision
previewCompatibility
activate
serverDiscoverTest
probeProtocolEra
requestMetadataTest
testHttpJsonRpcMap
testDuplicateRequestId
viewRawWireEvidence
createDiscoverySnapshot
compareDiscoverySnapshots
listTools
callTool
viewCall
cancelCall
reconcileCall
viewInputExchange
respondInputExchange
openSubscription
viewSubscription
cancelSubscription
viewStateHandle
closeStateHandle
viewTask
updateTask
cancelTask
readResource
renderPrompt
viewSchema
viewOpenAIProjection
viewRoutingHeaders
manageAliases
manageCallers
viewAgentBindings
enable
disable
viewUsage
viewMonitoring
viewLogs
viewAudit
repair
rollback
archive
deregister
```

### 40.5 Secret actions

```text
open
reveal
copy
editMetadata
createVersion
activateVersion
rotate
generateValue
showTotp
import
export
bind
unbind
bulkBind
bulkUnbind
testResolve
viewUsage
viewLogs
viewAudit
delete
```

Pro reserved Secret `KCML_OWNER_API_KEY` registr zpřístupní pouze `open`, `reveal`, `copy`, `rotate`, `viewUsage`, `viewLogs` a `viewAudit`; obecné edit/version/delete/binding akce nejsou applicable.

### 40.6 Browser session a automation actions

Browser session actions:

```text
open
attach
observe
navigate
openPage
activatePage
closePage
takeControl
releaseControl
returnToAI
pickTarget
revalidateTarget
sendCredential
verifyAccount
saveAccount
captureState
inspectStateBundle
respondDialog
respondPermission
resolveChallenge
upload
awaitDownload
pause
resume
recover
reconcileAction
resolveActionOutcome
close
viewPreview
viewPages
viewDocuments
viewActions
viewArtifacts
viewAuth
viewConcurrency
viewCleanup
viewLogs
viewAudit
```

Browser automation actions:

```text
open
edit
createRevision
compareRevision
startTeaching
stopTeaching
compileTeaching
preflight
verify
activate
rollback
run
viewRun
viewSteps
viewSession
takeControl
returnToRuntime
cancel
reconcile
resolveOutcome
resolveChallenge
reauthenticate
verifyAccount
manageSchedule
enable
disable
repair
viewArtifacts
viewDrift
viewConcurrency
viewCleanup
viewLogs
viewAudit
```

Bridge actions:

```text
enroll
test
viewCapabilities
viewConnections
viewProfiles
rotateCertificate
revoke
viewAssignments
viewLogs
viewAudit
```

Action registry určuje applicability podle exact lifecycle, control/action state, pending mutation, account/auth epoch, bridge connection/profile lease a cleanup. `reconcileAction`/`reconcile` provádí pouze current revision-defined read-back a nikdy nereplayuje původní browser input. `resolveActionOutcome`/`resolveOutcome` vyžaduje OWNER CAS nad exact evidence digestem.

### 40.7 Alert actions

```text
open
acknowledge
suppress
close
openSourceObject
openCorrelation
runProbe
startRepair
openRunbook
viewDeliveries
```

### 40.8 OWNER security actions

```text
openSecurity
viewFixedIdentity
startMfaEnrollment
resetMfa
rotateRecoveryCodes
revokeSession
revokeOtherSessions
revokeAllSessions
viewLogs
viewAudit
```

### 40.9 OWNER API key actions

```text
openApiKey
revealApiKey
copyApiKey
rotateApiKey
viewUsage
exchangeForSession
openAudit
openLogs
```

### 40.10 Centrální chat a self-test actions

```text
ask
sendCommand
openBrowserSession
attachBrowserSession
takeBrowserControl
returnBrowserToAI
pickBrowserTarget
saveBrowserAccount
resolveBrowserChallenge
startGeneration
runOperation
runSelfTest
cancelSelfTest
cleanupSelfTest
openEvidence
openApiKey
selectOpenAiModel
configureOpenAiKey
```

### 40.11 Generation actions

```text
open
createJob
sendMessage
steer
openBrowserSession
attachBrowserSession
takeBrowserControl
returnBrowserToAI
pickBrowserTarget
saveBrowserAccount
addSource
refreshCapabilitySnapshot
viewSpecification
compareSpecifications
runSpecificationPrecheck
approveSpecification
viewPlan
viewPhase
viewModelCall
viewWorkspace
compareWorkspaceRevisions
viewValidation
resolveBlocker
cancel
resume
retry
followUp
viewActivationSet
openResultObjects
runSelfTest
viewLogs
viewAudit
exportEvidence
```

## 41. Katalog parametrů a OWNER vstupů

Každá položka má klasifikaci `SYSTEM_MANAGED`, `OWNER_INPUT` nebo `OWNER_PREFERENCE`. Výchozí UI zobrazuje pouze položky vyžadující OWNER vstup a kontextové preference. Ostatní parametry systém spravuje automaticky a zpřístupňuje jejich účinnou hodnotu v detailu nebo centrálním chatu.

### 41.1 Network — `SYSTEM_MANAGED`

- `network.publicDomain = kaja.hcasc.cz`
- `network.apiBasePath = /api/v1`
- `network.componentZone = kaja.hcasc.cz`
- `network.publicIpv4 = 89.221.222.92`
- trusted proxy CIDRs,
- request body limits,
- upload limits,
- SSE heartbeat interval,
- HTTP timeouts,
- external DNS provider metadata.

### 41.2 Authentication — `SYSTEM_MANAGED`

- fixed singleton username `KRMAR78`,
- exactly one `owner_identity` a one `owner_api_credential`,
- absence of user/account/role/group/permission configuration,
- deployment password source `GitHub Actions: PASS`,
- unconditional password sync on every deploy,
- `KCML_OWNER_API_KEY` generation, verifier a atomic rotation,
- session duration,
- idle timeout,
- trusted device duration,
- MFA requirement policy,
- login throttle thresholds,
- recovery code count,
- session list retention.

### 41.3 AI — OpenAI

`OWNER_INPUT`:

- globální OpenAI model,
- volitelný OpenAI model centrálního chatu,
- volitelný OpenAI model generation workflow,
- volitelný OpenAI model konkrétního agenta,
- `OPENAI_API_KEY` v Password Manageru.

`SYSTEM_MANAGED`:

- provider `OPENAI`,
- oficiální SDK balíčky `openai` a `@openai/agents`,
- Responses API a Agents SDK execution mode,
- foreground/background policy,
- strict structured output profile,
- reasoning/output parameters,
- turn/model/tool limits,
- parallel tool-call policy,
- timeouts a deadlines,
- token/cost budgets,
- provider retry a retrieve policy,
- tracing a event persistence,
- refusal/incomplete normalization,
- model capability snapshots a pre-submit gates,
- immutable OpenAI request descriptor a request digest,
- create-class `maxRetries: 0` a explicitní replay-safety policy,
- provider response ID versus diagnostický request ID,
- session strategy, compaction a canonical history policy,
- side-by-side SDK runtime compatibility pro pending RunState,
- evaluace a readiness podle kapitoly 52.

Všechny klienty používají jeden `OPENAI_API_KEY`. Objekt bez vlastní modelové volby používá globální model. SDK verze jsou pinované lockfilem a jejich upgrade vyžaduje OpenAI/MCP/agent regression a production acceptance.

### 41.4 Generation — `SYSTEM_MANAGED`

- coordinator a phase worker concurrency,
- queue priorities a capacities,
- phase timeouts a leases,
- model-call foreground/background selection,
- max specialist runs a model turns,
- max tool calls, tokens a cost,
- generation plan node limits,
- workspace quota a file limits,
- patch operation a content limity,
- max repair iterations,
- stagnation thresholds a strategy count,
- validation cluster concurrency,
- capability search limit a cache policy,
- checkpoint interval,
- artifact retention,
- candidate a provisional cleanup,
- integration compensation policy,
- activation lock a postflight policy,
- approval UI preferences,
- release activation a rollback policy.

Účinné hodnoty se odvozují z target graphu, side-effect tříd, vybraného modelu a provozní evidence. Každá derivace je viditelná a auditovaná.

### 41.5 Browser

OWNER-editable browser inputs jsou pouze:

- URL/target a požadovaný business cíl,
- volba server-managed nebo OWNER Device Bridge, pokud má business význam,
- externí účet/tenant a credential/Secret/certificate binding,
- zda uložit účet nebo použít pouze ephemeral session,
- zda použít konkrétní OWNER lokální browser profile při bridge flow,
- allowed lokální target pro bridge,
- pravidlo periodické činnosti, timezone, schedule/event trigger a business overlap preference,
- explicitní irreversible confirmation nebo challenge/dialog/permission response,
- browser engine pouze při skutečném compatibility požadavku,
- rozhodnutí o uložení password/TOTP/virtual-authenticator state do Secret Manageru.

`SYSTEM_MANAGED` jsou:

- browser executable path, channel, launch mode a arguments,
- Node/Playwright/browser/OS/dependency/font/runtime build manifest a compatibility,
- host slot count, warm hosts, blue/green upgrade, drain a process recycling,
- non-persistent context policy, max contexts/pages/renderers a capacity,
- page/frame/document identity, navigation sequence a stale-event handling,
- one-writer control lease, epoch/fence, takeover drain a input-state reset,
- navigation/action/challenge/idle/reconciliation timeouts a deadlines,
- preview codec, frame cadence, resolution, quality, patch, key-frame a reconnect policy,
- viewport/DPR/zoom/visual-viewport/nested-frame transform,
- observation DOM/semantic/visual/network budget,
- locator compiler, actionability, trial/force a overlay policy,
- input strategy pro fill/type/composition/contenteditable/masked/locale fields,
- mutation-trigger classification a dispatch phase evidence,
- pre-armed popup/navigation/dialog/filechooser/download waiters,
- screenshot/trace/DOM snapshot policy,
- upload/download limits, persistence, content verification a scan flow,
- state-bundle member serializer, capture barrier, restore order, retention a verification cadence,
- account/tenant authenticated condition, auth epoch a account/resource concurrency,
- OTP/push/WebAuthn/passkey/client-certificate/CAPTCHA/native challenge adapters,
- bridge mTLS certificate/build/capability/connection epochs, profile lease a reconnect policy,
- schedule DST, misfire/catch-up, overlap, idempotency a bounded-run policy,
- queue capacity, backpressure, crash recovery, reconciliation a cleanup,
- drift/repair thresholds a monitoring SLO.

Technické hodnoty systém odvozuje z immutable browser/automation/account contractu, platform capability a provozní evidence. UI je zobrazuje v efektivním stavu s vysvětlením, ale běžný workflow je po OWNERovi nevyžaduje.

System-managed politika nesmí svévolně omezit legitimní OWNER browser capability. Volí bezpečný typed adapter, exact identity, fence, evidence a recovery. Pouze unattended activation vyžaduje deterministic target a postcondition; OWNER direct live control zůstává dostupné přes preview nebo bridge.

### 41.6 Monitoring — `SYSTEM_MANAGED`

- global scheduler interval,
- default probe interval,
- stale thresholds,
- repair cooldown,
- recertification interval,
- alert dedupe window,
- primary/backup channels,
- delivery retries,
- SLO defaults.

### 41.7 Logging a audit — `SYSTEM_MANAGED`

- process log level,
- debug retention,
- audit retention,
- archive path/target,
- live stream limits,
- export size,
- payload rendering,
- presentation mode default.

### 41.8 Runtime — `SYSTEM_MANAGED`

- queue capacities a worker poll intervals,
- lease durations a heartbeat intervals,
- runtime instance a monotonic runtime generation,
- canonical systemd unit/socket names, UDS paths, owners, groups a modes,
- systemd service generation, `InvocationID`, boot/start/cgroup identity validation,
- socket backlog, frame, connection, in-flight a stream/backpressure limits,
- namespace, read-only root, `/proc`/`/sys`/`/dev`/`/run` a writable tmpfs profiles,
- Linux capability, `NoNewPrivileges`, seccomp a syscall architecture profile digests,
- environment allowlist a inherited FD profile,
- child spawn aliases, process/thread count a orphan cleanup policy,
- CPU, memory high/max, swap, PID, FD, file, I/O a temporary storage limits,
- component shutdown/drain/kill timeout,
- state quotas,
- preview-ticket, state-handle a temporary-binding lifetimes,
- systemd credential generation/restart verification,
- runtime boundary audit/evidence retention,
- release retention.

Tyto hodnoty systém odvozuje z immutable runtime/release contractu a provozní evidence. OWNER je může zobrazit a ovládat canonical runtime actions, ale běžný workflow nevyžaduje ruční Linux/socket/seccomp konfiguraci. Žádná hodnota nevytváří runtime token, roli nebo permission.

### 41.9 Deployment — `SYSTEM_MANAGED`

- release root,
- data root,
- backup retention,
- readiness samples,
- startup deadline,
- accepted service list,
- certificate renewal threshold,
- maintenance window,
- acceptance schedules.

Workflow-specific `OWNER_INPUT` hodnoty vznikají kontextově v chatu nebo formuláři konkrétní integrace: credentials, účty, identifikátory, URL, soubory, obsah a business pravidla. Jejich technické propsání do těchto systémových parametrů provádí KájovoCML NG automaticky.

### 41.10 System-managed policy contract

Každá `SYSTEM_MANAGED` položka má immutable policy revision obsahující stable policy key, owner module, input schema a authoritative input refs, deterministic derivation algorithm/version/digest, unit, default, hard minimum/maximum, monotonic nebo hysteresis constraints, update trigger/cadence, effective-value state version, fallback, dependency failure behavior, audit event, requirement IDs, tests a acceptance gate.

Policy, která ovlivňuje deadline, lease/fence window, idempotency, side-effect admission, activation, rollback, security boundary, protocol compatibility, resource survival, retention nutnou pro replay nebo closure, nesmí být volně autotunovaná. Její safety bounds a relation jsou exact součástí operation/runtime contractu; policy smí uvnitř nich zvolit hodnotu pouze deterministicky z persistovaných vstupů.

Effective value se persistuje před použitím, pinne do příslušného request/run/release snapshotu a je viditelná v UI, API, chatu, auditě a evidence. Dva procesy se stejnou policy revision a vstupy musí vypočítat stejnou hodnotu. Neznámý vstup, derivation conflict nebo hodnota mimo bounds blokuje affected operation stabilní chybou; runtime nesmí použít skrytý default.

## 42. KCIP operation catalog

Názvy v této kapitole jsou jediný canonical operation namespace. Každá operation má právě jeden machine-readable record podle 55.6; dotted event, provider method, KBPP input nebo evidence kind, který zde není uveden, není implicitní operation. Alias smí existovat pouze jako explicitní transport/UI projection na jeden operation ID a nesmí vytvořit druhý logical outcome.

### 42.1 Component lifecycle operations

```text
component.register
component.revision.publish
component.validate
component.verify
component.activate
component.enable
component.disable
component.suspend
component.quarantine
component.restore
component.recertify
component.rollback
component.deregister
```

### 42.1.1 Component protocol operations

```text
component.heartbeat
component.state.query
component.state.report
component.control.enable
component.control.disable
component.control.ack
```

`component.enable` a `component.disable` jsou OWNER-visible business operations. Založí jednu logical operation a podle jejího state machine dispatchují odpovídající interní `component.control.enable` nebo `component.control.disable`. Interní control command, ACK, heartbeat a state report nevytvářejí druhý business outcome a používají přesný protokol kapitoly 44.

### 42.2 Runtime operations

```text
runtime.prepare
runtime.instance.start
runtime.ready.report
runtime.state.report
runtime.heartbeat
runtime.invoke
runtime.cancel
runtime.drain
runtime.stop
runtime.instance.restart
runtime.instance.reconcile
runtime.boundary.verify
runtime.connection.inspect
runtime.cleanup.resume
```

Operations vytvářejí serverové guarded commands. Neexistuje operation pro create/update execution contextu, přidělení runtime credentialu, změnu peer UID/GID/PID, vložení bindingu do callu, otevření interního socketu nebo bypass gateway.

### 42.3 MCP operations

```text
mcp.request.validateTransport
mcp.request.validateJsonRpc
mcp.request.reserveId
mcp.request.finalize
mcp.server.discover
mcp.era.probe
mcp.era.invalidate
mcp.discovery.snapshot
mcp.discovery.invalidate
mcp.cache.invalidate
mcp.tools.list
mcp.tools.call
mcp.tools.progress
mcp.tools.cancel
mcp.tools.reconcile
mcp.input.required
mcp.input.respond
mcp.resources.list
mcp.resources.templates.list
mcp.resources.read
mcp.prompts.list
mcp.prompts.get
mcp.subscription.listen
mcp.subscription.acknowledge
mcp.subscription.notify
mcp.subscription.complete
mcp.subscription.cancel
mcp.stateHandle.create
mcp.stateHandle.resolve
mcp.stateHandle.close
mcp.task.create
mcp.task.get
mcp.task.update
mcp.task.cancel
mcp.task.expire
mcp.task.notify
mcp.contract.validate
mcp.contract.compatibility
mcp.legacy.probe
mcp.legacy.adapt
mcp.wire.verify
```

Transport/JSON-RPC validation operations nevytvářejí business call ani handler side effect. `mcp.request.reserveId` je transportní in-flight reservation, nikoli business idempotency. `mcp.task.cancel` pouze persistuje intent; terminal state vzniká samostatným guarded task transitionem. `mcp.legacy.probe` může následovat pouze explicitní legacy-candidate evidence podle 10.19.

### 42.4 Agent operations

```text
agent.run.start
agent.run.status
agent.run.pause
agent.run.resume
agent.run.cancel
agent.message.append
agent.state.report
agent.model.started
agent.model.completed
agent.tool.request
agent.tool.result
agent.tool.failed
agent.delegate.request
agent.delegate.result
agent.approval.request
agent.approval.approve
agent.approval.reject
agent.checkpoint.created
agent.memory.read
agent.memory.write
agent.session.compact
agent.eval.start
agent.eval.result
agent.run.complete
agent.run.fail
agent.run.manualReview
```

### 42.5 Secret operations

```text
secret.resolve
secret.version.create
secret.version.activate
secret.rotate
secret.bind
secret.unbind
secret.usage.report
```

### 42.6 Monitoring operations

```text
monitor.probe.request
monitor.probe.result
monitor.heartbeat.observe
monitor.state.transition
monitor.alert.open
monitor.alert.update
monitor.alert.close
monitor.repair.enqueue
```

### 42.7 Browser operations

```text
browser.runtimeBuild.register
browser.runtimeBuild.verify
browser.host.ready
browser.host.drain
browser.host.recover
browser.session.create
browser.session.attach
browser.session.observe
browser.session.state
browser.session.pause
browser.session.resume
browser.session.close
browser.session.recover
browser.page.open
browser.page.activate
browser.page.close
browser.page.observed
browser.frame.observed
browser.document.changed
browser.navigation.observed
browser.preview.ticket.create
browser.preview.viewer.connected
browser.preview.viewer.disconnected
browser.preview.resync
browser.control.acquire
browser.control.release
browser.control.changed
browser.control.transfer
browser.target.pick
browser.target.revalidate
browser.action.start
browser.action.status
browser.action.dispatchPhase
browser.action.cancel
browser.action.reconcile
browser.action.resolveOutcome
browser.action.complete
browser.action.fail
browser.dialog.opened
browser.dialog.respond
browser.permission.request
browser.permission.respond
browser.challenge.required
browser.challenge.resolve
browser.auth.verify
browser.account.save
browser.account.verify
browser.account.logout
browser.account.authEpoch.increment
browser.state.capture
browser.state.verify
browser.state.activate
browser.state.invalidate
browser.upload.create
browser.upload.consume
browser.download.started
browser.download.persist
browser.download.verify
browser.artifact.created
browser.bridge.enroll
browser.bridge.connect
browser.bridge.assign
browser.bridge.release
browser.bridge.test
browser.bridge.rotateCertificate
browser.bridge.revoke
browser.profile.acquire
browser.profile.release
browser.teaching.start
browser.teaching.compile
browser.automation.preflight
browser.automation.verify
browser.automation.run
browser.automation.cancel
browser.automation.reauthenticate
browser.automation.reconcile
browser.automation.repair
browser.schedule.evaluate
browser.run.manualReview
browser.cleanup.resume
```

Každá operation používá KCIP envelope se serverovým source execution contextem, target Browser Runtime component, session/definition ID, exact revision/runtime-build/binding digests, browser identity snapshot, idempotency podle side effectu a correlation. Caller nemůže dodat authoritative host/context/page/frame/document/control identity.

Preview frame binary transport používá KBPP; lifecycle, page/frame/document registry, control, action dispatch phases, side-effect intent/outcome, account state a audit evidence zůstává KCIP/API/PostgreSQL autoritou.

`browser.action.dispatchPhase` přijímá pouze monotonic typed phase od current host/bridge fence. Phase po possible mutation triggeru může zpřísnit recovery na reconciliation, nikdy ji vrátit do safe pre-dispatch retry. `browser.action.reconcile` neodesílá původní input; provádí pouze exact read-back. `browser.action.resolveOutcome` je OWNER manual-review CAS.

Bridge operations jsou přijatelné pouze z exact mTLS bridge transportu a current connection/session assignmentu. Neexistuje operation pro raw CDP/Playwright, shell, arbitrary filesystem path, obecný network proxy nebo přímé přidělení profile bez BrowserSessionService.

### 42.8 Audit operations

```text
audit.event.append
audit.stream.ack
audit.stream.replay.request
audit.stream.replay.result
audit.integrity.verify
audit.archive.enqueue
audit.archive.complete
```

### 42.9 Centrální chat a API access

```text
chat.conversation.create
chat.message.append
chat.response.stream
chat.command.execute
chat.browser.session.create
chat.browser.session.attach
chat.browser.target.attach
chat.browser.control.acquire
chat.browser.control.returnToAi
ownerApiKey.read
ownerApiKey.reveal
ownerApiKey.rotate
ownerApiKey.session.exchange
```

### 42.10 Self-test operations

```text
selfTest.catalog.list
selfTest.run.start
selfTest.run.status
selfTest.run.cancel
selfTest.run.cleanup
selfTest.evidence.read
selfTest.registeredElement.run
```

### 42.11 Generation operations

```text
generation.job.create
generation.message.append
generation.turn.interrupt
generation.source.add
generation.capability.resolve
generation.spec.propose
generation.spec.precheck
generation.spec.approve
generation.plan.create
generation.plan.validate
generation.phase.start
generation.model.execute
generation.workspace.patch
generation.workspace.validate
generation.candidate.publish
generation.integration.step
generation.validation.run
generation.blocker.open
generation.blocker.resolve
generation.activation.prepare
generation.activation.switch
generation.activation.rollback
generation.job.cancel
generation.job.resume
generation.job.retry
generation.job.complete
```

### 42.12 Agentic authority a provenance operations

```text
authority.lineage.resolve
authority.lineage.append
authority.intent.compile
authority.intent.validate
authority.context.create
authority.context.validate
authority.actionPlan.compile
authority.actionPlan.validate
provenance.content.register
provenance.segment.compile
provenance.valueDerivation.create
secret.useContext.create
agentic.security.event.record
agentic.security.evidence.export
```

Tyto operace jsou interní deterministické kroky stávajících command/run state machines. Nevytvářejí principal, role, scope, grant, token, permission ani nový OWNER approval.

## 43. Iterační, konvergenční a rozhodovací pravidla

### 43.1 Discussion iterace

Jedna OWNER zpráva může vyvolat více modelových kol. Každé kolo má turn ID, model call ID, provider response ID, pořadí, visible text delta, output items, tool requests/results, budget consumption, started/completed timestamps a terminal reason.

Limit turns, času, tool calls, tokenů a cost systém odvozuje z typu úkolu, selected OpenAI modelu, velikosti vstupu a provozní evidence. Účinné hodnoty jsou viditelné. Vyčerpání limitu vytvoří typed blocker nebo failure; systém nepokračuje neomezeným skrytým loopem.

### 43.2 Model-call konvergence

Každý specialistický call má jeden očekávaný structured artifact. Server přijme pouze terminal, schema-validní a guardrail-validní output. `REFUSED`, `INCOMPLETE`, schema-invalid, provider failure, cancelled a expired jsou oddělené výsledky.

Schema repair používá stejný output schema digest a přesný validator report, ale je novou explicitní model logical operation s vlastním request descriptorem; neopakuje již potvrzený tool/hosted side effect. Po bounded počtu neúspěchů vznikne blocker nebo deterministic fallback transformace, pouze pokud ji revision předem deklaruje a output znovu projde exact schema.

### 43.3 Specification konvergence

Specification proposal vzniká po capability-first, source inspection a uzavření relevantních OWNER otázek. OWNER vstup po proposal vytváří novou revision. Identická canonical specification se neduplikuje. Každá revision zobrazuje diff a requirement coverage.

Specifikace konverguje pouze při prázdných blocking questions, current capability snapshotu, validních MCP/agent contracts a úplném acceptance/rollback plánu.

### 43.4 Plan konvergence

Planner vytvoří DAG a plan validator vrátí všechny graph, coverage, ordering a compensation chyby. Repair mění pouze plan candidate; approved specification zůstává immutable.

Plan je přijat, když každý requirement má producer a verification node, každý side effect má ordering a compensation a activation dependencies tvoří konzistentní set.

### 43.5 Implementation iterace

Implementation worker používá cyklus:

```text
produce typed patch
→ compare base digests
→ atomic apply
→ complete workspace read-back
→ static and focused tests
→ classify all diagnostics
→ repair coherent root-cause cluster
→ repeat
```

Každé kolo ukládá workspace revision, patch, validator outputs, tests, model/tool evidence, duration a progress signature. Server uplatňuje stejný konečný validator na každý výsledek bez ohledu na modelové tvrzení.

### 43.6 Stagnation

Stagnation detector sleduje:

- opakovaný diagnostics digest,
- opakovanou změnu stejných souborů bez gate progressu,
- rostoucí počet chyb,
- cyklické vracení workspace digestů,
- nulový requirement coverage progress.

Při stagnaci se automatický loop zastaví, vytvoří souhrnnou root-cause evidence a použije jiný repair strategy pouze pokud je předem deklarovaný. Po vyčerpání strategií vznikne `VALIDATION_STAGNATION` blocker.

### 43.7 MCP contract konvergence

MCP contract se považuje za konvergentní, když:

- naming a aliases jsou bez kolizí,
- input/output schemas jsou strict a testovatelné,
- side-effect/retry/idempotency/concurrency jsou úplné,
- handler behavior a errors pokrývají všechny requirements,
- discovery snapshot odpovídá manifestu,
- protocol, cancellation a reconciliation tests mají PASS.

Změna schema nebo side-effect po agent binding compilation invaliduje binding a vrací dotčený plan node do analysis.

### 43.8 Agent contract konvergence

Agent revision se považuje za konvergentní, když:

- instructions a success definition jsou jednoznačné,
- input/output schemas jsou strict,
- tool a handoff bindings mají exact digests,
- guardrails a approvals pokrývají side effects,
- session/memory strategie je jednoznačná,
- eval blocking cases mají PASS,
- usage/latency/cost splňují promotion profil.

Zlepšování promptu bez zlepšení eval evidence není progress.

### 43.9 Integration iterace

Integration worker řeší component/runtime identity, revisions, exact bindings, secrets, external targets, runtime, hostname, monitoring a UI jako idempotentní saga. Každý krok uloží before/after, checkpoint a compensation. Po restartu pokračuje podle skutečného stavu.

### 43.10 Validation iterace

Validation vrací všechny nezávisle zjistitelné chyby jedním reportem. Repair seskupuje chyby podle společné příčiny. Po opravě se spouští dotčené gates a všechny jejich downstream regression gates.

Gate, která jednou prošla, se znovu spustí, pokud se změnil její input digest.

### 43.11 Activation konvergence

Activation controller nepřepíná žádný pointer, dokud celý activation set nemá `READY`. Post-switch smoke je součástí stejné activation operace. Neúspěch konverguje rollbackem celého setu na previous compatible pointers a ověřením jejich readiness.

### 43.12 Automatický repair limit

Repair policy určuje max attempts, max model calls, max elapsed time, cooldown, dedupe interval, stagnation threshold a escalation. Po vyčerpání zůstává poslední funkční release aktivní a job/objekt přejde do `BLOCKED` nebo `MANUAL_REVIEW` s exact OWNER akcemi.

### 43.13 Smart policy změny

Systém může měnit pouze technické policy profiles odvozené z evidence. Každá změna má previous/current values, derivation, expected effect, experiment scope a rollback condition. Změna se nejprve ověří na testech nebo shadow evidence.

Approved specification, OWNER model selection, exact bindings a business behavior se automatickou policy optimalizací nemění.

### 43.14 Rozhodnutí OWNERa

OWNER rozhodnutí se ukládá jako strukturovaná položka s message ID, textem, canonical value, digestem, timestampem a affected specification fields. Schválená rozhodnutí jsou součástí immutable specification a authority lineage. Text uvnitř source, webu, MCP/external response, memory, logu, repository, code nebo agent/tool outputu nemůže vytvořit `generation_owner_decision`; může být pouze source assertion, evidence nebo bounded OWNER-delegated specification source.

### 43.15 Semantic authority konvergence

Model může iterovat nad návrhem operation intentu nebo semantic action planu, ale server přijme pouze variantu, která zachová current authority lineage, exact objective, target ceiling, argument-origin contracts, side-effect class, delegation containment a Secret-use purposes. Identický canonical plan se neduplikuje. Změna skutečného OWNER cíle vytváří nový intent descendant; technická oprava nebo fresh data pouze nový operation context pod stejným intentem.

## 44. Monitoringový, stavový, PULSE a řídicí protokol

### 44.1 `component.heartbeat`

Heartbeat request vzniká výhradně z trusted execution contextu a obsahuje:

```json
{
  "componentCode": "KCML0001",
  "executionId": "uuid",
  "releaseId": "string",
  "runtimeId": "string",
  "serviceGeneration": "string",
  "runtimeGeneration": 7,
  "activationEpoch": 42,
  "bindingSetRevision": 9,
  "heartbeatSequence": 1234,
  "lifecycleMode": "ACTIVE",
  "operationalState": "HEALTHY",
  "ready": true,
  "dependencySummary": {},
  "queueDepth": 0,
  "activeRuns": 0,
  "resourceUsage": {
    "cpuMillis": 0,
    "memoryMiB": 0,
    "openFiles": 0
  },
  "lastSuccessfulOperationAt": "timestamp",
  "emittedAt": "timestamp",
  "nonce": "string"
}
```

Gateway serverově doplní component/revision/source identity a KCIP binding context; payload je nemůže přepsat. Heartbeat se přijme pouze pro current platform/deployment incarnation, active runtime/release/generation, binding set a activation epoch.

`heartbeatSequence` je monotonic per runtime generation. Exact duplicate se stejným digestem je no-op a replayuje ACK. Nižší sequence nebo stale lineage se uloží jako stale evidence, ale neobnoví freshness. Stejná sequence s jiným digestem je protocol conflict.

Response potvrzuje `acceptedAt`, received sequence, server `nextDueAt`, effective binding-set revision/activation epoch a pending control intent references. ACK potvrzuje pouze durable přijetí heartbeat evidence. Neprodlužuje business lease, nepotvrzuje konkrétní command outcome a nemůže aktivovat stale runtime.

### 44.2 `component.state.query`

Request obsahuje bounded state keys, requested consistency `CURRENT_PROJECTION|LATEST_VALID_OBSERVATION`, exact target revision/binding/epoch snapshot a deadline. Response vrací pro každý key typed payload, schema/payload digest, state version, observedAt, source release/runtime/generation, source sequence a staleness classification.

`CURRENT_PROJECTION` čte autoritativní server projection. `LATEST_VALID_OBSERVATION` může vrátit čerstvější observation, ale musí ji výslovně označit jako nepromítnutou. State query nesmí samo měnit state ani freshness.

### 44.3 `component.state.report`

State report obsahuje jeden nebo více observations, z nichž každá má state key, typed payload, schema/payload digest, observedAt/emittedAt, release/runtime/service generation, activation epoch, binding-set revision a source sequence.

Server pod component root lockem ověří current lineage, schema, sequence a digest. Validní nová observation se uloží a podle state contractu promítne. Duplicate je no-op. Stale observation se uchová pouze jako evidence. Sequence conflict nebo schema mismatch se odmítne a vytvoří monitoring error; nesmí změnit current projection.

### 44.4 `component.control.enable` a `component.control.disable`

Control request obsahuje:

- command ID a logical operation ID,
- desired state,
- reason a correlation/causation,
- effective deadline,
- request digest a idempotency key,
- target component/revision/release/runtime generation,
- binding-set revision a activation epoch,
- expected current component/activation state version.

Gateway nejprve durable uloží command, idempotency record, expected guards a dispatch outbox. `ACCEPTED` odpověď znamená pouze commitnutý command. Target runtime před effectem ověří current generation, binding/epoch, deadline, cancellation a fence.

Enable/disable effective outcome vyžaduje kombinaci fenced control outcome, current route/runtime evidence, state projection a heartbeat current lineage. Samotné ACK ani heartbeat success nestačí.

### 44.5 `component.control.ack`

ACK rozlišuje dvě vrstvy:

1. **Admission ACK** — `ACCEPTED|REJECTED`.
2. **Outcome ACK** — `COMPLETED|FAILED|UNKNOWN`.

ACK obsahuje command ID, logical operation, request digest, status, current/observed state a state version, runtime/release/service/runtime generation, binding-set revision, activation epoch, source sequence, detail, result digest, correlation a timestamp.

Duplicate ACK se stejným digestem je idempotentní. Stejný command/sequence s jiným digestem je conflict. ACK ze stale runtime/release/binding/epoch se uloží jako stale evidence a nesmí změnit current command/component projection.

`ACCEPTED` není terminal outcome. `COMPLETED` potvrzuje exact requested postcondition. `FAILED` potvrzuje známé neprovedení nebo známé selhání. `UNKNOWN` vytváří reconciliation/manual review a blokuje conflicting command. `CANCELLED` je interní command terminal state až po known outcomes a cleanup, nikoli wire ACK status.

### 44.6 PULSE protocol

PULSE port a event používají contract 6.11. Producer v jedné transakci commitne domain state/event a PULSE outbox. Consumer v jedné transakci ověří exact active port/binding/schema/sequence, uloží inbox dedupe a případný command/event intent a teprve poté vrátí acceptance ACK.

ACK neznamená business completion. Duplicate event ID/same digest neaplikuje změnu. Same event ID/different digest je conflict. Schema/binding drift, contiguous gap a exhausted retry se uloží do dead-letter evidence; žádný event se potichu nezahodí nebo nepřesměruje.

### 44.7 Probe protocol

Probe request obsahuje probe ID, type, component/release/runtime lineage, timeout/deadline, test input, expected contract, idempotency key a cleanup contract. Probe result obsahuje status, latency, observed payload, expected comparison, evidence digest, logs a cleanup result.

Probe observation má vlastní sequence a nesmí přepsat business state. Mutující probe používá test fixture, explicitní side-effect/reconciliation contract a complete cleanup.

### 44.8 Alert protocol

Alert event obsahuje alert key, source, severity, occurrence, current state/version, evidence, correlation a recommended canonical actions. Doručovací envelope obsahuje idempotency key a signed/provider-specific payload podle exact external binding. Alert state a jednotlivé deliveries jsou oddělené; duplicate delivery nesmí vytvořit druhý alert occurrence.

### 44.9 Repair protocol

Repair enqueue obsahuje source component/release, functional authority reference, failure evidence, dedupe key, repair scope, current state/version/binding/epoch a rollback point. Repair result obsahuje candidate, validation/conformance evidence, activation nebo preserved release.

Repair nesmí z interpretace heartbeat, state observation, PULSE gap nebo unknown control outcome odvodit neexistující business failure. Nejprve provede declared verification/reconciliation a teprve poté vytvoří candidate nebo exact operational action.

## 45. Vizuální a interakční detaily

### 45.1 Typografie

- základní text 12,5–14 px podle kontextu,
- tabulky 12–13 px,
- page title 22–28 px,
- panel title 13,5–16 px,
- monospace pro codes, digests, JSON, logs a credentials,
- line-height 1,4–1,55,
- numeric tabular figures pro metriky a časy.

### 45.2 Spacing

Základní spacing scale:

```text
4, 6, 8, 10, 12, 16, 20, 24, 32, 40 px
```

Workspace desktop padding 24–26 px, mobil 12–16 px. Panel header 12–16 px. Tabulkové řádky jsou kompaktní a touch-friendly podle viewportu.

### 45.3 Status barvy

- green: healthy, active, completed, pass,
- amber: warning, pending, due, challenge,
- red: failed, unhealthy, critical, blocked,
- teal: primary action, selected, informational active,
- gray: inactive, disabled, archived, unknown.

Stav používá barvu, ikonu a text současně.

### 45.4 Tlačítka

Primary používá teal background a bílý text. Secondary používá bílý povrch a border. Danger používá tmavší red. Disabled stav zachovává label a vysvětlení v tooltipu nebo inline textu.

### 45.5 Tabulky

- sticky header,
- definované column widths,
- sort indicator,
- row hover a keyboard selection,
- bulk select,
- column visibility,
- filters a saved views,
- pagination/virtualization,
- bounded horizontal scroll uvnitř panelu,
- action menu na konci řádku.

### 45.6 Dialogy

Dialog má title, účel, affected object, form/content a pravostranné action footer. Debug detail je povinný pro error, conflict, stale, recovery, destructive, security a diagnostic dialog a v ostatních dialogách se vynechá. Destructive confirmation zobrazuje exact object, action a consequence. Focus je uzavřen uvnitř dialogu.

### 45.7 Log a JSON viewer

Viewer poskytuje line wrap toggle, syntax highlight, search, copy path/value, expand/collapse, raw/formatted, diff a download. Secret values jsou OWNERovi dostupné ve stejné podobě jako ostatní data.

### 45.8 Browser preview

Preview panel zachovává content aspect ratio a nabízí `Fit`, `Actual`, lokální display zoom, tab/page switcher, URL/title, account/tenant marker, back/forward/reload, target picker, screenshot, pointer/keyboard/touch mode, sensitivity/presentation toggle a action timeline.

Horní stavový pruh vždy zobrazuje control a recovery stav:

```text
AI ovládá
Přebírání
Ovládáte vy
Automatizace ovládá
Čeká na Vás
Reconciliuje mutaci
Pozastaveno
Obnovuje se
Uklízí
```

Vedle stavu je current page/document indicator, stream/control epoch a stale/resync badge. Technický detail po rozbalení zobrazuje runtime build, host/bridge/context generation, page/frame/document identity a viewport transform digest.

Remote page pointer používá vlastní cursor overlay. Action target používá outline, label, action kind, candidate count a mutation badge; OWNER target picker používá odlišný outline a po výběru vytvoří chip. Overlay není součástí cílového DOM ani page screenshot artifactu.

Input se povolí pouze při active OWNER control lease a po acknowledgmentu current stream/page/document/frame/viewport snapshotu. Při stale identity UI vstup nezopakuje; zobrazí `Obnovuji náhled` a čeká na fresh snapshot. Keyboard remote mode má zřetelný badge a Escape návrat do UI.

Takeover/return UI zobrazuje `DRAINING`, pending action a případnou reconciliation. OWNERovi se control nepřepne jen změnou textu v UI. Po reconnectu se pressed keys/buttons/touch/composition stav lokálně resetuje a žádný neacknowledged input se nereplayuje.

Challenge panel nepřekryje důležitou část preview a lze jej docknout. Zobrazuje exact origin/RP/account, challenge type, expiry a resolution route. Credential input a **Uložit účet** používají compact side sheet s exact targetem, account/tenantem, ukládanými state members, credential/auth epoch a authenticated condition.

Popup/frame tree umožňuje explicitní aktivaci page pro preview, ale upozorňuje, že jiné pages mohou dál běžet. Cross-origin/OOPIF, detached, inaccessible sandboxed/fenced frame a stale document mají odlišné badges.

Action timeline rozlišuje `intent`, `host accepted`, `target`, `input started`, `mutation possible`, `method returned`, `postcondition`, `reconciliation` a `outcome`. Neznámý outcome je výrazně označený a nabízí pouze canonical read-back/OWNER-resolution actions, nikoli opakovat click.

Upload panel používá artifact handles; download panel rozlišuje started, streaming, completed, failed, persisted a verified. Context close varuje před unfinished downloadem nebo pending state capture.

Mobilní preview používá full-screen canvas, bottom control bar, pinch pouze pro lokální zobrazení a samostatné tlačítko remote scroll. Klientský zoom se nesmí zaměnit za browser viewport transform. Touch gesture je odesláno pouze pod current transform a control epoch.

### 45.9 Topologický canvas

Canvas používá kontrastní nodes, port labels, directional edges, compatibility color a event animation s reduced-motion variantou. Edge selection má dostatečnou hit area. Keyboard connect používá source port → target port workflow.

## 46. Implementační traceability

### 46.1 Normativní atomy a requirement identity

Každý normativní atom tohoto SSOT má stable `requirementId` podle 55.5. Requirement ID není odvozen z line number, implementačního souboru, test name ani release. Přesná semantic změna vytváří nový content-addressed ID a explicitní lineage; čistě prezentační změna ID nemění.

Kanonický Requirement Registry je deterministická projekce SSOT. Registr nesmí doplnit význam, který v SSOT není, ani odstranit povinnost jako `not applicable` bez machine-checkable reason predicate.

### 46.2 Povinná lineage

Každý requirement record mapuje podle relevance celý řetězec:

```text
requirement
→ authority/source a případná OWNER intent lineage
→ canonical specification statement
→ domain module a canonical writer
→ aggregate root a state machine
→ operation a command schema
→ state/version/idempotency/concurrency/fencing contract
→ side-effect/checkpoint/reconciliation/cancellation/cleanup contract
→ PostgreSQL contract a physical constraints
→ runtime/trust boundary
→ exact bindings
→ API operation
→ UI surface a object action
→ central-chat capability
→ audit a evidence kinds
→ test, model command, invariant a fault points
→ recovery oracle a closure predicate
→ acceptance gate
→ repository artifact a release
→ runtime/production evidence
```

Nepoužitelný článek řetězce má explicitní `notApplicable` reason code. Prázdná hodnota, komentář nebo předpoklad architekta výjimku nevytváří.

### 46.3 Bidirectional artifact traceability

Každý produkční source file, generated artifact, schema, database object, migration, systemd unit, API route, UI action, chat capability, test, fixture, formal model, fault point, deployment script a acceptance evidence odkazuje na jeden nebo více requirement IDs.

Každý requirement odkazuje zpět na všechny aktivní implementation artifacts, tests a evidence. Generation artifact manifest obsahuje requirement IDs, operation IDs, registry record IDs, source digests a output digests pro každý produkční soubor, contract a test.

Aktivní artifact bez requirement relation je orphan implementation artifact. Aktivní requirement bez implementace, testu nebo evidence relation je orphan requirement. Oba stavy blokují build, CML conformance, release promotion a activation.

### 46.4 Traceability konzistence

Jeden requirement nesmí mít dvě aktivní implementační interpretace s rozdílným observable, persistence, concurrency, recovery, security nebo protocol významem. Dvě varianty implementace jsou přípustné pouze při shodném operation/state/DB/runtime/error/acceptance contractu a společných conformance tests.

Každý implementation artifact má právě jeden owner module a každý authoritative write artifact odkazuje na canonical writer podle 55.15. Shared library, migration, test helper ani deployment script nesmí vytvářet druhou write cestu.

### 46.5 Machine validation

CI a generation validation deterministicky ověřují:

- 100 % normativních atomů v Requirement Registry,
- 100 % operations v Operation Catalog Registry,
- 100 % mutable aggregates ve State Machine Registry,
- 100 % mutujících operations v PostgreSQL Contract Matrix,
- 100 % runtime types v Runtime Boundary Matrix,
- 100 % exact bindings v Binding Registry,
- 100 % critical cutpoints ve Fault Catalog,
- 100 % recovery-relevant states v Recovery Oracle Registry,
- 100 % terminal roots v Closure Predicate Registry,
- 100 % blocking gates v Acceptance Gate Registry,
- 100 % OWNER-visible operations v Exposure Parity Registry,
- nulový orphan requirement, artifact, operation, state edge, constraint, test nebo evidence record,
- shodu `ssotDigest`, registry digests a `packDigest`.

Validation report obsahuje exact chybějící record IDs a source refs. Chybějící mapování, unresolved conflict, stale digest nebo ručně upravená projekce je blocking failure; nelze jej nahradit modelem, textovým prohlášením ani následným rozhodnutím Software Architecta.

### 46.6 Release a runtime evidence

Release manifest pinne exact SSOT version/digest, Contract Pack digest, requirement coverage digest a artifact trace digest. Runtime evidence odkazuje na stejné requirement/operation/state/binding IDs jako build a test evidence.

Production closure report musí pro každý blocking requirement dohledat evaluator, test result, runtime evidence a terminal closure result. Evidence z jiné release, migration chain, browser runtime build, OpenAI SDK profile nebo activation epoch se nepovažuje za pokrytí current release.

## 47. Kompletní stav ukončení projektu

Projektový closure report obsahuje:

- accepted source commit a release ID,
- exact SSOT version/digest, Contract Pack digest, Requirement Registry coverage, Authority Ownership Registry a Architecture Readiness Gate results,
- produkční domain/IP/TLS stav,
- database baseline a migrations,
- current platform incarnation a application deployment epoch,
- activation head, binding-set revisions a active/rollback epoch inventory,
- služby, queues, leases, fencing tokens, heartbeats a concurrency claims,
- transactional outbox/inbox watermarks a gap evidence,
- object inventory včetně aggregate state versions a terminal states,
- MCP server/prvek inventory včetně discovery a contract digestů,
- MCP logical operations, idempotency outcomes, tasks, MRTR a side-effect reconciliation inventory,
- AI agent inventory včetně revision, tool bindings, eval, session policies, checkpoints a pending-call closure,
- agentic authority inventory včetně current lineages/intents/operation contexts, semantic action plans, provenance graph, value derivations, Secret-use contexts, security events a nulových invalid pending dispatches,
- browser runtime build, host/bridge connection, context, page/frame/document lineage, session, automation, schedule, account/auth binding, state bundle/profile lease a capability inventory,
- KBPP preview/control, target/locator, action attempt/dispatch phase/mutation trigger, challenge/dialog/permission, postcondition/reconciliation, upload/download, recovery a cleanup evidence,
- generation job, plan, checkpoint, validation a activation-set inventory,
- OpenAI model-call inventory, SDK versions, submit/retrieve evidence a single-key evidence,
- secrets, active versions, rotation epochs, Secret bindings/resolutions, contract bindings a external target bindings inventory,
- external side-effect intent/outcome/reconciliation a nulový seznam unresolved `UNKNOWN`,
- cleanup operation inventory s nulovým seznamem `INCOMPLETE`,
- monitoring/alerting/audit stav a audit/outbox consistency evidence,
- executable formal model, invariant coverage a crash-injection/nemesis výsledky,
- stateful command/state/edge coverage, stable fault-point catalog digest, complete single-fault × cutpoint matrix, pairwise/selected three-way schedules, random seed corpus, shrunk regression replays, linearizability histories, recovery-oracle a oracle-mutation evidence,
- current recovery barrier/database-start identity, recovery epoch, startup classification inventory a důkaz, že mutující admission začalo až po `READY`,
- terminal closure predicate report pro každý terminal root včetně nulového authority-bearing pending/provisional child inventory,
- A–W výsledky,
- 360/360 integrační výsledky,
- viewport QA,
- backup/restore a new-incarnation recovery evidence,
- production acceptance timing,
- `KCML_OWNER_API_KEY` a operation catalog coverage,
- centrální chat UI/API evidence,
- self-test catalog a registered-element results,
- MCP+agent live candidate, post-activation a rollback evidence,
- first CREATE `ABSENT` rollback a orphan cleanup evidence,
- UPDATE/REPAIR exact previous-snapshot rollback evidence,
- `KRMAR78`/`PASS` deployment sync evidence,
- trusted perimeter access-control evidence,
- seznam všech testů a prostředí,
- nulový seznam otevřených povinných požadavků, blockerů, unresolved `MANUAL_REVIEW`, orphan identities, provisional runtimes, stale leases/fences/bindings/checkpoints, mixed epochs, neúplných activation sets a pending cleanupů.

## 48. Kritické atomické invarianty výrobního toku

### 48.1 První CREATE candidate

Při selhání prvního CREATE candidate se rozhoduje podle activation linearizačního bodu:

1. před pointer switch commitem zůstává previous snapshot `ABSENT`; cleanup zastaví candidate runtime, deaktivuje candidate route/bindings a odstraní neautoritativní filesystem pointer,
2. po pointer switch commitu reverse-switch transakce s vyšším activation epoch atomicky obnoví `ABSENT`: odstraní active revision/release/binding/route pointers a zablokuje nové calls,
3. candidate runtime se zastaví až poté, co reverse switch commitl a candidate epoch je fenced,
4. release se označí `ROLLED_BACK` pouze po důkazu, že není current v žádném pointeru ani effective route/runtime,
5. cleanup musí být `COMPLETE` před vznikem remediation candidate,
6. job, provisional identity lineage, audit, attempts a evidence zůstávají.

Crash v každém kroku se obnoví podle activation/cleanup checkpointu; nevznikne orphan active identity, route, runtime ani binding.

### 48.2 Update a repair candidate

Při opuštění update/repair candidate frozen previous snapshot obsahuje exact revisions, releases, binding-set revision, routes, runtime targets, lifecycle/activation projection a activation epoch. Reverse-switch transakce ověří current candidate snapshot, alokuje vyšší epoch a obnoví celý previous snapshot atomicky.

Previous runtime se označí effective/active až po ack a readiness proti rollback epoch. Candidate je ihned po reverse switchi fenced a nemůže zapisovat. Failed candidate zůstává auditní evidence a cleanupne se idempotentně. Necompatible nebo neověřitelný previous snapshot activation před původním switchi blokuje; po switchi vede nejasný rollback do fail-closed `MANUAL_REVIEW`.

### 48.3 Live candidate integrace

Fáze `INTEGRATING` používá skutečný běžící candidate na jeho kanonickém HTTPS hostname. Provider callback, webhook challenge, Secret resolution, egress a exact target binding jsou ověřeny proti tomuto candidate.

### 48.4 Secret binding před resume

Před návratem blocked generation jobu do `INTEGRATING` systém:

1. uloží nebo aktivuje OWNER/provider secret,
2. odvodí exact Secret bindings z deklarovaných requirements a runtime revisions,
3. vytvoří nebo aktivuje tyto bindings jako součást candidate activation setu,
4. ověří runtime resolve,
5. teprve poté obnoví integrační provider/browser callback.

### 48.5 Provider-issued credential

Credential vydaný externím providerem se okamžitě uloží nebo rotuje v Secret Manageru, sváže s požadovanými komponentami a použije pro pokračující integraci.

### 48.6 Generalizovaný egress

`context.callExternal` podporuje HTTPS `GET`, `POST`, `PUT`, `PATCH`, `DELETE`, `HEAD`, query, headers, JSON, form, bounded binary response a provider secret auth. Vše prochází stejným exact external target bindingem, SSRF, Secret a audit policy.

### 48.7 Cross-origin browser

Browser Interaction Plane zachovává session pro redirects, tabs, popups, nested iframes a cross-origin/OOPIF contexts, ale nezaměňuje BrowserContext kontinuitu za kontinuitu dokumentu. Každá page má stable page ID a monotonic page generation; každý frame stable server ID a attachment epoch; každý committed document vlastní document ID/epoch. Locator, target reference, action, preview a postcondition uvádí celou route. Runtime směruje akci pouze do exact current context/page/frame/document a nový origin ověří před jakoukoli interakcí nebo credential exposure.

Popup/download/dialog/filechooser/navigation očekávání se registrují před trigger action. Event z jiného page/document/control epochu je evidence, nikoli splnění očekávání. OOPIF process swap nemění serverovou frame identity, pokud frame attachment/document lineage zůstává current; detach/reattach nebo nový document ji invaliduje.

OWNER pointer/keyboard/touch input je vázaný na current page/frame/document, observation revision, viewport transform a control epoch. Stale, hidden, covered nebo nejednoznačný input se odmítne bez silent re-targetu. Po input/change/blur nebo jiném možném mutation triggeru se failure řeší postcondition/reconciliation, nikoli automatickým opakováním clicku nebo textu.

### 48.8 Candidate validation

Server po každém modelovém výstupu znovu načte celý generated file set a ověří manifest, povinné soubory, export `tools`, export `invoke`, schemas, imports, runtime startup a sandbox. Validace je autoritou pro další kolo.

### 48.9 Rozdělení aplikace a generated release

KájovoCML NG application release používá signed CI/deploy flow. Generated component release vzniká lokálně v KájovoCML výrobním runtime a k aktivaci nepotřebuje externí repository, pull request, registry nebo vzdálený build systém.

### 48.10 Hlasové rozhraní

Voice klient používá stejné generation API, messages, SSE, tool loop, specification a approval jako textový web. Voice input se převádí na OWNER message a voice output čte pouze OWNER-visible assistant text a stavové souhrny.

### 48.11 Produkční API přístup

Každá UI a objektová funkce má kanonickou API operaci. Jediný `KCML_OWNER_API_KEY` používá plnou OWNER authority, komunikuje se všemi registrovanými prvky a obsluhuje centrální chat, testy, monitoring, správu i generation bez interaktivního loginu.

### 48.12 Jednotná OpenAI integrace

Každý AI agent a každá AI funkce používá oficiální balíčky `openai` a `@openai/agents`, Responses API, jeden `OPENAI_API_KEY` a OWNERem zvolený OpenAI model. AI podporované browserové iterace používají stejný klient; rutinní deterministický Browser Automation Runtime zůstává model-free.

### 48.13 Primary OWNER deployment

Pevná OWNER identita má username `KRMAR78`. GitHub Actions secret `PASS` je její autoritativní password source a každý deploy run provede synchronizaci a ověření této singleton identity.

### 48.14 UI vstupy a automatická konfigurace

Každý OWNER vstup vzniká v UI nebo centrálním chatu. OpenAI model a workflow-specific business/integration údaje tvoří OWNER-editable allowlist. Ostatní technické parametry systém spravuje automaticky a zobrazuje jejich účinný stav.

### 48.15 Důvěryhodný secret perimeter

Developerský počítač, privátní GitHub, deployment a produkční server tvoří jeden zabezpečený celek. Secrets se v něm používají přímo v kódu, chatu, logách, commitech, actions, artefaktech a runtime. AI a automatizované nástroje zachovávají tento režim a jeho plnou OWNER viditelnost.

### 48.16 Dostupnost bez OpenAI klíče

Aplikace startuje a poskytuje všechny ne-AI funkce, jediný OWNER API klíč, testy a správu ve stavu `OPENAI_CONFIGURATION_REQUIRED`. Vložení `OPENAI_API_KEY` v UI nebo chatu aktivuje AI capabilities bez jiné konfigurační operace.

### 48.17 Model-call idempotence

Local `ai_model_call`, immutable request descriptor, business idempotency record a provider-submit intent vzniknou před requestem. Pro create-class volání je implicitní retry oficiálního SDK vypnutý. Po commitnutém `DISPATCH_STARTED` se absence odpovědi nebo timeout nesmí vyložit jako `NOT_SUBMITTED`; bez persistovaného `response.id` vzniká `MODEL_SUBMIT_OUTCOME_UNKNOWN` a automatický druhý create je zakázaný.

Provider HTTP request ID je pouze diagnostika. Známý `response.id` se vždy retrieveuje, polluje, resumeuje nebo cancelleuje jako tentýž provider response. Každý tool-loop continuation je nový explicitní `ai_model_call` s unique successor reservation. Právě jeden provider output může být canonical pro attempt a parent jej použije pouze pod current fence.

### 48.18 Typovaný modelový výstup

Specification, plan, contract, patch, eval a release proposal mají strict versioned schema. Schema-neplatný modelový výstup nemění workspace, lifecycle, registr ani aktivní objekt.

### 48.19 Workspace compare-and-swap

Každý patch uvádí base workspace digest a expected previous file digests. Server patch atomicky odmítne při nesouladu, znovu načte celý workspace a vytvoří novou revision pouze z potvrzených souborů.

### 48.20 Exact agent tool binding

Agent binding obsahuje source component/tool revision a contract digest. Name-only binding je neplatný. Breaking discovery nebo contract drift zablokuje nový run před model call.

### 48.21 Approval a resume

Approval je vázané na exact pending call, arguments digest, target a expiration. Resume používá původní root run, agent graph a checkpoint. Změněný nebo nejednoznačný state je fail-closed.

### 48.22 Atomický activation set

Vzájemně závislé component releases, MCP/agent/automation revisions, contract/Secret/external bindings, routes a runtime targets se přepínají jednou pointer transakcí s jedním activation epoch. Activation barrier před switchi drain/reconciliuje mutující in-flight calls. Gateway připustí request proti právě jednomu current epoch; mixed set není možný. Postflight failure vyvolá atomický reverse switch celého previous nebo `ABSENT` snapshotu. Double activation, stale pointer a late candidate worker jsou odmítnuty CAS/fencing guards.

### 48.23 Background OpenAI recovery

Recoverable background Responses call explicitně používá `background=true` a `store=true`. Provider response ID se persistuje z create resultu nebo prvního `response.created` eventu. HTTP/SSE/client disconnect ani worker restart logical operation neukončí.

Recovery používá `responses.retrieve(responseId)` nebo resumable stream téhož response ID od posledního contiguous provider sequence. Resume se strict tools/structured parsing použije exact stejný tool/schema/settings descriptor. Terminal provider output se lokálně zpracuje právě jednou; handle expiry bez complete local outputu je `OPENAI_RESPONSE_EXPIRED`, nikoli důvod pro blind create.

### 48.24 Gate authority

Blocking validation, eval a conformance gate může označit PASS pouze serverový evaluator se vstupním a výsledkovým digestem. OWNER UI, chat ani model nemohou gate ručně obejít.

### 48.25 Completed generation hygiene

Job přejde do `COMPLETED` pouze po post-activation confirmation a kontrole, že nezůstává provisional runtime, orphan execution context, stale candidate route, pending binding, unresolved blocker, aktivní lease ani neúplný cleanup.

### 48.26 Jediný browser control writer

Browser session má nejvýše jeden active control lease enforced databázovým unique constraintem. Lease používá DB-time expiry, platform incarnation, application deployment epoch, context generation, monotonic fence a control epoch. Expired lease nelze obnovit heartbeat-em.

Takeover/return/recovery nejprve zastaví admission, drain/reconciliuje current mutaci a resetuje incomplete key/pointer/touch/composition state; teprve potom jednou transakcí revokuje starý lease, inkrementuje epoch/fence a vydá nový. Stará epoch/fence nemůže dispatchnout input, změnit page/frame/document projection, zapsat action outcome ani aktivovat state bundle.

Preview viewer, active page focus, browser process, bridge profile ani open WebSocket nejsou control authority.

### 48.27 Browser input bez stale page, frame, document nebo transformu

Pointer, touch, keyboard, text, secret, dialog, permission, filechooser a challenge input se provede pouze proti exact current runtime-build/host-or-bridge/context/page/frame/document/control identity a viewport/focus descriptoru.

Full navigation, BFCache restore, prerender activation, frame detach/reattach, OOPIF swap, page recovery, viewport/zoom/DPR change nebo takeover zneplatní relevantní input snapshot. Stale input je odmítnut před browser dispatch a nikdy se nepřepočítá odhadem ani nereplayuje po reconnectu.

### 48.28 Semantic target a mutation-trigger authority

Mutující AI nebo automation action používá právě jeden current semantic target, exact target reference nebo provider adapter. Bounding box, screenshot, pořadí kandidáta, persistovaný ElementHandle a podobná URL/label nejsou samostatnou autoritou pro unattended mutaci.

Každá potenciální mutace má explicitní earliest trigger. Side-effect intent, authority snapshot, target digest, account/resource concurrency claim a postcondition contract musí být commitnuty před pointer-down/click/keystroke/input/change/blur/submit/upload/dialog/permission/WebAuthn/device triggerem podle step contractu.

OWNER direct visual/native control zůstává dostupné; jednorázový OWNER input se však nestává unattended deterministic stepem bez jednoznačného targetu a ověřitelné postcondition.

### 48.29 Browser account continuity a device-bound state

Browser state bundle je použitelný pouze s matching account/tenant bindingem, external session family, auth epoch, runtime-build/serializer capability, credential/certificate version, binding revision, origin inventory a platnou authenticated postcondition.

Bundle capture probíhá po mutation barrieru a CAS pointer switchi. Concurrent logout, credential rotation, account switch nebo newer auth epoch znemožní aktivaci stale candidate bundle.

Standardní server bundle neobsahuje raw browser profile, cache/service-worker runtime, OS keychain, hardware private key ani reálnou platform passkey. Device-bound assertion/certificate používá current fenced OWNER Device Bridge nebo challenge; virtual authenticator je pouze explicitní automation/test account state.

### 48.30 Browser side-effect recovery a úplný cleanup

Worker/host/renderer/preview/bridge disconnect nesmí vytvořit duplicate browser side effect. Každá action má persistentní intent a monotonic dispatch phases. Recovery ověří celou browser identity n-tici, načte side-effect ledger a rozhodne:

- před possible mutation triggerem lze action bezpečně cancelnout nebo retryovat pouze s exact evidence,
- po possible triggeru se provede independent read-back,
- known result je `CONFIRMED_APPLIED` nebo `CONFIRMED_NOT_APPLIED`,
- neprokazatelný result je `UNKNOWN` a blokuje conflicting account/resource key do OWNER resolution.

Playwright success, URL, screenshot, network/load event, popup nebo download start samy nejsou canonical outcome.

Terminal session okamžitě blokuje nové actions/control/tickets a spustí persistentní cleanup. Cleanup revokuje control/bridge/profile assignment, reconciliuje mutations, dokončí/persistuje downloads, uloží final evidence/volitelný potvrzený state bundle, odstraní listeners/routes/init scripts/permissions/virtual authenticators/client-certificate handles, zavře pages/context, uvolní account/capacity claims a odstraní temporary artifacts.

Closure vyžaduje cleanup `COMPLETE`, nulový unresolved browser `UNKNOWN` a nulový orphan context/profile/ticket/download/authenticator/certificate handle; unknown outcome nikdy nevede k blind retry.

### 48.31 Trusted Runtime Boundary

Generated handler může vyvolat platformní operaci pouze přes anonymous capability FD k current runtime hostu. Runtime host a gateway odvodí execution context, target a exact binding serverově a při každém callu ověří Linux/systemd process identity, runtime generation, release/runtime digest, binding-set revision, activation epoch, application deployment epoch a platform incarnation.

Handler neobdrží OWNER API key, DB/broker credential, host socket path ani network/file descriptor. Private namespaces, read-only root, seccomp, nulové capabilities, environment/FD allowlist a cgroup cleanup zabrání bypassu přes loopback, UDS, `/proc`, filesystem handle, child nebo stale process.

Activation nebo deployment switch zneplatní starou runtime generation před cleanupem. Old process může zůstat fyzicky živý pouze jako fenced drain/cleanup subject a nemůže založit nový context ani authoritative write. Socket, process, namespace, child, stream a credential cleanup musí být `COMPLETE`; jinak closure zůstává blokovaná.


### 48.32 Immutable OWNER intent a authority lineage

Každý AI-originating mutující command má právě jednu current immutable authority lineage a OWNER intent nebo active revision intent. Modelový output, external content, tool result, browser page, memory, log ani generated code nemůže tyto roots vytvořit, přepsat nebo rozšířit. Změna skutečného cíle vzniká pouze novou autentizovanou OWNER zprávou, OWNER approval již definované specification nebo activation nové OWNER-approved revision.

### 48.33 Instruction/data separation a provenance preservation

Authoritative instructions se kompilují pouze z trusted revision/intent/runtime segments. Všechny ostatní vstupy zůstávají typed data/evidence/proposal/result s immutable provenance. Transformace, summarize, OCR, retrieval, handoff, memory, compaction a replay zachovávají původní instruction authority a nesmějí content přenést do authoritative instruction channelu.

### 48.34 Semantic action, target a argument authority

Tool, action, target a canonical arguments se dispatchnou pouze z `VALIDATED` semantic action planu pod current operation contextem. Každý model-derived field má declared slot; každý data-derived field má `value_derivation`. Rozdílný tool alias, target mimo intent, argument bez původu, unbound dynamic destination nebo vyšší side-effect class se odmítne před side-effect intentem.

### 48.35 Secret-use a delegation containment

Secret resolve vyžaduje existující exact Secret binding i immutable Secret-use context se shodným operation contextem, purpose, target/origin/account, placement a consumer step. Tato vazba není permission ani approval a neomezuje přímou OWNER práci se secrets. Handoff a agent-as-tool mohou předat pouze descendant operation context; child output ani untrusted content nemůže změnit Secret recipient nebo parent semantic ceiling.

### 48.36 Result re-entry a delayed injection

Model response, tool/MCP result, browser observation, external response, memory item, log, code/test output a compaction artifact se při každém opětovném vstupu označí původní provenance a authority. Obsah navrhující další akci je proposal; nový dispatch vyžaduje nový validated action plan pod stejným nebo novým legitimním OWNER intentem. Tím se delayed a second-order injection nemůže stát skrytým commandem.

### 48.37 Recovery admission barrier

Každý application start, PostgreSQL restart, database restore, application deployment epoch switch a explicitní platform recovery založí nebo obnoví persistentní `platform_recovery_head`. Dokud jeho current state není `READY` pro current database start identity, platform incarnation a application deployment epoch, žádná nová mutující operation nesmí získat dispatch authority, queue lease, browser mutation lease, model submit authority, external side-effect attempt ani activation pointer claim.

Read-only OWNER UI, health, audit, logy, inventory a recovery status zůstávají dostupné s explicitním stavem `RECOVERING`. Již durable accepted operation se nezahodí; recovery ji pod current fence klasifikuje právě jednou jako `RESUME`, `RECONCILE`, `CANCEL`, `TERMINAL_REPLAY` nebo `MANUAL_REVIEW`. Recovery worker sám používá lease, platform incarnation, deployment epoch, database start identity, monotonic fence, checkpoints a idempotentní successor reservation.

### 48.38 Terminal closure predicate

Business root nesmí přejít do úspěšného terminal stavu `COMPLETED`, `SUCCEEDED`, `ACTIVE`, `APPLIED` nebo ekvivalentu, dokud strojově vyhodnocený closure set není prázdný. Closure set obsahuje nejméně:

- pending nebo provisional child s možností dalšího dispatchu nebo authoritative write,
- active/stale lease, concurrency claim nebo unfenced worker,
- side-effect operation bez known terminal outcome nebo v dosud nerozhodnutém `MANUAL_REVIEW`,
- neodeslaný mandatory outbox/successor, inbox/sequence gap nebo neuzavřený task/MRTR/approval,
- partial activation/deployment/binding/route/pointer set,
- orphan runtime, identity, execution context, process/cgroup, socket, browser context/profile assignment, credential handle, upload/download nebo temporary artifact,
- required cleanup nebo compensation bez `COMPLETE`,
- stale checkpoint, operation context nebo pending result schopný později změnit terminal root.

Přípustné residuals jsou pouze immutable evidence, audit/log/event rows, terminal tombstones a delivery záznamy, které již nemají dispatch authority a odkazují na commitnutý canonical outcome. Explicitní `MANUAL_REVIEW` je známý recovery stav, nikoli successful closure; blokuje `COMPLETED`, `SUCCEEDED`, `ACTIVE`, release promotion a projektový closure do canonical resolution. `FAILED` nebo `CANCELLED` business outcome může být uložen před dokončením cleanupu, ale root ani projektový closure se nepovažuje za uzavřený, dokud povinný cleanup/compensation není `COMPLETE`.

### 48.39 Durable acceptance a kapacitní korektnost

`ACCEPTED` nebo HTTP `202` smí být vráceno pouze po commitu command rootu, idempotency recordu, exact request digestu a všech queue/outbox dependencies nutných k budoucímu zpracování. Pokud durable enqueue nelze commitnout kvůli queue, storage, filesystem nebo database capacity, request není přijat a nevzniká orphan command.

Již přijatá práce se při saturation nesmí zahodit, přepsat novější prací ani falešně terminalizovat. Scheduler rezervuje oddělenou minimální kapacitu pro reconciliation, cancellation, rollback, recovery a cleanup, aby běžná workload nemohla trvale vyhladovět uzavření již existujících operací. Admission používá bounded backpressure a stable capacity outcome; automatic retry nemění logical operation ani request digest.

### 48.40 Atomické publikování souborových artefaktů

Každý workspace, release, upload, download, browser artifact, export, backup a generated file, který se má stát autoritativně referencovaným, používá pořadí:

```text
persistent intent
→ unique owner-scoped temporary path
→ bounded write
→ file fsync
→ size/digest/schema/MIME/content validation
→ parent-directory fsync
→ atomic rename do immutable content-addressed final path
→ database pointer/reference commit
```

Databázový pointer nesmí ukazovat na částečný, nevalidovaný nebo přepisovatelný soubor. Crash před pointer commitem může zanechat pouze unreferenced temporary nebo immutable final garbage, které persistentní cleanup bezpečně odstraní podle owner/digest evidence. Crash po pointer commitu musí po restartu potvrdit exact digest; mismatch je blocking corruption a nikdy se neopraví tichým přegenerováním pod stejnou identitou.

## 49. Formální korektnost stavů, souběhu a obnovy

### 49.1 Autoritativní model stavu

Každý mutable doménový agregát KájovoCML NG je autoritativně popsán n-ticí:

```text
aggregateId
aggregateKind
platformIncarnationId
applicationDeploymentEpoch
state
stateVersion
commandSequence
eventSequence
currentFencingToken
cancellationVersion
activeRevisionId + activeRevisionDigest
bindingSetRevision
activationEpoch
latestCheckpointSequence
pendingSideEffectSet
terminalResultDigest
```

`applicationDeploymentEpoch`, `stateVersion`, `commandSequence`, `eventSequence`, `currentFencingToken`, `cancellationVersion`, `activationEpoch` a `latestCheckpointSequence` jsou nezáporné `bigint` hodnoty. Nikdy se nesnižují, nerecyklují ani se neodvozují z času. Identifikátor jednou použité identity, revision, release, runu, tasku, checkpointu, activation setu, lease nebo side-effect operation se nikdy znovu nepoužije pro jiný objekt.

Autoritativní změna je jediná PostgreSQL transakce, která:

1. nastaví exact transaction profile a získá potřebné transaction-scoped advisory locky,
2. zamkne platform incarnation a application deployment heads a claimne nebo načte idempotency record,
3. zamkne activation domain heads, aggregate rooty, concurrency claims a child rows v jediném pořadí 51.6,
4. po získání locků načte DB čas a znovu ověří `platformIncarnationId`, application deployment epoch, expected `stateVersion`, current state, terminalitu, cancellation intent, required active revision/binding/activation epoch, lease a fencing token,
5. validuje guard přechodu,
6. provede právě jeden povolený stavový přechod nebo jednu explicitně komutativní append operaci,
7. inkrementuje `stateVersion` právě o jedna,
8. přidělí následující aggregate-local `commandSequence` a `eventSequence`,
9. uloží doménový event, potřebný outbox/queue item, result digest nebo recovery stav a jako poslední zamkne/aktualizuje audit head,
10. commitne jako jediný linearizační bod operace.

Nalezení existujícího idempotency recordu se shodným request digestem, které pouze replayuje current nebo terminal outcome, není autoritativní změnou agregátu: neinkrementuje `stateVersion`, `commandSequence` ani `eventSequence` a nevytváří druhý terminal event. Rozdílný digest ve stejném scope končí konfliktem před doménovým přechodem.

Žádný model, SDK runner, handler, worker, browser host, reverse proxy ani externí provider není autoritou doménového stavu. Výsledek v process memory, HTTP response, SSE frame, provider response, browser DOM, filesystemu nebo systemd není autoritativní, dokud jej nepotvrdí výše uvedená transakce.

Formální bezpečnostní vlastnosti jsou:

- nejvýše jeden autoritativní terminal outcome každé logical operation,
- nejvýše jeden current writer pro každý fenced agregát nebo concurrency key,
- žádný autoritativní write po ztrátě lease, změně fence, změně platform incarnation nebo terminalizaci parentu,
- žádný side effect bez předem commitnutého intent recordu,
- žádný automatický opakovaný non-idempotentní side effect bez potvrzeného `CONFIRMED_NOT_APPLIED`,
- žádný částečně aktivní activation set,
- žádný přechod ze terminal stavu,
- žádné přijetí stale revision, bindingu, checkpointu, approval, browser frame, control epoch nebo activation epoch,
- žádný ztracený následný krok mezi doménovým commitem a enqueue,
- žádný lost wakeup závislý pouze na process notification.

Liveness platí za předpokladu dostupné PostgreSQL, dostupné deklarované dependency, férového scheduleru a konečného počtu transientních chyb. Retry je bounded; stav, který nelze bezpečně rozhodnout, končí `BLOCKED` nebo `MANUAL_REVIEW`, nikoli nekonečnou smyčkou.

### 49.2 Platform incarnation a ochrana proti ABA po restore

Systém udržuje singleton `platform_incarnation` s kryptograficky náhodným `platformIncarnationId`, pořadím `incarnationSequence`, časem vzniku a důvodem. Nová hodnota vzniká při disaster restore, obnově databáze ze snapshotu, explicitním vytvoření nové produkční instance nebo jiné operaci, která může vrátit databázové čítače do dřívějšího stavu.

Každý lease, fencing claim, runtime execution context, queue item claim, checkpoint, activation switch, browser host/session attachment a deployment run obsahuje current `platformIncarnationId`. Worker/process lease a execution context současně obsahuje current `applicationDeploymentEpoch`; změna application release tím invaliduje zápisy procesů běžících na starém release bez změny OWNER autority. Každý autoritativní write vyžaduje jeho shodu. Proces nebo host se starou incarnation nesmí obnovit lease, zapsat outcome, heartbeat, checkpoint ani terminal state, i kdyby se číselný fencing token po restore shodoval se starou hodnotou.

`platformIncarnationId` není credential ani permission konstrukce. Je to technická ochrana proti ABA a split-brain zápisu po obnově.

### 49.3 Kanonický command contract a souběžné OWNER příkazy

Každá mutující UI, API, chatová, KCIP, MCP, workerová nebo schedulerová operace vytváří serverový `domain_command` s:

- `commandId`,
- `logicalOperationId`,
- caller channel a serverovým execution contextem,
- operation key a contract revision,
- target aggregate kind a ID,
- canonical arguments a `requestDigest`,
- `idempotencyKey`,
- expected `stateVersion`, pokud operace není explicitně komutativní,
- expected revision/digest, binding-set revision a activation epoch podle kontraktu,
- deadline,
- correlation a causation,
- created, accepted a terminal timestamps.

Všechny současné webové relace, zařízení a API klienti jsou tentýž OWNER, ale jejich mutace nejsou implicitně last-write-wins. Pravidla souběhu jsou:

1. nekomutativní změna vyžaduje expected `stateVersion` nebo přesný ekvivalentní compare-and-swap guard,
2. append operace, například nová zpráva s unikátním `clientMessageId`, alokuje sequence pod root row lockem a nepotřebuje předchozí `stateVersion`,
3. idempotentní cancel, close, acknowledge nebo reconcile přijímá opakování stejného command digestu a vrací původní outcome,
4. dvě approval, activation, rotation, pointer nebo metadata operace nad stejnou expected version nemohou obě uspět,
5. vítězem je transakce, která první commitne; druhá vrátí current snapshot a `STATE_VERSION_CONFLICT`, nikoli tichý přepis,
6. bulk operace zamyká cíle v kanonickém pořadí a buď commitne celý deklarovaný atomic set, nebo žádný jeho člen,
7. operace nad nezávislými agregáty mohou běžet souběžně, pokud nesdílejí concurrency key nebo activation domain.

UI generuje stabilní idempotency key před prvním odesláním a při síťovém retry jej zachová. API klient mutující operaci bez požadovaného idempotency key nedostane k doménovému handleru. Response obsahuje `logicalOperationId`, `stateVersion`, `eventSequence`, `activationEpoch`, `resultDigest` a příznak `idempotencyReplay`.

### 49.4 Idempotency scope a exactly-once observable outcome

Idempotency scope je kanonická n-tice:

```text
operationContractId + operationContractRevision
callerAuthorityKind
stableCallerObjectId + stableCallerRevisionId
businessTargetId nebo concurrencyResourceKey
clientIdempotencyKey
```

Pro OWNER session a `KCML_OWNER_API_KEY` je `callerAuthorityKind = OWNER_FULL` a scope se neliší podle session ID, zařízení, IP ani fingerprintu klíče. Pro interní runtime je caller source component/agent/automation revision. Worker ID, execution attempt, lease token, HTTP request ID, JSON-RPC request ID, provider call ID a process restart nejsou součástí business idempotency scope.

Kanonický idempotency record používá stavy:

```text
RESERVED
EXECUTING
WAITING_FOR_INPUT
WAITING_FOR_RECONCILIATION
SUCCEEDED
FAILED_FINAL
CANCELLED_FINAL
MANUAL_REVIEW
```

Přechody:

- neexistující record → `RESERVED` pouze unique insertem,
- `RESERVED` → `EXECUTING` při fenced claimu,
- `EXECUTING` → `WAITING_FOR_INPUT` nebo `WAITING_FOR_RECONCILIATION`,
- non-terminal stav → `MANUAL_REVIEW` při neznámém outcome,
- `MANUAL_REVIEW → EXECUTING|WAITING_FOR_RECONCILIATION|SUCCEEDED|FAILED_FINAL|CANCELLED_FINAL` pouze exact OWNER resolution commandem s current evidence digestem,
- `SUCCEEDED`, `FAILED_FINAL` a `CANCELLED_FINAL` jsou immutable terminal stavy.

Stejný scope, key a request digest vrací current nebo terminal record. Stejný scope a key s jiným request digestem vždy vrací `IDEMPOTENCY_CONFLICT`. Retryable selhání jednoho attemptu neterminalizuje business idempotency record; vytvoří nový attempt pod stejným logical operation ID. Terminal failure se ukládá pouze tehdy, když kontrakt určuje, že stejný požadavek nemá být automaticky znovu proveden.

Exactly-once observable outcome znamená:

- právě jeden kanonický result/error digest,
- právě jeden terminal domain event,
- právě jeden active pointer outcome,
- opakovaný request vrací stejný outcome,
- pozdní nebo duplicitní worker nemůže výsledek změnit.

Exactly-once external execution se netvrdí, pokud jej neposkytuje target idempotency key nebo jednoznačná read-back postcondition. V takovém případě je bezpečným výsledkem `UNKNOWN` a `MANUAL_REVIEW`.

Idempotency record se nesmí odstranit ani expirovat, dokud může klient, queue, provider callback, task, MRTR exchange, browser run nebo recovery proces legitimně zopakovat danou logical operation. Po expiračním cleanupu se původní key nesmí přiřadit jiné business operaci v témže stabilním scope.

### 49.5 Události, pořadí, audit a transactional outbox

Každý aggregate root obsahuje vlastní poslední `eventSequence`. Následující sequence se alokuje změnou root řádku v téže transakci jako stav. Tím vzniká bezmezerové pořadí commitnutých doménových eventů v rámci agregátu. PostgreSQL sequence objekt se pro tento bezmezerový aggregate-local čítač nepoužívá.

Globální auditní hash chain alokuje `chainSequence` pod row lockem singletonu `audit_head`. Doménová transakce, která vyžaduje audit, současně uloží audit event, nový hash a nový audit head. Pokud audit write selže, doménová změna se necommitne.

Cross-aggregate globální business pořadí se nepředstírá. Vztah mezi různými agregáty vyjadřují `correlationId`, `causationId`, logical operation ID, activation epoch a transactional commit evidence. Časové razítko není concurrency guard.

Každý event určený pro SSE, queue, webhook delivery, cache invalidaci, runtime reconcile nebo jiného consumera má ve stejné transakci `transactional_outbox` řádek. Publisher používá at-least-once delivery. Consumer deduplikuje podle immutable event ID a pro jeden stream přijímá pouze následující expected sequence; duplicate ignoruje, gap vyvolá snapshot/replay nebo `SEQUENCE_GAP`, nikoli tiché přeskočení.

`LISTEN/NOTIFY`, condition variable, in-memory signal a socket wakeup jsou pouze optimalizace. Worker vždy polluje persistentní queue/outbox podle `availableAt`, takže ztracená notification nezpůsobí lost wakeup.

Event se publikuje až po commitu. Crash po commitu a před publikací zanechá outbox row k pozdějšímu doručení. Crash po publikaci a před označením delivery může způsobit duplicate delivery, nikoli ztrátu eventu.

PULSE, webhook, provider callback a jiný at-least-once inbound event používá `transactional_inbox`. Consumer v jedné transakci vloží unique `(consumerScope,eventId)`, ověří payload/schema/sequence digest, provede doménovou změnu a uloží outcome/event/audit/outbox. Duplicate stejného eventu vrátí původní outcome; stejné event ID s jiným digestem je protocol conflict.

PULSE stream s ordered delivery udržuje source stream ID a expected sequence. Event vyšší než expected se nedispatchne mimo pořadí; uloží se jako pending gap, vyžádá replay/snapshot nebo přejde do dead letter podle port contractu. Unordered port stále deduplikuje event ID a nesmí stejný event aplikovat dvakrát.

### 49.6 Lease, heartbeat, takeover a fencing

Každý dlouhotrvající writer používá lease tuple:

```text
platformIncarnationId
applicationDeploymentEpoch
leaseOwnerId
fencingToken
leaseAcquiredAt
leaseExpiresAt
lastHeartbeatAt
leaseState
```

Čas lease se vyhodnocuje výhradně PostgreSQL časem. Process clock, browser clock ani provider timestamp nejsou autoritou expiry.

Claim transakce:

1. zamkne queue item nebo aggregate lease row,
2. ověří current incarnation, application deployment epoch a non-terminal parent,
3. povolí claim pouze při chybějícím lease nebo `leaseExpiresAt <= databaseNow`,
4. alokuje fencing token větší než každý předchozí token daného fenced resource,
5. nastaví owner a novou expiry,
6. inkrementuje state version a uloží claim event.

Heartbeat je compare-and-swap update podmíněný současně shodou incarnation, application deployment epoch, owner, fencing tokenu, non-terminal parentu a `databaseNow < leaseExpiresAt`. Expired lease nelze heartbeat-em oživit. Jakmile takeover commitne nový token, žádný update starého ownera neuspěje.

Každý autoritativní write a každý bezprostřední pre-dispatch claim workeru guarduje současně current platform incarnation, application deployment epoch, active lease state, lease owner, fencing token, `databaseNow < leaseExpiresAt`, non-terminal parent a expected state/checkpoint version. Nulový počet aktualizovaných řádků znamená ztrátu authority; worker okamžitě zastaví další doménové writes a external dispatch, zahodí neautoritativní in-memory výsledek a pouze uloží lokální diagnostiku mimo autoritativní stream.

Fencing token chrání databázový a brokerovaný write, nikoli již odeslaný paket do externího systému. Proto každý external side effect používá intent, target idempotency key a reconciliation podle 49.8.

Explicitní release lease uspěje pouze current ownerovi s current tokenem. Release neinkrementuje token; následující claim vždy alokuje vyšší token. Graceful shutdown může lease uvolnit až po potvrzeném checkpointu nebo přechodu do reconciliation stavu. Pokud lease kalendářně expiruje během krátké DB transakce poté, co current owner zamkl resource row před expiry, transaction commit se linearizuje před pozdějším takeoverem; external dispatch však vyžaduje novou fresh pre-dispatch transakci a minimální zbývající authority window podle 51.15.

### 49.7 Concurrency keys, lock ordering a deadlock freedom

Concurrency claim má stabilní namespace, canonical key digest, owner logical operation, lease/fencing a deadline. Claim se získává ve stejné transakci jako přechod operace do executable stavu. Scope `NONE` znamená pouze absenci business serializace; state-version, idempotency a fencing guards zůstávají povinné.

Úplné a jediné pořadí transaction-scoped advisory locků, platform heads, idempotency rows, activation domain heads, aggregate roots, concurrency claims, sequence allocator rows, mutable child/work rows a globálního `audit_head` je normativně uvedeno v 51.6–51.8. `audit_head` se zamyká vždy poslední. Bulk targety se před transakcí seřadí podle stabilního kind ordinalu a surových UUID bytes; transakce, která pozdě zjistí dříve řazený root nebo key, rollbackne a začne s úplným setem.

Transakce nesmí držet PostgreSQL lock během OpenAI, HTTP, browser, filesystem, systemd, DNS, TLS nebo jiné externí operace. Před external call commitne intent a uvolní DB locks; po návratu provede novou guarded transakci.

Timeout při získávání locku nemění stav objektu a používá retry podle queue policy. Deadlock je retryovatelný pouze před external dispatch; po možném side effectu následuje reconciliation.

Fair scheduling zaručuje, že čekající nižší priorita není neomezeně vyhladověna. Priority může měnit pořadí claimu, nikoli obejít concurrency key nebo fence.

### 49.8 External side-effect intent, outcome a reconciliation

Každý side effect mimo právě probíhající PostgreSQL transakci má `side_effect_operation` se stavy:

```text
INTENT_RECORDED
DISPATCHING
OUTCOME_RECORDED
RECONCILING
CONFIRMED_APPLIED
CONFIRMED_NOT_APPLIED
FAILED_FINAL
UNKNOWN
```

Povolené hrany jsou:

```text
INTENT_RECORDED → DISPATCHING
DISPATCHING → OUTCOME_RECORDED | RECONCILING
OUTCOME_RECORDED → CONFIRMED_APPLIED | CONFIRMED_NOT_APPLIED | FAILED_FINAL | RECONCILING
RECONCILING → CONFIRMED_APPLIED | CONFIRMED_NOT_APPLIED | FAILED_FINAL | UNKNOWN
CONFIRMED_NOT_APPLIED → INTENT_RECORDED pouze v transakci, která při povoleném retry vytvoří právě jeden vyšší immutable attempt, jeho state projection, current-attempt pointer a authority outbox při current parent fence, nevyčerpaném budgetu a nezměněném request digestu
UNKNOWN → RECONCILING | CONFIRMED_APPLIED | CONFIRMED_NOT_APPLIED | FAILED_FINAL pouze exact resolution commandem
```

`CONFIRMED_APPLIED` a `FAILED_FINAL` jsou terminal. `CONFIRMED_NOT_APPLIED` je známý bezpečný outcome; je terminal, pokud retry policy další attempt nepovoluje, jinak může v jedné T1 transakci vytvořit právě jeden vyšší attempt a přejít do `INTENT_RECORDED`; teprve samostatný fresh pre-dispatch commit jej posune do `DISPATCHING`. `UNKNOWN` je suspended resolution state a nikdy sám nepovolí nový dispatch.

Record obsahuje logical operation ID, exact step key, target binding/revision, canonical request a digest, side-effect/retry class, target idempotency key, precondition digest, expected postcondition, reconciliation contract, compensation contract, current attempt sequence, parent fence/incarnation/deployment epoch, timestamps a evidence. Každý `side_effect_attempt` je immutable a unique podle operation + attempt sequence. Jeho měnitelný lifecycle je výhradně v jednořádkové `side_effect_attempt_state` projekci; operation a current-attempt projection se mění v jedné transakci a deferred consistency trigger zakáže jejich drift.

Povinný protokol:

1. v jedné transakci se uloží operation `INTENT_RECORDED`, immutable attempt, attempt-state `INTENT_RECORDED`, pre-side-effect checkpoint, idempotency relation a právě jeden authority outbox pro dispatch,
2. dispatch worker guarded transakcí změní operation i current attempt-state na `DISPATCHING`; tato změna znamená, že od tohoto bodu nelze absenci outcome interpretovat jako neodeslaný request,
3. bezprostředně před network/browser/system dispatch se znovu ověří active lease, fence, parent state, cancellation version a deadline; neúspěšný guard nic neodešle,
4. external request nese immutable target idempotency key, pokud jej target podporuje,
5. response, provider handle nebo transport error se persistuje jako append-only immutable attempt evidence a operation i attempt-state přejdou na `OUTCOME_RECORDED`, pokud je evidence syntakticky úplná,
6. jednoznačná klasifikace přejde na `CONFIRMED_APPLIED`, `CONFIRMED_NOT_APPLIED` nebo `FAILED_FINAL` a ve stejné transakci vytvoří post-side-effect checkpoint a continuation outbox,
7. timeout, disconnect, process death, lease expiry nebo nejednoznačný transport po `DISPATCHING` přejde na `RECONCILING`,
8. reconciliation provede deklarovaný read-back podle exact target bindingu a rozhodne applied/not applied/failed/unknown,
9. `UNKNOWN` zastaví automatické dispatchování a vytvoří `MANUAL_REVIEW` parentu. Není důkazem applied ani not applied; z tohoto stavu lze odejít pouze exact reconciliation nebo OWNER outcome rozhodnutím.

Absence response, absence lokálního outcome řádku, zavřený SSE stream, ztracené TCP spojení ani expired lease nikdy samy neznamenají `CONFIRMED_NOT_APPLIED`.

Compensation je samostatná child `side_effect_operation` s vlastním intentem, attempts, idempotency a reconciliation a s vazbou `compensatesOperationId`. Původní operation zůstává `CONFIRMED_APPLIED`; saga step může přejít do `COMPENSATING` a `COMPENSATED` až podle terminal outcome child compensation. Compensation není databázovým rollbackem původního external effectu a původní evidence zůstává immutable.

OWNER manual outcome rozhodnutí je povoleno pouze nad `UNKNOWN`, obsahuje exact side-effect operation, current state version a evidence digest, rozhodnutí `CONFIRMED_APPLIED`, `CONFIRMED_NOT_APPLIED` nebo `FAILED_FINAL`, důvod a audit a provede guarded transition. Dvě rozdílná rozhodnutí nad stejnou expected state version nemohou obě commitnout.

### 49.9 Checkpoint contract

Checkpoint je immutable snapshot autoritativního stavu, nikoli serializace process memory. Obsahuje:

- parent aggregate a exact state version,
- platform incarnation,
- lease fencing token, pod kterým vznikl,
- monotonic checkpoint sequence,
- state-machine state a substate,
- approved revision/specification/plan/agent graph digests,
- active binding-set revision a activation epoch,
- completed item/step/event watermarks,
- pending side-effect operation IDs a jejich stavy,
- provider handles a event cursors,
- workspace/session/browser state references a digests,
- budget, cancellation version a deadline,
- canonical payload a checkpoint digest.

Pre-side-effect checkpoint, intent record a dispatch outbox vznikají v jedné transakci. Post-side-effect checkpoint, confirmed outcome a pokračovací outbox vznikají v jedné transakci. Checkpoint pointer se nikdy neposune na checkpoint vytvořený stale fencing tokenem.

Resume:

1. claimne parent novým nebo current fenced lease,
2. načte current parent row a latest checkpoint pointer,
3. ověří checkpoint digest, monotonic sequence, incarnation, parent/revision/binding/activation lineage a terminalitu,
4. načte skutečný DB, provider, runtime, filesystem a external stav podle pending operation setu,
5. reconciliuje každý neuzavřený side effect,
6. pokračuje od prvního nepotvrzeného deterministic kroku.

Checkpoint s neplatným digestem, jinou authority lineage, změněným contract digestem, nekompatibilní schema version nebo activation epoch se nepřehraje. Parent přejde fail-closed do přesného blockeru nebo `MANUAL_REVIEW`; nevytvoří nový side effect.

### 49.10 Cancellation, interruption a terminal race

Cancellation je monotonic intent s `cancellationVersion`, requestedAt, reason, requester channel a correlation. Zrušení již terminal objektu vrací jeho terminal outcome jako idempotentní replay.

Cancel a success/failure race se linearizuje root row lockem:

- pokud terminal transition commitne dříve, pozdější cancel objekt nezmění,
- pokud cancel intent commitne dříve, pozdější success transition, která vyžaduje nepřítomnost cancellation intentu, se odmítne,
- worker po zjištění cancel intentu nesmí plánovat nový model, tool, browser, external nebo child call,
- rozpracovaný read-only call se může abortovat,
- rozpracovaný mutující call se nejprve reconciliuje,
- parent přejde do `CANCELLED` pouze pokud všechny pending side effects mají známý outcome a cleanup dosáhl požadovaného safe pointu,
- neznámý side effect vede do `MANUAL_REVIEW` nebo generation `BLOCKED` s classification `MANUAL_REVIEW`.

Cancellation nevymazává potvrzený side effect ani automaticky nespouští compensation. Compensation proběhne pouze podle schváleného saga/rollback kontraktu.

Interrupt discussion/model streamu používá stejný princip. Partial text je evidence, nikoli completed assistant outcome. Provider completion se starým turn fence po commitnutém interruptu nesmí terminalizovat turn ani vytvořit successor.

Child cancellation propaguje root causation. Parent může pokračovat po samostatném child cancel pouze tehdy, když exact binding deklaruje typed fallback; jinak přejde do svého cancel/failure/review toku.

### 49.11 Terminalita, retry a nové attempts

Explicitní terminal stavy jobs, runs, phase attempts, model calls, tool calls, tasks, browser actions, automation runs, deployment runs, cleanup operations a activation sets jsou immutable. `MANUAL_REVIEW`, `BLOCKED`, wait, pause, recovery a reconciliation stavy nejsou terminal, pokud jejich konkrétní mapa výslovně umožňuje resolution hranu. Oprava nebo retry nevzkřísí terminal řádek; resolution manual-review stavu používá current state version a evidence digest.

Retry vytváří nový attempt s:

- stejným logical operation ID,
- stejným business idempotency recordem,
- vyšším attempt sequence,
- novým lease/fencing tokenem,
- explicitní vazbou na předchozí attempt a jeho classification.

Nový attempt je přípustný pouze při retryable classification a při známém side-effect outcome. `UNKNOWN`, stale contract, stale approval, stale checkpoint, exhausted budget, cancelled parent a non-recoverable validation nevytváří automatický attempt.

Backoff a jitter ovlivňují `availableAt`, nikoli deadline, idempotency scope nebo pořadí business příkazů.

### 49.12 Component lifecycle, activation a pozorované stavy

Component lifecycle povoluje pouze následující hrany:

| From | To | Povinný guard |
|---|---|---|
| `DRAFT` | `REVIEW` | revision a manifest jsou schema-validní |
| `DRAFT` | `RETIRED` | objekt nebyl aktivován a abandon command je current |
| `REVIEW` | `DRAFT` | vzniká nová immutable revision; stará review evidence zůstává |
| `REVIEW` | `APPROVED` | blocking review gates mají PASS |
| `REVIEW` | `RETIRED` | abandon command je current |
| `APPROVED` | `ACTIVE` | activation set dosáhl `ACTIVE` a exact pointery odpovídají |
| `APPROVED` | `RETIRED` | neexistuje active release ani in-flight activation |
| `ACTIVE` | `SUSPENDED` | suspend command commitl a gateway zastavila nové mutující dispatches |
| `ACTIVE` | `QUARANTINED` | quarantine evidence je current a nové calls jsou blokované |
| `ACTIVE` | `RETIRED` | retirement saga potvrdila disable, route a dependency impact |
| `SUSPENDED` | `ACTIVE` | readiness, bindings a current active set jsou znovu potvrzené |
| `SUSPENDED` | `QUARANTINED` | quarantine guard |
| `SUSPENDED` | `RETIRED` | retirement guard |
| `QUARANTINED` | `SUSPENDED` | příčina byla odstraněna, ale enable nebyl potvrzen |
| `QUARANTINED` | `ACTIVE` | úplná recertification a activation/readiness PASS |
| `QUARANTINED` | `RETIRED` | retirement guard |
| `RETIRED` | `DEREGISTERED` | neexistuje active pointer, route, binding, lease, pending side effect ani dependent active binding |

`DEREGISTERED` je terminal. Přímé hrany neuvedené v tabulce jsou zakázané.

Activation state povoluje:

| From | To | Význam |
|---|---|---|
| `INACTIVE` | `READY` | statické a dependency readiness podmínky jsou splněné |
| `READY` | `READY_FOR_ACTIVATION` | blocking gates a activation membership jsou complete |
| `READY_FOR_ACTIVATION` | `ENABLE_REQUESTED` | enable command a activation barrier jsou commitnuté |
| `ENABLE_REQUESTED` | `ACTIVE` | effective runtime/route epoch a heartbeat jsou potvrzené |
| `ENABLE_REQUESTED` | `BLOCKED` | enable outcome je známě neúspěšný nebo vyžaduje vstup |
| `ACTIVE` | `DISABLE_REQUESTED` | disable intent je commitnutý a nové calls jsou zastavené |
| `DISABLE_REQUESTED` | `INACTIVE` | runtime, route a heartbeat potvrdily disabled |
| `DISABLE_REQUESTED` | `DISABLE_UNCONFIRMED` | dispatch proběhl, ale outcome je nejednoznačný |
| `DISABLE_UNCONFIRMED` | `INACTIVE` | reconciliation potvrdila disabled |
| `DISABLE_UNCONFIRMED` | `ACTIVE` | reconciliation potvrdila, že disable nebyl aplikován |
| `DISABLE_UNCONFIRMED` | `BLOCKED` | outcome zůstal `UNKNOWN` |
| `READY` nebo `READY_FOR_ACTIVATION` | `BLOCKED` | readiness dependency se stala neplatnou |
| `BLOCKED` | `INACTIVE`, `READY` nebo `READY_FOR_ACTIVATION` | nový stav je znovu vypočten z current evidence; nepřeskakuje se přímo do `ACTIVE` |

Cross-state invarianty:

- lifecycle `ACTIVE` vyžaduje activation `ACTIVE`, non-null active revision/release, current binding-set revision a active activation epoch,
- `RETIRED` a `DEREGISTERED` nesmí mít activation `ACTIVE`, enabled route ani nový execution context,
- lifecycle `SUSPENDED` nebo `QUARANTINED` blokuje nové business calls bez ohledu na poslední heartbeat,
- `enabled` není samostatná autorita; je odvozený projection activation state a effective runtime evidence,
- operational, monitoring a recertification state jsou pozorované/odvozené stavy s vlastní observation sequence, observed release ID a activation epoch,
- heartbeat nebo probe ze staršího activation epoch, release, binding setu nebo sequence se uloží jen jako stale evidence a nesmí změnit current operational state,
- dvě stejné observation sequence s rozdílným digestem jsou protocol conflict.

### 49.13 MCP transport request, call, MRTR, state handle, subscription a task

#### 49.13.1 Transport request a JSON-RPC ID

Každý parsed MCP request má persistovaný `mcp_request_event`. Přijetí je lineární v okamžiku, kdy gateway po transport/header/version/capability validaci rezervuje active JSON-RPC ID v exact source/access scope. Druhý request se stejným ID v tomto scope před finální response se odmítne bez handleru. Rezervace se uvolní až po persistovaném final response outcome nebo definitivním transport rejection.

Transport request ID není business operation identity. Znovupoužití po final response vytváří nový request event. Business dedupe probíhá pouze přes `mcp_idempotency_record` a logical operation.

Pre-handler failure nesmí vytvořit `mcp_call_run`, concurrency claim, side-effect intent ani handler invocation. Pokud parser nezná ID spolehlivě, error response ID se vynechá.

#### 49.13.2 MCP call state machine

Kanonický `mcp_call_run` používá stavy:

```text
RECEIVED
CLAIMED
EXECUTING
WAITING_FOR_INPUT
WAITING_FOR_TASK
RECONCILING
CANCEL_REQUESTED
SUCCEEDED
FAILED
CANCELLED
MANUAL_REVIEW
```

Povolené přechody:

- `RECEIVED → CLAIMED` fenced queue/inline claimem po exact idempotency a concurrency admission,
- `CLAIMED → EXECUTING` po current binding, revision/release, schema, deadline a cancellation snapshot preflightu,
- `EXECUTING → WAITING_FOR_INPUT` pouze s commitnutým MRTR exchange a checkpointem před dalším side effectem,
- `EXECUTING → WAITING_FOR_TASK` pouze s commitnutým durable taskem před vydáním CreateTaskResult,
- `EXECUTING → RECONCILING` při nejednoznačném external/non-atomic effectu,
- `WAITING_FOR_INPUT → CLAIMED` po atomickém consume accepted input setu a enqueue resume,
- `WAITING_FOR_TASK → CLAIMED` pouze pokud parent workflow po task terminal/input eventu skutečně potřebuje fenced continuation,
- non-terminal → `CANCEL_REQUESTED` durable cancel intentem,
- `EXECUTING|RECONCILING|CANCEL_REQUESTED → SUCCEEDED|FAILED|CANCELLED|MANUAL_REVIEW` podle persistované evidence,
- `MANUAL_REVIEW → RECONCILING|CLAIMED|FAILED|CANCELLED` pouze exact resolution commandem s current evidence digestem.

`SUCCEEDED`, `FAILED` a `CANCELLED` jsou immutable terminal. `MANUAL_REVIEW` je suspended decision state. `CANCELLED` vyžaduje known outcomes a complete required cleanup. Nejasný possible effect je `MANUAL_REVIEW`, nikoli `CANCELLED`.

Finální MCP response se materializuje pouze z persistovaného current/terminal call recordu. Handler completion v process memory není success. Response stream disconnect state nemění; pouze nastaví cancellation intent podle 10.3. Terminal DB commit před ztracenou delivery je nadále canonical a idempotentní retry jej načte.

#### 49.13.3 MRTR input exchange

`mcp_input_exchange` povoluje:

```text
PENDING → PARTIALLY_FULFILLED → FULFILLED → CONSUMED
PENDING → FULFILLED → CONSUMED
PENDING|PARTIALLY_FULFILLED → EXPIRED
PENDING|PARTIALLY_FULFILLED|FULFILLED → INVALIDATED
```

MRTR exchange vzniká pouze pro `tools/call`, `resources/read` nebo `prompts/get`, obsahuje alespoň `inputRequests` nebo `requestState` a musí vzniknout před non-idempotent side effectem continuation.

Respond transakce pod exchange/call root lockem:

1. ověří platform incarnation, current call/exchange state/version, source/access context, method, original salient argument digest, server/tool revision, binding-set revision, activation epoch, requestState integrity a expiry,
2. rozdělí response keys na current known, unknown, already satisfied a invalid,
3. unknown/already satisfied keys uloží jako ignored evidence a neaplikuje,
4. validní known subset uloží s per-key digestem a označí satisfied,
5. pokud required keys zbývají, atomicky přejde do `PARTIALLY_FULFILLED` a nevytvoří resume,
6. pokud je set úplný, přejde do `FULFILLED`, poté v téže transakci consume exchange, změní parent call na resumable state a rezervuje právě jeden successor/enqueue.

Exact replay consumed exchange se stejným aggregate accepted-response digestem vrací current/terminal parent outcome. Změněný recognized response, changed original args nebo wrong context vrací conflict bez side effectu. Expiry versus respond rozhodne první root-row commit.

#### 49.13.4 State handle

State handle používá `OPEN`, `CLOSED`, `EXPIRED` a monotonic `stateVersion`. Každé resolve/use ověří access context, owner tool/revision, contract/binding digest, activation epoch, namespace a expiry. Handle je ordinary tool value a nenahrazuje authorization.

`close` je idempotentní CAS. `OPEN → CLOSED` nebo `OPEN → EXPIRED` je terminal pro handle. Closed/expired handle se nereaktivuje; public ID, lookup digest ani generation nonce se nikdy nerecyklují. Concurrent use versus close rozhodne root-row lock a expected handle stateVersion; handler se po prohraném close race nespustí.

#### 49.13.5 Subscription

Subscription povoluje:

```text
OPENING → ACTIVE
OPENING → GRACEFUL_CLOSING → CLOSED
ACTIVE → CANCEL_REQUESTED → CLOSED
ACTIVE → GRACEFUL_CLOSING → CLOSED
OPENING|ACTIVE|GRACEFUL_CLOSING → FAILED
```

`OPENING → ACTIVE` vyžaduje persistovaný acknowledgment jako první protocol message a exact acknowledged filter subset. Prázdný subset vede `OPENING → GRACEFUL_CLOSING → CLOSED` po acknowledgmentu.

Client HTTP disconnect zapisuje cancel/closed evidence a server již neposílá response. Server graceful closure nejprve persistuje final response, emituje ji a poté zavře stream. Abrupt transport loss bez final response je `FAILED`/disconnect evidence pro stream, nikoli změna authoritative resource/tool/task stavu.

Notification sequence je evidence-only. Notification mimo acknowledged filter, bez exact subscription ID nebo po final response je protocol violation. Reconnect nikdy replayuje MCP subscription events; založí nový request a authoritative refresh.

#### 49.13.6 Task state machine

MCP task povoluje:

```text
WORKING → INPUT_REQUIRED
INPUT_REQUIRED → WORKING
WORKING → COMPLETED | FAILED | CANCELLED
INPUT_REQUIRED → COMPLETED | FAILED | CANCELLED
```

`COMPLETED`, `FAILED`, `CANCELLED` jsou immutable terminal. `COMPLETED` obsahuje final result původního requestu, včetně tool resultu `isError=true`. `FAILED` obsahuje JSON-RPC execution error. `CANCELLED` neobsahuje success result.

Create task transakce commitne task, original request lineage, idempotency relation, task-local sequence a případný initial queue item před vydáním CreateTaskResult. Vydaný task ID musí být okamžitě resolvable.

`tasks/get` pouze čte linearizovanou projection pod current access/binding checks. Neprodlužuje TTL ani lease.

`tasks/update` transakce:

1. ověří current Tasks capability, task/access/binding/revision/activation, nonterminal state a TTL,
2. načte current outstanding input keys,
3. unknown/already satisfied/superseded keys uloží jako ignored evidence,
4. accepted known subset persistuje a consume pod unique key constraintem,
5. rezervuje nejvýše jeden resume successor, pokud jsou splněny všechny required inputs,
6. vrátí ACK nezávisle na tom, zda worker již projection změnil z `INPUT_REQUIRED` na `WORKING`.

Client nedodává authoritative task stateVersion; CAS je serverový interní mechanismus. Exact accepted response replay je no-op. Lifetime request key se nerecykluje.

`tasks/cancel` pouze persistuje idempotent cancellation intent a ACK. Cancel intent není terminal a nesmí blokovat již dříve commitnutý validní completion. Po cancel intentu se nezahajuje nový child side effect; already dispatched effect se reconciliuje. První validní terminal root-row commit mezi `COMPLETED`, `FAILED` a bezpečně dosaženým `CANCELLED` vyhrává. Proto může task po cancel ACK skončit `COMPLETED` nebo `FAILED`.

Expiry claim používá DB time a root-row lock. Nonterminal task po expiry nejprve získá expiry/cancel intent, zastaví nový dispatch a reconciliuje pending effects. Poté přejde do `FAILED` s `MCP_TASK_EXPIRED`, do `CANCELLED`, nebo při unknown outcome do `FAILED` s `MCP_OUTCOME_UNKNOWN` a manual-review relation. Terminal task se zachová jako tombstone do retention purge; po purge `tasks/get` vrací not found.

Task events používají gap-free task-local sequence. `notifications/tasks` je pouze delivery projection úplného current task state; ztracená notification nemění task a client ji obnoví `tasks/get`.

### 49.14 Agent run, model call, tool call a handoff

Agent run povoluje tyto hrany:

| From | To |
|---|---|
| `QUEUED` | `PREPARING`, `CANCEL_REQUESTED` |
| `PREPARING` | `RUNNING`, `WAITING_FOR_OWNER`, `FAILED`, `CANCEL_REQUESTED` |
| `RUNNING` | `WAITING_FOR_MODEL`, `WAITING_FOR_TOOL`, `WAITING_FOR_MCP_INPUT`, `WAITING_FOR_MCP_TASK`, `WAITING_FOR_AGENT`, `WAITING_FOR_OWNER`, `CHALLENGE_REQUIRED`, `PAUSED`, `SUCCEEDED`, `FAILED`, `CANCEL_REQUESTED`, `MANUAL_REVIEW` |
| `WAITING_FOR_MODEL` | `RUNNING`, `FAILED`, `CANCEL_REQUESTED`, `MANUAL_REVIEW` |
| `WAITING_FOR_TOOL` | `RUNNING`, `WAITING_FOR_MCP_INPUT`, `WAITING_FOR_MCP_TASK`, `FAILED`, `CANCEL_REQUESTED`, `MANUAL_REVIEW` |
| `WAITING_FOR_MCP_INPUT` | `RUNNING`, `FAILED`, `CANCEL_REQUESTED` |
| `WAITING_FOR_MCP_TASK` | `RUNNING`, `FAILED`, `CANCEL_REQUESTED`, `MANUAL_REVIEW` |
| `WAITING_FOR_AGENT` | `RUNNING`, `FAILED`, `CANCEL_REQUESTED`, `MANUAL_REVIEW` |
| `WAITING_FOR_OWNER` | `RUNNING`, `FAILED`, `CANCEL_REQUESTED` |
| `CHALLENGE_REQUIRED` | `RUNNING`, `FAILED`, `CANCEL_REQUESTED` |
| `PAUSED` | `RUNNING`, `CANCEL_REQUESTED`, `FAILED` |
| `CANCEL_REQUESTED` | `CANCELLED`, `MANUAL_REVIEW` |
| `MANUAL_REVIEW` | `RUNNING`, `FAILED`, `CANCELLED` |

`SUCCEEDED`, `FAILED` a `CANCELLED` jsou terminal. `MANUAL_REVIEW` je suspended decision state a resume vyžaduje exact side-effect resolution, current evidence digest a nový fenced checkpoint. Hrana `RUNNING → SUCCEEDED` vyžaduje validní final output, PASS output guardrails, potvrzený memory/session commit, prázdný pending call set, známé outcomes všech side effects a current fence.

Každý Responses API create je samostatný `ai_model_call` attempt. Tool loop vytváří successor calls; již dokončený provider response se po tool outputu znovu neotevírá. Model-call state map:

```text
QUEUED → SUBMITTING | CANCEL_REQUESTED
SUBMITTING → IN_PROGRESS | STREAMING | COMPLETED | INCOMPLETE | REFUSED | FAILED | CANCEL_REQUESTED
IN_PROGRESS ↔ STREAMING
IN_PROGRESS | STREAMING → WAITING_FOR_TOOL_OUTPUT | COMPLETED | INCOMPLETE | REFUSED | FAILED | EXPIRED | CANCEL_REQUESTED
WAITING_FOR_TOOL_OUTPUT → COMPLETED | FAILED | CANCEL_REQUESTED
CANCEL_REQUESTED → CANCELLED | FAILED
```

Provider wire status se ukládá odděleně od local semantic state. `COMPLETED`, `INCOMPLETE`, `REFUSED`, `CANCELLED`, `FAILED` a `EXPIRED` jsou terminal pro konkrétní attempt. `COMPLETED` rozlišuje `FINAL_OUTPUT`, `TOOL_CALLS`, `HOSTED_TOOL_RESULT` a `NO_OUTPUT`.

Cancel versus provider completion rozhoduje první guarded model-call row commit. Foreground transport abort sám nedokazuje provider cancellation; background cancel se opakuje pouze nad stejným `response.id` a terminal stav se potvrzuje retrieve. Pozdní provider evidence nemůže obejít current parent/model fence.

Local call, immutable descriptor, idempotency claim a submit intent vzniknou před requestem. Worker před network sendem commitne `SUBMITTING/DISPATCH_STARTED`; oficiální SDK create retry je vypnutý. Pokud po tomto bodu není uložen `response.id`, timeout, connection error ani provider request ID neprokazují `NOT_SUBMITTED`. Call dostane `MODEL_SUBMIT_OUTCOME_UNKNOWN`, parent se zablokuje a druhý create nevznikne automaticky. Známý response ID se vždy retrieveuje nebo resumeuje.

Terminal provider snapshot a kompletní ordered output items se persistují před tool reservation nebo final validation. Neznámý output item se nezahodí; blokuje interpretation. Function call se páruje podle `call_id`, nikoli item ID. Tool outcomes, post-tool checkpoint a právě jedna successor reservation vznikají atomicky. Více provider responses může existovat jen jako evidence explicitní nové logical operation; právě jeden output je canonical pro daný attempt.

Tool call se persistuje před dispatch, má provider call ID unique v model callu, canonical argument digest, idempotency relation a side-effect operation. Tool output a agent checkpoint vzniknou v jedné transakci před následujícím modelovým kolem. Model nikdy neobdrží nepotvrzený tool success.

Handoff run používá exact source/target revisions a parent checkpoint. Child terminal output se atomicky připojí k parent runu jen při current parent fence, expected pending handoff ID a známém cancellation stavu. Pozdní child output zůstane evidence a parent nezmění.

Session append používá row lock na `agent_session`, expected session state version a následující item sequence. Agent run `SUCCEEDED` write, připojení jeho potvrzených canonical session items a canonical structured-memory writes proběhnou v jedné PostgreSQL transakci; nikdy nevznikne stav, kdy run hlásí `SUCCEEDED`, ale canonical session nebo povinná memory neobsahuje jeho final items. Následná indexace, vector projection nebo external memory sink používá transactional outbox a nemění canonical success. Persistent memory má unique source-run/namespace/key dedupe.

### 49.15 Generation job, phase run, source a discussion turn

Generation job povoluje:

```text
DISCUSSING → ANALYZING → IMPLEMENTING → INTEGRATING → VALIDATING → CML_CONFORMANCE → ACTIVATING → COMPLETED
```

Z kteréhokoli non-terminal happy-path stavu lze přejít do `BLOCKED`, `FAILED` nebo po dokončení cancellation/cleanup do `CANCELLED`. `BLOCKED` obsahuje exact `resumeState`, `resumePhase`, checkpoint ID, context digest a blocker. `BLOCKED → resumeState` je povoleno pouze po atomickém uzavření blockeru a revalidaci specification, authority, dependency, credential, binding, workspace, candidate a activation snapshotů. Změněný funkční kontrakt se neobnovuje; vrací job do `DISCUSSING` s novou specification revision.

`COMPLETED`, `FAILED` a `CANCELLED` jsou terminal. `MANUAL_REVIEW` generation toku se reprezentuje job state `BLOCKED` s classification `MANUAL_REVIEW`; job nepokračuje, dokud exact side-effect outcome není uzavřen.

Phase run povoluje:

```text
QUEUED → RUNNING
RUNNING → WAITING_FOR_DEPENDENCY | WAITING_FOR_OWNER | REPAIRING | SUCCEEDED | FAILED | CANCEL_REQUESTED
WAITING_FOR_DEPENDENCY | WAITING_FOR_OWNER → RUNNING | FAILED | CANCEL_REQUESTED
REPAIRING → RUNNING | SUCCEEDED | FAILED | CANCEL_REQUESTED
CANCEL_REQUESTED → CANCELLED | FAILED
```

`SUCCEEDED`, `FAILED` a `CANCELLED` jsou terminal pro konkrétní attempt. Nový attempt má vyšší attempt sequence a vznikne až po terminalizaci předchozího. Partial unique constraint blokuje více active phase runs a více active coordinator leases na job.

Phase success transakce současně:

1. ověří phase fence, current job state/phase, cancellation version, authority, spec/plan/dependency digests a complete output checkpoint,
2. terminalizuje phase run jako `SUCCEEDED`,
3. uloží phase result digest a terminal evidence,
4. změní job na právě následující povolený state,
5. nastaví new current phase pointer,
6. vytvoří next phase run a queue/outbox item,
7. uloží job event a audit.

Crash před commitem nezmění nic; crash po commitu ponechá next queue item v outboxu. Neexistuje stav „phase succeeded, ale další phase nebyla naplánována“.

Source state map:

```text
RECEIVED → PARSING → PARSED → VERIFIED
RECEIVED | PARSING | PARSED → REJECTED
RECEIVED | PARSING | PARSED | VERIFIED → SUPERSEDED
```

`REJECTED` a `SUPERSEDED` jsou terminal. Parser write vyžaduje source fence/state version. Normalized content a digest jsou immutable. Pozdní parser nesmí přepsat verified nebo superseded source.

Discussion turn map:

```text
QUEUED → RUNNING
RUNNING → COMPLETED | FAILED | INTERRUPT_REQUESTED
INTERRUPT_REQUESTED → INTERRUPTED | FAILED
```

Provider completion versus steer race zamkne turn a job. Pokud completion commitne první, turn přejde přímo `RUNNING → COMPLETED` a nová OWNER message vytvoří běžného unique successora. Pokud interrupt commitne první, turn přejde `RUNNING → INTERRUPT_REQUESTED`; pozdní provider completion je pouze evidence a nesmí turn dokončit. Po zrušení streamu a reconciliation read-only nebo mutujících calls přejde turn do `INTERRUPTED`, případně `FAILED` při technicky neuzavíratelné interruption chybě. V téže transakci vznikne nejvýše jeden successor relation a nejvýše jeden queue item pro successor. Partial assistant content je immutable interrupted evidence.

### 49.16 Generation plan node, workspace a integration saga

Generation plan node používá:

```text
PENDING → READY | CANCELLED
READY → RUNNING | CANCELLED
RUNNING → WAITING_FOR_DEPENDENCY | RECONCILING | SUCCEEDED | FAILED | CANCELLED | MANUAL_REVIEW
WAITING_FOR_DEPENDENCY → READY | FAILED | CANCELLED
RECONCILING → RUNNING | SUCCEEDED | FAILED | CANCELLED | MANUAL_REVIEW
MANUAL_REVIEW → RECONCILING | RUNNING | FAILED | CANCELLED
```

`SUCCEEDED`, `FAILED` a `CANCELLED` jsou terminal pro konkrétní node attempt. Node se stane `READY` pouze když všechny incoming execution edges mají confirmed outputs s expected digests. Node result je immutable. Scheduler alokuje ready nodes pod plan row lockem; stejné node ID a attempt nelze spustit dvakrát jako dvě business operace.

Workspace authority tvoří immutable database/object-storage content blobs a `generation_workspace_revision`, nikoli momentální materialized directory. Patch apply:

1. vytvoří content-addressed blobs do temporary/unreferenced prostoru,
2. zamkne job workspace root,
3. ověří base revision/digest a expected digest každé dotčené cesty,
4. vloží complete file mapping nové immutable revision,
5. atomicky přepne current workspace pointer, uloží patch result, event a outbox pro materialization,
6. po commitu materializuje filesystem z exact revision do temporary directory a přepne jej atomic rename.

Crash před DB commitem zanechá nejvýše unreferenced blobs, které cleanup odstraní. Crash po DB commitu a před filesystem rename obnoví materializer z current revision. Materialized directory nikdy není zdrojem rozhodnutí o current revision bez shody manifest digestu.

Integration saga step používá stavy:

```text
PENDING → INTENT_RECORDED | CANCELLED
INTENT_RECORDED → EXECUTING | CANCELLED
EXECUTING → RECONCILING | SUCCEEDED | FAILED
RECONCILING → SUCCEEDED | FAILED | MANUAL_REVIEW
SUCCEEDED → COMPENSATING pouze current rollback/cleanup commandem
COMPENSATING → COMPENSATED | FAILED | MANUAL_REVIEW
MANUAL_REVIEW → RECONCILING | COMPENSATING | FAILED | CANCELLED
```

`COMPENSATED`, `FAILED` a `CANCELLED` jsou terminal. `SUCCEEDED` je stable confirmed forward outcome a může přejít pouze do `COMPENSATING`, pokud rollback/cleanup contract vyžaduje child compensation operation. Každý step má unique `(jobId, activationSetId, stableStepKey)`, deterministic resource names, exact before/after digests, side-effect operation a compensation relation. Resume provede read-back všech kroků od posledního checkpointu a pokračuje od prvního neconfirmed step. Existence resource stejného jména není sama success; musí odpovídat expected candidate/authority/manifest digestu.

První CREATE alokuje provisional identity jednou a další attempts ji buď bezpečně znovu použijí v témže jobu, nebo ji po complete cleanup označí retired; nikdy nealokují paralelní druhou identitu pro stejný target graph node. UPDATE a REPAIR používají existující stabilní identity a vytvářejí pouze nové immutable revisions/releases.

Cleanup je vlastní checkpointovaná saga. Dokud cleanup není `COMPLETE`, nevznikne pro stejný job/target nový remediation candidate ani nová activation set membership.

### 49.17 Activation set, barrier, pointer switch a rollback

`generation_activation_set` používá stavy:

```text
DRAFT
READY
SWITCHING
VERIFYING
ACTIVE
ROLLING_BACK
ROLLBACK_VERIFYING
ROLLED_BACK
FAILED
MANUAL_REVIEW
```

`MANUAL_REVIEW → VERIFYING|ROLLING_BACK|FAILED` je povoleno pouze po exact resolution current effective-state evidence. `DRAFT → READY` vyžaduje immutable membership, previous/candidate pointer snapshots, complete rollback plan, fresh gates a candidate preflight. `READY → SWITCHING` vzniká fenced claimem activation controlleru. `SWITCHING → VERIFYING` linearizuje atomic pointer switch. `VERIFYING → ACTIVE` vyžaduje effective epoch acknowledgment a postflight PASS. `VERIFYING|ACTIVE → ROLLING_BACK` vzniká rollback intentem pouze pokud `activation_head` a všechny current pointers stále odpovídají tomuto setu a jeho candidate snapshotu. `ROLLING_BACK → ROLLBACK_VERIFYING` linearizuje atomic reverse switch. `ROLLBACK_VERIFYING → ROLLED_BACK` vyžaduje effective previous/absent epoch a rollback probes. `ROLLED_BACK` a `FAILED` jsou terminal. `ACTIVE` je stable successful state, ale není terminal po dobu, kdy je tento set current a lze jej explicitně rollbackovat; jakmile current head ukazuje jiný set, snapshot guard všechny další přechody historického `ACTIVE` setu odmítne. Nejednoznačný effective stav končí `MANUAL_REVIEW` a gateway blokuje dotčené mutující calls.

Activation set má vlastní lease/fencing token, state version, activation domain set, `previousSnapshotDigest`, `candidateSnapshotDigest`, `switchEpoch`, `rollbackEpoch`, `barrierId` a cleanup state. Pro překrývající se activation domain může existovat nejvýše jeden set ve stavu `SWITCHING`, `VERIFYING`, `ACTIVE` s neuzavřeným postflightem, `ROLLING_BACK` nebo `ROLLBACK_VERIFYING`.

Před switchi controller vytvoří activation barrier:

1. gateway atomicky zastaví admission nových mutujících calls v dotčených activation domains,
2. již přijaté mutující calls jsou pinned k previous epoch a doběhnou do známého terminal/checkpoint/reconciliation stavu,
3. read-only calls mohou dokončit proti epoch, ve které byly přijaty,
4. switch nezačne, dokud není prázdný set neuzavřených mutujících operations nebo dokud jejich outcomes nejsou známé,
5. timeout barrieru activation zablokuje; nepřepne pointer přes neznámou mutaci.

Switch transakce získá locks v kanonickém pořadí a ověří:

- set je current `SWITCHING` a controller fence je current,
- cancellation intent není commitnutý,
- každý current pointer, revision, release, binding-set revision, route a runtime target přesně odpovídá previous snapshotu včetně state version a activation epoch,
- každý candidate member a digest odpovídá frozen membership,
- readiness/gate evidence neexpirovala,
- candidate runtime je staged a nepřijímá neautorizovaný běžný provoz,
- rollback target je stále použitelný nebo je pro první CREATE explicitní `ABSENT`.

V jedné transakci:

1. singleton `activation_head` alokuje nový globálně monotonic activation epoch,
2. všechny active revision/release/binding/route/runtime pointers dotčeného setu se přepnou na candidate snapshot s tímto epoch,
3. set přejde `SWITCHING → VERIFYING`,
4. activation member rows se označí switched,
5. eventy, audit a runtime-reconcile outbox se uloží.

Gateway při admission každého requestu ověří current activation epoch dotčeného domainu. Cache smí být použita pouze po ověření, že její head epoch odpovídá databázovému `activation_head`; stale cache nesmí dispatchnout request. Jeden request je pinned k jedinému epoch a binding-set revision. Žádný request nemůže složit targety z více epoch.

Runtime, reverse proxy a service reconciliation jsou idempotentní podle activation set ID a epoch. Každý runtime/route ack obsahuje effective epoch a exact target digest. Ack staršího epoch je stale evidence. Set se nestane `ACTIVE`, dokud všechny required effective a smoke gates nepotvrdí candidate epoch.

Rollback používá stejný barrier a novou reverse-switch transakci. Ověří, že current pointers stále odpovídají candidate snapshotu a switch epoch. V jedné transakci alokuje vyšší rollback epoch a obnoví celý previous snapshot. Pro první CREATE je previous snapshot `ABSENT`: active pointers se nastaví na null/inactive, route se deaktivuje, candidate bindings přestanou být active a component lifecycle nesmí zůstat `ACTIVE`.

Pokud reverse switch commitl, pozdní candidate worker s candidate activation epoch nesmí zapisovat active state, heartbeat, binding, route, task ani result. Cleanup candidate runtime probíhá až po atomic reverse switchi; release se označí `ROLLED_BACK` po potvrzení, že není current. Selhání cleanupu ponechá cleanup state pending a alarm, ale nesmí znovu zpřístupnit candidate.

Double activation je blokována expected previous snapshotem, activation-domain unique constraintem a set fence. Opakovaný activate command stejného setu vrací current set outcome. Jiný set se stale previous snapshotem vrací `ACTIVATION_EPOCH_STALE`.

### 49.18 První CREATE, UPDATE a REPAIR rollback

První CREATE má tři odlišné případy:

1. **Selhání před switch commitem:** active pointer nikdy nevznikl. Cleanup zastaví provisional runtime, deaktivuje candidate-only route, retired candidate bindings, odstraní neautoritativní `current` filesystem link, označí release failed/rolled back podle evidence a zachová provisional identity pro audit nebo ji po dependency checku deregistruje.
2. **Selhání po switch commitu před `ACTIVE`:** rollback transakce přepne celý activation domain na `ABSENT` s vyšším epoch. Teprve potom se runtime zastaví a cleanupne.
3. **Selhání po `ACTIVE`:** běžný rollback opět použije previous `ABSENT`; OWNER-visible schopnost se atomicky stane neaktivní, nikoli částečně přístupná.

V žádném případě se release neoznačí `ROLLED_BACK` před potvrzením, že není current v žádném active pointeru, route, binding setu ani runtime effective epoch. Nový remediation candidate nevznikne, dokud předchozí cleanup nemá `COMPLETE` nebo explicitní `MANUAL_REVIEW` blocker.

UPDATE a REPAIR rollback vždy odkazuje na frozen previous snapshot. Atomic reverse switch obnoví previous revisions, releases, binding-set revision, routes, runtime target, component lifecycle/activation projection, agent/automation pointers a activation epoch. Restart previous runtime není samostatný zdroj pravdy; jeho effective ack a readiness se ověřují proti rollback epoch. Pokud previous snapshot již není compatible nebo dostupný, activation se před původním switchi zablokuje. Po switchi nejednoznačný rollback effective stav vede do `MANUAL_REVIEW` a fail-closed gateway.

### 49.19 Browser session, page/frame/document, control lease a action state machine

Browser session lifecycle povoluje:

```text
CREATING → READY | RECOVERING | CLOSING | FAILED | EXPIRED
READY → ACTIVE | PAUSED | RECOVERING | CLOSING | FAILED | EXPIRED
ACTIVE → CHALLENGE_REQUIRED | PAUSED | RECOVERING | CLOSING | FAILED | EXPIRED
CHALLENGE_REQUIRED → ACTIVE | PAUSED | RECOVERING | CLOSING | FAILED | EXPIRED
PAUSED → ACTIVE | RECOVERING | CLOSING | FAILED | EXPIRED
RECOVERING → READY | ACTIVE | CHALLENGE_REQUIRED | CLOSING | FAILED | EXPIRED
CLOSING → CLOSED | FAILED
```

`CLOSED`, `FAILED` a `EXPIRED` jsou terminal lifecycle stavy. Recovery terminal failure vytváří novou explicitní recovery/session operation, nikoli změnu terminal session zpět na active. Cleanup může pokračovat po terminal lifecycle, ale nové browser actions, preview tickets, context attach a control leases jsou zakázané.

Session current browser authority je exact snapshot:

```text
platform incarnation
application deployment epoch
runtime build manifest
host slot/host generation | bridge/connection epoch
context ID/generation
page ID/generation
frame ID/attachment epoch
document ID/epoch
navigation sequence
control epoch/fence
operation-scope digest
observation/frame/viewport revisions
account binding/auth epoch
```

Snapshot je serverový stav. Browser/bridge handle, URL, title, focus, preview frame ani modelový popis jej nemůže přepsat.

Page/frame/document observation se přijme pouze od current host/bridge/context generation. Page ID, frame ID a document epoch se nerecyklují v session. Frame detach/reattach, OOPIF replacement, full navigation, BFCache restore, prerender activation nebo recovery vytvoří nový attachment/document epoch podle adapter evidence. Stale event se uloží jako evidence, ale current projection nezmění.

Browser control lease používá pravidla 49.6 a unique active lease per session. `controlEpoch` se inkrementuje při každém udělení jinému holderovi, opětovném udělení po expiry, takeoveru, return-to-AI/runtime a recovery attachmentu. Holder identity, session context generation, epoch a fence se mění v jedné transakci. Stará epoch/fence se nikdy znovu nepoužije.

Control transfer používá:

```text
REQUESTED → DRAINING | REJECTED | CANCELLED
DRAINING → RESETTING_INPUT | GRANTED | REJECTED | CANCELLED | MANUAL_REVIEW
RESETTING_INPUT → GRANTED | MANUAL_REVIEW
MANUAL_REVIEW → DRAINING | RESETTING_INPUT | REJECTED | CANCELLED
```

`GRANTED`, `REJECTED` a `CANCELLED` jsou terminal. `DRAINING` blokuje nové actions dosavadního i budoucího holdera, dokončí nebo reconciliuje current mutating action a uloží safe checkpoint. `RESETTING_INPUT` uzavře pressed keys/buttons, touch IDs, pointer capture, drag a composition state bez vzniku nové nekontrolované mutace. Unknown current effect nebo neuzavřený input sequence nepovolí grant a vede do `MANUAL_REVIEW`.

Browser action run používá:

```text
QUEUED → VALIDATING | CANCEL_REQUESTED
VALIDATING → WAITING_FOR_PAGE | CLAIMED | FAILED | CANCEL_REQUESTED
WAITING_FOR_PAGE → VALIDATING | FAILED | CANCEL_REQUESTED
CLAIMED → INTENT_RECORDED | FAILED | CANCEL_REQUESTED
INTENT_RECORDED → DISPATCHING | CANCEL_REQUESTED | FAILED
DISPATCHING → VERIFYING | RECONCILING | FAILED | CANCEL_REQUESTED
VERIFYING → SUCCEEDED | RECONCILING | FAILED | CANCEL_REQUESTED
RECONCILING → SUCCEEDED | FAILED | CANCELLED | MANUAL_REVIEW
CANCEL_REQUESTED → CANCELLED | RECONCILING | MANUAL_REVIEW
MANUAL_REVIEW → RECONCILING | SUCCEEDED | FAILED | CANCELLED
```

`SUCCEEDED`, `FAILED` a `CANCELLED` jsou terminal. `INTENT_RECORDED` znamená, že action logical operation, exact authority snapshot, target/action schema, earliest mutation trigger, concurrency claims, precondition, postcondition/read-back a side-effect operation jsou durable. Browser/bridge dispatch před tímto stavem je zakázaný.

Přechod `VALIDATING → CLAIMED` v jedné transakci ověří:

- session lifecycle a nonterminal cleanup/admission,
- current runtime build/host-or-bridge/context identity,
- current control lease/epoch/fence a holder,
- page/page generation, frame attachment a document epoch,
- observation/frame/viewport freshness,
- operation scope, account/tenant/auth epoch a binding revision,
- target uniqueness a action-specific actionability,
- confirmation/challenge/dialog/permission policy,
- business idempotency a account/resource/external-session concurrency,
- cancellation/deadline a required pre-armed waiter plan.

`CLAIMED → INTENT_RECORDED` persistuje immutable pre-checkpoint, side-effect intent a dispatch outbox. Earliest mutation trigger může být pointer-down/click/keystroke/text/input/change/blur/submit/upload/drop/dialog/permission/WebAuthn/device confirmation. Intent musí předcházet nejčasnějšímu triggeru; nelze jej odložit až před očekávaný network request.

Host/bridge dispatch phases jsou monotonic evidence:

```text
COMMAND_ACCEPTED
TARGET_RESOLVED
ACTIONABILITY_PASSED
INPUT_SEQUENCE_STARTED
MUTATION_TRIGGER_POSSIBLY_ISSUED
NAVIGATION_OR_REQUEST_OBSERVED
METHOD_RETURNED
POST_OBSERVATION_CAPTURED
```

Phase se zapisuje pouze pod current action fence. Exact duplicate stejného digestu je no-op; stejná sequence/phase s jiným digestem je protocol conflict. Pozdější absence ACK nesnižuje nejvyšší durable/evidenced phase.

Event waiter pro popup/page/navigation/dialog/filechooser/download se pre-armuje před triggerem. Pokud adapter nemůže prokázat pre-arm nebo event mohl být ztracen, absence eventu není `CONFIRMED_NOT_APPLIED`.

Přímý `VERIFYING → SUCCEEDED` je povolen pouze po validní revision-defined postcondition a terminal DB commitu. Postcondition musí ověřit intended account/resource/business outcome; Playwright method success, URL, screenshot, load/networkidle, popup event nebo download start samy nestačí.

Retry po změně DOM, navigation, overlay, popup nebo crash je přípustný pouze tehdy, když evidence prokazuje, že mutation trigger nebyl vydán, nebo target-side idempotency zaručuje stejné observable outcome. Po `MUTATION_TRIGGER_POSSIBLY_ISSUED` následuje independent reconciliation.

Reconciliation vrací `CONFIRMED_APPLIED`, `CONFIRMED_NOT_APPLIED` nebo `UNKNOWN`. `UNKNOWN` vede do `MANUAL_REVIEW` a blokuje nový mutující holder/action na stejném account/resource/external-session concurrency key. Exact OWNER resolution může action vrátit do `RECONCILING`, potvrdit `SUCCEEDED`, nebo ji uzavřít `FAILED`/`CANCELLED`; nemůže přepsat immutable terminal outcome.

Worker/host/bridge attachment obsahuje celou authority n-tici. Připojení k podobnému context/page/profile handle bez shody n-tice je zakázané. Browser host/bridge přijme action pouze od current worker/connection/action fence a odpověď obsahuje stejný fence; pozdní response se neautorituje.

Preview frame, screenshot, bounding box, network event a visual sequence jsou evidence. Preview disconnect nemění control lease ani action state. Reconnect vytvoří nový stream epoch, fresh snapshot a acknowledgment barrier; neacknowledged input se nereplayuje.

### 49.20 Browser state bundle, authentication challenge a cleanup

Browser state bundle je immutable version. Capture operation:

1. získá account/session mutation barrier a zastaví nové mutující actions pro account/external-session key,
2. ověří, že není pending `UNKNOWN`, unfinished dialog/filechooser/download nebo unresolved auth registration,
3. provede exact account/tenant authenticated postcondition a uloží account auth epoch,
4. stabilizuje page/document inventory a určí quiescent capture point,
5. vytvoří origin/member inventory, encrypted content-addressed blob a digest,
6. ověří runtime-build/serializer, credential/certificate/virtual-authenticator versions a origin compatibility,
7. v jedné transakci vloží bundle version/member rows, evidence a podle expected account-binding state/auth version přepne active pointer,
8. uvolní barrier.

Crash před pointer transakcí zanechá pouze unreferenced encrypted blob/member candidates k cleanupu. Crash po transakci vrací current bundle přes idempotency replay. Dvě captures nad stejnou expected account/auth state version nemohou obě aktivovat svůj bundle.

Standardní bundle member kinds jsou cookies včetně partition metadata, localStorage, explicitní IndexedDB, explicitní sessionStorage adapter, permissions, client-certificate metadata a virtual WebAuthn test credential. Cache, service-worker runtime, raw browser profile, OS keychain, hardware private key a platform passkey nejsou standardní members.

Restore operation:

1. vytvoří fresh compatible context nebo získá exact bridge profile lease,
2. ověří account binding/auth epoch a credential/certificate versions,
3. instaluje state members v deklarovaném pořadí před první app navigation,
4. vloží sessionStorage pouze pre-bootstrap adapterem,
5. otevře safe start URL a provede fresh page/frame/document observation,
6. ověří exact account/tenant authenticated condition,
7. teprve potom povolí business mutaci.

Partial restore nebo neplatná account condition neznamená přihlášený stav; vede do deterministic relogin/challenge nebo failure.

Auth attempt používá typed flow state a může čekat na challenge. Challenge row používá:

```text
PENDING → RESOLVED | REJECTED | EXPIRED | CANCELLED
```

Resolution vyžaduje exact challenge state version, pending action digest, session/page/frame/document/origin/RP/account, control/auth epoch a unexpired deadline. Uložení resolution, consume challenge a enqueue resume je atomické. Duplicate stejné response digest vrací původní outcome; changed response je conflict.

Push/external-device challenge může být dokončena po timeoutu/disconnectu. Expiry klientského waitu sama neznamená provider rejection; runtime nejprve read-backem zjistí challenge/account state. TOTP/email/SMS auto-submit má action intent před prvním vloženým znakem podle flow contractu.

WebAuthn assertion a registration jsou rozdílné actions. Registration má external account side-effect operation a postcondition. Virtual authenticator je exact context-scoped test/automation account capability; platform passkey a non-exportable client certificate používá bridge/challenge.

Dialog a permission rows používají podobný pending/terminal contract a jsou vázány na exact page/frame/document/action. Step, který může vyvolat dialog/permission, musí mít listener pre-armed. Mutující workflow nepoužívá implicitní default accept/dismiss.

Cleanup je samostatná idempotentní state machine:

```text
PENDING → REVOKING_ADMISSION
REVOKING_ADMISSION → RECONCILING | FAILED
RECONCILING → CAPTURING_FINAL_EVIDENCE | MANUAL_REVIEW | FAILED
CAPTURING_FINAL_EVIDENCE → RELEASING_BROWSER_RESOURCES | FAILED
RELEASING_BROWSER_RESOURCES → RELEASING_PLATFORM_RESOURCES | MANUAL_REVIEW | FAILED
RELEASING_PLATFORM_RESOURCES → COMPLETE | FAILED
MANUAL_REVIEW → RECONCILING | RELEASING_BROWSER_RESOURCES | FAILED
```

`COMPLETE` a `FAILED` jsou terminal; `FAILED` je closure blocker a zachovává přesný seznam neuvolněných resources. Každý krok má immutable attempt, checkpoint a idempotentní retry.

Cleanup resources zahrnují control/preview tickets, bridge assignment/profile lease, pending actions/effects, pages/context, dialogs/permissions/challenges, uploads/downloads/temp files, routes/listeners/bindings/init scripts, permissions, virtual authenticators, client-certificate handles, state-bundle candidates, account/resource/capacity claims a ephemeral credentials.

Terminal session se nepovažuje za plně uklizenou, dokud cleanup není `COMPLETE`; od první terminal lifecycle transakce však nepřijímá nové actions, tickets, assignments ani leases. Unknown side effect drží cleanup v `RECONCILING` nebo `MANUAL_REVIEW`; temporary resource lease nesmí umožnit další business mutaci.

Boot recovery porovná DB inventory s host contexts, bridge assignments/profile locks a temp storage. Runtime resource bez current DB ownera se cleanupne. DB session bez live resource přejde do recovery/reconciliation. Raw handle se nepřipojí podle URL/profile name nebo process PID bez exact generation identity.

### 49.21 Browser automation run, schedule a step

Browser automation run povoluje:

| From | To |
|---|---|
| `QUEUED` | `PREPARING`, `CANCEL_REQUESTED` |
| `PREPARING` | `RUNNING`, `CHALLENGE_REQUIRED`, `FAILED`, `CANCEL_REQUESTED` |
| `RUNNING` | `WAITING_FOR_PAGE`, `CHALLENGE_REQUIRED`, `PAUSED`, `RECOVERING`, `SUCCEEDED`, `FAILED`, `CANCEL_REQUESTED`, `MANUAL_REVIEW` |
| `WAITING_FOR_PAGE` | `RUNNING`, `PAUSED`, `RECOVERING`, `FAILED`, `CANCEL_REQUESTED` |
| `CHALLENGE_REQUIRED` | `RUNNING`, `PAUSED`, `FAILED`, `CANCEL_REQUESTED` |
| `PAUSED` | `RUNNING`, `RECOVERING`, `FAILED`, `CANCEL_REQUESTED` |
| `RECOVERING` | `RUNNING`, `CHALLENGE_REQUIRED`, `PAUSED`, `FAILED`, `CANCEL_REQUESTED`, `MANUAL_REVIEW` |
| `CANCEL_REQUESTED` | `CANCELLED`, `MANUAL_REVIEW` |
| `MANUAL_REVIEW` | `RUNNING`, `FAILED`, `CANCELLED` |

`SUCCEEDED`, `FAILED` a `CANCELLED` jsou terminal. `MANUAL_REVIEW` je suspended decision state a resume vyžaduje exact side-effect resolution, current evidence digest, compatible revision/runtime build/account auth epoch a nový fenced checkpoint.

Run preparation atomicky rezervuje exact revision, runtime build compatibility, account binding/auth epoch, schedule fire, operation scope, account/resource concurrency a browser session create command. Disabled/stale definition, incompatible bridge/build nebo account claim konflikt selže před vytvořením browser side effectu.

OWNER takeover provede `RUNNING → PAUSED` až po safe checkpointu. Starý automation control epoch poté nemůže dispatchnout další step. Return-to-runtime vytvoří new control epoch, fresh account/page/frame/document observation a revaliduje current step.

Automation step používá:

```text
PENDING → VALIDATING | CANCEL_REQUESTED
VALIDATING → WAITING_FOR_PAGE | CHALLENGE_REQUIRED | CLAIMED | FAILED | CANCEL_REQUESTED
WAITING_FOR_PAGE → VALIDATING | FAILED | CANCEL_REQUESTED
CHALLENGE_REQUIRED → VALIDATING | FAILED | CANCEL_REQUESTED
CLAIMED → INTENT_RECORDED | FAILED | CANCEL_REQUESTED
INTENT_RECORDED → DISPATCHING | FAILED | CANCEL_REQUESTED
DISPATCHING → VERIFYING | RECONCILING | FAILED | CANCEL_REQUESTED
VERIFYING → SUCCEEDED | RECONCILING | FAILED | CANCEL_REQUESTED
RECONCILING → SUCCEEDED | FAILED | CANCELLED | MANUAL_REVIEW
CANCEL_REQUESTED → CANCELLED | RECONCILING | MANUAL_REVIEW
MANUAL_REVIEW → RECONCILING | SUCCEEDED | FAILED | CANCELLED
```

`SUCCEEDED`, `FAILED` a `CANCELLED` jsou terminal. Step `SUCCEEDED` vyžaduje postcondition/read-back digest. Step attempt je immutable a další attempt vzniká pouze při evidence před mutation triggerem, `CONFIRMED_NOT_APPLIED` nebo target-side idempotency.

Run current step pointer, step terminal outcome, typed output projection, successor step reservation a optional state-bundle capture request se mění atomicky. Neexistuje stav, ve kterém je step terminal success, ale run ukazuje předchozí step nebo chybí successor reservation.

Checkpoint obsahuje exact revision/runtime build, browser authority snapshot, account/tenant/auth epoch, page/frame/document identities, current dialogs/challenges/downloads, locator/action/trigger/postcondition digests, side-effect ledger, branch/loop counters a cleanup relation. Resume nepoužívá process memory, open Page/Frame/ElementHandle nebo network idle jako autoritu.

`clientRunId` scope je definition + caller + clientRunId. Stejný input/revision/account digest vrací existing run; jiný digest je conflict. Schedule fire používá stable fire key. Overlap policy:

- `SKIP` vytvoří terminal skipped evidence bez browser session,
- `QUEUE` vytvoří nový bounded run čekající na claim,
- `COALESCE` se připojí pouze k identical revision/input/account/resource scope,
- `REJECT` vrátí conflict bez runu.

Timezone/DST/misfire policy je immutable schedule contract. Catch-up nemůže vytvořit více stejných non-idempotentních business mutations než dovoluje declared fire/idempotency scope.

Disabled definition odmítne nový run před session create. Existující run se řídí frozen revision a explicitní disable/cancel policy. Credential/account invalidation může run pozastavit nebo vyžádat reauthentication; nesmí tichým způsobem pokračovat pod jiným accountem.

### 49.22 Runtime persistent state a component control

`context.state` poskytuje versioned operations:

- `get(key)` vrací value, schema version, value digest a `stateVersion`,
- `put(key, value, expectedStateVersion)` je compare-and-swap,
- `create(key, value)` uspěje pouze při neexistenci,
- `delete(key, expectedStateVersion)` vytváří tombstone s vyšší version,
- `transaction(namespace, expectedNamespaceVersion, operations[])` provede celý bounded set atomicky,
- `append(stream, clientEventId, payload)` deduplikuje client event a alokuje stream sequence pod lockem.

Handler nemůže obejít state broker přímým DB write. State schema migration je immutable, checkpointovaná a aktivuje se se stejným activation setem jako runtime revision. Rollback runtime smí číst current state pouze pokud previous revision deklaruje forward-compatible schema; jinak activation preflight zablokuje switch.

Component enable/disable/control command obsahuje expected component state version, expected release, binding-set revision a activation epoch. Command a control outbox vzniknou atomicky. ACK je observation svázaná s command ID a target epoch; stale ACK nemění desired/effective state. Timeout po dispatchi vede do reconcile přes state/heartbeat, nikoli do slepého druhého control commandu.

Heartbeat nonce zabraňuje pouze duplicate evidence; pořadí určuje serverový receive sequence a reported runtime epoch. Starší heartbeat nemůže přepsat novější operational state. Absence heartbeat může odvodit degraded/unhealthy, ale nemůže sama potvrdit úspěšný disable, activation nebo rollback.

### 49.22.1 Secret version, binding a credential rotation

Secret version je immutable. `secret_record` obsahuje `stateVersion`, active version pointer a monotonic `secretActivationEpoch`. Create version pouze vloží immutable candidate; activation/rotation pod secret row lockem ověří expected state/version, přesný candidate version a všechny systémové invarianty a v jedné transakci:

1. přepne active version pointer,
2. inkrementuje secret state version a activation epoch,
3. retire označí previous version podle policy,
4. pro `KCML_OWNER_API_KEY` současně atomicky nahradí verifier hash, fingerprint a credential version,
5. vytvoří binding/session-state invalidation events, audit a outbox.

Ztracená rotation response nevytvoří druhý klíč: opakovaný idempotency key vrátí current rotation outcome a platná OWNER session může revealnout jedinou active hodnotu. Ověření API klíče a přijetí `domain_command` má explicitní linearizační bod: command přijatý a persistovaný před rotation commitem může doběhnout pod svým immutable OWNER_FULL execution contextem; autentizace starou hodnotou zahájená nebo opakovaná po rotation commitu selže. Rotace nemění OWNER autoritu ani již commitnutý command, pouze zabrání vytvoření nového contextu ze starého verifieru.

Secret Broker resolve vytvoří immutable `secret_resolution` evidence s exact secret/version, secret activation epoch, source/target revisions, binding ID/revision/digest, logical operation, purpose a expiry. Hodnota je pinovaná pro právě jednu dispatch operation. New resolve po rotaci vždy používá nový epoch. Stale binding, retired version, změněný source/target revision nebo activation set je odmítnut před předáním hodnoty.

Rotace, která vyžaduje okamžitou invalidaci dependent browser/session/auth state, vytvoří invalidation epoch a barrier pro dotčené account/resource concurrency keys. New operations jsou blokované do invalidace. Již dispatchnuté external operations se dokončí nebo reconciliují podle své pinované version evidence; rotace jejich outcome nepřepisuje.

Secret binding candidate se stane active pouze atomicky s activation setem. Direct bind/unbind/activate operation používá state-version CAS a exact impact snapshot; nesmí vytvořit okamžik, kdy active revision odkazuje na neactive nebo jinak verzovaný binding. Tato korektnost nemění OWNER_FULL viditelnost ani závazný secret trust model.

### 49.23 Konfigurace, circuit breaker, monitoring a alerty

Operational setting má oddělené `desiredVersion` a per-service `effectiveVersion`. `PUT/reset/import` mění desired value pouze přes CAS a uloží config activation request. Každá desired version má právě jeden canonical apply logical operation; pozdější desired version vytváří nový apply run. Apply run používá:

```text
PENDING → VALIDATING
VALIDATING → APPLYING | FAILED
APPLYING → VERIFYING | ROLLING_BACK | FAILED | MANUAL_REVIEW
VERIFYING → APPLIED | ROLLING_BACK | FAILED | MANUAL_REVIEW
ROLLING_BACK → ROLLED_BACK | FAILED | MANUAL_REVIEW
MANUAL_REVIEW → VERIFYING | ROLLING_BACK | FAILED
```

`APPLIED`, `ROLLED_BACK` a `FAILED` jsou terminal pro danou desired version. Nová změna settingu nevzkřísí terminal run; vytvoří nový run s vyšší desired version. Manual review vyžaduje exact effective-state resolution.

Crash po změně souboru nebo restartu a před DB outcome se obnoví read-backem skutečného service config digestu, process release a readiness. `APPLIED` je přípustné pouze pokud všechny target services hlásí expected effective version. Mixed effective versions jsou explicitní `VERIFYING`/`ROLLING_BACK` stav, nikoli success. Atomic config group používá activation barrier a rollback snapshot.

Circuit breaker update používá state version a monotonic transition sequence. `CLOSED → OPEN → HALF_OPEN → CLOSED|OPEN`; právě jeden half-open probe drží fenced probe lease. Pozdní probe result nemění breaker po novém epochu nebo OWNER resetu.

Monitoring scheduler enqueue používá stable schedule occurrence ID. Opakovaný tick nevytvoří duplicate probe. Alert dedupe scope je source object + alert type + condition digest + active episode. Open/update/close používá state version; delivery je at-least-once s channel idempotency key. Stale probe může zůstat evidence, ale neotevře ani nezavře current alert episode.

Repair job vzniká unique podle source alert episode a repair policy revision. Opakované monitoring ticks vrací existující non-terminal repair job.

### 49.24 Application deployment a disaster recovery

Deployment run používá fenced state machine:

```text
QUEUED → PREPARING
PREPARING → BACKED_UP | FAILED
BACKED_UP → MIGRATING | ROLLING_BACK | FAILED
MIGRATING → STAGING | ROLLING_BACK | FAILED | MANUAL_REVIEW
STAGING → SWITCHING | ROLLING_BACK | FAILED
SWITCHING → RESTARTING | ROLLING_BACK | MANUAL_REVIEW
RESTARTING → VERIFYING | ROLLING_BACK | MANUAL_REVIEW
VERIFYING → ACTIVE | ROLLING_BACK | FAILED | MANUAL_REVIEW
ROLLING_BACK → ROLLED_BACK | FAILED | MANUAL_REVIEW
MANUAL_REVIEW → VERIFYING | ROLLING_BACK | FAILED
```

`ACTIVE`, `ROLLED_BACK` a `FAILED` jsou terminal pro konkrétní deployment run. Pozdější OWNER rollback již aktivního application release je nový deployment run se samostatným logical operation ID, previous/candidate snapshotem a vyšším application deployment epoch; terminal `ACTIVE` run se znovu neotevírá. Manual review vyžaduje exact filesystem/DB/process evidence resolution.

Každý step má intent, expected before/after digest, outcome a checkpoint. Migrations jsou forward-only a musí být kompatibilní s previous i candidate application release po celé rollback window. Externí call ani systemd restart neprobíhá uvnitř DB transakce.

Application release switch používá atomic filesystem rename pro `current` a databázový `applicationDeploymentEpoch`. Protože tyto dva zdroje nelze commitnout jednou transakcí, deployment recovery vždy čte oba, release manifest digest a skutečné process heartbeats. Stav se rozhodne deterministicky:

- oba ukazují previous → switch nebyl applied,
- oba ukazují candidate a required processes potvrzují epoch → applied,
- symlink a DB epoch se liší → recovery dokončí nebo vrátí switch podle persisted deployment intentu; readiness zůstává false,
- process release/epoch mix → gateway blokuje mutující provoz a restart/reconcile pokračuje,
- outcome, který nelze ověřit, vede do `MANUAL_REVIEW`.

Při změně singleton `application_deployment_head.current_epoch` přestanou staré process leases, execution contexts a queue claims přijímat autoritativní writes, protože každý guarded write ověřuje current application deployment epoch. Jejich pending side effects zůstávají v DB a nové procesy je reconciliují.

Disaster restore vytvoří nový platform incarnation před spuštěním workerů. Restore poté ověří audit head, active activation epochs, queue/outbox, leases, pending side effects, browser host/session stav, current application release a backups. Všechny leases z předchozí incarnation jsou neplatné bez čekání na jejich původní expiry. Externí pending outcomes se reconciliují; nepovažují se automaticky za not applied.

### 49.25 Crash-point rozhodovací matice

Pro každou autoritativní operaci platí následující rozhodnutí při smrti procesu přesně mezi kroky:

| Poslední potvrzený bod | Stav po restartu | Povinná akce |
|---|---|---|
| před vytvořením command/idempotency recordu | operace neexistuje | klient může request znovu odeslat se stejným key |
| po `RESERVED`, před queue/outbox commitem | command a enqueue jsou v jedné transakci, takže buď existují oba, nebo žádný | claim existing item nebo replay create |
| po queue claimu, před prvním work checkpointem | lease může expirovat; žádný side effect nemá intent | takeover novým fence a deterministic restart kroku |
| po pre-checkpointu a `INTENT_RECORDED`, před `DISPATCHING` | request nebyl předán dispatch workeru | dispatch existing intent právě jednou |
| po `DISPATCHING`, před network sendem | lokálně nerozlišitelné od smrti po sendu | reconciliation; nikdy automaticky `NOT_APPLIED` jen podle absence response |
| po external sendu, před response | outcome neznámý | target idempotency lookup/read-back; `UNKNOWN` → manual review |
| po response v process memory, před outcome commitem | response není autoritativní | retrieve/read-back/reconcile podle targetu |
| po confirmed outcome commitu, před post-checkpointem | zakázaná konstrukce; outcome a checkpoint pointer se commitují spolu | žádný mezistav nesmí existovat |
| po post-checkpointu, před enqueue pokračování | zakázaná konstrukce; checkpoint a outbox se commitují spolu | outbox publisher pokračování doručí |
| po phase terminal resultu, před změnou jobu/next phase | zakázaná konstrukce; jedna transakce | recovery vidí buď starý phase state, nebo complete transition |
| OpenAI 1 — před submit | descriptor/call může být `QUEUED`; `DISPATCH_STARTED` neexistuje | claimnout stejný call a provést právě jeden submit, nebo cancel bez effectu |
| OpenAI 2 — během submit | `DISPATCH_STARTED`; response ID může chybět | bez jednoznačného `NOT_SUBMITTED` nastavit `MODEL_SUBMIT_OUTCOME_UNKNOWN`; s handlem retrieve; nikdy blind create |
| OpenAI 3 — po submitu před persistencí response ID | provider mohl response vytvořit, local handle chybí | provider request ID uložit jen diagnosticky; bez `response.id` manual review; s durable raw handlem dokončit persistence |
| OpenAI 4 — po persistenci response ID | exact `response.id` a request digest | retrieve/poll/resume tentýž response a deduplikovat events/output |
| OpenAI 5 — během streamu | response ID, last contiguous provider sequence a partial events | background resume od cursoru, jinak retrieve stored response; delta ani EOF nejsou terminal success |
| OpenAI 6 — po model outputu před tool execution | terminal provider snapshot a ordered output items | znovu vyhodnotit items a vytvořit unique tool reservations; model znovu nevolat |
| OpenAI 7 — během tool execution | tool intent/attempt a pre-checkpoint | read-only podle contractu retry; mutující effect read-back/reconcile; stale fence nesmí write |
| OpenAI 8 — po tool side effectu před provider continuation | potvrzený tool outcome nebo reconciliation evidence | atomicky vytvořit output se stejným `call_id`, checkpoint a jedinou successor reservation; tool neopakovat |
| OpenAI 9 — před terminal persistence | final candidate/output existuje, parent terminal commit chybí | pod current fence znovu validovat a atomicky commitnout terminal run, session, memory, event, audit a outbox; model znovu nevolat |
| po workspace blob write, před workspace revision commitem | unreferenced blob | garbage cleanup; current workspace beze změny |
| po workspace revision commitu, před materialization | DB revision je current | znovu materializovat exact revision |
| po integration resource vytvoření, před step outcome | resource může existovat | read-back exact digest a candidate ownership; reconcile |
| po activation barrieru, před pointer switch | previous set zůstává current, admission je dočasně blokovaná | release barrier nebo zopakovat preflight |
| uprostřed pointer-switch transakce | PostgreSQL atomicita | celý previous nebo celý candidate snapshot, nikdy část |
| po pointer switchi, před runtime effective ack | set je `VERIFYING` | reconcile candidate epoch; při fail reverse switch |
| po reverse-switch transakci, před candidate shutdown | previous/absent snapshot je current | candidate je fenced; dokončit cleanup |
| po terminal state commitu, před SSE/HTTP response | terminal result existuje | replay přes idempotency/event stream |
| během cleanup kroku | cleanup checkpoint ukazuje poslední complete step | idempotentně pokračovat; nový business run neaktivovat před required safe pointem |
| po backup restore, před worker restartem | nová platform incarnation invaliduje staré writes | obnovit queues/outbox a reconcile pending side effects |
| po MCP HTTP/JSON parse rejection | nevznikl call ani handler side effect | klient opraví request; nový transport ID |
| po active JSON-RPC ID rezervaci, před call create | request event existuje, business call ještě ne | recovery rezervaci uzavře failure nebo dokončí create; duplicate ID se mezitím odmítá |
| po `input_required`/requestState commitu, před response | exchange je durable a parent čeká | retry stejné operation načte stejný exchange; nevytvoří nový side effect |
| po MRTR accepted input commitu, před resume delivery | consume a successor outbox jsou atomické | publisher doručí právě jeden resume |
| po task commitu, před CreateTaskResult delivery | task je durable a resolvable | caller načte task přes idempotentní retry/outcome; nevytvoří druhý task |
| po `tasks/update` consume commitu, před ACK | accepted keys a successor jsou durable | retry je idempotentní; `tasks/get` ukáže current state |
| po `tasks/cancel` intent commitu, před ACK | cancel intent je durable, task může ještě terminalizovat jinak | retry vrátí ACK/current state; first terminal commit wins |
| po subscription ACK commitu, před first frame | subscription nesmí emitovat jinou notification | recovery buď emituje ACK jako first message, nebo stream uzavře a client refreshne |
| po MCP terminal response commitu, před JSON/SSE delivery | canonical call/task outcome existuje | retry/poll načte outcome; transport loss jej nemění |
| po SSE disconnect před dispatch claimem | cancellation intent blokuje nový dispatch | terminalizovat bez effectu nebo načíst current outcome |
| po SSE disconnect po possible dispatchi | outcome není odvozen z disconnectu | reconcile; `UNKNOWN` → manual review |

Implementace nesmí obsahovat jiný recovery význam pro tentýž poslední potvrzený bod v MCP, agentním, generation, browserovém, deploymentovém nebo component-control runtime.

### 49.26 Databázové constraints a povinná technická pole

Každý mutable aggregate a samostatně lifecycle-řízený run/task/action obsahuje `state_version bigint not null` a current platform incarnation. Každý leased nebo fenced root navíc obsahuje non-null `current_fencing_token bigint` s monotonic pravidly 51.15; nefenced root fencing field nemá a jeho operation record musí mít `fencingPolicy = NONE`. Každá terminal tabulka má check/trigger guard, který odmítne změnu business fields po terminalizaci; povolené jsou pouze append-only evidence/retention metadata výslovně oddělená od terminal row.

Povinné databázové objekty jsou:

- `platform_incarnation`,
- `domain_command`,
- `domain_idempotency_record`,
- `side_effect_operation`,
- `side_effect_attempt`, `side_effect_attempt_state` a `side_effect_attempt_evidence`,
- `transactional_outbox`,
- `transactional_inbox`,
- `queue_item`,
- `concurrency_claim`,
- `binding_set_revision` a `binding_set_member`,
- `activation_domain_head` a `domain_command_activation_domain`,
- `activation_head`,
- `application_deployment_head`,
- `activation_domain_barrier`,
- `cleanup_operation` a `cleanup_resource`,
- `configuration_apply_run`,
- `schema_migration`.

Povinné constraints:

- unique idempotency scope + key,
- unique queue kind + parent + logical operation ID,
- unique transactional inbox consumer scope + event ID,
- unique active lease per fenced resource,
- unique active concurrency claim per namespace/key,
- unique event sequence per aggregate,
- unique checkpoint sequence per parent,
- unique phase attempt per job/phase/attempt number,
- partial unique nejvýše jeden active phase run a coordinator lease na job,
- unique provider tool call ID per model call,
- unique task/input exchange/state-handle public ID a lookup digest,
- unique active control lease per browser session a unique current control epoch/fence lineage,
- unique `(session_id,page_id,page_generation)`, `(page_id,frame_id,attachment_epoch)` a `(frame_id,document_epoch)` browser identity,
- unique current action attempt per logical browser action a monotonic dispatch phase/event sequence,
- partial unique active bridge session assignment a active local profile lease per bridge/profile,
- unique active account/resource concurrency claim pro konfliktující browser mutations,
- unique activation member per set/object,
- partial unique nepřekrývající active activation transaction per activation domain,
- foreign keys dokazující, že active pointer patří správnému parentu a candidate/previous snapshotu,
- check, že `lease_expires_at > lease_acquired_at`,
- check, že terminal result/error digest je přítomen právě podle terminal state,
- check, že `UNKNOWN` má reconciliation evidence a active manual-review relation,
- check, že checkpoint fence/incarnation odpovídá parentu v okamžiku pointer update,
- check, že event/outbox payload digest odpovídá canonical payloadu.

Critical guards se nevynucují pouze aplikačním `if`; mají PostgreSQL unique/check/partial-unique/composite-foreign-key constraints, immutable triggers a guarded `UPDATE ... WHERE state_version = ? AND fencing_token = ? AND platform_incarnation_id = ? AND application_deployment_epoch = ?` nebo transakčně ekvivalentní implementaci. Přesné physical patterns jsou v kapitole 51.

### 49.27 API concurrency a recovery contract

Každá mutující API operace deklaruje v OpenAPI/operation catalogu:

- povinnost a scope `Idempotency-Key`,
- zda vyžaduje `If-Match`/`expectedStateVersion`,
- expected revision, digest, binding-set revision a activation epoch,
- side-effect/retry/concurrency class,
- synchronous linearization point,
- async run/task reference,
- terminal result a reconciliation endpoint,
- stable conflict a stale error codes.

Standardní mutation response metadata obsahují:

```json
{
  "correlationId": "uuid",
  "logicalOperationId": "uuid",
  "commandId": "uuid",
  "stateVersion": 12,
  "eventSequence": 48,
  "activationEpoch": 9,
  "resultDigest": "sha256:...",
  "idempotencyReplay": false,
  "serverTime": "timestamp"
}
```

Async acceptance response znamená pouze, že command a queue/outbox byly commitnuté. Neznamená dokončený side effect. Stav je dostupný přes returned run/task/logical operation ID.

`409` se používá pro state-version, stale digest, activation epoch, control epoch, consumed approval/input a idempotency conflict. `423` nebo stabilní conflict classification může vyjádřit current concurrency/activation barrier. `202` obsahuje durable operation reference. Timeout klienta nemění serverový operation state.

Manual-review action vyžaduje expected side-effect state version a evidence digest. OWNER_FULL autorita je zachována; nejde o novou roli ani approval vrstvu.

### 49.28 Stabilní error codes korektnosti

Chapter 32.6 je jediný souhrnný stable error catalog. Formální a transakční vrstva používá nejméně:

```text
STATE_VERSION_CONFLICT
IDEMPOTENCY_CONFLICT
LEASE_EXPIRED
FENCING_TOKEN_STALE
PLATFORM_INCARNATION_STALE
CHECKPOINT_STALE
CHECKPOINT_DIGEST_INVALID
REVISION_STALE
BINDING_REVISION_STALE
ACTIVATION_EPOCH_STALE
ACTIVATION_IN_PROGRESS
ACTIVATION_EFFECTIVE_UNKNOWN
SEQUENCE_GAP
QUEUE_ITEM_STALE
CONCURRENCY_KEY_HELD
SIDE_EFFECT_OUTCOME_UNKNOWN
SIDE_EFFECT_RECONCILIATION_FAILED
MODEL_SUBMIT_OUTCOME_UNKNOWN
TERMINAL_STATE_IMMUTABLE
CANCELLATION_ALREADY_TERMINAL
MANUAL_OUTCOME_CONFLICT
CLEANUP_INCOMPLETE
MCP_DUPLICATE_IN_FLIGHT_REQUEST_ID
MCP_REQUEST_STATE_CONSUMED
MCP_TASK_EXPIRED
MCP_OUTCOME_UNKNOWN
KCIP_MESSAGE_ID_CONFLICT
KCIP_OUTCOME_UNKNOWN
```

Každý code má deterministic classification, retry directive, side-effect point, required current snapshot, logical operation ID, correlation ID a canonical next action. Stale/fence/terminal chyby nejsou retryovány stejným worker attemptem. `*_OUTCOME_UNKNOWN` nikdy nepoužívá automatic safe retry. Existing code meaning ani recovery se nesmí změnit; změna vyžaduje nový code a compatibility review.

### 49.29 Testovatelné invarianty

CI, self-test a production acceptance dokazují nejméně tyto invarianty:

1. jeden idempotency key a digest vytvoří právě jednu logical operation,
2. stejný key s jiným digestem nikdy nespustí handler,
3. dva CAS commands nad stejnou state version nemohou oba uspět,
4. aggregate event sequence nemá duplicate ani gap mezi commitnutými eventy,
5. domain commit vždy má požadovaný audit a outbox,
6. ztracený NOTIFY neztratí queue práci,
7. expired lease nelze heartbeat-em obnovit,
8. takeover vždy vytvoří vyšší fence,
9. late worker nemůže zapsat checkpoint, result, terminal event ani pointer,
10. platform restore invaliduje všechny staré leases i při shodném číselném tokenu,
11. external side effect nemůže být dispatchnut bez intentu,
12. crash v každém bodě matice 49.25 nevytvoří duplicate observable outcome,
13. unknown non-idempotent outcome nikdy nespustí blind retry,
14. cancel-first race nemůže skončit success, který naplánuje nový side effect,
15. success-first race zůstane immutable při pozdějším cancelu,
16. terminal row nelze znovu otevřít,
17. stale revision/binding/approval/input/checkpoint je odmítnut před side effectem,
18. phase success a next enqueue jsou all-or-nothing,
19. discussion completion/steer vytvoří právě jeden terminal turn a nejvýše jednoho successora,
20. model submit ambiguity nevytvoří automatický druhý provider call,
21. confirmed tool output se po restartu neopakuje,
22. workspace patch je celý aplikován, nebo celý odmítnut,
23. materialized workspace lze vždy obnovit z current immutable revision,
24. integration step resume nevytvoří duplicitní identity, route, binding, runtime ani release,
25. activation switch ukáže celý previous nebo celý candidate set,
26. žádný request nepoužije members z více activation epochs,
27. double activation se stale previous snapshotem je odmítnuta,
28. first CREATE rollback po switchi vede na explicitní `ABSENT` set bez active route,
29. UPDATE/REPAIR rollback obnoví přesný previous snapshot a vyšší epoch,
30. remediation candidate nevznikne před complete cleanupem předchůdce,
31. browser session a local profile mají nejvýše jednoho active control/action writer,
32. stale runtime build/host/context/page/frame/document/control/observation/viewport/target input není dispatchnut do browseru,
33. každá mutující action má commitnutý intent před nejčasnějším mutation triggerem a po možném triggeru nelze stav vrátit do blind safe retry,
34. crash, disconnect, takeover nebo navigation během možné mutace vede k independent postcondition/reconciliation a nejvýše jednomu observable outcome,
35. browser state bundle/account auth epoch/bridge assignment/profile lease aktivace používají CAS bez ABA a automation step success vždy má validní independent postcondition,
36. stale heartbeat/probe/ACK nemění current effective state,
37. config apply nehlásí success při mixed effective versions,
38. half-open circuit má právě jeden fenced probe,
39. deployment se smí označit active pouze při shodě DB epoch, symlinku, manifestu a process heartbeats,
40. recovery po restore nepovažuje pending external effect za not applied bez evidence,
41. read použitý k mutačnímu rozhodnutí je po locku znovu ověřen nebo kryt CAS/constraintem,
42. všechny transakce dodržují jediný lock order 51.6 a audit head zamykají poslední,
43. `SKIP LOCKED` se nepoužije pro activation, cleanup completeness ani globální invariant,
44. concurrent OWNER API-key authentication/command acceptance versus rotation má právě jeden linearizační výsledek,
45. activation barrier zabrání phantom mutating admission přes explicitní domain head guard,
46. terminal write a required successor enqueue jsou jeden commit,
47. immutable revision/evidence update nebo delete fyzicky odmítne DB trigger,
48. application rollback funguje nad forward-only expand/migrate/validate schema bez production down migration,
49. MCP pre-handler parse/header/version/capability failure nevytvoří call run ani side-effect intent,
50. JSON-RPC duplicate in-flight ID nemůže spustit druhý handler a transport ID reuse po terminal response není business replay,
51. HTTP/SSE disconnect nikdy sám neurčí applied/not-applied mutující outcome,
52. MCP terminal DB outcome existující před ztracenou response je při retry načten bez nového dispatchu,
53. unknown nebo unnegotiated resultType není předán agentovi ani dalšímu modelovému kolu,
54. MRTR exchange vznikne před non-idempotent continuation effectem a consumed replay nevytvoří druhý resume,
55. unknown/already-satisfied MRTR/task input key nezmění business state,
56. task je resolvable před vydáním task ID,
57. tasks/cancel ACK není terminalita a first valid terminal commit je jediný outcome,
58. tool result `isError=true` v tasku je `COMPLETED`, nikoli `FAILED`,
59. task expiry po possible effectu provede reconciliation před terminalizací,
60. subscription acknowledgment je první protocol message a acknowledged filter je podmnožinou requestu,
61. notification mimo acknowledged filter nebo po final response se nikdy neemituje,
62. MCP subscription disconnect nepředstírá replay ani authoritative completeness,
63. cache private entry se nikdy nesdílí mezi access/binding contexts,
64. MRTR request a input_required result se necachují,
65. invalid cursor invaliduje celý cached traversal a empty cursor zůstává validní,
66. schema `$ref` validation neprovede neautorizovaný runtime network fetch,
67. změna native MCP schema digestu invaliduje OpenAI projection a exact agent binding,
68. recognized modern protocol error nemůže vyvolat legacy fallback,
69. auth/network/5xx probe nemůže klasifikovat server jako legacy,
70. KCIP message dedupe a business idempotence nemohou zaměnit své identity nebo recovery.

### 49.30 Formální model a crash-injection ověření

Repository obsahuje executable state-machine model v TLA+/PlusCal pro minimálně:

- queue claim, lease expiry, heartbeat a takeover,
- idempotency claim a duplicate request,
- cancellation versus terminal transition,
- side-effect intent/dispatch/outcome/reconciliation,
- generation job/phase transition a next enqueue,
- discussion completion versus steer,
- activation barrier/switch/postflight/rollback,
- browser session/control takeover, page/frame/document replacement, action dispatch phases, mutation-trigger crash, state-bundle/auth epoch, bridge/profile lease a stale action,
- task/input response versus expiry/cancel,
- platform restore a stale worker.

Model ověřuje safety invarianty 49.1 a absence deadlocku v deklarovaném lock orderu. Bounded liveness se ověřuje za fair scheduling assumptions.

PostgreSQL concurrency suite spouští skutečné paralelní transakce, přesné isolation profiles, canonical row/advisory lock pořadí, deadlock/serialization retry, lock timeout, lease expiry během transakce, queue `SKIP LOCKED`, activation phantom, secret/API-key rotation a stale writes podle 51.34. Nemesis crash suite ukončuje worker po každém persistovaném kroku a před/po každém external adapter boundary podle 49.25. Každý test po recovery kontroluje DB constraints, event/audit/outbox sequence, side-effect counter/read-back, active pointers, orphan inventory, leases, cleanup a exactly-once observable result.

Production acceptance nemusí úmyslně provádět destruktivní external mutation. Používá provider sandbox nebo idempotentní fixture s počitadlem a read-back kontraktem. Formální model, PostgreSQL test a production-shaped crash harness jsou blocking gates; existence kódu nebo unit mock je nenahrazuje.

### 49.31 Architektonické closure pravidlo

Každá nová nebo generovaná mutující operace musí před activation uvést:

- aggregate root a state machine,
- exact allowed transitions a terminal states,
- PostgreSQL transaction profile a isolation level,
- ordered advisory/row lock plan a DB-time guard,
- state-version/CAS contract a physical unique/check/composite-FK constraints,
- idempotency scope,
- concurrency key,
- lease/fencing potřebu,
- pre- a post-checkpoint hranice,
- side-effect intent, target idempotency a reconciliation,
- cancellation linearization,
- event/audit/outbox transaction boundary,
- recovery rozhodnutí pro každý crash point,
- cleanup a compensation,
- activation/rollback relation,
- forward-schema migration a application rollback compatibility,
- invariant, parallel transaction, crash a nemesis tests.

Operace bez kteréhokoli relevantního bodu není implementačně uzavřená a její blocking gate nemůže být `PASS`. Technická neúplnost se neřeší implicitním předpokladem ani architektonickou domněnkou.

### 49.32 Agentic operation context state machine

`operation_context` používá pouze hrany:

```text
COMPILED → VALIDATED
COMPILED → INVALIDATED
VALIDATED → DISPATCH_RESERVED
VALIDATED → INVALIDATED
DISPATCH_RESERVED → DISPATCHED
DISPATCH_RESERVED → INVALIDATED
DISPATCHED → TERMINAL
DISPATCHED → MANUAL_REVIEW
MANUAL_REVIEW → TERMINAL
```

`DISPATCH_RESERVED` vzniká v jedné transakci s exact semantic-action-plan digestem, idempotency/concurrency claimem, pre-side-effect checkpointem a případným side-effect operation/outbox intentem. Po přechodu do `DISPATCHED` nelze context invalidovat jako by effect nenastal; cancellation nebo OWNER steer používá existující outcome/reconciliation pravidla. `MANUAL_REVIEW → TERMINAL` je přípustné pouze po persistovaném OWNER resolution nebo reconciliation, které uzavře jediný canonical outcome.

Před reservation se pod parent root lockem znovu ověří authority-lineage currentness, OWNER-intent digest, agent/automation/spec revision, target/binding/activation snapshot, argument-origin/value-derivation set, Secret-use contexts, deadline a cancellation version. Stale nebo odlišný digest nevytvoří dispatch.

OWNER steer nebo nová command revision invaliduje pouze dosud nedispatchnuté descendant contexts; invalidace `DISPATCH_RESERVED` atomicky uzavře neclaimnutý dispatch outbox a znemožní jeho pozdější claim. Model/tool/handoff output přijatý po invalidaci se uloží jako evidence, ale nemůže rezervovat successor nebo side effect. Každý terminal action/outcome odkazuje na jediný operation-context a action-plan digest.

Formální model obsahuje races: model output versus OWNER steer, data refresh versus target reservation, handoff return versus parent invalidation, Secret rotation versus resolve, compaction pointer switch versus active turn a stale operation-context worker versus activation/deployment epoch.

### 49.33 Persistentní platform recovery state machine

`platform_recovery_head` je singleton mutable aggregate s immutable recovery attempts. Používá pouze stavy:

```text
STARTING
STARTING → RECONCILING
RECONCILING → READY
RECONCILING → BLOCKED
RECONCILING → MANUAL_REVIEW
BLOCKED → RECONCILING
BLOCKED → MANUAL_REVIEW
MANUAL_REVIEW → RECONCILING
```

Každý attempt obsahuje current database start identity, platform incarnation, application deployment epoch, recovery epoch, state version, lease owner/fence/expiry, started/finished time, inventory watermark, schema/constraint digest, classification counts, unresolved object IDs a evidence digest. `READY` je platné pouze pro exact n-tici, která byla reconciliována; změna kteréhokoli členu ji okamžitě zneplatní.

Mutující admission transakce pod shared recovery guardem ověří, že:

1. existuje právě jeden current head,
2. head je `READY`,
3. current database start identity odpovídá persistovanému `readyDatabaseStartIdentity`,
4. platform incarnation a application deployment epoch odpovídají request/worker snapshotu,
5. recovery epoch a state version jsou current,
6. požadovaný domain scope nemá blocking unresolved recovery item.

Nesoulad vzniká před side-effect intentem a vrací `PLATFORM_RECOVERY_IN_PROGRESS` nebo `PLATFORM_RECOVERY_BLOCKED`. Read-only diagnostika může pokračovat, nesmí však získat žádnou write nebo dispatch authority.

Recovery inventory zahrnuje všechny nonterminal roots, queue/outbox/inbox items, leases, concurrency claims, side-effect operations/attempts, model calls, MCP calls/tasks/input exchanges, agent runs, browser sessions/actions/downloads/uploads, runtime instances/processes/sockets, generation phases, activation/deployment/rollback/cleanup operations a filesystem pointers/artifacts. Každá položka získá právě jednu persistovanou klasifikaci:

```text
TERMINAL_REPLAY
RESUME
RECONCILE
CANCEL
CLEANUP
MANUAL_REVIEW
```

Klasifikace a successor enqueue se commitují atomicky. Recovery worker lze zabít před/po každém itemu; takeover pokračuje pod vyšším fence bez opakovaného external dispatchu. `READY` se commitne pouze po stabilním druhém inventárním průchodu, nulovém unclassified item countu, platných constraints/pointer digestech a uzavření všech mandatory startup blockers.

### 49.34 Terminal closure, pending children a late evidence

Každý business root má machine-readable closure query a closure version. Úspěšný terminal transition zamkne root, načte všechny authority-bearing child scopes v canonical lock orderu, ověří closure predicate 48.38 a v jedné transakci uloží terminal outcome, closure evidence, event, audit a případný delivery outbox. Child vytvořený po terminal commitu musí být fyzicky znemožněn composite parent-state guardem nebo deferred constraint triggerem.

Late provider response, worker result, webhook, browser event, queue delivery, heartbeat nebo cleanup ACK po terminal commitu se uloží jako immutable evidence. Může potvrdit již existující canonical outcome nebo otevřít reconciliation/security incident, ale nesmí změnit terminal result last-write-wins, založit successor, obnovit lease ani aktivovat pointer. Pokud late evidence prokazuje rozpor s canonical outcome, vznikne `MANUAL_OUTCOME_CONFLICT` a automatic side effect je blokovaný.

`COMPLETED` generation job, successful agent/tool/browser run, active release a project closure nesmí obsahovat provisional nebo pending authority-bearing objekt. Terminal tombstone se nepovažuje za pending, pokud jeho DB constraints zakazují dispatch, write a pointer activation.

### 49.35 Capacity, backpressure a recovery fairness

Každý queue kind a broker deklaruje hard admission limit, soft backpressure limit, per-parent fairness, maximum payload bytes, reserved recovery slots a storage reserve. Limit je technický automaticky spravovaný parametr; OWNER jej nemusí ručně konfigurovat.

Při dosažení capacity platí:

- nový durable command se buď celý přijme s queue/outbox záznamem, nebo se celý odmítne bez `ACCEPTED`,
- accepted item se nezahodí, neevictuje a nepřepíše,
- duplicate stejné logical operation načte current item a nezvyšuje capacity count,
- retry používá stejné logical operation a nesmí vytvářet queue amplification,
- recovery, reconciliation, cancellation, rollback a cleanup používají rezervované claim lane a nejsou blokovány běžnou admission,
- deadline expirace sama nemaže item; terminalizace proběhne guarded transakcí podle side-effect evidence,
- queue saturation nesmí prodloužit lease bez heartbeat, změnit ordering ani obejít dependency/barrier guard.

Persistentní capacity counter je odvozený nebo transakčně chráněný a po crashi se reconciliuje s actual rows. Counter drift blokuje admission, nevede k tichému dropu.

### 49.36 Jednotná klasifikace po poruše

Po libovolném faultu se každý affected logical operation rozhoduje pouze podle persistované dispatch/effect evidence:

| Stav evidence | Přípustné recovery |
|---|---|
| intent neexistuje | operace nebyla přijata; nový command je nová operation |
| intent commitnutý, dispatch authority nevznikla | cancel nebo resume stejné operation |
| dispatch authority existuje, adapter prokazatelně neodeslal possible-effect trigger | `CONFIRMED_NOT_APPLIED`, retry stejné operation podle contractu |
| possible-effect trigger nastal a target má exact idempotency key | retrieve/replay target outcome; bez nového business effectu |
| possible-effect trigger nastal a existuje authoritative read-back | reconcile na `CONFIRMED_APPLIED`, `CONFIRMED_NOT_APPLIED` nebo `UNKNOWN` |
| possible-effect trigger nastal bez idempotency a bez decisive read-back | `UNKNOWN` → `MANUAL_REVIEW` |
| terminal DB commit existuje | canonical outcome se replayuje; transport failure jej nemění |
| evidence si odporuje | `MANUAL_OUTCOME_CONFLICT`/`RECOVERY_ORACLE_CONFLICT` → `MANUAL_REVIEW` |

DNS, connect a TLS failure jsou před possible application side-effect triggerem pouze tehdy, když instrumentation potvrzuje, že nebyl zapsán žádný application request byte. Po zahájení request headers/body, browser mutation triggeru, provider create submitu, filesystem pointer rename nebo systemd action triggeru se automatické `NOT_APPLIED` nesmí odvozovat z exception class, HTTP textu ani process exit code.

## 50. Trusted Runtime Boundary a Linux IPC Security

### 50.1 Rozsah a základní invarianty

Trusted Runtime Boundary je jednotná OS a runtime hranice mezi generated handlerem a autoritativní platformou. Jejím účelem je vynutit již závazný model bez nové role, scope, tokenu, permission registru, approval vrstvy nebo control plane.

Platí současně:

1. `OWNER_FULL` vzniká pouze po autentizaci OWNER session nebo jediného `KCML_OWNER_API_KEY` na veřejné HTTPS gateway.
2. Interní `CONTRACT` context vzniká pouze z commitnutého autoritativního dispatch/run záznamu a exact active bindingu.
3. `EXTERNAL` context vzniká pouze po ověření endpoint-local credentialu na exact route.
4. Generated handler nikdy nevytváří execution context a nemůže dodat source, target, revision, release, binding, access channel ani epoch.
5. Generated handler nemá přímý přístup k síti, PostgreSQL, Secret Manager storage, interním broker socketům, browser hostu, host filesystemu ani systemd credential directory.
6. Jedinou platformní schopností handleru je pre-opened capability channel k trusted runtime hostu.
7. Každý autoritativní call znovu ověřuje current platform incarnation, application deployment epoch, activation epoch, source/target revisions, release/runtime digest, runtime generation a binding-set revision.
8. PID, UID, GID, socket path, environment value, runtime manifest ani process memory nejsou samostatnou autoritou.
9. Stará release, runtime generation nebo service generation po pointer switchi nemůže přijmout nový autoritativní call ani zapsat authoritative outcome.
10. Každý socket, process, namespace, descriptor, stream, credential materialization a temporary artifact má jednoznačného ownera, generation, lifecycle a idempotentní cleanup.

### 50.2 Trusted computing base

Trusted computing base této hranice tvoří pouze:

- Linux kernel a systemd produkčního Ubuntu hostu,
- `kcml-web-api` jako veřejná gateway,
- `kcml-runtime-gateway`,
- `kcml-runtime-host@<runtime-instance-id>`,
- trusted sandbox launcher a component SDK bootstrap před načtením generated module,
- `kcml-secret-broker`, `kcml-egress-gateway` a state broker,
- browser worker, browser host a bridge gateway v rozsahu jejich přesných hranic,
- PostgreSQL autoritativní state machines a activation/deployment heads,
- immutable release, runtime a contract manifests.

Generated handler, jeho dependencies, child executable, modelový výstup, caller payload, browser DOM, external response, runtime environment a writable filesystem nejsou zdrojem platformní autority.

Závazný trust model secrets zůstává nezměněný. Tato kapitola neomezuje OWNERa, AI, development, GitHub, CI, deployment, logování ani runtime použití secret hodnot uvnitř důvěryhodného celku. Uzavírá pouze to, který proces smí vytvořit platformní operaci a kterou OS cestou ji provede.

### 50.3 Kanonická runtime identita

Nezaměnitelná runtime process identity je n-tice:

```text
platformIncarnation
applicationDeploymentEpoch
hostBootId
systemdUnitName
systemdInvocationId
linuxUid
linuxGid
linuxPid
processStartTicks
cgroupPath
runtimeInstanceId
runtimeGeneration
componentId
sourceRevisionId
releaseId
runtimeDigest
bindingSetRevision
activationEpoch
```

`linuxPid` se nikdy nepoužije bez `hostBootId`, `processStartTicks`, systemd unit, `InvocationID`, cgroup path, runtime instance a generation. UID/GID určují OS class procesu, nikoli component authority. Hodnota z payloadu, environmentu, filename nebo systemd instance stringu je pouze kandidát a musí být potvrzena current PostgreSQL runtime-instance recordem.

`runtimeGeneration` je monotonic číslo alokované pod runtime-instance row lockem při každém startu, replacementu nebo recovery attachi. `systemdInvocationId` identifikuje jeden konkrétní start unit. Ani jedna hodnota není credential a její znalost bez odpovídajícího kernel peer procesu neposkytuje přístup.

### 50.4 Vytvoření serverového execution contextu

#### 50.4.1 OWNER request

Veřejná gateway:

1. ověří TLS, hostname, route, request schema a OWNER session nebo jediný OWNER API klíč,
2. vytvoří nebo načte logical operation a idempotency record,
3. načte current target revision/release, activation epoch a operation contract,
4. v jedné PostgreSQL transakci uloží immutable `runtime_execution_context` s `binding.kind = OWNER_FULL`,
5. odstraní `Authorization`, cookie, CSRF a jiné gateway-auth metadata z interního payloadu,
6. předá handleru pouze operation arguments validované podle target schema.

OWNER context neobsahuje permission lookup. Každá operation v OWNER operation catalogu je dostupná s plnou OWNER autoritou.

#### 50.4.2 Endpoint-local external request

Veřejná gateway ověří exact external auth binding proti hostname, endpointu, method, route/tool, credential version a verification contractu. Poté vytvoří immutable context s `binding.kind = EXTERNAL`, exact binding ID/revision/digest a access channel `EXTERNAL_BINDING`. Credential se nestane obecným principalem a nelze jej použít na jiné route.

#### 50.4.3 Interní runtime request

Trusted dispatcher nebo runtime gateway vytvoří context pouze z commitnutého parent run/task/queue/control/side-effect recordu. V jedné transakci freeze:

- source object, revision a release,
- source runtime instance a generation,
- logical operation a execution attempt,
- current worker lease/fence, pokud jej operace používá,
- exact local capability alias a z něj odvozený target,
- exact binding ID/revision/digest a contract digest,
- target revision/release,
- binding-set revision a activation epoch,
- application deployment epoch a platform incarnation,
- deadline, cancellation version a correlation.

Context se vytvoří až po exact binding/readiness preflightu a před handler dispatchi. Neexistuje API, SQL operation ani handler operation pro jeho create/update. Terminal context se znovu neotevírá.

#### 50.4.4 Spoof a reserved fields

Public, internal i handler input schemas rezervují názvy `source`, `target`, `binding`, `executionContext`, `accessChannel`, `revisionId`, `releaseId`, `runtimeInstanceId`, `runtimeGeneration`, `activationEpoch`, `bindingSetRevision`, `platformIncarnation`, `applicationDeploymentEpoch` a jejich case/namespace varianty. Jejich přítomnost mimo explicitně deklarovaný business field způsobí `RUNTIME_CONTEXT_SPOOF_ATTEMPT` před side effectem.

### 50.5 OWNER_FULL, CONTRACT a EXTERNAL hranice

`OWNER_FULL`, `CONTRACT` a `EXTERNAL` jsou tři pevné serverové větve, nikoli role nebo konfigurovatelné permissions.

- `OWNER_FULL` přijímá pouze veřejná gateway po platné OWNER autentizaci. Interní UDS nemůže požádat o OWNER_FULL a generated handler jej nemůže propagovat.
- `CONTRACT` přijímá pouze trusted runtime gateway z exact active source revision → target revision/operation bindingu. Caller může vybrat jen local capability alias deklarovaný ve své immutable revision.
- `EXTERNAL` přijímá pouze veřejná gateway po exact endpoint-local credential verification. External credential ani jeho context nelze převést na CONTRACT nebo OWNER_FULL.

Při nested callu vzniká nový child context s vlastním exact bindingem a causation relation. Parent authority se nekopíruje jako bearer údaj. OWNER_FULL child call vzniká pouze uvnitř trusted serverové operation catalog cesty; generated handler pracuje jako target component execution a jeho následné component/egress/secret calls používají exact bindings jeho revision.

### 50.6 Systemd služby, UID/GID a groups

Kanonické identity jsou nejméně:

```text
kcml-web-api
kcml-runtime-gateway
kcml-runtime-host
kcml-secret-broker
kcml-egress-gateway
kcml-browser-worker
kcml-browser-host
kcml-browser-bridge
kcml-audit
```

Každá je non-login systémová identita bez shellu, home a lidského SSH přístupu. Generated handler běží v user namespace a není členem žádné platformní Unix group.

Unix groups jsou transportně úzké:

- `kcml-runtime-callers` obsahuje pouze trusted platform service identities, které smějí otevřít runtime gateway; neobsahuje generated handler UID ani lidský účet,
- broker socket group obsahuje pouze `kcml-runtime-gateway`,
- browser-host control socket group obsahuje pouze `kcml-browser-worker`,
- bridge transport nepoužívá lokální broad group; přijímá pouze mTLS bridge connection na exact gateway route.

Členství ve group dovoluje otevřít transport, ale samo nepovolí operaci. Gateway po acceptu vždy provede kernel/systemd/runtime identity a exact binding validation.

### 50.7 Kanonické UDS cesty a ownership

Kanonické socket paths jsou:

```text
/run/kajovocml-ng/gateway/runtime-gateway.sock
/run/kajovocml-ng/brokers/secret-broker.sock
/run/kajovocml-ng/brokers/egress-gateway.sock
/run/kajovocml-ng/brokers/state-broker.sock
/run/kajovocml-ng/browser-hosts/<slot>/control.sock
```

Platí:

- `/run/kajovocml-ng` je `root:root` mode `0755`,
- `/run/kajovocml-ng/gateway` je `root:kcml-runtime-callers` mode `0750`, bez write práva group,
- `/run/kajovocml-ng/brokers` je `root:kcml-runtime-gateway` mode `0750`, bez write práva group,
- `/run/kajovocml-ng/browser-hosts/<slot>` je `root:kcml-browser-worker` mode `0750`, bez write práva group,
- runtime gateway socket je `kcml-runtime-gateway:kcml-runtime-callers` mode `0660`,
- broker socket je `<broker-user>:kcml-runtime-gateway` mode `0660`,
- browser-host socket je `kcml-browser-host:kcml-browser-worker` mode `0660`.

Parent directory žádný caller nesmí zapisovat. Socket path není symlink, hardlink ani abstract-namespace socket. Abstract Unix sockets se pro platformní hranice nepoužívají, protože nemají filesystem ownership boundary.

Generated handler mount namespace neobsahuje host `/run/kajovocml-ng`. Jeho privátní `/run` je prázdný tmpfs a seccomp po startu zakazuje `socket`, `socketpair`, `connect`, `bind`, `listen`, `accept` a `accept4` pro generated code.

### 50.8 Socket activation a lifecycle

Každý filesystem UDS vytváří systemd `.socket` unit, nikoli aplikační service. Runtime gateway používá ekvivalent:

```ini
[Socket]
ListenStream=/run/kajovocml-ng/gateway/runtime-gateway.sock
SocketUser=kcml-runtime-gateway
SocketGroup=kcml-runtime-callers
SocketMode=0660
DirectoryMode=0750
Accept=no
RemoveOnStop=yes
Service=kcml-runtime-gateway.service
```

Service při startu:

1. přijme právě jeden named activation FD,
2. ověří `LISTEN_FDNAMES`, počet FD, `AF_UNIX`, `SOCK_STREAM`, listening stav a nonblocking/CLOEXEC flags,
3. porovná `fstat()` activation FD s `lstat()` path podle device, inode a socket type,
4. otevře parent path pomocí `openat2` s `RESOLVE_BENEATH|RESOLVE_NO_SYMLINKS|RESOLVE_NO_MAGICLINKS|RESOLVE_NO_XDEV`,
5. ověří owner/mode všech parentů a socketu,
6. odmítne start a readiness při jakémkoli rozdílu.

Service nikdy neprovádí `unlink`, `bind`, `chmod`, `chown` ani replace canonical socket path. Při stopu systemd zavře listening FD a `RemoveOnStop=yes` odstraní path. Stale path při bootu odstraňuje root-owned `tmpfiles.d` pravidlo pouze po `lstat` ověření, že jde o socket v exact canonical rootu, nikoli symlink nebo mountpoint.

Při service restartu listening socket může zůstat pod socket unit, ale všechny accepted connections staré service generation se uzavřou. Nová service má nový `InvocationID`; každý caller provede reconnect a plný handshake. Pending connection bez validní current identity je odmítnuta. Žádná in-memory call autorita nepřežívá restart.

### 50.9 Linux peer credentials a PID reuse

Runtime gateway bezprostředně po `accept4(SOCK_CLOEXEC|SOCK_NONBLOCK)` získá `SO_PEERCRED` a zaznamená UID, GID a PID. Následně:

1. ověří UID/GID proti přesné service class,
2. otevře pidfd pro peer PID a sleduje process exit,
3. dvakrát načte `/proc/<pid>/stat` start ticks před a po systemd/cgroup resolution; změna nebo zmizení proces odmítne,
4. načte host boot ID,
5. načte `/proc/<pid>/cgroup` a ověří exact systemd unit instance,
6. přes systemd manager ověří unit `MainPID`, `InvocationID`, active state a expected service template,
7. porovná peer HELLO runtime instance/generation s current PostgreSQL recordem a immutable launch manifest digestem,
8. ověří, že connection nevykazuje EOF/HUP před dokončením identity commit,
9. uloží immutable `runtime_ipc_connection` evidence.

Peer HELLO je důkaz shody, ne credential. Musí obsahovat exact service/unit/runtime metadata, ale autoritou zůstává kernel peer + systemd + PostgreSQL shoda. PID reuse nemůže projít, protože numeric PID bez stejného start ticks, boot ID, `InvocationID`, cgroup, runtime generation a živého socket peeru je odmítnut.

Při `pidfd` exit eventu gateway okamžitě uzavře connection, zablokuje nové calls a rozpracované operations předá do crash/reconciliation pravidel. Accepted connection není znovu přiřazena novému procesu se stejným PID.

U datagram/seqpacket transportu, pokud je někde výslovně použit, service zapne `SO_PASSCRED` a vyžaduje kernel `SCM_CREDENTIALS` na každém packetu. Runtime gateway a browser-host control používají connected stream s `SO_PEERCRED`; anonymous handler channel je svázán přímo s pidfd-tracked child procesem.

### 50.10 Runtime instance a launch manifest

`runtime_instance` je serverový objekt svázaný s exact component runtime targetem. Jeho immutable start snapshot obsahuje:

- component ID a source revision,
- release ID, artifact digest a runtime digest,
- dependency lock digest,
- binding-set revision a activation epoch,
- application deployment epoch a platform incarnation,
- runtime instance ID a runtime generation,
- systemd unit name a expected service class,
- resource profile digest,
- namespace, seccomp, environment a FD profile digests,
- handler entrypoint a export/schema digests,
- state schema revision,
- cleanup inventory template.

Systemd může snapshot materializovat jako `LoadCredential=runtime-manifest.json:<root-owned-source>`. Tento soubor je read-only launch input, nikoli bearer credential. Runtime host před readiness načte DB row a porovná canonical manifest digest. Nesoulad končí `RUNTIME_LAUNCH_MANIFEST_MISMATCH`, handler se nespustí a unit není ready.

Žádná hodnota z launch manifestu nemůže aktivovat inactive revision, vytvořit binding nebo obejít current DB head. Runtime gateway při každém callu ověřuje current head nezávisle na manifestu.

### 50.11 Sandbox launch linearizační body

Runtime host spouští právě jeden current generated handler process tree pro runtime generation. Launch probíhá:

1. ověření current runtime instance snapshotu, release digests a activation eligibility,
2. otevření immutable release root přes trusted directory FD a digest manifest,
3. vytvoření `AF_UNIX SOCK_STREAM|SOCK_CLOEXEC` socketpair,
4. `clone3` s PID tracking a new user, mount, network, IPC, UTS, PID a cgroup namespace,
5. child-side mount setup, private root, `/proc`, `/dev` a tmpfs,
6. UID/GID mapping pouze do runtime-host host UID/GID; generated process nemá capability v parent user namespace,
7. uzavření všech neallowlisted FD přes `close_range`,
8. dup capability endpoint na FD `3`, standard streams na přesné FD a žádné další inherited handles,
9. drop všech capabilities, securebits a setgroups,
10. `PR_SET_NO_NEW_PRIVS`, parent-death signal, seccomp a environment allowlist,
11. exec trusted component SDK bootstrap,
12. bootstrap nastaví `FD_CLOEXEC` na capability FD před importem generated module,
13. handler HELLO/READY s export/schema/runtime digests,
14. runtime host input/output conformance test,
15. guarded readiness evidence a teprve potom runtime admission.

Autoritativní start linearizační bod je commit readiness evidence odpovídající stejné runtime generation a živému pidfd. Process start bez tohoto commitu není ready a nepřijímá business call.

### 50.12 Namespaces

Generated handler používá:

- **user namespace** — generated UID 0 je mapováno pouze na neprivilegovaný runtime-host UID v parent namespace; před generated code jsou všechny capabilities zrušeny,
- **mount namespace** — vlastní root, žádná propagation do hostu, všechny mounty `MS_PRIVATE`,
- **network namespace** — žádné interfaces s výjimkou kernel-created loopback ponechaného administrativně down; handler nemá capability jej zapnout,
- **IPC namespace** — oddělené SysV/POSIX IPC objekty,
- **UTS namespace** — fixed non-authoritative hostname bez host identity,
- **PID namespace** — handler a jeho children nevidí host PID ani jiné runtimy; trusted sandbox init reaps zombies,
- **cgroup namespace** — unit cgroup je zobrazen jako root, cgroup filesystem není writable ani delegated.

`setns`, `unshare`, creation nested namespaces a mount namespace změny jsou generated code zakázané seccompem. Namespace FD nejsou handleru předány a host `/proc` není namountován.

### 50.13 Read-only root a filesystem boundary

Release root je content-addressed immutable directory. Aktivace vyžaduje manifest všech paths, type, mode, size a SHA-256 digest. Před launchi se ověří:

- každý path je canonical UTF-8 relative path bez `..`, NUL a absolutního prefixu,
- regular file má očekávaný owner/mode/digest,
- executable má exact manifest entry a není setuid/setgid,
- directory není group/world writable,
- device node, socket, FIFO a mountpoint nejsou v release přípustné,
- hardlink count regular file je `1`,
- symlink je pouze build-time schválený relativní link, jeho úplné resolution zůstává v release rootu a nevede přes magic link nebo mount crossing,
- žádný writable mount nepřekrývá immutable path.

Runtime launcher používá directory FD a `openat2` s `RESOLVE_BENEATH|RESOLVE_NO_MAGICLINKS|RESOLVE_NO_XDEV`; security rozhodnutí nevychází z string path po chrootu. Po `pivot_root` se starý root odpojí a všechny host directory FD zavřou. Samotný `chroot`, změna working directory nebo path-prefix validation není ekvivalentní a v produkčním sandboxu se nepoužije.

Writable paths jsou pouze:

```text
/tmp
/run
/work
```

Jde o privátní tmpfs s `nodev,nosuid,noexec`, exact size a inode limitem. `/work` je execution/runtime-generation scoped. Persistentní component state není filesystem mount a používá pouze `context.state`.

### 50.14 `/proc`, `/sys`, `/dev`, `/run` a host exposure

Sandbox `/proc` je nový procfs nad jeho PID namespace, mount flags `nosuid,nodev,noexec`, se skrytím cizích procesních údajů. Neobsahuje host PID, host environment, host fd tables ani jiné component runtimy.

`/sys` není namountován. Pokud runtime library vyžaduje konkrétní read-only pseudo-file, build manifest jej poskytne jako immutable synthetic file; host sysfs se nepředává.

`/dev` obsahuje pouze bind/mknod-less minimální zařízení vytvořená launcherem: `null`, `zero`, `random`, `urandom` a potřebné `fd/stdin/stdout/stderr` links v privátním devfs/tmpfs. `tty`, block/char devices, `/dev/net/tun`, GPU, input devices, host PTY a device control nodes nejsou dostupné, pokud konkrétní managed runtime není samostatně navržen mimo generated handler boundary.

`/run` je prázdný privátní tmpfs. Neobsahuje systemd, D-Bus, PostgreSQL, Docker/container, browser, Secret Broker, runtime gateway ani service credential socket/path. `/run/credentials` uvnitř handleru neexistuje.

### 50.15 Linux capabilities, privilege a seccomp

Před generated entrypointem jsou:

```text
CapabilityBoundingSet=
AmbientCapabilities=
EffectiveCapabilities=
PermittedCapabilities=
InheritableCapabilities=
NoNewPrivileges=true
```

Setuid/setgid bits jsou v release zakázané a systemd používá `RestrictSUIDSGID=yes`. Handler nemůže zvýšit privilege přes file capability, user namespace, keyring, ptrace, namespace nebo inherited FD.

Kanonický generated Node.js 24 seccomp profil:

- dovoluje pouze syscalls potřebné pro runtime, memory, timers, signals, futex/threading, read-only release files, private tmpfs files a existující capability FD,
- povoluje `clone/clone3` pouze pro thread flags potřebné runtime; vytvoření nového process tree probíhá přes `context.runtime`,
- zakazuje `socket`, `socketpair`, `connect`, `bind`, `listen`, `accept`, `accept4` generated code,
- zakazuje `mount`, `umount2`, `pivot_root`, `move_mount`, `open_tree`, `fsopen`, `fsmount`, `fspick`, `setns`, `unshare`,
- zakazuje `ptrace`, `process_vm_readv`, `process_vm_writev`, `pidfd_getfd`, `kcmp`,
- zakazuje `bpf`, `perf_event_open`, `userfaultfd`, `fanotify_init`, `open_by_handle_at`, `name_to_handle_at`,
- zakazuje `keyctl`, `add_key`, `request_key`, module, reboot, kexec, swap, raw I/O a device administration,
- zakazuje `io_uring_setup`, `io_uring_enter` a `io_uring_register`; environment fixuje `UV_USE_IO_URING=0`,
- omezuje `prctl` na explicitní runtime allowlist,
- používá `SystemCallArchitectures=native`.

V8 runtime profil má explicitně JIT-compatible executable-memory pravidlo a nepoužívá globální `MemoryDenyWriteExecute`, které by znemožnilo schválený Node runtime. Exekutovatelný file-backed obsah je omezen immutable release mountem a runtime profilem. Jiný runtime používá vlastní immutable seccomp/MDWE profile digest a bez blocking compatibility testu se neaktivuje.

Seccomp profile digest je součástí release a runtime instance. Neznámý syscall requirement vede do validation failure, nikoli do production-wide vypnutí seccompu.

### 50.16 Environment a file descriptor inheritance

Handler environment vzniká z prázdného prostředí. Allowlist obsahuje pouze exact runtime values, například:

```text
LANG=C.UTF-8
LC_ALL=C.UTF-8
TZ=UTC
NODE_ENV=production
HOME=/work/home
TMPDIR=/tmp
PATH=/runtime/bin
UV_USE_IO_URING=0
KCML_CONTEXT_FD=3
```

Environment neobsahuje secret, `KCML_OWNER_API_KEY`, DB URL/password, broker path, runtime gateway path, systemd credential path, GitHub credential, external API credential ani caller-supplied `NODE_OPTIONS`, `LD_PRELOAD`, `LD_LIBRARY_PATH`, `PYTHONPATH`, `RUSTFLAGS` nebo obdobnou injection proměnnou.

Při exec jsou otevřené pouze:

- FD 0 — `/dev/null` nebo exact bounded input pipe,
- FD 1 — runtime-host log pipe,
- FD 2 — runtime-host error log pipe,
- FD 3 — capability channel.

Všechny ostatní FD se zavřou `close_range` a allowlisted FD se ověří `fstat`. Capability FD je pro první trusted bootstrap exec bez CLOEXEC a bootstrap jej nastaví CLOEXEC před importem generated code. Generated child process jej proto nezdědí. Stdout/stderr consumer uplatňuje byte/rate limity, nikdy nepředává journal socket a při truncation uloží explicitní evidence místo deadlocku.

Runtime host, gateway a platform services používají `O_CLOEXEC`, `SOCK_CLOEXEC`, `accept4(...SOCK_CLOEXEC)` a `close_range` při child launchi. Inherited listening, DB, broker, browser, deployment nebo systemd notification FD je blocking test failure.

### 50.17 Child processes, threads a orphan cleanup

Generated code nesmí přímo spustit shell, daemon ani arbitrary executable. Revision může obsahovat system-managed `runtimeSpawnProfile` s exact aliasem, release-relative executable, content digestem, argument a environment schema, maximum instances, timeoutem a resource podprofilem.

Handler požádá přes `context.runtime` pouze o `spawnAlias` a validované argumenty. Runtime host:

1. ověří alias proti current immutable profile,
2. otevře executable přes release directory FD a ověří digest,
3. spustí jej bez shellu pomocí `execveat` nebo ekvivalentního fd-based exec,
4. ponechá jej ve stejných user/mount/network/IPC/UTS/PID/cgroup boundaries,
5. předá jen explicitní stdin/stdout/stderr pipes; capability FD a platform FD nepředá,
6. registruje child pidfd, deadline, output limity a correlation,
7. po parent exit/cancel/timeoutu child ukončí.

Trusted sandbox init je child subreaper, forwarduje signály a reapuje všechny descendants. Handler ani child nemůže double-forkem, `setsid`, parent death nebo daemonizací uniknout z unit cgroup. `KillMode=control-group`, `SendSIGKILL=yes` a cgroup emptiness check ukončí parent, children i orphan descendants.

Worker threads jsou povoleny v exact runtime profile a počítají se do `TasksMax`. Nový OS process mimo runtime spawn broker je seccomp porušení.

### 50.18 Cgroups a resource limity

Každý `kcml-runtime-host@<runtime-instance-id>` má vlastní systemd cgroup. Effective resource profile vždy obsahuje konečné hodnoty:

- `CPUWeight` a `CPUQuota`,
- `MemoryHigh`, `MemoryMax` a `MemorySwapMax=0`,
- `TasksMax`,
- `LimitNOFILE`,
- `LimitFSIZE`,
- `LimitCORE=0`,
- tmpfs byte a inode limity,
- per-call request/response/stream limity,
- concurrency a queue admission limit,
- `TimeoutStartSec`, `TimeoutStopSec`, `RuntimeMaxSec` podle lifecycle,
- I/O weight a storage limit pro povolené persistentní host paths.

Resource profile je systémem odvozený, immutable pro runtime generation a viditelný v UI/auditu. Hodnota `infinity`, `0 = unlimited` nebo chybějící production limit je validation failure.

Memory pressure nejprve překročí `MemoryHigh`; `MemoryMax` nebo OOM kill vytvoří `RUNTIME_RESOURCE_EXHAUSTED`, uzavře channel a spustí outcome reconciliation. PID limit odmítne nový child před startem. Temporary storage limit vrátí typed error a nezapisuje mimo tmpfs.

### 50.19 `KCML-RUNTIME-IPC/1`

Anonymous handler channel používá connected `AF_UNIX SOCK_STREAM` bez filesystem path. Každý frame má 16-byte network-order header:

```text
magic[4] = "KCR1"
version  = 1
frameType: uint8
flags: uint16
payloadLength: uint32
sequence: uint32
```

Payload je UTF-8 canonical JSON pro control/request/result/error frames nebo raw bounded bytes pro stream chunk. `payloadLength` nesmí překročit `1 MiB`; větší data používají stream nebo artifact handle. Globální unary request/response hard ceiling je `16 MiB` a exact operation contract jej může pouze snížit. Stream chunk je nejvýše `64 KiB`.

Kanonické frame types jsou:

```text
HELLO
READY
REQUEST
RESPONSE
ERROR
STREAM_OPEN
STREAM_CHUNK
STREAM_CREDIT
STREAM_END
CANCEL
SHUTDOWN
HEARTBEAT
```

Každý request payload obsahuje pouze:

```json
{
  "requestId": "uuid",
  "operation": "secret|callComponent|callExternal|state|logger|runtime",
  "capabilityAlias": "immutable-local-alias-or-null",
  "deadlineAt": "RFC3339",
  "cancellationVersion": 3,
  "correlationId": "uuid",
  "payload": {}
}
```

Neobsahuje serverovou identity/binding envelope. Runtime host ji vloží ze current execution contextu. Sequence je strictly increasing per direction; duplicate, gap mimo negotiated stream rule, wraparound nebo request ID reuse s jiným digestem uzavře channel.

Handler nesmí posílat ancillary FD. Runtime host používá `recvmsg`, odmítne každé `SCM_RIGHTS`, unexpected credentials nebo control message a uzavře channel jako `RUNTIME_DESCRIPTOR_INJECTION`. Platformní FD se handleru nikdy nepředává jako response.

### 50.20 Handler argument, output a protocol validation

Před `REQUEST` do handleru runtime host:

- ověří current execution context a admission snapshot,
- validuje exact input schema a canonical argument digest,
- odstraní server-reserved metadata,
- ověří request size, depth, array/string/object limity,
- pinne deadline a cancellation version,
- uloží dispatch evidence.

Handler `RESPONSE`:

- musí odkazovat na právě jeden in-flight request,
- musí mít monotonic sequence a known frame type,
- nesmí obsahovat unknown top-level properties,
- musí splnit output/error schema a size limit,
- nesmí obsahovat descriptor nebo stream bez předchozího `STREAM_OPEN`,
- je uložen jako raw attempt outcome před doménovým vyhodnocením.

Schema-neplatný success se nikdy nepersistuje jako success. Process exit, EOF, truncated frame nebo protocol violation před známým side-effect outcome vede do `RECONCILING` podle operation class.

Maximum in-flight requestů, active streamů, buffered bytes a log frames je součástí resource profile. Výchozí platformní ceiling je 32 in-flight requestů, 16 streamů a 1 MiB unacknowledged stream window na connection; contract může hodnotu snížit.

### 50.21 Runtime gateway a exact contract binding

Runtime gateway při každém call admission:

1. ověří live `runtime_ipc_connection` a service generation,
2. načte execution context a immutable context digest,
3. ověří current platform incarnation a application deployment epoch,
4. ověří current activation head a request-pinned activation epoch,
5. ověří source component/revision/release/runtime digest/runtime generation,
6. mapuje handler local capability alias na exact binding; handler target metadata nepřijímá,
7. ověří binding lifecycle, source/target revision, operation, contract digest, binding revision a binding-set revision,
8. ověří target lifecycle, activation, readiness a route,
9. ověří deadline, cancellation, idempotency, state version, concurrency a fence,
10. vytvoří child context nebo broker operation a teprve poté dispatchuje.

Cache je pouze performance hint. Před dispatchi se její `activationHeadEpoch`, binding-set revision a deployment epoch porovnají s authoritative head; stale cache request nespustí. Jeden call je pinned k jednomu activation epoch a nemůže kombinovat members z více setů.

Handler může vybrat pouze alias deklarovaný ve své immutable revision. Alias mapuje na jeden exact target operation a digest. Raw component ID, hostname, UDS path, route, revision, digest nebo binding ID v handler payloadu se odmítá.

### 50.22 State broker boundary

`context.state` request obsahuje pouze operation, key/stream, typed payload, expected version a client event ID. Runtime host doplní component state namespace, source revision, release, runtime instance/generation, execution context, binding-set revision a activation epoch.

State broker:

- přijímá pouze runtime gateway identity přes vlastní UDS,
- nemá socket namountovaný do handleru,
- používá PostgreSQL service credential nedostupný runtime hostu a handleru,
- ověří exact namespace/schema revision a state operation contract,
- provede CAS/transaction/append podle 49.22,
- zapíše event, audit a result v jedné transakci.

Pokus zvolit jiný namespace, SQL, table, schema, connection string nebo raw query není součástí IPC schema a končí `RUNTIME_STATE_BOUNDARY_VIOLATION`.

### 50.23 Secret Broker boundary

Secret resolution probíhá pouze:

```text
handler context.secret(alias)
  → runtime host
  → runtime gateway
  → kcml-secret-broker UDS
  → exact secret_resolution
  → stejný handler call
```

Runtime gateway odvodí z aliasu exact secret binding a předá brokeru serverový execution context, authority-lineage/OWNER-intent/operation-context digests, exact `secret_use_context`, logical operation, purpose, target context, source/target revisions, binding ID/revision/digest, binding-set revision, activation epoch a correlation. Broker ověří:

- source execution context je current a immutable,
- Secret binding je exact, active a neexpired,
- requested alias odpovídá stable name/purpose z revision,
- Secret-use context odpovídá current operation contextu, declared purpose, consumer step, exact target/origin/account a placement path,
- source/target revisions a activation set jsou current,
- selected secret version a `secretActivationEpoch` jsou platné,
- stejné logical side-effect operation nepřechází na jinou secret version.

Hodnota se vrátí pouze jako response konkrétního pending capability requestu. Runtime ji nevkládá do environmentu, argv, launch manifestu, filesystemu ani globálního cross-operation cache. Handler ji po vydání používá uvnitř závazného trusted secret režimu; tato kapitola nezavádí maskování, zákaz logování, nový Secret scope ani OWNER approval. Secret-use context pouze zabraňuje tomu, aby untrusted content změnil recipienta nebo placement již povoleného použití.

Broker socket, master key, ciphertext database credential a encryption key jsou handleru i runtime hostu nedostupné. Každé resolve má immutable evidence a audit i při deny, timeoutu nebo crashi.

### 50.24 Egress Gateway boundary

Generated handler nemá AF_INET/AF_INET6 socket a každé external spojení vzniká přes `context.callExternal` nebo bounded `TCP_TLS` stream v egress gateway. Handler vybírá pouze immutable `targetAlias`, relative path/operation a typed business payload.

Egress gateway před connectem:

1. mapuje alias na exact active external target binding,
2. ověří allowed scheme, hostname, port, method, path prefix, request schema a size,
3. canonicalizuje URL a zakáže userinfo, fragment, alternate numeric IP form a ambiguous host,
4. provede DNS A/AAAA resolution,
5. validuje každou resolved IPv4/IPv6/mapped adresu proti target policy,
6. pinne vybranou IP pro konkrétní connection a zachová exact TLS SNI/HTTP Host,
7. ověří certifikát a servername,
8. přidá exact endpoint-local credential ze Secret Brokeru; handler jej nemůže přepsat,
9. uplatní rate, circuit, timeout, byte, retry a cancellation policy,
10. uloží dispatch/outcome/reconciliation evidence.

Každý redirect se znovu canonicalizuje, DNS-resolvuje a validuje jako nový destination. Credential se na nový origin nepřenese, pokud exact binding výslovně neobsahuje tento redirect origin. Redirect count je bounded.

Loopback, private, link-local, multicast, reserved, metadata, Unix socket, `file:`, `data:`, `ftp:` a jiné necontract schemes jsou zakázané, pokud exact external target nebo OWNER Device Bridge local-target binding výslovně neobsahuje konkrétní destination. DNS rebinding se řeší revalidací všech odpovědí a connection pinningem.

`TCP_TLS` poskytuje typed bounded stream frames, nikoli network FD. Handler nikdy nezíská socket descriptor, DNS resolver nebo obecný proxy tunnel.

### 50.25 Deadline, cancellation, streaming a cleanup

Deadline je serverem určený monotonic/realtime pair a pouze se zkracuje přes child calls. Handler nemůže deadline prodloužit. Runtime host nastaví timer a cancellation version; gateway a každý broker dostane zbývající budget.

Cancellation:

1. commitne cancellation intent/version v parent state machine,
2. zastaví admission nových child calls,
3. odešle `CANCEL` přes capability channel,
4. zruší pending broker/network/browser operation, pokud je bezpečně abortovatelná,
5. mutující dispatch přejde do reconciliation,
6. po grace uzavře channel a ukončí process tree,
7. terminalizuje pouze podle známých outcomes.

Streaming používá credit-based backpressure. Producer nesmí mít více unacknowledged bytes než current credit window. Disconnect, cancellation nebo terminal result okamžitě zavře stream state, file descriptors a buffers. Slow consumer nemůže blokovat cancellation, heartbeat nebo shutdown; control frames mají rezervovanou queue a prioritu.

EOF není success. Final response se emituje pouze z persistovaného canonical outcome. Descriptor, socket, HTTP body, stream buffer a temporary file se uzavírá v `finally`/RAII a současně eviduje persistentní cleanup, pokud přežil process crash.

### 50.26 Browser worker a browser host boundary

Browser worker neotevírá raw Playwright websocket ani CDP port. Komunikuje s `kcml-browser-host@<slot>` pouze přes versioned typed browser-host control protocol na systemd-owned UDS.

Browser host:

- běží pod `kcml-browser-host`, worker pod `kcml-browser-worker`,
- ověřuje peer UID/GID/PID, worker unit, `InvocationID`, boot ID, start ticks a cgroup,
- má immutable runtime build manifest a monotonic host generation,
- drží Playwright/private browser client a browser process uvnitř vlastní service boundary,
- spouští Chromium přes anonymous remote-debugging pipe a Firefox/WebKit přes exact private inherited transport, nikoli veřejný TCP port,
- vytváří běžné server sessions jako non-persistent BrowserContexty,
- používá persistent profile pouze v explicitním bridge/compatibility profile-lease contractu,
- nemá Secret Manager DB credential; secret/certificate value dostane pouze v exact context/action requestu,
- nepředává raw protocol command, endpoint, descriptor, runtime object ID nebo filesystem path workeru, Preview Gateway, OWNER clientu, generated handleru ani bridge gateway.

Typed host command obsahuje nejméně:

```text
protocol version
platform/application incarnation
worker service generation
runtime build manifest digest
host slot/generation
session ID
context ID/generation
page ID/generation
frame ID/attachment epoch
document ID/epoch
navigation sequence
control epoch/fence
action ID/attempt/fence
action schema + input digest
operation-scope/target/locator digests
mutation trigger
pre-armed waiter plan
deadline/cancellation version
```

Host doplní pouze vlastní current runtime handles. Command nesmí obsahovat caller-selected raw Page/Frame/ElementHandle/CDP session. Host před methodou ověří, že runtime handle stále odpovídá exact KájovoCML identity. Mismatch je stale failure bez inputu.

Host odpověď a async event obsahuje stejnou identity n-tici, monotonic per-action phase sequence a evidence digest. Povolené action phases jsou typed a zahrnují command accepted, target resolved, actionability passed, input started, mutation possible, method returned a post-observation captured. Host nesmí hlásit `NOT_TRIGGERED`, pokud adapter neumí prokázat, že žádný relevantní input/event nebyl vydán.

Browser host nepřijímá business success/failed rozhodnutí. Vrací pouze typed browser evidence. Browser worker/BrowserSessionService provede postcondition, reconciliation a guarded DB transition.

Host drží per-context page/frame/document registry mirror pouze pro transport. Autoritativní registry jsou v PostgreSQL. Frame detach/reattach, OOPIF replacement, full navigation, BFCache restore, prerender activation, renderer crash a page close emituje typed identity event; stale handle se odstraní.

Action listener pro popup/page/navigation/dialog/filechooser/download se uvnitř host commandu pre-armuje před triggerem. Pokud listener setup selže, mutující input se neodešle. Event po action fence změně je raw stale evidence.

Host restart vytvoří vyšší host generation. Old accepted connection/response se neautorituje. Worker recovery porovná DB sessions s live host context inventory, provede fresh observation a pending mutation reconciliation. Context se nepřenáší mezi odlišnými runtime build manifests raw handlem.

`CHROMIUM_CDP_COMPATIBILITY` může uvnitř exact host/bridge adapteru použít konkrétní endpoint, ale endpoint je network/mount namespacem omezen na adapter, není publikován, není vrácen klientovi a není obecný tunnel. Compatibility adapter musí mapovat raw events na stejnou typed identity/phase/recovery sémantiku nebo danou capability označit unsupported.

### 50.27 Preview Gateway boundary

Preview Gateway v `kcml-web-api` má pouze KBPP event/frame transport a BrowserSessionService operations. Není členem browser-host socket group, nemá browser-host socket namountovaný jako accessible path a nemá raw Playwright/CDP endpoint, descriptor, profile path ani bridge native channel.

OWNER klient dostává pouze:

- frame/snapshot/patch,
- typed page/frame/document/navigation/observation metadata,
- typed control/input/action/challenge/dialog/permission events,
- artifact handles,
- current session/control/reconciliation/cleanup state.

Každý KBPP handshake ověří OWNER session nebo jediný OWNER API key, Origin, exact session binding, one-time ticket, expiry, audience a capabilities. Ticket není bridge/browser-host credential a nelze jej použít na jiné API/session. Bearer credential ani ticket se nevkládá do URL nebo access logu.

Každý input ověří stream epoch, current control lease/epoch/fence, context/page/frame/document identity, observation/frame revision, viewport transform, focus descriptor podle typu, sequence, size a rate před enqueue browser action. Gateway sama neposílá Playwright input a nemůže změnit target/action schema.

Reconnect vytvoří nový stream epoch a current snapshot. Klient musí snapshot acknowledge před inputem. Gateway zahodí neacknowledged client input z předchozího streamu; nepřevádí jej na retry. Pointer/key/touch/composition state je client-side reset a server control transfer contract rozhoduje, zda pending page effect vyžaduje reconciliation.

Frame backpressure může zahodit pouze nahraditelné non-key image frames. State, control, action phase, challenge, reconciliation, cancellation a cleanup events používají durable event stream/API a nejsou závislé na doručení každého preview frame.

Browser-host raw transport exposure test otevírá všechny public interfaces, API routes, WSS protocols, process listening sockets, UDS mount inventory a `/proc/net` a musí prokázat nulový Playwright/CDP/bridge-native endpoint dostupný OWNER klientovi nebo veřejné síti.

### 50.28 OWNER Device Bridge boundary

Bridge enrollment probíhá:

1. OWNER_FULL operation vytvoří single-use bounded enrollment challenge pro exact bridge ID,
2. bridge lokálně vygeneruje private key a uloží jej v OS secure/protected store,
3. gateway ověří challenge a vydá mTLS certificate svázaný s bridge ID, public key, app build, OS/architecture, capability fingerprint a certificate generation,
4. bridge naváže pouze outbound mTLS/WSS na exact bridge transport route,
5. server uloží connection epoch, peer certificate fingerprint, negotiated protocol/build/capabilities a heartbeat.

Certifikát není OWNER API credential a gateway jej odmítne na všech non-bridge routes. Private key, platform passkey a non-exportable client-certificate key server nikdy neobdrží.

Capability snapshot obsahuje nejméně bridge build, OS/architecture, browser builds, profile inventory, native input/dialog capability, platform authenticator/passkey, client-certificate store, protected storage, local target a artifact support. Windows/macOS/Linux capability se neodvozuje z názvu platformy. Required mismatch zablokuje assignment před dispatchi.

Bridge nemůže vybrat libovolnou session. Server mu přiřadí exact:

```text
bridge ID/certificate generation/connection epoch
session ID/context generation
operation scope/local target
account binding/auth epoch
browser build/profile ID/profile lease
control epoch/action fence
page/frame/document identity
action schema/target/mutation trigger/deadline
```

Bridge protocol obsahuje pouze typed browser/session operations, page/frame/document/observation events, exact challenge/dialog/permission, bounded upload/download artifact handles a heartbeat. Neobsahuje shell, command line, arbitrary filesystem path, arbitrary TCP/UDP/Unix proxy, LAN scan, raw CDP method, raw Playwright message ani obecný browser websocket tunnel.

Local target binding uvádí exact hostname/IP/CIDR, port, protocol, origin, purpose a expiry. Každá navigation a redirect se ověřuje vůči němu. Binding neumožňuje obecný LAN proxy nebo discovery.

Local browser profile používá persistentní `browser_profile_lease`. Bridge před attach ověří profile directory identity, browser build a current owning process. Profil s jiným writerem se nepřebírá potichu. Explicitní OWNER takeover drainuje/uzavírá previous writer podle browser/profile capability a vydá nový fence; neprokazatelný process/profile stav je blocker.

Bridge command vrací monotonic dispatch phases stejně jako host. Po possible mutation triggeru disconnect, app crash, OS sleep, network partition nebo certificate rotation vyžaduje server-side/provider read-back; server nesmí stejný step naslepo spustit v novém bridge connection nebo server-managed contextu.

Revocation nebo certificate rotation inkrementuje bridge certificate generation, uzavře current connections, zneplatní assignments/profile leases a další frame/command ze stale connection epoch odmítne. Version/capability mismatch, certificate expiry/revocation nebo heartbeat expiry zablokuje nový action před bridge dispatchi.

Bridge update je blue/green podle build/capability fingerprintu. Pending session zůstane na exact compatible build nebo se pause/recover; nelze ji tiše připojit k jiné semantic implementation.

Cleanup revokuje assignment, uzavře input/session stream, dokončí/reconciliuje pending effects, uvolní profile lease, odstraní temporary artifacts/ephemeral handles a ověří, že stale connection již nemůže emitovat authoritative event.

### 50.29 Activation switch, release a stale proces

Runtime instance je použitelná pouze při exact shodě:

```text
active component revision
active release
runtime digest
binding-set revision
activation epoch
application deployment epoch
platform incarnation
runtime generation
systemd service generation
```

Activation barrier před pointer switchi zastaví nové mutující admissions a drain/reconciliuje in-flight mutations podle 49.17. Pointer-switch transaction přepne revision/release/binding/route/runtime target jako jeden set a alokuje vyšší activation epoch.

Po commitu:

- gateway odmítne každý nový request ze staré epoch, release, runtime digest, binding set nebo runtime generation,
- old runtime connection dostane `DRAIN` a nemůže založit nový execution context,
- read-only in-flight call může dokončit pouze podle pinned barrier policy,
- mutující in-flight call může zapsat pouze pokud jeho operation byla před barrierem přijata a current fence/outcome contract to dovoluje,
- old heartbeat, ready report, state report, ACK, result, checkpoint nebo binding observation je stale evidence,
- runtime controller po drain timeoutu ukončí starou unit cgroup.

Reverse switch používá vyšší epoch a stejné pravidlo. First CREATE rollback na `ABSENT` nejprve atomicky zneplatní route/binding/runtime pointers a teprve potom zastaví candidate process. Živý starý proces po ztrátě DB autority nemůže reaktivovat sám sebe.

### 50.30 Systemd credential lifecycle a deployment

Secret-bearing host credentials jsou service-specific a materializují se pouze systemd credential mechanismem. Source je root-owned mode `0600`, persistentní forma je encrypted, service dostane read-only per-invocation materialization v systemd credential directory.

Credential rotation operation:

1. vytvoří novou immutable credential version/fingerprint,
2. atomicky nahradí root-owned source nebo encrypted credential blob,
3. inkrementuje desired credential generation,
4. restartuje pouze exact affected service units,
5. ověří nový `InvocationID`, effective credential fingerprint/generation, readiness a dependent smoke,
6. uzavře accepted connections staré service generation,
7. označí previous generation retired až po effective confirmation.

Generated handler credential directory nevidí. Runtime launch manifest credential neobsahuje bearer secret a rotuje s runtime generation/release reconciliation. Master encryption key, DB credential, TLS private key a bridge CA zůstávají pouze u exact platform service.

Application deployment switch mění `applicationDeploymentEpoch`. Proces se starým epoch nemůže po restart/symlink race provést authoritative write. Gateway readiness je false při mixed DB epoch, filesystem `current`, process release, systemd invocation nebo heartbeat evidence.

### 50.31 Runtime cleanup a boot recovery

Každý runtime instance/generation má persistentní `runtime_cleanup_operation` s inventory:

- systemd unit a cgroup,
- main/handler/child pidfd identities,
- runtime gateway connection IDs,
- anonymous handler channel,
- active request/stream IDs,
- namespace/process-tree evidence,
- temporary directories, tmpfs a artifacts,
- materialized service credentials,
- socket/path evidence,
- outstanding side-effect/reconciliation operations,
- lease/concurrency/capacity claims.

Cleanup pořadí:

1. stop new admission a mark runtime draining,
2. persist cancellation/drain intent,
3. reconcile pending side effects,
4. close handler capability channel a broker streams,
5. SIGTERM process tree a bounded grace,
6. SIGKILL unit cgroup a wait until `populated=0`,
7. close pidfds, accepted sockets a buffers,
8. unmount/remove private tmpfs a runtime-generation paths,
9. release concurrency/capacity/leases pod current fence,
10. verify no current pointer, route, binding nebo execution context references generation,
11. store final evidence a mark `COMPLETE`.

Boot recovery:

- systemd socket units and root tmpfiles restore canonical directories/paths,
- PostgreSQL runtime instances v non-terminal state se reconciliují proti actual units, invocation IDs, cgroups a PIDs,
- unknown process bez matching runtime instance se zastaví a eviduje jako orphan,
- stale DB process identity bez live unit se označí exited a pending calls se reconciliují,
- stale socket path se neotevře ani slepě nepřepíše,
- credential materialization z předchozího invocation se nepoužije,
- nový dispatch začne až po runtime inventory convergence.

### 50.32 Failure states a stable error codes

Runtime boundary používá nejméně:

```text
RUNTIME_CONTEXT_SPOOF_ATTEMPT
RUNTIME_CONTEXT_NOT_CURRENT
RUNTIME_PEER_IDENTITY_REJECTED
RUNTIME_PID_REUSE_DETECTED
RUNTIME_SERVICE_GENERATION_STALE
RUNTIME_PROCESS_STALE
RUNTIME_LAUNCH_MANIFEST_MISMATCH
RUNTIME_RELEASE_STALE
RUNTIME_DIGEST_MISMATCH
RUNTIME_BINDING_STALE
RUNTIME_ACTIVATION_STALE
RUNTIME_SOCKET_INTEGRITY_FAILED
RUNTIME_SOCKET_STALE
RUNTIME_CHANNEL_CLOSED
RUNTIME_PROTOCOL_VIOLATION
RUNTIME_DESCRIPTOR_INJECTION
RUNTIME_PAYLOAD_TOO_LARGE
RUNTIME_STREAM_BACKPRESSURE
RUNTIME_DEADLINE_EXCEEDED
RUNTIME_CANCELLED
RUNTIME_SANDBOX_SETUP_FAILED
RUNTIME_SECCOMP_VIOLATION
RUNTIME_FILESYSTEM_BOUNDARY_VIOLATION
RUNTIME_NETWORK_BOUNDARY_VIOLATION
RUNTIME_STATE_BOUNDARY_VIOLATION
RUNTIME_SECRET_BINDING_REJECTED
RUNTIME_EGRESS_BINDING_REJECTED
RUNTIME_RESOURCE_EXHAUSTED
RUNTIME_CHILD_SPAWN_REJECTED
RUNTIME_CHILD_CLEANUP_INCOMPLETE
RUNTIME_CREDENTIAL_GENERATION_STALE
RUNTIME_OUTCOME_UNKNOWN
BROWSER_HOST_IDENTITY_REJECTED
BROWSER_HOST_GENERATION_STALE
BROWSER_RAW_TRANSPORT_EXPOSURE
BRIDGE_SESSION_SCOPE_REJECTED
BROWSER_BRIDGE_CONNECTION_STALE
```

Failure semantics:

- identity, socket, manifest, binding, schema nebo sandbox failure před dispatchi končí known `FAILED` bez handler side effectu,
- disconnect/timeout před persistovaným side-effect intentem je known not-dispatched,
- disconnect/timeout po intentu, dispatchi nebo možné mutaci přejde `RECONCILING`; nejasný výsledek je `MANUAL_REVIEW`,
- stale process/revision/binding/epoch write se odmítne a nemění canonical outcome,
- seccomp kill, OOM, SIGKILL nebo browser/bridge crash používá stejný crash matrix podle posledního persistovaného checkpointu,
- cleanup failure neznovuaktivuje runtime; zůstává `CLEANUP_INCOMPLETE` s retry/alarmem,
- audit/log emission failure, který by porušil transactional audit invariant, zablokuje authoritative commit.

### 50.33 Audit, correlation a produkční evidence

Každá runtime connection evidence obsahuje:

- socket path, device/inode, owner/mode a socket unit,
- acceptedAt/closedAt a close reason,
- peer UID/GID/PID, boot ID a start ticks,
- systemd unit, `InvocationID`, MainPID a cgroup,
- runtime instance/generation, component/revision/release/runtime digest,
- binding-set revision, activation epoch, deployment epoch a platform incarnation,
- launch/resource/namespace/seccomp/environment/FD profile digests,
- first/last message sequence a protocol violations.

Každý capability call evidence obsahuje parent/child execution context, request/correlation/causation IDs, operation a local alias, server-resolved target/binding/secret/external/state metadata, input/output digest, bytes, stream credits, deadline, cancellation version, attempt, outcome, error, cleanup a audit event.

Audit se propisuje přes handler → runtime host → gateway → broker/target a zpět. Timeout, cancellation, disconnect a process crash musí mít uzavřený correlation graph s posledním known pointem, pending effectem a canonical next action. Chybějící hop je blocking acceptance failure.

OWNER může v runtime detailu zobrazit plnou evidence, systemd identity, socket integrity, sandbox profile, process tree, resource usage, calls, rejects, stale observations a cleanup. To nemění možnost OWNERa zobrazit secrets a payloady podle trusted perimeteru.

### 50.34 Closure jednotlivých hranic

#### 50.34.1 Veřejná OWNER gateway

1. **Autorita:** `kcml-web-api` po platné OWNER session nebo jediném `KCML_OWNER_API_KEY`.
2. **Identita:** fixed OWNER `KRMAR78`, auth channel, credential version/fingerprint a serverový request record.
3. **Vazba:** `OWNER_FULL`, exact target revision/release/operation a activation epoch.
4. **Transport:** TLS HTTPS nebo same-origin WSS; interně in-process/runtime gateway.
5. **Izolace:** bearer končí na gateway a není forwardován handleru.
6. **Lifecycle:** authenticate → context commit → dispatch → terminal outcome; key rotation podle 49.22.1.
7. **Souběh:** idempotency, CAS, activation barrier a deployment epoch.
8. **Selhání:** auth reject před contextem; po dispatchi reconciliation podle operation class.
9. **Důkaz:** public E2E, header stripping, key rotation race, no-key-in-runtime inventory a audit correlation.

#### 50.34.2 Endpoint-local EXTERNAL gateway

1. **Autorita:** public gateway a exact external verifier.
2. **Identita:** credential binding/version a exact endpoint, nikoli obecný external principal.
3. **Vazba:** exact `EXTERNAL` binding, route/method/tool a target revision.
4. **Transport:** public TLS endpoint → internal gateway.
5. **Izolace:** credential nelze použít na jinou route ani získat OWNER_FULL/CONTRACT.
6. **Lifecycle:** verify → context → dispatch → terminal/reconcile → binding retire.
7. **Souběh:** endpoint idempotency/rate/concurrency a activation epoch.
8. **Selhání:** invalid/stale binding reject před handlerem.
9. **Důkaz:** cross-route replay deny, retired credential deny a exact audit.

#### 50.34.3 Trusted runtime host → runtime gateway

1. **Autorita:** current `kcml-runtime-host@<runtime-instance-id>` service generation.
2. **Identita:** SO_PEERCRED + boot/start/systemd InvocationID/cgroup + runtime instance/generation.
3. **Vazba:** exact current execution context, release/runtime digest, binding set a epoch.
4. **Transport:** systemd-owned filesystem UDS.
5. **Izolace:** socket group neobsahuje handler; service has no bearer credential.
6. **Lifecycle:** socket activation, peer handshake, connection record, drain/close/reconnect.
7. **Souběh:** connection/message sequence, pidfd exit, activation/deployment fencing.
8. **Selhání:** identity or generation mismatch closes connection before call.
9. **Důkaz:** peer spoof, UID/GID deny, PID reuse, restart race, socket substitution a stale process tests.

#### 50.34.4 Generated handler → runtime host

1. **Autorita:** trusted runtime host alone maps capability request to server context.
2. **Identita:** pidfd-tracked child, runtime generation, anonymous channel and monotonic sequence.
3. **Vazba:** local alias from immutable handler revision; server resolves exact binding.
4. **Transport:** anonymous AF_UNIX socketpair FD 3 with `KCML-RUNTIME-IPC/1`.
5. **Izolace:** namespaces, no network/host `/run`, no extra FD, no capabilities.
6. **Lifecycle:** launcher creates channel before exec; close on terminal/restart; no path reuse.
7. **Souběh:** bounded in-flight IDs, sequence, deadline and cancellation version.
8. **Selhání:** malformed/spoof/descriptor frame closes call/channel; possible effect reconciles.
9. **Důkaz:** FD inventory, protocol fuzz, child inheritance, direct socket/network/DB/broker deny tests.

#### 50.34.5 Secret Broker

1. **Autorita:** `kcml-secret-broker` accepts only runtime gateway.
2. **Identita:** current execution context and gateway kernel/service identity.
3. **Vazba:** exact active Secret binding/version/purpose/target and activation epoch.
4. **Transport:** broker UDS; value returns on exact pending capability call.
5. **Izolace:** handler has no DB/master-key/socket; no environment/file materialization.
6. **Lifecycle:** reserve → resolve/reject → consume/expire; rotation invalidates new resolves.
7. **Souběh:** logical operation pins secret version; state-version and secret epoch.
8. **Selhání:** stale/missing binding rejects before value; ambiguous side effect remains pinned/reconciled.
9. **Důkaz:** exact binding positive/negative, rotation race, direct DB/socket deny and access audit tests.

#### 50.34.6 Egress Gateway

1. **Autorita:** `kcml-egress-gateway` only.
2. **Identita:** source execution context and exact external target alias.
3. **Vazba:** active target binding, method/path/schema/credential and digest.
4. **Transport:** egress broker UDS then server-owned TCP/TLS; handler receives typed result/stream.
5. **Izolace:** handler no network FD/DNS; redirects and destinations revalidated.
6. **Lifecycle:** intent → DNS/validation → dispatch → outcome/reconcile → cleanup.
7. **Souběh:** idempotency, rate, circuit, deadline and side-effect fence.
8. **Selhání:** invalid destination deny; uncertain mutation `UNKNOWN`/manual review.
9. **Důkaz:** loopback/private/rebinding/redirect/credential-leak deny and cancellation tests.

#### 50.34.7 State Broker

1. **Autorita:** state broker/domain service.
2. **Identita:** current execution context and server-assigned component namespace.
3. **Vazba:** exact state schema revision and operation contract.
4. **Transport:** gateway → state broker UDS/in-process DB service.
5. **Izolace:** handler no DB credential/socket/query interface.
6. **Lifecycle:** validate → CAS transaction → event/audit/outbox → result.
7. **Souběh:** state/namespace version, append sequence and activation snapshot.
8. **Selhání:** conflict is typed and no partial write; stale runtime write rejected.
9. **Důkaz:** direct DB deny, namespace spoof, CAS race and stale epoch tests.

#### 50.34.8 Browser worker → browser host

1. **Autorita:** current browser worker service, BrowserSessionService action state machine a committed side-effect intent.
2. **Identita:** peer credentials, worker unit/`InvocationID`, runtime build manifest, host generation a full session/context/page/frame/document/control/action fence.
3. **Vazba:** exact action schema, operation scope, target/locator digest, mutation trigger, account/resource concurrency a pre-armed waiter plan.
4. **Transport:** systemd-owned browser-host UDS; browser private protocol pouze uvnitř hostu přes anonymous pipe/private adapter.
5. **Izolace:** no public/raw CDP or Playwright; Preview Gateway, handler a OWNER client nemají socket group/path/descriptor/profile.
6. **Lifecycle:** host build start/readiness → context create/attach → page/frame/document observation → action phases → drain/restart/context cleanup.
7. **Souběh:** one writer, action fence, host/context/page/document generations a stale response reject.
8. **Selhání:** disconnect/crash před possible trigger může retry pouze s exact evidence; po triggeru se pending mutation reconciliuje před novou action.
9. **Důkaz:** raw-port inventory, build mismatch, stale host/context/document, popup/OOPIF, phase crash matrix, cross-session a restart tests.

#### 50.34.9 Preview Gateway

1. **Autorita:** BrowserSessionService a OWNER-authenticated preview route; preview není action authority.
2. **Identita:** OWNER channel, session binding, ticket audience/capabilities, stream epoch a current page/frame/document snapshot.
3. **Vazba:** exact browser session/control/operation scope, viewport transform, target/focus descriptor a input sequence.
4. **Transport:** KBPP/1.0 same-origin WSS.
5. **Izolace:** frames/typed input/events only; no browser-host membership, endpoint, descriptor, profile path nebo raw bridge channel.
6. **Lifecycle:** handshake/ticket consume → snapshot acknowledgment → stream/input → disconnect/resync bez input replay.
7. **Souběh:** control epoch/fence, stream/input sequence, page/document/frame revision a backpressure.
8. **Selhání:** stale input rejected před action; preview disconnect authority nemění a neznámý page effect se nepovažuje za not-applied.
9. **Důkaz:** one-time ticket, Origin, stale document/transform, reconnect input reset, flood/backpressure a raw transport deny tests.

#### 50.34.10 OWNER Device Bridge

1. **Autorita:** bridge gateway, BrowserSessionService a current server-assigned browser session/profile lease.
2. **Identita:** mTLS certificate, bridge/certificate generation, connection epoch, build/capability fingerprint a local browser/profile process identity.
3. **Vazba:** exact session/context, operation scope/local target, account/auth epoch, profile lease, control/action fence a page/frame/document target.
4. **Transport:** outbound mTLS/WSS typed bridge protocol.
5. **Izolace:** browser/native/device operations only; no shell, LAN proxy/scan, arbitrary filesystem path, raw CDP/Playwright tunnel nebo private-key export.
6. **Lifecycle:** enrollment/local key → connect/heartbeat → capability negotiation → session/profile assignment → revoke/rotate/expire/cleanup.
7. **Souběh:** one writer per session/profile, bridge connection epoch, control/action fence a account/resource claims.
8. **Selhání:** revoke/version/offline closes connection; post-trigger pending mutation reconciles a nesmí blind-fallbackovat na jiný host/bridge.
9. **Důkaz:** platform capability, passkey/client-cert, cross-session/profile, post-revoke, raw-command, LAN proxy, file path, stale connection a crash tests.

### 50.35 Povinné negativní testy

Blocking test catalog obsahuje nejméně:

1. Handler `socket(AF_INET/AF_INET6)` a connect na veřejnou, loopback, private a metadata adresu selže; stejná operation přes `context.callExternal` uspěje pouze s exact bindingem.
2. Handler `socket(AF_UNIX)`/connect na runtime gateway, Secret Broker, state broker, PostgreSQL, browser host a jinou component UDS selže i při znalosti path.
3. Handler nemůže číst PostgreSQL přes TCP ani Unix socket a nemá DB credential v environmentu, argv, files nebo FD.
4. Handler nemůže číst Secret Manager ciphertext, master key, broker DB nebo broker socket.
5. Handler payload s source, target, binding, revision, runtime, execution ID nebo access channel je odmítnut před handler/target side effectem.
6. Handler local alias nelze změnit na jiný target, operation, revision nebo contract digest.
7. Runtime se starou source revision, release, runtime digest, binding-set revision, activation epoch, deployment epoch nebo runtime generation nemůže založit nový call ani authoritative write.
8. Neautorizovaný UID/GID a správný UID v nesprávné systemd unit/cgroup nemůže použít runtime socket.
9. PID reuse, systemd restart se stejným numeric PID a stale `InvocationID` jsou odmítnuty.
10. Socket symlink, regular file, FIFO, foreign socket, wrong owner/mode, replaced inode a stale path zablokují readiness; service je nepřepíše.
11. Service restart nezanechá použitelnou accepted connection, stale socket credential nebo old service generation.
12. Handler initial FD inventory neobsahuje DB, gateway, broker, browser, listening, journal, deployment ani credential FD.
13. Child process nezdědí capability FD a nemůže pokračovat v platformních calls po parent/runtime terminal state.
14. Double-fork, setsid, ignored SIGTERM a orphan child jsou po stopu ukončeny; cgroup `populated=0` je blocking assertion.
15. `/proc` neodhalí host processes/env/fd; `/sys` a host `/run` nejsou dostupné; path traversal, magic link, symlink escape, hardlink a device node selžou.
16. Handler nemůže `setns`, `unshare`, mount, ptrace, BPF, keyring, io_uring, pidfd_getfd ani získat capability.
17. Environment injection přes `LD_PRELOAD`, `NODE_OPTIONS`, `PATH`, locale nebo caller env se nepropíše.
18. Ancillary `SCM_RIGHTS` frame, oversize frame, sequence replay/gap, malformed header, unknown opcode a truncated stream se odmítne a audit zůstane úplný.
19. Activation switch odřízne old runtime od nových operations před cleanupem; old result/heartbeat/checkpoint/state write je stale evidence.
20. Rollback na previous/`ABSENT` odřízne candidate runtime před shutdownem a candidate se nemůže znovu připojit jako current.
21. Secret je vydán pouze přes exact active binding konkrétní operation; wrong alias, source, target, purpose, revision, epoch a retired version jsou denied.
22. Secret rotation během in-flight side effectu zachová pinovanou version pro tento effect a nový call dostane current version.
23. Egress zablokuje DNS rebinding, alternate IP notation, IPv4-mapped IPv6, redirect do private/loopback, cross-origin credential forwarding a oversized response.
24. Browser host neposkytuje public ani OWNER-client-accessible Playwright/CDP port, websocket, UDS nebo descriptor; client ani generated handler nemůže připojit raw CDP session, arbitrary script nebo jiný BrowserContext.
25. Preview Gateway process nemůže otevřít browser-host control socket a KBPP ticket nelze použít jako host/bridge credential; stale stream/page/document/control epoch input je odmítnut před action reservation a pressed input se při takeoveru neutralizuje.
26. Device Bridge odmítne non-bridge API route, jinou session/profile, stale certificate/connection/assignment epoch, nevyjednanou capability, raw CDP, shell, arbitrary file path, TCP proxy a LAN scan; client certificate/passkey operace nelze přesměrovat na jiný origin/account.
27. Browser host/worker/bridge stale build/context/page/frame/document/control/action fence nemůže přijmout input, změnit dispatch phase, splnit postcondition, zapsat action outcome ani získat control/profile lease; crash po možném mutation triggeru vyžaduje reconciliation.
28. Audit a correlation obsahují všechny hops i při timeoutu, cancellation, EOF, SIGKILL, OOM, gateway restart, broker restart a handler crash.
29. `KCML_OWNER_API_KEY` není v handler environmentu, process argv, launch manifestu, runtime UDS frame, browser host, broker request ani generated release materialization.
30. Systemd credential directory a source files nejsou viditelné handleru; old materialization po restartu nelze použít.

### 50.36 Failure-injection a crash matrix

Linux/systemd nemesis harness provádí kill, disconnect nebo fault injection nejméně:

- před a po UDS acceptu,
- mezi SO_PEERCRED a systemd identity commit,
- před a po runtime connection recordu,
- před a po execution-context commitu,
- před handler `REQUEST`, po jeho přijetí a před `RESPONSE`,
- před a po Secret resolve, state write a egress dispatch,
- během DNS, TLS handshake, redirect, request body a response stream,
- před/po stream credit, cancellation a terminal frame,
- během sandbox mount setup, capability drop, seccomp install a generated exec,
- při child spawn, parent exit, SIGTERM grace a cgroup SIGKILL,
- před/po systemd credential source switch a service restart,
- před/po activation barrier, pointer switch, runtime effective ACK a reverse switch,
- při browser host attach, context/page/frame/document create/close/navigate/detach, observation capture a preview reconnect,
- před a po action reservation, precondition, intent commit, listener/prearm, každém input/mutation-trigger phase event, postcondition a reconciliation read-back,
- při browser/renderer/worker/host crash, OOPIF process swap, popup/download/dialog/filechooser race a state-bundle capture/restore/pointer switch,
- při bridge enrollment, certificate rotation, connection replacement, capability mismatch, profile lease, passkey/client-certificate challenge, revocation a session close,
- během cleanup každého resource typu.

Po každém faultu test ověří:

- právě jeden canonical logical operation/outcome,
- žádný unauthorized nebo stale write,
- exact DB event/audit/outbox sequence,
- žádný použitelný stale socket/connection/credential,
- cgroup/process/namespace/FD/stream cleanup,
- správný `CONFIRMED_APPLIED`, `CONFIRMED_NOT_APPLIED`, `UNKNOWN` nebo typed failure,
- complete correlation graph,
- možnost deterministic recovery bez duplicate side effectu.

### 50.37 Produkční acceptance a closure

Production acceptance pro Trusted Runtime Boundary vydá immutable evidence bundle obsahující:

- Ubuntu kernel a systemd versions a boundary capability preflight,
- canonical systemd unit/socket files a jejich digests,
- OS users/groups a nulové human membership,
- socket path owner/mode/device/inode inventory,
- running service unit, `InvocationID`, PID/start/cgroup/boot identities,
- runtime instance/release/runtime/binding/epoch snapshots,
- namespace, mount, `/proc`, `/sys`, `/dev`, `/run`, capability, seccomp, environment a FD inspection,
- cgroup effective CPU/memory/swap/PID/FD/storage limits,
- positive canonical capability calls,
- všechny negativní testy 50.35,
- representative failure injection 50.36 v production-shaped harnessu,
- browser raw-transport a bridge-scope evidence,
- activation/rollback stale-process evidence,
- systemd credential rotation/restart evidence,
- audit/correlation a cleanup inventory.

Blocking gate `TRUSTED_RUNTIME_BOUNDARY` je `PASS` pouze při nulovém seznamu:

- unknown peer identity,
- broad writable socket directory,
- handler-accessible internal socket,
- inherited unauthorized FD,
- process se stale revision/release/binding/epoch/generation authority,
- orphan child/cgroup/namespace,
- stale socket nebo credential materialization,
- raw browser transport exposure,
- bridge scope bypass,
- unresolved runtime side-effect outcome,
- incomplete audit correlation,
- pending runtime cleanup.

Vlastnost prokázaná pouze unit mockem, source grepem, konfigurací bez spuštění nebo screenshotem není dostačující. Povinný důkaz je behaviorální Linux/systemd test a produkčně tvarovaná evidence.

### 50.38 Agentic operation-context closure

Každý AI, generation, agent, central-chat nebo adaptive-browser dispatch do runtime gateway obsahuje serverem odvozený `operationContextId` a digest. Generated handler, MCP payload, model output ani external caller je nesmí dodat nebo změnit. Runtime gateway načte exact context, ověří state `DISPATCH_RESERVED|DISPATCHED`, parent fence, authority lineage, intent, target/binding/epoch a action-plan digest a teprve poté vytvoří capability call.

Capability IPC frame nese opaque context reference; business handler neobdrží možnost vybrat lineage, target nebo Secret-use context. `context.callComponent`, `context.callExternal`, `context.secret` a browser adapter používají stejný current operation-context digest. Nested call vytvoří serverový descendant context s inherited semantic ceiling a novým exact target/action planem.

Pozdní nebo replayovaný handler frame s jiným context digestem, po OWNER steer, revision switchi, activation/deployment switchi nebo terminal parentu se odmítne před brokerem. Evidence rozlišuje OS/runtime identity failure od semantic-context failure; ani jeden se neopravuje rozšířením capability nebo novým tokenem.

## 51. PostgreSQL transakční, souběhový a migrační kontrakt

### 51.1 Autorita, rozsah a závaznost

Tato kapitola je fyzickým PostgreSQL 16 kontraktem pro všechny invarianty kapitol 2, 5–30, 32–37 a 40–55, které persistují nebo mění autoritativní stav. Určuje přesné isolation levels, pořadí locků, row-lock modes, advisory-lock namespaces, CAS podmínky, unique/check/foreign-key constraints, frontové claimy, lease a fencing pravidla, atomické pointer switche, side-effect hranice, migrace a povinné souběhové testy.

PostgreSQL je jedinou autoritou mutable business stavu. Žádná aplikační cache, in-memory mutex, Redis, filesystem lock, systemd stav, provider handle, HTTP/SSE spojení ani browserový proces nesmí nahradit zde uvedený databázový guard. In-memory synchronizace smí pouze snižovat contention; correctness musí zůstat zachována po jejím úplném odstranění.

Každá implementovaná mutující operation má v operation catalogu právě jeden `dbTransactionProfile`, který odkazuje na konkrétní profil této kapitoly. Operace bez určeného isolation levelu, lock planu, CAS guardu, idempotency scope, audit/outbox hranice a side-effect rozdělení není uzavřená a její blocking gate nesmí být `PASS`.

Tato kapitola nezavádí nový produktový subsystém, roli, permission vrstvu, credential registry ani control plane. Zavádí pouze fyzické databázové objekty a constraints nezbytné k realizaci již definovaných invariantů.

### 51.2 Databázové role, session stav a transakční profily

Každá trusted systemd služba používá deploymentem spravovanou PostgreSQL login roli s nejmenším potřebným DML/EXECUTE rozsahem; společné NOLOGIN group role oddělují domain write, broker write, read-only/acceptance a migration oprávnění. Tyto infrastrukturové role nejsou aplikační principal, OWNER role ani permission systém. Pouze deployment migrator smí měnit schema; běžné role nesmějí vytvářet extensions/roles/schema, používat superuser nebo `BYPASSRLS` ani převzít jinou roli mimo svůj explicitní grant. Generated handler databázový credential ani síťovou cestu k PostgreSQL nemá.

Connection pool po každém checkoutu i návratu resetuje session-local stav. Aplikační kód nesmí spoléhat na session-level advisory lock, dočasnou tabulku, změněný `search_path`, změněný isolation level, změněný timeout ani jiný stav z předchozího požadavku.

Kanonické online profily jsou:

| Profil | Isolation | `lock_timeout` | `statement_timeout` | `idle_in_transaction_session_timeout` | Použití |
|---|---:|---:|---:|---:|---|
| `ONLINE_MUTATION` | `READ COMMITTED` | 1500 ms | 10 s | 10 s | UI/API/chat/KCIP krátké autoritativní mutace |
| `WORKER_COMMIT` | `READ COMMITTED` | 3000 ms | 15 s | 15 s | claim, checkpoint, outcome, terminalizace, enqueue |
| `ACTIVATION_SWITCH` | `READ COMMITTED` | 5000 ms | 30 s | 30 s | barrier state, atomic switch a reverse switch |
| `CONSISTENT_READ` | `REPEATABLE READ READ ONLY` | 3000 ms | 60 s | 60 s | export, diff, vícequery diagnostický snapshot |
| `CLOSURE_SNAPSHOT` | `SERIALIZABLE READ ONLY DEFERRABLE` | 5000 ms | 120 s | 120 s | closure, restore a cross-table invariant verification |
| `SERIALIZABLE_PREDICATE` | `SERIALIZABLE` | 3000 ms | 15 s | 15 s | pouze výslovně katalogovaná bounded predicate mutace |

Timeouty jsou automatická platformní konfigurace, nikoli OWNER-editable produktové parametry. Konkrétní operace může mít přísnější limit. Vyšší limit vyžaduje operation-catalog evidence, že transakce stále neobsahuje externí I/O a nemůže být rozdělena bez porušení atomického invariantu.

`READ UNCOMMITTED` se nepoužívá. Izolace se nastaví bezprostředně po `BEGIN` a před prvním business dotazem. Aplikační transaction wrapper při dokončení ověří, že transakce není ponechána `idle in transaction`.

### 51.3 Isolation-level rozhodovací matice

`READ COMMITTED` je výchozí pro autoritativní mutace pouze proto, že každý měněný invariant používá explicitně zamknutý guard row, unique/check/foreign-key constraint nebo přesný CAS. Dvě po sobě jdoucí nezamknutá čtení ve stejné `READ COMMITTED` transakci se nesmí považovat za stabilní snapshot.

Použití je závazné:

| Operace | Isolation | Povinná ochrana proti phantomu nebo stale readu |
|---|---|---|
| Mutace jednoho aggregate rootu | `READ COMMITTED` | root `FOR UPDATE`, expected `state_version`, platform/deployment head `FOR SHARE` |
| Atomická mutace více známých rootů | `READ COMMITTED` | všechny rooty zamknuté v kanonickém pořadí; vstupní set je před transakcí kanonicky určen |
| Idempotency claim a replay | `READ COMMITTED` | unique scope+key, následné `FOR UPDATE` recordu |
| Queue/outbox/inbox claim | `READ COMMITTED` | `FOR UPDATE SKIP LOCKED` pouze na explicitně eligible řádcích |
| Lease heartbeat/takeover | `READ COMMITTED` | resource/lease row `FOR UPDATE`, DB time získaný po locku |
| Activation admission/barrier/switch | `READ COMMITTED` | `activation_domain_head` guard rows, frozen membership a pointer snapshot |
| Secret/API-key rotation | `READ COMMITTED` | credential a secret root row locks, unique active version, CAS |
| Browser control takeover | `READ COMMITTED` | browser session root a current lease `FOR UPDATE`, partial unique active lease |
| Audit chain append | isolation parent operation | singleton `audit_head FOR UPDATE` jako poslední lock |
| Konzistentní vícequery read bez autoritativního rozhodnutí | `REPEATABLE READ READ ONLY` | žádná následná mutace z tohoto snapshotu bez nového CAS |
| Closure/restore predicate verification | `SERIALIZABLE READ ONLY DEFERRABLE` | výsledek platí až po úspěšném startu a dokončení transakce |
| Bounded predicate mutace bez materializovatelného guard row | `SERIALIZABLE` | celý transaction body se při `40001` opakuje od začátku |

`SERIALIZABLE` není náhradou za chybějící idempotency, state version, unique constraint, fence ani lock ordering. Operace se smí zařadit do `SERIALIZABLE_PREDICATE` pouze tehdy, když operation catalog obsahuje přesný predicate, maximum čtených/měněných řádků, retry budget a důkaz, že před retry nemohl proběhnout external dispatch.

Phantom-sensitive eligibility se přednostně převádí na zamykatelný guard row nebo explicitní materializovaný stav. Activation eligibility, queue eligibility, cleanup completeness, active binding uniqueness a one-writer lease nesmějí záviset pouze na nezamknutém `NOT EXISTS` dotazu.

### 51.4 Databázový čas, canonical bytes, identifikátory a digests

Lease expiry, deadline, `available_at`, token takeover, challenge expiry, approval expiry a jiné autoritativní časové guardy používají hodnotu z PostgreSQL `clock_timestamp()`. `now()`, `transaction_timestamp()` a aplikační čas se pro expiry rozhodnutí nepoužijí, protože mohou před lock waitem zastarat.

Transakce, která rozhoduje podle času:

1. získá všechny blokující row/advisory locky potřebné pro rozhodnutí,
2. teprve potom načte jednu hodnotu `db_now = clock_timestamp()`,
3. tutéž hodnotu použije pro všechny guardy a timestamps daného přechodu,
4. před external dispatch provede novou krátkou transakci a získá nový `db_now`.

UUID pořadí v lock planu je lexikografické pořadí 16 RFC 4122 network-order bytes získaných PostgreSQL funkcí `uuid_send(id)`, nikoli locale-dependent text nebo display string. SQL bulk lock query používá `ORDER BY aggregate_kind_ordinal, uuid_send(id)`; aplikační pre-sort musí produkovat totožné pořadí a má cross-language test vectors. Canonical key digest a payload digest jsou `bytea` délky 32 s `CHECK (octet_length(...) = 32)`.

Každý digestovaný JSON vstup má uložené canonical UTF-8 bytes nebo canonical JSON text a samostatné `jsonb` pro dotazy. Digest se počítá z canonical bytes, nikoli z nahodilého serializačního pořadí klienta. DB constraint nebo immutable DB function ověří `digest(canonical_bytes, 'sha256')` proti uloženému digestu.

`xmin`, `ctid`, WAL LSN, process PID, timestamp ani PostgreSQL sequence current value nejsou business state version, fence, idempotency key ani stable identity.

### 51.5 Kanonická transakční obálka a linearizační bod

Každá autoritativní mutace používá tuto obálku:

1. `BEGIN` a nastavení přesného transaction profilu,
2. normalizace inputu mimo transakci; žádné provider/browser/network I/O,
3. získání potřebných transaction-scoped advisory locků v pořadí 51.8,
4. zamknutí platform/deployment heads a podle access/operation typu singleton security nebo activation headu,
5. claim nebo lock idempotency recordu,
6. zamknutí activation domain heads,
7. zamknutí aggregate rootů a dalších rows v pořadí 51.6,
8. získání `db_now`, načtení current hodnot z právě zamknutých rows a opětovné vyhodnocení všech guards,
9. provedení právě jednoho povoleného přechodu nebo deklarovaného atomic setu,
10. guarded update se `state_version = state_version + 1`, případně alokace sequences,
11. vložení immutable event/checkpoint/evidence rows,
12. vložení queue/outbox/invalidation/reconcile rows,
13. zamknutí a update `audit_head` jako posledního sdíleného hot row,
14. `COMMIT` jako jediný linearizační bod,
15. response/SSE/NOTIFY se vytvoří pouze z commitnutého stavu.

Každý read použitý k mutačnímu rozhodnutí musí být buď:

- načten z řádku již zamknutého odpovídajícím row lockem,
- znovu ověřen CAS predikátem v `UPDATE`/`DELETE`,
- nebo chráněn unique/check/foreign-key constraintem, jehož violation je explicitně zpracována.

Validace provedená před získáním locku je preflight, nikoli autoritativní guard. Všechny current state, eligibility, deadline, cancellation, revision, binding, epoch, fence a terminality podmínky se znovu vyhodnotí po získání locků.

External side effect nikdy není uvnitř této transakce. Transakce před external side effectem končí commitnutým `side_effect_operation` intentem a dispatch outboxem. Následující outcome/reconciliation používá novou transakci.

### 51.6 Jediné kanonické pořadí row locků

Všechny transakce v systému získávají row locky v tomto úplném pořadí; přeskočené třídy se ignorují, pořadí zbylých se nemění:

```text
A. transaction-scoped advisory locks: (namespace_id ASC, signed_key ASC)
B. singleton authority heads:
   B1 platform_incarnation singleton FOR SHARE
   B2 application_deployment_head singleton FOR SHARE
   B3 owner_api_credential FOR SHARE při API-key acceptance nebo FOR UPDATE při rotaci
   B4 activation_head singleton FOR UPDATE pouze při alokaci epoch
C. domain_idempotency_record: (scope_digest, key_digest)
D. activation_domain_head: domain_key byte order
E. aggregate roots: (aggregate_kind_ordinal, aggregate_uuid raw bytes)
F. concurrency_claim: (namespace_id, key_digest)
G. dedicated sequence allocator rows: (sequence_namespace, parent_uuid, sequence_kind)
H. mutable child/head/claimable-work rows: (child_kind_ordinal, primary_parent_uuid, child stable ID/sequence)
I. audit_head singleton FOR UPDATE
```

Kanonické `aggregate_kind_ordinal` je stabilní serverový enum. Greenfield baseline obsahuje úplné mapování všech samostatně zamykaných rootů; čísla se nikdy nepřidělují podle názvu tabulky ani pořadí migrace:

| Ordinal | Aggregate kind | Kanonický root row |
|---:|---|---|
| 10 | `OWNER_IDENTITY` | `owner_identity`; session/MFA/recovery/throttle jsou children |
| 20 | `SECRET_RECORD` | `secret_record` |
| 30 | `COMPONENT` | `component`; revisions/contracts/releases/readiness/E2E jsou children nebo immutable rows |
| 45 | `EXTERNAL_TARGET` | `external_target`; mutable circuit fields jsou součástí tohoto root row |
| 60 | `AGENT_DEFINITION` | `agent_definition`; eval suite/cases/runs jsou children |
| 70 | `BROWSER_SESSION` | `browser_session`; page/frame/lease/input/action/challenge/teaching jsou children |
| 75 | `BROWSER_ACCOUNT_BINDING` | `browser_account_binding`; state-bundle revisions jsou immutable children |
| 80 | `BROWSER_AUTOMATION_DEFINITION` | `browser_automation_definition` |
| 85 | `BINDING_SET` | `binding_set`; revisions a members jsou immutable children |
| 90 | `GENERATION_JOB` | `generation_job`; turn/phase/plan/workspace/validation/checkpoint jsou children |
| 100 | `AGENT_RUN` | `agent_run`; model/tool/handoff/approval/checkpoint jsou children |
| 105 | `AGENT_SESSION` | `agent_session` |
| 106 | `SYSTEM_CHAT_CONVERSATION` | `system_chat_conversation` |
| 107 | `AGENT_MEMORY_NAMESPACE` | `agent_memory_namespace` |
| 110 | `MCP_CALL_RUN` | `mcp_call_run`; progress/input-exchange/subscription jsou children |
| 115 | `MCP_TASK` | `mcp_task` |
| 116 | `MCP_STATE_HANDLE` | `mcp_state_handle` |
| 120 | `GENERATION_ACTIVATION_SET` | `generation_activation_set` |
| 125 | `BROWSER_AUTOMATION_RUN` | `browser_automation_run`; run steps jsou children |
| 127 | `RUNTIME_INSTANCE` | `runtime_instance`; process/IPC identity rows jsou children |
| 130 | `DEPLOYMENT_RUN` | `deployment_run`; deployment steps jsou children |
| 140 | `OPERATIONAL_SETTING` | `operational_setting` |
| 145 | `CONFIGURATION_APPLY_RUN` | `configuration_apply_run` |
| 150 | `CLEANUP_OPERATION` | `cleanup_operation`; cleanup resources jsou children |
| 155 | `OPERATIONAL_ALERT` | `operational_alert`; deliveries jsou children/outbox work |
| 165 | `SELF_TEST_RUN` | `self_test_run`; case results jsou children |
| 166 | `PRODUCTION_ACCEPTANCE_RUN` | `production_acceptance_run` |
| 170 | `SCHEMA_MIGRATION` | `schema_migration` persistentní migration step/root |

MCP server/tool/resource/prompt identity a revision rows jsou vždy children kanonického `COMPONENT = 30`; nemají alternativní root kind. `application_release`, immutable revisions/evidence a immutable provider/output rows se nezamykají jako mutable root. Pokud jedna mutable child row odkazuje na více parent rootů, operation před child lockem zamkne všechny tyto rooty podle tabulky.

`owner_api_credential`, `platform_incarnation`, `application_deployment_head` a `activation_head` zůstávají ve singleton class B; `activation_domain_head` ve class D; `concurrency_claim` ve class F; `audit_head` ve class I. Každý root row má UUID `id`; stable text key je samostatný unique business key a nikdy se nepoužívá místo UUID v row-lock sortu.

Nový aggregate kind dostane nový nikdy nerecyklovaný ordinal. Ordinal již zveřejněný v migraci se nesmí změnit. Bulk operation před `BEGIN` kanonicky seřadí celý známý target set; během transakce nesmí objevit další dříve řazený root. Pokud je nový root zjištěn až po locku, transakce rollbackne a začne znovu s úplným setem.

`INSERT`, `UPSERT`, first-create unique-index wait a získání row locku nad mutable rootem se pro lock-order pravidla považují za získání jeho class E slotu. Root insert se proto provede až na jeho seřazené pozici po všech nižších rootech.

Class G obsahuje pouze dedicated sequence allocator, pokud čítač není přímo v již zamknutém rootu. Class H používá tento stabilní baseline `child_kind_ordinal`:

| Child ordinal | Mutable child/work kind |
|---:|---|
| 10 | OWNER session/MFA/recovery/throttle state |
| 20 | Secret version lifecycle, binding a invalidation child |
| 30 | Component pointer/runtime/control/readiness child |
| 35 | `domain_command` mutable lifecycle row |
| 40 | Activation admission, barrier a switched-member projection |
| 50 | Generation turn/phase/checkpoint/workspace mutable head |
| 60 | Agent model/tool/handoff/approval child |
| 65 | Agentic authority intent/context/action-plan/provenance mutable child |
| 70 | MCP progress/input exchange/subscription child |
| 80 | Browser control lease/input/action/challenge child |
| 90 | Browser automation run-step child |
| 100 | Runtime process/IPC/credential-generation child |
| 110 | Deployment step child |
| 120 | Cleanup resource child |
| 130 | Configuration effective, monitoring projection a alert-delivery child |
| 140 | `side_effect_operation` mutable state |
| 150 | Mutable attempt/delivery projection nad immutable attempt evidence |
| 900 | `queue_item` claim/enqueue row |
| 910 | `transactional_outbox` a delivery-claim projection; audit archive row má zvláštní insert pravidlo 51.25 |
| 920 | `transactional_inbox` row |

Immutable child insert má stejný ordinal svého druhu, i když nevyžaduje následný row lock. `primary_parent_uuid` je explicitní non-null lock-parent column; u multi-parent child určuje pouze H sort, nikoli ownership, a všechny skutečné parent rooty musí být již zamknuté. Ordinal 900–999 je rezervovaný pro claimable work/delivery rows, takže successor queue/outbox/inbox insert proběhne po všech doménových child rows a před audit headem. Nový child kind dostane explicitní nikdy nerecyklovaný ordinal v migration manifestu; runtime nesmí odvozovat pořadí z názvu tabulky nebo OID.

`audit_head` se vždy zamyká poslední. Doménový diff, event payload a všechny outcome digests musí být před jeho lockem definitivní. Tím se globální chain serializace nestane příčinou lock-order inversion.

Každý insert/update/delete mutable child row, který patří do aggregate invariantu, nejprve zamkne jeho parent aggregate root. Jedinou výjimkou je worker delivery claim nebo delivery-marker update class H ordinalů `900|910` podle 51.14; ten nesmí měnit parent business state a před dispatch má samostatnou fresh parent-root transakci. Proto je ostatní child-set predicate vyhodnocený pod parent lockem stabilní vůči korektním writerům; unique/FK constraints zůstávají konečným guardem proti nekorektnímu nebo budoucímu writeru.

Lock plan každé operation je testovatelná metadata. Runtime v test/debug režimu zaznamenává pořadí získaných lock classes a odmítne operation, která se pokusí získat nižší class po vyšší.

### 51.7 Row-lock modes a absent-row problém

Použití row locks je přesné:

- mutable aggregate root: `FOR UPDATE`,
- platform/deployment head během běžné mutace: `FOR SHARE`,
- platform/deployment head při jeho změně: `FOR UPDATE`,
- OWNER API credential při API-key acceptance: `FOR SHARE`, při rotaci: `FOR UPDATE`,
- activation domain při admission: `FOR SHARE`, při barrier/switchi: `FOR UPDATE`,
- idempotency, lease, task, approval, input exchange, control lease, pointer head a sequence allocator: `FOR UPDATE`,
- queue/outbox candidate: `FOR UPDATE SKIP LOCKED`,
- immutable revision/evidence row: bez row locku; existence a parent ownership chrání FK/unique constraint,
- audit head: `FOR UPDATE`.

Neexistující row nelze chránit `SELECT ... FOR UPDATE`. Race při first-create používá jednu z těchto konstrukcí v uvedeném pořadí preference:

1. parent/head guard row, který vždy existuje a zamkne se,
2. unique nebo partial unique index jako konečný arbiter,
3. `INSERT ... ON CONFLICT DO NOTHING`, následovaný `SELECT ... FOR UPDATE` existujícího row,
4. transaction-scoped advisory lock z namespace 51.8 pro omezení souběžného first-create contention.

Advisory lock nikdy nenahrazuje unique constraint. Hash collision smí pouze zbytečně serializovat dvě různé operace; nesmí způsobit záměnu identity nebo přijetí neplatného stavu.

`NOWAIT` se používá pouze tam, kde operation contract výslovně vrací okamžitý `CONCURRENCY_KEY_HELD` nebo `ACTIVATION_IN_PROGRESS`. Jinak se používá bounded čekání podle `lock_timeout`.

### 51.8 Advisory-lock namespaces a key derivation

Povoleny jsou pouze transaction-scoped funkce `pg_advisory_xact_lock(int,int)` a `pg_try_advisory_xact_lock(int,int)`. Session-level `pg_advisory_lock`, `pg_advisory_unlock` a držení advisory locku přes commit jsou zakázané.

Kanonické namespace IDs jsou:

| Namespace ID | Symbol | Účel |
|---:|---|---|
| 1001 | `BOOTSTRAP_SINGLETON` | bootstrap právě jednoho OWNER/platform head row |
| 1010 | `COMPONENT_CODE` | first-create KCML number/code/hostname |
| 1020 | `SECRET_STABLE_NAME` | first-create stable secret name |
| 1030 | `IDEMPOTENCY_SCOPE` | first-create idempotency row při vysokém contention |
| 1040 | `CONCURRENCY_CLAIM` | first-create persistent concurrency claim row |
| 1050 | `ACTIVATION_DOMAIN` | first-create activation domain head |
| 1060 | `BROWSER_CONTROL` | first-create browser control lease row |
| 1070 | `CLEANUP_REMEDIATION` | first remediation/cleanup candidate pro owner scope |
| 1080 | `SCHEDULE_OCCURRENCE` | unique materializace schedule occurrence |
| 1090 | `INBOUND_EVENT` | first-create inbox row při provider duplicate burstu |
| 1100 | `SCHEMA_MIGRATION` | jediný migrator/deployment schema step |

Druhý `int` key vznikne přesně z prvních čtyř bytes SHA-256 canonical key bytes v network byte order: bytes se interpretují jako unsigned big-endian `uint32` a stejný bit pattern se převede two's-complement na PostgreSQL signed `int4`. Aplikační knihovna `PostgresAdvisoryKey` je jediná implementace převodu a má cross-language test vectors včetně hodnot nad `0x7fffffff`. Plný 32-byte digest je vždy uložen v row a chráněn unique constraintem; advisory key není identita.

Více advisory locků se získává seřazeně podle `(namespace_id, signed_key)`. Try-lock failure nemění business stav. Žádný advisory lock se nedrží při HTTP, OpenAI, browser, filesystem, systemd, DNS, TLS ani jiné externí operaci.

### 51.9 `state_version`, `lock_version` a guarded writes

`state_version bigint NOT NULL DEFAULT 0 CHECK (state_version >= 0)` patří na každý aggregate root a každý samostatně lifecycle-řízený run/task/action/lease. Každá autoritativní business mutace daného row zvýší `state_version` právě o jedna.

`lock_version` se používá pouze na samostatných mutable leaf rows, které nejsou aggregate rootem, například dashboard layout nebo čistě prezentační metadata. Jeden row nesmí používat `state_version` a `lock_version` jako dvě alternativní autority. Pokud row ovlivňuje lifecycle, active pointer, credential, binding, side effect, queue, lease, checkpoint nebo terminal outcome, používá `state_version`.

Kanonický CAS update má tvar:

```sql
UPDATE component
SET lifecycle = $new_state,
    state_version = state_version + 1,
    aggregate_event_sequence = aggregate_event_sequence + 1,
    updated_at = $db_now
WHERE id = $component_id
  AND state_version = $expected_state_version
  AND platform_incarnation_id = $platform_incarnation_id
  AND lifecycle = $expected_state
  AND current_activation_epoch = $expected_activation_epoch
  AND active_binding_set_revision_id IS NOT DISTINCT FROM $expected_binding_set_revision_id
RETURNING state_version, aggregate_event_sequence;
```

Přesný guard se může rozšířit, nesmí se zúžit proti operation contractu. Nulový počet rows je conflict/stale authority, nikoli success. Aplikace po nulovém výsledku načte current snapshot; nesmí provést blind retry s novou version bez nového business commandu.

Předchozí `SELECT FOR UPDATE` nezbavuje povinnosti zahrnout expected CAS a fence v závěrečném `UPDATE`. Tím se brání budoucímu refaktoru, který by lock omylem odstranil.

### 51.10 Immutable revisions, attempts, evidence a active pointers

Revision, release manifest, binding-set revision, automation revision, agent revision, checkpoint, side-effect attempt, model output item, audit event, domain event a terminal evidence jsou immutable business rows. DB trigger `kcml_reject_immutable_mutation()` odmítne jejich `UPDATE` a `DELETE`. Retention metadata, archive location nebo verification projection se ukládají do odděleného mutable row, nikoli změnou immutable payloadu.

Každá parent-owned revision má nejméně:

```text
UNIQUE (parent_id, revision_number)
UNIQUE (parent_id, canonical_digest)
UNIQUE (parent_id, id)
```

Active pointer používá composite foreign key dokazující parent ownership:

```sql
FOREIGN KEY (component_id, active_revision_id)
  REFERENCES component_revision (component_id, id)
  ON DELETE RESTRICT;
```

Stejný vzor platí pro release, agent revision, automation revision, binding-set revision, state schema a runtime target. Pointer target se nikdy fyzicky nesmaže, dokud na něj může odkazovat current nebo frozen previous/candidate snapshot.

Pointer row obsahuje `state_version`, `activation_epoch`, target ID, target digest a activation set ID. Pointer se mění pouze activation transakcí nebo explicitním direct operation kontraktem, který je sám jednoprvkovým activation setem. Samostatné postupné přepínání vzájemně závislých pointerů je zakázané.

### 51.11 Singleton OWNER, platform heads a bootstrap

`owner_identity`, `owner_api_credential`, `platform_incarnation`, `activation_head`, `application_deployment_head` a `audit_head` používají fyzický singleton key:

```sql
singleton_key smallint PRIMARY KEY DEFAULT 1 CHECK (singleton_key = 1)
```

Bootstrap migrace pod advisory namespace `BOOTSTRAP_SINGLETON` vloží chybějící rows a ověří přesně jeden row každého druhu. Trigger zakáže změnu singleton key a `DELETE`. Readiness selže, pokud row chybí nebo jeho fixní hodnoty neodpovídají SSOT.

`owner_identity.username` je typu `citext` pro case-insensitive login lookup, ale uloženou kanonickou hodnotu vynucuje `CHECK (username::text = 'KRMAR78')`; současně má unique constraint. Neexistence druhého row je vynucena singleton primary key, nikoli pouze startup kontrolou.

Každá autoritativní transakce získá `platform_incarnation FOR SHARE` a `application_deployment_head FOR SHARE` před business rooty. Restore nebo deployment epoch switch získá oba rows `FOR UPDATE`; nemůže tedy commitnout současně s transakcí, která byla přijata pod starou incarnation/epoch, aniž by jednoznačně nastalo pořadí commitů.

### 51.12 Idempotency, request digest a logical operation creation

`domain_idempotency_record` obsahuje:

- `scope_digest bytea` s délkou 32,
- `key_digest bytea` s délkou 32,
- canonical key pouze pro OWNER debug,
- `request_digest bytea` s délkou 32,
- `logical_operation_id uuid`,
- lifecycle, state version a canonical outcome digests.

Povinné constraints:

```text
UNIQUE (scope_digest, key_digest)
UNIQUE (logical_operation_id)
CHECK (octet_length(scope_digest) = 32)
CHECK (octet_length(key_digest) = 32)
CHECK (octet_length(request_digest) = 32)
```

Claim algoritmus je přesně:

1. již autentizovaný internal/session path může před singleton heads volitelně získat xact advisory lock `IDEMPOTENCY_SCOPE`; API-key acceptance caller-derived advisory lock před credential verification nepoužívá,
2. provede `INSERT ... ON CONFLICT DO NOTHING` s novým `logical_operation_id`,
3. provede `SELECT ... FOR UPDATE` podle `(scope_digest,key_digest)`,
4. porovná `request_digest` constant-time v aplikační vrstvě a byte equality v DB,
5. při shodě replayuje nebo pokračuje pod existujícím logical operation ID,
6. při neshodě vrátí `IDEMPOTENCY_CONFLICT` před root mutací a před side-effect intentem.

`ON CONFLICT DO UPDATE` se pro idempotency claim nepoužívá. Request digest, logical operation ID ani terminal outcome existujícího recordu se nikdy nepřepíší payloadem retry requestu.

Vytvoření `domain_command`, idempotency recordu a přijetí async operation je jedna transakce. Po class C idempotency a class D activation-domain admission zamkne všechny existující target aggregate rooty class E, znovu ověří expected state/revision/binding/activation snapshot a teprve potom vloží `domain_command` child ordinal `35` a queue/outbox ordinal `900|910`. CREATE bez existujícího target rootu zamkne jeho stable parent/namespace guard a first-create advisory podle operation contractu. Worker execution všechny guards znovu ověří; acceptance lock není náhradou execution CAS. HTTP `202` lze vrátit pouze po commitu commandu a queue/outbox row. Ztracená HTTP odpověď se řeší replayem stejného idempotency key.

### 51.13 Monotonic a contiguous sequence allocation

Sekvence se dělí na dvě třídy:

1. **Identity sequence s povolenými gaps** — interní surrogate IDs a neautoritativní ordering; může použít PostgreSQL sequence.
2. **Business contiguous nebo fenced sequence** — aggregate event, command, checkpoint, session item, stream, audit chain, activation epoch, control epoch, attempt number; používá zamknutý head/root row.

`MAX(sequence)+1` je vždy zakázané. Contiguous allocator provede pod parent/root `FOR UPDATE` atomické `last_sequence = last_sequence + 1 RETURNING last_sequence`. Insert child row a update head jsou ve stejné transakci.

Attempt sequence smí mít gap pouze tehdy, když reservation sama představuje immutable attempt evidence. Číslo rezervované v rollbacknuté transakci neexistuje a není gapem v commitnutém streamu.

Activation epoch alokuje pouze `activation_head FOR UPDATE`. Browser control epoch alokuje browser session root. Lease fencing token alokuje persistentní fenced resource row; row se po release neodstraňuje, aby nedošlo k ABA.

### 51.14 Queue, outbox a inbox claim přes `SKIP LOCKED`

`queue_item`, `transactional_outbox` a `transactional_inbox` jsou class H claimable-work rows s child ordinals `900`, `910` a `920`; nejsou business aggregate roots. Při doménovém commitu se vkládají po required concurrency claims, sequence allocators a všech nižších domain child rows. Při worker claimu je lze zamknout bez parent rootu pouze podle zde uvedeného protokolu, protože nesou materializovaný parent/cancellation/epoch snapshot a před external dispatch následuje fresh parent-root transakce.

`FOR UPDATE SKIP LOCKED` se používá pouze pro queue, outbox, archive outbox, retention batch a jinou explicitně unordered nebo partitioned worker distribuci. Nepoužívá se pro activation eligibility, cleanup completeness, unique active pointer, approval rozhodnutí, secret rotation ani jiné globální business rozhodnutí.

Queue eligibility je materializovaný stav. Candidate row musí mít současně:

```text
state = READY
available_at <= db_now
unmet_dependency_count = 0
barrier_blocked = false
cancellation_requested = false
attempt_count < max_attempts
deadline_at IS NULL OR deadline_at > db_now
```

Dynamic dependency, parent cancellation nebo barrier rozhodnutí se promítne do těchto polí v transakci, která mění dependency/domain stav. Candidate query nesmí spoléhat na phantom-prone korelovaný `NOT EXISTS` bez zamknutého guard row.

Kanonický queue claim dodržuje class F před class H a používá neautoritativní preselection plus jednu krátkou claim transakci na zvoleného kandidáta:

1. mimo claim transakci nebo nezamykajícím prvním statementem načte bounded ordered candidate hints `id`, `state_version`, `concurrency_key_set`, `concurrency_key_set_digest`, materialized incarnation/deployment/parent snapshot; preliminary `clock_timestamp()` pouze omezuje kandidáty,
2. pro jeden candidate zahájí `WORKER_COMMIT` a pomocí `pg_try_advisory_xact_lock` získá všechny potřebné first-create locks pro concurrency keys v class A pořadí; selže-li jediný try-lock, okamžitě rollbackne candidate bez změny stavu, jinak pokračuje zamknutím platform/deployment heads class B,
3. `INSERT ... ON CONFLICT DO NOTHING` zajistí existenci persistentních `concurrency_claim` rows a všechny je zamkne v class F pořadí; výběr používá `FOR UPDATE SKIP LOCKED` a pokud nevrátí přesně celý expected key set, transakce rollbackne a worker zkusí další candidate,
4. teprve potom zamkne exact `queue_item` class H row přes `SELECT ... WHERE id = $candidate_id FOR UPDATE SKIP LOCKED`; nula rows znamená rollback této candidate transakce,
5. po všech locks načte jediný `db_now = clock_timestamp()` a znovu ověří queue eligibility, expected queue `state_version`, key-set digest, materialized parent/cancellation/barrier snapshot, incarnation, deployment epoch a deadline,
6. je-li některý concurrency claim current a neexpired pod jinou logical operation, worker claim nemění; queue row ponechá `READY`, v bounded backoff větvi posune `available_at`, zvýší queue `state_version` a uloží backoff event/audit, potom commitne bez executable claimu,
7. jsou-li všechny claims dostupné, každý claim row zvýší vlastní monotonic fence a přejde na current owner/expiry; queue row současně přejde `READY → CLAIMED`, zvýší attempt, queue fence a state version,
8. claim event, audit a případný worker wake evidence se commitnou společně. Nulový výsledek kteréhokoli final CAS znamená rollback celé executable-claim větve, takže nepersistuje ani jediný partial concurrency nebo queue claim.

Preselection pořadí je:

```sql
SELECT q.id,
       q.state_version,
       q.concurrency_key_set,
       q.concurrency_key_set_digest,
       q.platform_incarnation_id,
       q.application_deployment_epoch
FROM queue_item q
WHERE q.queue_kind = $queue_kind
  AND q.state = 'READY'
  AND q.available_at <= clock_timestamp()
  AND q.unmet_dependency_count = 0
  AND q.barrier_blocked = false
  AND q.cancellation_requested = false
  AND q.attempt_count < q.max_attempts
ORDER BY q.priority DESC,
         q.fairness_key ASC NULLS LAST,
         q.available_at,
         q.id
LIMIT $bounded_candidate_count;
```

Po class F locks se exact queue row zamkne:

```sql
SELECT q.*
FROM queue_item q
WHERE q.id = $candidate_id
FOR UPDATE SKIP LOCKED;
```

Final queue transition používá current locked row a nejméně tyto guards:

```sql
UPDATE queue_item q
SET state = 'CLAIMED',
    attempt_count = attempt_count + 1,
    lease_owner_id = $worker_id,
    fencing_token = fencing_token + 1,
    lease_acquired_at = $db_now,
    last_heartbeat_at = $db_now,
    lease_expires_at = $db_now + $lease_interval,
    platform_incarnation_id = $current_incarnation,
    application_deployment_epoch = $current_deployment_epoch,
    state_version = state_version + 1
WHERE q.id = $candidate_id
  AND q.state = 'READY'
  AND q.available_at <= $db_now
  AND q.unmet_dependency_count = 0
  AND q.barrier_blocked = false
  AND q.cancellation_requested = false
  AND q.attempt_count < q.max_attempts
  AND (q.deadline_at IS NULL OR q.deadline_at > $db_now)
  AND q.platform_incarnation_id = $expected_queue_incarnation
  AND q.application_deployment_epoch = $expected_queue_deployment_epoch
  AND q.state_version = $expected_queue_state_version
  AND q.concurrency_key_set_digest = $expected_key_set_digest
RETURNING q.*;
```

Je-li concurrency claim platně držen jinou operation, backoff `available_at` je nejméně `db_now + min_backoff` a nejvýše observed claim expiry plus bounded jitter. Worker nesmí v tight loopu znovu volit stále tentýž blokovaný item.

Operation contract obsahuje přesně jedno `concurrencyClaimPoint = ACCEPTANCE | WORKER_CLAIM`. `ACCEPTANCE` získá class F claims v původní doménové transakci a queue/outbox row nese jejich exact fences; delivery worker je pouze validuje. `WORKER_CLAIM` vloží READY work row bez claimu a použije výše uvedený F-before-H protokol. Generic async queue používá `WORKER_CLAIM`; odlišná volba je přípustná jen jako explicitní immutable operation contract. Jedna logical operation nesmí oba režimy smíchat ani alokovat druhý claim fence pro tentýž attempt.

Každý external `side_effect_attempt` má právě jeden canonical dispatch authority row a tím je `transactional_outbox` s purpose `SIDE_EFFECT_DISPATCH`. `queue_item` smí naplánovat interní producer/handler krok, ale nikdy neopravňuje provést tentýž external effect. Auxiliary wake/notification outbox má `is_dispatch_authority = false`. Povinné uniqueness je partial unique `(side_effect_operation_id, side_effect_attempt_sequence) WHERE purpose = 'SIDE_EFFECT_DISPATCH' AND is_dispatch_authority = true`; worker provede external request pouze z tohoto row, immutable attemptu a exact attempt-state/delivery fence.

Outbox claim bez business concurrency keys přeskočí class F a vybere class H `transactional_outbox` rows pomocí `FOR UPDATE SKIP LOCKED`, poté načte `db_now`, znovu ověří eligibility a guarded nastaví delivery lease/fence. Pokud destination contract vyžaduje concurrency key, používá stejný F-before-H protokol. Pro obyčejný event/message outbox je delivery at-least-once a duplicate řeší consumer inbox dedupe. Pro purpose `SIDE_EFFECT_DISPATCH` však claim neopravňuje k blind redelivery po možném dispatchi: po commitu attempt-state `DISPATCHING` vede timeout, crash nebo ztracený ACK do reconciliation; nový external request smí vzniknout pouze novým attemptem po `CONFIRMED_NOT_APPLIED`, nebo jako explicitně povolený retry se stejným target idempotency key podle side-effect contractu.

Outbox lifecycle je podle `purpose` přesný. Ordinary event/message delivery používá `READY|RETRY_WAIT → CLAIMED → DELIVERED|RETRY_WAIT|FAILED_FINAL`; expired `CLAIMED` lze reclaimnout vyšším delivery fence. `SIDE_EFFECT_DISPATCH` používá `READY → CLAIMED → DISPATCH_AUTHORIZED → RECONCILING|CLOSED`. Expired `CLAIMED` před D commitem lze reclaimnout, protože external authority ještě nevznikla. `DISPATCH_AUTHORIZED` se nikdy nevrací do `READY` ani `CLAIMED`; po expiry lze pouze převést reconciliation ownership na vyšší delivery fence při zachování state `DISPATCH_AUTHORIZED` nebo přejít do `RECONCILING`, přičemž nový owner smí jen retrieve/read-back/reconcile exact attempt a nesmí odeslat request. `CLOSED` vznikne až v T3 při canonical known/unknown resolution podle operation state.

Inbox processing získá xact advisory `INBOUND_EVENT`, platform/idempotency/domain heads, všechny target aggregate roots, concurrency claims a sequence allocators v classes A–G; potom zamkne všechny nižší domain child rows a nakonec `transactional_inbox` child ordinal `920`. First-create používá `INSERT ... ON CONFLICT DO NOTHING`, následovaný `SELECT ... FOR UPDATE`; unique `(consumer_scope_digest, external_event_id)` je konečný arbiter. Po locku porovná payload digest a teprve potom provede nebo replayuje domain transition a uloží canonical outcome/event/audit/outbox v jednom commitu. Stejný event ID s jiným digestem je `PROTOCOL_CONFLICT` a handler se nespustí.

### 51.15 Lease claim, heartbeat, expiry během transakce a takeover

Každý fenced resource má persistentní row s posledním `fencing_token`; row se po release ani expiry nemaže. Claim/takeover zamkne resource row `FOR UPDATE`, získá `db_now` až po locku a uspěje pouze při:

```text
lease_state != ACTIVE
OR lease_expires_at <= db_now
OR lease_owner_id = same owner a explicitní idempotentní claim kontrakt
```

Úspěšný nový claim vždy provede `fencing_token = fencing_token + 1`. Token není PostgreSQL sequence ani timestamp. Heartbeat token nezvyšuje a používá guarded `UPDATE` se shodou owner, fence, incarnation, deployment epoch, active state a `lease_expires_at > db_now`.

Lease může kalendářně expirovat během krátké transakce poté, co current owner zamkl resource row ještě před expiry. Takeover nemůže získat stejný row lock, dokud první transakce neskončí. Proto platí:

- DB-only autoritativní commit smí dokončit, pokud row byl zamknut před expiry a všechny ostatní guards byly current; commit linearizuje před pozdějším takeoverem,
- po uvolnění locku může takeover alokovat vyšší fence,
- žádný external dispatch nesmí začít pouze na základě dřívějšího locku; bezprostředně před dispatch se provede nová pre-dispatch transakce, nový `db_now` a kontrola `lease_expires_at - db_now >= min_dispatch_authority_window`,
- `min_dispatch_authority_window` je immutable operation-contract hodnota nejméně `adapter_timeout + 2 × heartbeat_interval + 5 s`; configured lease interval musí být nejméně tato hodnota plus maximální pre-dispatch DB budget,
- pokud lease již expirovala, nelze ji prodloužit ani heartbeat-em oživit a pre-dispatch rollbackne; pokud je stále active, ale window nestačí, worker ji v téže pre-dispatch transakci bezpečně prodlouží pod current owner/fence a guardem `lease_expires_at > db_now`, potom znovu ověří výslednou expiry, nebo přejde do safe retry bez dispatchu,
- external call, který již byl dispatchnut, se rozhoduje intent/reconciliation, nikoli předstíráním, že fence paket stáhl zpět.

Takeover a heartbeat race rozhodne první commit nad resource row. Expired lease nelze obnovit heartbeat-em ani když takeover ještě neproběhl; heartbeat `WHERE lease_expires_at > db_now` ovlivní nula rows.

### 51.16 Terminal write, successor reservation a enqueue

Každý workflow edge, jehož predecessor terminalizace vyžaduje successor, používá jednu PostgreSQL transakci. V ní se současně:

1. zamkne parent/predecessor root a ověří current fence/state version,
2. uloží terminal result/error digest a immutable terminal evidence,
3. inkrementuje parent state/event sequence,
4. vytvoří nebo aktivuje právě jednu successor reservation,
5. vloží právě jeden unique queue item/outbox event pro successor,
6. uloží domain event, audit a checkpoint pointer,
7. commitne.

Povinný unique constraint je nejméně `(parent_logical_operation_id, successor_kind, successor_ordinal)` nebo doménově ekvivalentní stabilní klíč. Redelivery predecessor completion pouze replayuje existující successor.

Tento kontrakt platí pro phase success + next phase, approval/input fulfilment + resume, task input + resume, model output + tool dispatch, tool completion + next model turn, discussion completion + successor turn, cleanup step + next cleanup step a configuration apply step + next target.

### 51.17 Persistentní concurrency claim

`concurrency_claim` je persistentní fenced resource s `PRIMARY KEY (namespace_id,key_digest)`, non-null 32-byte digestem a monotonic `fencing_token`. Active invariant se nevynucuje mazáním řádků ani time-based partial indexem; jediný row drží current owner, state, last fence a expiry.

First-create claim row používá advisory namespace `CONCURRENCY_CLAIM`, `INSERT ... ON CONFLICT DO NOTHING` a následné `FOR UPDATE`. Claim vzniká ve stejné transakci jako přechod logical operation/queue itemu do executable stavu. Release nebo terminalizace parentu ve stejné transakci nastaví claim inactive, ale zachová poslední fence.

Více required concurrency keys se před transakcí seřadí `(namespace_id,key_digest)` a zamknou v tomto pořadí. Operation nesmí dynamicky získat nový klíč s nižším pořadím; musí rollbacknout a opakovat s úplným setem.

Claim lease není substitute za aggregate CAS. Write musí současně ověřit parent state version, platform/deployment epoch a claim fence.

Operation contract má povinné `concurrencyClaimPoint`. V režimu `ACCEPTANCE` vznikne claim ve stejné transakci jako parent executable transition a každý queue/outbox attempt pinne jeho fence. V režimu `WORKER_CLAIM` parent pouze vytvoří READY work row a claim vznikne atomicky s worker queue lease podle 51.14. Reclaim/takeover vždy používá tentýž režim a vyšší fence; implementace nesmí rozhodnout claim point ad hoc podle workeru nebo transportu.

### 51.18 Activation domain admission, barrier, switch a rollback

Každý activation domain má vždy existující `activation_domain_head`:

```text
domain_key + domain_digest
state_version
current_activation_epoch
current_activation_set_id
admission_state = OPEN | DRAINING | CLOSED
barrier_id
platform_incarnation_id
updated_at
```

`domain_key` je stable canonical identity a unique. First-create head používá advisory namespace `ACTIVATION_DOMAIN` a unique insert. `activation_domain_head` je autoritativní guard proti phantom admission; `activation_domain_barrier.pending_mutating_operation_count` je kontrolní projection, nikoli jediný zdroj pravdy.

Každá nová mutující `domain_command` v téže acceptance transakci:

1. zamkne všechny dotčené domain heads `FOR SHARE` v `domain_key` pořadí,
2. ověří `admission_state = OPEN` a expected current epoch,
3. vloží unique `domain_command_activation_domain(command_id,domain_id,pinned_epoch,state)`,
4. zvýší pending projection nebo vytvoří odpovídající immutable admission event,
5. teprve poté commitne command a queue/outbox.

Read-only command může být přijat proti current epoch podle contractu, ale jeho admission row je označena `READ_ONLY` a switch na něj nemusí čekat. Mutující command je po celou dobu pinned k jednomu epoch; změna target epoch uprostřed logical operation je zakázaná.

Barrier start transakce dodrží class D před class E:

1. po platform/deployment heads a případném idempotency row zamkne všechny dotčené domain heads `FOR UPDATE` ve stabilním pořadí,
2. potom zamkne `generation_activation_set` root `FOR UPDATE`, ověří nebo CAS-claimne current controller lease/fence a znovu ověří set state,
3. ověří exact previous snapshot a že jiný překrývající barrier není active,
4. nastaví `admission_state = DRAINING`, barrier ID a zvýší domain state versions,
5. vytvoří/aktualizuje `activation_domain_barrier` class H child rows, event, audit a outbox,
6. commitne.

Samostatná dřívější controller-lease claim transakce je přípustná, ale barrier start vždy znovu ověří current fence po domain locks; nesmí držet activation-set root a teprve potom žádat domain head lock.

Po tomto commitu žádná nová mutující admission neuspěje. Controller mimo transakci čeká, až všechny pre-barrier mutující `domain_command_activation_domain` rows přejdou do terminal/checkpoint/reconciliation stavu. Každý jejich terminal write v téže transakci aktualizuje admission relation. Controller nespoléhá pouze na counter; před switchi pod domain heads `FOR UPDATE` provede autoritativní `NOT EXISTS` nad explicitními relation rows. Proto nemůže vzniknout phantom, protože každý writer musí nejprve získat shared lock na stejném domain headu.

Switch transakce používá profil `ACTIVATION_SWITCH` a přesný globální lock order:

```text
platform/deployment heads FOR SHARE
activation_head FOR UPDATE
domain heads FOR UPDATE sorted
všechny dotčené aggregate roots včetně activation setu sorted podle 51.6
mutable binding/route/runtime pointer child rows sorted
audit_head FOR UPDATE last
```

V baseline tedy component/binding/agent/job a jiné rooty s ordinalem nižším než `GENERATION_ACTIVATION_SET = 120` předcházejí activation-set rootu; rooty s vyšším ordinalem následují. Controller nesmí zamknout activation set a následně žádat dříve řazený member root.

Po locku znovu ověří controller fence, set state `SWITCHING`, closed barrier, prázdný mutating admission set, frozen membership digest, previous snapshot IDs/digests/state versions/epochs, candidate eligibility a fresh gate evidence.

V jednom commitu:

- `activation_head.last_epoch = last_epoch + 1`,
- všechny revision/release/binding-set/route/runtime/agent/automation/state-schema pointers přejdou na candidate snapshot,
- všechny domain heads dostanou stejný nový epoch a candidate set ID,
- set přejde `SWITCHING → VERIFYING`,
- members dostanou switched evidence,
- vzniknou runtime/route/reconcile outbox rows, domain eventy a audit.

Žádný pointer se nesmí změnit před nebo po této transakci samostatně. Gateway admission čte a validuje domain head epoch; request s více domains zamkne/načte heads v jednom transaction snapshotu a přijme jej pouze při exact activation-set compatibility. Smíchání members z různých epochs je odmítnuto.

Rollback používá nový barrier a stejný lock plan. Reverse-switch transakce ověří, že current pointer snapshot stále odpovídá candidate snapshotu a původnímu switch epoch, alokuje vyšší epoch a atomicky obnoví celý frozen previous snapshot nebo `ABSENT`. Rollback nikdy nesnižuje epoch.

Povinné physical constraints:

- `UNIQUE (domain_key)` na `activation_domain_head`,
- `UNIQUE (command_id,domain_id)` na admission relation,
- `UNIQUE (activation_set_id,domain_id)` na barrier relation,
- partial unique nejvýše jeden barrier v `REQUESTED|DRAINING|CLOSED` na domain,
- `UNIQUE (activation_set_id,object_kind,object_id)` na membership,
- composite FKs pro každý previous/candidate pointer target,
- check, že switch/rollback epoch je vyšší než předchozí domain epoch,
- terminal/active set guards podle 49.17.

### 51.19 Binding-set revisions a component bindings

`binding_set` je mutable aggregate root `BINDING_SET = 85` s parent ownerem, current revision pointerem, `state_version` a activation epoch. Owner kind je přesně `COMPONENT`, `AGENT_DEFINITION` nebo `BROWSER_AUTOMATION_DEFINITION`; platformní binding set je vlastněn platformní `COMPONENT`. Každá mutace zamkne owner root a binding-set root v ordinal orderu, tedy owner `30|60|80` před binding setem `85`. `binding_set_revision` je immutable parent-owned revision s canonical digestem. Obsahuje úplný frozen set exact contract, Secret, external target, route a tool bindings, nikoli pouze diff. `binding_set_member` je immutable a unique podle exact source/target/operation/purpose identity uvnitř revision.

Povinné constraints:

```text
UNIQUE (binding_set_id, revision_number)
UNIQUE (binding_set_id, canonical_digest)
UNIQUE (binding_set_revision_id, member_key_digest)
CHECK (octet_length(member_key_digest) = 32)
```

Každý member má check podle kind, že právě relevantní foreign keys jsou non-null, a composite FKs dokazující ownership revision. Wildcard source, all-secrets member a neurčitý target nejsou representovatelné validním row.

Candidate binding set se vytvoří immutable inserty a validuje mimo active pointer. Jeho aktivace proběhne pouze v activation switch transakci. Direct bind/unbind operation vytvoří novou immutable binding-set revision a jednoprvkový activation set; nemění current revision in-place.

Runtime resolution vždy pinne exact `binding_set_revision_id`, member ID, member digest a activation epoch do execution contextu. Late runtime s předchozí revision nemůže resolve ani zapsat outcome po změně current epoch/fence.

### 51.20 Secret version, binding a atomická rotace `KCML_OWNER_API_KEY`

`secret_version` je immutable. Fyzické constraints zahrnují:

```text
UNIQUE (secret_id, version_number)
UNIQUE (secret_id, id)
CHECK (octet_length(canonical_value_digest) = 32)
partial UNIQUE (secret_id) WHERE lifecycle = 'ACTIVE'
```

Ciphertext, nonce, algorithm, key ID, fingerprint a canonical value digest se po insertu nemění. Active pointer a `secret_activation_epoch` jsou pouze v `secret_record` rootu. Lifecycle projection version row smí v activation transakci přejít `CREATED|RETIRED → ACTIVE` a previous version `ACTIVE → RETIRED`; tím zůstává možné znovu aktivovat OWNERem vybranou historickou verzi bez kopírování hodnoty. Každý takový přechod vytvoří immutable domain event/audit s novým secret activation epoch; projection timestamps na version row historii nenahrazují. Trigger zakáže změnu cryptographic fields.

Obecná secret activation/rotation:

1. claimne idempotency record,
2. před mutací odvodí celý finite set dotčených aggregate rootů a zamkne je v jediném pořadí 51.6; případný `OWNER_IDENTITY = 10` předchází `SECRET_RECORD = 20`, zatímco component/browser/agent/runtime roots následují podle svých ordinalů,
3. nad zamknutým `secret_record` načte candidate i current version a ověří parent, lifecycle, digest a expected secret state version,
4. zamkne dependent mutable invalidation child/head rows až po všech jejich parent roots a v class H pořadí,
5. pokud candidate není current, nejprve změní previous `ACTIVE → RETIRED` s `retired_at = db_now` a potom candidate `CREATED|RETIRED → ACTIVE` s `activated_at = db_now`, `retired_at = NULL`, aby immediate partial unique index nebyl ani na okamžik porušen,
6. přepne active pointer, zvýší secret state version a activation epoch,
7. vytvoří invalidation epoch, exact binding/session/browser invalidation outbox, immutable activation event a audit,
8. commitne. Pokud candidate již je current active version, operation uloží nebo replayuje canonical `ALREADY_ACTIVE` no-op outcome, neinkrementuje secret epoch ani state version a nevytváří invalidation event.

`KCML_OWNER_API_KEY` používá navíc singleton `owner_api_credential`. Credential je singleton authority head třídy B3 a zamyká se před idempotency row; `SECRET_RECORD` je následný aggregate root ordinal 20. Credential row má composite FK `(secret_id,secret_version_id)` na version téhož secretu a DB trigger/check ověřuje stable name `KCML_OWNER_API_KEY`.

Rotace OWNER API klíče je jedna transakce, která současně a v tomto fyzickém pořadí:

- ověří rotation idempotency a expected credential/secret state versions,
- vloží novou immutable secret version ve stavu `CREATED`,
- změní previous version `ACTIVE → RETIRED` a potom novou version `CREATED → ACTIVE`,
- přepne `secret_record.active_version_id` a zvýší secret epoch,
- přepne `owner_api_credential.secret_version_id`, verifier hash, fingerprint a `credential_version`,
- zvýší credential `state_version` a credential activation epoch,
- uloží canonical rotation outcome, invalidation events, audit a outbox.

Neexistuje commit, v němž active Secret version a verifier ukazují na různé hodnoty. Deferred constraint trigger `kcml_secret_active_pointer_consistency` po každé změně ověří, že `secret_record.active_version_id` je právě jediná `ACTIVE` version téhož secretu; pro reserved OWNER key navíc ověří shodu `owner_api_credential.secret_version_id`, credential version/epoch a verifier fingerprintu.

Authentication OWNER API klíčem a durable acceptance `domain_command` mají společný krátký linearizační kontrakt:

1. transaction po platform/deployment heads zamkne `owner_api_credential FOR SHARE`,
2. načte current verifier/version/epoch a provede bounded constant-time verifier check bez externího I/O,
3. při úspěchu následně claimne idempotency row a ve stejné transakci vytvoří immutable `OWNER_FULL` execution context snapshot, append-only usage/audit evidence, command acceptance a queue/outbox,
4. commitne; teprve tento commit znamená přijetí requestu. `last_used_at` a souhrnná usage metadata jsou odvozená projection z append-only evidence a nezpůsobují upgrade shared credential locku při každém callu.

Rotation transaction potřebuje `owner_api_credential FOR UPDATE`, a proto nastane právě jedno pořadí: command acceptance commitne před rotací a může doběhnout pod pinovaným contextem, nebo rotace commitne první a starý verifier již nový command nepřijme. Stav „starý klíč ověřen, ale command ještě není persistovaný a rotace mezitím commitla“ není přípustný.

Reveal klíče pod OWNER session je read operation nad current locked snapshotem a audit eventem. Ztracená rotation response se neopakuje s novou hodnotou; stejný idempotency key vrátí původní outcome a OWNER session může revealnout jedinou active version.

### 51.21 Approval, checkpoint a resume

Approval request ukládá non-null `decision_scope_digest` z decision kind, exact target, argument digest a checkpoint ID. Nejvýše jeden `PENDING` approval v tomto scope vynucuje partial unique `(root_run_id,decision_scope_digest) WHERE status = 'PENDING'`; full digest odstraňuje nullable-column ambiguity.

Approval decision transaction:

1. claimne decision idempotency,
2. zamkne root run a approval row `FOR UPDATE` v parent/child pořadí,
3. získá `db_now`, ověří pending, expiry, exact argument/checkpoint digest, expected approval a run state versions,
4. zapíše `APPROVED` nebo `REJECTED`, immutable decision evidence a run transition,
5. při approval vytvoří právě jednu resume reservation/queue item,
6. uloží checkpoint pointer, event, audit a outbox,
7. commitne.

Stejný decision digest replayuje outcome. Opačné nebo změněné rozhodnutí po prvním commitu je `APPROVAL_ALREADY_DECIDED`. Expiry worker používá stejný root/child lock a závod rozhodne první commit.

Checkpoint je immutable a unique `(parent_kind,parent_id,checkpoint_sequence)`. Parent root pod lockem alokuje sequence. Pointer na latest checkpoint se mění v téže transakci jako state transition, který checkpoint reprezentuje. Checkpoint obsahuje parent state version před/po, fence, incarnation, deployment epoch, activation/binding/revision snapshot a digest chain.

Resume transaction zamkne parent, approval/input/task relation a latest checkpoint, ověří všechny current snapshot digests a teprve potom enqueue successor. Serialized SDK state bez těchto DB guards není resume autorita.

### 51.22 MCP request, call, MRTR, subscription, task a state handle

#### 51.22.1 Transport request admission a JSON-RPC ID

`mcp_request_event` se vloží až po bounded byte/JSON parse a obsahuje exact request-ID type/value, source/access fingerprint, method, headers/body digests a processing stage. Request s neplatným framingem, duplicate JSON memberem nebo ID shape může mít rejection evidence, ale nesmí vytvořit `mcp_call_run`.

Active request ID chrání partial unique index ekvivalentní:

```sql
UNIQUE (source_access_fingerprint, request_id_type, request_id_canonical)
WHERE is_jsonrpc_request = true
  AND final_response_persisted_at IS NULL;
```

String a integer ID mají rozdílný `request_id_type`; canonical text nekolabuje `"1"` a `1`. Reservation insert je linearizační bod duplicate-in-flight detekce. Unique violation se mapuje na `MCP_DUPLICATE_IN_FLIGHT_REQUEST_ID`; druhý request nevytvoří call ani side-effect intent. Po final response se reservation považuje za uzavřenou a stejné transport ID lze použít pro nový request.

HTTP status, JSON-RPC error/result, raw response digest a `final_response_persisted_at` se commitují společně. Delivery marker je append/update evidence a nemění canonical outcome.

#### 51.22.2 MCP call root a dispatch

`mcp_call_run` je aggregate root s unique logical operation a idempotency relation. Queue distribution nejprve atomicky claimne queue item, business concurrency claim a queue lease podle 51.14.

T1 prepare/intent transakce:

1. zamkne případný parent `AGENT_RUN = 100`, poté `MCP_CALL_RUN = 110`,
2. ověří current queue/concurrency fences, platform/deployment incarnation, cancellation, deadline, server/tool revision/release, binding-set revision, activation epoch, schema/contract digest a request event,
3. provede `RECEIVED → CLAIMED|EXECUTING`,
4. vloží pre-side-effect checkpoint, immutable attempt, attempt-state a případný side-effect intent,
5. rezervuje dispatch-authority outbox,
6. uloží event/audit a commitne.

Bezprostředně před runtime/provider invocation musí uspět fresh D transakce 51.28, která přejde exact operation/attempt do `DISPATCHING`. T1 commit sám external invocation neopravňuje. DB lock se během handleru/HTTP/provider callu nedrží.

Raw handler/provider attempt outcome se ukládá jako immutable evidence. Autoritativní `SUCCEEDED|FAILED|CANCELLED|MANUAL_REVIEW` transakce zamkne parent run, call root a relevantní task/handle roots v pořadí 51.6; ověří current fence, cancellation version, binding/revision/activation snapshot, pending input/task set a reconciled side effects. Validated result/error digest, post-checkpoint, idempotency terminal/current outcome, event, audit a response materialization/outbox se commitnou společně.

#### 51.22.3 MRTR exchange a input items

`mcp_input_exchange` má nejméně:

```text
UNIQUE (call_run_id, exchange_sequence)
UNIQUE (request_state_lookup_digest)
partial UNIQUE (call_run_id, consume_idempotency_key_digest)
  WHERE consume_idempotency_key_digest IS NOT NULL
```

`mcp_input_request_item` má `UNIQUE(exchange_id, request_key)`. `mcp_input_response_item` má unique accepted relation k `(input_request_item_id, accepted_digest)` a disposition pro ignored/invalid attempts.

MRTR creation transaction zamkne call root, ověří supported method, capability a pre-side-effect checkpoint, vloží exchange/items, nastaví call `WAITING_FOR_INPUT`, checkpoint/event/audit a response materialization. InputRequiredResult se nesmí emitovat před commitem.

Respond transakce:

1. zamkne call root a exchange, poté input item rows v request-key byte orderu,
2. získá `db_now` a ověří nonterminal exchange, expiry, source/access context, method, original argument digest, revision/binding/epoch a requestState integrity,
3. classified unknown/already-satisfied keys vloží jako ignored evidence bez změny business state,
4. validní known subset vloží pod unique constraints a označí satisfied,
5. při partial setu přejde na `PARTIALLY_FULFILLED` bez successor queue,
6. při úplném setu provede `FULFILLED → CONSUMED`, update parent call/checkpoint a právě jednu successor reservation/queue item v jednom commitu,
7. uloží event/audit/outbox a commitne.

Exact retry se stejným accepted aggregate digestem replayuje current/terminal outcome. Jiný recognized digest po consume, changed original args, wrong context nebo expiry se odmítne. Expiry worker používá stejné locky; race rozhodne první commit.

#### 51.22.4 Subscription

`mcp_subscription` je mutable child `MCP child ordinal 70` pod owner server/component relation a má unique active `(source_access_fingerprint, request_id_type, request_id_canonical)` již kryté request-event reservation.

Open transaction vloží subscription, requested filter a persisted acknowledgment subset. Acknowledgment sequence `1` je rezervovaná a check constraint zakazuje vložit jinou subscription notification s nižší nebo stejnou sequence. `OPENING → ACTIVE` nebo `OPENING → GRACEFUL_CLOSING` a acknowledgment outbox jsou jeden commit.

Notification insert ověřuje under subscription row lock, že subscription je `ACTIVE`, method/resource/task patří acknowledged filteru a sequence je next. Delivery failure nemění authoritative source state. Client disconnect/cancel a server graceful closure používají odlišné close reasons. Graceful final response materialization se commitne před close-delivery outboxem; abrupt close finální response nevyrábí.

#### 51.22.5 Durable Task

`mcp_task` je aggregate root `MCP_TASK = 115`, má unique public lookup digest a stable relation k original call/logical operation. Create transaction zamkne parent call `110` před task insert/root `115`, vloží task, initial event sequence, queue item a response materialization v jednom commitu. CreateTaskResult se nesmí emitovat dříve.

Task input request key má `UNIQUE(task_id, request_key)` bez recyklace. Accepted response má unique relation k task/request key a canonical digest. Unknown/already-satisfied/superseded keys se ukládají s ignored disposition, nikoli jako accepted state mutation.

`tasks/update` transaction:

1. zamkne parent call, task root a input children v canonical pořadí,
2. získá `db_now`, ověří current Tasks capability, source/access/binding/revision/activation, nonterminal state a expiry,
3. accepted known subset persistuje pod unique constraints,
4. consume current pending requests a případný `INPUT_REQUIRED → WORKING` přechod provede pod task `state_version`,
5. rezervuje nejvýše jeden resume successor,
6. uloží ACK materialization, event, audit a outbox v jednom commitu.

ACK může být doručen dříve, než worker vykoná resume; to není nekonzistence. Client neposílá authoritative expected task stateVersion.

`tasks/cancel` zamkne task root a persistuje idempotent cancellation intent/version a ACK. Nezapisuje automaticky `CANCELLED`. Completion/failure/cancellation worker získá stejný root lock, znovu ověří pending side effects a první validní terminal commit vítězí. Cancel ACK proto může následovat terminal `COMPLETED` nebo `FAILED`. Po cancel intentu nesmí nový dispatch projít fresh D transakcí; již dispatchnuté effects se reconciliují.

Task completion transaction ukládá:

- `COMPLETED` s exact original method result, včetně tool `isError=true`,
- `FAILED` pouze s JSON-RPC error,
- `CANCELLED` pouze po known outcomes a cleanup.

Task result/error, task event, parent/current relation, idempotency outcome podle contractu, audit a notification outbox jsou jeden commit.

TTL/expiry worker používá `db_now`, parent-call-before-task lock order a fenced expiry claim. Nonterminal task nejprve dostane expiry/cancel intent, poté se reconciliuje a terminalizuje. Terminal/tombstone retention a purge jsou oddělené; public lookup digest zůstává rezervovaný do purge a audit/evidence retention.

#### 51.22.6 State handle

State handle close/use je guarded CAS nad `MCP_STATE_HANDLE = 116` root `state_version`. Pokud operation současně mění originating call/task, lock order je call `110` → task `115` → handle `116`.

Handle use transakce znovu ověří source/access context, owner tool/revision, contract/binding digest, activation epoch, namespace, status a `expires_at > db_now`. `OPEN → CLOSED` nebo `OPEN → EXPIRED` je terminal. Row se nesmaže ani znovu nepoužije; public handle digest a generation nonce zůstávají never-reused. Handle sám není credential a nevynechává public/external/internal access validation.

### 51.23 Agent run, model call, session a memory

Agent run root, model call, tool call a handoff attempts používají parent-first lock order. Jeden `ai_model_call` odpovídá jednomu OpenAI create-class attemptu. Request descriptor, local idempotency claim, submit intent a authority outbox se commitují před SDK requestem; síťové volání neprobíhá pod DB lockem.

Pre-dispatch transaction zamkne run a model-call root, ověří current fence, cancellation, deadline, budget, capability snapshot a descriptor digest a nastaví `SUBMITTING/DISPATCH_STARTED`. Production `openai` create používá `maxRetries: 0`. Po tomto commitu se call bez `response.id` nesmí znovu submitnout, dokud není prokázáno `CONFIRMED_NOT_SUBMITTED`; jinak vzniká `MODEL_SUBMIT_OUTCOME_UNKNOWN`.

Provider response ID se zapisuje guarded insert/update a je unique. Provider request ID je diagnostické pole bez recovery autority. Eventy používají unique local sequence a, je-li dostupná, unique `(provider_response_id, provider_sequence)`; stejná sequence s jiným digestem je hard conflict. Output item index, content-part index a provider `call_id` mají unique constraints podle 52.34.

Model output evaluation transaction zamkne run a model call, uloží complete ordered output items a vytvoří unique tool/handoff reservations. Function tool outcome, function-call-output payload, post-tool checkpoint a právě jedna `ai_model_continuation`/successor reservation se commitují atomicky. Duplicate retrieve stejného response/output digestu je no-op; konfliktní digest, call ID nebo continuation generation je odmítnut.

Session append:

1. zamkne session root `FOR UPDATE`,
2. ověří expected session state version,
3. alokuje contiguous session sequence,
4. vloží items s unique `(session_id,sequence)` a unique `(session_id,source_run_id,source_item_key)`,
5. aktualizuje session version.

Run `SUCCEEDED` a jeho povinné canonical session/memory writes jsou jedna transakce. Tato multi-root transakce zamkne `AGENT_RUN = 100`, potom podle typu právě jeden nebo více `AGENT_SESSION = 105` a `SYSTEM_CHAT_CONVERSATION = 106` rootů a potom všechny `AGENT_MEMORY_NAMESPACE = 107` rooty, v každé třídě podle UUID; až poté zamyká session/memory child rows. Pokud mění také agent definition pointer, `AGENT_DEFINITION = 60` se zamkne před runem. Memory item má unique source-run/namespace/key dedupe a exact item digest. Derived vector indexace nebo external memory sink se enqueueuje outboxem a není součástí canonical success podmínky, pokud revision výslovně neurčuje opak.

### 51.24 Browser session, page/frame/document, control, action a account state

Browser session root drží current lifecycle, `control_epoch`, `last_control_fencing_token`, runtime-build/host-or-bridge/context generation, active page/page generation/document epoch, account/auth epoch, latest confirmed browser-state pointer a state version.

`browser_control_lease` je history row; partial unique index zajišťuje nejvýše jeden row se `state = 'ACTIVE'` per session. Časový predicate se v partial indexu nepoužívá. Expiry se materializuje guarded transakcí podle DB času.

Agent-driven browser reservation, která současně mění více rootů, používá pořadí:

```text
BROWSER_SESSION = 70
BROWSER_ACCOUNT_BINDING = 75
BROWSER_AUTOMATION_DEFINITION = 80
AGENT_RUN = 100
BROWSER_AUTOMATION_RUN = 125
CLEANUP_OPERATION = 150
```

Volitelné rooty se zamykají pouze pokud jsou součástí operation a vždy v ordinal/UUID pořadí. Již zamknutý agent/automation run nikdy nesmí dodatečně žádat dříve řazený browser/account root.

#### 51.24.1 Session, host/context a browser identity observations

Session create transaction:

1. claimne client/business idempotency,
2. zamkne parent root podle globálního pořadí a případný account binding,
3. vloží `browser_session` ve `CREATING` s immutable requested target/runtime-build capability a operation scope,
4. vloží právě jeden host/bridge assignment queue item/outbox,
5. uloží event/audit a commitne.

Host/context attach transaction zamkne session root, ověří current platform/deployment epoch, ready runtime build manifest, host/bridge generation, assignment fence a lifecycle. Inkrementuje `context_generation`, vloží immutable `browser_context_instance`, přepne current pointer a uloží readiness event. Raw runtime handle je pouze encrypted/opaque child evidence; uniqueness a authority vzniká serverovým session/context generation.

Page, frame, document a navigation observations jsou append-first evidence. Current projection se mění guarded update pouze při exact current host/bridge/context generation a vyšší source sequence. Povinné constraints:

```text
UNIQUE (session_id,page_id,page_generation)
UNIQUE (page_id,page_generation,frame_id,frame_attachment_epoch)
UNIQUE (frame_id,frame_attachment_epoch,document_epoch)
UNIQUE (session_id,source_generation,source_sequence)
UNIQUE (page_id,page_generation,navigation_sequence)
```

Page/frame/document IDs se nerecyklují. Same source sequence s jiným digestem je protocol conflict. Stale observation se uloží, ale current session/page/frame projection neaktualizuje.

#### 51.24.2 Control takeover, return a input-state reset

Takeover/return/recovery transaction:

1. claimne operation idempotency,
2. zamkne involved rooty podle 51.6: session, případný account a cleanup,
3. teprve potom zamkne current active lease, control-transfer row, pending mutating action a input-state child rows,
4. získá `db_now`, materializuje expired active lease a ověří current context generation,
5. při unknown mutujícím outcome nastaví transfer `MANUAL_REVIEW` a neudělí nový lease,
6. označí previous lease `REVOKED|EXPIRED|RELEASED`,
7. vloží/ověří input-state reset checkpoint,
8. zvýší session `control_epoch` právě o jedna a `last_control_fencing_token` právě o jedna,
9. vloží nový active lease s právě alokovaným fence; `MAX(fencing_token)+1` je zakázané,
10. uloží checkpoint, event, audit a preview/control invalidation outbox,
11. commitne.

Expired active row se nejprve označí `EXPIRED`; teprve potom se vloží nový active row. Partial unique index zůstává konečným one-writer arbiterem.

`browser_input_event` má:

```text
UNIQUE (session_id,control_epoch,client_sequence)
CHECK (client_sequence >= 0)
```

Acceptance transaction zamkne session a active lease, ověří holder/fence/expiry, exact context/page/frame/document/observation/frame/viewport identity a focus/target freshness, vloží input event a podle typu právě jednu browser action reservation. Input se nedispatchne, dokud tento commit neexistuje. Duplicate same sequence/digest replayuje acceptance; different digest je conflict.

#### 51.24.3 Browser action prepare, intent a dispatch

Browser action používá business idempotency scope deklarovaný operation/revision contractem. Prepare transakce:

1. zamkne idempotency row a rooty session → account → definition → automation/agent run podle pořadí,
2. zamkne active control lease, current context/page/frame/document projection, operation scope, target reference a relevant confirmation/challenge,
3. získá account/resource/external-session concurrency claims v class F,
4. ověří lifecycle, cancellation, deadline, runtime build/host-or-bridge/context generation, control fence, page/frame/document/viewport freshness, account/tenant/auth epoch, exact binding, target uniqueness/actionability a pre-armed waiter contract,
5. vloží immutable `browser_action_run`/attempt a pre-checkpoint,
6. vloží `side_effect_operation` a immutable side-effect attempt i pro action, jejíž mutace může začít input/change/blur/upload/dialog/permission/device triggerem,
7. vloží právě jeden dispatch outbox s exact target/input/action schema and mutation trigger,
8. přepne action do `INTENT_RECORDED`, parent step do waiting/dispatching state,
9. vloží event/audit a commitne.

Žádný browser/bridge input nesmí být odeslán před T1 commitem. DB lock se nedrží během Playwright, bridge, page wait, download nebo reconciliation.

Queue/worker claim podle 51.14 ověří fresh current session/context/control/action fence bez změny business idempotency. Bezprostředně před host/bridge sendem provede bounded fresh authority transaction nebo guarded read podle profile 51.15. Stale authority terminalizuje attempt bez dispatchu.

#### 51.24.4 Dispatch phases a stale response

`browser_action_dispatch_event` má:

```text
UNIQUE (action_attempt_id,phase_sequence)
UNIQUE (action_attempt_id,phase)
CHECK (phase_sequence > 0)
```

Phase append transaction zamkne action root/attempt, ověří current action fence, host/bridge/context generation a monotonic allowed transition. Exact duplicate phase/digest je no-op; different digest je hard conflict. Phase `MUTATION_TRIGGER_POSSIBLY_ISSUED` je monotonic recovery barrier a pozdější event ji nemůže snížit nebo odstranit.

Late browser host/bridge může vložit raw diagnostic evidence do oddělené append-only tabulky, nesmí však změnit action, session/page/frame projection, state bundle pointer, idempotency outcome ani parent run.

Page/frame/document events vyvolané actionem se připojí pouze při exact action causation/fence nebo bounded adapter correlation. Event z jiné page/action může aktualizovat obecnou page projection, ale není success evidence current action bez postcondition contractu.

#### 51.24.5 Postcondition, reconciliation a terminal outcome

Po external/browser fázi outcome transaction zamkne action root, parent step/run a relevantní account/resource roots v globálním pořadí, potom side-effect/attempt rows. Ověří fence, cancellation, expected state versions a exact postcondition/read-back digest.

- Validní postcondition uloží `CONFIRMED_APPLIED`, canonical action result a terminal `SUCCEEDED`.
- Exact evidence před mutation triggerem nebo read-back `CONFIRMED_NOT_APPLIED` uloží known not-applied outcome a dovolí contract-defined retry/new attempt.
- Nejednoznačný read-back uloží `UNKNOWN`, action/run `MANUAL_REVIEW` a ponechá account/resource claim v blocking state.

Action terminal write, idempotency observable outcome, step terminal write, parent current-step advance/successor reservation, domain event, audit a outbox jsou jedna transakce. Nelze commitnout step success bez action/postcondition resultu ani action success bez parent-visible outcome.

Manual resolution transaction zamkne action, side-effect, parent run a blocking concurrency claim, ověří expected state version a evidence digest a provede jediný allowed applied/not-applied/failure/cancel transition. Dvě různá OWNER rozhodnutí nemohou obě commitnout.

#### 51.24.6 Account binding, auth epoch a state bundle

`browser_account_binding` root drží current auth epoch, expected/observed account/tenant, active state-bundle pointer, credential/certificate/virtual-authenticator versions, concurrency mode a state version.

Login/logout/credential rotation/account switch/state capture, které mohou invalidovat external session family, zamykají account root. Auth epoch se inkrementuje atomicky s invalidation eventem. Stale session/bundle s nižším auth epoch nemůže provést nový mutující action nebo aktivovat bundle.

Browser state bundle candidate a members jsou immutable. Capture candidate může vzniknout mimo pointer transaction jako encrypted content-addressed blob, ale aktivace pointeru zamkne:

1. relevantní credential/certificate `SECRET_RECORD = 20` roots podle UUID,
2. `BROWSER_SESSION = 70`,
3. `BROWSER_ACCOUNT_BINDING = 75`,
4. current candidate bundle/member rows.

Transakce ověří secret activation/invalidation epochs, credential/certificate versions, account-binding state/auth epoch, runtime-build/serializer capability, origin/member inventory a authenticated-condition digest. Bundle vytvořený proti retired/invalidated credential nebo stale auth epoch se neaktivuje. Dvě candidate activace nemohou obě přepsat pointer.

Standardní DB constraint/validator odmítne forbidden standard member kinds `RAW_PROFILE`, `BROWSER_CACHE`, `SERVICE_WORKER_RUNTIME`, `PLATFORM_PRIVATE_KEY`, pokud nejde o explicitní versioned provider-adapter member type; platform passkey/private key nikdy není server bundle member.

#### 51.24.7 Bridge connection, assignment a profile lease

Bridge certificate generation a current connection epoch jsou mutable root state bridge objektu. New connection transaction ověří mTLS fingerprint/build/capability, zamkne bridge root, terminalizuje previous current connection, inkrementuje connection epoch a vloží nový active connection. Partial unique zajišťuje právě jedno current connection per bridge/certificate generation.

Session assignment zamkne session → account → bridge roots a potom connection/profile child rows. Ověří exact required capabilities, local-target binding, account/auth epoch a current connection. Profile lease používá partial unique právě jednoho active writer per `(bridge_id,profile_stable_id)` a monotonic fence. Lease založený na neověřeném profile/process identity se nevydá.

Revocation/rotation transaction inkrementuje certificate generation, revokuje current connection/assignments/profile leases, vloží disconnect outbox a zablokuje new dispatch. Pending action po possible mutation triggeru zůstává v reconciliation; není terminalizován jako not-applied pouze kvůli connection close.

#### 51.24.8 Download, upload a cleanup ordering

Upload handle má unique owner/digest/policy scope a consumed CAS. Filechooser acceptance zamkne upload handle a action, ověří page/frame/document/control identity a atomicky označí handle consumed s action dispatch reservation.

Download start je append evidence, nikoli terminal success. `browser_download` state transition na `COMPLETED` vyžaduje persisted artifact, size/digest a expected content/origin verification. Context cleanup nesmí odstranit temp download před terminal download/persistence decision.

Browser cleanup root používá 51.26. Completion transaction navíc ověří `NOT EXISTS`:

- active control lease/preview ticket/bridge assignment/profile lease,
- nonterminal action/side-effect/reconciliation,
- pending challenge/dialog/permission,
- unfinished upload/download,
- live context/page/profile resource,
- unreleased account/resource/capacity claim,
- temporary virtual authenticator/client-certificate/init-script/route/artifact resource.

Teprve potom lze cleanup označit `COMPLETE` a session closure zahrnout do global closure reportu.

### 51.25 Audit sequence a hash chain

`audit_head` je singleton s `last_sequence bigint`, `last_hash bytea`, `chain_format_version`, `CHECK (last_sequence >= 0)` a `CHECK (octet_length(last_hash) = 32)` a state version. Bootstrap hodnota je přesně `last_sequence = 0`, `last_hash = decode(repeat('00',32),'hex')`, `chain_format_version = 1`; první event používá tento zero hash jako previous hash. Každá auditovaná domain transaction zamkne `audit_head FOR UPDATE` až po všech ostatních rows.

Append provede:

1. `next_sequence = last_sequence + 1`,
2. sestaví immutable canonical audit bytes obsahující next sequence, previous hash, event/actor/object, before/after digests, correlation/causation/trace a DB timestamp,
3. vypočte `event_hash = kcml_audit_hash(chain_format_version,previous_hash,next_sequence,canonical_bytes)`,
4. vloží `audit_event` s `UNIQUE (chain_sequence)` a `CHECK (octet_length(event_hash) = 32)`,
5. aktualizuje head na next sequence/hash,
6. vloží archive outbox právě tehdy, když active audit-retention policy revision požaduje external archive pro danou `chain_sequence`; jinak uloží explicitní `archive_required = false` v audit delivery projection,
7. commitne společně s domain mutation.

`kcml_audit_hash` je versioned immutable DB function s test vectors; změna hash formátu vytváří explicitní chain-format revision, nikoli tichou změnu.

Audit row je immutable. `audit_event` je buď nepartitionovaná, nebo range-partitionovaná podle `chain_sequence`, aby PostgreSQL mohl fyzicky vynutit globálně unikátní sequence; time-only partitioning bez sequence partition key se pro canonical chain nepoužívá. Unpartitioned `audit_head` zůstává jediným allocator headem.

Po získání class I `audit_head` už transakce nesmí zamknout ani měnit žádný dříve existující A–H row. Povoleny jsou pouze append-only inserty nového `audit_event` a jeho nového `audit_archive_outbox` row s právě alokovanou sequence/event ID; jejich keys nemohou kolidovat s jinou transakcí. Pozdější archive delivery claim zachází s existujícím archive-outbox row jako s class H ordinalem `910`. Archive delivery může být at-least-once, ale canonical chain v DB se nemění podle úspěchu archivu.

### 51.26 Cleanup operation a orphan-free completion

Každý cleanup má root `cleanup_operation` a normalizované `cleanup_resource` rows pro runtime, process/cgroup, socket, route, binding, filesystem path, external side effect, browser context, secret/session invalidation a jiný cleanup target. Resource row má stable key, desired terminal condition, state, state version, attempt relation a evidence digest.

Povinné constraints:

```text
UNIQUE (cleanup_operation_id, resource_kind, stable_resource_key_digest)
UNIQUE (cleanup_operation_id, step_ordinal)
partial UNIQUE (owner_kind,owner_id,cleanup_kind)
  WHERE terminal_at IS NULL
```

External cleanup krok používá `side_effect_operation`; DB transakce pouze commitne intent/dispatch. Outcome/read-back se uloží následně. Resource se stane `COMPLETE` pouze s potvrzenou postcondition evidence.

Cleanup root lze přepnout na `COMPLETE` pouze v transakci, která:

1. zamkne owner aggregate root a cleanup root v globálním aggregate ordinal/UUID pořadí,
2. zamkne všechny non-terminal cleanup resource rows v stable pořadí,
3. ověří `NOT EXISTS` non-terminal resource, pending side effect, current pointer/binding/route/runtime reference a aktivní child process/cgroup evidence,
4. ověří, že pro owner nevznikla nová resource po cleanup barrieru,
5. uloží completion checkpoint, event, audit a případný successor enqueue,
6. commitne.

Owner root lock je guard proti phantomu nového remediation candidate/resource. Nový candidate pro stejný owner scope musí získat stejný root lock a ověřit cleanup terminal state.

### 51.27 Operational configuration, circuit breaker, monitoring a alerty

Desired configuration update zamkne všechny dotčené `OPERATIONAL_SETTING = 140` rooty podle UUID, ověří expected state/desired version, vloží immutable desired revision a potom vloží/uzamkne `CONFIGURATION_APPLY_RUN = 145`; v jednom commitu přepne desired pointers a vytvoří právě jeden apply run a queue/outbox.

Per-service effective acknowledgement je observation s exact desired version, deployment/activation epoch a source sequence. Stale ACK se uloží jako evidence, ale guarded projection update ovlivní nula rows.

Circuit breaker mutable fields jsou součástí `EXTERNAL_TARGET = 45` root row; před každou failure/success/probe transition se zamkne tento external-target root. `HALF_OPEN` probe používá persistentní claim row a partial unique právě jednoho active probe. Probe external call má side-effect intent; success/failure transition a release probe claimu jsou jeden DB commit po outcome.

Alert dedupe používá unique canonical alert key. `INSERT ... ON CONFLICT DO NOTHING`, následný row lock a guarded occurrence increment zabraňuje ztraceným increments. Alert delivery je outbox/queue workflow s idempotency per `(alert_id,channel,delivery_generation)`.

Monitoring heartbeat/probe projections používají source-specific monotonic sequence nebo server receive sequence. Update current projection obsahuje `WHERE incoming_sequence > current_sequence` a exact epoch/release guards. Stale observation se nikdy nestane current ani při pozdějším DB receive timestampu.

### 51.28 External side effect, intent, dispatch a reconciliation transakce

Každý external effect je rozdělen nejméně do tří samostatných DB transakcí a jedné external fáze:

**T1 — prepare/intent**

- podle 51.6 získá platform/deployment heads class B, idempotency row class C, relevantní activation-domain heads class D a všechny parent/root rows class E,
- ověří nebo vytvoří exact mutating admission relation `ADMITTED` s pinned epoch,
- podle immutable `concurrencyClaimPoint` buď získá required concurrency claims v class F, nebo připraví claimable dispatch work pro pozdější `WORKER_CLAIM`; nikdy obojí,
- ověří preconditions, cancellation, lease/fence a exact bindings,
- uloží pre-checkpoint,
- vloží unique mutable `side_effect_operation`, immutable `side_effect_attempt`, jeho počáteční `side_effect_attempt_state = INTENT_RECORDED` a immutable request evidence,
- vloží právě jeden canonical `SIDE_EFFECT_DISPATCH` outbox pro tento attempt; immutable attempt již obsahuje exact target idempotency key a deferred composite reference na tento outbox,
- parent přejde do waiting/dispatchable stavu,
- event, audit a všechny uvedené rows se commitnou společně.

**D — fresh pre-dispatch DB transakce**

Outbox delivery claim je samostatná krátká class-H claim transakce podle 51.14. Samotnou external autoritu vytváří až nová `WORKER_COMMIT` transakce, která nesmí začít s již drženým outbox row lockem a získá locks znovu od nižších tříd:

1. zamkne platform/deployment heads class B,
2. zamkne relevantní `activation_domain_head` rows class D `FOR SHARE` v canonical pořadí a ověří exact pre-barrier admission relation, pinned epoch a že logical operation smí jako již přijatá mutace pokračovat; nevytváří novou admission,
3. zamkne všechny parent aggregate roots class E,
4. zamkne a ověří přesný current set `concurrency_claim` rows class F, pokud je operation používá,
5. zamkne exact `domain_command_activation_domain` child ordinal `40`, potom `side_effect_operation` ordinal `140`, `side_effect_attempt_state` ordinal `150` a authority `transactional_outbox` ordinal `910`; relation musí být `ADMITTED|CHECKPOINTED` pro tentýž command/domain/pinned epoch,
6. získá nový `db_now` a ověří současně parent state/version, cancellation, binding/revision/activation snapshot, immutable attempt/request digest, operation current-attempt sequence, outbox purpose/authority ID, delivery owner/fence, attempt fence, platform incarnation, deployment epoch a `lease_expires_at - db_now >= min_dispatch_authority_window`,
7. pokud je kterákoli required lease již expirovaná (`lease_expires_at <= db_now`), rollbackne bez prodloužení a bez external callu; pokud je stále active, ale window nestačí, guarded ji prodlouží pod stejnými current owner/fences a podmínkou `lease_expires_at > db_now`, potom znovu ověří výslednou expiry; jakýkoli nulový CAS rollbackne,
8. atomicky přejde operation i attempt-state `INTENT_RECORDED → DISPATCHING`, zvýší jejich `state_version`, pod locked attempt-state headem alokuje další evidence sequence, uloží immutable `DISPATCH_PREPARED` evidence, adapter/provider request ID a dispatch timestamp a označí outbox delivery jako `DISPATCH_AUTHORIZED`,
9. commitne. Pouze úspěšný commit tohoto exact attemptu opravňuje následující external request.

Retry, který nalezne attempt-state `DISPATCHING`, nesmí znovu provést krok 7 ani odeslat další request naslepo. Přejde do outcome retrieve/read-back/reconciliation. Nový dispatch je možný pouze novým immutable attemptem po potvrzeném `CONFIRMED_NOT_APPLIED`, nebo exact same-key retry větví výslovně povolenou provider kontraktem.

**E — external execution mimo DB transakci**

- teprve po D commitu worker provede provider/HTTP/browser/systemd/filesystem efekt s immutable target idempotency key a adapter request ID,
- během external callu nedrží PostgreSQL row ani advisory lock,
- raw response/error/read-back data drží pouze do T2; process memory sama není autorita.

**T2 — raw outcome**

- zamkne platform/deployment heads class B, relevantní activation-domain heads class D `FOR SHARE`, všechny parent roots class E, current concurrency claims class F a potom class H rows v přesném pořadí: admission relation ordinal `40`, `side_effect_operation` `140`, `side_effect_attempt_state` `150` a relevantní delivery row `910`,
- ověří exact admission/pinned epoch, current parent/attempt/delivery fences, incarnation, deployment epoch, request digest a state `DISPATCHING|RECONCILING`,
- vloží append-only immutable `side_effect_attempt_evidence` s raw response/error/read-back digestem,
- guarded přejde attempt-state a operation na `OUTCOME_RECORDED` nebo `RECONCILING`; při reconciliation současně přejde admission relation na `RECONCILING`,
- enqueue reconciliation, event, audit a required outbox se commitnou společně, pokud outcome není jednoznačný,
- stale worker smí uložit pouze oddělenou neautoritativní diagnostiku; canonical T2 update ovlivní nula rows.

**T3 — canonical confirmation/terminal**

- zamkne platform/deployment heads class B, terminalizovaný `domain_idempotency_record` class C, relevantní activation-domain heads class D přímo `FOR UPDATE`, všechny parent roots class E, current concurrency claims class F, případné dedicated sequence allocators class G a potom všechny class H rows podle ordinalu: admission relation `40`, doménové checkpoint/successor children, `side_effect_operation` `140`, attempt-state `150`, queue `900` a outbox `910`; `audit_head` class I získá poslední,
- pod domain-head locks znovu ověří pinned epoch a v témže commitu přejde každá příslušná mutating admission relation právě do `TERMINAL` a aktualizuje její kontrolní projection, takže activation barrier nevidí falešné dokončení ani phantom,
- uloží právě jeden `CONFIRMED_APPLIED|CONFIRMED_NOT_APPLIED|FAILED_FINAL|UNKNOWN` v operation i attempt-state projekci a immutable confirmation evidence,
- při known outcome uloží post-checkpoint, parent transition, idempotency outcome, event/audit, release concurrency claimu a successor v jednom commitu,
- při `UNKNOWN` vytvoří manual-review blocker, ponechá potřebné claim/barrier semantics podle contractu a zakáže další dispatch.

Crash mezi T1, D, external requestem, T2 a T3 je rozlišitelný podle immutable attemptu, attempt-state, dispatch-authority outboxu a evidence. External response v process memory se nikdy nepovažuje za canonical outcome bez T2/T3 commitu.

### 51.29 Forward-only, rollback-compatible migrations

Greenfield baseline obsahuje všechny objekty této kapitoly. Další schema evolution je sekvenční, checksumovaná a forward-only. Application rollback znamená návrat na předchozí application release nad forward-compatible schema; databázové down migrations se v produkčním rollbacku automaticky nespouštějí.

`schema_migration` obsahuje UUID, version, immutable checksum, release/build, state `PLANNED|RUNNING|APPLIED|FAILED|MANUAL_REVIEW`, transaction mode, started/completed timestamps a evidence. Deployment controller pod krátkým xact advisory namespace `SCHEMA_MIGRATION` zamkne platform/deployment heads, potom `DEPLOYMENT_RUN = 130` a `SCHEMA_MIGRATION = 170` v class E pořadí, atomicky claimne persistentní migration/deployment lease a ověří checksum. Jediného migratora po dobu kroku vynucuje tento persistentní lease, fencing token, partial unique active migration a systemd deployment singleton; advisory lock se přes commit ani přes non-transactional DDL nedrží.

Každá migration revision deklaruje immutable ordered `phasePlan`. `EXPAND`, `VALIDATE` a `ACTIVATE` jsou povinné pro každou schema změnu ovlivňující běžící application release; `MIGRATE` je povinné při transformaci existujících rows; `CONTRACT` je povinné při odstranění nebo zpřísnění schema po rollback window. Neuvedená fáze se nesmí spustit a uvedená fáze se nesmí přeskočit. Fáze jsou:

1. **EXPAND** — nové nullable columns/tables/indexes/constraints kompatibilní s previous i candidate release,
2. **MIGRATE** — idempotentní bounded backfill s persistentním cursor/checkpointem a `SKIP LOCKED` pouze pro batch distribuci,
3. **VALIDATE** — schema fingerprint, row counts, digests, `VALIDATE CONSTRAINT`, dual-read/dual-write evidence,
4. **ACTIVATE** — application/activation pointer switch,
5. **CONTRACT** — odstranění starého schema až po skončení rollback window a prokázání nulového old-release use.

DDL podporující transaction běží v jedné migration transakci. `CREATE INDEX CONCURRENTLY`, `DROP INDEX CONCURRENTLY` a jiný příkaz zakázaný v transaction blocku je samostatný checkpointovaný migration step: předem se persistuje intent, příkaz běží mimo transaction, následná transakce ověří catalog state/index validity a uloží outcome. Failed/invalid concurrent index se deterministicky opraví nebo odstraní před pokračováním.

Velké foreign key/check constraints se přidávají `NOT VALID`, data se opraví, constraint se `VALIDATE` a teprve potom se zpřísní write path. `NOT NULL` se nastaví až po bounded backfillu a validované ekvivalentní check evidence. Unique invariant musí být před activation plně vynucen validním unique indexem; částečně postavený index není gate evidence.

Enum změna, column type rewrite, table rewrite, destructive rename/drop a změna canonical digest formátu vyžaduje explicitní compatibility plan. Runtime release se neaktivuje, pokud previous release neumí číst schema po EXPAND/ACTIVATE nebo candidate neumí číst current rows.

Migration nesmí měnit immutable business payload, přepočítat historický audit hash ani přidělit historickému eventu jinou sequence. Derived columns mohou být backfillnuty pouze s source digest/evidence.

### 51.30 Povinná transaction-boundary matice autoritativních operací

Následující operace mají nejméně tuto atomickou hranici. Sloupec T1 je autoritativní prepare/intent nebo transition commit. Sloupec D je nová krátká claim/pre-dispatch transakce bez zděděných row locků; je povinná před každým non-DB side effectem. T2/T3 jsou nové outcome/reconciliation/terminal transakce.

| Operace | T1 — jedna DB transakce | D — fresh claim/pre-dispatch DB transakce | Mimo DB transakci | T2/T3 — následující DB transakce |
|---|---|---|---|---|
| Component lifecycle command | idempotency + domain admission + component CAS + desired command + control intent/attempt + audit + authority outbox | current component/domain/lease/fence/epoch + attempt `DISPATCHING` | runtime/control command | raw ACK/read-back, reconcile, effective projection a terminal |
| Phase completion | phase terminal + job transition + next phase row/reservation + queue + checkpoint + audit | successor queue/concurrency/phase lease claim a fresh job/phase guards | next phase worker execution; jeho externí kroky mají vlastní T1/D | next checkpoint/outcome nebo terminal + další successor |
| Tool/MCP dispatch | call/run transition + checkpoint + immutable attempt/state + authority outbox | exact admission, call/parent/concurrency/delivery fences + `DISPATCHING` | handler/runtime/provider call | raw outcome evidence, reconcile, result/terminal + idempotency outcome |
| Model submit | run/model intent + checkpoint + immutable submit attempt + authority outbox | current run/model lease/fence, request digest a provider-submit attempt `DISPATCHING` | OpenAI submit/retrieve | response/output evidence, reconcile, next tool/final transition |
| Approval decision | approval terminal + run transition + právě jedna resume reservation/queue + audit | resume queue/concurrency/run claim | resumed run work; případné model/tool calls mají vlastní T1/D | checkpoint/outcome/terminal |
| MRTR/task input | exact response consume + task/call transition + resume queue | resume claim + current call/task/input/checkpoint guards | další MCP request, pokud je vyžadován | raw outcome, reconcile, task/call terminal |
| Secret activation | version lifecycle + active pointer + epochs + invalidation intent/outbox + audit | invalidation worker claim + current secret/binding/session epoch guards | dependent service/session/browser invalidation | effective ACK/read-back, reconcile a invalidation terminal |
| OWNER API-key rotation | new version + secret pointer + verifier + credential version/epoch + audit/outbox | pouze případné dependent invalidation claims; samotná rotace nemá external step | žádný nutný external effect | invalidation/evidence; rotation outcome již existuje v T1 |
| Activation switch | global epoch + všechny pointers + set/domain states + reconcile outbox + audit | per-target runtime/route reconcile claim s exact switch epoch | runtime/route apply + smoke | effective ACK, `ACTIVE`, nebo nový reverse-switch T1 a jeho D/T2/T3 |
| Browser takeover | old lease revoke + new control epoch/fence/lease + checkpoint + invalidation/host-transfer intent | current session/control/host lease a transfer attempt `DISPATCHING` | browser-host control transfer | attach/read-back/action evidence, reconcile nebo terminal |
| Browser action | input/action reservation + pre-checkpoint + intent/attempt + authority outbox | exact session/control/page/frame/target/delivery fences + `DISPATCHING` | browser input/action | raw observation, postcondition, reconcile a action terminal |
| Configuration change | desired revision/pointer + apply run + queue/outbox + audit | target apply claim + desired version/deployment/activation guards | service apply | per-service ACK/read-back + success/rollback terminal |
| Cleanup step | resource transition + checkpoint + intent/attempt + authority outbox | cleanup/resource/current-pointer/fence revalidation + `DISPATCHING` | OS/provider/browser/filesystem cleanup | read-back + resource `COMPLETE|FAILED|RECONCILING` + successor |
| Audit archive | domain audit + archive outbox | archive delivery claim/fence | archive upload | delivery evidence/marker; canonical audit chain se nemění |
| Deployment epoch switch | deployment head/release state + recovery intents/outbox + audit | exact deployment-step lease/fence a process target preflight | symlink/systemd/process/DNS/TLS krok podle deployment sagy | effective heartbeats/read-back/acceptance nebo reconcile/rollback step |

Jakýkoli implementation split, který oddělí položky uvnitř jednoho T1, D nebo T2/T3 commitu, je nevyhovující. T1 a D se nikdy nespojí přes external I/O a D nikdy nepřebírá row lock z claim transakce. Přidání dalšího external side effectu vyžaduje vlastní immutable attempt, authority outbox, D commit a reconciliation contract; nesmí se vložit do jiné DB transakce ani sdílet dispatch authority s jiným effectem.

### 51.31 Povinné DDL a constraint patterns

Greenfield schema a migrations obsahují fyzicky ekvivalentní constraints k těmto vzorům:

```sql
-- single active secret version
CREATE UNIQUE INDEX uq_secret_version_active
ON secret_version (secret_id)
WHERE lifecycle = 'ACTIVE';

-- one pending approval in exact scope
CREATE UNIQUE INDEX uq_agent_approval_pending
ON agent_approval_request (root_run_id, decision_scope_digest)
WHERE status = 'PENDING';

-- one active browser writer
CREATE UNIQUE INDEX uq_browser_control_active
ON browser_control_lease (session_id)
WHERE state = 'ACTIVE';

-- one active OWNER Device Bridge assignment per session
CREATE UNIQUE INDEX uq_browser_bridge_assignment_active
ON browser_bridge_session_assignment (session_id)
WHERE terminal_at IS NULL;

-- one active writer for one local browser profile
CREATE UNIQUE INDEX uq_browser_profile_writer_active
ON browser_profile_lease (bridge_id, profile_stable_id)
WHERE state = 'ACTIVE';

-- one immutable occurrence of each dispatch phase per browser action attempt
CREATE UNIQUE INDEX uq_browser_action_dispatch_phase
ON browser_action_dispatch_event (action_attempt_id, phase);

-- one active phase run per job
CREATE UNIQUE INDEX uq_generation_phase_active
ON generation_phase_run (job_id)
WHERE state IN ('QUEUED','RUNNING','WAITING_FOR_DEPENDENCY','WAITING_FOR_OWNER','REPAIRING','CANCEL_REQUESTED');

-- one active activation barrier per domain
CREATE UNIQUE INDEX uq_activation_domain_barrier_active
ON activation_domain_barrier (domain_id)
WHERE state IN ('REQUESTED','DRAINING','CLOSED');

-- one current cleanup saga in owner scope
CREATE UNIQUE INDEX uq_cleanup_active_scope
ON cleanup_operation (owner_kind, owner_id, cleanup_kind)
WHERE terminal_at IS NULL;

-- one active schema migration/deployment step
CREATE UNIQUE INDEX uq_schema_migration_active
ON schema_migration ((1))
WHERE terminal_at IS NULL;

-- one canonical external dispatch authority per side-effect attempt
CREATE UNIQUE INDEX uq_side_effect_dispatch_authority
ON transactional_outbox (side_effect_operation_id, side_effect_attempt_sequence)
WHERE purpose = 'SIDE_EFFECT_DISPATCH' AND is_dispatch_authority = true;
```

`side_effect_attempt` má non-partial unique `(operation_id,attempt_sequence)` a `transactional_outbox` non-partial unique `(side_effect_operation_id,side_effect_attempt_sequence,id)`. `side_effect_attempt.dispatch_authority_outbox_id` má `DEFERRABLE INITIALLY DEFERRED` composite foreign key `(operation_id,attempt_sequence,dispatch_authority_outbox_id)` na tento outbox key; opačný deferred composite FK `(side_effect_operation_id,side_effect_attempt_sequence)` z authority outboxu odkazuje na immutable attempt. Oba rows tak mohou vzniknout v T1 bez dočasně neplatného commitu.

`transactional_outbox` má check, že `purpose = 'SIDE_EFFECT_DISPATCH'` právě tehdy, když `is_dispatch_authority = true` a side-effect operation/attempt, target idempotency key a request digest jsou non-null; pro ostatní purpose je `is_dispatch_authority = false`. Deferred constraint trigger `kcml_side_effect_dispatch_authority_consistency` vyžaduje přesně jeden matching authority row pro každý `side_effect_attempt_state` ve stavu `INTENT_RECORDED|DISPATCHING|OUTCOME_RECORDED|RECONCILING|CONFIRMED_APPLIED|CONFIRMED_NOT_APPLIED|FAILED_FINAL|UNKNOWN`; partial unique index vynucuje nejvýše jeden a trigger nejméně jeden.

Deferred constraint trigger `kcml_side_effect_operation_attempt_consistency` navíc ověří, že `side_effect_operation.current_attempt_sequence` odkazuje na existující immutable attempt a jeho jedinou state projection a že operation state odpovídá current attempt-state pro `INTENT_RECORDED|DISPATCHING|OUTCOME_RECORDED|RECONCILING|CONFIRMED_APPLIED|CONFIRMED_NOT_APPLIED|FAILED_FINAL|UNKNOWN`. Historické attempts mohou zůstat terminal; pouze current attempt smí určovat operation dispatch/outcome state. `side_effect_attempt_state.last_evidence_sequence` se alokuje pod jeho `FOR UPDATE` lockem a každá evidence má unique `(operation_id,attempt_sequence,evidence_sequence)`; `MAX(evidence_sequence)+1` je zakázané.

Partial-index predicates používají pouze row columns, konstanty a immutable operators/functions; nesmějí obsahovat `now()`, `clock_timestamp()` ani jinou volatile funkci. Expired row se nejprve transakčně převede z active state, potom může vzniknout nový active row.

Check constraints zajišťují nejméně:

- non-negative versions/sequences/fences/epochs,
- `lease_expires_at > lease_acquired_at` pro active lease,
- terminal state právě s odpovídajícím result/error digestem,
- `UNKNOWN` právě s reconciliation/manual-review relation,
- lifecycle-specific nullability podle row kind,
- 32-byte digests,
- canonical fixed singleton values,
- immutable revision parent/digest ownership,
- activation/rollback epoch monotonicity,
- queue claimed state právě s owner/fence/expiry.

Cross-row invariant, který nelze vyjádřit plain check constraintem, se vynutí unique/FK indexem, zamknutým head row a guarded stored procedure/transaction service. PostgreSQL `CHECK` nesmí obsahovat předpoklad o jiném row.

Critical mutace jsou přístupné pouze přes jednu serverovou domain service nebo versioned SQL function, která realizuje zde uvedený lock/CAS plan. Ad-hoc ORM `save()` bez explicitního `WHERE state_version/fence/incarnation` je zakázaný.

### 51.32 SQLSTATE, retry, deadlock a lock timeout

Transaction wrapper klasifikuje nejméně:

| SQLSTATE | Význam | Povolená reakce |
|---|---|---|
| `40001` | serialization failure | rollback celé transakce a bounded retry se stejným logical operation/idempotency; pouze před external dispatch |
| `40P01` | deadlock detected | rollback celé transakce; zaznamenat lock-plan defect; bounded retry pouze před external dispatch |
| `55P03` | lock not available / lock timeout profil | žádná state change; queue backoff nebo typed concurrency conflict |
| `23505` | unique violation | pouze očekávaný first-create/idempotency race se znovu načte; jinak invariant defect/conflict |
| `23503` | foreign key violation | stale/invalid pointer nebo implementation defect; bez blind retry |
| `23514` | check violation | validation/invariant defect; bez blind retry |
| `57014` | statement cancelled/timeout | rollback; pokud external effect nebyl možný, bounded retry, jinak reconciliation |

Retry opakuje celý transaction closure od `BEGIN`, nikoli jednotlivý failed statement přes savepoint. Všechny input bytes, idempotency key a logical operation ID zůstávají stejné; current DB rows se znovu načtou a guards vyhodnotí.

Deadlock `40P01` v blocking testu je chyba lock planu, i když retry uspěje. Produkční bounded retry chrání dostupnost, ale CI musí odstranit deterministickou lock-order inversion.

Unique violation se nesmí obecně převést na success. Success/replay je přípustný pouze po načtení conflicting row a ověření shodného stable key, request digestu a canonical outcome.

Po jakémkoli možném external dispatch se databázová chyba neřeší opakováním dispatch transaction. Worker nejprve persistuje nebo rekonstruuje side-effect attempt a spustí reconciliation.

### 51.33 Zakázané PostgreSQL a aplikační vzory

Zakázáno je zejména:

- `SELECT` eligibility a pozdější mutation bez row locku/CAS,
- `MAX(...)+1` pro business sequence,
- session-level advisory lock,
- advisory lock jako jediná ochrana uniqueness,
- DB lock držený přes network/provider/browser/filesystem/systemd call,
- `ON CONFLICT DO UPDATE` přepisující idempotency request digest nebo canonical outcome,
- `SKIP LOCKED` pro globální invariant nebo activation/cleanup completeness,
- expiry predicate v partial indexu založený na current time,
- soft-delete a následné znovupoužití stable identity/name/key,
- přímá změna immutable revision/checkpoint/event/evidence payloadu,
- current pointer bez composite parent-owned FK a activation epoch,
- current state odvozený z nejnovějšího timestampu bez source sequence/epoch guardu,
- heartbeat, který obnoví již expired lease,
- fence alokovaný timestampem, random hodnotou nebo resetovatelnou sequence,
- blind retry `EXTERNAL_NON_IDEMPOTENT` nebo `DESTRUCTIVE` operation,
- ruční změna state bez domain eventu, auditu a required outboxu,
- response emit před commitem authoritative state,
- dlouhý transaction otevřený během streamování nebo čekání na OWNER input,
- business rozhodnutí podle `xmin`, `ctid`, PID, connection ID nebo cache version,
- production down migration při application rollbacku,
- DDL migration bez checksumované ledger evidence,
- ORM cascade delete bez explicitní SSOT delete policy.

### 51.34 Povinné PostgreSQL concurrency a failure-injection testy

Blocking suite používá nejméně dvě skutečná PostgreSQL connections, explicitní barriers a deterministické interleavings. Mock databáze ani pouze sequential unit test nestačí. Povinné scénáře jsou:

1. dva commands se stejnou expected state version — právě jeden commit,
2. stejný idempotency key/digest ve 100 paralelních requests — jeden logical operation,
3. stejný key s různými digests — žádný druhý handler/intent,
4. first-create component code/stable secret name/concurrency claim — jedna identity,
5. multi-root bulk mutace v opačném vstupním pořadí — interní canonical order a žádný deadlock,
6. uměle porušené pořadí v test fixture — `40P01` detekce a lock-plan gate failure,
7. queue claim s více workery — každý item nejvýše jednomu current fence,
8. locked první queue item — `SKIP LOCKED` umožní jiný eligible item bez globálního business skipu,
9. queue eligibility změna souběžně s claimem — stale row není dispatchnut,
10. lease heartbeat přesně před expiry versus takeover — jeden current owner,
11. heartbeat po expiry před takeoverem — update nula rows,
12. lease expiry během locked DB transaction — commit/takeover má jednoznačné pořadí a žádný duplicate dispatch,
13. late fence po takeoveru — žádný checkpoint/result/event/idempotency terminal write,
14. terminal write + successor enqueue crash před/po commit — nula nebo právě jeden successor,
15. side-effect T1/D/outbox race se 100 workers, souběžným activation barrierem a crashem před/po pre-dispatch commitu — právě jeden immutable attempt/state projection, právě jeden authority outbox, shodný operation/current-attempt state, contiguous attempt evidence, nejvýše jeden `DISPATCHING` commit, exact pinned-epoch admission, expired pre-dispatch lease bez resurrection a po možném dispatchi pouze reconciliation; `CONFIRMED_NOT_APPLIED` retry vytvoří právě jeden vyšší attempt; ordinary event outbox současně prokáže at-least-once + inbox dedupe,
16. inbox duplicate stejného digestu — jeden domain effect; jiný digest — protocol conflict,
17. aggregate contiguous sequence pod 100 paralelními appends — bez duplicate/gap commitnutých events,
18. audit chain paralelních domain commits — contiguous sequence a validní hash chain,
19. audit insert/head failure — parent domain mutation rollbackne,
20. immutable revision, `side_effect_attempt` a attempt evidence `UPDATE/DELETE` — DB odmítne; povolený lifecycle se mění pouze v `side_effect_attempt_state`,
21. active pointer na revision jiného parentu — FK odmítne,
22. dvě active secret versions — unique index odmítne,
23. concurrent secret activations se stejnou expected version — právě jedna,
24. OWNER API authentication/command acceptance versus rotation — command buď durable před rotací, nebo starý key odmítnut,
25. ztracená rotation response — retry vrátí stejnou version/outcome,
26. concurrent bind/unbind/activation — žádný active revision s neactive binding setem,
27. activation admission versus barrier close — command je buď pinned pre-barrier, nebo odmítnut,
28. phantom command insert při drain checku — domain head lock zabrání switchi přes neuzavřenou mutaci,
29. dva overlapping activation sets — partial unique/domain head zabrání double switchi,
30. crash uprostřed pointer switch transaction — celý previous nebo celý candidate snapshot,
31. reverse switch versus nový activation — stale snapshot odmítnut,
32. first CREATE rollback na `ABSENT` — nulové active pointers/routes/bindings,
33. approval decision versus expiry/reject — jeden terminal decision a nejvýše jeden resume,
34. MRTR response duplicate/different digest/consume race — jeden consume a resume,
35. task cancel versus completion — první root-row commit určí terminal state,
36. model retrieve duplicate — jeden set output items/tool reservations,
37. session append paralelních runs — unique source dedupe a contiguous session sequence,
38. browser OWNER/AI/AUTOMATION takeover/return race s pressed key/button/touch — jeden active lease, monotonic control epoch/fence, právě jeden transfer outcome a před dalším dispatch potvrzený input-state reset,
39. browser action crash interleavings před intentem, po intentu před triggerem, na každém input/change/blur/pointer/key/submit/navigation triggeru a před/po postcondition — nejvýše jeden dispatch authority, žádný stale phase write a po možném triggeru pouze reconciliation/applied/not-applied/unknown,
40. state bundle capture/active-pointer, account auth-epoch rotation, bridge session assignment a local profile lease races — právě jeden current pointer/assignment/lease, stale restore/connection odmítnut a žádné ABA,
41. cleanup resource vznik versus completion — owner root guard zabrání false `COMPLETE`,
42. remediation candidate versus incomplete cleanup — unique active scope odmítne,
43. config desired updates — stale expected version odmítnuta, apply run právě jednou,
44. half-open circuit probes — právě jeden active fenced probe,
45. stale monitoring ACK/heartbeat s pozdějším timestampem — current projection se nezmění,
46. `SERIALIZABLE_PREDICATE` write skew fixture — jedna transaction `40001` a celý retry,
47. `SERIALIZABLE READ ONLY DEFERRABLE` closure snapshot — invarianty odpovídají jednomu serial pointu,
48. migration checksum mismatch — deployment zablokován,
49. failed `CREATE INDEX CONCURRENTLY` — invalid index detekován a krok není `APPLIED`,
50. expand/migrate/activate application rollback — previous release funguje nad forward schema,
51. DB restart/connection loss před a po commit ACK — idempotency určí current/terminal outcome,
52. platform restore s resetovanými čítači — old incarnation write odmítnut,
53. application deployment epoch switch versus worker commit — old release write odmítnut nebo linearizován před switchi,
54. lock timeout — žádná partial state change ani orphan queue/outbox,
55. unique/check/FK violation v každém critical path — celý domain commit rollbackne,
56. 10 000 randomizovaných nemesis interleavings — nulový invariant violation, orphan a mixed epoch.

Každý test kontroluje databázi přímo: roots, versions, fences, leases, pointers, immutable rows, idempotency, events, audit chain, outbox/inbox, queue, side effects, cleanup a orphan inventory. Pouhé ověření HTTP response nestačí.

### 51.35 Produkční observabilita a startup verification

Startup/readiness ověří nejméně:

- PostgreSQL major version 16 a required extensions,
- schema migration checksum chain,
- přesně jeden singleton row každého druhu,
- přítomnost a validitu všech critical unique/partial/composite FK/check constraints,
- přítomnost immutable triggers a critical deferred constraint triggers včetně secret active-pointer, side-effect dispatch-authority a operation/current-attempt consistency,
- žádný invalid index,
- žádné duplicate active secret/control/browser-profile/bridge-assignment/phase/cleanup rows,
- shodu active browser runtime build/host/context/page/frame/document lineage, action dispatch phase a pending reconciliation s parent/fence,
- shodu active pointers s parent/digest/activation epoch,
- current platform incarnation a application deployment epoch,
- nulový unknown schema compatibility blocker.

Databázové metriky obsahují lock wait podle operation profile, deadlock count, serialization failure count, retry count, transaction duration, idle-in-transaction kill, queue claim latency, skipped locked rows, lease takeover, stale fence reject, idempotency conflict/hit, activation barrier drain, audit head contention, constraint violation a migration step outcome.

Deadlock count vyšší než nula je alert a blocking regresní vstup. Rostoucí serialization failure rate vyžaduje review predicate transactionu/index planu. Audit-head contention může vést k optimalizaci payload přípravy před lockem, nikoli k oddělení auditu od domain commitu.

### 51.36 Databázové closure pravidlo

Každá nová nebo generovaná mutující operation před activation doloží:

- konkrétní transaction profile a isolation level,
- úplný ordered advisory/row lock plan,
- všechny read-after-lock guards,
- přesný `state_version`/`lock_version` a fencing CAS,
- unique/check/composite FK constraints,
- idempotency scope, key a request digest uniqueness,
- sequence allocation mechanism,
- jednu DB transakci pro state/event/audit/outbox a successor enqueue,
- external side-effect T1/D/E/T2/T3 rozdělení,
- queue/lease/takeover a expiry-during-transaction semantics,
- migration compatibility a rollback window,
- SQLSTATE retry classification,
- paralelní, crash a nemesis testy z 51.34.

Software architect ani implementátor nesmí žádný z těchto bodů odvozovat z obecné formulace. Chybějící fyzický constraint, lock guard, CAS predicate, transaction boundary nebo test je blocker. Aplikační `if`, komentář, unit mock ani předpoklad „worker běží jen jeden“ databázový invariant nenahrazuje.

### 51.37 Agentic authority, provenance a semantic-dispatch transakce

Agentic entities kapitoly 25.17 jsou fyzicky součástí parent command/run aggregate; nevytvářejí nový auth aggregate. `authority_lineage`, immutable intent revisions, provenance, instruction segments, action-plan payloads, value derivations a Secret-use contexts mají reject-update/delete trigger. Mutable current lifecycle je pouze na `operation_intent_head` a `operation_context` root children se `state_version`; immutable intent/context payloady se nepřepisují.

Kanonický pre-dispatch profil používá `READ COMMITTED` a lock order 51.6:

1. claimne domain idempotency record,
2. zamkne všechny relevantní parent aggregate roots,
3. zamkne current operation-intent/head child ordinal 65,
4. vloží nebo zamkne operation context a semantic action plan ordinal 65,
5. ověří current authority/spec/revision/binding/activation/deployment/incarnation snapshots,
6. ověří complete argument-origin/value-derivation a Secret-use child set,
7. vloží pre-side-effect checkpoint, concurrency claim a side-effect operation/attempt,
8. přepne context `VALIDATED → DISPATCH_RESERVED` CAS,
9. vloží právě jeden dispatch-authority outbox, event a audit,
10. commitne před external/model/browser/runtime dispatch.

Povinné constraints zahrnují:

```text
UNIQUE (authority_lineage_id, lineage_digest)
UNIQUE (operation_intent_id, revision_number)
UNIQUE (parent_owner_kind, parent_owner_id, intent_stable_key, intent_digest)
UNIQUE (operation_context_id, context_digest)
UNIQUE (parent_logical_operation_id, semantic_step_key, context_generation)
UNIQUE (operation_context_id, action_plan_digest)
UNIQUE (action_plan_id, destination_argument_path) pro one-value slots
UNIQUE (operation_context_id, secret_binding_id, consumer_step_key, placement_path, attempt_number)
UNIQUE (request_descriptor_id, segment_sequence)
CHECK (dispatch_state <> 'DISPATCH_RESERVED' OR validated_at IS NOT NULL)
CHECK (instruction_authority <> 'NONE' OR destination <> 'INSTRUCTIONS')
```

Poslední CHECK se realizuje jako deferred constraint trigger přes referenced provenance/segment rows, protože cross-table CHECK není PostgreSQL syntax. OWNER-delegated source je validní pouze při composite FK na current immutable intent revision a matching artifact/section digest.

Model/tool/browser output transakce smí vložit nový proposal/action-plan candidate, ale nesmí sama přepnout context do `DISPATCH_RESERVED`; reservation provádí trusted dispatcher po fresh parent locks a guards. OWNER steer nebo revision change provede CAS invalidaci všech `COMPILED|VALIDATED` contexts a těch `DISPATCH_RESERVED` contexts, jejichž dispatch outbox ještě nebyl claimnut ani odeslán; v téže transakci označí neclaimnutý outbox `SUPPRESSED` a znemožní jeho pozdější claim. Context s možným dispatchi nebo `DISPATCHED` evidence se řeší outcome/reconciliation, nikoli delete.

Concurrency a failure-injection suite ověřuje double model result, stale context reservation, target data refresh race, value-derivation conflict, Secret rotation/resolve race, handoff expansion race, compaction authority drift, OWNER steer versus dispatch a activation epoch switch. Žádný interleaving nesmí vytvořit dispatch bez jednoho current context/action-plan digestu.

### 51.38 Recovery barrier, closure a capacity physical contract

PostgreSQL fyzicky vynucuje recovery a terminal closure nejméně těmito objekty:

```text
platform_recovery_head
platform_recovery_attempt
platform_recovery_item
terminal_closure_evidence
capacity_reservation
artifact_publication
```

`platform_recovery_head` je singleton row s `singleton_key = 1`, current `database_start_identity`, `platform_incarnation_id`, `application_deployment_epoch`, monotonic `recovery_epoch`, `state`, `state_version`, current attempt/fence a `ready_evidence_digest`. `database_start_identity` je canonical digest stable cluster identity a current `pg_postmaster_start_time()`; po PostgreSQL restartu se proto liší, i když application process ještě drží starý persisted `READY`.

Povinné constraints zahrnují nejméně:

```text
PRIMARY KEY (singleton_key)
CHECK (singleton_key = 1)
CHECK (state IN ('STARTING','RECONCILING','READY','BLOCKED','MANUAL_REVIEW'))
UNIQUE (recovery_epoch)
UNIQUE (recovery_attempt_id, owner_kind, owner_id, classification_revision)
UNIQUE (terminal_root_kind, terminal_root_id, terminal_state_version)
UNIQUE (capacity_kind, reservation_key) WHERE released_at IS NULL
UNIQUE (artifact_owner_kind, artifact_owner_id, logical_name, publication_revision)
CHECK (state <> 'READY' OR ready_evidence_digest IS NOT NULL)
CHECK (artifact_state <> 'PUBLISHED' OR final_digest IS NOT NULL)
```

Každá mutující admission transakce získá jako úplně první platformní lock shared transaction advisory lock `PLATFORM_RECOVERY_BARRIER`, načte current head a ověří `READY`, database start identity, incarnation, deployment epoch a recovery epoch. Teprve potom získává domain advisory/row locks podle 51.6. Startup/recovery transition získá stejný lock exclusive, přepne head do `STARTING|RECONCILING`, commitne a tím znemožní nový mutující admission se starým snapshotem. Shared/exclusive platform lock se nikdy nedrží během external, modelového, browserového, filesystem nebo systemd callu.

Worker claim, side-effect outbox claim, model submit, browser mutation reservation, runtime dispatch a activation switch obsahují current `recovery_epoch` ve write predicate. Claim vytvořený před barrier transition může dokončit pouze tehdy, pokud linearizoval před exclusive barrier; jinak fresh pre-dispatch check selže a item jde do recovery klasifikace. `READY` transition používá `SERIALIZABLE_PREDICATE` nebo ekvivalentní explicitní inventory locks tak, aby phantom nonterminal/blocking item znemožnil commit.

Terminal root transition používá closure transaction profile: root row lock, ordered parent-owned child locks, `NOT EXISTS` kontroly nad indexed blocking predicates a deferred constraint triggers pro cross-table authority-bearing children. `terminal_closure_evidence` ukládá root state version, exact blocking-query/catalog digest, inventory watermarks a result digest. Úspěšný terminal stav bez matching closure evidence je constraint violation.

Capacity reservation a durable enqueue jsou jedna transakce. Reservation se uvolní pouze guarded terminal/cleanup transakcí; crash nemůže vytvořit přijatý command bez queue itemu ani queue item bez capacity ownera. Recovery/cleanup reservations používají oddělený reserved class a partial unique constraints, které běžná admission nemůže spotřebovat.

`artifact_publication` ukládá temp path identity, expected/final size a digest, write/fsync/validate/rename/pointer phases a owner root. Pointer row má composite FK na `artifact_publication(state='PUBLISHED', final_digest)` realizovaný validním parent-owned key/trigger patternem. Database commit nesmí zveřejnit nevalidovaný artifact a cleanup nesmí odstranit final path referencovaný current pointerem.

Povinná concurrency suite navíc ověřuje:

1. DB restart s persisted old `READY` versus okamžitý worker reconnect,
2. exclusive recovery barrier versus concurrent shared admissions,
3. phantom nonterminal item při pokusu o `READY`,
4. stale `recovery_epoch` při queue/model/browser/runtime/activation write,
5. terminal root versus concurrent child/successor creation,
6. saturation admission versus duplicate/idempotency replay,
7. recovery-reserved capacity versus běžná queue flood,
8. artifact rename/pointer commit/disk-full crash body,
9. recovery worker kill/takeover před a po každé item klasifikaci,
10. closure evidence versus late provider/worker/browser result.

## 52. OpenAI Responses API a Agents SDK runtime kontrakt

### 52.1 Rozsah, autorita a precedence

Tato kapitola je závazným průřezovým kontraktem pro každý OpenAI call site KájovoCML NG: centrální chat, generation discussion, specialistické fáze, AI agenty, evaluace, AI podporované browserové iterace, opravy, testy a production acceptance.

Kapitola zpřesňuje kapitoly 11, 12, 25, 26, 27, 32, 33, 41, 43, 48, 49 a 51 pouze pro hranici OpenAI Responses API a `@openai/agents`. Kapitola 49 zůstává autoritou state/recovery safety, kapitola 51 fyzické persistence, kapitola 53 authority/provenance a kapitola 54 test/closure evidence. Pravidla se aplikují současně podle 55.2; žádné OpenAI pravidlo nesmí oslabit idempotenci, fencing, side-effect reconciliation, terminalitu, checkpoint, activation epoch, exact binding ani PostgreSQL contract.

OpenAI je jediný AI provider. Tato kapitola nezavádí obecnou provider abstraction, novou lidskou roli, nový permission systém, nový approval režim ani omezení funkčního rozsahu AI. Instrumentační adaptéry popsané níže jsou OpenAI-specific runtime součásti, nikoli rozhraní pro alternativní providery.

PostgreSQL je jedinou autoritou lifecycle, historie, checkpointů, tool outcomes, approval stavu, session, memory a terminal výsledku. OpenAI response, Agents SDK `RunResult`, stream, trace, provider conversation, previous response chain ani serializovaný `RunState` nejsou autoritativní bez guarded persistence KájovoCML.

### 52.2 Normativní pojmy

- **Model logical operation** — jeden serverově zamýšlený modelový krok s jedním immutable request contractem; přežívá worker restart a transportní retry rozhodnutí.
- **Model-call attempt** — jeden konkrétní pokus o provider create/compact operaci. Nový attempt smí vzniknout pouze podle této kapitoly.
- **Provider response** — objekt Responses API s `response.id`, provider statusem, ordered output items, usage a případným error/incomplete detailem.
- **Provider response ID** — `response.id`; jediný standardní provider execution/retrieve handle použitelný pro retrieve, background resume a background cancel.
- **Provider request ID** — HTTP `x-request-id`, v SDK dostupné jako `_request_id` nebo `request_id`; je diagnostické metadata a není response ID, idempotency key, retrieve handle ani důkaz, že modelová práce nezačala.
- **Provider event sequence** — `sequence_number` stream eventu, pokud jej daný transport/event poskytuje.
- **Output item** — jeden item v `response.output`, včetně message, reasoning, function call, hosted tool, MCP call, approval nebo jiného API itemu.
- **Continuation call** — nový Responses API call navazující na předchozí response pomocí `previous_response_id`, provider conversation nebo úplného local replay inputu.
- **SDK run state** — serializovatelný `RunState` Agents SDK; je recovery artefakt svázaný s exact SDK runtime, agent graphem a checkpointem.
- **Canonical history** — ordered KájovoCML session/conversation items potvrzené serverovým commitem.
- **Canonical output** — právě jeden guarded provider output vybraný pro model-call attempt a potvrzený serverovou validací.

### 52.3 Runtime vrstvy

OpenAI execution path má právě tyto vrstvy:

1. KájovoCML domain orchestrator vlastní run/job state, budgets, leases, fences, approvals, tool dispatch, checkpoints, cancellation a terminal commit.
2. `OpenAIClientService` sestavuje immutable OpenAI request descriptor, capability preflight, klienta, transport, retry policy, event normalization, usage a provider evidence.
3. Oficiální balíček `openai` provádí Responses API create, retrieve, cancel, stream a compact operace.
4. Oficiální `@openai/agents` provádí agent loop, handoff, agent-as-tool, guardrails a interruption mechanics nad OpenAI-specific model adaptérem KájovoCML.
5. KájovoCML tool/MCP adapter provádí local function tools a interní MCP volání přes exact KCIP bindings a persistentní side-effect state machines.
6. PostgreSQL persistuje každý rozhodovací bod před jeho publikací nebo použitím v dalším kroku.

`KcmlOpenAIResponsesModel` je OpenAI-specific instrumentation adapter pro Agents SDK. Deleguje modelové volání do oficiálního OpenAI Responses modelu/klienta, ale před provider requestem vytvoří persistentní model-call intent a po provider události zapisuje canonical evidence. Nesmí podporovat jiného providera ani obcházet `OpenAIClientService`.

### 52.4 Konstrukce klienta a zákaz skrytého retry

Všechny call sites používají klienta vytvořeného `OpenAIClientService`. SDK-created default client bez serverové konfigurace je v produkci zakázaný.

Pro každý provider create-class request platí:

- `openai` client retry je explicitně vypnutý pomocí `maxRetries: 0` na klientovi nebo na konkrétním requestu,
- timeout nesmí automaticky spustit SDK retry,
- Agents SDK model retry je bez explicitní KCML policy vypnutý,
- retry callback nesmí nikdy automaticky schválit unsafe nebo unknown replay,
- retry po prvním stream eventu nebo po `responseStarted=true` je zakázaný,
- každý případný Agents SDK retry je nový persistentní model-call attempt a vznikne před tím, než SDK odešle další request.

Create-class zahrnuje nejméně:

- `responses.create`,
- každý interní Responses call vyvolaný Agents SDK,
- `responses.compact`,
- nový provider conversation create, pokud jej runtime používá.

Handle-class operace pracují nad již známým exact provider handlem:

- `responses.retrieve(responseId)`,
- stream retrieve/resume stejného `responseId`,
- `responses.cancel(responseId)` pro background response.

Handle-class operace mohou používat bounded transport retry, protože nemohou vytvořit druhý modelový response. Retry vždy používá stejný response ID, deadline, correlation a local operation. Cancel retry je pouze opakování cancellation requestu nad stejným background response ID; canonical cancellation se odvozuje až z potvrzeného provider/local stavu.

Provider request ID, local metadata, trace ID ani request digest se nepoužívají jako lookup API. Jeho jediný normativní význam je diagnostika a provider support correlation. Pokud budoucí oficiální OpenAI API zavede provider idempotency nebo lookup contract, lze jej použít až po immutable capability revision, contract testu a activation; do té doby nesmí být součástí recovery důkazu `CONFIRMED_NOT_SUBMITTED`.

### 52.5 Immutable OpenAI request descriptor

Před každým create-class requestem vznikne immutable `openai_request_descriptor` obsahující nejméně:

- local model logical operation ID a attempt number,
- owner kind a owner ID,
- exact agent/generation/chat revision a checkpoint,
- authority-lineage ID/digest, OWNER-intent ID/digest a operation-context ID/digest,
- ordered instruction/data segment map, content-provenance manifest a digest,
- OpenAI model ID z effective revision,
- API kind `RESPONSES_CREATE|RESPONSES_COMPACT|AGENTS_MODEL_CALL`,
- transport `HTTP|HTTP_STREAM|RESPONSES_WEBSOCKET`,
- `background`, `store` a stream policy,
- session strategy a provider conversation/previous response handle, jsou-li použity,
- compiled instructions bytes a digest,
- ordered input items a aggregate digest,
- ordered tool definitions, tool choice a tool registry digest,
- strict text/output schema ID, bytes a digest,
- model settings včetně reasoning, verbosity, truncation, max output tokens a parallel tool calls,
- expected output kind,
- absolute deadline a per-attempt timeout,
- call/run/tool/token/cost budget snapshot,
- model capability snapshot ID a digest,
- `openai` a `@openai/agents` package versions,
- adapter/serializer version,
- correlation, causation, trace a group IDs,
- local idempotency scope/key a canonical request digest.

Request digest zahrnuje všechny hodnoty, které mohou změnit modelový význam, authority boundaries, OWNER intent, operation context, provenance segment map, output contract, dostupné tools, continuation nebo cost/turn behavior. Nezahrnuje worker ID, lease token, transport connection ID, provider request ID, local created timestamp ani diagnostická metadata bez modelového významu.

Descriptor se po prvním submit intent commitu nemění. Změna instructions, inputu, toolů, schema, modelu, settings, session strategy nebo continuation handle vytváří nový model logical operation nebo nový explicitní repair/continuation call, nikoli mutaci existujícího attemptu.

### 52.6 Capability snapshot a gates

Každý modelový request používá current immutable `openai_model_capability_snapshot`. Snapshot je svázán s exact model ID, API/SDK verzí, observed date, source evidence, verification runem a expiry a obsahuje nejméně:

- Responses API support,
- foreground stream support,
- background support,
- stored response retrieve support,
- background cancel support,
- resumable background stream a `starting_after` support,
- strict function tools,
- strict structured `text.format`,
- podporovaný JSON Schema profil a limity,
- parallel function calls,
- hosted tools a hosted MCP,
- provider conversation,
- `previous_response_id`,
- Responses compaction a context-management možnosti,
- supported reasoning/output settings,
- supported modalities a input item kinds,
- max context/output a relevantní provider limity,
- Agents SDK compatibility a required runtime version.

Capability gate proběhne před submit intentem. Chybějící povinná capability vrací `OPENAI_MODEL_CAPABILITY_UNSUPPORTED` bez provider requestu. Runtime nesmí:

- potichu změnit OWNERem zvolený model,
- odebrat povinný tool, schema, modality, handoff nebo output contract,
- převést strict structured output na volný text,
- vypnout required background/resume behavior,
- zapnout unsupported parameter a spoléhat na provider ignore,
- použít stale capability snapshot pro nový mutující nebo generation run.

Semanticky rovnocenný transport fallback je povolen pouze tehdy, když revision výslovně uvádí více transportů jako ekvivalentní a fallback nemění persistence, ordering, cancellation, output nebo recovery contract. Jinak je chybějící capability blocker.

### 52.7 Model-call a provider-attempt lifecycle

Jeden Responses API create je právě jeden `ai_model_call` attempt. Tool loop vytváří sekvenci model calls; continuation není pokračování stejného provider response v jednom DB row.

Local states jsou:

```text
QUEUED
SUBMITTING
IN_PROGRESS
STREAMING
WAITING_FOR_TOOL_OUTPUT
COMPLETED
INCOMPLETE
REFUSED
CANCEL_REQUESTED
CANCELLED
FAILED
EXPIRED
```

Provider wire status je oddělené pole s hodnotami podporovanými current Responses API, nejméně:

```text
queued
in_progress
completed
failed
cancelled
incomplete
```

`REFUSED`, `WAITING_FOR_TOOL_OUTPUT` a `EXPIRED` jsou local semantic states, nikoli domýšlené provider statuses.

Povolené local hrany:

```text
QUEUED → SUBMITTING | CANCEL_REQUESTED
SUBMITTING → IN_PROGRESS | STREAMING | COMPLETED | INCOMPLETE | REFUSED | FAILED | CANCEL_REQUESTED
IN_PROGRESS ↔ STREAMING
IN_PROGRESS | STREAMING → WAITING_FOR_TOOL_OUTPUT | COMPLETED | INCOMPLETE | REFUSED | FAILED | EXPIRED | CANCEL_REQUESTED
WAITING_FOR_TOOL_OUTPUT → COMPLETED | FAILED | CANCEL_REQUESTED
CANCEL_REQUESTED → CANCELLED | FAILED
```

`COMPLETED`, `INCOMPLETE`, `REFUSED`, `CANCELLED`, `FAILED` a `EXPIRED` jsou terminal pro konkrétní model-call attempt. `COMPLETED` má `completionKind = FINAL_OUTPUT|TOOL_CALLS|HOSTED_TOOL_RESULT|NO_OUTPUT`. `TOOL_CALLS` znamená, že producing response je uzavřený a případný další modelový krok je nový successor `ai_model_call`.

### 52.8 Submit protocol a linearizační body

Submit probíhá přes následující durable hranice:

**T0 — prepare**

V jedné PostgreSQL transakci runtime:

1. zamkne parent run/turn a current checkpoint,
2. ověří current fence, cancellation, deadline, budget a capability snapshot,
3. claimne local idempotency key/request digest,
4. vloží immutable request descriptor,
5. vytvoří `ai_model_call` ve stavu `QUEUED`,
6. vytvoří submit intent a queue/outbox reservation,
7. uloží event a audit,
8. commitne.

**T1 — pre-dispatch claim**

Worker v krátké transakci:

1. claimne queue item a lease/fence,
2. znovu ověří parent, model-call state, descriptor digest, cancellation, deadline a capability,
3. nastaví `SUBMITTING`, `dispatchStartedAt` a transport evidence `DISPATCH_STARTED`,
4. commitne před síťovým requestem.

**D — provider dispatch**

Worker odešle právě jeden create-class request s vypnutým implicitním SDK retry.

**T2 — handle/evidence persistence**

Jakmile je znám provider response ID nebo response headers/body, runtime jej persistuje v nejbližší samostatné transakci. Provider response ID se zapisuje dříve než publikace prvního client-visible provider delta, pokud jej provider již poskytl.

**T3 — provider output persistence**

Každý event/output item/usage/error je persistován idempotentně. Terminal provider snapshot a jeho ordered output items se commitují před tool reservation, final validation nebo parent transition.

**T4 — model-call terminal/continuation**

Server pod current parent/model fence vyhodnotí status, refusal, output schema a tool calls a atomicky uloží model-call terminal/current outcome, checkpoint, případné tool reservations nebo právě jednu successor reservation, event, audit a outbox.

Síťový call nikdy neběží uvnitř T0, T1, T2, T3 ani T4.

### 52.9 Submit ambiguity

`CONFIRMED_NOT_SUBMITTED` je přípustné pouze tehdy, když durable transport evidence prokazuje, že provider dispatch nezačal, například:

- request selhal při lokální serializaci před otevřením transportu,
- capability/schema/preflight selhal před T1,
- worker skončil po T0 a před T1,
- transport adapter jednoznačně potvrdil, že request nebyl vydán.

Jakmile T1 commitnul `DISPATCH_STARTED`, connection error, timeout, process crash nebo chybějící HTTP response samo o sobě neprokazuje `NOT_SUBMITTED`.

Pokud po `DISPATCH_STARTED` není persistovaný provider response ID ani providerem garantovaný idempotency/lookup handle, outcome je `UNKNOWN`. Runtime:

- nastaví stable code `MODEL_SUBMIT_OUTCOME_UNKNOWN`,
- nepovolí druhý create attempt stejného business kroku automaticky,
- zablokuje parent run/job v `MANUAL_REVIEW` nebo generation `BLOCKED/MANUAL_REVIEW`,
- uchová provider request ID, pokud existuje, pouze jako diagnostickou evidence,
- nesmí tvrdit, že provider call nebyl účtován nebo nevytvořil output.

Explicitní OWNER rozhodnutí může založit nový model logical operation; původní unknown attempt zůstává immutable evidence a nový output se nesmí automaticky sloučit jako tentýž attempt.

### 52.10 Foreground stream

Foreground stream používá `stream: true` a serverový provider connection nezávislý na browser/client SSE connection.

Platí:

- browser disconnect sám provider stream neruší,
- každý provider event se validuje, canonicalizuje a persistuje před KájovoCML SSE publikací,
- local event sequence je monotonic a gap-free per model call,
- provider `sequence_number`, je-li přítomen, má unique constraint per response ID,
- stejná provider sequence a stejný digest je replay/no-op,
- stejná provider sequence s jiným digestem je `OPENAI_STREAM_PROTOCOL_CONFLICT`,
- text/function argument deltas jsou pouze progress evidence,
- tool arguments se neprovádějí z delta streamu,
- final text nebo structured output se nepřijme z `output_text` helperu bez kontroly terminal response statusu a complete ordered output,
- čisté ukončení iteratoru není důkaz `completed`; runtime vždy kontroluje poslední response status,
- partial text lze zobrazit jako `PARTIAL/INTERRUPTED` evidence, nikoli jako canonical terminal assistant message.

Přerušení stream iteratoru nebo AbortSignal ukončí lokální transport attempt, ale samo nepotvrzuje provider cancellation. Pokud je response ID známý a response je stored, recovery použije retrieve. Pokud response ID známý není, použije se submit ambiguity kontrakt.

### 52.11 Background response, polling a resumable stream

Background mode je povolen pouze capability gate. Recoverable background call vždy explicitně nastaví:

```text
background = true
store = true
```

Background create může být ne-streamovaný nebo současně streamovaný. Runtime musí response ID persistovat při prvním `response.created`/create výsledku a před závislostí na dalším eventu.

Polling:

- používá `responses.retrieve(responseId)`,
- respektuje provider `retry-after`, local backoff a absolute deadline,
- nezakládá nový Responses create,
- persistuje každý odlišný provider snapshot digest,
- terminalizuje pouze podle provider statusu a local validation.

Resume stream:

- používá stejný response ID,
- `starting_after` je poslední contiguous persistovaná provider sequence, nikoli pouze nejvyšší pozorované číslo,
- event po cursoru se deduplikuje proti již persistovaným events,
- při použití parsed tools nebo structured output helperu se použije stejná immutable tools/text konfigurace jako při původním callu,
- accumulated helper snapshot není autorita; canonical items vznikají z persistovaných provider events/terminal retrieve.

Ztráta browser SSE, worker restart nebo HTTP stream disconnect background response neruší. Nový worker retrieveuje nebo obnoví stream stejného response ID.

### 52.12 Provider status, refusal, incomplete a failure

Normalizace používá tuto mapu:

- provider `queued|in_progress` → local `IN_PROGRESS` nebo `STREAMING`,
- provider `completed` bez function/hosted/MCP callu → kandidát `COMPLETED`,
- provider `completed` s local function calls čekajícími na outcome → `WAITING_FOR_TOOL_OUTPUT`,
- provider `completed` s refusal contentem a bez validního požadovaného outputu → `REFUSED`,
- provider `incomplete` → `INCOMPLETE` s exact `incomplete_details`,
- provider `failed` → `FAILED` s exact safe provider error a raw OWNER evidence,
- provider `cancelled` → kandidát `CANCELLED`, pokud local cancellation/terminal race dovolí cancellation commit,
- provider status neznámý current adapteru → `FAILED` s `OPENAI_PROVIDER_STATUS_UNKNOWN`; žádný output se nepoužije.

Refusal není JSON/schema chyba. Incomplete není success ani implicitní retry. Provider failure po známém response ID se znovu načítá pouze pro potvrzení current state; nový create je samostatný attempt podle explicitní policy.

Local `EXPIRED` znamená, že required provider handle již není retrievable a KájovoCML nemá complete terminal response. Pokud complete terminal provider output již je lokálně persistovaný, pozdější provider retention purge local canonical outcome nemění.

### 52.13 Ordered output items

Runtime persistuje celý `response.output` v provider pořadí. Pro každý item ukládá nejméně:

- response ID,
- provider output index,
- item ID, pokud existuje,
- item type a status,
- function/MCP call ID, pokud existuje,
- raw canonical JSON a digest,
- ordered content parts a jejich indices/digests,
- first/last provider sequence,
- local interpretation state,
- source model-call attempt.

`response.output_text` je pouze convenience projection pro display. Nesmí nahradit raw messages, reasoning items, tool calls, refusals, annotations ani jiné output items.

Unknown output item type se uloží beze ztráty jako raw evidence. Pokud jej current adapter neumí bezpečně zahrnout do continuation nebo vyhodnotit, call se nepovažuje za úspěšný a activation/production run se zablokuje `OPENAI_OUTPUT_ITEM_UNSUPPORTED`. Adapter nesmí unknown item potichu zahodit.

Manual history replay používá version-pinned oficiální normalizaci typu `toResponseInputItems()` nebo ekvivalentní testovaný adapter a zachovává úplné replayable output items v původním pořadí. Pouhé assistant messages nestačí. Reasoning/tool-call items poskytované API se zachovají, modelové interní chain-of-thought, které API neposkytuje, se nevytváří ani nereprodukuje.

### 52.14 Strict structured outputs

Bounded typed modelový výstup používá Responses structured `text.format` se strict JSON Schema; function tool arguments používají strict function schema. Každý schema contract je immutable, canonical a digestovaný.

Před submit:

- schema projde local dialect/profile validací,
- každý object má `additionalProperties: false`,
- všechny model-visible properties jsou required,
- optional business význam se vyjadřuje explicitní nullable/discriminated variantou,
- unsupported keyword nebo limit blokuje call před providerem,
- exact schema bytes/digest jsou v request descriptoru.

Po provider response:

1. runtime nejprve ověří provider terminal status,
2. oddělí refusal/incomplete/failure,
3. uloží raw ordered output,
4. provede SDK parse a nezávislou serverovou JSON Schema validaci,
5. ověří artifact kind/schema version a business invariants,
6. teprve potom označí structured output validní.

Schema-invalid output je `MODEL_OUTPUT_SCHEMA_INVALID`, nikoli provider retry stejného attemptu. Bounded repair je nový explicitní model logical operation s vlastním request digestem, repair budgetem a odkazem na raw invalid output/diagnostics. Repair nesmí zopakovat již potvrzený tool side effect; používá jeho persistovaný result.

Fallback error handler Agents SDK smí vytvořit final output pouze tehdy, když jej server znovu validuje proti exact output schema a handler neprovádí nový model call ani tool replay. Původ `MODEL_ERROR_HANDLER` se uloží do output evidence.

### 52.15 Function call identity a arguments

Pro Responses function tool je autoritativním párovacím identifikátorem `call_id`, nikoli optional output item `id`.

Každý function call:

- vzniká pouze z complete provider output itemu,
- má unique `(producing_model_call_id, call_id)`,
- ukládá provider item ID odděleně,
- ukládá tool name/alias, exact binding/revision/digest, provider output index, raw argument string, parsed canonical arguments a argument digest,
- ukládá operation-context reference, server-resolved stable operation, exact target, argument-origin mapu a value-derivation references,
- validuje JSON parse, strict model schema, inverse projection, native tool schema a semantic containment proti OWNER intentu,
- získá existující approval a side-effect classification z exact bindingu, nikoli z modelového textu nebo tool description/resultu,
- vytvoří persistentní dispatch reservation až po validním semantic action planu a Secret-use contextu.

Argument delta event se nikdy nevykonává. Chybějící, duplicate nebo conflicting `call_id` blokuje tool dispatch. Stejný `call_id` a stejný argument digest replayuje current/terminal tool outcome; stejný `call_id` s jiným digestem je `OPENAI_TOOL_CALL_ID_CONFLICT`.

### 52.16 Tool execution a function call output

Tool execution používá kapitoly 6, 10, 11, 49 a 51. Model call ID ani provider call ID nejsou samy business idempotency key; dispatcher odvodí stable domain idempotency scope z runu, bindingu, call ID a canonical arguments.

Tool outcome je jeden z:

```text
SUCCEEDED
FAILED_KNOWN
WAITING_FOR_APPROVAL
WAITING_FOR_INPUT
WAITING_FOR_TASK
CANCELLED_CONFIRMED
OUTCOME_UNKNOWN
```

`OUTCOME_UNKNOWN` se modelu neposílá jako success ani jako safe-retry error. Parent run přejde do reconciliation/`MANUAL_REVIEW` a další provider continuation je blokovaná.

Function result se do Responses continuation vytvoří až po persistentním outcome commitu. Output item:

- používá exact původní `call_id`,
- obsahuje deterministic text, canonical JSON string nebo podporovaný typed content podle tool contractu,
- má local result digest,
- nesmí tvrdit success, pokud side effect není potvrzený,
- je immutable po zařazení do successor descriptoru.

Všechny function calls z jednoho producing response musí mít před pokračováním právě jeden známý resolution: persisted output, persisted rejection, validní pending approval/input/task, nebo terminal failure parentu. Call se nesmí potichu vynechat.

### 52.17 Ordering a parallel tool calls

Provider output order je oddělený od tool completion order. Při více function calls runtime:

- alokuje `callOrdinal` podle provider output indexu,
- může fyzicky spustit pouze calls povolené parallel policy,
- ukládá outcome každého callu nezávisle,
- sestaví continuation outputs deterministicky podle `callOrdinal`, nikoli podle času dokončení,
- páruje každý output exact `call_id`.

Parallel dispatch je povolen pouze při exact revision policy a současném splnění:

- všechny calls jsou `READ_ONLY`, nebo jejich kontrakt výslovně prokazuje vzájemně nezávislé idempotentní effects,
- concurrency keys jsou disjunktní,
- neexistuje causation/data dependence,
- approval, workspace mutation, handoff, browser mutation, destructive nebo non-idempotent external effect nejsou součástí paralelní skupiny,
- aggregate budget a deadline dovolují dokončení/reconciliation celé skupiny.

Jinak `parallel_tool_calls=false` a Agents SDK `toolExecution.maxFunctionToolConcurrency=1`. Model ani provider output nemůže tuto serverovou policy rozšířit.

### 52.18 Tool-loop continuation

Každý další modelový krok po tool outcomes je nový `ai_model_call` s `continuationOfModelCallId` a parent run turn sequence.

V jedné transakci po známých tool outcomes runtime:

1. zamkne parent run a producing model call,
2. ověří current fence, cancellation, deadline, budget a complete resolution set,
3. uloží immutable ordered function outputs,
4. uloží post-tool checkpoint,
5. rezervuje právě jeden successor model logical operation a queue/outbox,
6. označí producing call `COMPLETED` s `completionKind=TOOL_CALLS`,
7. uloží event a audit,
8. commitne.

Crash mezi tool outcome a continuation nemůže zopakovat tool, protože successor vzniká z persisted outcome. Crash po successor reservation nemůže vytvořit druhý successor, protože unique constraint je `(parent_run_id, producing_model_call_id, continuation_generation)`.

Continuation request používá právě jednu history strategii:

- `PREVIOUS_RESPONSE_CHAIN` — `previous_response_id` je exact producing response ID; `conversation` chybí; instructions, tools, schema a relevantní settings se znovu explicitně posílají; function outputs používají exact call IDs.
- `OPENAI_CONVERSATION` — používá exact conversation ID a pouze current turn delta; `previous_response_id` a local full-history replay se nepoužijí.
- `KCML_SESSION` — request obsahuje canonical local history a complete ordered replayable output items; provider conversation ani `previous_response_id` se nepoužijí.
- `STATELESS` — request neobsahuje žádnou předchozí conversation history mimo explicitní current input/context projection.

`previous_response_id` a `conversation` jsou vzájemně výlučné. Při previous-response chaining se předchozí instructions nepovažují za zděděné; compiler je vždy vloží z exact revision znovu.

### 52.19 Hosted tools a hosted MCP

Hosted OpenAI tools a hosted MCP jsou povolené pouze exact agent/model revision a capability snapshotem. Jejich provider output items, approval requests, calls a results se persistují ve stejném ordered item store jako ostatní Responses output.

Hosted MCP konfigurace obsahuje exact server label/URL, tool allowlist/filter, existing approval policy, auth/credential binding, contract/discovery digest, model capability snapshot a revision digest. Modelový text, hosted server instructions, descriptions, resource/prompt content, tool result nebo provider-generated metadata nemůže server, tool list, credential, target, Secret-use context ani approval policy změnit. Všechny hosted outputs vstupují do continuation jako data/result segments s provider provenance.

Provider-hosted tool/MCP effect je součástí provider response execution. Nejasný create submit se proto nesmí řešit druhým blind model call, protože by mohl zopakovat hosted effect. Approval item se mapuje na již definovaný `agent_approval_request` s exact provider item/call ID a argument digestem. Resume používá původní checkpoint a provider continuation contract; nevytváří novou approval vrstvu.

Interní KájovoCML tools a komponenty se nevystavují jako hosted bypass. Používají function-tool adapter a KCIP, aby exact bindings, idempotency, Secret resolve, side-effect evidence a audit zůstaly serverově vynutitelné.

Agents SDK `toolUseBehavior` se nepoužívá jako autorita pro hosted/MCP terminalitu. Server vždy vyhodnotí actual ordered output, pending calls a final output schema.

### 52.20 Agents SDK run mapping

Jeden `agent_run` je KájovoCML aggregate. Agents SDK run je execution helper pod jeho fence.

Runtime persistuje nejméně:

- exact root agent revision a stable graph identity map,
- handoff a agent-as-tool graph digest,
- exact instructions/tool/output/guardrail digests,
- `toolUseBehavior`, tool-not-found policy a reasoning item ID policy,
- Runner/RunConfig a model settings digest,
- max turns a current turn,
- `RunResult` high-level fields,
- ordered run items a raw model responses,
- `lastAgent`, `lastResponseId`, usage a interruptions,
- current session strategy a cursor,
- serialized `RunState`, pokud run není terminal.

Každá interní SDK model invocation prochází `KcmlOpenAIResponsesModel`; žádný skrytý SDK model call nesmí vzniknout bez `ai_model_call` T0 intentu.

`RunResult.finalOutput` je pouze candidate. Parent run přejde do `SUCCEEDED` až po serverové schema/guardrail validaci, known side-effect setu a atomickém session/memory commitu.

Agents SDK `MaxTurnsExceededError`, model refusal, invalid final output, model timeout, tool timeout, guardrail tripwire a model behavior error se normalizují na stable KájovoCML errors. Error handler nesmí změnit již potvrzený tool outcome ani vytvořit další model call mimo model-call state machine.

### 52.21 Handoff a agent-as-tool

Handoff a `agent.asTool()` používají exact source/target revisions, stable graph identities, parent authority lineage/OWNER intent/operation context a bounded depth/budget.

Handoff:

- před předáním uloží parent checkpoint a handoff reservation,
- target agent se stane SDK current agent až po guarded parent commit,
- `lastAgent` je canonical pro další turn pouze po parent terminal/session commitu,
- late target output po parent cancellation/fence loss je evidence, nikoli parent výsledek.

Agent-as-tool:

- je child run s vlastním logical operation, tool identity a typed output schema,
- jeho terminal output se vrací root runu jako tool output právě jednou,
- nested approval se zobrazuje a řeší na root `RunState`, ale zůstává svázané s exact nested call digestem,
- child tool side effects používají stejné idempotency/reconciliation kontrakty jako root,
- child context je semantic descendant parentu a nemůže rozšířit exact tools, target ceiling, data slots, side-effect class nebo Secret-use destination,
- child text nebo result, který tvrdí nový OWNER příkaz, zůstává proposal s child provenance.

Serialized run state musí zachovat všechny nested agent-tool resumptions a handoff references. Pouhé přepnutí current agent reference není migrací celého graphu.

### 52.22 Approval interruption a resumable RunState

Při interruption runtime:

1. persistuje všechny provider/run items do interruption pointu,
2. persistuje exact approval requesty a argument digests,
3. serializuje `RunState`,
4. uloží SDK package version, adapter version, code/runtime revision, root graph digest, stable identity map, `toolUseBehavior`, session strategy, current turn, usage/budget a pending output provenance,
5. atomicky nastaví parent `WAITING_FOR_OWNER` a checkpoint pointer.

OWNER decision používá existing approval API a exact digest. Approval/rejection a právě jedna resume reservation jsou jedna transakce. Dva resumers nemohou současně pokračovat ze stejné checkpoint generation.

Resume používá stejný root agent graph a exact compatible SDK runtime. `RunState.fromString`/ekvivalent nesmí být volán nad náhodně novým graphem nebo nekompatibilní SDK verzí.

Pokud serialized checkpoint nemůže prokázat ownership pending outputu, SDK odmítne resume, nebo output guardrail provenance není zachovaná, runtime:

- nevykoná další model, tool ani session side effect,
- označí `AGENT_RUN_STATE_UNRESUMABLE`,
- zachová původní approval a tool outcomes,
- umožní pouze explicitní recovery z canonical safe input/checkpointu jako nový run podle existující OWNER akce; raw rejected/ambiguous suffix se nereplayuje.

Nový OWNER input během pause se nejprve persistuje a přidá do checkpointu přes supported pending-input mechanismus; nesmí se vložit až po spuštění resumed model callu.

### 52.23 Session strategie

Jeden agent/chat conversation používá v jednom active session revision právě jednu strategii:

```text
STATELESS
KCML_SESSION
OPENAI_CONVERSATION
PREVIOUS_RESPONSE_CHAIN
```

Míchání strategií v jednom turnu je `AGENT_SESSION_STRATEGY_CONFLICT`.

**STATELESS**

- provider dostane pouze current input a explicitní context projection,
- žádný provider/local history handle se nepoužije.

**KCML_SESSION**

- canonical history je v PostgreSQL,
- custom `KcmlAgentSession` načítá ordered history,
- SDK session writes se nejprve ukládají jako run-scoped staged items,
- canonical assistant/tool items se připojí pod session lockem v parent terminal/checkpoint transakci,
- provider conversation a `previous_response_id` se nepoužijí.

**OPENAI_CONVERSATION**

- exact provider conversation ID se persistuje a mirroruje,
- request posílá pouze current turn delta,
- současně se nepoužije `previous_response_id`, full local replay ani další SDK Session nad stejnou historií,
- KájovoCML stále persistuje canonical mirror outputů a tool outcomes pro audit/recovery.

**PREVIOUS_RESPONSE_CHAIN**

- exact last response ID je continuation handle,
- `conversation` a SDK Session history replay se nepoužijí,
- každý call znovu posílá current instructions, tools, schema a settings,
- local DB drží úplný ordered mirror a dovolí prokazatelnou migraci na nový local-history chain, pokud provider handle zanikne a neexistuje pending/unknown effect.

Strategy se během active run nemění. Změna mezi turns vytváří new session revision s explicitním migration checkpointem a history digestem.

### 52.24 Canonical session persistence

`KcmlAgentSession` implementuje Agents SDK Session contract, ale SDK callback nemůže sám terminalizovat KájovoCML run ani přepsat canonical history.

Session operace jsou atomické a idempotentní:

- `getItems` čte jeden current session version snapshot,
- current user input se persistuje před run startem s client message/run idempotency,
- model/tool outputs se stageují s source model/run item keys,
- `addItems` duplicate stejného source key/digest je no-op,
- stejný source key s jiným digestem je conflict,
- canonical append alokuje contiguous sequence pod `agent_session FOR UPDATE`,
- run success, required output items, session pointer a mandatory memory writes jsou jeden PostgreSQL commit.

Streaming partial items se ukládají do event/evidence store, ne do canonical session jako final assistant message. Přerušený turn může mít canonical status `INTERRUPTED` a explicitní partial payload, ale další turn jej nesmí zaměnit za completed assistant output.

### 52.25 Context compaction

Compaction je optimalizační transformace contextu, nikoli změna historické pravdy.

Compaction candidate obsahuje:

- session/revision,
- exact source item range a source aggregate digest,
- compaction mode `INPUT|PREVIOUS_RESPONSE_ID`,
- model/capability/SDK versions,
- immutable request descriptor,
- provider response/compact handle, je-li dostupný,
- compacted ordered items a digest,
- semantic preservation test evidence.

`OpenAIResponsesCompactionSession` nebo ekvivalent se smí nad `KCML_SESSION` použít pouze v `INPUT` mode nad KájovoCML history. Nesmí obalovat `OpenAIConversationsSession` ani server-managed conversation strategii. Pro `PREVIOUS_RESPONSE_CHAIN` volá KájovoCML `responses.compact` přímo jako samostatný persistentní create-class attempt s exact source response ID; nepřidává současně SDK Session history replay.

Compaction:

- neběží souběžně s active turnem, pending approval, unresolved tool call nebo session append,
- pro low-latency stream standardně probíhá mezi turns nebo v idle fázi,
- zamkne session/compaction root a ověří expected source range/version,
- nikdy neodstraní neuzavřený function call bez jeho outputu, approval, handoff boundary, authority-lineage/intent boundary, provenance segment map nebo required reasoning/tool item,
- v `PREVIOUS_RESPONSE_ID` mode atomicky nahradí pouze current provider-chain handle compacted response ID a zachová source/target relation; history strategy se nemění,
- aktivuje pouze nový compaction pointer/revision; původní items zůstávají immutable audit evidence,
- při provider/validation/replace failure zachová current uncompacted history,
- při crashi po provider resultu před pointer switchi může stejný candidate znovu validovat a aktivovat bez dalšího provider callu.

Compaction output je model-generated context artifact s instruction authority `NONE` a nesmí přepisovat domain memory, OWNER message, authority lineage, OWNER intent, tool outcome, approval decision nebo audit. Pointer switch vyžaduje semantic-preservation evidence, že trusted instructions a data roles zůstaly oddělené; jinak vzniká `AGENTIC_COMPACTION_AUTHORITY_DRIFT`.

### 52.26 Memory writes

Session není dlouhodobá semantic memory. Model může navrhnout memory mutation pouze typed tool/output kontraktem.

Každý memory write:

- určuje namespace, schema version, key, canonical content/metadata a digest,
- má source run/model/output item, content provenance, content role a instruction authority,
- prochází serverovou validací, semantic re-entry validation a exact bindingem,
- používá idempotency `(namespace,sourceRun,key)` a expected state version,
- je staged do checkpointu,
- required writes se commitují atomicky s run success,
- optional derived index/vector sink se doručuje outboxem.

Volný assistant text, provider conversation state, compaction summary ani Agents SDK context objekt nejsou automatickým memory write.

### 52.27 Budgets, turns a deadlines

Každý run a model call má současně:

- absolute deadline,
- per-call timeout,
- max model calls,
- max Agents SDK turns,
- max tool calls a max parallelism,
- max input/output/total tokens,
- max cost,
- max handoff/agent-as-tool depth,
- max repair calls,
- max background polling/retrieve duration.

Budget se kontroluje před každým submit, tool dispatch, handoff a resume. Runtime rezervuje odhadovaný budget před create a po usage eventu provede exact accounting.

Pokud provider response překročí zbývající budget až po dokončení:

- response a usage se persistují,
- již provedený model call se nezahodí ani neopakuje,
- nový tool/continuation call se nezahájí,
- parent skončí stable budget error nebo validním final outputem pouze tehdy, pokud jej contract dovoluje.

SDK `maxTurns` je defense-in-depth; KájovoCML turn counter je autoritativní. Runtime nesmí po limitu automaticky zvýšit budget nebo založit nový run se stejným cílem.

Pro exact structured/tool workflows je truncation implicitně `disabled`; context overflow se řeší explicitním compaction/context policy. `auto` truncation je přípustné pouze revision contractem, který dovoluje ztrátu nejstarších context items a má testovaný semantic dopad.

### 52.28 Cancellation, timeout a provider disconnect

Cancellation linearizační bod je KájovoCML DB commit, nikoli AbortSignal ani provider timestamp.

Před T1 lze call bezpečně zrušit bez provider effect. Po T1:

- foreground AbortSignal zastaví lokální čekání/stream, ale nepotvrzuje provider cancellation,
- background response se canceluje výhradně stejným response ID,
- cancel request/poll/retrieve se opakuje pouze nad stejným handlem,
- provider completion a local cancel race rozhoduje první validní guarded model-call terminal commit,
- late provider result se uloží jako immutable evidence a nesmí přepsat terminal parent/call,
- parent se označí `CANCELLED` pouze po known provider/tool outcomes a required cleanup.

Per-call timeout může ukončit current transport attempt. Pokud dispatch začal a response ID není známý, timeout je submit ambiguity. Pokud response ID známý je, runtime retrieveuje/canceluje podle background policy. Timeout nesmí sám založit nový create.

Provider disconnect během hosted/local tool output čekání nemění persisted tool outcome. Continuation se znovu vytvoří pouze z durable successor reservation; tool se neopakuje.

### 52.29 Provider retention, missing handle a expiry

Recoverable calls explicitně používají provider storage. Provider retention je capability/effective response property, nikoli hardcoded business předpoklad.

Retrieve `not found`/expired se nejprve klasifikuje podle:

- exact response ID a project/key context,
- provider auth/project mismatch,
- transient dependency failure,
- documented retention window,
- local complete response evidence.

Pokud complete terminal response a output items již jsou v KájovoCML, provider purge nic nemění. Pokud required output chybí:

- call přejde `EXPIRED`/`OPENAI_RESPONSE_EXPIRED`,
- žádný blind create se nespustí,
- parent se zablokuje pro explicitní recovery,
- nový call lze založit pouze jako novou logical operation po ověření, že nezopakuje tool/hosted effect.

Missing provider conversation nebo previous response handle lze nahradit novým local-history chainem pouze mezi model calls, z complete canonical history, po kontrole, že neexistuje pending approval/call, unresolved side effect ani unpaired provider item. Migrace má vlastní checkpoint a nový session revision.

### 52.30 SDK a adapter upgrade compatibility

Balíčky `openai`, `@openai/agents`, Zod a adapter jsou pinované lockfilem a evidence manifestem.

Každý `ai_model_call` a `ai_run_state_checkpoint` ukládá:

- package versions,
- serializer/schema version,
- adapter build/release digest,
- Node runtime major,
- agent graph/code revision,
- event/output compatibility profile.

Upgrade vyžaduje:

1. compile/type check,
2. recorded event/output differential tests,
3. strict schema/tool loop tests,
4. stream/background/retrieve tests,
5. session/compaction tests,
6. RunState serialize/deserialize/resume tests,
7. handoff a nested agent-as-tool interruption tests,
8. production smoke s current selected models,
9. rollback test.

Pending serialized RunState se neotevírá automaticky novou SDK verzí. Runtime selector načte exact compatible SDK/code generation. Při dlouhodobých approvals systém drží předchozí SDK runtime side-by-side pomocí package aliasu nebo immutable release workeru, dokud pending checkpoints nedoběhnou, neexpirují nebo nejsou explicitně ukončené.

Nová SDK generation může vytvářet nové runs až po activation. Stará generation po cutoveru nesmí přijímat nové runs, smí však fenced resumeovat pouze checkpointy, které ji exact vyžadují.

Migration serialized state je přípustná pouze testovaným versioned converterem se source/target digestem. Neúspěšná nebo nejednoznačná migration je `AGENT_CHECKPOINT_INCOMPATIBLE` nebo `AGENT_RUN_STATE_UNRESUMABLE`; nesmí replayovat tool effects.

Unknown SDK event/output typ se raw persistuje a blokuje canonical interpretation. Upgrade nesmí zavést silent ignore nové terminal, tool, approval nebo usage semantics.

### 52.31 Tracing, usage a evidence

Každý provider attempt ukládá:

- provider request ID, je-li dostupný,
- response ID,
- local/provider timestamps,
- HTTP status a safe headers,
- SDK/API versions,
- ordered event/output digests,
- usage raw snapshot a normalized usage,
- model/service tier a relevantní response settings,
- latency po fázích submit/first-event/terminal/retrieve,
- correlation, causation, trace a group IDs,
- retry/recovery decision a evidence.

Provider request ID se zobrazuje OWNERovi pro support/debug. Nesmí se vydávat za idempotency nebo response handle.

KájovoCML persistuje pouze reasoning items, summaries a metadata skutečně zpřístupněná API. Nezobrazuje ani nevytváří skryté interní chain-of-thought tokeny.

Usage/cost je evidence provider callu i tehdy, když parent run později selže, je cancelled nebo output schema neprojde. Cost se nesmí odečíst zpětně jen proto, že output nebyl přijat.

### 52.32 Stable errors a recovery directives

Minimální OpenAI/agent codes a jejich jediný recovery význam:

| Code | Význam | Directive |
|---|---|---|
| `OPENAI_CONFIGURATION_REQUIRED` | chybí active `OPENAI_API_KEY` | `DO_NOT_RETRY` do konfigurace |
| `OPENAI_MODEL_CAPABILITY_UNSUPPORTED` | selected model neumí required contract | `DO_NOT_RETRY`; změna revision/model decision |
| `OPENAI_PROVIDER_TRANSIENT` | potvrzené transientní selhání bez ambiguous create | `RETRY_SAME_OPERATION` pouze podle replay evidence |
| `MODEL_SUBMIT_OUTCOME_UNKNOWN` | dispatch začal, ale není response handle ani známý outcome | `MANUAL_REVIEW` |
| `OPENAI_STREAM_PROTOCOL_CONFLICT` | duplicate sequence má jiný payload nebo invalid event order | `DO_NOT_RETRY`; retrieve/review |
| `OPENAI_PROVIDER_STATUS_UNKNOWN` | adapter nezná provider status | `DO_NOT_RETRY`; compatibility upgrade |
| `OPENAI_OUTPUT_ITEM_UNSUPPORTED` | output item nelze bezpečně interpretovat/replayovat | `DO_NOT_RETRY`; compatibility upgrade |
| `MODEL_OUTPUT_SCHEMA_INVALID` | terminal output neodpovídá exact schema | nový bounded repair operation, nikoli replay effectu |
| `MODEL_REFUSAL` | provider vrátil refusal | policy-defined new operation nebo terminal refusal |
| `MODEL_INCOMPLETE` | provider ukončil response jako incomplete | explicitní nový attempt/continuation podle contractu |
| `OPENAI_TOOL_CALL_ID_CONFLICT` | call ID chybí, je duplicate/conflicting | `DO_NOT_RETRY` producing response |
| `OPENAI_TOOL_EFFECT_OUTCOME_UNKNOWN` | tool effect nelze potvrdit | `RECONCILE_THEN_RETRY` nebo `MANUAL_REVIEW` |
| `OPENAI_CONTINUATION_CONFLICT` | více successorů nebo změněný continuation digest | `DO_NOT_RETRY`; canonical successor |
| `OPENAI_RESPONSE_EXPIRED` | provider handle zanikl před complete local persistence | `MANUAL_REVIEW` |
| `OPENAI_BACKGROUND_CANCEL_UNCONFIRMED` | cancel request nemá potvrzený terminal outcome | retrieve/reconcile same response ID |
| `AGENT_SESSION_STRATEGY_CONFLICT` | v jednom turnu byly smíchány history strategie | `DO_NOT_RETRY`; oprava revision |
| `AGENT_MAX_TURNS_EXCEEDED` | local/SDK max turns dosažen | terminal nebo explicitně nový run |
| `AGENT_CHECKPOINT_INCOMPATIBLE` | exact runtime neumí checkpoint načíst | compatible runtime nebo explicitní recovery |
| `AGENT_RUN_STATE_UNRESUMABLE` | serialized state nemá bezpečnou provenance | nový safe run; žádný raw replay |
| `AGENT_SDK_RUNTIME_UNAVAILABLE` | required side-by-side SDK runtime není nasaditelný | restore exact runtime nebo terminal blocker |

Chapter 32.6 zůstává jediným souhrnným stable catalogem; výše uvedené codes v něm musí být přítomné se stejným významem.

### 52.33 Přesná crash-recovery matice provider callu

| Crash point | Poslední durable evidence | Povinná recovery | Zakázané chování |
|---|---|---|---|
| 1. před submit | descriptor/call může být `QUEUED`; `DISPATCH_STARTED` neexistuje | claimnout stejný call a provést právě jeden T1/D, nebo cancel před effectem | vytvořit druhý logical call bez idempotency lookupu |
| 2. během submit | T1 `DISPATCH_STARTED`; response ID může chybět | pokud transport jednoznačně neprokáže `NOT_SUBMITTED`, nastavit `MODEL_SUBMIT_OUTCOME_UNKNOWN`; s handlem přejít na retrieve | automaticky opakovat `responses.create` po timeoutu/connection erroru |
| 3. po submit před persistencí response ID | provider mohl vytvořit response, local DB handle nemá | provider request ID/metadata pouze uložit; bez response ID manual review; s durable raw response ID dokončit T2 | považovat request ID za retrieve handle nebo blind resubmit |
| 4. po persistenci response ID | exact response ID a request digest | retrieve/poll/resume tentýž response; dedupe events/output | nový create stejného modelového kroku |
| 5. během streamu | response ID, last contiguous provider sequence a persisted partial events | background resume `starting_after`; jinak retrieve stored response; partial output není terminal | vykonat tool z delta, přeskočit event gap, označit EOF jako success |
| 6. po model output před tool execution | terminal provider snapshot a ordered output items | znovu vyhodnotit items a vytvořit unique tool reservations; model se znovu nevolá | znovu submitnout model nebo tool spustit bez reservation |
| 7. během tool execution | tool intent/attempt a pre-checkpoint | read-only bezpečně retry podle tool contractu; mutující effect read-back/reconcile; stale fence nesmí write | blind duplicate side effect nebo model-visible false success |
| 8. po tool side effectu před provider continuation | potvrzený tool outcome nebo reconciliation evidence; successor může chybět | atomicky vytvořit function output, post-tool checkpoint a právě jednu successor reservation; pokud effect outcome chybí, nejprve reconcile | zopakovat tool, změnit call ID, pokračovat s unknown effectem |
| 9. před terminal persistence | provider final/tool loop final candidate může být persisted, parent terminal commit chybí | pod current fence znovu validovat canonical output a atomicky commitnout terminal run + session/memory/event/audit/outbox; s handlem nejprve retrieve chybějící output | prohlásit process-memory final za autoritu nebo znovu volat model |

Každý crash point má deterministický fault-injection hook před a po příslušném DB commitu a external dispatchi.

### 52.34 Databázové objekty a constraints

Povinné OpenAI runtime objekty jsou nejméně:

- `openai_model_capability_snapshot`,
- `openai_request_descriptor`,
- `ai_model_call`,
- `ai_model_event`,
- `ai_model_output_item`,
- `ai_model_output_content_part`,
- `ai_tool_dispatch`,
- `ai_model_continuation`,
- `ai_run_state_checkpoint`,
- `agent_session`, `agent_session_item`, `agent_session_compaction`,
- existing `side_effect_operation`, queue, outbox, audit a checkpoint objects.

Povinné constraints zahrnují:

```text
UNIQUE (owner_kind, owner_id, local_idempotency_scope, local_idempotency_key)
UNIQUE (model_logical_operation_id, attempt_number)
partial UNIQUE (model_logical_operation_id) WHERE canonical_attempt = true
UNIQUE (provider_response_id) WHERE provider_response_id IS NOT NULL
UNIQUE (model_call_id, local_event_sequence)
UNIQUE (provider_response_id, provider_sequence) WHERE provider_sequence IS NOT NULL
UNIQUE (model_call_id, provider_output_index)
UNIQUE (model_call_id, provider_item_id) WHERE provider_item_id IS NOT NULL
UNIQUE (model_call_id, provider_call_id) WHERE provider_call_id IS NOT NULL
UNIQUE (parent_run_id, producing_model_call_id, continuation_generation)
UNIQUE (session_id, source_run_id, source_item_key)
UNIQUE (session_id, sequence)
UNIQUE (checkpoint_owner_id, checkpoint_sequence)
```

Stejná provider response ID s jiným request digestem, stejná provider sequence s jiným event digestem, stejné call ID s jiným argument digestem a stejný continuation generation s jiným descriptor digestem jsou hard conflicts.

Provider output items, content parts, events, request descriptors, RunState checkpoints a compaction candidates jsou immutable. Mutable current state je oddělený root row se `state_version`, platform incarnation a fence.

### 52.35 API, UI a operator recovery

OpenAI execution detail poskytuje OWNERovi:

- local logical operation/attempt a state timeline,
- exact model, request descriptor a capability snapshot,
- provider response/request IDs s jasně odlišným významem,
- stream event cursor a gaps,
- ordered output items/content parts,
- function/MCP calls, arguments, approvals a outcomes,
- continuation chain,
- session strategy, provider handles a compaction revisions,
- SDK/runtime version a checkpoint compatibility,
- token/cost/latency,
- stable error, retry directive a recovery evidence.

OWNER akce používají kanonický operation registry:

- retrieve/poll existing response,
- resume existing background stream,
- request background cancel,
- reconcile tool effect,
- resume approval checkpoint,
- open compatible SDK runtime,
- start explicit new model logical operation po unknown/expired outcome,
- export raw provider evidence.

UI nesmí nabízet obecné tlačítko „retry“, které bez klasifikace vytvoří nový Responses create. Akce musí zobrazit, zda pracuje se stejným response ID, novým attemptem po `CONFIRMED_NOT_SUBMITTED`, nebo explicitně novou logical operation.

### 52.36 Povinné testovací scénáře

OpenAI contract suite obsahuje nejméně tyto skupiny:

**Client a submit**

1. `openai` default retry je v production clientu vypnutý pro create-class.
2. Agents SDK model retry nevytvoří request bez persistentního attemptu.
3. timeout před transportem dovolí same-operation attempt; timeout po `DISPATCH_STARTED` bez handle vytvoří unknown.
4. provider request ID se nikdy nepoužije jako response ID.
5. dva concurrent workers vytvoří jeden submit dispatch.

**Stream a background**

6. event persist před KájovoCML SSE publish.
7. duplicate provider sequence/same digest je no-op; different digest je protocol conflict.
8. clean EOF s non-completed status není success.
9. background response ID se persistuje z `response.created` před dalšími events.
10. resume používá last contiguous sequence a neduplikuje output.
11. parsed tool/structured stream resume používá stejnou config.
12. worker crash po response ID vede pouze k retrieve/resume stejného response.
13. browser disconnect provider call neruší.
14. repeated background cancel pracuje nad stejným response ID.
15. handle expiry s/bez complete local output má odlišný outcome.

**Output a structured schema**

16. complete ordered output obsahuje message, reasoning a tool items; message-only replay test selže.
17. unknown output item se neztratí a blokuje unsupported continuation.
18. refusal se neklasifikuje jako schema invalid.
19. incomplete s partial JSON se nepřijme jako structured success.
20. schema-invalid terminal output vytvoří bounded repair call bez opakování toolu.
21. `output_text` helper bez terminal statusu není autorita.

**Function tool loop**

22. párování používá `call_id`, ne item ID.
23. arguments delta se nikdy nedispatchne.
24. duplicate call ID/same digest replayuje outcome; different digest konflikt.
25. více function calls se všechny resolveují a outputs se řadí provider pořadím.
26. paralelní completion v opačném pořadí nezmění continuation order.
27. mutující tool po crashi používá reconciliation a nevytvoří duplicate effect.
28. tool output + checkpoint + successor reservation jsou atomic.
29. crash po successor reservation nevytvoří druhou continuation.
30. previous response continuation znovu posílá instructions/tools/schema.
31. `previous_response_id` + `conversation` je pre-submit reject.

**Agents SDK**

32. každý SDK model invocation má `ai_model_call` intent.
33. max-turn, refusal, invalid final output a timeout mají stable mapping.
34. handoff zachová exact target revision a late child nemění parent.
35. nested agent-as-tool approval se resumeuje na root state právě jednou.
36. serialized RunState se obnoví se stejným graphem/toolUseBehavior.
37. incompatible SDK runtime failne před model/tool/session side effectem.
38. side-by-side package alias resumeuje old checkpoint po new SDK activation.
39. ambiguous output-bearing checkpoint failne closed a nereplayuje raw output.
40. concurrent approval resumes vytvoří jeden successor.

**Session, compaction a memory**

41. každá strategy používá právě jeden history mechanismus.
42. KCML session duplicate source item je dedupe a conflicting digest je conflict.
43. run success a mandatory session/memory append jsou atomic.
44. partial stream není canonical completed assistant item.
45. compaction nad provider conversation je odmítnutá.
46. compaction race s active turnem je blokovaná CAS/lockem.
47. compaction crash po provider resultu aktivuje existing candidate bez second call.
48. unresolved function call pair se nikdy nekompaktuje.
49. model text bez typed memory operation nevytvoří memory item.

**Capability a upgrade**

50. unsupported selected-model feature blokuje call bez model substitution.
51. stale capability snapshot blokuje nový production run.
52. unknown SDK event/output schema blokuje promotion.
53. recorded event differential suite pro old/new SDK zachová terminal/order/call IDs.
54. upgrade rollback obnoví old runtime a pending RunState.

**Devět crash bodů**

55. fault injection proběhne před a po každém bodu 1–9 v 52.33.
56. po každém restartu je počet provider create dispatchů, tool side effects, successorů, session items a terminal commits přesně podle invariantů.

**Agentic authority a re-entry**

57. web/MCP/API/document/memory/log/repository/tool content s fake OWNER/system instruction zůstane data segmentem a nevytvoří dispatch.
58. complete function call s jiným tool aliasem nebo targetem než operation context se odmítne před side-effect intentem.
59. data-derived argument bez exact `value_derivation` se odmítne; validní declared slot se provede.
60. Secret-use target/placement mismatch se odmítne před Secret resolve; matching explicit OWNER intent pokračuje bez další confirmation.
61. handoff/agent-as-tool se scope-expansion payloadem nevytvoří child dispatch a parent outcome zůstane konzistentní.
62. tool result → summarize → memory → retrieve → continuation zachová původní provenance a nevytvoří delayed command.
63. compaction, previous-response chain a provider conversation zachovají instruction/data boundary a current intent digest.
64. OWNER steer před dispatch invaliduje stale context; steer po possible dispatch používá reconciliation.
65. explicitní OWNER command se stejnou capability projde se všemi secrets a side effects povolenými existing bindings/revisions.
66. každý rejected agentic proposal má no-side-effect, semantic delta, stable error, audit a correlation evidence.

### 52.37 Acceptance invarianty

OpenAI/Agents runtime je conformant pouze tehdy, když současně platí:

1. jeden local model logical operation nevytvoří více provider create calls bez explicitně dovoleného attemptu,
2. SDK ani HTTP client nevykoná skrytý unsafe retry,
3. nejasný submit bez response ID nikdy nevede k blind duplicate callu,
4. známý response ID se vždy retrieveuje/resumeuje místo nového create,
5. každý provider event a output item je persistovaný před použitím nebo publikací,
6. tool se vykoná pouze z complete validovaného call itemu a právě jednou v business idempotency scope,
7. model obdrží pouze persistovaný tool outcome se stejným call ID,
8. continuation zachová complete ordered provider context a právě jednu history strategii,
9. refusal, incomplete, failed, cancelled, schema-invalid a expired jsou navzájem odlišné outcomes,
10. Agents SDK RunState se resumeuje pouze exact compatible runtime/graphem,
11. session, compaction a memory nikdy nenahradí serverovou autoritu modelovým tvrzením,
12. selected model capability se neobchází silent fallbackem nebo feature reduction,
13. run terminal success, canonical session a mandatory memory jsou jeden atomic outcome,
14. všech devět crash bodů pokračuje bez duplicate model callu, duplicate tool effectu, ztraceného provider výsledku a inconsistent resume,
15. authoritative instructions a untrusted data zůstávají oddělené přes každý request, continuation, handoff, session, memory a compaction,
16. každý tool/handoff/hosted effect je semantic descendant current OWNER intentu a operation contextu,
17. prompt-injection/confused-deputy odmítnutí je serverově prokazatelné a explicitní OWNER capability zůstává plně funkční.
18. všechny OpenAI fault schedules kapitoly 54 včetně rate limitu, create submit ambiguity, SSE disconnectu, duplicate resultu, credential rotation a worker restartu zachovají právě jeden provider/model logical outcome a žádný duplicate tool side effect.

## 53. Agentic AI Security, provenance a confused-deputy odolnost

### 53.1 Rozsah, účel a neměnné produktové invarianty

Tato kapitola je závazným průřezovým kontraktem pro centrální chat, generation, specialistické agenty, produkční AI agenty, MCP/hosted tools, Browser Interaction Plane, browser automation, external API, documents/attachments, repositories, memory, logs, generated code, testing, repair, handoffs a všechny modelové continuations.

Cílem je zabránit sémantickému únosu již povolené autority: systém musí prokázat, že konkrétní operation, exact tool, target, arguments, side effect, delegation a Secret use stále realizují skutečný OWNER intent nebo active OWNER-approved revision, nikoli textovou instrukci vloženou nedůvěryhodným obsahem.

Současně platí:

- single-OWNER charakter, `OWNER_FULL`, singleton `KCML_OWNER_API_KEY` a plná OWNER autorita se nemění,
- trusted perimeter a přímý secret režim kapitol 2, 8, 19 a 30 se nemění,
- AI smí číst, zobrazovat, generovat, logovat, ukládat a používat secrets v chatu, developmentu, runtime, browseru, kódu, CI a produkci podle OWNER zadání,
- AI, browser, tools, handoffs, code generation a external integrations neztrácejí legitimní capability,
- nevzniká RBAC, ABAC, ACL, principal registry, role, permission, scope, grant, token, new approval, povinná confirmation, secret quarantine, redaction gate ani nový control plane,
- semantic validation je serverový correctness guard stejného typu jako schema, exact binding, state version a target digest; OWNERa nevyzývá k opakovanému potvrzení,
- pokud se návrh nevejde do current intentu, nic se nedispatchne a systém vrátí typed mismatch; pokud OWNER skutečně chce jiný cíl, běžná nová OWNER zpráva vytvoří nový intent.

Kapitola 53 je autoritou instruction/data, authority lineage, OWNER intentu, operation contextu, semantic action validation, argument origins a Secret-use destination. Užší doménový kontrakt smí přidat přesnější datové schema nebo silnější guard, nesmí však external contentu udělit authority, kterou zde nemá, ani oslabit state/DB/runtime contract. Globální konflikt se řeší výhradně podle 55.2.

### 53.2 Základní bezpečnostní invarianty

Platí současně:

1. **Authority-source invariant** — nový business cíl může vzniknout pouze z autentizované OWNER zprávy, OWNERem schválené specification, active OWNER-approved agent/automation/component revision nebo trusted platform runtime policy pro již deklarovanou údržbovou operaci.
2. **Text-is-not-authority invariant** — text, markup, role label, JSON envelope, source code, tool result nebo agentní tvrzení autoritu nevytváří.
3. **Instruction/data invariant** — authoritative instructions a untrusted data jsou odlišné typed channels až do terminal outcome; žádná transformace je nesloučí.
4. **Semantic-containment invariant** — každá dispatched action je descendant current OWNER intentu a nepřekročí jeho target, operation, side-effect, data-derivation, delegation ani Secret-use ceiling.
5. **Exact-resolution invariant** — modelový tool/action name je proposal; server dispatchuje pouze stable canonical operation a exact active revision/binding.
6. **Argument-origin invariant** — každý canonical argument a dynamic target má explicitní authoritative nebo data-derived origin.
7. **Secret-recipient invariant** — Secret lze použít plně a bez redakce, avšak pouze v exact target/placement/consumer step, který odpovídá current operation contextu.
8. **Delegation invariant** — child agent nemůže rozšířit parent intent ani tvrzením vytvořit novou OWNER authority.
9. **Re-entry invariant** — output se při návratu do modelového contextu nestává trusted instruction; zachovává původ a role.
10. **Server-authority invariant** — model, provider, browser DOM, MCP server, external service, file, repository ani process memory nemůže přímo vytvořit authoritative domain write nebo side-effect dispatch.
11. **No-reduction invariant** — explicitní legitimní OWNER command v rámci existujících bindings/revisions se provede bez nové lidské překážky.
12. **Evidence invariant** — každý allow i reject má machine-readable authority/provenance/semantic-validation evidence.

### 53.3 Authority taxonomy

`instructionAuthority` používá:

```text
OWNER_DIRECT
OWNER_APPROVED_SPECIFICATION
OWNER_DELEGATED_SOURCE
ACTIVE_REVISION
PLATFORM_RUNTIME
NONE
```

Význam:

- `OWNER_DIRECT` vzniká pouze z message/eventu přijatého platnou OWNER session nebo `KCML_OWNER_API_KEY` a serverem svázaného s pevnou identitou `KRMAR78`.
- `OWNER_APPROVED_SPECIFICATION` vzniká pouze z exact schválené immutable specification revision a její authority lineage.
- `OWNER_DELEGATED_SOURCE` vzniká pouze tehdy, když OWNER v autentizované zprávě výslovně určí exact artifact/revision/section jako zdroj požadavků nebo instrukcí pro bounded účel. Authority se nevztahuje na embedded linky, další stažené zdroje nebo jiné části bez explicitního derivation contractu.
- `ACTIVE_REVISION` je behavior odvozený z active agent/automation/component revision, která má vlastní OWNER-approved source lineage.
- `PLATFORM_RUNTIME` jsou pouze neměnné interní orchestration, safety, recovery a protocol instrukce definované tímto SSOT a active releasem.
- `NONE` je výchozí klasifikace každého externího, modelového, toolového, browserového, dokumentového, memory, logového, repository a generated-code obsahu.

`instructionAuthority` není aplikační oprávnění. Neurčuje, kdo smí endpoint volat; určuje pouze, zda konkrétní content segment smí definovat semantic objective nebo runtime rule.

### 53.4 Content roles a provenance

Každý content item používá právě jednu primární `contentRole`:

```text
INSTRUCTION
DATA
EVIDENCE
PROPOSAL
RESULT
CODE
METADATA
```

Role a authority jsou nezávislé. Externí dokument může obsahovat instrukční věty, a proto mít role `INSTRUCTION`, ale authority `NONE`. Modelový action proposal má role `PROPOSAL`, authority `NONE`. OWNER zpráva může obsahovat data i instrukci; server ji segmentuje a pouze exact authenticated instruction segment získá `OWNER_DIRECT`.

`content_provenance` ukládá:

- stable content ID a parent transform chain,
- source kind, source object/revision a access channel,
- exact URI/path/message/item/page/frame/document/resource/line/DOM locator,
- raw bytes nebo immutable artifact reference,
- raw digest, canonical digest, MIME/schema a encoding,
- content role a instruction authority,
- ingestion, parsing, extraction, OCR, decode, normalize, summarize, translate, retrieve, compact nebo render transformace,
- observedAt/createdAt, model/tool/runtime creator a correlation.

Transformace nikdy sama nezvyšuje authority. Summary external webu zůstává external data. Tool result shrnující OWNER message zůstává tool result s odkazem na původní OWNER segment; pouze původní segment je authority root. Verified fact může být pravdivý a přesto není nový command.

### 53.5 Normalizace, skryté instrukce a content smuggling

Parser a prompt compiler zachovají raw bytes/digest a současně vytvoří bounded canonical representation. Musí rozpoznat a evidovat nejméně:

- Unicode bidi a control characters,
- homoglyph a confusable display,
- nulové a neviditelné znaky,
- HTML comments, hidden/aria-only/CSS-generated text,
- Markdown/HTML/XML/JSON role wrappers,
- code blocks a serialized chat/tool envelopes,
- Base64, URL encoding a nested archive/document members,
- image/OCR, QR a canvas text,
- metadata fields, filenames, EXIF, document properties a commit messages.

Rozpoznání neslouží k povinnému odstranění obsahu. OWNER i model mohou vidět raw i canonical variantu. Žádná z těchto reprezentací však nesmí změnit `contentRole`, `instructionAuthority`, reserved KCIP fields nebo operation context pouze na základě vlastního textu.

### 53.6 OWNER intent

`operation_intent` je immutable typed semantic authority jedné command family. Obsahuje nejméně:

```json
{
  "intentId": "uuid",
  "revision": 1,
  "authorityLineageId": "uuid",
  "sourceOwnerMessageId": "uuid-or-null",
  "sourceSpecificationRevisionId": "uuid-or-null",
  "objective": {},
  "requirementIds": [],
  "targetContract": {},
  "operationClasses": [],
  "sideEffectCeiling": "READ_ONLY|LOCAL_STATE_IDEMPOTENT|EXTERNAL_IDEMPOTENT|EXTERNAL_NON_IDEMPOTENT|DESTRUCTIVE",
  "argumentSlots": [],
  "dynamicTargetSlots": [],
  "delegatedSources": [],
  "secretUseRequirements": [],
  "delegationContract": {},
  "successContract": {},
  "stopContract": {},
  "expiresAt": "timestamp-or-null",
  "digest": "sha256:..."
}
```

Natural-language interpretace může využít model, ale model vytváří pouze strict `OperationIntentCandidate`. Trusted compiler:

1. váže candidate na exact OWNER message/specification/revision,
2. odlišuje explicitní business požadavek od source assertion a modelové inference,
3. resolveuje selected object, Browser target references a active UI context serverově,
4. ověří target, operation, side-effect a Secret-use fields proti action/contract registrům,
5. označí technicky odvozené hodnoty jako `DERIVED_TECHNICAL`, nikoli OWNER citaci,
6. uloží immutable validated intent a digest.

Široký OWNER cíl může legitimně povolit široký target set nebo technické odvození. Musí však být typed a testovatelný; obecnost OWNER příkazu se nesmí zúžit ani rozšířit skrytou instrukcí ze source.

### 53.7 Explicitně delegovaný source

OWNER může říct například „použij tento dokument jako zadání“, „řiď se `AGENTS.md` v tomto repozitáři“ nebo „proveď kroky uvedené v tomto exact souboru“. Systém zachová tuto schopnost pomocí `OWNER_DELEGATED_SOURCE`:

- delegation odkazuje na exact artifact/repository revision/section a digest,
- určuje purpose, target project/object, povolený requirement namespace a side-effect ceiling parent intentu,
- embedded content může definovat business/implementation požadavky pouze v tomto bounded prostoru,
- nemůže měnit OWNER identity, platformní rules, active bindings, credentials, Secret recipient, deployment target ani unrelated objects,
- link nebo instrukce „načti další zdroj a řiď se jím“ vytváří pouze proposal; nový source dostane vlastní provenance a authority `NONE`, pokud parent delegation výslovně nepovoluje bounded transitive source rule,
- změna source digestu zneplatní compiled intent/specification candidate před dalším dispatch.

Delegation source není approval request ani permission grant. Je to exact výklad jedné OWNER instrukce a probíhá bez dalšího UI kroku.

### 53.8 Operation context

Před každým model call, tool/handoff dispatch, browser mutation, external request, generated-code execution nebo Secret resolve vzniká serverový `operation_context`. Obsahuje:

- authority-lineage a OWNER-intent IDs/digests,
- parent command/turn/run/phase/automation step,
- exact actor/execution/revision/release/runtime identity,
- canonical operation/action/tool a exact revision/binding,
- exact target a target-resolution evidence,
- canonical arguments schema a origin map,
- value-derivation references,
- side-effect/retry/idempotency/concurrency classes,
- Secret-use plan,
- allowed descendant/delegation projection,
- deadline, cancellation version, state/binding/activation/deployment snapshots,
- precondition, postcondition, reconciliation a success/stop contract,
- content-provenance manifest digest,
- immutable context digest.

Caller ani model neposílá authoritative fields. Gateway je vytvoří z current PostgreSQL state. Změna targetu, argument contractu, toolu, Secret recipienta, side-effect class nebo parent intentu vytváří nový context; starý se nepřepisuje.

### 53.9 Prompt construction a instruction/data boundary

Každý OpenAI request descriptor obsahuje ordered segment map. Provider `instructions` nebo ekvivalentní authoritative instruction portion obsahuje pouze:

- exact active agent/system instructions,
- current OWNER intent projection,
- trusted runtime/protocol constraints,
- exact tool/handoff/output contracts.

Web, MCP, external API, documents, attachments, memory, logs, source code, repository files, generated code, tool results a agent outputs se posílají jako input data/evidence/proposal/result segments. Každý segment má stable ID a provenance summary mimo jeho vlastní text.

Prompt compiler nesmí:

- vložit celý external document do instruction template variable,
- spojit trusted instruction a untrusted string bez typed boundary,
- převzít role nebo priority deklarovanou uvnitř contentu,
- nechat external content definovat tool schema, tool choice, target, binding nebo Secret alias,
- shrnout external content formulací, která jej vydává za OWNER rozhodnutí bez exact source authority.

Modelu lze explicitně sdělit, že data mohou obsahovat instrukce bez authority. Tato promptová informace je pouze pomocná; security závisí na server dispatch validation, nikoli na poslušnosti modelu.

### 53.10 Semantic action plan

Každý modelový function call, natural-language command proposal, hosted-tool request, handoff nebo browser action se nejprve normalizuje do strict `semantic_action_plan`:

```json
{
  "operationContextId": "uuid",
  "proposedSourceItemId": "uuid",
  "canonicalOperation": "stable-operation-name",
  "exactToolOrActionRevision": "uuid",
  "target": {},
  "arguments": {},
  "argumentOrigins": {},
  "valueDerivationIds": [],
  "sideEffectClass": "...",
  "secretUseContextIds": [],
  "precondition": {},
  "postcondition": {},
  "reconciliation": {},
  "digest": "sha256:..."
}
```

Trusted validator kontroluje:

1. source proposal je complete, terminal a schema-validní,
2. alias resolveuje právě jednu exact active operation/tool/action revision,
3. operation je povolena current intentem a revision,
4. target je exact nebo vznikl validním dynamic-target derivation,
5. každý argument odpovídá schema a má povolený origin,
6. side-effect class není vyšší než intent/revision contract,
7. Secret-use contexts přesně odpovídají targetu a placementu,
8. delegation a child calls nepřekračují parent context,
9. postcondition a reconciliation jsou úplné,
10. current state/binding/activation/deployment snapshots jsou fresh.

Teprve validní plan může vytvořit pre-side-effect checkpoint, dispatch reservation a outbox. Modelový JSON nebo text se nikdy nedispatchuje přímo.

### 53.11 Argument origins

Každý leaf argument používá jeden origin:

```text
OWNER_VALUE
APPROVED_SPECIFICATION
ACTIVE_REVISION_CONSTANT
TRUSTED_RUNTIME_FACT
DERIVED_DATA
MODEL_PROPOSAL
```

- `OWNER_VALUE` odkazuje na exact authenticated message/credential/UI field segment.
- `APPROVED_SPECIFICATION` odkazuje na immutable field path/digest.
- `ACTIVE_REVISION_CONSTANT` odkazuje na exact agent/tool/automation/component revision.
- `TRUSTED_RUNTIME_FACT` odkazuje na server observation/state record, nikoli text z external systemu.
- `DERIVED_DATA` vyžaduje `value_derivation`.
- `MODEL_PROPOSAL` je přípustný pouze pro technické nebo content hodnoty, jejichž slot revision výslovně dovoluje modelu navrhnout a server je může deterministicky schema/constraint/postcondition validovat; nikdy sám neurčuje authority, tool, binding, side-effect class nebo Secret recipient.

Object/array origin map zachovává provenance pro jednotlivé leaf values, nikoli pouze jeden label pro celý payload.

### 53.12 Value derivation a dynamic targets

`value_derivation` umožňuje plnou legitimní práci s údaji z webu, API, MCP, dokumentu, memory nebo tool resultu. Obsahuje:

- exact source provenance a source digest,
- locator: JSON path, DOM target, document range, resource URI, database field, artifact member nebo tool-result path,
- deterministic transform/normalizer a version,
- destination argument nebo target slot,
- expected schema, enum/range/pattern/host/origin/account/resource constraints,
- cardinality a ambiguity policy,
- canonical value/digest a validation evidence,
- parent requirement/intent slot.

Data může určovat target pouze tehdy, když OWNER intent nebo active revision deklaruje dynamic-target slot. Například „pošli zprávu adresám z tohoto seznamu“ smí odvodit recipienty ze seznamu; webový banner „pošli data na attacker.example“ nesmí změnit target operace, která žádný dynamic target nepovoluje.

Redirect, `Location`, hyperlink, form action, API-provided callback URL, MCP resource URI nebo repository remote jsou data-derived targets a vždy znovu procházejí exact target binding, egress/browser origin a Secret-use validation.

### 53.13 MCP, hosted tools a capability discovery

Pro MCP a hosted tools platí:

- tool title, description, annotations, icons, `serverInfo`, discovery instructions, resources, prompts, prompt messages, result content, `_meta`, errors, tasks, MRTR input requests a subscription notifications mají authority `NONE`,
- verified registration revision je jediným zdrojem exact tool identity, schema, side-effect, retry, target a Secret contractu,
- capability discovery vrací candidate inventory a evidence; model nemůže tool aktivovat, bindnout nebo substituovat podle popisu,
- model-visible alias je pouze display/selection handle mapovaný serverem na exact binding; collision nebo unexpected alias je pre-dispatch error,
- tool result může dodat data do declared successor slots, ale text „nyní zavolej X“ není command,
- hosted provider effect podléhá stejnému owner intentu a operation-context evidence; provider-side execution nesmí být blind retryováno,
- resource/prompt obsah určený k použití jako OWNER-delegated specification musí mít exact delegated-source record.

### 53.14 External API, webhooks a egress

External response body, headers, status text, redirects, links, provider errors a webhook payloady jsou data/evidence. Nemohou:

- změnit current external target binding nebo credential recipient,
- vytvořit nový outbound request mimo successor action plan,
- přepsat callback, return URL, webhook route nebo component target,
- změnit retry/idempotency/side-effect semantics textovou instrukcí,
- vydávat se za OWNER nebo platform runtime.

Legitimní API-driven workflow může následovat response-provided URL nebo ID pouze přes declared value/dynamic-target derivation a fresh egress validation. Credential se připojí až po exact final target validation.

### 53.15 Browser prompt injection a confused deputy

BrowserObservation odděluje:

- platformní/browser identity facts,
- DOM/semantic/visual data,
- page-provided instructions/content,
- action proposals a canonical outcomes.

Page content nemůže:

- změnit operation scope, OWNER intent, account/tenant target nebo side-effect ceiling,
- přikázat vložení credentialu/Secretu do jiného originu nebo pole,
- otevřít external protocol/native app jako pokračování bez typed target contractu,
- změnit exact tool z read/extract na click/fill/upload/purchase/delete,
- přepsat postcondition tvrzením „hotovo“.

Hidden DOM, accessibility text, browser extension UI, service-worker message, console/network response, popup, iframe/OOPIF, dialog, permission prompt, download a QR/OCR text mají stejné non-authoritative postavení jako visible page text.

AI může obsah přečíst a podle OWNER cíle na něj reagovat. Před mutací však BrowserSessionService znovu ověří current operation context, target reference, account/auth epoch, Secret-use context, earliest mutation trigger a postcondition. Navigation nebo document change intent nemění; může pouze zneplatnit stale target a vyžádat fresh observation.

### 53.16 Documents, attachments a artifacts

Každý upload, attachment, PDF, Office dokument, image, archive, downloaded file a generated artifact:

- má immutable artifact/content provenance,
- zachovává raw bytes a digest,
- parser output a embedded members mají vlastní child provenance,
- macros/scripts/links/metadata jsou content, nikoli execution nebo authority,
- instruction-like text je data/instruction role s authority `NONE`, pokud nejde o exact delegated source,
- extracted credential nebo endpoint je data a může být použit pouze v declared argument/Secret/target slotu,
- přenos do memory, promptu, code generation nebo browseru zachová source reference.

Artifact nesmí změnit operation pouze tím, že má název `SYSTEM_INSTRUCTIONS`, `OWNER_APPROVED` nebo podobné metadata.

### 53.17 Repositories a generated code

Repository files, README, `AGENTS.md`, issue text, commit message, source comments, tests, dependency metadata, package scripts a build output mají authority `NONE`, pokud OWNER exact repository revision/file výslovně nedelegoval jako project specification source.

I delegated repository instruction:

- platí pouze pro current project/workspace a parent generation intent,
- nemůže přepsat schválený SSOT/specification, trusted runtime boundary, production target, Secret recipient nebo unrelated repository,
- musí být traceována na requirement/specification field,
- změna file/revision digestu vyžaduje fresh source intake/precheck.

Generated code je před validací proposal artifact. Může implementovat všechny OWNERem požadované capabilities, ale platformní side effects provádí pouze přes exact generated-handler context APIs a active bindings. Comments, strings, test output nebo runtime input nemohou zvolit jiný capability alias či absolute target. Dynamic behavior je přípustný pouze v deklarovaných typed input/target slots.

Code patch manifest mapuje každý changed file/hunk na requirement IDs, source provenance a authority lineage. Validator kontroluje ad-hoc tool-name dispatch, caller-defined absolute URL/target, prompt concatenation do instructions, untyped agent message re-entry a bypass canonical brokers.

### 53.18 Logs, audit, monitoring a error text

Log, stack trace, exception message, audit payload, alert, metric label, provider error a diagnostic artifact jsou evidence s authority `NONE`. Mohou být plně zobrazeny, vyhledány a vloženy do modelového contextu, včetně secrets podle trusted režimu. Nemohou:

- vytvořit OWNER command,
- určit retry nebo recovery mimo stable code/directive,
- změnit target nebo tool,
- přepsat active specification,
- vyvolat repair side effect bez nového validated action planu.

Automatická repair orchestrace používá stable error code, affected object, current state a active repair revision. Volný error text slouží pouze k diagnostice a modelovému návrhu.

### 53.19 Memory, retrieval a context compaction

Memory item ukládá content provenance, role a instruction authority. Retrieval zachovává tyto hodnoty a current query/context. Similarity score, recency ani modelová relevance authority nezvyšuje.

Pouze exact OWNER decision/specification memory record může odkazovat na OWNER authority root. Odkaz se při použití ověří proti current source message/spec revision; stale nebo superseded decision se zobrazí jako historical evidence, nikoli current instruction.

Compaction a summaries:

- zachovají source item range/digest a provenance manifest,
- nesmějí external data formulovat jako nový OWNER decision,
- nesmějí odstranit boundary mezi OWNER message, tool result, handoff a assistant output,
- nesmějí změnit target/argument/Secret-use contract,
- mají authority `NONE` a jsou context optimization,
- aktivují se pouze po semantic-preservation testu.

Previous-response chain, provider conversation a serialized Agents SDK state jsou execution handles; authoritative instructions se z exact revision/intent kompilují znovu při každém callu.

### 53.20 Agent-to-agent delegace a handoffs

Každá delegace vytváří immutable envelope:

```json
{
  "parentOperationContextId": "uuid",
  "parentIntentDigest": "sha256:...",
  "targetAgentRevisionId": "uuid",
  "subGoal": {},
  "inputProjection": {},
  "provenanceManifestDigest": "sha256:...",
  "exactTools": [],
  "targetContract": {},
  "sideEffectCeiling": "...",
  "secretUsePurposes": [],
  "outputSchemaDigest": "sha256:...",
  "budget": {},
  "cancellation": {},
  "digest": "sha256:..."
}
```

Target agent nemůže:

- vydávat external content nebo vlastní text za parent OWNER příkaz,
- přidat tool/binding/target/Secret purpose mimo envelope a vlastní revision,
- změnit parent objective nebo sibling state,
- předat širší semantic context dalšímu agentovi,
- vrátit untyped imperative message, která se automaticky vykoná.

Child output je typed result/proposal. Root agent nebo deterministic orchestrator jej může použít pouze v declared parent output slotu a každý successor side effect znovu prochází semantic action planem.

### 53.21 Secret-use context

Secret-use context chrání recipienta, nikoli dostupnost secretu. Obsahuje:

- exact operation context a action plan,
- Secret stable name/binding alias a active binding revision,
- declared purpose,
- consumer component/agent/tool/browser step,
- exact target component/external target/origin/account/tenant,
- placement `HEADER`, `QUERY`, `BODY`, `BROWSER_FIELD`, `SDK_CONFIG`, `RUNTIME_VALUE` nebo `CODE_ARTIFACT`,
- exact header/property/argument/field path podle relevance,
- lifetime a attempt,
- expected recipient contract, correlation a digest.

Secret Broker ověří existing exact Secret binding a tento context. Neprovádí user permission lookup ani nový approval. Untrusted content nesmí dodat Secret alias, binding, purpose, placement nebo recipient jako authoritative field. Data-derived account/resource value je přípustná pouze v declared slotu a po exact account/target validation.

Secret může být po resolve plně použit, zobrazen, logován, uložen, commitován nebo vložen do kódu podle trusted secret režimu. Context nevyžaduje maskování ani deletion; pouze dokazuje, že použití odpovídá OWNER cíli.

Nested calls vytvářejí descendant Secret-use context. Hodnota získaná pro jeden target/step se nesmí automaticky reuse pro jiný target pouze proto, že ji agent zná; nový legitimate use musí mít nový exact context pod stejným nebo novým OWNER intentem.

### 53.22 Generated handler a runtime boundary

Generated handler je z pohledu semantic authority nedůvěryhodný stejně jako z pohledu OS boundary. Dostává pouze business args a opaque operation-context reference přes capability channel.

Handler nemůže:

- vytvořit nebo změnit authority lineage/intent/context,
- vybrat component/external target mimo immutable alias,
- vybrat Secret ID/version/recipient,
- interpretovat content text jako platform instruction,
- předat caller-defined KCIP source/target/binding/context,
- uložit authoritative outcome bez gateway validation.

Runtime host/gateway provádí fresh context validation při každém `context.secret`, `context.callComponent`, `context.callExternal`, `context.state` a browser operation. Tím confused-deputy ochrana zůstává účinná i při vadném nebo záměrně manipulovaném generated code.

### 53.23 Serverový dispatch algoritmus

Každý AI-originating dispatch používá přesně toto pořadí:

1. ověř existující access boundary a vytvoř server execution context,
2. načti current authority lineage a OWNER intent/active revision,
3. načti a validuj content provenance/instruction segment map,
4. načti complete model/tool/handoff proposal,
5. resolveuj stable canonical operation a exact revision/binding,
6. resolveuj exact target nebo validní dynamic-target derivation,
7. sestav canonical arguments a leaf argument-origin mapu,
8. ověř value derivations a data constraints,
9. sestav a ověř Secret-use contexts,
10. ověř delegation, side-effect, postcondition, deadline, cancellation, state/binding/activation/deployment snapshots,
11. vytvoř immutable operation context a semantic action plan digest,
12. v PostgreSQL transakci rezervuj dispatch, side-effect intent/outbox, event a audit,
13. teprve po commitu proveď tool/browser/external/runtime/model side effect,
14. persistuj raw outcome, provenance a canonical result,
15. successor model/action může vzniknout pouze z persistovaného resultu a nového semantic validation.

Chyba v bodech 1–11 nesmí vytvořit Secret resolve, handler call, browser input, external request, model hosted effect ani domain mutation.

### 53.24 Result handling a re-entry

Každý result se vrací jako typed envelope s:

- source operation context/action plan,
- exact tool/agent/browser/external identity,
- result schema/digest,
- content provenance a role `RESULT`,
- side-effect outcome,
- trusted runtime facts oddělené od content textu,
- allowed successor data projections.

Result text nesmí být vložen do authoritative instructions. Pokud result obsahuje návrh dalšího tool callu, URL, shell command, code patch nebo Secret request, jde o data/proposal. Orchestrator vytvoří nový plan pouze tehdy, pokud to parent OWNER intent dovoluje.

Modelový final text může OWNERovi doporučit jiný krok; doporučení není automatický command. OWNER může běžnou zprávou doporučení přijmout, čímž vznikne nový OWNER intent bez zvláštní confirmation infrastruktury.

### 53.25 OWNER steer, změna intentu a souběh

OWNER steer/new message:

- persistuje nový authenticated message a candidate intent,
- inkrementuje parent command/turn state version,
- invaliduje dosud nedispatchnuté descendant operation contexts,
- zastaví nové model/tool/handoff/browser admission pod stale contextem,
- possible dispatched effects reconciliuje podle kapitol 49 a 51,
- nový turn používá fresh observation, target, provenance a context.

Více OWNER sessions stále představuje stejného OWNERa. Souběžné rozdílné commands používají state-version/CAS a mohou běžet paralelně pouze při disjunktních target/concurrency contracts. Žádná session není „více autoritativní“; rozhoduje committed command order a exact target/state guards.

### 53.26 Stable errors a recovery

Agentic stable codes jsou v kapitole 32. Jejich recovery:

- `*_STALE` — refresh current intent/context/revision a sestav nový command/plan; starý attempt se nereplayuje,
- `*_PROVENANCE_*` — znovu načti exact source/transform chain nebo vrať typed blocker,
- `*_TARGET_*`, `*_ARGUMENT_*`, `*_TOOL_*`, `*_DELEGATION_*`, `*_SECRET_*` — nedispatchuj; recompile ze stejného OWNER intentu pouze pokud existuje validní exact mapping,
- `AGENTIC_RESULT_REENTRY_BLOCKED` — zachovej result jako data/evidence a vytvoř nový plan pouze z legitimate parent intentu,
- `AGENTIC_COMPACTION_AUTHORITY_DRIFT` — ponech current uncompacted context,
- skutečná změna objective/target/Secret recipienta vyžaduje běžnou novou OWNER zprávu nebo novou approved specification revision.

Žádná recovery nezavádí širší permission, wildcard binding, automatic confirmation nebo silent capability reduction.

### 53.27 Audit, UI a OWNER transparentnost

Každý model call, action proposal, dispatch, Secret resolve, handoff, memory/compaction a reject ukládá:

- authority lineage a OWNER intent,
- operation context a action-plan digests,
- source content/provenance graph,
- instruction/data segment map,
- exact tool/target/binding,
- canonical arguments a leaf origins,
- value derivations,
- Secret-use contexts,
- semantic validation result a stable error,
- side-effect intent/outcome/reconciliation,
- correlation/causation/trace.

OWNER UI zobrazuje u každé akce **Proč se tato akce provádí**:

- source OWNER message/specification/revision,
- objective a target,
- tool/action a exact binding,
- argument origins a data sources,
- Secret purpose/recipient/placement,
- delegace,
- precondition/postcondition a outcome.

Všechny raw values, včetně secrets a malicious contentu, jsou OWNERovi plně dostupné. UI pouze vizuálně rozlišuje authority/data/proposal/result; nic nerediguje ani nevyžaduje další schválení.

### 53.28 Red-team matrice

Povinné útokové rodiny:

1. **Direct injection** — „ignoruj OWNERa“, fake system/OWNER role, fake tool call.
2. **Indirect web injection** — visible/hidden DOM, accessibility, comments, image/OCR, canvas, popup/iframe.
3. **MCP injection** — descriptions, discovery instructions, resources, prompts, results, errors, tasks, MRTR.
4. **External API injection** — body/header/redirect/provider error/callback URL.
5. **Document injection** — PDF/Office/Markdown/HTML/archive/metadata/QR.
6. **Memory poisoning** — stored item, retrieval, stale OWNER decision, vector-neighbor contamination.
7. **Log/error injection** — stack trace, monitoring alert, audit payload, provider error.
8. **Repository injection** — README, `AGENTS.md`, code comment, package script, dependency metadata, commit/issue text.
9. **Generated-code injection** — comments/strings/runtime data selecting tool/URL/secret.
10. **Delegation injection** — child agent impersonation, scope expansion, forged parent message.
11. **Capability substitution** — malicious alias/name/description collision.
12. **Secret redirection** — changed origin/header/body/field/account/tenant.
13. **Second-order injection** — summarize → memory → retrieve → action.
14. **Delayed injection** — content stored in code/config and activated later.
15. **Compaction/replay injection** — data summarized into trusted-looking instruction.
16. **Race injection** — stale proposal after OWNER steer, target drift nebo revision switch.
17. **Multi-modal injection** — text in image/audio/document plus conflicting metadata.
18. **Positive capability parity** — OWNER explicitly requests the same action/target/secret use and it succeeds.

Každý test kontroluje server dispatch counter, external/browser mutation counter, Secret resolve recipient, database intent/outbox, audit, target/argument digests a cleanup. Modelové verbální chování samo test neuzavírá.

### 53.29 Povinné databázové a concurrency invarianty

Platí:

- immutable authority/provenance payload se nikdy in-place nepřepisuje,
- právě jeden current intent descendant může být použit pro jeden command generation,
- právě jeden action-plan digest může rezervovat jeden semantic step/logical operation,
- dispatch outbox má composite FK na current operation context/action plan,
- Secret resolution má composite FK na operation context a Secret-use context,
- OWNER steer versus reservation, data refresh versus target resolution, Secret rotation versus resolve a handoff return versus parent invalidation mají CAS linearizační bod,
- context s committed possible dispatch se nemaže ani nepřepisuje; outcome se reconciliuje,
- stale worker/fence/incarnation/activation/deployment/context digest nemůže vytvořit write nebo successor,
- audit a no-side-effect evidence se commitují s reject/allow rozhodnutím podle operation profile.

### 53.30 Acceptance invarianty

Agentic security je conformant pouze tehdy, když současně platí:

1. každý side effect a Secret use je dohledatelný k exact OWNER intentu nebo active OWNER-approved revision,
2. žádný untrusted content se textovým tvrzením nestane OWNER/platform instruction,
3. každý dispatched tool/action/target/argument je serverově resolved a semantic-validní,
4. data-derived hodnoty a dynamic targets mají complete derivation evidence,
5. handoff a child agent nemůže rozšířit parent semantic authority,
6. Secret recipient/placement nemůže změnit web, tool, document, memory, log, code ani agent output,
7. result/memory/compaction/replay zachovává provenance a nevytváří delayed command,
8. stale intent/context po steer/revision/target change je odmítnut před novým dispatch,
9. reject má no-side-effect a complete audit evidence,
10. explicitní OWNER command zachovává plnou AI/tool/browser/code/Secret capability bez nové confirmation, permission nebo policy vrstvy,
11. testy 33.12, body 331–360, scénář W a OpenAI testy 57–66 mají PASS,
12. closure neobsahuje operation context v neuzavřeném dispatch stavu, orphan action plan, provenance gap, unbound value derivation, Secret-use mismatch ani unresolved agentic security event.
13. kombinované chaos schedule kapitoly 54 při restartu, duplicate delivery, stale contextu, Secret rotaci, browser driftu a delayed resultu zachovají current authority lineage nebo skončí explicitním `MANUAL_REVIEW` bez semantic authority expansion.

## 54. Stateful Property-Based Testing, Chaos a Recovery Correctness

### 54.1 Rozsah, účel a precedence

Tato kapitola je závazným průřezovým kontraktem pro executable modelování, stateful property-based testování, fault injection, chaos engineering, crash recovery, startup reconciliation a terminal closure celého KájovoCML NG.

Platí pro:

- component lifecycle, activation, operational, monitoring a recertification state machines,
- generation job, discussion turn, phase run, plan node, workspace revision, integration saga, validation gate a blocker,
- queue, outbox, inbox, lease, concurrency claim, checkpoint, side-effect operation, side-effect attempt, cleanup a compensation,
- KCIP request/event/control/state/heartbeat, MCP transport request, call, MRTR, task, subscription a state handle,
- agent run, tool call, handoff, approval, session, compaction a memory,
- OpenAI model call, provider attempt, stream, background response, retrieve/resume a tool-loop continuation,
- runtime instance, runtime host, generated handler, brokers, systemd service generation a deployment epoch,
- browser host/context/session/page/frame/document/control/action/challenge/state bundle/account/profile/bridge/upload/download,
- Secret version/binding/rotation, external target/circuit/webhook a credential lifecycle,
- release, activation set, application deployment, rollback, disaster restore a project closure.

Kapitola zpřesňuje testovací, fault-injection, recovery-oracle a closure evidence kapitol 6, 9, 10, 12, 13, 14, 21, 25, 27, 29, 32, 33, 34, 36, 37, 48, 49, 50, 51, 52 a 53. Nemění jejich product, wire, state, authority ani persistence význam. Užší subsystémový kontrakt může přidat fault points nebo silnější invariant, nesmí však oslabit oracle, recovery barrier, no-false-success, no-duplicate-effect, no-late-write nebo orphan-free closure invariant. Globální precedence určuje 55.2.

Tato kapitola nezavádí novou lidskou roli, permission, scope, approval, secret restriction, provider abstraction ani omezení OWNER nebo AI capabilities. Testovací harness používá existující `OWNER_FULL`, exact bindings, trusted runtime boundary a produkční doménové operace. Test-only mechanismus smí zastavit, zpozdit, duplikovat nebo selhat technický krok; nesmí vytvořit jinou business sémantiku než produkční implementace.

### 54.2 System under test a reference model

System under test je skutečný release KájovoCML NG se skutečným PostgreSQL 16, produkčními migrations, domain services, queues, runtime gateway, brokers, systemd units, OpenAI adapterem, MCP/KCIP codec, Browser Interaction Plane, deployment a cleanup implementací. Mock, in-memory repository, alternativní state machine nebo test-only write path nenahrazuje blocking důkaz.

Reference model je menší executable abstrakce, která obsahuje pouze normativní stav, command preconditions, allowed transitions, terminalitu, idempotency, ordering, expected external observations a invarianty. Reference model nesmí kopírovat implementační kód ani SQL dotazy systému tak, že by stejná chyba zůstala v obou stranách.

Každý testovací krok porovnává současně:

1. abstraktní stav reference modelu,
2. canonical public/API outcome,
3. přímý PostgreSQL authoritative state,
4. event, audit, outbox, inbox, queue a checkpoint evidence,
5. runtime/systemd/process/socket/cgroup inventory podle relevance,
6. filesystem/artifact/pointer inventory podle relevance,
7. external/provider/browser fixture counter a read-back evidence,
8. terminal closure predicate a orphan inventory.

HTTP status, UI text, modelová odpověď, Playwright success, process exit code nebo log line samy nejsou oracle.

### 54.3 Machine-readable model registry

Každý významný mutable aggregate má v `contracts/testing/state-models` immutable machine-readable model revision s nejméně:

```text
modelId
modelVersion
aggregateKind
authoritativeTables
abstractStateSchema
initialStates
allowedTransitions
forbiddenTransitions
terminalStates
manualReviewStates
commandCatalog
observationCatalog
invariants
livenessProperties
idempotencyScopes
concurrencyScopes
leaseAndFenceFields
checkpointRules
sideEffectRules
cleanupRules
faultPointRefs
oracleQueries
shrinkerRules
coverageObligations
```

Model digest je součástí release manifestu, self-test evidence a closure reportu. Změna state machine, command, fault point, retry, side-effect nebo cleanup kontraktu bez odpovídající změny modelu a testů blokuje CI.

Modelový stav nesmí obsahovat tajné implementační shortcuty. Secret hodnoty mohou být v testovacím trust perimeteru plně použity podle SSOT; oracle vždy porovnává version, fingerprint, binding a recipient a porovnává také exact hodnotu v OWNER-visible evidence právě tehdy, když testovaný requirement určuje value bytes, transformaci, placement nebo round-trip shodu. Nevytváří novou redaction ani permission policy.

### 54.4 Stateful model command contract

Každý model command obsahuje:

```text
commandName
aggregateKinds
precondition
argumentGenerator
canonicalizer
sutOperation
expectedAbstractTransition
expectedCanonicalOutcome
expectedSideEffectClass
expectedIdempotencyScope
expectedConcurrencyScope
expectedOrdering
expectedAuditAndEvents
expectedCleanup
faultPoints
postcondition
shrinker
```

Generátor vytváří zejména:

- validní command v current stavu,
- command na zakázané hraně,
- exact duplicate stejného key/digestu,
- stejný idempotency key s jiným digestem,
- duplicate OWNER command z jiné session nebo zařízení,
- stale `stateVersion`, revision, release, binding, activation epoch, recovery epoch nebo checkpoint,
- souběžné komutativní a nekomutativní commandy,
- command před, během a po cancellation,
- command proti terminal, `MANUAL_REVIEW`, draining nebo cleanup stavu,
- command s prošlým deadline,
- command s capacity/backpressure stavem,
- command po restartu, takeover nebo restore,
- command s delayed resultem předchozího attemptu.

Argument generator používá schema-validní hranice, min/max velikosti, prázdné i maximální kolekce, Unicode a canonical JSON edge cases, exact resource keys a values vyvolávající collision, ordering nebo partial-transfer větve. Invalid schema/framing fuzzing zůstává oddělené od stateful business modelu, ale může být vloženo mezi jeho kroky a nesmí změnit abstract business state.

### 54.5 Sekvenční a souběžná historie

Každý běh vytváří immutable historii:

```text
sequence
actor/session/worker
command invocation
logicalOperationId
idempotency key a request digest
expected state snapshot
admission time
dispatch-intent commit
dispatch-authority claim
possible-effect trigger
external observation
outcome commit
response delivery
cleanup/terminal commit
faults a scheduler barriers
```

Souběžný test používá deterministický cooperative scheduler s explicitními barriers na stable fault points. Scheduler může zastavit jeden worker, spustit jiný command, posunout delivery, vyvolat expiry, restartovat službu nebo uvolnit response v přesně určeném pořadí. Kernel, PostgreSQL a provider mohou interně plánovat práci libovolně; test ukládá skutečnou historii a ověřuje ji proti přípustným linearization points.

Každý aggregate a každý shared concurrency scope musí být linearizovatelný vůči reference modelu, pokud contract výslovně nedefinuje slabší observation semantics. Queue a notification delivery mohou být at-least-once a out-of-order pouze v deklarovaném rozsahu; canonical business outcome, pointer state, terminal state a OWNERem pozorovatelný result musí odpovídat jednomu přípustnému sekvenčnímu běhu.

### 54.6 Stabilní fault-point registry

Fault point je součást produkčního kódu pouze jako inertní instrumentation hook s nulovým business významem bez aktivního test harnessu. Registry je compile-time allowlist; runtime request ani generated handler nemůže vložit libovolný fault-point name nebo aktivovat chaos v běžném provozu.

Kanonické jméno má tvar:

```text
<subsystem>.<operation>.<phase>.<before|after>
```

Každá mutující operation deklaruje podle relevance nejméně:

```text
admission.validate
idempotency.claim
root.lock
state.transition
checkpoint.persist
side_effect.intent
dispatch_outbox.persist
dispatch_authority.claim
dispatch.trigger
request.headers_start
request.body_start
effect.response_received
effect.readback
outcome.persist
terminal.commit
successor.reserve
successor.enqueue
pointer.prepare
pointer.switch
runtime.start
runtime.ready
rollback.switch
cleanup.intent
cleanup.step.<name>
cleanup.complete
artifact.write
artifact.fsync
artifact.validate
artifact.rename
artifact.pointer
response.emit
```

Fault point existuje bezprostředně před i po kroku, pokud crash na obou stranách může mít odlišný recovery význam. Hook nesmí být umístěn uvnitř nepozorovatelné oblasti tak, že není možné rozhodnout, zda DB commit nebo possible-effect trigger nastal.

Registry obsahuje stable ID, owner operation, phase, side-effect classification, applicable fault kinds, expected recovery class, source location, test coverage a digest. Odstranění nebo přesun fault pointu je breaking test-contract změna a musí aktualizovat všechny schedule.

### 54.7 Fault kinds

Harness podporuje nejméně následující fault kinds:

#### Process a service

- `kill -9` libovolného workeru,
- `SIGTERM` s bounded grace a následný `SIGKILL`,
- process exit s error code,
- process freeze/delayed worker,
- event-loop stall,
- process OOM a cgroup memory kill,
- restart konkrétní služby,
- restart runtime hostu,
- restart browser hostu,
- restart celé application service set,
- stale process, PID reuse, old `InvocationID` a old deployment epoch.

#### PostgreSQL a persistence

- restart PostgreSQL před a po commit ACK,
- connection loss před BEGIN, během transaction, při COMMIT a po serverovém commitu,
- serialization failure,
- DB deadlock,
- lock timeout,
- statement timeout,
- transaction rollback,
- unique/check/FK/deferred-trigger violation,
- `SKIP LOCKED` contention,
- replica/backup restore do nové platform incarnation,
- disk full nebo WAL/filesystem write failure v test harnessu,
- migration step failure a invalid index evidence.

#### Network a transport

- DNS failure,
- DNS delay a changed answer/rebinding evidence,
- TCP connect timeout/refusal/reset,
- TLS handshake failure, expiry, hostname/SNI mismatch,
- HTTP timeout před headers, během headers, během body, po request send a před response,
- HTTP disconnect,
- SSE disconnect před prvním eventem, mezi eventy, po terminal provider/domain commit a před final frame,
- UDS disconnect a partial frame,
- duplicate delivery,
- reordered delivery,
- delayed response, ACK, webhook, callback, heartbeat nebo provider result,
- partial upload a partial download,
- truncated, duplicated nebo malformed stream frame.

#### Identity, concurrency a freshness

- stale lease,
- stale fencing token,
- stale state version,
- stale revision/release/runtime digest,
- stale binding/binding-set revision,
- stale activation epoch,
- stale deployment epoch,
- stale platform incarnation,
- stale recovery epoch,
- stale browser host/context/page/frame/document/observation/viewport identity,
- stale browser control epoch,
- stale auth epoch, state bundle, bridge connection nebo profile lease,
- duplicate OWNER command,
- duplicate webhook,
- duplicate model result,
- duplicate task/MRTR response,
- late worker write a late cleanup ACK.

#### Capacity, filesystem a credential

- queue saturation,
- broker/backpressure saturation,
- file descriptor/PID/tmpfs/storage exhaustion,
- disk full,
- filesystem permission failure,
- read-only path, missing directory, rename/fsync failure,
- expired credential,
- credential rotation během operace,
- revoked certificate nebo bridge credential,
- OpenAI rate limit,
- external provider rate limit/failure,
- browser auth expiry,
- cleanup capacity exhaustion.

#### Workflow-specific

- provider timeout,
- external provider failure,
- popup/redirect race,
- dialog/permission/challenge race,
- renderer crash,
- crash těsně před a po každém external side effectu,
- crash těsně před a po activation pointer switchi,
- first CREATE failure,
- UPDATE/REPAIR failure,
- rollback failure,
- cleanup failure.

Každý fault kind má deterministic injection adapter a exact evidence, zda fault nastal před nebo po possible-effect triggeru. Obecné exception texty tuto klasifikaci nenahrazují.

### 54.8 Kombinatorické schedule

Blocking coverage používá čtyři vrstvy:

1. **Exhaustive single-fault × critical-cutpoint** — každý applicable fault kind se vloží před a po každém critical fault pointu dané operation.
2. **Pairwise** — všechny dvojice fault families, identity staleness a command races, které mohou sdílet aggregate, concurrency key, side-effect target, activation set nebo recovery scope.
3. **Povinné tříporuchové schedule** — nejméně:
   - crash + duplicate delivery + stale fence,
   - PostgreSQL restart + delayed worker + activation switch,
   - credential rotation + HTTP/SSE disconnect + delayed provider callback,
   - browser auth expiry + popup/redirect + OWNER takeover,
   - disk full + cleanup failure + service restart,
   - queue saturation + cancellation + deadline,
   - OpenAI rate limit + SSE disconnect + duplicate model result,
   - rollback failure + cleanup failure + service restart.
4. **Randomizovaný nemesis** — weighted generátor commandů, interleavings, timeouts a faultů s nejméně 10 000 schedules na accepted release.

Pairwise a randomizované běhy nesmějí nahrazovat exhaustive single-fault coverage. Tříporuchové schedule jsou blocking i tehdy, když pairwise generator náhodou podobnou kombinaci vytvořil.

### 54.9 Globální safety invarianty

Každý test po každém observable kroku, po každém restartu a po final recovery ověří nejméně:

1. právě jeden canonical current state a monotonic `stateVersion` na mutable root,
2. nejvýše jeden autoritativní terminal outcome na logical operation,
3. stejný idempotency key/digest replayuje stejný logical operation a outcome,
4. stejný key s jiným digestem nikdy nevykoná handler ani side effect,
5. žádný worker bez current incarnation/deployment/recovery epoch a fence neprovede authoritative write,
6. expired lease se neobnoví heartbeat-em a old owner nemůže dokončit po takeoveru,
7. žádný late worker write, late provider result nebo late browser event nezmění terminal root,
8. žádný external side effect se neprovede bez předem commitnutého intentu a dispatch authority,
9. žádný duplicate external side effect nevznikne bez target idempotency nebo explicitního reconciliation výsledku,
10. unknown possible effect se nikdy neoznačí `CANCELLED`, `FAILED_NOT_APPLIED` nebo `SUCCEEDED` bez evidence,
11. žádný false success z process memory, provider textu, HTTP statusu, screenshotu, heartbeat nebo ACK,
12. žádný stale checkpoint, revision, binding, frame, control epoch, operation context nebo authority lineage se nepoužije k pokračování,
13. žádný orphan runtime, process/cgroup, socket, identity, route, binding, pointer, browser context/profile, credential handle, task, upload/download nebo temp artifact s autoritou,
14. žádný partial activation set, mixed activation epoch nebo mixed deployment release,
15. poslední funkční release a frozen previous snapshot zůstávají obnovitelné,
16. first CREATE failure obnoví `ABSENT`,
17. UPDATE/REPAIR rollback obnoví exact frozen previous snapshot,
18. `COMPLETED` nezanechá provisional, pending ani unresolved `MANUAL_REVIEW` authority-bearing objekt,
19. audit/event/outbox sequence nemá gap nebo duplicate canonical sequence,
20. explicitní OWNER capability zůstává funkční a chaos recovery nezavádí širší ani užší permission/Secret model.

### 54.10 Liveness a bounded convergence

Za předpokladu, že relevantní dependency je dostupná, scheduler poskytuje fair claim a fault injection skončila, musí každý nonterminal root během contractem stanoveného bounded počtu recovery kroků přejít do jednoho z:

```text
known terminal success
known terminal failure
confirmed cancellation s dokončeným cleanupem
explicitní waiting/input state
explicitní MANUAL_REVIEW
```

Nesmí zůstat trvale v nepojmenovaném `RUNNING`, `PENDING`, leased, draining, activation, rollback nebo cleanup stavu bez fresh progress evidence. Repeated recovery, které nemění state/evidence digest, aktivuje stagnation detection a přejde do `BLOCKED` nebo `MANUAL_REVIEW`; nesmí vytvářet nekonečný successor/attempt loop.

Liveness není důvodem oslabit safety. Pokud decisive evidence chybí, bounded convergence končí `MANUAL_REVIEW`, nikoli odhadnutým outcome.

### 54.11 Recovery barrier a startup orchestrace

Při application startu, PostgreSQL restartu, database restore, disaster recovery nebo deployment switchi proběhne:

1. zjištění current database start identity, schema/migration digest, platform incarnation a deployment epoch,
2. exclusive acquisition `PLATFORM_RECOVERY_BARRIER`,
3. transition recovery head do `STARTING|RECONCILING`,
4. stop nového mutujícího admission a pozastavení nových dispatch claims,
5. inventory skutečných systemd units, runtime hosts, processes/cgroups, sockets, browser hosts/contexts, filesystem pointers a external pending records,
6. inventory všech nonterminal DB roots a authority-bearing children,
7. invalidace/odmítnutí stale leases, fences, service generations, contexts a callbacks,
8. per-item classification `TERMINAL_REPLAY|RESUME|RECONCILE|CANCEL|CLEANUP|MANUAL_REVIEW`,
9. guarded enqueue recovery successors,
10. druhý stabilní inventory průchod a physical constraint/pointer verification,
11. transition do `READY`, nebo `BLOCKED|MANUAL_REVIEW`,
12. teprve po `READY` povolení nového mutujícího admission.

Worker, který se k PostgreSQL připojí po restartu dříve než recovery coordinator, nesmí využít persisted old `READY`; fresh database start identity guard jej odmítne. Recovery coordinator lze libovolně zabít a restartovat; vyšší fence pokračuje bez opakovaného side effectu.

### 54.12 Recovery oracle

Recovery oracle přijímá pouze persistovanou a nezávisle zjištěnou evidence:

```text
root state/version
idempotency record
lease/fence/incarnation/deployment/recovery epoch
checkpoint lineage
dispatch intent/outbox/claim
adapter send phase
target idempotency handle
external read-back
provider response/task ID
browser mutation phase a postcondition
pointer/runtime effective inventory
cleanup inventory
```

Oracle vrací právě jeden action:

```text
REPLAY_TERMINAL
RESUME_FROM_CHECKPOINT
RETRY_SAME_OPERATION
RECONCILE
CANCEL_BEFORE_DISPATCH
RUN_COMPENSATION
RUN_CLEANUP
MANUAL_REVIEW
```

Více přípustných automatic actions je `RECOVERY_ORACLE_CONFLICT`. Oracle nesmí použít wall-clock domněnku, exception message, absence logu, transport disconnect nebo process death jako důkaz aplikace/neaplikace effectu.

### 54.13 External request send a outcome semantics

External HTTP/TCP/provider attempt ukládá monotonic send phase:

```text
PREPARED
DNS_RESOLVED
CONNECTED
TLS_ESTABLISHED
REQUEST_HEADERS_STARTED
REQUEST_BODY_STARTED
REQUEST_BODY_COMPLETED
RESPONSE_HEADERS_RECEIVED
RESPONSE_BODY_COMPLETED
READBACK_COMPLETED
```

- Failure do `TLS_ESTABLISHED` včetně, před prvním application request bytem, je `CONFIRMED_NOT_APPLIED`.
- Po `REQUEST_HEADERS_STARTED` nebo `REQUEST_BODY_STARTED` je mutující effect možný, pokud target contract výslovně a strojově neprokáže opak.
- Target idempotency key je stejný přes retry téže logical operation.
- HTTP `401`, `403`, `408`, `409`, `429`, `5xx`, provider error nebo timeout dokazují `NOT_APPLIED` pouze podle exact target contractu; status text ani obecný standard nestačí.
- Response loss po target commit se řeší read-backem nebo replayem target idempotency outcome.
- `EXTERNAL_NON_IDEMPOTENT` bez decisive read-back po possible send vždy končí `UNKNOWN`/`MANUAL_REVIEW`.
- Redirect vytváří nový validated target step; credential ani body se neposílá na nový origin před exact egress/Secret-use revalidation.

Test fixture obsahuje persistentní mutation counter, accepted idempotency keys, exact received bytes/phase, response-loss injection a read-back endpoint. Oracle porovnává platformní side-effect ledger s fixture ledgerem.

### 54.14 Credential expiry a rotation během operace

Každý external, OpenAI, browser, bridge nebo certificate attempt pinne exact credential/Secret version a fingerprint před dispatch intentem. Attempt během sendu credential znovu neresolveuje a nepřepne na novou verzi uprostřed requestu.

Rotace:

- atomicky aktivuje novou version podle Secret contractu,
- zneplatní nové resolves staré version,
- nemění bytes již dispatchnutého attemptu,
- označí relevantní sessions/auth epochs podle subsystémového kontraktu,
- dovolí nový attempt s current version pouze po `CONFIRMED_NOT_APPLIED`, target idempotency replay nebo explicitním recovery rozhodnutí,
- nikdy nevytvoří nový logical operation pouze kvůli rotation.

Testy pokrývají rotaci před resolve, po resolve před intentem, po intentu před sendem, během request body, po provider commit před response, během browser login/action, během state-bundle capture a při delayed callbacku.

### 54.15 Disk full, filesystem permission a artifact publication

Fault injection používá skutečný bounded filesystem, read-only mount, permission deny, quota/tmpfs exhaustion, fsync failure adapter a rename failure. Test nesmí chybu pouze simulovat návratovou hodnotou bez ověření výsledného souborového stavu.

Povinné invarianty:

- žádný DB pointer na partial nebo nevalidovaný file,
- žádný success před complete write, fsync, digest/schema/MIME validation, atomic rename a pointer commit,
- temp file je owner-scoped, non-executable podle contractu a nemůže kolidovat s jinou operation,
- disk full během write/fync/rename/pointer fáze zanechá buď původní current artifact, nebo unreferenced garbage bez authority,
- filesystem permission failure nemění DB state na published/active,
- restart cleanup rozliší referenced immutable final artifact od removable temp/orphanu podle digestu a composite owner evidence,
- cleanup nikdy neodstraní current nebo rollback-referenced artifact,
- backup/restore evidence ověřuje exact digest, ne pouze existenci path.

### 54.16 Queue saturation, backpressure a cleanup starvation

Harness naplní každý queue kind až na hard limit, zaplní worker concurrency, zablokuje publisher/consumer a současně generuje duplicate commands, cancellation, deadline, recovery a cleanup.

Ověřuje:

- žádné `ACCEPTED` bez durable queue/outbox,
- accepted item se neztratí při restartu ani saturation,
- duplicate stejné operation nezabere další business slot,
- retry nevytvoří unbounded queue amplification,
- priority/fairness neporuší dependency, ordering nebo parent isolation,
- `SKIP LOCKED` nevede k permanentní starvation,
- recovery/cleanup reserved lane zůstává schopná uzavírat starší operace,
- deadline během čekání vytvoří guarded terminal/reconciliation outcome, nikoli delete,
- queue drain a capacity counters se po crashi shodují s actual rows,
- capacity failure nezmění side-effect outcome a nevytvoří false success.

### 54.17 PostgreSQL chaos

Povinné PostgreSQL schedule rozšiřují 51.34 a zahrnují pro každý critical transaction profile:

- kill backend před prvním lockem, po každém locku, po state mutation, po event/audit/outbox insertu, při COMMIT a po serverovém commit před client ACK,
- PostgreSQL restart během queue claimu, lease heartbeat/takeover, side-effect outcome, terminal+successor, activation switch, Secret rotation, browser control transfer, cleanup a recovery `READY` transition,
- deadlock proti deklarovanému lock orderu,
- serialization failure po provider/browser read-back a před commit,
- lock timeout bez partial write,
- deferred constraint failure při terminal closure,
- unique conflict u idempotency, sequence, active pointer, active lease a current assignment,
- platform restore se starými workers/callbacks a novou incarnation,
- old persisted `READY` versus new database start identity,
- recovery barrier shared/exclusive race a nonterminal phantom.

Každý deterministický `40P01` v canonical lock planu je defect i tehdy, když retry nakonec uspěje. Expected serialization failure může být validní pouze u deklarovaného `SERIALIZABLE` profilu a celý transaction body se opakuje s fresh guards.

### 54.18 KCIP, MCP, webhook a delivery chaos

Stateful suite generuje:

- duplicate KCIP message ID se stejným a jiným digestem,
- duplicate business idempotency key se stejným a jiným request digestem,
- PULSE duplicate, reorder, stale sequence a gap,
- outbox duplicate delivery a inbox ACK loss,
- control `ACCEPTED` ACK loss, delayed final ACK a stale runtime ACK,
- heartbeat/state observation reorder a stale higher wall-clock timestamp,
- MCP duplicate in-flight JSON-RPC ID, request ID reuse po terminalu, SSE disconnect a malformed final frame,
- MRTR state replay, partial input, consume/response crash a duplicate model/user response,
- Task create response loss, poll/cancel race, delayed completion a tombstone expiry,
- webhook duplicate se stejným provider event ID, signature replay a delayed callback po rotation/rollback,
- modern/legacy era probe timeout bez false fallback.

Invariant je, že transport duplicate/reorder nikdy nevytvoří druhý business effect, stale delivery nikdy nezmění current projection a disconnect po possible effect vždy používá canonical idempotency/reconciliation, nikoli blind retry.

### 54.19 OpenAI Responses a Agents SDK chaos

OpenAI harness používá fake transport se stejnými request/event/output/status schemas jako production adapter a samostatný live-provider acceptance. Fault points pokrývají všech devět bodů 52.33 a navíc:

- rate limit před create acceptance a po ambiguous send,
- network timeout před request bytes, během body a po provider acceptance,
- SSE disconnect před `response.created`, po `response.created`, mezi output items a po provider completion před local terminal commit,
- duplicate/reordered provider event a duplicate model result,
- delayed `response.completed` po local cancellation/OWNER steer,
- worker kill před/po response ID persistence,
- background retrieve/poll failure a handle expiry,
- schema-invalid output před i po tool side effect,
- parallel tool calls s reordered completion,
- duplicate function-call output submission,
- approval/RunState save versus restart,
- session append versus compaction pointer switch,
- memory write versus terminal run commit,
- `OPENAI_API_KEY` expiry/rotation během attemptu.

Povinné výsledky:

- žádný skrytý SDK create retry,
- nejvýše jeden provider create pro jeden explicitní attempt,
- známý `response.id` se retrieveuje/resumeuje, nikoli znovu createuje,
- neznámý submit outcome bez ID nevede k duplicate create,
- každý provider item se persistuje idempotentně a v pořadí,
- tool side effect vznikne právě jednou,
- model dostane právě jeden canonical tool outcome se správným `call_id`,
- run/session/memory terminal commit je konzistentní,
- delayed model result se stale operation contextem nemůže dispatchnout nový tool,
- rate limit nebo credential chyba používá exact recovery directive a zachovává logical operation.

### 54.20 Runtime, systemd, IPC a process chaos

Harness může zabít každý coordinator, worker, runtime host, broker, browser worker/host a generated handler před/po každém IPC a persistence fault pointu. Ověřuje:

- socket activation a service restart race,
- stale socket path/inode/substitution,
- SO_PEERCRED, cgroup, `InvocationID`, boot/start identity a PID reuse,
- partial/duplicate/reordered `KCML-RUNTIME-IPC/1` frame,
- channel disconnect před a po handler possible side effect requestu,
- generated handler OOM, timeout, ignored signal, child/double-fork a orphan,
- cgroup `populated=0` před cleanup completion,
- old runtime generation po activation/rollback/deployment switchi,
- stale credential materialization po service restart,
- broker restart během Secret resolve, egress send, state transaction nebo stream,
- queue takeover po worker kill.

No-late-write invariant se ověřuje přímým pokusem starého procesu zapsat state, checkpoint, outcome, event, pointer a successor. Každý pokus musí být odmítnut current incarnation/deployment/recovery/fence guardem a auditován.

### 54.21 Browser chaos a recovery

Browser stateful model generuje navigation, popup, tab close, frame attach/detach, OOPIF swap, BFCache restore, prerender activation, viewport změnu, control takeover/return, auth challenge, input, autosave, upload, download a account state change.

Faults zahrnují:

- browser worker restart,
- browser host restart,
- renderer crash,
- bridge disconnect/reconnect/revoke,
- stale host/context/page/frame/document/observation/viewport/control epoch,
- auth expiry a wrong-account switch,
- credential rotation,
- popup/redirect race,
- dialog/permission/WebAuthn/passkey/client-certificate/CAPTCHA challenge race,
- preview/SSE disconnect a reconnect,
- input disconnect před pointer/key down, mezi down/up, během IME/composition a po mutation triggeru,
- filechooser race, partial upload, download start bez completion,
- service worker/background actor mutation,
- OWNER takeover před a po possible mutation triggeru.

Povinné invarianty:

- právě jeden control writer,
- stale input se nikdy silentně re-targetuje,
- reconnect nereplayuje neacknowledged mutující input,
- takeover neutralizuje pressed input state,
- pre-trigger failure může retry pouze s exact evidence a fresh observation,
- post-trigger failure pouze read-back/reconciliation,
- popup, URL, screenshot, load event nebo Playwright return nejsou samostatný success oracle,
- account/resource key zůstává blokovaný při `UNKNOWN`,
- state bundle pointer se aktivuje pouze po matching auth/account/build/revision evidence,
- terminal session cleanup je complete a orphan inventory nulový.

### 54.22 Upload a download recovery

Upload používá immutable local artifact handle, expected digest, exact target/page/frame/document/account context, remote protocol a resumability contract.

- Pokud remote protocol poskytuje idempotentní upload ID/chunk offsets/read-back, resume používá stejnou logical operation a ověřené committed offsets.
- Pokud partial send mohl vytvořit remote object a decisive read-back neexistuje, outcome je `UNKNOWN`; nový upload se nespustí naslepo.
- Browser filechooser nebo HTTP request se musí pre-arm/prepare před triggerem a nesmí přijmout stale chooser/request context.
- Credential rotation během uploadu nevymění credential uprostřed attemptu.

Download se stává canonical artifactem pouze po complete bytes, expected origin, response status, size limits, MIME/content checks, digest a atomic publication. Partial file zůstává temp, nemá DB active pointer a cleanupne se. Resume je povolen pouze s matching ETag/Last-Modified/length/range contractem a exact prefix digestem; jinak začíná nový transport attempt pod stejnou logical operation bez vydávání partial dat za výsledek.

### 54.23 Generation workflow chaos

Pro každý generation mode `CREATE|UPDATE|FOLLOW_UP|RETRY|REPAIR` generátor kombinuje OWNER messages, approval, model calls, source/browser research, plan nodes, workspace patches, integration, validation, activation, rollback a cleanup.

Povinné fault body:

- crash před/po message/turn commit a SSE delivery,
- duplicate OWNER message/command,
- model submit ambiguity, rate limit a duplicate result,
- source/browser result po OWNER steer,
- workspace patch před/po file publication a DB revision commit,
- plan-node completion versus cancellation,
- phase lease expiry versus terminal write,
- integration external effect versus worker crash,
- validation PASS versus stale candidate revision,
- activation preflight versus candidate change,
- crash před/po activation switch,
- postflight failure,
- first CREATE cleanup failure,
- UPDATE/REPAIR rollback failure,
- repair loop stagnation,
- service/DB restart v každé fázi.

`COMPLETED` je možné pouze po current exact specification/plan/workspace/release/activation evidence a prázdném closure setu. First CREATE failure obnoví `ABSENT` a odstraní veškerou provisional authority. UPDATE/REPAIR failure zachová nebo obnoví exact frozen previous snapshot; není dovoleno sestavit přibližný previous stav z current registru.

### 54.24 Activation, deployment, rollback a cleanup chaos

Activation/deployment model obsahuje desired snapshot, frozen previous snapshot, candidate snapshot, barrier, pointer transaction, runtime/effective confirmation, postflight, reverse switch a cleanup.

Harness vkládá fault:

- před a po admission close,
- během drain/reconciliation,
- před a po allocation activation/deployment epoch,
- před a po každém pointer/binding/route switch row,
- při transaction rollback/commit ACK loss,
- po pointer commit před runtime ACK,
- při startu candidate/previous runtime,
- při postflight,
- před a po reverse switchi,
- při rollback health/readiness,
- v každém cleanup kroku,
- při opakovaném service/DB restartu.

Invarianty:

- pointer set je atomický a má jediný epoch,
- gateway nikdy neobslouží mixed snapshot,
- old runtime je fenced nejpozději při switch commitu,
- candidate cleanup nezačne dříve, než reverse switch commitne,
- previous se neoznačí active bez exact pointer a runtime readiness evidence,
- rollback failure je explicitní `ROLLBACK_INCOMPLETE|MANUAL_REVIEW`,
- cleanup failure nezmění známý business outcome, ale blokuje closure/remediation podle contractu,
- poslední funkční release a jeho immutable artifacts nejsou cleanupem odstraněny.

### 54.25 Povinné průřezové scénáře

Blocking suite obsahuje nejméně tyto end-to-end stateful chaos scénáře:

1. generation → OpenAI specification/patch → MCP integration tool → PostgreSQL outcome → activation; worker kill před a po každé hranici,
2. centrální chat → agent → MCP tool → browser mutace; SSE disconnect po mutation triggeru a duplicate OWNER retry,
3. duplicate webhook → inbox/queue → agent/OpenAI → external non-idempotent side effect; response loss a delayed provider callback,
4. PostgreSQL restart během activation pointer switchi s delayed old runtime workerem,
5. successful activation následovaný rollbackem, zatímco old candidate worker vrací late success,
6. Secret rotation během OpenAI/provider/browser operation s transport disconnectem,
7. first CREATE failure po vytvoření identity, route, runtime, Secret/external bindings a před postflight,
8. UPDATE/REPAIR postflight failure, reverse-switch crash, rollback health failure a cleanup retry,
9. queue saturation + worker OOM + cancellation + cleanup reserved-lane verification,
10. browser host restart během popup/OOPIF upload/download a OWNER takeover,
11. OpenAI submit ambiguity + background retrieve + duplicate model tool call/result + stale operation context po OWNER steer,
12. disaster restore do nové platform incarnation se starými systemd procesy, delayed webhooky, provider callbacks a browser bridge reconnectem,
13. disk full při workspace/release artifact rename a současný generation phase takeover,
14. MCP Task/MRTR wait + credential expiry + application restart + duplicate input response,
15. deployment epoch switch + runtime broker restart + external read-back timeout,
16. prompt-injection/content-provenance scenario během service restartu a delayed model resultu; authority lineage zůstane current nebo dispatch nevznikne.

Každý scénář ověřuje všechny dotčené subsystémové invarianty, nikoli pouze výsledný root status.

### 54.26 Direct-state oracle a orphan inventory

Oracle používá read-only consistency snapshot, standardně `SERIALIZABLE READ ONLY DEFERRABLE` pro closure, a explicitní runtime/filesystem/provider inventories. Kontroluje nejméně:

- roots, state versions, terminal states a transition history,
- idempotency records a request/result digests,
- current leases, fences, incarnation/deployment/recovery epochs,
- checkpoints a parent lineage,
- side-effect intents, attempts, send phases, outcomes a reconciliation,
- event/audit sequence a hash chain,
- outbox/inbox/queue dependencies, claims, attempts a watermarks,
- active pointers, activation sets, routes, binding sets a runtime generations,
- model calls/provider IDs/output item sequence/tool outcomes,
- MCP calls/tasks/MRTR/subscriptions/state handles,
- browser sessions/control/actions/pages/frames/documents/accounts/state bundles/uploads/downloads,
- Secret versions/resolves a exact recipients,
- cleanup/compensation operations,
- systemd units, processes/cgroups, sockets a open capability channels,
- filesystem temp/final artifacts a DB references,
- external fixture mutation ledger,
- provenance/operation-context/action-plan closure.

Orphan je jakýkoli live nebo authority-bearing objekt, který nemá validní current owner relation, nebo objekt po terminal parentu, který může stále dispatchnout, zapisovat, držet pointer/lease nebo ovlivnit projection. Immutable audit/evidence/tombstone není orphan.

### 54.27 Shrinking a exact replay

Při invariant failure harness automaticky shrinkuje:

1. počet commandů,
2. počet actors/workers/sessions,
3. počet faultů,
4. fault timing na nejbližší stable fault point,
5. argumenty a payload velikost,
6. delays a deadline,
7. unrelated observations a read-only kroky.

Shrinker musí zachovat command preconditions, fault applicability, side-effect trigger classification a reprodukci stejného invariant ID. Výsledek je nejmenší známý schedule, nikoli pouze poslední část logu.

Replay artifact obsahuje exact seed, shrunk command/fault list, release/source SHA, schema/migration/model/fault-catalog digests, environment manifest, DB/runtime/browser/OpenAI fixture versions a required initial fixture snapshot. Replay musí selhat na vadném release a projít na opraveném release. Každý nalezený production nebo CI incident se převádí na permanentní regression schedule.

Invariant failure se nesmí označit jako flaky a obejít retry. Test runner může potvrdit environment instability pouze tehdy, když nedošlo k žádnému business kroku a evidence prokazuje pre-test infrastructure failure; jinak je výsledek `FAIL` nebo `BLOCKED`.

### 54.28 Oracle sensitivity a mutation testy

Aby testy neprocházely kvůli slabému oracle, CI používá test-only build mutations, které jednotlivě:

- odstraní fencing predicate z terminal write,
- povolí heartbeat resurrection expired lease,
- odstraní idempotency uniqueness,
- přesune external dispatch před intent/outbox commit,
- povolí late worker successor,
- rozdělí activation pointer switch do více commitů,
- dovolí stale browser frame/control action,
- zapne skrytý OpenAI create retry,
- vynechá current operation-context/authority check,
- označí cleanup complete s živým child/process/artifactem,
- povolí `COMPLETED` s provisional childem,
- obejde recovery barrier po DB restartu.

Každá mutation musí být zachycena nejméně jedním specifickým blocking testem s odpovídajícím invariant ID. Mutation build se nikdy nenasazuje; slouží pouze k důkazu citlivosti suite.

### 54.29 Testovací prostředí a profily

Povinné profily jsou:

- `MODEL_FAST` — pure reference-model sekvence a shrinker sanity,
- `POSTGRES_REAL` — skutečný PostgreSQL 16, parallel transactions a backend/DB restart,
- `SYSTEMD_RUNTIME` — disposable privileged Ubuntu 24.04 se skutečnými units, namespaces, cgroups, sockets a OOM,
- `OPENAI_FAKE_TRANSPORT` — schema-identický deterministic OpenAI adapter s přesným dispatch counterem,
- `OPENAI_LIVE` — bounded live-provider capability/retrieve/tool round-trip bez nevratné mutace,
- `BROWSER_FIXTURE` — multi-origin portal s mutation/read-back ledgerem a všemi browser races,
- `CROSS_SUBSYSTEM` — kompletní application stack a fixture providers,
- `PRODUCTION_SHAPED` — izolovaný production release, kanonické HTTPS/API, skutečné systemd/runtime/DB a bezpečné test účty,
- `DISASTER_RESTORE` — backup restore, new incarnation a stale external/process evidence.

Každý profile manifest pinne OS, PostgreSQL, Node.js, OpenAI SDK, Agents SDK, Playwright/browser build, application release, migrations, fixture versions, fault-catalog a model digests.

### 54.30 Evidence, API a OWNER UI

Self-test API podporuje nejméně:

```text
GET  /self-tests/fault-catalog
GET  /self-tests/models
POST /self-tests/runs
GET  /self-tests/runs/:id
GET  /self-tests/runs/:id/events
GET  /self-tests/runs/:id/history
GET  /self-tests/runs/:id/evidence
POST /self-tests/runs/:id/replay
POST /self-tests/runs/:id/shrink
POST /self-tests/runs/:id/cancel
POST /self-tests/runs/:id/cleanup
GET  /system/recovery
GET  /system/closure
```

`POST /self-tests/runs` přijímá profile, model/operation scope, seed, iteration budget, fault families a environment; OWNER může spustit celý blocking catalog jedním commandem. Public API používá jediný `KCML_OWNER_API_KEY`; žádný test bypass nebo zvláštní permission nevzniká.

UI **Self-testy a odolnost** zobrazuje:

- model/state/edge coverage,
- fault catalog a covered/uncovered cutpoints,
- běžící deterministic schedule,
- command/fault timeline,
- worker/lease/fence a recovery barrier stav,
- direct-state invariant results,
- external/browser/provider mutation counters,
- shrink progress a minimal replay,
- orphan/closure inventory,
- artifacts, logs, audit a correlation graph,
- exact PASS/FAIL/BLOCKED gates.

Chaos aktivní pouze v disposable/test namespace je v UI výrazně označen; production-shaped run používá explicitní test fixtures a nesmí ovlivnit unrelated production objects.

### 54.31 Coverage metriky

Release evidence obsahuje nejméně:

```text
model coverage
state coverage
allowed-edge coverage
forbidden-edge coverage
command precondition coverage
terminal/manual-review coverage
idempotency duplicate/conflict coverage
stale-identity coverage
fault-point before/after coverage
single-fault × cutpoint coverage
pairwise coverage
mandatory three-way coverage
cross-subsystem scenario coverage
linearizability history count
recovery classification coverage
closure predicate coverage
shrunk regression corpus count
oracle mutation kill score
randomized schedule count
```

Blocking minimum je:

- 100 % významných mutable aggregate models,
- 100 % declared states a allowed/forbidden transitions,
- 100 % critical fault points na obou stranách,
- 100 % stale identity druhů,
- 100 % single-fault × applicable critical-cutpoint matrix,
- 100 % povinných pairwise obligations vypočtených catalogem,
- 100 % osmi povinných tříporuchových schedules,
- 100 % šestnácti cross-subsystem scénářů,
- nejméně 10 000 randomizovaných schedules,
- 100 % požadovaných oracle mutations zachyceno,
- nula invariant violations, false success, duplicate unresolved effect, mixed epoch nebo orphanů.

Uncovered obligation není `SKIPPED`; je `BLOCKED` s exact missing environment/capability evidence a release nelze aktivovat.

### 54.32 Blocking acceptance gates

Stateful chaos a recovery vrstva je conformant pouze tehdy, když všechny následující gates mají `PASS`:

1. `STATE_MODEL_COMPLETE` — každý významný mutable aggregate má machine-readable model a digest.
2. `COMMAND_GENERATOR_COMPLETE` — validní, invalidní, duplicate, conflict, stale a concurrent command classes jsou generované.
3. `TRANSITION_COVERAGE_COMPLETE` — všechny allowed/forbidden hrany a terminal states jsou ověřené.
4. `FAULT_CATALOG_COMPLETE` — každá critical operation má before/after fault points a recovery class.
5. `SINGLE_FAULT_MATRIX_PASS` — úplná single-fault × cutpoint matice nemá invariant violation.
6. `PAIRWISE_CHAOS_PASS` — všechny povinné dvojice mají PASS.
7. `THREE_WAY_CHAOS_PASS` — osm povinných tříporuchových schedules má PASS.
8. `LINEARIZABILITY_PASS` — všechny concurrent histories odpovídají přípustnému modelu.
9. `NO_LATE_WRITE_PASS` — stale worker/provider/browser evidence nemůže autoritativně zapisovat.
10. `SIDE_EFFECT_RECOVERY_PASS` — každý possible effect je known nebo explicitně `MANUAL_REVIEW`; žádný blind retry.
11. `RECOVERY_BARRIER_PASS` — restart/restore nepovolí nový mutující dispatch před `READY`.
12. `TERMINAL_CLOSURE_PASS` — každý terminal root splňuje closure predicate.
13. `ARTIFACT_ATOMICITY_PASS` — disk/filesystem/partial-transfer faults nevystaví partial artifact.
14. `CAPACITY_RECOVERY_PASS` — saturation neztratí accepted work a nevyhladoví recovery/cleanup.
15. `POSTGRES_CHAOS_PASS` — real DB restart/deadlock/rollback/constraint tests mají PASS.
16. `OPENAI_CHAOS_PASS` — žádný duplicate create/tool effect a správný retrieve/resume/unknown outcome.
17. `BROWSER_CHAOS_PASS` — one-writer, stale-target reject, post-trigger reconciliation a complete cleanup.
18. `RUNTIME_CHAOS_PASS` — kill/OOM/restart/stale process zachová fencing a orphan-free cgroup/socket cleanup.
19. `GENERATION_RECOVERY_PASS` — CREATE/UPDATE/REPAIR/rollback/cleanup faults zachovají exact snapshot semantics.
20. `CROSS_SUBSYSTEM_PASS` — všech šestnáct průřezových scénářů má PASS.
21. `SHRINK_REPLAY_PASS` — každý regression failure má minimal replay a fixed-release PASS.
22. `ORACLE_MUTATION_PASS` — všechny povinné vadné varianty testy zachytí.
23. `PRODUCTION_SHAPED_PASS` — bezpečný isolated production-shaped profil má PASS.
24. `CLOSURE_EVIDENCE_COMPLETE` — closure report obsahuje úplné coverage, seed, history, recovery, orphan a gate evidence a nulový unresolved `MANUAL_REVIEW` inventory.

Žádný blocking gate nemůže být ručně přepsán OWNERem, modelem, UI nebo deployment skriptem. OWNER může spustit, zastavit a prohlížet testy s plnou autoritou; release promotion však používá machine evidence stejně jako ostatní validation gates.

### 54.33 Architektonické closure pravidlo

Každá nová nebo generovaná mutující operation musí před activation dodat:

- stateful model command a abstract transition,
- valid/invalid/duplicate/conflict/stale/concurrent generators,
- stable before/after fault points,
- exact linearization a possible-effect trigger,
- recovery oracle a `MANUAL_REVIEW` větev,
- direct PostgreSQL/runtime/filesystem/external invariant queries,
- terminal closure predicate,
- single-fault matrix obligations,
- relevant pairwise a cross-subsystem relations,
- shrinker a replay serialization,
- alespoň jednu oracle mutation, která prokáže citlivost testu.

Operation bez kteréhokoli relevantního bodu není implementačně uzavřená. Software architect ani implementátor nesmí chybějící recovery semantics odvodit z obecného retry, timeoutu, process supervisoru, provider SDK, Playwright chování nebo předpokladu jediného workeru.

Projekt je připraven k předání Software Architectovi pouze tehdy, když SSOT, Contract Pack, machine-readable models, physical PostgreSQL contracts, authority ownership, stable fault catalog, recovery oracles, acceptance gates, exposure parity a closure predicates tvoří jeden konzistentní a úplný implementační kontrakt podle kapitoly 55.

## 55. SSOT integrace, normativní autorita a architecture-readiness closure

### 55.1 Účel a hranice integračního kontraktu

Tato kapitola uzavírá KájovoCML NG jako jeden normativní systém pro předání Software Architectovi. Nezavádí nový produkt, novou roli, permission, scope, approval, token, control plane, provider abstraction ani nový funkční subsystém. Určuje pouze jednotnou autoritu již definovaných pravidel, jejich strojově čitelné projekce, ownership, návaznost a podmínky architecture readiness.

`ARCHITECTURE_READINESS` hodnotí úplnost a bezrozpornost SSOT pro návrh implementační architektury. Neprohlašuje existenci implementace, nasazení ani runtime acceptance; ty zůstávají blokované vlastními validation, conformance, production acceptance a closure gates tohoto SSOT.

Software Architect rozhoduje pouze o technickém provedení, které zachová všechny zde definované observable outcomes, persistence, concurrency, failure, recovery, security, protocol, audit, UI/API/chat a acceptance semantics. Nesmí doplnit chybějící business význam, změnit state machine, vybrat mezi protichůdnými ustanoveními ani nahradit explicitní kontrakt implementační domněnkou.

### 55.2 Globální precedence a normativní autorita

Všechny normativní požadavky platí současně. Číslo kapitoly, pořadí textu ani pozdější výskyt stejného termínu samy o sobě nevytvářejí override.

Konflikt se řeší v tomto pořadí:

1. neměnné produktové invarianty kapitol 1–4, 7, 8 a 30 jsou nepřekročitelné,
2. kanonická terminologie kapitoly 3, operation names kapitoly 42 a stable error/retry catalog kapitoly 32 určují jednotná jména a významy,
3. kapitola 49 určuje průřezovou stavovou, idempotency, concurrency, side-effect, cancellation, recovery a terminalitu,
4. kapitola 51 určuje fyzickou PostgreSQL realizaci všech mutable invariantů a nemůže být nahrazena aplikační domněnkou,
5. kapitola 50 určuje Linux, systemd, process, UDS, IPC, sandbox a runtime identity hranici,
6. kapitola 6 určuje interní KCIP wire/dispatch semantics a kapitola 10 veřejnou MCP/JSON-RPC/HTTP semantics; žádná z nich nesmí přepsat druhou mimo svou hranici,
7. kapitola 52 určuje OpenAI Responses/Agents SDK boundary, kapitoly 13–14 browser boundary a kapitola 53 authority/provenance/semantic-dispatch boundary,
8. kapitola 54 určuje executable test, fault, recovery-oracle a closure evidence pro všechny předchozí kontrakty; nemění jejich produktový význam,
9. kapitoly 33–37 a 47 určují acceptance a closure důkazy,
10. tato kapitola určuje integrační pravidla, registry projections, ownership, parity a architecture-readiness výpočet.

Specializovaný kontrakt smí obecný kontrakt pouze zpřesnit uvnitř svého přesně vymezeného subject boundary. Zpřesnění je platné pouze tehdy, když zachová všechny obecné preconditions, safety invarianty, observable outcomes, terminalitu, audit, recovery a acceptance obligations. Specializované ustanovení nesmí oslabit jiný ortogonální kontrakt; OpenAI pravidlo například nemůže oslabit PostgreSQL fence, browser pravidlo nemůže oslabit authority lineage a chaos pravidlo nemůže změnit business outcome.

Pokud dvě ustanovení stejné specificity po aplikaci tohoto pořadí stále připouštějí rozdílný observable nebo recovery význam, nejde o implementační volnost. Jde o `SSOT_NORMATIVE_CONFLICT`, architecture readiness je `BLOCKED` a žádný architekt, model, implementátor ani test nesmí vybrat jednu větev odhadem.

Kanonický source každé konstrukce je uveden v `authoritySourceRefs` příslušného machine-readable registry recordu. Každá source relation používá právě jednu roli `AUTHORITY`, `SPECIALIZATION` nebo `REFERENCE`. `AUTHORITY` definuje canonical význam, `SPECIALIZATION` přidává pouze užší guards nebo evidence bez oslabení canonical pravidla a `REFERENCE` nepřidává nový význam. Duplicitní text mimo canonical source musí mít explicitní relation k canonical requirement/record ID a jeho normalizovaný význam a digest musí odpovídat deklarované roli.

### 55.3 Normativní atom, příklady a přípustná implementační variabilita

Každý deklarativní odstavec, odrážka, očíslovaný krok, tabulkový řádek, enum member, schema field, transition edge, SQL/JSON contract line a explicitní zákaz v tomto SSOT je normativní atom. Heading, čistě navigační text a lidská ukázková hodnota nejsou samostatným atomem, ale zůstávají kontextem atomu, který vysvětlují.

V contract ukázkách jsou normativní názvy fields, required/optional/null vztahy, types, enum literals, strukturální vazby a omezení výslovně určená okolním textem. Ilustrativní UUID, timestamp, digest, hostname, číslo, text nebo payload value nejsou exact požadovanou runtime hodnotou, pokud je okolní text neoznačí jako konstantu. Compiler nesmí lidský příklad rozšířit na nový požadavek ani naopak vynechat schema constraint proto, že je zapsaný v code blocku.

Formulace `například`, `zejména`, `nejméně`, `může`, `MAY`, `SHOULD`, `podle contractu`, `SYSTEM_MANAGED` nebo odkaz na ekvivalentní implementaci nikdy nepovoluje změnit:

- canonical state nebo edge,
- precondition, guard, linearization point nebo terminalitu,
- persistence a transaction boundary,
- idempotency, ordering, lease, fence, epoch, generation nebo revision semantics,
- possible-effect trigger, retry, reconciliation, cancellation nebo cleanup,
- trusted/untrusted boundary, exact binding nebo Secret-use destination,
- protocol field, code, digest, compatibility nebo observable response,
- audit, evidence, parity, acceptance nebo closure predicate.

Taková formulace je přípustná pouze pro technické provedení, jehož varianty jsou observationally equivalent podle všech registry predicates. Každá povolená varianta musí mít stejný operation contract, stejné requirement coverage a stejné blocking tests. `Equivalent` znamená shodu všech machine-readable preconditions, outputs, state transitions, error/retry directives, persistence effects, security boundaries a acceptance evidence; podobný účel, stejný happy path nebo stejný text response nestačí.

`SYSTEM_MANAGED` neznamená rozhodnutí ponechané runtime nebo architektovi. Znamená deterministic versioned policy record podle 41.10 a 55.4, jehož effective value, inputs, bounds, derivation, fallback a evidence jsou persistované a testovatelné.

### 55.4 Machine-readable Contract Pack — společný formát

Machine-readable Contract Pack je deterministická projekce tohoto SSOT a nesmí se stát druhým produktovým zdrojem pravdy. Je uložen pod `contracts/registries/` a má root manifest:

```json
{
  "schemaVersion": "1.0",
  "ssotVersion": "2026.08.30.8",
  "ssotDigest": "sha256:<64 lowercase hex>",
  "canonicalization": "KCML-CANONICAL-JSON/1",
  "schemaBundleRef": "contracts/registry-schemas/bundle-manifest.json",
  "schemaBundleDigest": "sha256:<64 lowercase hex>",
  "generatedBy": {
    "compilerId": "kcml-contract-pack",
    "compilerVersion": "semver",
    "compilerDigest": "sha256:<64 lowercase hex>"
  },
  "registries": [
    {
      "kind": "REQUIREMENT_REGISTRY",
      "schemaRef": "contracts/registry-schemas/requirement-registry.schema.json",
      "dataRef": "contracts/registries/requirements/requirements.json",
      "recordCount": 0,
      "digest": "sha256:<64 lowercase hex>"
    }
  ],
  "packDigest": "sha256:<64 lowercase hex>"
}
```

Contract Pack používá JSON Schema 2020-12, UTF-8 bez BOM, zákaz duplicate members, canonical JSON a SHA-256 pravidla kapitol 6.3 a 51.4. Všechny `schemaRef`, `dataRef` a `schemaBundleRef` jsou repository-root-relative POSIX paths, nesmějí obsahovat `..`, symlink escape ani environment-dependent absolutní cestu a jejich exact bytes jsou zahrnuty do příslušného digestu. Registry arrays jsou deterministicky seřazené podle stable ID. `null`, empty array a absence field mají rozdílný význam; každý core field je povinný a nepoužitelná hodnota se zapisuje jako explicitní `null` pouze tehdy, když schema field null povoluje.

Každý record obsahuje společná pole:

```text
recordId
recordKind
schemaVersion
authoritySourceRefs[]
sourceRelations[]
requirementIds[]
canonicalName
canonicalDigest
lifecycle = ACTIVE|SUPERSEDED|RETIRED
supersedes[]
supersededBy[]
extensions{}
```

`authoritySourceRefs` používají stable URI `ssot://<chapter>/<section>/<semantic-anchor>`, nikoli line number. `semantic-anchor` je deterministický slug explicitního numbered headingu a ordinal normativního atomu v parsed Markdown AST; změna line wrappingu jej nemění. `sourceRelations` obsahují `sourceRef`, `relationKind = AUTHORITY|SPECIALIZATION|REFERENCE`, `canonicalRecordId`, `canonicalRequirementIds[]` a `relationDigest`. Každý active record má nejméně jednu `AUTHORITY` relation; `SPECIALIZATION` a `REFERENCE` bez existujícího canonical targetu jsou invalidní. `extensions` smí obsahovat pouze namespaced doplňující evidence; nesmí změnit význam core fields. Dva records stejného kind nesmějí mít stejný stable ID s jiným canonical digestem.

Contract compiler musí být pure nad exact SSOT bytes a exact pinned schema bundlem uvedeným v `schemaBundleDigest`. Schema bundle je pouze validační encoding core fields, enums a referenčních pravidel této kapitoly; každý jeho normativní člen má requirement relation a nesmí přidat, odstranit ani přepsat produktovou semantics. Rozpor schema bundle versus SSOT je vždy `CONTRACT_PACK_DRIFT` ve prospěch SSOT. Síť, model, process time, unordered filesystem listing ani runtime database state nesmějí ovlivnit výsledek. Dvě kompilace stejných vstupů musí vytvořit byte-identický pack digest. `compilerVersion` je exact SemVer release compileru a `compilerDigest` digest jeho immutable executable artifactu; text `semver` v manifest shape označuje typ fieldu, nikoli wire literal. `recordCount` je exact počet records daného registry. `packDigest` se počítá nad canonical manifestem s tímto fieldem nastaveným na `null` a nad ordered registry digests; poté se hodnota doplní bez dalšího přepočtu.

Nesoulad SSOT a projekce je `CONTRACT_PACK_DRIFT`; pack se nesmí ručně opravit. Opraví se SSOT nebo compiler a celý pack se znovu deterministicky vygeneruje. Build, CI, generation, validation, activation, self-test a architecture handoff používají exact `ssotDigest` a `packDigest`.

### 55.5 Requirement Registry

Každý normativní atom má právě jeden stable `requirementId`. Identita přesného normativního významu je content-addressed:

```text
KCML-REQ-<DOMAIN>-<64 lowercase hex>
```

Šedesát čtyři hex znaků tvoří celý SHA-256 digest nad canonical requirement identity bytes:

```text
domain
canonical subject key
normalized normative statement
normalized table/schema/enum context
normative level
```

Canonical requirement identity bytes vznikají tímto pevným postupem:

1. input se dekóduje jako UTF-8, Unicode se normalizuje na NFC a CRLF/CR na LF,
2. Markdown se parsuje pinned CommonMark-compatible parserem deklarovaným compiler manifestem; parser version a digest jsou součástí compiler digestu,
3. trailing whitespace se odstraní a prose whitespace mimo inline/code literals se normalizuje na jeden U+0020; case, punctuation, code tokeny, quoted strings, čísla a enum literals se nemění,
4. list order, numbered-step order, table row/column identity, schema nesting a transition direction se zachovávají,
5. čistě ilustrativní values podle 55.3 se z identity statementu vyjmou a nahradí typed markerem odvozeným ze schema contextu; exact constants zůstávají beze změny,
6. domain, canonical subject key, normative level a normalized context se serializují canonical JSON pravidly 6.3 a 51.4 a teprve poté se počítá SHA-256.

Line number, line wrapping a čistě prezentační Markdown změna identity bytes nemění. Compiler nesmí provádět překlad, stemming, case folding, synonymizaci ani modelovou parafrázi. Změna observable, state, persistence, security, protocol, recovery nebo acceptance významu vytváří nový requirement ID a explicitní lineage `supersedes`; tichá změna významu pod starým ID je zakázaná. Byte-identické canonical statements stejného subjectu vytvářejí jeden requirement ID s více `authoritySourceRefs`. Dvě rozdílná statements stejného subjectu jsou conflict, pokud SSOT jedno výslovně neoznačí jako `REFERENCE` nebo `SPECIALIZATION` s přesnou vazbou na canonical requirement ID.

Requirement record obsahuje přesně tato core pole:

```text
requirementId
shortName
domain
normativeLevel = MUST|MUST_NOT|SHOULD|SHOULD_NOT|MAY|DECLARATIVE_CONTRACT
authorityRoot
ownerIntentRelation
canonicalStatement
authoritySourceRefs[]
specializationSourceRefs[]
subjectKind
subjectId
domainModule
aggregateRoots[]
stateMachineIds[]
operationIds[]
apiOperationIds[]
uiSurfaceIds[]
chatCapabilityIds[]
persistenceObjectIds[]
bindingIds[]
errorCodeIds[]
testCaseIds[]
acceptanceGateIds[]
runtimeEvidenceKinds[]
artifactIds[]
closurePredicateIds[]
status = ACTIVE|SUPERSEDED|RETIRED
supersedes[]
supersededBy[]
canonicalDigest
```

Prázdné mapování je povoleno pouze s explicitní `notApplicable` strukturou obsahující field, stable reason code a supporting requirement ID. Textová poznámka bez reason code není výjimka.

Každý requirement musí mít nejméně jeden acceptance gate a jeden evidence kind. Mutující requirement musí navíc odkazovat na operation, aggregate, state machine, persistence contract, test a closure predicate. OWNER-visible requirement musí odkazovat na exposure-parity record. Runtime/security/protocol requirement musí odkazovat na odpovídající boundary nebo wire registry.

CI musí blokovat:

- normativní atom bez requirement ID,
- requirement bez authority source,
- dva aktivní records stejného subjectu s rozporným statementem,
- orphan requirement bez implementation/test/evidence relation,
- artifact, route, UI action, state edge, migration nebo test bez requirement relation,
- stale source reference nebo digest,
- lineage cycle,
- použití retired requirementu aktivním artifactem.

### 55.6 Operation Catalog Registry

Kapitola 42 je kanonický namespace operation names. Každý její operation name má právě jeden Operation Catalog record; jiný mutující command, interní dispatch nebo OWNER action nesmí existovat mimo katalog. Event, evidence kind, KBPP input nebo provider method není operation pouze proto, že používá dotted name; jeho record používá vlastní kind a nesmí kolidovat s operation namespace.

Operation record obsahuje přesně:

```text
operationId
operationName
operationRevision
operationFamily
exposureClass
canonicalWriterId
aggregateRoot
commandSchemaRef
responseSchemaRef
expectedStates[]
expectedStateVersionPolicy
stateMachineId
allowedTransitionIds[]
idempotencyScope
idempotencyKeySource
requestDigestProfile
concurrencyScope
concurrencyKeyDerivation
concurrencyClaimPoint
deadlinePolicy
sideEffectClass
subsystemSideEffectClass
retryClass
retryDirectiveMapRef
transactionProfileId
orderedLockPlanId
fencingPolicy
checkpointPolicy
possibleEffectTrigger
reconciliationOracleId
cancellationPolicy
terminalOutcomes[]
auditEventTypes[]
outboxPurposes[]
successorPolicy
cleanupPolicy
activationRelation
apiOperationIds[]
uiActionIds[]
chatCapabilityIds[]
selfTestCaseIds[]
acceptanceGateIds[]
requirementIds[]
authoritySourceRefs[]
canonicalDigest
```

`sideEffectClass` používá pouze průřezové hodnoty kapitoly 10.12. Subsystém může zachovat užší klasifikaci v `subsystemSideEffectClass`, ale musí ji projektovat na právě jednu průřezovou hodnotu. Browser mapování je:

| Browser class | Povinná průřezová projekce |
|---|---|
| `READ_ONLY` | `READ_ONLY` |
| `MUTATION_IDEMPOTENT` | `EXTERNAL_IDEMPOTENT` |
| `MUTATION_NON_IDEMPOTENT` | `EXTERNAL_NON_IDEMPOTENT` |
| `DESTRUCTIVE` | `DESTRUCTIVE` |
| `LOCAL_INPUT` | `LOCAL_STATE_IDEMPOTENT` pouze při prokázaném zákazu external/account effectu; jinak musí revision použít mutující class |
| `AUTHENTICATION` | exact revision určí `EXTERNAL_IDEMPOTENT`, `EXTERNAL_NON_IDEMPOTENT` nebo `DESTRUCTIVE`; bez projekce je operation blocked |

Operation bez jediného exact recordu je `OPERATION_CONTRACT_INCOMPLETE`. Default odvozený pouze z HTTP method, názvu operation, workeru nebo SDK chování je zakázaný.

OWNER-level `component.enable` a `component.disable` jsou business commands. Vytvářejí a vlastní jednu logical operation a podle ní interně dispatchují `component.control.enable` nebo `component.control.disable`; interní control operation není druhý business outcome ani paralelní state machine. `component.control.ack`, `component.heartbeat`, `component.state.query` a `component.state.report` jsou interní protocol operations s exposure class podle 55.18.

### 55.7 State Machine Registry

Každý mutable aggregate, run, task, action, lease, pointer set, cleanup a recovery root má právě jeden State Machine record:

```text
stateMachineId
aggregateKind
aggregateRootTable
canonicalWriterId
initialStates[]
states[]
terminalStates[]
suspendedDecisionStates[]
recoveryStates[]
transitions[]
forbiddenTransitionPolicy = DENY_BY_DEFAULT
stateVersionField
cancellationVersionField
fencingFields[]
activationFields[]
linearizationProfile
lateEvidencePolicy
closurePredicateId
operationIds[]
requirementIds[]
acceptanceGateIds[]
authoritySourceRefs[]
canonicalDigest
```

Každý transition record obsahuje:

```text
transitionId
fromState
toState
triggerOperationId
preconditions[]
guards[]
orderedLocks[]
linearizationPoint
stateVersionIncrement
sequenceAllocations[]
eventTypes[]
auditType
outboxEffects[]
sideEffectPrecondition
cancellationRaceRule
failureOutcome
recoveryAction
```

Přechod neuvedený v `transitions` je zakázaný. Terminalita je vlastnost konkrétní state machine, nikoli globálního názvu `FAILED`, `CANCELLED`, `ACTIVE` nebo `CLOSED`. Stejný state token v různých state machines nesmí být použit k odvození stejné terminality bez registry reference.

`UNKNOWN` má vždy kvalifikovaný namespace:

- operational/monitoring `UNKNOWN` je observation projection a sama o sobě neurčuje side-effect outcome,
- side-effect `UNKNOWN` znamená neprokázaný possible effect, není success ani safe failure a root přechází do `MANUAL_REVIEW` nebo blocking recovery state,
- protocol era `UNKNOWN`/`AMBIGUOUS` blokuje binding/activation a není external-effect outcome.

Nequalified `UNKNOWN` v machine-readable contractu je neplatný.

### 55.8 PostgreSQL Contract Matrix

Každá mutující operation má právě jeden PostgreSQL Contract Matrix record:

```text
postgresContractId
operationId
transactionProfileId
isolationLevel
transactionSegments[]
orderedAdvisoryLocks[]
orderedRowLocks[]
absentRowGuard
readAfterLockGuards[]
stateVersionCas
fencingPredicate
platformIncarnationPredicate
applicationDeploymentEpochPredicate
bindingActivationPredicate
uniqueConstraints[]
checkConstraints[]
foreignKeys[]
deferredConstraintTriggers[]
idempotencyUniqueness
sequenceAllocation
outboxWrites[]
inboxWrites[]
successorReservation
successorEnqueue
externalEffectSplit
sqlstateRetryMap
migrationImplications[]
rollbackCompatibility
parallelTestIds[]
crashTestIds[]
requirementIds[]
authoritySourceRefs[]
canonicalDigest
```

`transactionSegments` používají pouze `T1`, `D`, `T2`, `T3` a explicitní read-only snapshot segment. Každý non-DB side effect vyžaduje vlastní fresh `D` a authority outbox podle 51.30. Operation record a PostgreSQL record musí odkazovat jeden na druhý a jejich digests musí být zahrnuty do release evidence.

Fyzický constraint, který lze vyjádřit PostgreSQL unique/check/composite FK/deferred constraint triggerem, nesmí být nahrazen pouze aplikační validací. Chybějící physical guard je `POSTGRES_CONTRACT_INCOMPLETE`, nikoli implementační preference.

### 55.9 Runtime Boundary Matrix

Každý runtime/process type má právě jeden Runtime Boundary record:

```text
runtimeBoundaryId
runtimeType
processIdentity
systemdUnit
osUser
osGroup
socketOrCapabilityChannels[]
peerIdentityContract
executionContextSource
allowedNetworkFamilies[]
allowedEndpoints[]
allowedFilesystemRead[]
allowedFilesystemWrite[]
allowedCredentials[]
allowedInheritedFds[]
allowedCapabilities[]
seccompProfileDigest
namespaceProfileDigest
cgroupProfile
prohibitedResources[]
lifecycleStateMachineId
runtimeGenerationPolicy
serviceGenerationPolicy
staleProcessFencing
shutdownDrainKillPolicy
childProcessPolicy
cleanupPredicateId
auditEvidence[]
testIds[]
requirementIds[]
authoritySourceRefs[]
canonicalDigest
```

`prohibitedResources` je deny-by-default a explicitně obsahuje všechny zdroje, které daný runtime nesmí použít. Generated handler record musí mít pouze anonymous capability channel k current runtime hostu a nesmí mít síť, PostgreSQL credential, Secret storage/broker path, browser-host path, host filesystem nebo OWNER API key.

Runtime matrix nesmí vytvořit bearer credential, service principal ani permission registry. Linux/systemd identity, generation, epoch a fence jsou execution evidence a write guards, nikoli přenositelné oprávnění.

### 55.10 Binding Registry

Binding Registry obsahuje exact records pro:

```text
CONTRACT_BINDING
SECRET_BINDING
EXTERNAL_TARGET_BINDING
EXTERNAL_AUTH_BINDING
AGENT_TOOL_BINDING
BROWSER_ACCOUNT_BINDING
BROWSER_PROFILE_ASSIGNMENT
ACTIVATION_SET_MEMBERSHIP
ROUTE_BINDING
```

Každý binding record obsahuje:

```text
bindingId
bindingKind
bindingRevision
bindingDigest
sourceObjectId
sourceRevisionId
targetObjectId
targetRevisionId
targetOperationOrRoute
contractSchemaDigest
secretVersionSelector
externalTargetOrOrigin
accountOrTenantConstraint
purpose
lifecycle
bindingSetRevision
activationSetId
activationEpoch
validFrom
validUntil
canonicalWriterId
requirementIds[]
authoritySourceRefs[]
canonicalDigest
```

Nepoužitelné fields jsou explicitně `null`. Wildcard source, inherited grant, name-only tool target, all-secrets binding, implicit route reuse a binding bez activation relation jsou zakázané. Každý runtime dispatch pinne exact binding record a každý binding change vzniká canonical binding/activation operation.

### 55.10.1 Error and Retry Registry

Error and Retry Registry je jedinou machine-readable projekcí stable catalogu kapitoly 32. Každý stable error code má právě jeden active record; žádná doménová, KCIP, MCP, OpenAI, browserová, runtime, testovací ani architecture vrstva nesmí zavést stable code pouze ve zdrojovém kódu.

Error record obsahuje:

```text
errorCodeId
code
namespace
classification
canonicalMeaning
sideEffectPoint = PRE_ADMISSION|PRE_DISPATCH|POSSIBLE_EFFECT|POST_COMMIT|EVIDENCE_ONLY
terminalityRule
recoveryRuleKind = FIXED|EVIDENCE_DECISION_TABLE
fixedRetryDirective
recoveryDecisionTableId
sameLogicalOperationRequired
refreshRequired
reconciliationRequired
manualReviewRequired
requiredSnapshotFields[]
affectedObjectKinds[]
httpMappings[]
kcipMappings[]
mcpMappings[]
uiMessageKey
canonicalOwnerActions[]
auditEvidenceKinds[]
testCaseIds[]
requirementIds[]
authoritySourceRefs[]
canonicalDigest
```

`fixedRetryDirective` je non-null pouze pro `FIXED`; `EVIDENCE_DECISION_TABLE` odkazuje na exact ordered predicates a outcomes, které končí právě jednou directive kapitoly 32.6. HTTP status, exception class, SDK hint ani boolean `retryable` nesmějí vytvořit druhou recovery cestu. Stejný code nemůže mít v různých subsystémech jiný canonical význam; wire mapping smí měnit pouze transportní reprezentaci.

Unknown stable code, code bez kapitoly 32, dvě active meanings, chybějící directive/decision table nebo mapping měnící side-effect semantics jsou `ERROR_RECOVERY_CONTRACT_INCOMPLETE` a blokují Contract Pack, build, activation i architecture readiness.

### 55.11 Fault Catalog

Fault Catalog je projekcí kapitoly 54.6. Každý applicable critical cutpoint má stable name `<subsystem>.<operation>.<phase>.<before|after>` a record:

```text
faultPointId
faultPointName
operationId
stateMachineId
phase
side
sourceLocation
possibleEffectClass
applicableFaultKinds[]
expectedRecoveryOracleId
expectedCanonicalOutcomeClasses[]
requiredDirectStateQueries[]
requiredCleanupChecks[]
testCaseIds[]
coverageStatus
requirementIds[]
authoritySourceRefs[]
canonicalDigest
```

Přesun hooku přes DB commit, possible-effect trigger, pointer switch, terminal commit nebo cleanup boundary vytváří nový fault-point ID. Rename bez semantic změny používá explicitní alias lineage; test schedule nesmí být potichu přesměrován.

### 55.12 Recovery Oracle Registry

Každý recovery-relevantní state machine a operation má oracle record:

```text
recoveryOracleId
subjectId
observedAuthoritativeStateSchema
requiredEvidence[]
forbiddenEvidenceAssumptions[]
rules[]
defaultOutcome
manualReviewSchemaRef
conflictingOperationBlockKeys[]
closurePredicateId
testCaseIds[]
requirementIds[]
authoritySourceRefs[]
canonicalDigest
```

Každé `rules` entry obsahuje:

```text
priority
predicate
allowedAction = REPLAY_TERMINAL|RESUME_FROM_CHECKPOINT|RETRY_SAME_OPERATION|RECONCILE|CANCEL_BEFORE_DISPATCH|RUN_COMPENSATION|RUN_CLEANUP|MANUAL_REVIEW
canonicalOutcome
retryDirective
stateTransitionId
requiredFencingGuards[]
evidenceToPersist[]
```

Predikáty se musí vzájemně vylučovat nebo vést ke stejnému action/outcome. Dva rozdílné automatické actions pro stejný observed state jsou `RECOVERY_ORACLE_CONFLICT`. Process memory, chybějící log, timeout text, HTTP exception nebo smrt workeru nejsou authoritative evidence.

### 55.13 Closure Predicate Registry

Každý terminal root má machine-checkable closure predicate record:

```text
closurePredicateId
rootKind
terminalStates[]
requiredChildPredicates[]
forbiddenPendingChildKinds[]
forbiddenProvisionalKinds[]
leaseAndFencePredicate
sideEffectPredicate
pointerAndEpochPredicate
runtimeProcessPredicate
bindingPredicate
queueOutboxInboxPredicate
artifactFilesystemPredicate
cleanupPredicate
auditEvidencePredicate
manualReviewPredicate
directQueryIds[]
passExpression
failureCode = TERMINAL_CLOSURE_INCOMPLETE
requirementIds[]
authoritySourceRefs[]
canonicalDigest
```

`passExpression` je boolean expression nad authoritative DB, runtime/process, filesystem a external read-back evidence. Terminal business state bez closure PASS zůstává business outcome evidence, ale nesmí být publikován jako úplně uzavřený `COMPLETED`, promoted release, final cleanup nebo project closure.

Known applied external effect se po cleanup failure nepřepisuje na failure nebo cancellation; cleanup failure blokuje closure a conflicting resource reuse. Unknown effect blokuje closure i conflicting key do reconciliation nebo exact OWNER resolution.

### 55.14 Acceptance Gate Registry

Každý blocking i non-blocking gate má record:

```text
gateId
gateKind
blocking
subjectScope
inputs[]
inputDigests[]
evaluatorId
evaluatorVersion
evaluatorDigest
environmentProfile
requiredEvidence[]
dependencies[]
passPredicate
failCodes[]
notApplicablePredicate
resultSchemaRef
requirementIds[]
authoritySourceRefs[]
canonicalDigest
```

Gate result obsahuje:

```text
gateId
subjectId
status = PASS|FAIL|BLOCKED|NOT_APPLICABLE
inputDigest
evaluatorDigest
environmentDigest
actualEvidence[]
missingEvidence[]
resultDigest
correlationId
startedAt
completedAt
```

Blocking gate je splněn pouze `PASS`. `NOT_APPLICABLE` je přípustné pouze při pravdivém machine predicate z gate recordu. `SKIPPED`, manual override, modelové tvrzení, existence souboru nebo neověřený screenshot není PASS.

### 55.15 Authority Ownership Registry a single-writer pravidlo

Každý authoritative mutable object, pointer, ledger nebo projection má právě jeden `canonicalWriterId` a jednu canonical state machine. Více procesů smí vykonávat tentýž writer code pod DB locks, leases a fences; nesmějí existovat dvě různé write semantics stejného objektu.

Authority Ownership record obsahuje:

```text
authorityObjectKind
canonicalWriterId
ownerModule
ownerServiceOrWorker
stateMachineId
allowedOperationIds[]
authoritativePersistence[]
acceptedEvidenceProducers[]
prohibitedDirectWriters[]
projectionConsumers[]
closurePredicateId
requirementIds[]
authoritySourceRefs[]
canonicalDigest
```

Kanonická ownership mapa je:

| Authoritative objekt | Canonical writer / state-machine owner | Jediná write cesta |
|---|---|---|
| fixed OWNER identity, password sync, MFA, sessions | Identity domain v `apps/server` | auth a session operations přes public gateway a guarded PostgreSQL transakce |
| singleton `KCML_OWNER_API_KEY` | Identity/Secret credential state machine | `ownerApiKey.rotate` a special read/reveal/session operations; obecné Secret CRUD nesmí pointer změnit |
| component identity, lifecycle a metadata | Component Registry domain | `component.*` business operations přes canonical command handler |
| component operational/monitoring/recertification projection | Monitoring projection owner | validní current-lineage observations; producer observation není přímý writer |
| immutable component/MCP/agent/automation revisions | příslušný registry domain | publish/create-revision operation; row je poté immutable |
| releases a release lifecycle | Release domain | build/validate/promote/rollback state machine |
| activation set membership a lifecycle | Generation activation domain | generation activation operations a 49.17 state machine |
| active revision/release/binding/route/runtime pointers | Activation pointer owner | jediná `ACTIVATION_SWITCH` nebo jednoprvkový activation-set transakce |
| binding-set revision a exact contract bindings | Binding domain | canonical bind/unbind/publish plus activation relation |
| Secret item, versions a active version | Secret domain v `apps/server` | Secret operations; active pointer pod 51.20 |
| Secret resolution evidence | Secret Broker | `secret.resolve` pod exact operation context; consumer hodnotu nepersistuje jako binding authority |
| external targets, auth bindings a circuit state | External Integration domain | external-target/circuit canonical operations |
| generation job, turn, source, blocker, phase a plan | Generation Orchestrator | `kcml-generation-coordinator` vykonává shared state machine; phase workers vracejí evidence |
| generation workspace revision a artifact manifest | Generation Workspace domain | atomic CAS patch/publish operations; filesystem je materializace |
| integration saga | Generation Integration domain | integration step state machine a side-effect ledger |
| validation/conformance gate result | Validation evaluator | server evaluator s input/evaluator/result digestem |
| OpenAI request descriptor, model call, provider attempt a canonical output | `packages/openai-runtime` / OpenAIClientService | jediný OpenAI runtime path; SDK/provider vrací evidence, nikoli domain write |
| agent run, tool call, handoff, approval, checkpoint, session a memory | Agent Runtime domain | `agent.*` operations vykonané `kcml-agent-worker` pod shared state machines |
| MCP transport reservation, call, MRTR, task, subscription a state handle | MCP Runtime domain | `mcp.*` operations přes MCP/KCIP gateway |
| browser runtime build and host slot registry | BrowserSessionService | browser build/host operations; host report je evidence |
| browser session, context assignment, page/frame/document projection | BrowserSessionService | `browser.session.*` a current fenced observations |
| browser control lease, epoch a fence | BrowserSessionService control state machine | `browser.control.*`; preview/browser process není writer |
| browser action root, dispatch phase, outcome a reconciliation | Browser action state machine | `browser.action.*`; host/bridge smí pouze emitovat fenced typed evidence |
| browser account binding, auth epoch, state-bundle pointer a profile assignment | BrowserSessionService account/state state machines | browser account/state/profile operations |
| automation definition/revision/run/schedule | Browser Automation domain | browser automation/schedule operations přes stejný Browser Interaction Plane |
| side-effect operation, attempt, dispatch authority a outcome ledger | Side-effect state machine v shared domain module | intent/outbox/D/outcome/reconciliation transakce; adapter pouze evidence producer |
| queue item, outbox, inbox, concurrency claim a lease | Queue/Work domain | canonical PostgreSQL claim/heartbeat/takeover/terminal operations |
| domain event sequence a audit chain | aggregate writer + Audit append contract | domain event/audit/outbox ve stejné transakci; archiver nemění canonical event |
| alert episode a delivery state | Monitoring/Alert domain | monitor/alert operations; primary/backup delivery workers sdílejí jeden state machine |
| repair job | Repair/Generation domain | `monitor.repair.enqueue` a generation repair workflow |
| runtime instance, runtime generation, execution context a launch state | Runtime Gateway domain | runtime operations; systemd/host evidence se potvrzuje guarded write |
| application deployment run, current release a deployment epoch | Deployment state machine | deploy/rollback guarded operations; symlink/systemd jsou external evidence |
| platform incarnation a recovery barrier | Platform Recovery domain | restore/startup recovery state machine; žádný worker jej nepřepisuje |
| cleanup root, steps a completeness | Cleanup state machine vlastnící parent resource | canonical cleanup operation; jednotlivé subsystémy provádějí pouze přidělený step |
| configuration revision, desired/effective values | Configuration domain | canonical config operation a per-service apply evidence |
| requirement/operation/state/contract registries | Contract Pack compiler | deterministic projection z SSOT; ruční write zakázaný |

Jakýkoli subsystém může o změnu požádat pouze voláním povolené canonical operation. Přímé SQL, ruční změna pointeru, test helper, deployment script, provider callback, browser event, model output nebo generated handler nesmí obejít canonical writer. Migrations smí měnit schema a transformovat data pouze podle migration contractu; nesmějí vytvářet nový business outcome mimo příslušnou state machine.

### 55.16 Architecture handoff a repository ownership

Software Architect dostává následující jednoznačné bounded-context a ownership hranice; implementace je smí fyzicky seskupit pouze tehdy, když zůstanou zachovány identity, persistence, writer a dependency boundaries:

| Context | Implementační domov | Vlastněná autorita |
|---|---|---|
| Public OWNER/API/Auth | `apps/server`, `packages/domain` | public authentication, OWNER commands/queries, session, action dispatch admission |
| Component Registry, Release, Binding, Activation | `packages/domain`, `apps/server`, `apps/generation-activation-worker` | component/revision/release/binding/activation state machines |
| KCIP/MCP | `packages/kcip`, `packages/mcp-runtime`, `apps/server`, `apps/runtime-gateway` | wire normalization, request reservation, calls/tasks/MRTR/subscriptions |
| Trusted Runtime | `apps/runtime-gateway`, `apps/runtime-host`, `packages/runtime-boundary`, `packages/runtime-capability-ipc` | execution context, runtime instance/generation, IPC and sandbox boundary |
| Secret | `apps/secret-broker`, `apps/server` | Secret lifecycle, active pointer, exact resolution and audit |
| External/Egress | `apps/egress-gateway`, `apps/server` | external target/auth binding, dispatch evidence and circuit state |
| OpenAI | `packages/openai-runtime`, AI-using workers | every direct `openai`/`@openai/agents` call, provider attempts and normalization |
| Agent Runtime | `apps/agent-worker`, `packages/agent-sdk`, `packages/domain` | agent runs, tools, handoffs, approvals, sessions and memory |
| Generation | coordinator and generation workers, `packages/generation-orchestrator`, `packages/generation-workspace` | jobs, turns, phases, workspaces, integration, validation and activation |
| Browser Interaction | BrowserSessionService in `apps/server`, browser worker/host/bridge apps and browser packages | sessions, control, actions, account/state, automation and bridge assignment |
| Monitoring/Alert/Repair | monitor and alert workers, `packages/observability` | observations, projections, alert episodes, delivery and repair enqueue |
| Audit | aggregate writers, `apps/audit-archiver`, `packages/observability` | canonical audit append/hash; archive is delivery projection |
| Test/Acceptance | `apps/self-test-runtime`, `apps/acceptance-runner`, `packages/testing`, production boundaries | test scheduling/evidence; žádný test-only writer |
| Contract Pack | `packages/contract-pack`, `contracts/registry-schemas`, `contracts/registries` | deterministic registry projections and architecture gates |
| Deployment/Recovery | `deploy`, deployment/recovery domain in `packages/domain` | release deploy, epoch, restore barrier, rollback and closure evidence |

Dependency směr je acyclic na authority úrovni: adapters a workers závisí na domain contracts; domain contracts nezávisí na UI, provider SDK, Playwright, systemd output ani test harnessu. `packages/database` realizuje physical repositories a migrations, ale nevlastní druhou business state machine. Shared library nesmí založit skrytý scheduler, queue, permission lookup, pointer manager nebo control plane.

OpenAI přístup vede výhradně přes `packages/openai-runtime`. Generated handler vede výhradně přes runtime capability channel a runtime gateway. Browser actions vedou výhradně přes BrowserSessionService, browser worker a typed host/bridge protocol; paralelní raw Playwright/CDP stack není přípustný. Testy používají produkční API, gateways, queues, brokers, state machines a persistence boundaries.

### 55.17 Jednotná failure, retry, cancellation a recovery semantics

Kanonická failure semantics je:

| Failure druh | Autoritativní klasifikace | Povinná recovery |
|---|---|---|
| validation/auth/binding/protocol chyba před T1 nebo dispatch intentem | known not executed | opravit/refreshnout vstup; nový command pouze podle directive |
| timeout/disconnect/cancel před durable dispatch intentem | known no effect | cancel/retry stejné operation podle idempotency a deadline |
| timeout/disconnect po intentu, před dispatch claimem | intent existuje, effect nebyl autorizován | cancel intent nebo claimnout existující intent; nevytvářet druhý intent |
| failure po `DISPATCHING` nebo possible-effect triggeru | possible effect | `RECONCILE_THEN_RETRY`; bez known read-back `MANUAL_REVIEW` |
| transport ztracený po terminal DB commitu | canonical outcome je beze změny | replay terminal outcome; žádný nový effect |
| cancellation po possible effectu | cancellation intent, nikoli rollback | reconcile; `CANCELLED` pouze při known outcomes a required cleanup |
| worker/process/service restart | process memory ztracená, DB autorita zůstává | recovery barrier podle rozsahu, lease takeover s vyšším fence, resume stejné logical operation |
| PostgreSQL restart nebo restore | žádná domněnka o in-flight effects | startup recovery barrier; po restore nová platform incarnation; reconcile před admission |
| stale lease/fence/incarnation/deployment epoch | write authority neplatná | reject write/dispatch; fresh claim nebo nový attempt podle existing logical operation |
| stale revision/binding/activation epoch | command snapshot neplatný | `REFRESH_AND_RETRY_NEW_COMMAND`; původní command se nemění |
| duplicate delivery/request | stejný key+digest je replay; jiný digest conflict | vrátit canonical current/terminal outcome nebo conflict |
| reorder/sequence gap | podle declared sequence policy | reject stale/gap; žádný implicitní reorder nebo dopočet |
| partial external effect | possible effect | target idempotency lookup nebo independent read-back; `UNKNOWN` → `MANUAL_REVIEW` |
| provider submit ambiguity | possible provider response | se známým `response.id` retrieve/resume; bez handle `MODEL_SUBMIT_OUTCOME_UNKNOWN`, žádný blind create |
| browser crash/disconnect po mutation triggeru | possible browser/account effect | fresh identity inventory a independent read-back; žádný replay inputu |
| filesystem/disk failure před validated atomic publish | artifact není autoritativní | cleanup temp a retry stejné publish operation podle checkpointu |
| filesystem failure po pointer/symlink change | possible/effective publish podle inventory | reconcile DB pointer, digest, fsync/rename a process evidence |
| deployment failure | previous/candidate/effective state podle persisted intent a inventory | pokračovat nebo reverse-switch s vyšším epoch; ambiguity → `MANUAL_REVIEW` |
| cleanup failure | business outcome se nemění; closure není complete | idempotentně resume cleanup, blokovat conflicting reuse/closure |
| recovery oracle conflict | více automatických actions | žádný nový dispatch; `MANUAL_REVIEW` |

Retry directive z kapitoly 32 je autoritativní nad HTTP status, exception type, SDK retry flag, worker attempt count, UI tlačítko i boolean `retryable`. `RETRY_SAME_OPERATION` vždy zachová logical operation, business idempotency key a request digest. `REFRESH_AND_RETRY_NEW_COMMAND` vždy vytvoří nový command/key po fresh snapshotu. `RECONCILE_THEN_RETRY` zakazuje nový dispatch do terminal reconciliation decision. `MANUAL_REVIEW` zakazuje automatic retry a conflicting operations.

`FAILED` nesmí být použito jako zkratka pro neznámý external outcome. `CANCELLED` nesmí skrýt potvrzený ani neznámý effect. `UNKNOWN` nesmí být automaticky přepsáno timeoutem, restartem, heartbeat loss, provider `5xx`, browser close nebo chybějícím logem.

### 55.18 UI, API, chat, audit a self-test parity

Každá operation má právě jednu exposure class:

```text
OWNER_COMMAND
OWNER_QUERY
PUBLIC_PROTOCOL
INTERNAL_PROTOCOL
AUTOMATED_MAINTENANCE
EVIDENCE_ONLY
```

Povinné projekce jsou:

| Exposure class | Povinné surfaces |
|---|---|
| `OWNER_COMMAND` | domain operation, production API, applicable OWNER UI action, central-chat command capability, audit, self-test case a acceptance evidence |
| `OWNER_QUERY` | production API, OWNER UI view, central-chat query capability, audit/access evidence podle citlivosti a self-test |
| `PUBLIC_PROTOCOL` | public protocol/API route, protocol contract tests, OWNER diagnostic UI/chat query, audit a acceptance evidence |
| `INTERNAL_PROTOCOL` | exact internal KCIP/runtime operation, parent business-operation relation, audit/evidence, self-test a OWNER-visible diagnostic/recovery projection; přímý public mutation endpoint je zakázaný |
| `AUTOMATED_MAINTENANCE` | scheduler/monitor trigger, canonical operation, OWNER UI/API/chat inspection a manual canonical start/stop/repair action, audit a self-test |
| `EVIDENCE_ONLY` | query/API/UI/chat visibility a audit relation; žádná command route |

Internal admission ACK, heartbeat report, provider output item, browser dispatch phase, observation a domain event nejsou OWNER commands. Parita nevyžaduje veřejný endpoint, který by je falšoval; vyžaduje jejich čitelnost, diagnostiku a canonical parent action. OWNER-level command nesmí existovat pouze v UI, pouze v chatu, pouze v deployment skriptu, pouze ručním SQL nebo pouze test helperem.

Exposure Parity record obsahuje:

```text
operationId
exposureClass
apiOperationIds[]
uiSurfaceIds[]
uiActionIds[]
chatCapabilityIds[]
auditEventTypes[]
selfTestCaseIds[]
acceptanceGateIds[]
parentOperationId
notApplicableReasons[]
requirementIds[]
canonicalDigest
```

Každá chybějící povinná surface je blocking `EXPOSURE_PARITY_INCOMPLETE`. Výjimka existuje pouze jako machine-checkable `notApplicableReason` odvozený z exposure class; volný text nebo implementační pohodlí výjimku nevytváří.

### 55.19 Artifact traceability a zákaz orphan implementation

Každý produkční artifact, source file, migration, systemd unit, route, schema, UI surface, test, fixture, model, fault point a deployment script má Artifact Trace record:

```text
artifactId
artifactKind
repositoryPath
contentDigest
ownerModule
requirementIds[]
operationIds[]
stateMachineIds[]
registryRecordIds[]
testIds[]
releaseIds[]
generatedFrom[]
generationToolDigest
lifecycle
canonicalDigest
```

Artifact bez requirement relation je orphan a build fail. Requirement bez aktivního artifactu/testu/evidence relation je orphan requirement a activation fail, pokud nejde o explicitní specification-only future candidate mimo active product; current SSOT žádnou takovou future-implementation výjimku nepovoluje.

Generated artifact musí odkazovat exact source requirement/operation/registry digests. Ručně vytvořený artifact smí být validní pouze po stejné traceability registraci. Test bez requirement a oracle relation se nepočítá do coverage. Test helper, fixture nebo migration nesmí implementovat druhou doménovou cestu.

### 55.20 Contract Pack derivace a consistency gates

Contract compiler provede v jednom deterministickém běhu:

1. parse exact SSOT a ověří heading, table, code-fence a internal-reference integritu,
2. vytvoří normative atoms a Requirement Registry,
3. sestaví canonical terminology, enum, state, operation, error a retry symbol tables,
4. detekuje duplicate subject definitions a porovná canonical meanings,
5. sestaví Operation, State Machine, PostgreSQL, Runtime, Binding a Authority registries,
6. sestaví Error and Retry, Fault, Recovery Oracle, Closure Predicate a Acceptance Gate registries,
7. sestaví Exposure Parity a Artifact Trace registries,
8. ověří všechny foreign-key-like references mezi registry,
9. ověří single-writer uniqueness každého authoritative object kind,
10. ověří, že každý mutable operation má úplný lifecycle, DB contract, recovery oracle a closure predicate,
11. ověří, že každý stable error code má právě jednu classification a retry directive,
12. ověří, že každý OWNER-visible command má úplnou parity mapu,
13. vypočte registry digests, pack digest a architecture-readiness gate inputs.

Compiler musí selhat při unknown symbolu, nequalified state/error významu, chybějícím reference targetu, cycle v authority/lineage, více current writerech, mixed canonical enums, operation bez state/DB/recovery contractu, gate bez evaluatoru, closure bez direct query nebo orphan relation.

### 55.21 Architecture Readiness Gate Registry

Architecture readiness používá přesně tyto blocking gates:

| Gate ID | PASS predicate |
|---|---|
| `ARCH_CROSS_CHAPTER_CONSISTENT` | symbol tables a duplicate subject definitions nemají contradiction |
| `ARCH_NORMATIVE_AMBIGUITY_CLOSED` | žádná implementační volba nemění observable, persistence, recovery, security, protocol nebo acceptance semantics |
| `ARCH_SINGLE_WRITER_COMPLETE` | každý authoritative mutable object/pointer má právě jeden canonical writer a state machine |
| `ARCH_OPERATION_LIFECYCLE_COMPLETE` | každý mutující operation record má states, guards, linearization, terminality, cancellation, recovery a cleanup |
| `ARCH_POSTGRES_CONTRACT_COMPLETE` | každý mutující operation má complete physical PostgreSQL record a constraints |
| `ARCH_RUNTIME_BOUNDARY_COMPLETE` | každý runtime type má complete process/IPC/resource/stale-process matrix |
| `ARCH_PROTOCOL_SEMANTICS_COMPLETE` | KCIP, MCP, HTTP, SSE, PULSE, control a error mappings jsou jednoznačné |
| `ARCH_OPENAI_LIFECYCLE_COMPLETE` | každý OpenAI call site vede přes OpenAI runtime a má submit/retrieve/resume/tool/terminal recovery |
| `ARCH_BROWSER_LIFECYCLE_COMPLETE` | session/control/page/frame/document/action/account/bridge/recovery/cleanup contracts jsou complete |
| `ARCH_AGENTIC_AUTHORITY_COMPLETE` | každý side-effecting model/tool/browser/Secret path má immutable intent, lineage, context a semantic validation |
| `ARCH_FAILURE_RECOVERY_CONSISTENT` | stejné fault classes mají jedinou cross-subsystem semantics a oracle |
| `ARCH_CONTRACT_PACK_DERIVABLE` | všechny registry lze deterministicky vytvořit bez modelové nebo architektonické interpretace |
| `ARCH_TRACEABILITY_COMPLETE` | každý requirement a artifact má complete bidirectional lineage a nulové orphan records |
| `ARCH_ACCEPTANCE_MACHINE_CHECKABLE` | každý blocking gate má inputs, evaluator, evidence a PASS predicate |
| `ARCH_CLOSURE_PREDICATES_COMPLETE` | každý terminal root má machine-checkable orphan-free closure predicate |
| `ARCH_NO_OWNER_DECISION_PENDING` | neexistuje chybějící business rozhodnutí OWNERa potřebné k určení product semantics |
| `ARCH_REPOSITORY_OWNERSHIP_COMPLETE` | každý service/module/runtime/registry má jediný repository home a žádnou duplicated authority |
| `ARCH_EXPOSURE_PARITY_COMPLETE` | každý operation má validní exposure class a povinné surfaces/evidence |

Gate evaluator používá exact SSOT digest, Contract Pack digest a report všech consistency checks. Každý gate result obsahuje nulový unresolved finding inventory. Gate nelze ručně přepsat, waiverovat ani označit PASS pouze proto, že implementační architekt může později rozhodnout.

`ARCHITECTURE_READINESS` nabývá hodnoty `PASS` pouze jako konjunkce všech výše uvedených PASS výsledků. Jediný unresolved owner-business blocker vyžaduje hodnotu `BLOCKED` a exact jedno rozhodnutí; technická neúplnost se před předáním nejprve uzavírá v SSOT.

### 55.22 Kanonický normativní stav

ARCHITECTURE_READINESS = PASS
