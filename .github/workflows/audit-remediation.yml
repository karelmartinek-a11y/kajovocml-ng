name: Audit remediation batch two
on:
  push:
    branches: [audit-remediation-2026-09-06]
permissions:
  contents: write
concurrency:
  group: audit-remediation-${{ github.ref }}
  cancel-in-progress: true
jobs:
  apply:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: audit-remediation-2026-09-06
      - uses: pnpm/action-setup@v4
        with:
          version: 11.19.0
          run_install: false
      - uses: actions/setup-node@v4
        with:
          node-version: '24.11.1'
          cache: pnpm
      - name: Correct batch two source marker
        run: |
          python3 - <<'PY'
          from pathlib import Path
          path = Path('scripts/audit-remediation-batch2.py')
          text = path.read_text(encoding='utf-8')
          old = 'needle="contentDigest, context.logicalOperationId, context.correlationId, context.activationEpoch.toString(), context.platformIncarnationId, context.applicationDeploymentEpoch.toString()"'
          new = 'needle="digest(content), context.logicalOperationId, context.correlationId, context.activationEpoch.toString(), context.platformIncarnationId, context.applicationDeploymentEpoch.toString()"'
          if old not in text:
              raise SystemExit('batch two generation message needle declaration not found')
          text = text.replace(old, new, 1)
          old = 't=t.replace(needle,"contentDigest, digest({ jobId, sequence: sequence.toString(), role: context.arguments.role ?? \'OWNER\', content: context.arguments.content, attachments: context.arguments.attachments ?? [] })'
          new = 't=t.replace(needle,"digest(content), digest({ jobId, sequence: sequence.toString(), role: context.arguments.role ?? \'OWNER\', content: context.arguments.content, attachments: context.arguments.attachments ?? [] })'
          if old not in text:
              raise SystemExit('batch two generation message replacement declaration not found')
          path.write_text(text.replace(old, new, 1), encoding='utf-8')
          PY
      - name: Apply forensic batch two
        run: python3 scripts/audit-remediation-batch2.py
      - name: Refresh lockfile
        run: pnpm install --no-frozen-lockfile
      - name: Verify SSOT surface and registries
        run: node scripts/compile-ssot-surface.mjs --check && node scripts/verify-authority-ownership.mjs && node scripts/verify-error-codes.mjs
      - name: Verify lint
        run: pnpm lint
      - name: Verify types
        run: pnpm typecheck
      - name: Verify workspace build
        run: pnpm -r --sort --if-present build
      - name: Verify patch whitespace
        run: git diff --check
      - name: Commit verified batch
        run: |
          if git diff --quiet && [[ -z "$(git ls-files --others --exclude-standard)" ]]; then
            echo 'No remediation changes.'
            exit 0
          fi
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -A
          git commit -m 'fix(audit): apply verified forensic remediation batch two'
          git push origin HEAD:audit-remediation-2026-09-06
